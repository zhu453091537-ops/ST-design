# @st/platform-utils

`@st/platform-utils` is reserved for pure utilities shared by platform packages.

Current status:

1. Package boundary is reserved.
2. No runtime utility has been migrated yet.
3. Utilities here must not depend on `apps/web-antd`, browser route state, mocks, or business API modules.
