# @st/platform-types

`@st/platform-types` is reserved for shared platform TypeScript types that are independent from business pages.

Current status:

1. Package boundary is reserved.
2. Component-local types still live beside their platform components.
3. Business route, mock, and API types must stay in `apps/web-antd`.
