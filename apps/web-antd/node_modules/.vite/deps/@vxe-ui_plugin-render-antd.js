import { o as __toESM } from "./chunk-CqwQKh_b.js";
import { It as onMounted, W as computed, Xt as resolveComponent, _n as watch, mt as h, qn as ref, rt as defineComponent } from "./runtime-dom.esm-bundler-BEW9uiVc.js";
import "./vue.js";
import { t as require_xe_utils } from "./xe-utils-CectUEgD.js";
//#region ../../node_modules/.pnpm/@vxe-ui+plugin-render-antd@4.3.1/node_modules/@vxe-ui/plugin-render-antd/es/store/index.js
var globalConfig = {};
var componentMaps = {};
//#endregion
//#region ../../node_modules/.pnpm/@vxe-ui+plugin-render-antd@4.3.1/node_modules/@vxe-ui/plugin-render-antd/es/util/comp.js
function getCurrComponent(name) {
	const comp = componentMaps[name] || (globalConfig.Antd ? globalConfig.Antd[name] : null);
	if (comp) return comp;
	return resolveComponent(name);
}
//#endregion
//#region ../../node_modules/.pnpm/@vxe-ui+plugin-render-antd@4.3.1/node_modules/@vxe-ui/plugin-render-antd/es/table/index.js
var import_xe_utils = /* @__PURE__ */ __toESM(require_xe_utils());
/**
* 表格 - 渲染器
*/
function defineTableRender(VxeUI) {
	function isEmptyValue(cellValue) {
		return cellValue === null || cellValue === void 0 || cellValue === "";
	}
	function getOnName(type) {
		return "on" + type.substring(0, 1).toLocaleUpperCase() + type.substring(1);
	}
	function getModelProp(renderOpts) {
		let prop = "value";
		switch (renderOpts.name) {
			case "ASwitch":
				prop = "checked";
				break;
		}
		return prop;
	}
	function getModelEvent(renderOpts) {
		let type = "update:value";
		switch (renderOpts.name) {
			case "ASwitch":
				type = "update:checked";
				break;
		}
		return type;
	}
	function dateFormatToVxeFormat(format) {
		if (format) return `${format}`.replace("YYYY", "yyyy").replace("DD", "dd");
		return format;
	}
	function getChangeEvent(renderOpts) {
		return "change";
	}
	function getCellEditFilterProps(renderOpts, params, value, defaultProps) {
		return import_xe_utils.default.assign({}, defaultProps, renderOpts.props, { [getModelProp(renderOpts)]: value });
	}
	function formatText(cellValue) {
		return "" + (isEmptyValue(cellValue) ? "" : cellValue);
	}
	function getCellLabelVNs(renderOpts, params, cellLabel) {
		const { placeholder } = renderOpts;
		return [h("span", { class: "vxe-cell--label" }, placeholder && isEmptyValue(cellLabel) ? [h("span", { class: "vxe-cell--placeholder" }, formatText(placeholder))] : formatText(cellLabel))];
	}
	function getOns(renderOpts, params, inputFunc, changeFunc) {
		const { events } = renderOpts;
		const modelEvent = getModelEvent(renderOpts);
		const changeEvent = getChangeEvent(renderOpts);
		const isSameEvent = changeEvent === modelEvent;
		const ons = {};
		import_xe_utils.default.objectEach(events, (func, key) => {
			ons[getOnName(key)] = function(...args) {
				func(params, ...args);
			};
		});
		if (inputFunc) ons[getOnName(modelEvent)] = function(targetEvnt) {
			inputFunc(targetEvnt);
			if (events && events[modelEvent]) events[modelEvent](params, targetEvnt);
			if (isSameEvent && changeFunc) changeFunc(targetEvnt);
		};
		if (!isSameEvent && changeFunc) ons[getOnName(changeEvent)] = function(...args) {
			changeFunc(...args);
			if (events && events[changeEvent]) events[changeEvent](params, ...args);
		};
		return ons;
	}
	function getEditOns(renderOpts, params) {
		const { $table, row, column } = params;
		return getOns(renderOpts, params, (value) => {
			import_xe_utils.default.set(row, column.field, value);
		}, () => {
			$table.updateStatus(params);
		});
	}
	function getFilterOns(renderOpts, params, option, changeFunc) {
		return getOns(renderOpts, params, (value) => {
			option.data = value;
		}, changeFunc);
	}
	function matchCascaderData(index, list, values, labels) {
		const val = values[index];
		if (list && values.length > index) import_xe_utils.default.each(list, (item) => {
			if (item.value === val) {
				labels.push(item.label);
				matchCascaderData(++index, item.children, values, labels);
			}
		});
	}
	function formatDatePicker(defaultFormat) {
		return function(renderOpts, params) {
			return getCellLabelVNs(renderOpts, params, getDatePickerCellValue(renderOpts, params, defaultFormat));
		};
	}
	function formatTimePicker(defaultFormat) {
		return function(renderOpts, params) {
			const { props = {} } = renderOpts;
			const { row, column } = params;
			let cellValue = import_xe_utils.default.get(row, column.field);
			try {
				if (cellValue) {
					if (!import_xe_utils.default.isString(cellValue)) cellValue = cellValue.format ? cellValue.format(props.format || props.valueFormat || defaultFormat) : import_xe_utils.default.toDateString(cellValue, dateFormatToVxeFormat(props.format || props.valueFormat || defaultFormat));
				}
			} catch (e) {}
			return getCellLabelVNs(renderOpts, params, cellValue);
		};
	}
	function getSelectCellValue(renderOpts, params) {
		const { options = [], optionGroups, props = {}, optionProps = {}, optionGroupProps = {} } = renderOpts;
		const { row, column } = params;
		const labelProp = optionProps.label || "label";
		const valueProp = optionProps.value || "value";
		const groupOptions = optionGroupProps.options || "options";
		const cellValue = import_xe_utils.default.get(row, column.field);
		if (!isEmptyValue(cellValue)) return import_xe_utils.default.map(props.mode === "multiple" ? cellValue : [cellValue], optionGroups ? (value) => {
			let selectItem;
			for (let index = 0; index < optionGroups.length; index++) {
				selectItem = import_xe_utils.default.find(optionGroups[index][groupOptions], (item) => item[valueProp] === value);
				if (selectItem) break;
			}
			return selectItem ? selectItem[labelProp] : value;
		} : (value) => {
			const selectItem = import_xe_utils.default.find(options, (item) => item[valueProp] === value);
			return selectItem ? selectItem[labelProp] : value;
		}).join(", ");
		return "";
	}
	function getCascaderCellValue(renderOpts, params) {
		const { props = {} } = renderOpts;
		const { row, column } = params;
		const values = import_xe_utils.default.get(row, column.field) || [];
		const labels = [];
		matchCascaderData(0, props.options, values, labels);
		return (props.showAllLevels === false ? labels.slice(labels.length - 1, labels.length) : labels).join(` ${props.separator || "/"} `);
	}
	function getRangePickerCellValue(renderOpts, params) {
		const { props = {} } = renderOpts;
		const { row, column } = params;
		let cellValue = import_xe_utils.default.get(row, column.field);
		if (cellValue) cellValue = import_xe_utils.default.map(cellValue, (date) => {
			return date && date.format ? date.format(props.format || "YYYY-MM-DD") : import_xe_utils.default.toDateString(date, dateFormatToVxeFormat(props.format || "YYYY-MM-DD"));
		}).join(" ~ ");
		return cellValue;
	}
	function getTreeSelectCellValue(renderOpts, params) {
		const { props = {} } = renderOpts;
		const { treeData, treeCheckable } = props;
		const { row, column } = params;
		const cellValue = import_xe_utils.default.get(row, column.field);
		if (!isEmptyValue(cellValue)) return import_xe_utils.default.map(treeCheckable ? cellValue : [cellValue], (value) => {
			const matchObj = import_xe_utils.default.findTree(treeData, (item) => item.value === value, { children: "children" });
			return matchObj ? matchObj.item.title : value;
		}).join(", ");
		return cellValue;
	}
	function getDatePickerCellValue(renderOpts, params, defaultFormat) {
		const { props = {} } = renderOpts;
		const { row, column } = params;
		let cellValue = import_xe_utils.default.get(row, column.field);
		try {
			if (cellValue) {
				if (!defaultFormat) {
					if (renderOpts.name === "ADatePicker") switch (props.picker) {
						case "week":
							defaultFormat = "YYYY-WW周";
							break;
						case "month":
							defaultFormat = "YYYY-MM";
							break;
						case "year":
							defaultFormat = "YYYY";
							break;
						default:
							defaultFormat = props.showTime ? "YYYY-MM-DD HH:mm:ss" : "YYYY-MM-DD";
							break;
					}
				}
				cellValue = cellValue.format ? cellValue.format(props.format || defaultFormat) : import_xe_utils.default.toDateString(cellValue, dateFormatToVxeFormat(props.format || defaultFormat));
			}
		} catch (e) {}
		return cellValue;
	}
	function createEditRender(defaultProps) {
		return function(renderOpts, params) {
			const { row, column } = params;
			const { name, attrs } = renderOpts;
			const cellValue = import_xe_utils.default.get(row, column.field);
			return [h(getCurrComponent(name), Object.assign(Object.assign(Object.assign({}, attrs), getCellEditFilterProps(renderOpts, params, cellValue, defaultProps)), getEditOns(renderOpts, params)))];
		};
	}
	function defaultButtonEditRender(renderOpts, params) {
		const { attrs } = renderOpts;
		return [h(getCurrComponent("a-button"), Object.assign(Object.assign(Object.assign({}, attrs), getCellEditFilterProps(renderOpts, params, null)), getOns(renderOpts, params)), cellText(renderOpts.content))];
	}
	function defaultButtonsEditRender(renderOpts, params) {
		const { children } = renderOpts;
		if (children) return children.map((childRenderOpts) => defaultButtonEditRender(childRenderOpts, params)[0]);
		return [];
	}
	function createFilterRender(defaultProps) {
		return function(renderOpts, params) {
			const { column } = params;
			const { name, attrs } = renderOpts;
			return [h("div", { class: "vxe-table--filter-antd-wrapper" }, column.filters.map((option, oIndex) => {
				const optionValue = option.data;
				return h(getCurrComponent(name), Object.assign(Object.assign(Object.assign({ key: oIndex }, attrs), getCellEditFilterProps(renderOpts, params, optionValue, defaultProps)), getFilterOns(renderOpts, params, option, () => {
					handleConfirmFilter(params, !!option.data, option);
				})));
			}))];
		};
	}
	function handleConfirmFilter(params, checked, option) {
		const { $panel } = params;
		$panel.changeOption(null, checked, option);
	}
	/**
	* 模糊匹配
	* @param params
	*/
	function defaultFuzzyFilterMethod(params) {
		const { option, row, column } = params;
		const { data } = option;
		const cellValue = import_xe_utils.default.get(row, column.field);
		return import_xe_utils.default.toValueString(cellValue).indexOf(data) > -1;
	}
	/**
	* 精确匹配
	* @param params
	*/
	function defaultExactFilterMethod(params) {
		const { option, row, column } = params;
		const { data } = option;
		return import_xe_utils.default.get(row, column.field) === data;
	}
	function cellText(cellValue) {
		return [formatText(cellValue)];
	}
	function createDatePickerExportMethod(defaultFormat) {
		return function(params) {
			const { row, column, options } = params;
			return options && options.original ? import_xe_utils.default.get(row, column.field) : getDatePickerCellValue(column.editRender || column.cellRender, params, defaultFormat);
		};
	}
	function createExportMethod(getExportCellValue) {
		return function(params) {
			const { row, column, options } = params;
			return options && options.original ? import_xe_utils.default.get(row, column.field) : getExportCellValue(column.editRender || column.cellRender, params);
		};
	}
	VxeUI.renderer.mixin({
		AAutoComplete: {
			tableAutoFocus: "input",
			renderTableDefault: createEditRender(),
			renderTableEdit: createEditRender(),
			renderTableFilter: createFilterRender(),
			tableFilterDefaultMethod: defaultExactFilterMethod
		},
		AInput: {
			tableAutoFocus: "input",
			renderTableDefault: createEditRender(),
			renderTableEdit: createEditRender(),
			renderTableFilter: createFilterRender(),
			tableFilterDefaultMethod: defaultFuzzyFilterMethod
		},
		AInputNumber: {
			tableAutoFocus: "input",
			renderTableDefault: createEditRender(),
			renderTableEdit: createEditRender(),
			renderTableFilter: createFilterRender(),
			tableFilterDefaultMethod: defaultFuzzyFilterMethod
		},
		ASelect: {
			renderTableEdit(renderOpts, params) {
				const { options, optionGroups } = renderOpts;
				const { row, column } = params;
				const { attrs } = renderOpts;
				const props = getCellEditFilterProps(renderOpts, params, import_xe_utils.default.get(row, column.field));
				const ons = getEditOns(renderOpts, params);
				if (optionGroups) return [h(getCurrComponent("a-select"), Object.assign(Object.assign(Object.assign(Object.assign({}, props), attrs), { options: optionGroups }), ons))];
				return [h(getCurrComponent("a-select"), Object.assign(Object.assign(Object.assign(Object.assign({}, props), attrs), { options: props.options || options }), ons))];
			},
			renderTableCell(renderOpts, params) {
				return getCellLabelVNs(renderOpts, params, getSelectCellValue(renderOpts, params));
			},
			renderTableFilter(renderOpts, params) {
				const { options = [], optionGroups, optionGroupProps = {} } = renderOpts;
				const groupOptions = optionGroupProps.options || "options";
				const { column } = params;
				const { attrs } = renderOpts;
				return [h("div", { class: "vxe-table--filter-antd-wrapper" }, optionGroups ? column.filters.map((option, oIndex) => {
					const optionValue = option.data;
					const props = getCellEditFilterProps(renderOpts, params, optionValue);
					return h(getCurrComponent("a-select"), Object.assign(Object.assign(Object.assign(Object.assign({ key: oIndex }, attrs), props), { options: groupOptions }), getFilterOns(renderOpts, params, option, () => {
						handleConfirmFilter(params, props.mode === "multiple" ? option.data && option.data.length > 0 : !import_xe_utils.default.eqNull(option.data), option);
					})));
				}) : column.filters.map((option, oIndex) => {
					const optionValue = option.data;
					const props = getCellEditFilterProps(renderOpts, params, optionValue);
					return h(getCurrComponent("a-select"), Object.assign(Object.assign(Object.assign(Object.assign({ key: oIndex }, attrs), props), { options: props.options || options }), getFilterOns(renderOpts, params, option, () => {
						handleConfirmFilter(params, props.mode === "multiple" ? option.data && option.data.length > 0 : !import_xe_utils.default.eqNull(option.data), option);
					})));
				}))];
			},
			tableFilterDefaultMethod(params) {
				const { option, row, column } = params;
				const { data } = option;
				const { field, filterRender: renderOpts } = column;
				const { props = {} } = renderOpts;
				const cellValue = import_xe_utils.default.get(row, field);
				if (props.mode === "multiple") {
					if (import_xe_utils.default.isArray(cellValue)) return import_xe_utils.default.includeArrays(cellValue, data);
					return data.indexOf(cellValue) > -1;
				}
				return cellValue == data;
			},
			tableExportMethod: createExportMethod(getSelectCellValue)
		},
		ACascader: {
			renderTableEdit: createEditRender(),
			renderTableCell(renderOpts, params) {
				return getCellLabelVNs(renderOpts, params, getCascaderCellValue(renderOpts, params));
			},
			tableExportMethod: createExportMethod(getCascaderCellValue)
		},
		ADatePicker: {
			renderTableEdit: createEditRender(),
			renderTableCell: formatDatePicker(),
			tableExportMethod: createDatePickerExportMethod()
		},
		AMonthPicker: {
			renderTableEdit: createEditRender(),
			renderTableCell: formatDatePicker("YYYY-MM"),
			tableExportMethod: createDatePickerExportMethod("YYYY-MM")
		},
		ARangePicker: {
			renderTableEdit: createEditRender(),
			renderTableCell(renderOpts, params) {
				return getCellLabelVNs(renderOpts, params, getRangePickerCellValue(renderOpts, params));
			},
			tableExportMethod: createExportMethod(getRangePickerCellValue)
		},
		AWeekPicker: {
			renderTableEdit: createEditRender(),
			renderTableCell: formatDatePicker("YYYY-WW周"),
			tableExportMethod: createDatePickerExportMethod("YYYY-WW周")
		},
		ATimePicker: {
			renderTableEdit: createEditRender(),
			renderTableCell: formatTimePicker("HH:mm:ss"),
			tableExportMethod: createDatePickerExportMethod("HH:mm:ss")
		},
		ATreeSelect: {
			renderTableEdit: createEditRender(),
			renderTableCell(renderOpts, params) {
				return getCellLabelVNs(renderOpts, params, getTreeSelectCellValue(renderOpts, params));
			},
			tableExportMethod: createExportMethod(getTreeSelectCellValue)
		},
		ARate: {
			renderTableDefault: createEditRender(),
			renderTableEdit: createEditRender(),
			renderTableFilter: createFilterRender(),
			tableFilterDefaultMethod: defaultExactFilterMethod
		},
		ASwitch: {
			renderTableDefault: createEditRender(),
			renderTableEdit: createEditRender(),
			renderTableFilter(renderOpts, params) {
				const { column } = params;
				const { name, attrs } = renderOpts;
				return [h("div", { class: "vxe-table--filter-antd-wrapper" }, column.filters.map((option, oIndex) => {
					const optionValue = option.data;
					return h(name, Object.assign(Object.assign(Object.assign({ key: oIndex }, attrs), getCellEditFilterProps(renderOpts, params, optionValue)), getFilterOns(renderOpts, params, option, () => {
						handleConfirmFilter(params, import_xe_utils.default.isBoolean(option.data), option);
					})));
				}))];
			},
			tableFilterDefaultMethod: defaultExactFilterMethod
		},
		AButton: {
			renderTableEdit: defaultButtonEditRender,
			renderTableDefault: defaultButtonEditRender
		},
		AButtons: {
			renderTableEdit: defaultButtonsEditRender,
			renderTableDefault: defaultButtonsEditRender
		}
	});
}
//#endregion
//#region ../../node_modules/.pnpm/@vxe-ui+plugin-render-antd@4.3.1/node_modules/@vxe-ui/plugin-render-antd/es/form/index.js
/**
* 表单 - 渲染器
*/
function defineFormRender(VxeUI) {
	function isEmptyValue(cellValue) {
		return cellValue === null || cellValue === void 0 || cellValue === "";
	}
	function getOnName(type) {
		return "on" + type.substring(0, 1).toLocaleUpperCase() + type.substring(1);
	}
	function getModelProp(renderOpts) {
		let prop = "value";
		switch (renderOpts.name) {
			case "ASwitch":
				prop = "checked";
				break;
		}
		return prop;
	}
	function getModelEvent(renderOpts) {
		let type = "update:value";
		switch (renderOpts.name) {
			case "ASwitch":
				type = "update:checked";
				break;
		}
		return type;
	}
	function getChangeEvent(renderOpts) {
		return "change";
	}
	function getItemProps(renderOpts, params, value, defaultProps) {
		return import_xe_utils.default.assign({}, defaultProps, renderOpts.props, { [getModelProp(renderOpts)]: value });
	}
	function formatText(cellValue) {
		return "" + (isEmptyValue(cellValue) ? "" : cellValue);
	}
	function getOns(renderOpts, params, inputFunc, changeFunc) {
		const { events } = renderOpts;
		const modelEvent = getModelEvent(renderOpts);
		const changeEvent = getChangeEvent(renderOpts);
		const isSameEvent = changeEvent === modelEvent;
		const ons = {};
		import_xe_utils.default.objectEach(events, (func, key) => {
			ons[getOnName(key)] = function(...args) {
				func(params, ...args);
			};
		});
		if (inputFunc) ons[getOnName(modelEvent)] = function(targetEvnt) {
			inputFunc(targetEvnt);
			if (events && events[modelEvent]) events[modelEvent](params, targetEvnt);
			if (isSameEvent && changeFunc) changeFunc(targetEvnt);
		};
		if (!isSameEvent && changeFunc) ons[getOnName(changeEvent)] = function(...args) {
			changeFunc(...args);
			if (events && events[changeEvent]) events[changeEvent](params, ...args);
		};
		return ons;
	}
	function getItemOns(renderOpts, params) {
		const { $form, data, field } = params;
		return getOns(renderOpts, params, (value) => {
			import_xe_utils.default.set(data, field, value);
		}, () => {
			$form.updateStatus(params);
			if (renderOpts.changeToSubmit) $form.handleSubmitEvent(new Event("change"));
		});
	}
	function cellText(cellValue) {
		return [formatText(cellValue)];
	}
	function createFormItemRender(defaultProps) {
		return function(renderOpts, params) {
			const { data, field } = params;
			const { name } = renderOpts;
			const { attrs } = renderOpts;
			const itemValue = import_xe_utils.default.get(data, field);
			return [h(getCurrComponent(name), Object.assign(Object.assign(Object.assign({}, attrs), getItemProps(renderOpts, params, itemValue, defaultProps)), getItemOns(renderOpts, params)))];
		};
	}
	function defaultButtonItemRender(renderOpts, params) {
		const { attrs } = renderOpts;
		const props = getItemProps(renderOpts, params, null);
		return [h(getCurrComponent("a-button"), Object.assign(Object.assign(Object.assign({}, attrs), props), getItemOns(renderOpts, params)), { default: () => cellText(renderOpts.content || props.content) })];
	}
	function defaultButtonsItemRender(renderOpts, params) {
		const { children } = renderOpts;
		if (children) return children.map((childRenderOpts) => defaultButtonItemRender(childRenderOpts, params)[0]);
		return [];
	}
	/**
	*
	* 已废弃
	* @deprecated
	*/
	function createOldFormItemRadioAndCheckboxRender() {
		return function(renderOpts, params) {
			const { name, options = [], optionProps = {} } = renderOpts;
			const { data, field } = params;
			const { attrs } = renderOpts;
			const labelProp = optionProps.label || "label";
			const valueProp = optionProps.value || "value";
			const itemValue = import_xe_utils.default.get(data, field);
			return [h(getCurrComponent(`${name}Group`), Object.assign(Object.assign(Object.assign({}, attrs), getItemProps(renderOpts, params, itemValue)), getItemOns(renderOpts, params)), { default: () => {
				return options.map((option, oIndex) => {
					return h(getCurrComponent(name), {
						key: oIndex,
						value: option[valueProp],
						disabled: option.disabled
					}, { default: () => cellText(option[labelProp]) });
				});
			} })];
		};
	}
	VxeUI.renderer.mixin({
		AAutoComplete: { renderFormItemContent: createFormItemRender() },
		AInput: { renderFormItemContent: createFormItemRender() },
		AInputNumber: { renderFormItemContent: createFormItemRender() },
		ASelect: { renderFormItemContent(renderOpts, params) {
			const { options = [], optionGroups } = renderOpts;
			const { data, field } = params;
			const { attrs } = renderOpts;
			const props = getItemProps(renderOpts, params, import_xe_utils.default.get(data, field));
			const ons = getItemOns(renderOpts, params);
			if (optionGroups) return [h(getCurrComponent("a-select"), Object.assign(Object.assign(Object.assign(Object.assign({}, attrs), props), { options: optionGroups }), ons))];
			return [h(getCurrComponent("a-select"), Object.assign(Object.assign(Object.assign(Object.assign({}, attrs), props), { options: props.options || options }), ons))];
		} },
		ACascader: { renderFormItemContent: createFormItemRender() },
		ADatePicker: { renderFormItemContent: createFormItemRender() },
		AMonthPicker: { renderFormItemContent: createFormItemRender() },
		ARangePicker: { renderFormItemContent: createFormItemRender() },
		AWeekPicker: { renderFormItemContent: createFormItemRender() },
		ATimePicker: { renderFormItemContent: createFormItemRender() },
		ATreeSelect: { renderFormItemContent: createFormItemRender() },
		ARate: { renderFormItemContent: createFormItemRender() },
		ASwitch: { renderFormItemContent: createFormItemRender() },
		ARadioGroup: { renderFormItemContent(renderOpts, params) {
			const { options = [], optionProps = {} } = renderOpts;
			const { data, field } = params;
			const { attrs } = renderOpts;
			const labelProp = optionProps.label || "label";
			const valueProp = optionProps.value || "value";
			const itemValue = import_xe_utils.default.get(data, field);
			return [h(getCurrComponent("a-radio-group"), Object.assign(Object.assign(Object.assign({}, attrs), getItemProps(renderOpts, params, itemValue)), getItemOns(renderOpts, params)), { default: () => {
				return options.map((option, oIndex) => {
					return h(getCurrComponent("a-radio"), {
						key: oIndex,
						value: option[valueProp],
						disabled: option.disabled
					}, { default: () => cellText(option[labelProp]) });
				});
			} })];
		} },
		ACheckboxGroup: { renderFormItemContent(renderOpts, params) {
			const { options = [], optionProps = {} } = renderOpts;
			const { data, field } = params;
			const { attrs } = renderOpts;
			const labelProp = optionProps.label || "label";
			const valueProp = optionProps.value || "value";
			const itemValue = import_xe_utils.default.get(data, field);
			return [h(getCurrComponent("a-checkbox-group"), Object.assign(Object.assign(Object.assign({}, attrs), getItemProps(renderOpts, params, itemValue)), getItemOns(renderOpts, params)), { default: () => {
				return options.map((option, oIndex) => {
					return h(getCurrComponent("a-checkbox"), {
						key: oIndex,
						value: option[valueProp],
						disabled: option.disabled
					}, { default: () => cellText(option[labelProp]) });
				});
			} })];
		} },
		AButton: { renderFormItemContent: defaultButtonItemRender },
		ARadio: { renderFormItemContent: createOldFormItemRadioAndCheckboxRender() },
		ACheckbox: { renderFormItemContent: createOldFormItemRadioAndCheckboxRender() },
		AButtons: { renderFormItemContent: defaultButtonsItemRender }
	});
}
//#endregion
//#region ../../node_modules/.pnpm/@vxe-ui+plugin-render-antd@4.3.1/node_modules/@vxe-ui/plugin-render-antd/es/form-design/input-widget.js
function createWidgetAInput(VxeUI) {
	const getWidgetAInputConfig = (params) => {
		return {
			title: "输入框",
			icon: "vxe-icon-input",
			options: { placeholder: "" }
		};
	};
	return {
		getWidgetAInputConfig,
		WidgetAInputFormComponent: defineComponent({
			props: {
				renderOpts: {
					type: Object,
					default: () => ({})
				},
				renderParams: {
					type: Object,
					default: () => ({})
				}
			},
			emits: [],
			setup(props) {
				const VxeUIFormComponent = VxeUI.getComponent("VxeForm");
				const VxeUIFormItemComponent = VxeUI.getComponent("VxeFormItem");
				const VxeUISwitchComponent = VxeUI.getComponent("VxeSwitch");
				const VxeUIInputComponent = VxeUI.getComponent("VxeInput");
				return () => {
					const { renderParams } = props;
					const { widget } = renderParams;
					const { options } = widget;
					return h(VxeUIFormComponent, {
						class: "vxe-form-design--widget-render-form-wrapper",
						vertical: true,
						span: 24,
						titleBold: true,
						titleOverflow: true,
						data: options
					}, { default() {
						return [
							h(VxeUIFormItemComponent, { title: VxeUI.getI18n("vxe.formDesign.widgetProp.name") }, { default() {
								return h(VxeUIInputComponent, {
									modelValue: widget.title,
									placeholder: options.placeholder,
									"onUpdate:modelValue"(val) {
										widget.title = val;
									}
								});
							} }),
							h(VxeUIFormItemComponent, {
								title: VxeUI.getI18n("vxe.formDesign.widgetProp.placeholder"),
								field: "placeholder",
								itemRender: { name: "VxeInput" }
							}),
							h(VxeUIFormItemComponent, { title: VxeUI.getI18n("vxe.formDesign.widgetProp.required") }, { default() {
								return h(VxeUISwitchComponent, {
									modelValue: widget.required,
									"onUpdate:modelValue"(val) {
										widget.required = val;
									}
								});
							} })
						];
					} });
				};
			}
		}),
		WidgetAInputViewComponent: defineComponent({
			props: {
				renderOpts: {
					type: Object,
					default: () => ({})
				},
				renderParams: {
					type: Object,
					default: () => ({})
				}
			},
			emits: [],
			setup(props) {
				const VxeUIFormItemComponent = VxeUI.getComponent("VxeFormItem");
				const changeEvent = () => {
					const { renderParams } = props;
					const { widget, $formView } = renderParams;
					if ($formView) {
						const itemValue = $formView ? $formView.getItemValue(widget) : null;
						$formView.updateWidgetStatus(widget, itemValue);
					}
				};
				return () => {
					const { renderParams } = props;
					const { widget, $formView } = renderParams;
					return h(VxeUIFormItemComponent, {
						class: ["vxe-form-design--widget-render-form-item"],
						field: widget.field,
						title: widget.title
					}, { default() {
						return h(getCurrComponent("a-input"), {
							value: $formView ? $formView.getItemValue(widget) : null,
							onChange: changeEvent,
							"onUpdate:value"(val) {
								if ($formView) $formView.setItemValue(widget, val);
							}
						});
					} });
				};
			}
		})
	};
}
//#endregion
//#region ../../node_modules/.pnpm/@vxe-ui+plugin-render-antd@4.3.1/node_modules/@vxe-ui/plugin-render-antd/es/form-design/textarea-widget.js
function createWidgetATextarea(VxeUI) {
	const getWidgetATextareaConfig = (params) => {
		return {
			title: "文本域",
			icon: "vxe-icon-textarea",
			options: { placeholder: "请输入" }
		};
	};
	return {
		getWidgetATextareaConfig,
		WidgetATextareaFormComponent: defineComponent({
			props: {
				renderOpts: {
					type: Object,
					default: () => ({})
				},
				renderParams: {
					type: Object,
					default: () => ({})
				}
			},
			emits: [],
			setup(props) {
				const VxeUIFormComponent = VxeUI.getComponent("VxeForm");
				const VxeUIFormItemComponent = VxeUI.getComponent("VxeFormItem");
				const VxeUISwitchComponent = VxeUI.getComponent("VxeSwitch");
				const VxeUIInputComponent = VxeUI.getComponent("VxeInput");
				return () => {
					const { renderParams } = props;
					const { widget } = renderParams;
					return h(VxeUIFormComponent, {
						class: "vxe-form-design--widget-render-form-wrapper",
						vertical: true,
						span: 24,
						titleBold: true,
						titleOverflow: true,
						data: widget.options
					}, { default() {
						return [
							h(VxeUIFormItemComponent, { title: VxeUI.getI18n("vxe.formDesign.widgetProp.name") }, { default() {
								return h(VxeUIInputComponent, {
									modelValue: widget.title,
									"onUpdate:modelValue"(val) {
										widget.title = val;
									}
								});
							} }),
							h(VxeUIFormItemComponent, {
								title: VxeUI.getI18n("vxe.formDesign.widgetProp.placeholder"),
								field: "placeholder",
								itemRender: { name: "ElInput" }
							}),
							h(VxeUIFormItemComponent, { title: VxeUI.getI18n("vxe.formDesign.widgetProp.required") }, { default() {
								return h(VxeUISwitchComponent, {
									modelValue: widget.required,
									"onUpdate:modelValue"(val) {
										widget.required = val;
									}
								});
							} })
						];
					} });
				};
			}
		}),
		WidgetATextareaViewComponent: defineComponent({
			props: {
				renderOpts: {
					type: Object,
					default: () => ({})
				},
				renderParams: {
					type: Object,
					default: () => ({})
				}
			},
			emits: [],
			setup(props) {
				const VxeUIFormItemComponent = VxeUI.getComponent("VxeFormItem");
				const changeEvent = () => {
					const { renderParams } = props;
					const { widget, $formView } = renderParams;
					if ($formView) {
						const itemValue = $formView ? $formView.getItemValue(widget) : null;
						$formView.updateWidgetStatus(widget, itemValue);
					}
				};
				return () => {
					const { renderParams } = props;
					const { widget, $formView } = renderParams;
					const { options } = widget;
					return h(VxeUIFormItemComponent, {
						class: ["vxe-form-design--widget-render-form-item"],
						field: widget.field,
						title: widget.title
					}, { default() {
						return h(getCurrComponent("a-textarea"), {
							value: $formView ? $formView.getItemValue(widget) : null,
							placeholder: options.placeholder,
							autoSize: {
								minRows: 2,
								maxRows: 4
							},
							onChange: changeEvent,
							"onUpdate:value"(val) {
								if ($formView) $formView.setItemValue(widget, val);
							}
						});
					} });
				};
			}
		})
	};
}
//#endregion
//#region ../../node_modules/.pnpm/@vxe-ui+plugin-render-antd@4.3.1/node_modules/@vxe-ui/plugin-render-antd/es/form-design/number-input-widget.js
function createWidgetAInputNumber(VxeUI) {
	const getWidgetAInputNumberConfig = (params) => {
		return {
			title: "数字",
			icon: "vxe-icon-number",
			options: { placeholder: "请输入" }
		};
	};
	return {
		getWidgetAInputNumberConfig,
		WidgetAInputNumberFormComponent: defineComponent({
			props: {
				renderOpts: {
					type: Object,
					default: () => ({})
				},
				renderParams: {
					type: Object,
					default: () => ({})
				}
			},
			emits: [],
			setup(props) {
				const VxeUIFormComponent = VxeUI.getComponent("VxeForm");
				const VxeUIFormItemComponent = VxeUI.getComponent("VxeFormItem");
				const VxeUISwitchComponent = VxeUI.getComponent("VxeSwitch");
				const VxeUIInputComponent = VxeUI.getComponent("VxeInput");
				return () => {
					const { renderParams } = props;
					const { widget } = renderParams;
					return h(VxeUIFormComponent, {
						class: "vxe-form-design--widget-render-form-wrapper",
						vertical: true,
						span: 24,
						titleBold: true,
						titleOverflow: true,
						data: widget.options
					}, { default() {
						return [
							h(VxeUIFormItemComponent, { title: VxeUI.getI18n("vxe.formDesign.widgetProp.name") }, { default() {
								return h(VxeUIInputComponent, {
									modelValue: widget.title,
									"onUpdate:modelValue"(val) {
										widget.title = val;
									}
								});
							} }),
							h(VxeUIFormItemComponent, {
								title: VxeUI.getI18n("vxe.formDesign.widgetProp.placeholder"),
								field: "placeholder",
								itemRender: { name: "ElInput" }
							}),
							h(VxeUIFormItemComponent, { title: VxeUI.getI18n("vxe.formDesign.widgetProp.required") }, { default() {
								return h(VxeUISwitchComponent, {
									modelValue: widget.required,
									"onUpdate:modelValue"(val) {
										widget.required = val;
									}
								});
							} })
						];
					} });
				};
			}
		}),
		WidgetAInputNumberViewComponent: defineComponent({
			props: {
				renderOpts: {
					type: Object,
					default: () => ({})
				},
				renderParams: {
					type: Object,
					default: () => ({})
				}
			},
			emits: [],
			setup(props) {
				const VxeUIFormItemComponent = VxeUI.getComponent("VxeFormItem");
				const changeEvent = () => {
					const { renderParams } = props;
					const { widget, $formView } = renderParams;
					if ($formView) {
						const itemValue = $formView ? $formView.getItemValue(widget) : null;
						$formView.updateWidgetStatus(widget, itemValue);
					}
				};
				return () => {
					const { renderParams } = props;
					const { widget, $formView } = renderParams;
					const { options } = widget;
					return h(VxeUIFormItemComponent, {
						class: ["vxe-form-design--widget-render-form-item"],
						field: widget.field,
						title: widget.title
					}, { default() {
						return h(getCurrComponent("a-input-number"), {
							value: $formView ? $formView.getItemValue(widget) : null,
							placeholder: options.placeholder,
							onChange: changeEvent,
							"onUpdate:value"(val) {
								if ($formView) $formView.setItemValue(widget, val);
							}
						});
					} });
				};
			}
		})
	};
}
//#endregion
//#region ../../node_modules/.pnpm/@vxe-ui+plugin-render-antd@4.3.1/node_modules/@vxe-ui/plugin-render-antd/es/form-design/date-picker-widget.js
function createWidgetADatePicker(VxeUI) {
	const getWidgetADatePickerConfig = (params) => {
		return {
			title: "日期",
			icon: "vxe-icon-input",
			options: { placeholder: "" }
		};
	};
	return {
		getWidgetADatePickerConfig,
		WidgetADatePickerFormComponent: defineComponent({
			props: {
				renderOpts: {
					type: Object,
					default: () => ({})
				},
				renderParams: {
					type: Object,
					default: () => ({})
				}
			},
			emits: [],
			setup(props) {
				const VxeUIFormComponent = VxeUI.getComponent("VxeForm");
				const VxeUIFormItemComponent = VxeUI.getComponent("VxeFormItem");
				const VxeUISwitchComponent = VxeUI.getComponent("VxeSwitch");
				const VxeUIInputComponent = VxeUI.getComponent("VxeInput");
				return () => {
					const { renderParams } = props;
					const { widget } = renderParams;
					return h(VxeUIFormComponent, {
						class: "vxe-form-design--widget-render-form-wrapper",
						vertical: true,
						span: 24,
						titleBold: true,
						titleOverflow: true,
						data: widget.options
					}, { default() {
						return [
							h(VxeUIFormItemComponent, { title: VxeUI.getI18n("vxe.formDesign.widgetProp.name") }, { default() {
								return h(VxeUIInputComponent, {
									modelValue: widget.title,
									"onUpdate:modelValue"(val) {
										widget.title = val;
									}
								});
							} }),
							h(VxeUIFormItemComponent, {
								title: VxeUI.getI18n("vxe.formDesign.widgetProp.placeholder"),
								field: "placeholder",
								itemRender: { name: "VxeInput" }
							}),
							h(VxeUIFormItemComponent, { title: VxeUI.getI18n("vxe.formDesign.widgetProp.required") }, { default() {
								return h(VxeUISwitchComponent, {
									modelValue: widget.required,
									"onUpdate:modelValue"(val) {
										widget.required = val;
									}
								});
							} })
						];
					} });
				};
			}
		}),
		WidgetADatePickerViewComponent: defineComponent({
			props: {
				renderOpts: {
					type: Object,
					default: () => ({})
				},
				renderParams: {
					type: Object,
					default: () => ({})
				}
			},
			emits: [],
			setup(props) {
				const VxeUIFormItemComponent = VxeUI.getComponent("VxeFormItem");
				const changeEvent = () => {
					const { renderParams } = props;
					const { widget, $formView } = renderParams;
					if ($formView) {
						const itemValue = $formView ? $formView.getItemValue(widget) : null;
						$formView.updateWidgetStatus(widget, itemValue);
					}
				};
				return () => {
					const { renderParams } = props;
					const { widget, $formView } = renderParams;
					const { options } = widget;
					return h(VxeUIFormItemComponent, {
						class: ["vxe-form-design--widget-render-form-item"],
						field: widget.field,
						title: widget.title
					}, { default() {
						return h(getCurrComponent("a-date-picker"), {
							value: $formView ? $formView.getItemValue(widget) : null,
							placeholder: options.placeholder,
							onChange: changeEvent,
							"onUpdate:value"(val) {
								if ($formView) $formView.setItemValue(widget, val);
							}
						});
					} });
				};
			}
		})
	};
}
//#endregion
//#region ../../node_modules/.pnpm/@vxe-ui+plugin-render-antd@4.3.1/node_modules/@vxe-ui/plugin-render-antd/es/form-design/use.js
function useWidgetPropDataSource(VxeUI, props, isSubOption) {
	const VxeUIFormItemComponent = VxeUI.getComponent("VxeFormItem");
	const VxeUIButtonComponent = VxeUI.getComponent("VxeButton");
	const VxeUITextareaComponent = VxeUI.getComponent("VxeTextarea");
	const VxeUITipComponent = VxeUI.getComponent("VxeTip");
	const optionsContent = ref("");
	const expandIndexList = ref([]);
	const addOptionEvent = () => {
		const { renderParams } = props;
		const { widget } = renderParams;
		const options = widget.options.options || [];
		options.push({ value: VxeUI.getI18n("vxe.formDesign.widgetProp.dataSource.defValue", [options.length + 1]) });
		widget.options.options = [...options];
	};
	const subRE = /^(\s|\t)+/;
	const hasSubOption = (str) => {
		return subRE.test(str);
	};
	const expandAllOption = () => {
		const { renderParams } = props;
		const { widget } = renderParams;
		const options = widget.options.options || [];
		const indexList = [];
		options.forEach((group, gIndex) => {
			const { options } = group;
			if (options && options.length) indexList.push(gIndex);
		});
		expandIndexList.value = indexList;
	};
	const toggleExpandOption = (item, gIndex) => {
		if (expandIndexList.value.includes(gIndex)) expandIndexList.value = expandIndexList.value.filter((num) => num !== gIndex);
		else expandIndexList.value.push(gIndex);
	};
	const confirmBatchAddOptionEvent = () => {
		const { renderParams } = props;
		const { widget } = renderParams;
		const optList = [];
		const rowList = optionsContent.value.split("\n");
		let prevGroup = null;
		if (isSubOption) rowList.forEach((str, index) => {
			const nextStr = rowList[index + 1];
			const value = str.trim();
			if (!value) return;
			const item = { value };
			if (prevGroup) {
				if (hasSubOption(str)) {
					prevGroup.options.push(item);
					return;
				}
				prevGroup = null;
				optList.push(item);
			} else optList.push(item);
			if (nextStr) {
				if (hasSubOption(nextStr)) prevGroup = Object.assign(item, { options: [] });
			}
		});
		else rowList.forEach((str) => {
			optList.push({ value: str.trim() });
		});
		widget.options.options = optList;
		expandAllOption();
	};
	const openPopupEditEvent = () => {
		var _a;
		const { renderParams } = props;
		const { widget } = renderParams;
		const contList = [];
		(_a = widget.options.options) === null || _a === void 0 || _a.forEach((group) => {
			var _a;
			contList.push(group.value);
			(_a = group.options) === null || _a === void 0 || _a.forEach((item) => {
				contList.push(`\t${item.value}`);
			});
		});
		optionsContent.value = contList.join("\n");
		VxeUI.modal.open({
			title: `${widget.title} - ${VxeUI.getI18n("vxe.formDesign.widgetProp.dataSource.batchEditOption")}`,
			width: 500,
			height: "50vh ",
			resize: true,
			showFooter: true,
			showCancelButton: true,
			showConfirmButton: true,
			confirmButtonText: VxeUI.getI18n("vxe.formDesign.widgetProp.dataSource.buildOption"),
			onConfirm: confirmBatchAddOptionEvent,
			slots: { default() {
				return h("div", { class: "vxe-form-design--widget-form-item-data-source-popup" }, [h(VxeUITipComponent, {
					status: "primary",
					title: "",
					content: VxeUI.getI18n(`vxe.formDesign.widgetProp.dataSource.${isSubOption ? "batchEditSubTip" : "batchEditTip"}`)
				}), h(VxeUITextareaComponent, {
					resize: "none",
					modelValue: optionsContent.value,
					"onUpdate:modelValue"(val) {
						optionsContent.value = val;
					}
				})]);
			} }
		});
	};
	const renderOption = (item, hasFirstLevel, isExpand, gIndex, hasSub, isFirst, isLast) => {
		return h("div", { class: ["vxe-form-design--widget-form-item-data-source-option", {
			"is--first": isFirst,
			"is--last": isLast
		}] }, [
			h("div", { class: "vxe-form-design--widget-expand-btn" }, hasFirstLevel && hasSub ? [h("i", {
				class: isExpand ? VxeUI.getIcon().FORM_DESIGN_WIDGET_OPTION_EXPAND_CLOSE : VxeUI.getIcon().FORM_DESIGN_WIDGET_OPTION_EXPAND_OPEN,
				onClick() {
					toggleExpandOption(item, gIndex);
				}
			})] : []),
			h("input", {
				class: "vxe-default-input",
				value: item.value,
				onInput(evnt) {
					item.value = evnt.currentTarget.value;
				}
			}),
			h(VxeUIButtonComponent, {
				status: "danger",
				mode: "text",
				icon: VxeUI.getIcon().FORM_DESIGN_WIDGET_DELETE
			})
		]);
	};
	const renderOptions = () => {
		const { renderParams } = props;
		const { widget } = renderParams;
		const { options } = widget;
		const groups = options.options;
		const optVNs = [];
		if (groups) groups.forEach((group, gIndex) => {
			const { options } = group;
			const isExpand = expandIndexList.value.includes(gIndex);
			if (options && options.length) {
				optVNs.push(renderOption(group, true, isExpand, gIndex, true, gIndex === 0, gIndex === groups.length - 1));
				if (isExpand) optVNs.push(h("div", { class: "vxe-form-design--widget-form-item-data-source-sub-option" }, options.map((item) => renderOption(item, false, isExpand, 0, false, false, false))));
			} else optVNs.push(renderOption(group, true, isExpand, gIndex, false, gIndex === 0, gIndex === groups.length - 1));
		});
		return optVNs;
	};
	watch(() => props.renderParams.widget, () => {
		expandAllOption();
	});
	onMounted(() => {
		expandAllOption();
	});
	return { renderDataSourceFormItem() {
		return h(VxeUIFormItemComponent, {
			title: VxeUI.getI18n("vxe.formDesign.widgetProp.dataSource.name"),
			field: "options"
		}, { default() {
			return [h("div", {}, [h(VxeUIButtonComponent, {
				status: "primary",
				mode: "text",
				content: VxeUI.getI18n("vxe.formDesign.widgetProp.dataSource.addOption"),
				onClick: addOptionEvent
			}), h(VxeUIButtonComponent, {
				status: "primary",
				mode: "text",
				content: VxeUI.getI18n("vxe.formDesign.widgetProp.dataSource.batchEditOption"),
				onClick: openPopupEditEvent
			})]), h("div", { class: "vxe-form-design--widget-form-item-data-source" }, renderOptions())];
		} });
	} };
}
//#endregion
//#region ../../node_modules/.pnpm/@vxe-ui+plugin-render-antd@4.3.1/node_modules/@vxe-ui/plugin-render-antd/es/form-design/select-widget.js
function createWidgetASelect(VxeUI) {
	const getWidgetASelectConfig = (params) => {
		return {
			title: "下拉框",
			icon: "vxe-icon-select",
			options: {
				placeholder: "请选择",
				options: import_xe_utils.default.range(0, 3).map((v, i) => {
					return { value: VxeUI.getI18n("vxe.formDesign.widgetProp.dataSource.defValue", [i + 1]) };
				})
			}
		};
	};
	return {
		getWidgetASelectConfig,
		WidgetASelectFormComponent: defineComponent({
			props: {
				renderOpts: {
					type: Object,
					default: () => ({})
				},
				renderParams: {
					type: Object,
					default: () => ({})
				}
			},
			emits: [],
			setup(props) {
				const VxeUIFormComponent = VxeUI.getComponent("VxeForm");
				const VxeUIFormItemComponent = VxeUI.getComponent("VxeFormItem");
				const VxeUISwitchComponent = VxeUI.getComponent("VxeSwitch");
				const VxeUIInputComponent = VxeUI.getComponent("VxeInput");
				const { renderDataSourceFormItem } = useWidgetPropDataSource(VxeUI, props, false);
				return () => {
					const { renderParams } = props;
					const { widget } = renderParams;
					return h(VxeUIFormComponent, {
						class: "vxe-form-design--widget-render-form-wrapper",
						vertical: true,
						span: 24,
						titleBold: true,
						titleOverflow: true,
						data: widget.options
					}, { default() {
						return [
							h(VxeUIFormItemComponent, { title: VxeUI.getI18n("vxe.formDesign.widgetProp.name") }, { default() {
								return h(VxeUIInputComponent, {
									modelValue: widget.title,
									"onUpdate:modelValue"(val) {
										widget.title = val;
									}
								});
							} }),
							h(VxeUIFormItemComponent, {
								title: VxeUI.getI18n("vxe.formDesign.widgetProp.placeholder"),
								field: "placeholder",
								itemRender: { name: "ElInput" }
							}),
							h(VxeUIFormItemComponent, { title: VxeUI.getI18n("vxe.formDesign.widgetProp.required") }, { default() {
								return h(VxeUISwitchComponent, {
									modelValue: widget.required,
									"onUpdate:modelValue"(val) {
										widget.required = val;
									}
								});
							} }),
							renderDataSourceFormItem()
						];
					} });
				};
			}
		}),
		WidgetASelectViewComponent: defineComponent({
			props: {
				renderOpts: {
					type: Object,
					default: () => ({})
				},
				renderParams: {
					type: Object,
					default: () => ({})
				}
			},
			emits: [],
			setup(props) {
				const VxeUIFormItemComponent = VxeUI.getComponent("VxeFormItem");
				const changeEvent = () => {
					const { renderParams } = props;
					const { widget, $formView } = renderParams;
					if ($formView) {
						const itemValue = $formView ? $formView.getItemValue(widget) : null;
						$formView.updateWidgetStatus(widget, itemValue);
					}
				};
				return () => {
					const { renderParams } = props;
					const { widget, $formView } = renderParams;
					const { options } = widget;
					return h(VxeUIFormItemComponent, {
						class: ["vxe-form-design--widget-render-form-item"],
						field: widget.field,
						title: widget.title
					}, { default() {
						return h(getCurrComponent("a-select"), {
							value: $formView ? $formView.getItemValue(widget) : null,
							options: options.options,
							placeholder: options.placeholder,
							onChange: changeEvent,
							"onUpdate:value"(val) {
								if ($formView) $formView.setItemValue(widget, val);
							}
						});
					} });
				};
			}
		})
	};
}
//#endregion
//#region ../../node_modules/.pnpm/@vxe-ui+plugin-render-antd@4.3.1/node_modules/@vxe-ui/plugin-render-antd/es/form-design/radio-widget.js
function createWidgetARadio(VxeUI) {
	const getWidgetARadioConfig = (params) => {
		return {
			title: "单选框",
			icon: "vxe-icon-radio-checked",
			options: { options: import_xe_utils.default.range(0, 3).map((v, i) => {
				return { value: VxeUI.getI18n("vxe.formDesign.widgetProp.dataSource.defValue", [i + 1]) };
			}) }
		};
	};
	return {
		getWidgetARadioConfig,
		WidgetARadioFormComponent: defineComponent({
			props: {
				renderOpts: {
					type: Object,
					default: () => ({})
				},
				renderParams: {
					type: Object,
					default: () => ({})
				}
			},
			emits: [],
			setup(props) {
				const VxeUIFormComponent = VxeUI.getComponent("VxeForm");
				const VxeUIFormItemComponent = VxeUI.getComponent("VxeFormItem");
				const VxeUISwitchComponent = VxeUI.getComponent("VxeSwitch");
				const VxeUIInputComponent = VxeUI.getComponent("VxeInput");
				const { renderDataSourceFormItem } = useWidgetPropDataSource(VxeUI, props, false);
				return () => {
					const { renderParams } = props;
					const { widget } = renderParams;
					return h(VxeUIFormComponent, {
						class: "vxe-form-design--widget-render-form-wrapper",
						vertical: true,
						span: 24,
						titleBold: true,
						titleOverflow: true,
						data: widget.options
					}, { default() {
						return [
							h(VxeUIFormItemComponent, { title: VxeUI.getI18n("vxe.formDesign.widgetProp.name") }, { default() {
								return h(VxeUIInputComponent, {
									modelValue: widget.title,
									"onUpdate:modelValue"(val) {
										widget.title = val;
									}
								});
							} }),
							h(VxeUIFormItemComponent, {
								title: VxeUI.getI18n("vxe.formDesign.widgetProp.placeholder"),
								field: "placeholder",
								itemRender: { name: "ElInput" }
							}),
							h(VxeUIFormItemComponent, { title: VxeUI.getI18n("vxe.formDesign.widgetProp.required") }, { default() {
								return h(VxeUISwitchComponent, {
									modelValue: widget.required,
									"onUpdate:modelValue"(val) {
										widget.required = val;
									}
								});
							} }),
							renderDataSourceFormItem()
						];
					} });
				};
			}
		}),
		WidgetARadioViewComponent: defineComponent({
			props: {
				renderOpts: {
					type: Object,
					default: () => ({})
				},
				renderParams: {
					type: Object,
					default: () => ({})
				}
			},
			emits: [],
			setup(props) {
				const VxeUIFormItemComponent = VxeUI.getComponent("VxeFormItem");
				const radioOptions = computed(() => {
					const { renderParams } = props;
					const { widget } = renderParams;
					const { options } = widget;
					return options.options.map((item) => {
						return {
							label: item.value,
							value: item.value
						};
					});
				});
				const changeEvent = () => {
					const { renderParams } = props;
					const { widget, $formView } = renderParams;
					if ($formView) {
						const itemValue = $formView ? $formView.getItemValue(widget) : null;
						$formView.updateWidgetStatus(widget, itemValue);
					}
				};
				return () => {
					const { renderParams } = props;
					const { widget, $formView } = renderParams;
					return h(VxeUIFormItemComponent, {
						class: ["vxe-form-design--widget-render-form-item"],
						field: widget.field,
						title: widget.title
					}, { default() {
						return h(getCurrComponent("a-radio-group"), {
							value: $formView ? $formView.getItemValue(widget) : null,
							options: radioOptions.value,
							onChange: changeEvent,
							"onUpdate:value"(val) {
								if ($formView) $formView.setItemValue(widget, val);
							}
						});
					} });
				};
			}
		})
	};
}
//#endregion
//#region ../../node_modules/.pnpm/@vxe-ui+plugin-render-antd@4.3.1/node_modules/@vxe-ui/plugin-render-antd/es/form-design/checkbox-widget.js
function createWidgetACheckbox(VxeUI) {
	const getWidgetACheckboxConfig = (params) => {
		return {
			title: "复选框",
			icon: "vxe-icon-checkbox-checked",
			options: { options: import_xe_utils.default.range(0, 3).map((v, i) => {
				return { value: VxeUI.getI18n("vxe.formDesign.widgetProp.dataSource.defValue", [i + 1]) };
			}) }
		};
	};
	return {
		getWidgetACheckboxConfig,
		WidgetACheckboxFormComponent: defineComponent({
			props: {
				renderOpts: {
					type: Object,
					default: () => ({})
				},
				renderParams: {
					type: Object,
					default: () => ({})
				}
			},
			emits: [],
			setup(props) {
				const VxeUIFormComponent = VxeUI.getComponent("VxeForm");
				const VxeUIFormItemComponent = VxeUI.getComponent("VxeFormItem");
				const VxeUISwitchComponent = VxeUI.getComponent("VxeSwitch");
				const VxeUIInputComponent = VxeUI.getComponent("VxeInput");
				const { renderDataSourceFormItem } = useWidgetPropDataSource(VxeUI, props, false);
				return () => {
					const { renderParams } = props;
					const { widget } = renderParams;
					return h(VxeUIFormComponent, {
						class: "vxe-form-design--widget-render-form-wrapper",
						vertical: true,
						span: 24,
						titleBold: true,
						titleOverflow: true,
						data: widget.options
					}, { default() {
						return [
							h(VxeUIFormItemComponent, { title: VxeUI.getI18n("vxe.formDesign.widgetProp.name") }, { default() {
								return h(VxeUIInputComponent, {
									modelValue: widget.title,
									"onUpdate:modelValue"(val) {
										widget.title = val;
									}
								});
							} }),
							h(VxeUIFormItemComponent, {
								title: VxeUI.getI18n("vxe.formDesign.widgetProp.placeholder"),
								field: "placeholder",
								itemRender: { name: "ElInput" }
							}),
							h(VxeUIFormItemComponent, { title: VxeUI.getI18n("vxe.formDesign.widgetProp.required") }, { default() {
								return h(VxeUISwitchComponent, {
									modelValue: widget.required,
									"onUpdate:modelValue"(val) {
										widget.required = val;
									}
								});
							} }),
							renderDataSourceFormItem()
						];
					} });
				};
			}
		}),
		WidgetACheckboxViewComponent: defineComponent({
			props: {
				renderOpts: {
					type: Object,
					default: () => ({})
				},
				renderParams: {
					type: Object,
					default: () => ({})
				}
			},
			emits: [],
			setup(props) {
				const VxeUIFormItemComponent = VxeUI.getComponent("VxeFormItem");
				const checkboxOptions = computed(() => {
					const { renderParams } = props;
					const { widget } = renderParams;
					const { options } = widget;
					return options.options.map((item) => {
						return {
							label: item.value,
							value: item.value
						};
					});
				});
				const changeEvent = () => {
					const { renderParams } = props;
					const { widget, $formView } = renderParams;
					if ($formView) {
						const itemValue = $formView ? $formView.getItemValue(widget) : null;
						$formView.updateWidgetStatus(widget, itemValue);
					}
				};
				return () => {
					const { renderParams } = props;
					const { widget, $formView } = renderParams;
					return h(VxeUIFormItemComponent, {
						class: ["vxe-form-design--widget-render-form-item"],
						field: widget.field,
						title: widget.title
					}, { default() {
						return h(getCurrComponent("a-checkbox-group"), {
							value: $formView ? $formView.getItemValue(widget) : null,
							options: checkboxOptions.value,
							onChange: changeEvent,
							"onUpdate:value"(val) {
								if ($formView) $formView.setItemValue(widget, val);
							}
						});
					} });
				};
			}
		})
	};
}
//#endregion
//#region ../../node_modules/.pnpm/@vxe-ui+plugin-render-antd@4.3.1/node_modules/@vxe-ui/plugin-render-antd/es/form-design/switch-widget.js
function createWidgetASwitch(VxeUI) {
	const getWidgetASwitchConfig = (params) => {
		return {
			title: "是/否",
			icon: "vxe-icon-switch",
			options: {}
		};
	};
	return {
		getWidgetASwitchConfig,
		WidgetASwitchFormComponent: defineComponent({
			props: {
				renderOpts: {
					type: Object,
					default: () => ({})
				},
				renderParams: {
					type: Object,
					default: () => ({})
				}
			},
			emits: [],
			setup(props) {
				const VxeUIFormComponent = VxeUI.getComponent("VxeForm");
				const VxeUIFormItemComponent = VxeUI.getComponent("VxeFormItem");
				const VxeUISwitchComponent = VxeUI.getComponent("VxeSwitch");
				const VxeUIInputComponent = VxeUI.getComponent("VxeInput");
				return () => {
					const { renderParams } = props;
					const { widget } = renderParams;
					return h(VxeUIFormComponent, {
						class: "vxe-form-design--widget-render-form-wrapper",
						vertical: true,
						span: 24,
						titleBold: true,
						titleOverflow: true,
						data: widget.options
					}, { default() {
						return [
							h(VxeUIFormItemComponent, { title: VxeUI.getI18n("vxe.formDesign.widgetProp.name") }, { default() {
								return h(VxeUIInputComponent, {
									modelValue: widget.title,
									"onUpdate:modelValue"(val) {
										widget.title = val;
									}
								});
							} }),
							h(VxeUIFormItemComponent, {
								title: VxeUI.getI18n("vxe.formDesign.widgetProp.placeholder"),
								field: "placeholder",
								itemRender: { name: "ElInput" }
							}),
							h(VxeUIFormItemComponent, { title: VxeUI.getI18n("vxe.formDesign.widgetProp.required") }, { default() {
								return h(VxeUISwitchComponent, {
									modelValue: widget.required,
									"onUpdate:modelValue"(val) {
										widget.required = val;
									}
								});
							} })
						];
					} });
				};
			}
		}),
		WidgetASwitchViewComponent: defineComponent({
			props: {
				renderOpts: {
					type: Object,
					default: () => ({})
				},
				renderParams: {
					type: Object,
					default: () => ({})
				}
			},
			emits: [],
			setup(props) {
				const VxeUIFormItemComponent = VxeUI.getComponent("VxeFormItem");
				const changeEvent = () => {
					const { renderParams } = props;
					const { widget, $formView } = renderParams;
					if ($formView) {
						const itemValue = $formView ? $formView.getItemValue(widget) : null;
						$formView.updateWidgetStatus(widget, itemValue);
					}
				};
				return () => {
					const { renderParams } = props;
					const { widget, $formView } = renderParams;
					return h(VxeUIFormItemComponent, {
						class: ["vxe-form-design--widget-render-form-item"],
						field: widget.field,
						title: widget.title
					}, { default() {
						return h(getCurrComponent("a-switch"), {
							checked: $formView ? $formView.getItemValue(widget) : null,
							onChange: changeEvent,
							"onUpdate:checked"(val) {
								if ($formView) $formView.setItemValue(widget, val);
							}
						});
					} });
				};
			}
		})
	};
}
//#endregion
//#region ../../node_modules/.pnpm/@vxe-ui+plugin-render-antd@4.3.1/node_modules/@vxe-ui/plugin-render-antd/es/form-design/index.js
/**
* 表单设计器 - 渲染器
*/
function defineFormDesignRender(VxeUI) {
	const { getWidgetAInputConfig, WidgetAInputViewComponent, WidgetAInputFormComponent } = createWidgetAInput(VxeUI);
	const { getWidgetATextareaConfig, WidgetATextareaViewComponent, WidgetATextareaFormComponent } = createWidgetATextarea(VxeUI);
	const { getWidgetAInputNumberConfig, WidgetAInputNumberViewComponent, WidgetAInputNumberFormComponent } = createWidgetAInputNumber(VxeUI);
	const { getWidgetADatePickerConfig, WidgetADatePickerViewComponent, WidgetADatePickerFormComponent } = createWidgetADatePicker(VxeUI);
	const { getWidgetASelectConfig, WidgetASelectViewComponent, WidgetASelectFormComponent } = createWidgetASelect(VxeUI);
	const { getWidgetARadioConfig, WidgetARadioViewComponent, WidgetARadioFormComponent } = createWidgetARadio(VxeUI);
	const { getWidgetACheckboxConfig, WidgetACheckboxViewComponent, WidgetACheckboxFormComponent } = createWidgetACheckbox(VxeUI);
	const { getWidgetASwitchConfig, WidgetASwitchViewComponent, WidgetASwitchFormComponent } = createWidgetASwitch(VxeUI);
	VxeUI.renderer.mixin({
		AInputWidget: {
			createFormDesignWidgetConfig: getWidgetAInputConfig,
			renderFormDesignWidgetView(renderOpts, renderParams) {
				return h(WidgetAInputViewComponent, {
					renderOpts,
					renderParams
				});
			},
			renderFormDesignWidgetFormView(renderOpts, renderParams) {
				return h(WidgetAInputFormComponent, {
					renderOpts,
					renderParams
				});
			}
		},
		ATextareaWidget: {
			createFormDesignWidgetConfig: getWidgetATextareaConfig,
			renderFormDesignWidgetView(renderOpts, renderParams) {
				return h(WidgetATextareaViewComponent, {
					renderOpts,
					renderParams
				});
			},
			renderFormDesignWidgetFormView(renderOpts, renderParams) {
				return h(WidgetATextareaFormComponent, {
					renderOpts,
					renderParams
				});
			}
		},
		ANumberInputWidget: {
			createFormDesignWidgetConfig: getWidgetAInputNumberConfig,
			renderFormDesignWidgetView(renderOpts, renderParams) {
				return h(WidgetAInputNumberViewComponent, {
					renderOpts,
					renderParams
				});
			},
			renderFormDesignWidgetFormView(renderOpts, renderParams) {
				return h(WidgetAInputNumberFormComponent, {
					renderOpts,
					renderParams
				});
			}
		},
		ADatePickerWidget: {
			createFormDesignWidgetConfig: getWidgetADatePickerConfig,
			renderFormDesignWidgetView(renderOpts, renderParams) {
				return h(WidgetADatePickerViewComponent, {
					renderOpts,
					renderParams
				});
			},
			renderFormDesignWidgetFormView(renderOpts, renderParams) {
				return h(WidgetADatePickerFormComponent, {
					renderOpts,
					renderParams
				});
			}
		},
		ASelectWidget: {
			createFormDesignWidgetConfig: getWidgetASelectConfig,
			renderFormDesignWidgetView(renderOpts, renderParams) {
				return h(WidgetASelectViewComponent, {
					renderOpts,
					renderParams
				});
			},
			renderFormDesignWidgetFormView(renderOpts, renderParams) {
				return h(WidgetASelectFormComponent, {
					renderOpts,
					renderParams
				});
			}
		},
		ARadioWidget: {
			createFormDesignWidgetConfig: getWidgetARadioConfig,
			renderFormDesignWidgetView(renderOpts, renderParams) {
				return h(WidgetARadioViewComponent, {
					renderOpts,
					renderParams
				});
			},
			renderFormDesignWidgetFormView(renderOpts, renderParams) {
				return h(WidgetARadioFormComponent, {
					renderOpts,
					renderParams
				});
			}
		},
		ACheckboxWidget: {
			createFormDesignWidgetConfig: getWidgetACheckboxConfig,
			createFormDesignWidgetFieldValue: () => [],
			renderFormDesignWidgetView(renderOpts, renderParams) {
				return h(WidgetACheckboxViewComponent, {
					renderOpts,
					renderParams
				});
			},
			renderFormDesignWidgetFormView(renderOpts, renderParams) {
				return h(WidgetACheckboxFormComponent, {
					renderOpts,
					renderParams
				});
			}
		},
		ASwitchWidget: {
			createFormDesignWidgetConfig: getWidgetASwitchConfig,
			renderFormDesignWidgetView(renderOpts, renderParams) {
				return h(WidgetASwitchViewComponent, {
					renderOpts,
					renderParams
				});
			},
			renderFormDesignWidgetFormView(renderOpts, renderParams) {
				return h(WidgetASwitchFormComponent, {
					renderOpts,
					renderParams
				});
			}
		}
	});
}
//#endregion
//#region ../../node_modules/.pnpm/@vxe-ui+plugin-render-antd@4.3.1/node_modules/@vxe-ui/plugin-render-antd/es/index.esm.js
function getEventTarget(evnt) {
	const target = evnt.target;
	if (target && target.shadowRoot && evnt.composed) return evnt.composedPath()[0] || target;
	return target;
}
/**
* 检查触发源是否属于目标节点
*/
function getEventTargetNode(evnt, container, className) {
	let targetElem;
	let target = getEventTarget(evnt);
	const rootEl = document.documentElement || document.querySelector("html");
	while (target && target.nodeType && target !== rootEl) {
		if (className && target.className && target.className.split && target.className.split(" ").indexOf(className) > -1) targetElem = target;
		else if (target === container) return {
			flag: className ? !!targetElem : true,
			container,
			targetElem
		};
		target = target.parentElement;
	}
	return { flag: false };
}
function toComponentName(name) {
	if (name) return name.slice(0, 1).toUpperCase() + name.slice(1);
	return name;
}
var VxeUIPluginRenderAntd = {
	component(comp) {
		if (comp && comp.name) {
			const kcName = import_xe_utils.default.kebabCase(comp.name);
			const ccName = toComponentName(import_xe_utils.default.camelCase(comp.name));
			componentMaps[kcName] = comp;
			componentMaps[ccName] = comp;
		} else console.error("[@vxe-ui/plugin-render-antd 4.3.1] error component.", comp);
	},
	install(VxeUI, options) {
		const pluginOpts = Object.assign({}, options);
		if (options) Object.assign(globalConfig, options);
		if (VxeUI.checkVersion) {
			const pVersion = 4;
			const sVersion = 11;
			if (!VxeUI.checkVersion(VxeUI.tableVersion, pVersion, sVersion)) console.error(`[@vxe-ui/plugin-render-antd 4.3.1] ${VxeUI.getI18n("vxe.error.errorVersion", [`vxe-table@${VxeUI.tableVersion || "?"}`, `vxe-table v${pVersion}.${sVersion}+`])} https://vxeui.com/other4/#/plugin-render-antd/install`);
		} else if (!/^(4)\./.test(VxeUI.uiVersion || VxeUI.tableVersion)) console.error("[@vxe-ui/plugin-render-antd 4.3.1] Requires vxe-table 4.7.0+ version. https://vxeui.com/other4/#/plugin-render-antd/install");
		/**
		* 事件兼容性处理
		*/
		const handleClearEvent = (params) => {
			const { $event } = params;
			const bodyElem = document.body;
			const prefixCls = `${pluginOpts.prefixCls || "ant"}`.replace(/-$/, "");
			if (getEventTargetNode($event, bodyElem, `${prefixCls}-select-dropdown`).flag || getEventTargetNode($event, bodyElem, `${prefixCls}-cascader-menus`).flag || getEventTargetNode($event, bodyElem, `${prefixCls}-picker-dropdown`).flag || getEventTargetNode($event, bodyElem, `${prefixCls}-calendar-picker-container`).flag || getEventTargetNode($event, bodyElem, `${prefixCls}-time-picker-panel`).flag) return false;
		};
		defineTableRender(VxeUI);
		defineFormRender(VxeUI);
		defineFormDesignRender(VxeUI);
		VxeUI.interceptor.add("event.clearFilter", handleClearEvent);
		VxeUI.interceptor.add("event.clearEdit", handleClearEvent);
		VxeUI.interceptor.add("event.clearAreas", handleClearEvent);
		VxeUI.interceptor.add("event.clearActived", handleClearEvent);
	}
};
if (typeof window !== "undefined") {
	if (window.VxeUI && window.VxeUI.use) window.VxeUI.use(VxeUIPluginRenderAntd);
	if (window.antd) globalConfig.Antd = window.antd;
}
//#endregion
export { VxeUIPluginRenderAntd, VxeUIPluginRenderAntd as default };

//# sourceMappingURL=@vxe-ui_plugin-render-antd.js.map