import { o as __toESM, r as __exportAll } from "./chunk-CqwQKh_b.js";
import { At as onActivated, Bt as onUnmounted, Gn as reactive, It as onMounted, L as Teleport, Mt as onBeforeUnmount, Pt as onDeactivated, St as inject, W as computed, Wt as provide, Xt as resolveComponent, _n as watch, kt as nextTick, mt as h, q as createCommentVNode, qn as ref } from "./runtime-dom.esm-bundler-BEW9uiVc.js";
import "./vue.js";
import { t as require_xe_utils } from "./xe-utils-CectUEgD.js";
import { A as globalEvents, B as setTheme, C as getLanguage, D as globalResize, E as setLanguage, F as getSlotVNs, H as VxeCore, I as globalStore, L as getConfig, M as renderCustomIcon, N as renderGlobalIcon, O as GLOBAL_EVENT_KEYS, P as setIcon, R as setConfig, S as getI18n, T as setI18n, U as coreVersion, _ as formats, a as hasComponent, b as renderer, c as useFns, d as hooks, f as handleCheckInfo, g as commands, h as interceptor, i as getComponent, j as getIcon, k as createEvent, l as usePermission, m as clipboard, n as checkVersion, o as renderEmptyElement, p as permission, r as component, s as use, t as VxeUI, u as useSize, v as menus, w as hasLanguage, x as log, y as validators, z as getTheme } from "./index.esm-9ZlbHbuz.js";
import { a as errLog, c as dynamicApp, i as version, l as dynamicStore, n as setup, o as warnLog, s as checkDynamic, t as config, u as defineVxeComponent } from "./ui-DJM_GiG-.js";
import { i as getSlotVNs$1, n as getModelEvent, r as getOnName, t as getChangeEvent } from "./vn-CWvME2BO.js";
import { a as getOffsetPos, d as isScale, f as removeClass, h as updatePanelPlacement, i as getEventTargetNode, l as hasControlKey, m as toCssUnit, o as getPaddingTopBottomSize, p as scrollToView, r as getDomNode, s as getTpImg, t as addClass } from "./dom-8Eri1BpN.js";
import { c as nextSubZIndex, i as getLastZIndex, l as nextZIndex, n as getFuncText, r as getGlobalDefaultConfig, s as isEnableConf, t as eqEmptyValue } from "./utils-C8rmPdj_.js";
import { n as VxeTooltip, r as tooltip_default, t as Tooltip } from "./tooltip-DMZ7yq0H.js";
import { t as VxeButtonComponent } from "./button-DWk9JG_j.js";
import { n as VxeButton, t as Button } from "./button-VHxW7JL_.js";
import { a as handleValueFormat, c as parseDateObj, d as toStringTimeDate, i as getRangeDateByCode, l as parseDateString, n as getDateByCode, o as hasDateValueType, r as getDateQuarter, s as hasTimestampValueType, t as input_default, u as parseDateValue } from "./input-pQxSruLL.js";
import { i as loading_default, n as LoadingController, r as VxeLoading, t as Loading } from "./loading-Bav5QDAk.js";
import { n as VxeCheckbox, r as checkbox_default, t as Checkbox } from "./checkbox-CgljssLu.js";
import { t as number_input_default } from "./number-input-B7AdUtlu.js";
import { n as VxeIcon, r as icon_default, t as Icon } from "./icon-Fn_3IcPJ.js";
import { n as VxeInput, t as Input } from "./input-DoQKigk12.js";
import { n as ModalController, r as VxeModal, t as Modal } from "./modal--EJ805bX.js";
import { n as VxeNumberInput, t as NumberInput } from "./number-input-CQPt8WCN.js";
import { n as VxePager, t as Pager } from "./pager-DfeAbqhw.js";
import { i as radio_default, n as VxeRadioGroup, r as button_default$1, t as RadioGroup } from "./radio-group-ZCoaEYX2.js";
import { n as VxeSelect, t as Select } from "./select-x92-Mb08.js";
import { i as saveLocalFile, n as VxeUpload, r as readLocalFile, t as Upload } from "./upload-Dwbk1ba1.js";
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/language/zh-CN.js
var zh_CN_default = { vxe: {
	base: {
		pleaseInput: "请输入",
		pleaseSelect: "请选择",
		comma: "，",
		fullStop: "。"
	},
	loading: { text: "加载中..." },
	error: {
		downErr: "下载失败",
		errLargeData: "当绑定的数据量过大时，应该请使用 {0}，否则可能会出现卡顿",
		groupFixed: "如果使用分组表头，冻结列必须按组设置",
		groupMouseRange: "分组表头与 \"{0}\" 不能同时使用，这可能会出现错误",
		groupTag: "分组列头应该使用 \"{0}\" 而不是 \"{1}\"，这可能会出现错误",
		scrollErrProp: "启用虚拟滚动后不支持该参数 \"{0}\"",
		errConflicts: "参数 \"{0}\" 与 \"{1}\" 有冲突",
		modelConflicts: "绑定的字段值 \"{0}\" 与 \"{1}\" 存在冲突，将会出现错误",
		notSupportProp: "当启用参数 \"{0}\" 时不支持 \"{1}\"，应该为 \"{2}\"，否则将会出现错误",
		reqSupportProp: "当使用 \"{0}\" 时，应该设置 \"{1}\"，否则可能会出现错误",
		notConflictProp: "当使用 \"{0}\" 时，应该设置 \"{1}\"，否则可能会存在功能冲突",
		unableInsert: "无法插入到指定位置，请检查参数是否正确",
		useErr: "安装 \"{0}\" 模块时发生错误，可能顺序不正确，依赖的模块需要在 Table 之前安装",
		barUnableLink: "工具栏无法关联表格",
		expandContent: "展开行的插槽应该是 \"content\"，请检查是否正确",
		reqComp: "缺少 \"{0}\" 组件，请检查是否正确安装。 https://vxeui.com/#/start/useGlobal",
		reqModule: "缺少 \"{0}\" 模块",
		reqProp: "缺少必要的 \"{0}\" 参数，这可能会导致出现错误",
		emptyProp: "参数 \"{0}\" 不允许为空",
		errProp: "不支持的参数 \"{0}\"，可能为 \"{1}\"",
		colRepet: "column.{0}=\"{1}\" 重复了，这可能会导致某些功能无法使用",
		notFunc: "方法 \"{0}\" 不存在",
		errFunc: "参数 \"{0}\" 不是一个方法",
		notValidators: "全局校验 \"{0}\" 不存在",
		notFormats: "全局格式化 \"{0}\" 不存在",
		notCommands: "全局指令 \"{0}\" 不存在",
		notSlot: "插槽 \"{0}\" 不存在",
		noTree: "树结构不支持 \"{0}\"",
		noGroup: "数据分组后不支持 \"{0}\"",
		notProp: "不支持的参数 \"{0}\"",
		checkProp: "当数据量过大时可能会导致复选框卡顿，建议设置参数 \"{0}\" 提升渲染速度",
		coverProp: "\"{0}\" 的参数 \"{1}\" 重复定义，这可能会出现错误",
		uniField: "字段名 \"{0}\" 重复定义，这可能会出现错误",
		repeatKey: "主键重复 {0}=\"{1}\"，这可能会出现错误",
		repeatProp: "参数重复 {0}=\"{1}\"，这可能会出现错误",
		delFunc: "方法 \"{0}\" 已废弃，请使用 \"{1}\"",
		delProp: "参数 \"{0}\" 已废弃，请使用 \"{1}\"",
		delEvent: "事件 \"{0}\" 已废弃，请使用 \"{1}\"",
		removeProp: "参数 \"{0}\" 已废弃，不建议使用，这可能会导致出现错误",
		errFormat: "全局的格式化内容应该使用 \"VXETable.formats\" 定义，挂载 \"formatter={0}\" 的方式已不建议使用",
		notType: "不支持的文件类型 \"{0}\"",
		notExp: "该浏览器不支持导入/导出功能",
		impFields: "导入失败，请检查字段名和数据格式是否正确",
		treeNotImp: "树表格不支持导入",
		treeCrossDrag: "只能拖拽第一层级",
		treeDragChild: "父级不能拖拽到自己的子级中",
		reqPlugin: "扩展插件未安装 \"{1}\" https://vxeui.com/other{0}/#/{1}/install",
		errMaxRow: "超过支持的最大数据量 {0} 行，这可能会导致出现错误",
		useNew: "不建议使用 {0}，请使用 {1}",
		errorVersion: "版本不匹配，当前版本 {0}，最低支持版本为 {1}"
	},
	table: {
		emptyText: "暂无数据",
		allTitle: "全选/取消",
		seqTitle: "序号",
		actionTitle: "操作",
		confirmFilter: "筛选",
		resetFilter: "重置",
		allFilter: "全部",
		sortAsc: "升序：最低到最高",
		sortDesc: "降序：最高到最低",
		filter: "对所选的列启用筛选",
		impSuccess: "成功导入 {0} 条记录",
		expLoading: "正在导出中",
		expSuccess: "导出成功",
		expError: "导出失败",
		expFilename: "导出_{0}",
		expOriginFilename: "导出_源_{0}",
		customTitle: "列设置",
		customAll: "全部",
		customConfirm: "确认",
		customClose: "关闭",
		customCancel: "取消",
		customRestore: "恢复默认",
		maxFixedCol: "最大冻结列的数量不能超过 {0} 个",
		maxGroupCol: "最大分组字段的数量不能超过 {0} 个",
		dragTip: "移动：{0}",
		resizeColTip: "宽：{0} 像素",
		resizeRowTip: "高：{0} 像素",
		rowGroupContentTotal: "{0}（{1}）",
		menuLoading: "加载中..."
	},
	grid: {
		selectOneRecord: "请至少选择一条记录！",
		deleteSelectRecord: "您确定要删除所选记录吗？",
		removeSelectRecord: "您确定要移除所选记录吗？",
		dataUnchanged: "数据未改动！",
		delSuccess: "成功删除所选记录！",
		saveSuccess: "保存成功！",
		operError: "发生错误，操作失败！"
	},
	select: {
		clear: "清除",
		allChecked: "全选",
		total: "{0} / {1}",
		search: "搜索",
		loadingText: "加载中...",
		emptyText: "暂无数据",
		maxSize: "最大可选择的数量不能超过 {0} 个",
		overSizeErr: "已超出最大可选数量 {0} 个，超出部分将被忽略！",
		searchEmpty: "未匹配到数据！"
	},
	tree: {
		searchEmpty: "未匹配到数据！",
		dragTip: "移动：{0}"
	},
	treeSelect: {
		clearChecked: "清除",
		allChecked: "全选",
		allExpand: "全部展开",
		clearExpand: "全部收起",
		total: "已选 {0}",
		search: "搜索",
		emptyText: "暂无数据"
	},
	pager: {
		goto: "前往",
		gotoTitle: "页数",
		pagesize: "{0}条/页",
		total: "共 {0} 条记录",
		pageClassifier: "页",
		homePage: "首页",
		homePageTitle: "首页",
		prevPage: "上一页",
		prevPageTitle: "上一页",
		nextPage: "下一页",
		nextPageTitle: "下一页",
		prevJump: "向上跳页",
		prevJumpTitle: "向上跳页",
		nextJump: "向下跳页",
		nextJumpTitle: "向下跳页",
		endPage: "末页",
		endPageTitle: "末页"
	},
	alert: { title: "系统提示" },
	button: {
		confirm: "确认",
		cancel: "取消",
		clear: "清除"
	},
	filter: { search: "搜索" },
	custom: {
		cstmTitle: "列设置",
		cstmRestore: "恢复默认",
		cstmCancel: "取消",
		cstmConfirm: "确定",
		cstmConfirmRestore: "请确认是否恢复成默认列配置？",
		cstmDragTarget: "移动：{0}",
		setting: {
			colSort: "排序",
			sortHelpTip: "点击并拖动图标可以调整顺序",
			colTitle: "列标题",
			colResizable: "列宽（像素）",
			colVisible: "是否显示",
			colFixed: "冻结列",
			colFixedMax: "冻结列（最多 {0} 列）",
			fixedLeft: "左侧",
			fixedUnset: "不设置",
			fixedRight: "右侧"
		}
	},
	import: {
		modes: {
			covering: "覆盖方式（直接覆盖表格数据）",
			insert: "底部追加（在表格的底部追加新数据）",
			insertTop: "顶部追加（在表格的顶部追加新数据）",
			insertBottom: "底部追加（在表格的底部追加新数据）"
		},
		impTitle: "导入数据",
		impFile: "文件名",
		impSelect: "选择文件",
		impType: "文件类型",
		impOpts: "参数设置",
		impMode: "导入模式",
		impConfirm: "导入",
		impCancel: "取消"
	},
	export: {
		types: {
			csv: "CSV (逗号分隔)(*.csv)",
			html: "网页(*.html)",
			xml: "XML 数据(*.xml)",
			txt: "文本文件(制表符分隔)(*.txt)",
			xls: "Excel 97-2003 工作簿(*.xls)",
			xlsx: "Excel 工作簿(*.xlsx)",
			pdf: "PDF (*.pdf)"
		},
		modes: {
			empty: "空数据",
			current: "当前数据（当前页的数据）",
			selected: "选中数据（当前页选中的数据）",
			all: "全量数据（包括所有分页的数据）"
		},
		printTitle: "打印数据",
		expTitle: "导出数据",
		expName: "文件名",
		expNamePlaceholder: "请输入文件名",
		expSheetName: "标题",
		expSheetNamePlaceholder: "请输入标题",
		expType: "保存类型",
		expMode: "选择数据",
		expCurrentColumn: "全部字段",
		expColumn: "选择字段",
		expOpts: "参数设置",
		expOptHeader: "表头",
		expHeaderTitle: "是否需要表头",
		expOptFooter: "表尾",
		expFooterTitle: "是否需要表尾",
		expOptColgroup: "分组表头",
		expOptTitle: "列标题",
		expTitleTitle: "是否为列标题，否则显示为列的字段名",
		expColgroupTitle: "如果存在，则支持带有分组结构的表头",
		expOptMerge: "合并",
		expMergeTitle: "如果存在，则支持带有合并结构的单元格",
		expOptAllExpand: "展开树",
		expAllExpandTitle: "如果存在，则支持将带有层级结构的数据全部展开",
		expOptUseStyle: "样式",
		expUseStyleTitle: "如果存在，则支持带样式的单元格",
		expOptOriginal: "源数据",
		expOriginalTitle: "如果为源数据，则支持导入到表格中",
		expPrint: "打印",
		expConfirm: "导出",
		expCancel: "取消"
	},
	modal: {
		errTitle: "错误提示",
		zoomMin: "最小化",
		zoomIn: "最大化",
		zoomOut: "还原",
		close: "关闭",
		miniMaxSize: "最小化窗口的数量不能超过 {0} 个",
		footPropErr: "show-footer 仅用于启用表尾，需配合 show-confirm-button | show-cancel-button | 插槽使用"
	},
	drawer: { close: "关闭" },
	form: {
		folding: "收起",
		unfolding: "展开"
	},
	toolbar: {
		import: "导入",
		export: "导出",
		print: "打印",
		refresh: "刷新",
		zoomIn: "全屏",
		zoomOut: "还原",
		custom: "列设置",
		customAll: "全部",
		customConfirm: "确认",
		customRestore: "重置",
		fixedLeft: "冻结在左侧",
		fixedRight: "冻结在右侧",
		cancelFixed: "取消冻结列"
	},
	datePicker: { yearTitle: "{0} 年" },
	dateRangePicker: { pleaseRange: "请选择开始日期与结束日期" },
	input: { date: {
		m1: "01 月",
		m2: "02 月",
		m3: "03 月",
		m4: "04 月",
		m5: "05 月",
		m6: "06 月",
		m7: "07 月",
		m8: "08 月",
		m9: "09 月",
		m10: "10 月",
		m11: "11 月",
		m12: "12 月",
		quarterLabel: "{0} 年",
		monthLabel: "{0} 年",
		dayLabel: "{0} 年 {1}",
		labelFormat: {
			date: "yyyy-MM-dd",
			time: "HH:mm:ss",
			datetime: "yyyy-MM-dd HH:mm:ss",
			week: "yyyy 年第 WW 周",
			month: "yyyy-MM",
			quarter: "yyyy 年第 q 季度",
			year: "yyyy"
		},
		weeks: {
			w: "",
			w0: "周日",
			w1: "周一",
			w2: "周二",
			w3: "周三",
			w4: "周四",
			w5: "周五",
			w6: "周六"
		},
		months: {
			m0: "一月",
			m1: "二月",
			m2: "三月",
			m3: "四月",
			m4: "五月",
			m5: "六月",
			m6: "七月",
			m7: "八月",
			m8: "九月",
			m9: "十月",
			m10: "十一月",
			m11: "十二月"
		},
		quarters: {
			q1: "第一季度",
			q2: "第二季度",
			q3: "第三季度",
			q4: "第四季度"
		}
	} },
	calendar: {
		yearLabel: "{0} 年",
		monthLabel: "{0} 月"
	},
	numberInput: { currencySymbol: "¥" },
	imagePreview: {
		popupTitle: "预览",
		operBtn: {
			zoomOut: "缩小",
			zoomIn: "放大",
			pctFull: "等比例缩放",
			pct11: "显示原始尺寸",
			rotateLeft: "向左旋转",
			rotateRight: "向右旋转",
			print: "点击打印图片",
			download: "点击下载图片"
		}
	},
	upload: {
		fileBtnText: "点击或拖拽上传",
		imgBtnText: "点击或拖拽上传",
		dragPlaceholder: "请把文件拖放到这个区域即可上传",
		imgSizeHint: "单张{0}",
		imgCountHint: "最多{0}张",
		fileTypeHint: "支持 {0} 文件类型",
		fileSizeHint: "单个文件大小不超过{0}",
		fileCountHint: "最多可上传{0}个文件",
		uploadTypeErr: "文件类型不匹配！",
		overCountErr: "最多只能选择{0}个文件！",
		overCountExtraErr: "已超出最大数量{0}个，超出的{1}个文件将被忽略！",
		overSizeErr: "文件大小最大不能超过{0}！",
		manualUpload: "点击上传",
		reUpload: "重新上传",
		uploadProgress: "上传中 {0}%",
		uploadErr: "上传失败",
		uploadSuccess: "上传成功",
		moreBtnText: "更多（{0}）",
		moreImgBtnText: "图片（{0}）",
		moreFileBtnText: "文件（{0}）",
		viewItemTitle: "点击查看",
		morePopup: {
			readTitle: "查看列表",
			imageTitle: "上传图片",
			fileTitle: "上传文件"
		}
	},
	empty: { defText: "暂无数据" },
	colorPicker: {
		clear: "清除",
		confirm: "确认",
		copySuccess: "已复制到剪贴板：{0}",
		hex: "十六进制"
	},
	formDesign: {
		formName: "表单名称",
		defFormTitle: "未命名的表单",
		widgetPropTab: "控件属性",
		widgetFormTab: "表单属性",
		error: {
			wdFormUni: "该类型的控件在表单中只允许添加一个",
			wdSubUni: "该类型的控件在子表中只允许添加一个"
		},
		styleSetting: {
			btn: "样式设置",
			title: "表单的样式设置",
			layoutTitle: "控件布局",
			verticalLayout: "上下布局",
			horizontalLayout: "横向布局",
			styleTitle: "标题样式",
			boldTitle: "标题加粗",
			fontBold: "加粗",
			fontNormal: "常规",
			colonTitle: "显示冒号",
			colonVisible: "显示",
			colonHidden: "隐藏",
			alignTitle: "对齐方式",
			widthTitle: "标题宽度",
			alignLeft: "居左",
			alignRight: "居右",
			unitPx: "像素",
			unitPct: "百分比"
		},
		widget: {
			group: {
				base: "基础控件",
				layout: "布局控件",
				system: "系统控件",
				module: "模块控件",
				chart: "图表控件",
				advanced: "高级控件"
			},
			copyTitle: "副本_{0}",
			component: {
				input: "输入框",
				textarea: "文本域",
				select: "下拉选择",
				row: "一行多列",
				title: "标题",
				text: "文本",
				subtable: "子表",
				VxeSwitch: "是/否",
				VxeInput: "输入框",
				VxeNumberInput: "数字",
				VxeDatePicker: "日期",
				VxeTextarea: "文本域",
				VxeSelect: "下拉选择",
				VxeTreeSelect: "树形选择",
				VxeRadioGroup: "单选框",
				VxeCheckboxGroup: "复选框",
				VxeUploadFile: "文件",
				VxeUploadImage: "图片",
				VxeRate: "评分",
				VxeSlider: "滑块"
			}
		},
		widgetProp: {
			name: "控件名称",
			placeholder: "提示语",
			required: "必填校验",
			multiple: "允许多选",
			displaySetting: {
				name: "显示设置",
				pc: "电脑端",
				mobile: "手机端",
				visible: "显示",
				hidden: "隐藏"
			},
			dataSource: {
				name: "数据源",
				defValue: "选项{0}",
				addOption: "添加选项",
				batchEditOption: "批量编辑",
				batchEditTip: "每行对应一个选项，支持从表格、Excel、WPS 中直接复制粘贴。",
				batchEditSubTip: "每行对应一个选项，如果是分组，子项可以是空格或制表键开头，支持从表格、Excel、WPS 中直接复制粘贴。",
				buildOption: "生成选项"
			},
			rowProp: {
				colSize: "列数",
				col2: "两列",
				col3: "三列",
				col4: "四列",
				col6: "六列",
				layout: "布局"
			},
			textProp: {
				name: "内容",
				alignTitle: "对齐方式",
				alignLeft: "居左",
				alignCenter: "居中",
				alignRight: "居右",
				colorTitle: "字体颜色",
				sizeTitle: "字体大小",
				boldTitle: "字体加粗",
				fontNormal: "常规",
				fontBold: "加粗"
			},
			subtableProp: {
				seqTitle: "序号",
				showSeq: "显示序号",
				showCheckbox: "允许多选",
				errSubDrag: "子表不支持该控件，请使用其他控件",
				colPlace: "将控件拖拽进来"
			},
			uploadProp: {
				limitFileCount: "文件数量限制",
				limitFileSize: "文件大小限制",
				multiFile: "允许上传多个文件",
				limitImgCount: "图片数量限制",
				limitImgSize: "图片大小限制",
				multiImg: "允许上传多张图片"
			}
		}
	},
	listDesign: {
		fieldSettingTab: "字段设置",
		listSettingTab: "参数设置",
		searchTitle: "查询条件",
		listTitle: "列表字段",
		searchField: "查询字段",
		listField: "列表字段",
		activeBtn: {
			ActionButtonUpdate: "编辑",
			ActionButtonDelete: "删除"
		},
		search: {
			addBtn: "编辑",
			emptyText: "未配置查询条件",
			editPopupTitle: "编辑查询字段"
		},
		searchPopup: {
			colTitle: "标题",
			saveBtn: "保存"
		}
	},
	text: {
		copySuccess: "已复制到剪贴板",
		copyError: "当前环境不支持该操作"
	},
	countdown: { formats: {
		yyyy: "年",
		MM: "月",
		dd: "天",
		HH: "时",
		mm: "分",
		ss: "秒"
	} },
	contextMenu: { loadingText: "加载中..." },
	gantt: {
		tFullFormat: {
			year: "{yyyy}年",
			quarter: "{yyyy}年第{q}季度",
			month: "{yyyy}年{MM}月",
			week: "{yyyy}年第{W}周",
			day: "{yyyy}年{MM}月 {E}",
			date: "{yyyy}年{MM}月{dd}日",
			hour: "{yyyy}年{MM}月{dd}日{HH}时",
			minute: "{yyyy}年{MM}月{dd}日{HH}时{mm}分",
			second: "{yyyy}年{MM}月{dd}日{HH}时{mm}分{ss}秒"
		},
		tSimpleFormat: {
			year: "{yyyy}年",
			quarter: "{q}季度",
			month: "{M}月",
			week: "{W}周",
			day: "{d}",
			date: "{d}",
			hour: "{HH}",
			minute: "{mm}",
			second: "{ss}"
		},
		dayss: {
			w0: "日",
			w1: "一",
			w2: "二",
			w3: "三",
			w4: "四",
			w5: "五",
			w6: "六"
		}
	},
	plugins: {
		extendCellArea: {
			area: {
				mergeErr: "无法对合并单元格进行该操作",
				multiErr: "无法对多重选择区域进行该操作",
				selectErr: "无法操作指定区域的单元格",
				extendErr: "如果延伸的区域包含被合并的单元格，所有合并的单元格需大小相同",
				pasteMultiErr: "无法粘贴，需要相同大小的复制的区域和粘贴的区域才能执行此操作",
				cpInvalidErr: "该操作无法进行，您选择的区域中存在被禁止的列（{0}）"
			},
			fnr: {
				title: "查找和替换",
				titleFd: "查找",
				titleRe: "替换",
				findLabel: "查找",
				replaceLabel: "替换",
				findTitle: "查找内容：",
				replaceTitle: "替换为：",
				tabs: {
					find: "查找",
					replace: "替换"
				},
				filter: {
					re: "正则表达式",
					whole: "全词匹配",
					sensitive: "区分大小写"
				},
				btns: {
					findNext: "查找下一个",
					findAll: "查找全部",
					replace: "替换",
					replaceAll: "替换全部",
					cancel: "取消"
				},
				header: {
					seq: "#",
					cell: "单元格",
					value: "值"
				},
				body: {
					row: "行：{0}",
					col: "列：{0}"
				},
				empty: "(空值)",
				reError: "无效的正则表达式",
				recordCount: "已找到 {0} 个单元格",
				notCell: "找不到匹配的单元格",
				replaceSuccess: "成功替换 {0} 个单元格"
			}
		},
		extendPivotTable: {
			aggregation: {
				grouping: "分组",
				values: "值",
				groupPlaceholder: "拖至此处进行分组",
				valuesPlaceholder: "拖至此处进行聚合",
				dragExistCol: "该列已存在",
				sortHelpTip: "点击并拖动图标可以调整顺序"
			},
			aggFuncs: {
				sum: "求和",
				count: "计数",
				avg: "平均值",
				min: "最小值",
				max: "最大值",
				first: "首个值",
				last: "末尾值"
			}
		},
		filterComplexInput: {
			menus: {
				fixedColumn: "冻结列",
				fixedGroup: "冻结分组",
				cancelFixed: "取消冻结",
				fixedLeft: "冻结左侧",
				fixedRight: "冻结右侧"
			},
			cases: {
				equal: "等于",
				gt: "大于",
				lt: "小于",
				begin: "开头是",
				endin: "结尾是",
				include: "包含",
				isSensitive: "区分大小写"
			}
		},
		filterCombination: {
			menus: {
				sort: "排序",
				clearSort: "清除排序",
				sortAsc: "升序",
				sortDesc: "降序",
				fixedColumn: "冻结列",
				fixedGroup: "冻结分组",
				cancelFixed: "取消冻结",
				fixedLeft: "冻结左侧",
				fixedRight: "冻结右侧",
				clearFilter: "清除筛选",
				textOption: "文本筛选",
				numberOption: "数值筛选",
				dateOption: "日期筛选"
			},
			popup: {
				title: "自定义筛选的方式",
				currColumnTitle: "显示行：",
				and: "与",
				or: "或",
				describeHtml: "可用 ? 代表单个字符<br/>用 * 代表任意多个字符"
			},
			cases: {
				equal: "等于",
				unequal: "不等于",
				gt: "大于",
				ge: "大于或等于",
				lt: "小于",
				le: "小于或等于",
				begin: "开头是",
				notbegin: "开头不是",
				endin: "结尾是",
				notendin: "结尾不是",
				include: "包含",
				exclude: "不包含",
				between: "介于",
				isAfter: "在以下日期之后",
				eqAfter: "在以下日期之后或与之相同",
				isBefore: "在以下日期之前",
				eqBefore: "在以下日期之前或与之相同",
				top10: "前10项",
				aboveAverage: "高于平均值",
				belowAverage: "低于平均值",
				before: "之前",
				after: "之后",
				tomorrow: "明天",
				today: "今天",
				yesterday: "昨天",
				nextWeek: "下周",
				thisWeek: "本周",
				lastWeek: "上周",
				nextMonth: "下月",
				thisMonth: "本月",
				lastMonth: "上月",
				nextYear: "明年",
				thisYear: "今年",
				lastYear: "去年",
				yearToDate: "本年度截止到现在",
				custom: "自定义筛选",
				insensitive: "不区分大小写",
				isSensitive: "区分大小写"
			},
			empty: "(空白)",
			notData: "无匹配项"
		}
	},
	pro: {
		area: {
			mergeErr: "无法对合并单元格进行该操作",
			multiErr: "无法对多重选择区域进行该操作",
			extendErr: "如果延伸的区域包含被合并的单元格，所有合并的单元格需大小相同",
			pasteMultiErr: "无法粘贴，需要相同大小的复制的区域和粘贴的区域才能执行此操作"
		},
		fnr: {
			title: "查找和替换",
			titleFd: "查找",
			titleRe: "替换",
			findLabel: "查找",
			replaceLabel: "替换",
			findTitle: "查找内容：",
			replaceTitle: "替换为：",
			tabs: {
				find: "查找",
				replace: "替换"
			},
			filter: {
				re: "正则表达式",
				whole: "全词匹配",
				sensitive: "区分大小写"
			},
			btns: {
				findNext: "查找下一个",
				findAll: "查找全部",
				replace: "替换",
				replaceAll: "替换全部",
				cancel: "取消"
			},
			header: {
				seq: "#",
				cell: "单元格",
				value: "值"
			},
			empty: "(空值)",
			reError: "无效的正则表达式",
			recordCount: "已找到 {0} 个单元格",
			notCell: "找不到匹配的单元格",
			replaceSuccess: "成功替换 {0} 个单元格"
		}
	},
	renderer: {
		search: "搜索",
		cases: {
			equal: "等于",
			unequal: "不等于",
			gt: "大于",
			ge: "大于或等于",
			lt: "小于",
			le: "小于或等于",
			begin: "开头是",
			notbegin: "开头不是",
			endin: "结尾是",
			notendin: "结尾不是",
			include: "包含",
			exclude: "不包含",
			between: "介于",
			top10: "前10项",
			aboveAverage: "高于平均值",
			belowAverage: "低于平均值",
			custom: "自定义筛选",
			insensitive: "不区分大小写",
			isSensitive: "区分大小写"
		},
		combination: {
			menus: {
				sort: "排序",
				clearSort: "清除排序",
				sortAsc: "升序",
				sortDesc: "降序",
				fixedColumn: "冻结列",
				fixedGroup: "冻结分组",
				cancelFixed: "取消冻结",
				fixedLeft: "冻结到左侧",
				fixedRight: "冻结到右侧",
				clearFilter: "清除筛选",
				textOption: "文本筛选",
				numberOption: "数值筛选",
				dateOption: "日期筛选"
			},
			popup: {
				title: "自定义筛选的方式",
				currColumnTitle: "显示行：",
				and: "与",
				or: "或",
				describeHtml: "可用 ? 代表单个字符<br/>用 * 代表任意多个字符"
			},
			empty: "(空白)",
			notData: "无匹配项"
		}
	}
} };
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/alert/src/alert.js
var import_xe_utils = /* @__PURE__ */ __toESM(require_xe_utils());
var alert_default = defineVxeComponent({
	name: "VxeAlert",
	props: {
		title: {
			type: [String, Number],
			default: () => getConfig().alert.title
		},
		content: [String, Number],
		status: String,
		showIcon: Boolean,
		showClose: Boolean,
		icon: {
			type: String,
			default: () => getConfig().alert.icon
		}
	},
	emits: ["close"],
	setup(props, context) {
		const { emit, slots } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeMaps = {};
		const $xeAlert = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const alertMethods = { dispatchEvent(type, params, evnt) {
			emit(type, createEvent(evnt, { $alert: $xeAlert }, params));
		} };
		const closeEvent = (evnt) => {
			alertMethods.dispatchEvent("close", {}, evnt);
		};
		Object.assign($xeAlert, alertMethods, {});
		const renderVN = () => {
			const { status, content, icon, title, showIcon, showClose } = props;
			const defaultSlot = slots.default;
			const titleSlot = slots.title;
			const iconSlot = slots.icon;
			return h("div", {
				ref: refElem,
				class: ["vxe-alert", { [`theme--${status}`]: status }]
			}, [
				iconSlot || showIcon && status || icon ? h("div", { class: "vxe-alert--icon" }, iconSlot ? getSlotVNs$1(iconSlot({})) : [h("i", { class: icon || getIcon()[`ALERT_${status === null || status === void 0 ? void 0 : status.toUpperCase()}`] })]) : renderEmptyElement($xeAlert),
				h("div", { class: "vxe-alert--body" }, [titleSlot || title ? h("div", { class: "vxe-alert--title" }, titleSlot ? getSlotVNs$1(titleSlot({})) : import_xe_utils.default.toValueString(title)) : renderEmptyElement($xeAlert), h("div", { class: "vxe-alert--content" }, defaultSlot ? getSlotVNs$1(defaultSlot({})) : import_xe_utils.default.toValueString(content))]),
				showClose ? h("div", {
					class: "vxe-alert--close-btn",
					onClick: closeEvent
				}, [h("i", { class: getIcon().ALERT_CLOSE })]) : renderEmptyElement($xeAlert)
			]);
		};
		$xeAlert.renderVN = renderVN;
		return $xeAlert;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/alert/index.js
var VxeAlert = Object.assign({}, alert_default, { install(app) {
	app.component(alert_default.name, alert_default);
} });
dynamicApp.use(VxeAlert);
VxeUI.component(alert_default);
var Alert = VxeAlert;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/anchor/src/util.js
function assembleAnchorLink($xeAnchor, elem, linkConfig, $xeParentAnchorLink) {
	const staticLinks = $xeAnchor.reactData.staticLinks;
	const parentElem = elem.parentNode;
	const parentLinkConfig = $xeParentAnchorLink ? $xeParentAnchorLink.linkConfig : null;
	const parentLinks = parentLinkConfig ? parentLinkConfig.children : staticLinks;
	if (parentElem && parentLinks) {
		parentLinks.splice(import_xe_utils.default.arrayIndexOf(parentElem.children, elem), 0, linkConfig);
		$xeAnchor.reactData.staticLinks = staticLinks.slice(0);
	}
}
function destroyAnchorLink($xeAnchor, linkConfig) {
	const staticLinks = $xeAnchor.reactData.staticLinks;
	const matchObj = import_xe_utils.default.findTree(staticLinks, (item) => item.id === linkConfig.id, { children: "children" });
	if (matchObj) matchObj.items.splice(matchObj.index, 1);
	$xeAnchor.reactData.staticLinks = staticLinks.slice(0);
}
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/anchor/src/anchor-link.js
var anchor_link_default = defineVxeComponent({
	name: "VxeAnchorLink",
	props: {
		content: [String, Number],
		title: [String, Number],
		href: String
	},
	emits: [],
	setup(props, context) {
		const { slots } = context;
		const $xeAnchor = inject("$xeAnchor", null);
		const $xeParentAnchorLink = inject("$xeAnchorLink", null);
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const reactData = reactive({});
		const linkConfig = reactive({
			id: xID,
			href: props.href,
			children: []
		});
		const refMaps = { refElem };
		const computeIsActive = computed(() => {
			const { href } = props;
			if ($xeAnchor) return $xeAnchor.reactData.activeHref === href;
			return null;
		});
		const computeMaps = {};
		const $xeAnchorLink = {
			xID,
			props,
			context,
			reactData,
			linkConfig,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const clickEvent = (event) => {
			const { href } = props;
			if ($xeAnchor) $xeAnchor.handleClickLink(event, href);
		};
		watch(() => props.href, (val) => {
			linkConfig.href = val;
		});
		onMounted(() => {
			const elem = refElem.value;
			if ($xeAnchor && elem) assembleAnchorLink($xeAnchor, elem, linkConfig, $xeParentAnchorLink);
		});
		onUnmounted(() => {
			if ($xeAnchor) destroyAnchorLink($xeAnchor, linkConfig);
		});
		const renderVN = () => {
			const { href, content, title } = props;
			const defaultSlot = slots.default;
			const subSlot = slots.sub;
			const isActive = computeIsActive.value;
			return h("div", {
				ref: refElem,
				class: ["vxe-anchor-link", { "is--active": isActive }]
			}, [h("a", {
				class: "vxe-anchor-link--item",
				href,
				title,
				onClick: clickEvent
			}, defaultSlot ? defaultSlot({}) : import_xe_utils.default.toValueString(content)), subSlot ? h("div", { class: "vxe-anchor-link--sub-items" }, subSlot({})) : renderEmptyElement($xeAnchorLink)]);
		};
		provide("$xeAnchorLink", $xeAnchorLink);
		$xeAnchorLink.renderVN = renderVN;
		return $xeAnchorLink;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/anchor/src/anchor.js
var anchor_default = defineVxeComponent({
	name: "VxeAnchor",
	props: {
		modelValue: String,
		options: Array,
		container: [
			String,
			Object,
			Function
		],
		showMarker: {
			type: Boolean,
			default: true
		}
	},
	emits: [
		"update:modelValue",
		"change",
		"click"
	],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const refMarkerElem = ref();
		const reactData = reactive({
			activeHref: null,
			staticLinks: [],
			containerElem: null
		});
		const refMaps = { refElem };
		const computeAllHrefList = computed(() => {
			const list = [];
			import_xe_utils.default.eachTree(reactData.staticLinks, (item) => {
				list.push(item.href || "");
			}, { children: "children" });
			return list;
		});
		const computeMaps = {};
		const $xeAnchor = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const anchorMethods = { dispatchEvent(type, params, evnt) {
			emit(type, createEvent(evnt, { $anchor: $xeAnchor }, params));
		} };
		const getContainerElem = () => {
			const { container } = props;
			if (container) {
				if (import_xe_utils.default.isElement(container)) return container;
				if (import_xe_utils.default.isString(container)) return document.querySelector(container);
				if (import_xe_utils.default.isFunction(container)) return container({ $anchor: $xeAnchor });
			}
			return null;
		};
		const emitEvent = (value) => {
			reactData.activeHref = value;
			emit("update:modelValue", value);
		};
		const handleContainerScrollEvent = () => {
			const allHrefList = computeAllHrefList.value;
			const { containerElem } = reactData;
			if (containerElem) {
				const wrapperElList = containerElem.querySelectorAll(allHrefList.map((href) => `${href}`).join(","));
				for (let i = 0; i < wrapperElList.length; i++) {
					const wrapperEl = wrapperElList[i];
					if (wrapperEl.getBoundingClientRect().top > 0) {
						reactData.activeHref = `#${wrapperEl.id}`;
						break;
					}
				}
			}
		};
		const removeContainerElemScroll = () => {
			const { containerElem } = reactData;
			if (containerElem) containerElem.removeEventListener("scroll", handleContainerScrollEvent);
		};
		const updateContainerElem = () => {
			const containerElem = getContainerElem();
			reactData.containerElem = containerElem;
			if (containerElem) containerElem.addEventListener("scroll", handleContainerScrollEvent, { passive: false });
		};
		const updateMarkerPos = () => {
			nextTick(() => {
				const { activeHref } = reactData;
				const elem = refElem.value;
				const markerEl = refMarkerElem.value;
				if (elem && markerEl) {
					if (activeHref) {
						const linkEl = elem.querySelector(`[href="${activeHref}"]`);
						if (linkEl) {
							const { top } = getOffsetPos(linkEl, elem);
							markerEl.style.top = `${top}px`;
						}
					}
				}
			});
		};
		Object.assign($xeAnchor, anchorMethods, { handleClickLink(evnt, href) {
			evnt.preventDefault();
			const targetEl = document.getElementById(`${href}`.replace("#", ""));
			if (targetEl) targetEl.scrollIntoView({ behavior: "smooth" });
			emitEvent(href);
			anchorMethods.dispatchEvent("click", { href }, evnt);
		} });
		const renderSubItems = (options) => {
			const itemVNs = [];
			if (options) options.forEach((item) => {
				const subItems = item.children;
				if (subItems && subItems.length) itemVNs.push(h(anchor_link_default, {
					content: item.content,
					title: item.title,
					href: item.href
				}, { sub: () => renderSubItems(subItems) }));
				else itemVNs.push(h(anchor_link_default, {
					content: item.content,
					title: item.title,
					href: item.href
				}));
			});
			return itemVNs;
		};
		const renderVN = () => {
			const { options, showMarker } = props;
			const defaultSlot = slots.default;
			return h("div", {
				ref: refElem,
				class: ["vxe-anchor", { "is--marker": showMarker }]
			}, [h("div", { class: "vxe-anchor--list" }, defaultSlot ? defaultSlot({}) : renderSubItems(options)), showMarker ? h("div", {
				ref: refMarkerElem,
				class: "vxe-anchor--marker"
			}) : renderEmptyElement($xeAnchor)]);
		};
		watch(() => props.modelValue, (val) => {
			reactData.activeHref = val;
		});
		watch(() => reactData.activeHref, () => {
			updateMarkerPos();
		});
		watch(() => props.container, () => {
			removeContainerElemScroll();
			updateContainerElem();
		});
		onMounted(() => {
			nextTick(() => {
				updateContainerElem();
			});
		});
		onBeforeUnmount(() => {
			removeContainerElemScroll();
		});
		provide("$xeAnchor", $xeAnchor);
		$xeAnchor.renderVN = renderVN;
		return $xeAnchor;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/anchor/index.js
var VxeAnchor = Object.assign({}, anchor_default, { install(app) {
	app.component(anchor_default.name, anchor_default);
} });
dynamicApp.use(VxeAnchor);
VxeUI.component(anchor_default);
var Anchor = VxeAnchor;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/anchor-link/index.js
var VxeAnchorLink = Object.assign({}, anchor_link_default, { install(app) {
	app.component(anchor_link_default.name, anchor_link_default);
} });
dynamicApp.use(VxeAnchorLink);
VxeUI.component(anchor_link_default);
var AnchorLink = VxeAnchorLink;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/avatar/src/avatar.js
var avatar_default = defineVxeComponent({
	name: "VxeAvatar",
	props: {
		count: [String, Number],
		dot: Boolean,
		content: [String, Number],
		icon: String,
		src: String,
		width: [String, Number],
		height: [String, Number],
		circle: {
			type: Boolean,
			default: () => getConfig().avatar.circle
		},
		status: {
			type: String,
			default: () => getConfig().avatar.status
		},
		size: {
			type: String,
			default: () => getConfig().avatar.size || getConfig().size
		}
	},
	emits: [],
	setup(props, context) {
		const { emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const refElem = ref();
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeAvatarStyle = computed(() => {
			const { width, height } = props;
			const stys = {};
			if (width) stys.width = toCssUnit(width);
			if (height) stys.height = toCssUnit(height);
			return stys;
		});
		const computeCountNum = computed(() => {
			const { count } = props;
			return count ? import_xe_utils.default.toNumber(count) : 0;
		});
		const computeMaps = {};
		const $xeAvatar = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $avatar: $xeAvatar }, params));
		};
		Object.assign($xeAvatar, { dispatchEvent }, {});
		const renderContent = () => {
			const { icon, content, src } = props;
			if (icon) return h("span", { class: "vxe-avatar--icon" }, [h("i", { class: icon })]);
			if (content) return h("span", { class: "vxe-avatar--content" }, `${content}`);
			if (src) return h("img", {
				class: "vxe-avatar--img",
				src
			});
			return renderEmptyElement($xeAvatar);
		};
		const renderVN = () => {
			const { circle, dot, status } = props;
			const vSize = computeSize.value;
			const countNum = computeCountNum.value;
			const avatarStyle = computeAvatarStyle.value;
			return h("div", {
				ref: refElem,
				class: ["vxe-avatar", {
					[`size--${vSize}`]: vSize,
					[`theme--${status}`]: status,
					"is--circle": circle,
					"is--dot": dot
				}],
				style: avatarStyle
			}, [renderContent(), countNum ? h("span", { class: "vxe-avatar--count" }, countNum > 99 ? "99+" : `${countNum}`) : renderEmptyElement($xeAvatar)]);
		};
		$xeAvatar.renderVN = renderVN;
		return $xeAvatar;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/avatar/index.js
var VxeAvatar = Object.assign({}, avatar_default, { install(app) {
	app.component(avatar_default.name, avatar_default);
} });
dynamicApp.use(VxeAvatar);
VxeUI.component(avatar_default);
var Avatar = VxeAvatar;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/backtop/src/backtop.js
function createInternalData$10() {
	return { targetEl: null };
}
function createReactData$7() {
	return {
		showBtn: false,
		backtopZindex: 0
	};
}
var backtop_default = defineVxeComponent({
	name: "VxeBacktop",
	props: {
		target: String,
		size: {
			type: String,
			default: () => getConfig().backtop.size || getConfig().size
		},
		circle: {
			type: Boolean,
			default: () => getConfig().backtop.circle
		},
		right: {
			type: [String, Number],
			default: () => getConfig().backtop.right
		},
		bottom: {
			type: [String, Number],
			default: () => getConfig().backtop.bottom
		},
		status: {
			type: [String, Number],
			default: () => getConfig().backtop.status
		},
		icon: {
			type: String,
			default: () => getConfig().backtop.icon
		},
		showIcon: {
			type: Boolean,
			default: () => getConfig().backtop.showIcon
		},
		content: {
			type: [String, Number],
			default: () => getConfig().backtop.content
		},
		showContent: {
			type: Boolean,
			default: () => getConfig().backtop.showContent
		},
		showTop: {
			type: Boolean,
			default: () => getConfig().backtop.showTop
		},
		showBottom: {
			type: Boolean,
			default: () => getConfig().backtop.showBottom
		},
		shadow: {
			type: Boolean,
			default: () => getConfig().backtop.shadow
		},
		zIndex: {
			type: [String, Number],
			default: () => getConfig().backtop.zIndex
		},
		threshold: {
			type: [String, Number],
			default: () => getConfig().backtop.threshold
		},
		position: {
			type: String,
			default: () => getConfig().backtop.position
		}
	},
	emits: ["click"],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const { computeSize } = useSize(props);
		const internalData = createInternalData$10();
		const reactData = reactive(createReactData$7());
		const refMaps = { refElem };
		const computeWrapperStyle = computed(() => {
			const { right, bottom } = props;
			const { backtopZindex } = reactData;
			const stys = {};
			if (right) stys.right = toCssUnit(right);
			if (bottom) stys.bottom = toCssUnit(bottom);
			if (backtopZindex) stys.zIndex = backtopZindex;
			return stys;
		});
		const computeMaps = {};
		const $xeBacktop = {
			xID,
			props,
			context,
			internalData,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $backtop: $xeBacktop }, params));
		};
		const updateZIndex = () => {
			const { position, zIndex } = props;
			const { backtopZindex } = reactData;
			if (zIndex) reactData.backtopZindex = import_xe_utils.default.toNumber(zIndex);
			else if (position === "fixed") {
				if (backtopZindex < getLastZIndex()) reactData.backtopZindex = nextZIndex();
			}
		};
		const showBacktop = () => {
			updateZIndex();
			reactData.showBtn = true;
		};
		const hideBacktop = () => {
			reactData.showBtn = false;
		};
		const handleScrollEvent = (evnt) => {
			const { threshold } = props;
			if (evnt.currentTarget.scrollTop > Math.max(1, import_xe_utils.default.toNumber(threshold))) showBacktop();
			else hideBacktop();
		};
		const handleToTop = () => {
			const { targetEl } = internalData;
			if (!targetEl) return;
			const scrollTop = targetEl.scrollTop;
			if (scrollTop > 0) {
				requestAnimationFrame(handleToTop);
				const currScrollTop = scrollTop - Math.max(12, scrollTop / 6);
				targetEl.scrollTop = currScrollTop > 10 ? currScrollTop : 0;
			}
		};
		const removeScrollEvent = () => {
			const { targetEl } = internalData;
			if (targetEl) targetEl.removeEventListener("scroll", handleScrollEvent);
		};
		const addScrollEvent = () => {
			const { targetEl } = internalData;
			if (targetEl) targetEl.addEventListener("scroll", handleScrollEvent, { passive: true });
		};
		const handleTargetElement = () => {
			nextTick(() => {
				const { target } = props;
				if (!target) {
					removeScrollEvent();
					errLog("vxe.error.reqProp", ["target"]);
					return;
				}
				if (import_xe_utils.default.isString(target)) {
					const tEl = document.querySelector(target);
					if (!tEl) errLog("vxe.error.errProp", [`target=${target}`, "body"]);
					const { targetEl } = internalData;
					if (targetEl !== tEl) {
						removeScrollEvent();
						internalData.targetEl = tEl;
						addScrollEvent();
					}
				}
			});
		};
		const clickEvent = (evnt) => {
			handleToTop();
			dispatchEvent("click", {}, evnt);
		};
		Object.assign($xeBacktop, { dispatchEvent }, {});
		const renderVN = () => {
			const { circle, position, status, icon, showIcon, content, showContent, showTop, showBottom, shadow } = props;
			const { showBtn } = reactData;
			const wrapperStyle = computeWrapperStyle.value;
			const vSize = computeSize.value;
			const defaultSlot = slots.default;
			const topSlot = slots.top;
			const bottomSlot = slots.bottom;
			return h("div", {
				ref: refElem,
				class: [
					"vxe-backtop",
					position === "fixed" ? "is--" + position : "is--absolute",
					{
						[`size--${vSize}`]: vSize,
						"is--visible": showBtn
					}
				],
				style: wrapperStyle
			}, [
				showTop && topSlot ? h("div", { class: "vxe-backtop--top-wrapper" }, topSlot({})) : renderEmptyElement($xeBacktop),
				h("div", {
					class: "vxe-backtop--content-wrapper",
					onClick: clickEvent
				}, [defaultSlot ? defaultSlot({}) : [h(VxeButton, {
					circle,
					status,
					shadow,
					icon: showIcon ? icon || getIcon().BACKTOP_TOP : "",
					content: showContent ? content : ""
				})]]),
				showBottom && bottomSlot ? h("div", { class: "vxe-backtop--bottom-wrapper" }, bottomSlot({})) : renderEmptyElement($xeBacktop)
			]);
		};
		watch(() => props.position, () => {
			updateZIndex();
		});
		watch(() => props.target, () => {
			handleTargetElement();
		});
		onMounted(() => {
			const { showTop } = props;
			if (showTop) updateZIndex();
			handleTargetElement();
		});
		onBeforeUnmount(() => {
			removeScrollEvent();
			import_xe_utils.default.assign(reactData, createReactData$7());
			import_xe_utils.default.assign(internalData, createInternalData$10());
		});
		$xeBacktop.renderVN = renderVN;
		return $xeBacktop;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/backtop/index.js
var VxeBacktop = Object.assign({}, backtop_default, { install(app) {
	app.component(backtop_default.name, backtop_default);
} });
dynamicApp.use(VxeBacktop);
VxeUI.component(backtop_default);
var Backtop = VxeBacktop;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/badge/src/badge.js
var badge_default = defineVxeComponent({
	name: "VxeBadge",
	props: {
		count: [String, Number],
		dot: Boolean,
		content: [String, Number],
		size: {
			type: String,
			default: () => getConfig().badge.size || getConfig().size
		}
	},
	emits: [],
	setup(props, context) {
		const { emit, slots } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const { computeSize } = useSize(props);
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeCountNum = computed(() => {
			const { count } = props;
			return count ? import_xe_utils.default.toNumber(count) : 0;
		});
		const computeMaps = {};
		const $xeBadge = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $badge: $xeBadge }, params));
		};
		Object.assign($xeBadge, { dispatchEvent }, {});
		const renderVN = () => {
			const { dot, content } = props;
			const vSize = computeSize.value;
			const countNum = computeCountNum.value;
			const defaultSlot = slots.default;
			return h("div", {
				ref: refElem,
				class: ["vxe-badge", {
					[`size--${vSize}`]: vSize,
					"is--dot": dot
				}]
			}, [defaultSlot || content ? h("div", { class: "vxe-badge--content" }, defaultSlot ? defaultSlot({}) : `${content || ""}`) : [], countNum ? h("span", { class: "vxe-badge--count" }, countNum > 99 ? "99+" : `${countNum}`) : renderEmptyElement($xeBadge)]);
		};
		$xeBadge.renderVN = renderVN;
		return $xeBadge;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/badge/index.js
var VxeBadge = Object.assign({}, badge_default, { install(app) {
	app.component(badge_default.name, badge_default);
} });
dynamicApp.use(VxeBadge);
VxeUI.component(badge_default);
var Badge = VxeBadge;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/breadcrumb/src/breadcrumb-item.js
var breadcrumb_item_default = defineVxeComponent({
	name: "VxeBreadcrumbItem",
	props: {
		title: String,
		routerLink: Object
	},
	emits: [],
	setup(props, context) {
		const { slots } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const $xeBreadcrumb = inject("$xeBreadcrumb", null);
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeSeparator = computed(() => {
			if ($xeBreadcrumb) return $xeBreadcrumb.props.separator;
			return "";
		});
		const clickEvent = (evnt) => {
			if ($xeBreadcrumb) {
				const item = {
					title: props.title,
					routerLink: props.routerLink
				};
				$xeBreadcrumb.handleClickLink(evnt, item);
			}
		};
		const computeMaps = {};
		const $xeBreadcrumbItem = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const renderVN = () => {
			const { title, routerLink } = props;
			const separator = computeSeparator.value;
			const defaultSlot = slots.default;
			return h("span", {
				ref: refElem,
				class: "vxe-breadcrumb-item",
				onClick: clickEvent
			}, [h("span", { class: "vxe-breadcrumb-item--content" }, [routerLink ? h(resolveComponent("router-link"), {
				class: "vxe-breadcrumb-item--content-link",
				title,
				to: routerLink
			}, { default() {
				return h("span", { class: "vxe-breadcrumb-item--content-text" }, defaultSlot ? defaultSlot({}) : `${title || ""}`);
			} }) : h("span", { class: "vxe-breadcrumb-item--content-text" }, defaultSlot ? defaultSlot({}) : `${title || ""}`)]), separator ? h("span", { class: "vxe-breadcrumb-item--separator" }, `${separator}`) : renderEmptyElement($xeBreadcrumbItem)]);
		};
		$xeBreadcrumbItem.renderVN = renderVN;
		return $xeBreadcrumbItem;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/breadcrumb/src/breadcrumb.js
var breadcrumb_default = defineVxeComponent({
	name: "VxeBreadcrumb",
	props: {
		separator: {
			type: String,
			default: () => getConfig().breadcrumb.separator
		},
		options: Array
	},
	emits: ["click"],
	setup(props, context) {
		const { emit, slots } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeMaps = {};
		const $xeBreadcrumb = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const breadcrumbMethods = { dispatchEvent(type, params, evnt) {
			emit(type, createEvent(evnt, { $breadcrumb: $xeBreadcrumb }, params));
		} };
		Object.assign($xeBreadcrumb, breadcrumbMethods, { handleClickLink(evnt, option) {
			breadcrumbMethods.dispatchEvent("click", { option }, evnt);
		} });
		const renderItems = () => {
			const { options } = props;
			if (options && options.length) return options.map((item) => {
				return h(breadcrumb_item_default, {
					title: item.title,
					routerLink: item.routerLink
				});
			});
			return [];
		};
		const renderVN = () => {
			const defaultSlot = slots.default;
			return h("div", {
				ref: refElem,
				class: "vxe-breadcrumb"
			}, defaultSlot ? defaultSlot({}) : renderItems());
		};
		provide("$xeBreadcrumb", $xeBreadcrumb);
		$xeBreadcrumb.renderVN = renderVN;
		return $xeBreadcrumb;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/breadcrumb/index.js
var VxeBreadcrumb = Object.assign({}, breadcrumb_default, { install(app) {
	app.component(breadcrumb_default.name, breadcrumb_default);
} });
dynamicApp.use(VxeBreadcrumb);
VxeUI.component(breadcrumb_default);
var Breadcrumb = VxeBreadcrumb;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/breadcrumb-item/index.js
var VxeBreadcrumbItem = Object.assign({}, breadcrumb_item_default, { install(app) {
	app.component(breadcrumb_item_default.name, breadcrumb_item_default);
} });
dynamicApp.use(VxeBreadcrumbItem);
VxeUI.component(breadcrumb_item_default);
var BreadcrumbItem = VxeBreadcrumbItem;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/button/src/button-group.js
var button_group_default = defineVxeComponent({
	name: "VxeButtonGroup",
	props: {
		options: Array,
		mode: String,
		status: String,
		round: Boolean,
		vertical: Boolean,
		circle: Boolean,
		align: String,
		className: [String, Function],
		disabled: Boolean,
		permissionCode: [String, Number],
		size: {
			type: String,
			default: () => getConfig().buttonGroup.size || getConfig().size
		}
	},
	emits: ["click", "contextmenu"],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const reactData = reactive({});
		const computeMaps = {};
		const $xeButtonGroup = {
			xID,
			props,
			context,
			reactData,
			getComputeMaps: () => computeMaps
		};
		useSize(props);
		const { computePermissionInfo } = usePermission(props);
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $buttonGroup: $xeButtonGroup }, params));
		};
		const buttonGroupMethods = { dispatchEvent };
		const buttonGroupPrivateMethods = { handleClick(params, evnt) {
			const { options } = props;
			const { name } = params;
			const option = options ? options.find((item) => item.name === name) : null;
			buttonGroupMethods.dispatchEvent("click", Object.assign(Object.assign({}, params), { option }), evnt);
		} };
		const contextmenuEvent = (evnt) => {
			dispatchEvent("contextmenu", {}, evnt);
		};
		Object.assign($xeButtonGroup, buttonGroupMethods, buttonGroupPrivateMethods);
		const renderVN = () => {
			const { className, options, vertical } = props;
			const permissionInfo = computePermissionInfo.value;
			const defaultSlot = slots.default;
			if (!permissionInfo.visible) return renderEmptyElement($xeButtonGroup);
			return h("div", {
				class: [
					"vxe-button-group",
					className ? import_xe_utils.default.isFunction(className) ? className({ $buttonGroup: $xeButtonGroup }) : className : "",
					{ "is--vertical": vertical }
				],
				onContextmenu: contextmenuEvent
			}, defaultSlot ? defaultSlot({}) : options ? options.map((item, index) => {
				return h(VxeButtonComponent, Object.assign({ key: index }, item));
			}) : []);
		};
		$xeButtonGroup.renderVN = renderVN;
		provide("$xeButtonGroup", $xeButtonGroup);
		return $xeButtonGroup;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/button-group/index.js
var VxeButtonGroup = Object.assign({}, button_group_default, { install(app) {
	app.component(button_group_default.name, button_group_default);
} });
dynamicApp.use(VxeButtonGroup);
VxeUI.component(button_group_default);
var ButtonGroup = VxeButtonGroup;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/calendar/src/calendar.js
var { menus: menus$3, getConfig: getConfig$3, getI18n: getI18n$2 } = VxeUI;
var calendar_default = defineVxeComponent({
	name: "VxeCalendar",
	props: {
		modelValue: [
			String,
			Number,
			Date
		],
		type: {
			type: String,
			default: "date"
		},
		className: String,
		size: {
			type: String,
			default: () => getConfig$3().calendar.size || getConfig$3().size
		},
		multiple: Boolean,
		width: [String, Number],
		height: [String, Number],
		minDate: {
			type: [
				String,
				Number,
				Date
			],
			default: () => getConfig$3().calendar.minDate
		},
		maxDate: {
			type: [
				String,
				Number,
				Date
			],
			default: () => getConfig$3().calendar.maxDate
		},
		startDay: {
			type: [String, Number],
			default: () => getConfig$3().calendar.startDay
		},
		labelFormat: String,
		valueFormat: String,
		festivalMethod: {
			type: Function,
			default: () => getConfig$3().calendar.festivalMethod
		},
		disabledMethod: {
			type: Function,
			default: () => getConfig$3().calendar.disabledMethod
		},
		cellStyle: [Object, Function],
		menuConfig: Object,
		selectDay: {
			type: [String, Number],
			default: () => getConfig$3().calendar.selectDay
		}
	},
	emits: [
		"update:modelValue",
		"change",
		"cell-click",
		"date-prev",
		"date-today",
		"date-next",
		"view-change",
		"cell-menu",
		"menu-click"
	],
	setup(props, context) {
		const { emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const reactData = reactive({
			selectValue: props.modelValue,
			inputValue: props.modelValue,
			datePanelValue: null,
			datePanelLabel: "",
			datePanelType: "day",
			selectMonth: null,
			currentDate: null
		});
		const internalData = {
			yearSize: 12,
			monthSize: 20,
			quarterSize: 8
		};
		const refElem = ref();
		const refMaps = { refElem };
		const $xeCalendar = {
			xID,
			props,
			context,
			reactData,
			internalData,
			getRefMaps: () => refMaps
		};
		const computeCalendarStyle = computed(() => {
			const { height, width } = props;
			const stys = {};
			if (width) stys.width = toCssUnit(width);
			if (height) stys.height = toCssUnit(height);
			return stys;
		});
		const computeIsDisabled = computed(() => {
			return false;
		});
		const computeIsCalendarType = computed(() => {
			return [
				"date",
				"week",
				"month",
				"quarter",
				"year"
			].indexOf(props.type) > -1;
		});
		const computeDateStartTime = computed(() => {
			return props.minDate ? import_xe_utils.default.toStringDate(props.minDate) : null;
		});
		const computeDateEndTime = computed(() => {
			return props.maxDate ? import_xe_utils.default.toStringDate(props.maxDate) : null;
		});
		const computeSupportMultiples = computed(() => {
			return [
				"date",
				"week",
				"month",
				"quarter",
				"year"
			].indexOf(props.type) > -1;
		});
		const computeMenuOpts = computed(() => {
			return Object.assign({}, getConfig$3().calendar.menuConfig, props.menuConfig);
		});
		const computeDateListValue = computed(() => {
			const { multiple } = props;
			const { selectValue } = reactData;
			const isCalendarType = computeIsCalendarType.value;
			const dateValueFormat = computeDateValueFormat.value;
			if (multiple && selectValue && isCalendarType) return import_xe_utils.default.toValueString(selectValue).split(",").map((item) => {
				const date = parseDate(item, dateValueFormat);
				if (import_xe_utils.default.isValidDate(date)) return date;
				return null;
			});
			return [];
		});
		const computeDateMultipleValue = computed(() => {
			const dateListValue = computeDateListValue.value;
			const dateValueFormat = computeDateValueFormat.value;
			return dateListValue.map((date) => import_xe_utils.default.toDateString(date, dateValueFormat));
		});
		const computeDateMultipleLabel = computed(() => {
			const dateListValue = computeDateListValue.value;
			const dateLabelFormat = computeDateLabelFormat.value;
			return dateListValue.map((date) => import_xe_utils.default.toDateString(date, dateLabelFormat)).join(", ");
		});
		const computeDateValueFormat = computed(() => {
			const { valueFormat } = props;
			if (valueFormat) return valueFormat;
			return "yyyy-MM-dd";
		});
		const computeDateValue = computed(() => {
			const { selectValue } = reactData;
			const isCalendarType = computeIsCalendarType.value;
			const dateValueFormat = computeDateValueFormat.value;
			let val = null;
			if (selectValue && isCalendarType) {
				const date = parseDate(selectValue, dateValueFormat);
				if (import_xe_utils.default.isValidDate(date)) val = date;
			}
			return val;
		});
		const computeIsDisabledPrevDateBtn = computed(() => {
			const dateStartTime = computeDateStartTime.value;
			const { selectMonth } = reactData;
			if (selectMonth && dateStartTime) return selectMonth <= dateStartTime;
			return false;
		});
		const computeIsDisabledNextDateBtn = computed(() => {
			const dateEndTime = computeDateEndTime.value;
			const { selectMonth } = reactData;
			if (selectMonth && dateEndTime) return selectMonth >= dateEndTime;
			return false;
		});
		const computeDateHMSTime = computed(() => {
			const dateValue = computeDateValue.value;
			return dateValue ? (dateValue.getHours() * 3600 + dateValue.getMinutes() * 60 + dateValue.getSeconds()) * 1e3 : 0;
		});
		const computeDateLabelFormat = computed(() => {
			const { labelFormat } = props;
			const isCalendarType = computeIsCalendarType.value;
			const dateValueFormat = computeDateValueFormat.value;
			if (isCalendarType) return labelFormat || dateValueFormat || getI18n$2(`vxe.input.date.labelFormat.${props.type}`);
			return null;
		});
		const computeYearList = computed(() => {
			const { selectMonth, currentDate } = reactData;
			const { yearSize } = internalData;
			const years = [];
			if (selectMonth && currentDate) {
				const currFullYear = currentDate.getFullYear();
				const selectFullYear = selectMonth.getFullYear();
				const startYearDate = new Date(selectFullYear - selectFullYear % yearSize, 0, 1);
				for (let index = -4; index < yearSize + 4; index++) {
					const date = import_xe_utils.default.getWhatYear(startYearDate, index, "first");
					const itemFullYear = date.getFullYear();
					years.push({
						date,
						isCurrent: true,
						isPrev: index < 0,
						isNow: currFullYear === itemFullYear,
						isNext: index >= yearSize,
						year: itemFullYear
					});
				}
			}
			return years;
		});
		const computeSelectDatePanelObj = computed(() => {
			const isCalendarType = computeIsCalendarType.value;
			let y = "";
			let m = "";
			if (isCalendarType) {
				const { datePanelType, selectMonth } = reactData;
				const yearList = computeYearList.value;
				let year = "";
				let month;
				if (selectMonth) {
					year = selectMonth.getFullYear();
					month = selectMonth.getMonth() + 1;
				}
				if (datePanelType === "quarter" || datePanelType === "month") y = `${year}`;
				else if (datePanelType === "year") y = yearList.length ? `${yearList[0].year} - ${yearList[yearList.length - 1].year}` : "";
				else {
					y = `${year}`;
					m = month ? getI18n$2("vxe.calendar.monthLabel", [month]) : "-";
				}
			}
			return {
				y: getI18n$2("vxe.calendar.yearLabel", [y]),
				m
			};
		});
		const computeFirstDayOfWeek = computed(() => {
			const { startDay } = props;
			return import_xe_utils.default.toNumber(startDay);
		});
		const computeWeekDatas = computed(() => {
			const weeks = [];
			if (computeIsCalendarType.value) {
				let sWeek = computeFirstDayOfWeek.value;
				weeks.push(sWeek);
				for (let index = 0; index < 6; index++) {
					if (sWeek >= 6) sWeek = 0;
					else sWeek++;
					weeks.push(sWeek);
				}
			}
			return weeks;
		});
		const computeDateHeaders = computed(() => {
			if (computeIsCalendarType.value) return computeWeekDatas.value.map((day) => {
				return {
					value: day,
					label: getI18n$2(`vxe.input.date.weeks.w${day}`)
				};
			});
			return [];
		});
		const computeWeekHeaders = computed(() => {
			if (computeIsCalendarType.value) {
				const dateHeaders = computeDateHeaders.value;
				return [{ label: getI18n$2("vxe.input.date.weeks.w") }].concat(dateHeaders);
			}
			return [];
		});
		const computeYearDatas = computed(() => {
			const yearList = computeYearList.value;
			return import_xe_utils.default.chunk(yearList, 4);
		});
		const computeQuarterList = computed(() => {
			const { selectMonth, currentDate } = reactData;
			const { quarterSize } = internalData;
			const quarters = [];
			if (selectMonth && currentDate) {
				const currFullYear = currentDate.getFullYear();
				const currQuarter = getDateQuarter(currentDate);
				const firstYear = import_xe_utils.default.getWhatYear(selectMonth, 0, "first");
				const selFullYear = firstYear.getFullYear();
				for (let index = -2; index < quarterSize - 2; index++) {
					const date = import_xe_utils.default.getWhatQuarter(firstYear, index);
					const itemFullYear = date.getFullYear();
					const itemQuarter = getDateQuarter(date);
					const isPrev = itemFullYear < selFullYear;
					quarters.push({
						date,
						isPrev,
						isCurrent: itemFullYear === selFullYear,
						isNow: itemFullYear === currFullYear && itemQuarter === currQuarter,
						isNext: !isPrev && itemFullYear > selFullYear,
						quarter: itemQuarter
					});
				}
			}
			return quarters;
		});
		const computeQuarterDatas = computed(() => {
			const quarterList = computeQuarterList.value;
			return import_xe_utils.default.chunk(quarterList, 2);
		});
		const computeMonthList = computed(() => {
			const { selectMonth, currentDate } = reactData;
			const { monthSize } = internalData;
			const months = [];
			if (selectMonth && currentDate) {
				const currFullYear = currentDate.getFullYear();
				const currMonth = currentDate.getMonth();
				const selFullYear = import_xe_utils.default.getWhatYear(selectMonth, 0, "first").getFullYear();
				for (let index = -4; index < monthSize - 4; index++) {
					const date = import_xe_utils.default.getWhatYear(selectMonth, 0, index);
					const itemFullYear = date.getFullYear();
					const itemMonth = date.getMonth();
					const isPrev = itemFullYear < selFullYear;
					months.push({
						date,
						isPrev,
						isCurrent: itemFullYear === selFullYear,
						isNow: itemFullYear === currFullYear && itemMonth === currMonth,
						isNext: !isPrev && itemFullYear > selFullYear,
						month: itemMonth
					});
				}
			}
			return months;
		});
		const computeMonthDatas = computed(() => {
			const monthList = computeMonthList.value;
			return import_xe_utils.default.chunk(monthList, 4);
		});
		const computeDayList = computed(() => {
			const { selectMonth, currentDate } = reactData;
			const days = [];
			if (selectMonth && currentDate) {
				const dateHMSTime = computeDateHMSTime.value;
				const weekDatas = computeWeekDatas.value;
				const currFullYear = currentDate.getFullYear();
				const currMonth = currentDate.getMonth();
				const currDate = currentDate.getDate();
				const selFullYear = selectMonth.getFullYear();
				const selMonth = selectMonth.getMonth();
				const selDay = selectMonth.getDay();
				const prevOffsetDate = -weekDatas.indexOf(selDay);
				const startDayDate = new Date(import_xe_utils.default.getWhatDay(selectMonth, prevOffsetDate).getTime() + dateHMSTime);
				for (let index = 0; index < 42; index++) {
					const date = import_xe_utils.default.getWhatDay(startDayDate, index);
					const itemFullYear = date.getFullYear();
					const itemMonth = date.getMonth();
					const itemDate = date.getDate();
					const isPrev = date < selectMonth;
					days.push({
						date,
						isPrev,
						isCurrent: itemFullYear === selFullYear && itemMonth === selMonth,
						isNow: itemFullYear === currFullYear && itemMonth === currMonth && itemDate === currDate,
						isNext: !isPrev && selMonth !== itemMonth,
						label: itemDate
					});
				}
			}
			return days;
		});
		const computeDayDatas = computed(() => {
			const dayList = computeDayList.value;
			return import_xe_utils.default.chunk(dayList, 7);
		});
		const computeWeekDates = computed(() => {
			const dayDatas = computeDayDatas.value;
			const firstDayOfWeek = computeFirstDayOfWeek.value;
			return dayDatas.map((list) => {
				const firstItem = list[0];
				return [{
					date: firstItem.date,
					isWeekNumber: true,
					isPrev: false,
					isCurrent: false,
					isNow: false,
					isNext: false,
					label: import_xe_utils.default.getYearWeek(firstItem.date, firstDayOfWeek)
				}].concat(list);
			});
		});
		const parseDate = (value, format) => {
			return import_xe_utils.default.toStringDate(value, format);
		};
		const handleChange = (value, evnt) => {
			reactData.inputValue = value;
			emit("update:modelValue", value);
			if (import_xe_utils.default.toValueString(props.modelValue) !== value) dispatchEvent("change", { value }, evnt);
		};
		const dateParseValue = (value) => {
			const { type } = props;
			const dateLabelFormat = computeDateLabelFormat.value;
			const dateValueFormat = computeDateValueFormat.value;
			const firstDayOfWeek = computeFirstDayOfWeek.value;
			let dValue = null;
			let dLabel = "";
			if (value) dValue = parseDate(value, dateValueFormat);
			if (import_xe_utils.default.isValidDate(dValue)) {
				dLabel = import_xe_utils.default.toDateString(dValue, dateLabelFormat, { firstDay: firstDayOfWeek });
				if (dateLabelFormat && type === "week") {
					if (import_xe_utils.default.getWhatWeek(dValue, 0, firstDayOfWeek, firstDayOfWeek).getFullYear() < dValue.getFullYear()) {
						const yyIndex = dateLabelFormat.indexOf("yyyy");
						if (yyIndex > -1) {
							const yyNum = Number(dLabel.substring(yyIndex, yyIndex + 4));
							if (yyNum && !isNaN(yyNum)) dLabel = dLabel.replace(`${yyNum}`, `${yyNum - 1}`);
						}
					}
				}
			} else dValue = null;
			reactData.datePanelValue = dValue;
			reactData.datePanelLabel = dLabel;
		};
		/**
		* 值变化时处理
		*/
		const changeValue = () => {
			const isCalendarType = computeIsCalendarType.value;
			const { inputValue } = reactData;
			if (isCalendarType) {
				dateParseValue(inputValue);
				reactData.inputValue = props.multiple ? computeDateMultipleLabel.value : reactData.datePanelLabel;
			}
		};
		/**
		* 检查初始值
		*/
		const initValue = () => {
			if (computeIsCalendarType.value) changeValue();
		};
		const dateCheckMonth = (date) => {
			const month = import_xe_utils.default.getWhatMonth(date, 0, "first");
			if (!import_xe_utils.default.isEqual(month, reactData.selectMonth)) reactData.selectMonth = month;
		};
		const dateChange = (date) => {
			const { modelValue, multiple } = props;
			const dateValueFormat = computeDateValueFormat.value;
			const firstDayOfWeek = computeFirstDayOfWeek.value;
			if (props.type === "week") {
				const sWeek = import_xe_utils.default.toNumber(props.selectDay);
				date = import_xe_utils.default.getWhatWeek(date, 0, sWeek, firstDayOfWeek);
			}
			const inpVal = import_xe_utils.default.toDateString(date, dateValueFormat, { firstDay: firstDayOfWeek });
			dateCheckMonth(date);
			reactData.selectValue = date;
			if (multiple) {
				const dateMultipleValue = computeDateMultipleValue.value;
				if (dateMultipleValue.some((val) => import_xe_utils.default.isEqual(val, inpVal))) handleChange(dateMultipleValue.filter((val) => !import_xe_utils.default.isEqual(val, inpVal)).join(","), { type: "update" });
				else handleChange(dateMultipleValue.concat([inpVal]).join(","), { type: "update" });
			} else if (!import_xe_utils.default.isEqual(modelValue, inpVal)) handleChange(inpVal, { type: "update" });
		};
		const dateMonthHandle = (date, offsetMonth) => {
			reactData.selectMonth = import_xe_utils.default.getWhatMonth(date, offsetMonth, "first");
		};
		const dateNowHandle = () => {
			const currentDate = import_xe_utils.default.getWhatDay(Date.now(), 0, "first");
			reactData.currentDate = currentDate;
			dateMonthHandle(currentDate, 0);
		};
		const dateToggleYearTypeEvent = () => {
			reactData.datePanelType = "year";
		};
		const dateToggleMonthTypeEvent = (evnt) => {
			let { datePanelType } = reactData;
			if (datePanelType === "month" || datePanelType === "quarter") datePanelType = "year";
			else datePanelType = "month";
			reactData.datePanelType = datePanelType;
			changeViewEvent(evnt);
		};
		const datePrevEvent = (evnt) => {
			const { type } = props;
			const { datePanelType, selectMonth } = reactData;
			const { yearSize } = internalData;
			if (!computeIsDisabledPrevDateBtn.value) {
				if (type === "year") reactData.selectMonth = import_xe_utils.default.getWhatYear(selectMonth, -yearSize, "first");
				else if (type === "month" || type === "quarter") if (datePanelType === "year") reactData.selectMonth = import_xe_utils.default.getWhatYear(selectMonth, -yearSize, "first");
				else reactData.selectMonth = import_xe_utils.default.getWhatYear(selectMonth, -1, "first");
				else if (datePanelType === "year") reactData.selectMonth = import_xe_utils.default.getWhatYear(selectMonth, -yearSize, "first");
				else if (datePanelType === "month") reactData.selectMonth = import_xe_utils.default.getWhatYear(selectMonth, -1, "first");
				else reactData.selectMonth = import_xe_utils.default.getWhatMonth(selectMonth, -1, "first");
				dispatchEvent("date-prev", { type }, evnt);
				changeViewEvent(evnt);
			}
		};
		const dateTodayMonthEvent = (evnt) => {
			dateNowHandle();
			if (!props.multiple) dateChange(reactData.currentDate);
			dispatchEvent("date-today", { type: props.type }, evnt);
			changeViewEvent(evnt);
		};
		const dateNextEvent = (evnt) => {
			const { type } = props;
			const { datePanelType, selectMonth } = reactData;
			const { yearSize } = internalData;
			if (!computeIsDisabledNextDateBtn.value) {
				if (type === "year") reactData.selectMonth = import_xe_utils.default.getWhatYear(selectMonth, yearSize, "first");
				else if (type === "month" || type === "quarter") if (datePanelType === "year") reactData.selectMonth = import_xe_utils.default.getWhatYear(selectMonth, yearSize, "first");
				else reactData.selectMonth = import_xe_utils.default.getWhatYear(selectMonth, 1, "first");
				else if (datePanelType === "year") reactData.selectMonth = import_xe_utils.default.getWhatYear(selectMonth, yearSize, "first");
				else if (datePanelType === "month") reactData.selectMonth = import_xe_utils.default.getWhatYear(selectMonth, 1, "first");
				else reactData.selectMonth = import_xe_utils.default.getWhatMonth(selectMonth, 1, "first");
				dispatchEvent("date-next", { type }, evnt);
				changeViewEvent(evnt);
			}
		};
		const isDateDisabled = (item) => {
			const { disabledMethod } = props;
			const { datePanelType } = reactData;
			return disabledMethod && disabledMethod({
				type: datePanelType,
				viewType: datePanelType,
				date: item.date,
				$calendar: $xeCalendar
			});
		};
		const changeViewEvent = (evnt) => {
			const { datePanelType } = reactData;
			const yearDatas = computeYearDatas.value;
			const quarterDatas = computeQuarterDatas.value;
			const monthDatas = computeMonthDatas.value;
			const weekDates = computeWeekDates.value;
			const dayDatas = computeDayDatas.value;
			const viewDates = [];
			let dataList = [];
			switch (datePanelType) {
				case "year":
					dataList = yearDatas;
					break;
				case "quarter":
					dataList = quarterDatas;
					break;
				case "month":
					dataList = monthDatas;
					break;
				case "week":
					dataList = weekDates;
					break;
				case "day":
					dataList = dayDatas;
					break;
			}
			dataList.forEach((rows) => {
				rows.forEach((item) => {
					viewDates.push(item.date);
				});
			});
			dispatchEvent("view-change", {
				viewType: datePanelType,
				viewDates
			}, evnt);
		};
		const dateSelectItem = (date) => {
			const { type } = props;
			const { datePanelType } = reactData;
			if (type === "month") if (datePanelType === "year") {
				reactData.datePanelType = "month";
				dateCheckMonth(date);
				changeViewEvent(null);
			} else dateChange(date);
			else if (type === "year") dateChange(date);
			else if (type === "quarter") if (datePanelType === "year") {
				reactData.datePanelType = "quarter";
				dateCheckMonth(date);
				changeViewEvent(null);
			} else dateChange(date);
			else if (datePanelType === "month") {
				reactData.datePanelType = type === "week" ? type : "day";
				dateCheckMonth(date);
				changeViewEvent(null);
			} else if (datePanelType === "year") {
				reactData.datePanelType = "month";
				dateCheckMonth(date);
				changeViewEvent(null);
			} else dateChange(date);
		};
		const dateClickEvent = (evnt, item) => {
			const { type } = props;
			const { datePanelType } = reactData;
			const { date } = item;
			if (!isDateDisabled(item)) {
				dateSelectItem(date);
				dispatchEvent("cell-click", {
					date,
					type,
					viewType: datePanelType
				}, evnt);
			}
		};
		const datContextmenuEvent = (evnt, item) => {
			const { type } = props;
			const { datePanelType } = reactData;
			const { menuConfig } = props;
			const menuOpts = computeMenuOpts.value;
			if (menuConfig ? isEnableConf(menuOpts) : menuOpts.enabled) {
				const { options, visibleMethod } = menuOpts;
				const { date } = item;
				if (!visibleMethod || visibleMethod({
					$calendar: $xeCalendar,
					options,
					date,
					type,
					viewType: datePanelType
				})) {
					if (VxeUI.contextMenu) VxeUI.contextMenu.openByEvent(evnt, {
						options,
						events: { optionClick(eventParams) {
							const { option } = eventParams;
							const gMenuOpts = menus$3.get(option.code);
							const cmMethod = gMenuOpts ? gMenuOpts.calendarMenuMethod : null;
							const params = {
								menu: option,
								date,
								type,
								viewType: datePanelType,
								$event: evnt,
								$calendar: $xeCalendar
							};
							if (cmMethod) cmMethod(params, evnt);
							dispatchEvent("menu-click", params, eventParams.$event);
						} }
					});
				}
			}
			dispatchEvent("cell-menu", {
				date: item.date,
				type,
				viewType: datePanelType
			}, evnt);
		};
		const dateMoveDay = (offsetDay) => {
			if (!isDateDisabled({ date: offsetDay })) {
				if (!computeDayList.value.some((item) => import_xe_utils.default.isDateSame(item.date, offsetDay, "yyyyMMdd"))) dateCheckMonth(offsetDay);
				dateParseValue(offsetDay);
			}
		};
		const dateMoveYear = (offsetYear) => {
			if (!isDateDisabled({ date: offsetYear })) {
				if (!computeYearList.value.some((item) => import_xe_utils.default.isDateSame(item.date, offsetYear, "yyyy"))) dateCheckMonth(offsetYear);
				dateParseValue(offsetYear);
			}
		};
		const dateMoveQuarter = (offsetQuarter) => {
			if (!isDateDisabled({ date: offsetQuarter })) {
				if (!computeQuarterList.value.some((item) => import_xe_utils.default.isDateSame(item.date, offsetQuarter, "yyyyq"))) dateCheckMonth(offsetQuarter);
				dateParseValue(offsetQuarter);
			}
		};
		const dateMoveMonth = (offsetMonth) => {
			if (!isDateDisabled({ date: offsetMonth })) {
				if (!computeMonthList.value.some((item) => import_xe_utils.default.isDateSame(item.date, offsetMonth, "yyyyMM"))) dateCheckMonth(offsetMonth);
				dateParseValue(offsetMonth);
			}
		};
		const dateMouseenterEvent = (item) => {
			if (!isDateDisabled(item)) {
				const { datePanelType } = reactData;
				if (datePanelType === "month") dateMoveMonth(item.date);
				else if (datePanelType === "quarter") dateMoveQuarter(item.date);
				else if (datePanelType === "year") dateMoveYear(item.date);
				else dateMoveDay(item.date);
			}
		};
		const dateMouseleaveEvent = () => {
			reactData.datePanelValue = null;
		};
		const dateConfirmEvent = () => {};
		const dateOpenPanel = () => {
			const { type } = props;
			const dateValue = computeDateValue.value;
			if ([
				"year",
				"quarter",
				"month",
				"week"
			].indexOf(type) > -1) reactData.datePanelType = type;
			else reactData.datePanelType = "day";
			reactData.currentDate = import_xe_utils.default.getWhatDay(Date.now(), 0, "first");
			if (dateValue) {
				dateMonthHandle(dateValue, 0);
				dateParseValue(dateValue);
			} else dateNowHandle();
		};
		const renderDateLabel = (item, label) => {
			const { festivalMethod } = props;
			if (festivalMethod) {
				const { datePanelType } = reactData;
				const festivalRest = festivalMethod({
					type: datePanelType,
					viewType: datePanelType,
					date: item.date,
					$calendar: $xeCalendar
				});
				const festivalItem = festivalRest ? import_xe_utils.default.isString(festivalRest) ? { label: festivalRest } : festivalRest : {};
				const extraItem = festivalItem.extra ? import_xe_utils.default.isString(festivalItem.extra) ? { label: festivalItem.extra } : festivalItem.extra : null;
				const labels = [h("span", { class: ["vxe-calendar--label", { "is-notice": festivalItem.notice }] }, extraItem && extraItem.label ? [h("span", { class: "vxe-calendar--label--number" }, `${label || ""}`), h("span", {
					class: [
						"vxe-calendar--label--extra",
						extraItem.important ? "is-important" : "",
						extraItem.className
					],
					style: extraItem.style
				}, import_xe_utils.default.toValueString(extraItem.label))] : [`${label || ""}`])];
				const festivalLabel = festivalItem.label;
				if (festivalLabel) {
					const festivalLabels = import_xe_utils.default.toValueString(festivalLabel).split(",");
					labels.push(h("span", {
						class: [
							"vxe-calendar--festival",
							festivalItem.important ? "is-important" : "",
							festivalItem.className
						],
						style: festivalItem.style
					}, [festivalLabels.length > 1 ? h("span", { class: ["vxe-calendar--festival--overlap", `overlap--${festivalLabels.length}`] }, festivalLabels.map((label) => h("span", label.substring(0, 3)))) : h("span", { class: "vxe-calendar--festival--label" }, festivalLabels[0].substring(0, 3))]));
				}
				return labels;
			}
			return `${label || ""}`;
		};
		const renderDateDayTable = () => {
			const { multiple, cellStyle } = props;
			const { datePanelType, datePanelValue } = reactData;
			const dateValue = computeDateValue.value;
			const dateHeaders = computeDateHeaders.value;
			const dayDatas = computeDayDatas.value;
			const dateListValue = computeDateListValue.value;
			const matchFormat = "yyyyMMdd";
			return [h("div", { class: ["vxe-calendar--view-wrapper", `type--${datePanelType}`] }, [h("div", {
				class: "vxe-calendar--view-header",
				style: { height: `${100 / (dayDatas.length + 1)}%` }
			}, [h("div", { class: "vxe-calendar--view-row" }, dateHeaders.map((item) => {
				return h("div", {
					class: "vxe-calendar--view-item",
					style: { width: `${100 / dateHeaders.length}%` }
				}, [h("div", { class: "vxe-calendar--view-item-inner" }, [h("div", { class: "vxe-calendar--view-item-label" }, item.label)])]);
			}))]), h("div", { class: "vxe-calendar--view-body" }, dayDatas.map((rows) => {
				return h("div", {
					class: "vxe-calendar--view-row",
					style: { height: `${100 / dayDatas.length}%` }
				}, rows.map((item) => {
					const isSelected = multiple ? dateListValue.some((val) => import_xe_utils.default.isDateSame(val, item.date, matchFormat)) : import_xe_utils.default.isDateSame(dateValue, item.date, matchFormat);
					return h("div", {
						class: ["vxe-calendar--view-item", {
							"is--prev": item.isPrev,
							"is--current": item.isCurrent,
							"is--now": item.isNow,
							"is--next": item.isNext,
							"is--disabled": isDateDisabled(item),
							"is--selected": isSelected,
							"is--hover": import_xe_utils.default.isDateSame(datePanelValue, item.date, matchFormat)
						}],
						style: Object.assign({}, import_xe_utils.default.isFunction(cellStyle) ? cellStyle({
							type: datePanelType,
							viewType: datePanelType,
							date: item.date,
							$calendar: $xeCalendar
						}) : cellStyle, { width: `${100 / rows.length}%` }),
						onClick: (evnt) => dateClickEvent(evnt, item),
						onContextmenu: (evnt) => datContextmenuEvent(evnt, item),
						onMouseenter: () => dateMouseenterEvent(item),
						onMouseleave: dateMouseleaveEvent
					}, [h("div", { class: "vxe-calendar--view-item-inner" }, renderDateLabel(item, item.label))]);
				}));
			}))])];
		};
		const renderDateWeekTable = () => {
			const { multiple, cellStyle } = props;
			const { datePanelType, datePanelValue } = reactData;
			const dateValue = computeDateValue.value;
			const weekHeaders = computeWeekHeaders.value;
			const weekDates = computeWeekDates.value;
			const dateListValue = computeDateListValue.value;
			const matchFormat = "yyyyMMdd";
			return [h("div", { class: ["vxe-calendar--view-wrapper", `type--${datePanelType}`] }, [h("div", {
				class: "vxe-calendar--view-header",
				style: { height: `${100 / (weekDates.length + 1)}%` }
			}, [h("div", { class: "vxe-calendar--view-row" }, weekHeaders.map((item, rIndex) => {
				return h("div", {
					class: "vxe-calendar--view-item",
					style: { width: `${rIndex ? 13 : 9}%` }
				}, [h("div", { class: "vxe-calendar--view-item-inner" }, [h("div", { class: "vxe-calendar--view-item-label" }, item.label)])]);
			}))]), h("div", { class: "vxe-calendar--view-body" }, weekDates.map((rows) => {
				const isSelected = multiple ? rows.some((item) => dateListValue.some((val) => import_xe_utils.default.isDateSame(val, item.date, matchFormat))) : rows.some((item) => import_xe_utils.default.isDateSame(dateValue, item.date, matchFormat));
				const isHover = rows.some((item) => import_xe_utils.default.isDateSame(datePanelValue, item.date, matchFormat));
				const isNowWeek = rows.some((item) => item.isNow);
				return h("div", {
					class: "vxe-calendar--view-row",
					style: { height: `${100 / weekDates.length}%` }
				}, rows.map((item, rIndex) => {
					return h("div", {
						class: ["vxe-calendar--view-item", {
							"is--prev": item.isPrev,
							"is--current": item.isCurrent,
							"is--now": rIndex ? item.isNow : isNowWeek,
							"is--next": item.isNext,
							"is--disabled": isDateDisabled(item),
							"is--selected": isSelected,
							"is--hover": isHover
						}],
						style: Object.assign({}, import_xe_utils.default.isFunction(cellStyle) ? cellStyle({
							type: datePanelType,
							viewType: datePanelType,
							date: item.date,
							$calendar: $xeCalendar
						}) : cellStyle, { width: `${rIndex ? 13 : 9}%` }),
						onClick: (evnt) => dateClickEvent(evnt, item),
						onContextmenu: (evnt) => datContextmenuEvent(evnt, item),
						onMouseenter: () => dateMouseenterEvent(item),
						onMouseleave: dateMouseleaveEvent
					}, [h("div", { class: "vxe-calendar--view-item-inner" }, renderDateLabel(item, item.label))]);
				}));
			}))])];
		};
		const renderDateMonthTable = () => {
			const { multiple, cellStyle } = props;
			const { datePanelType, datePanelValue } = reactData;
			const dateValue = computeDateValue.value;
			const monthDatas = computeMonthDatas.value;
			const dateListValue = computeDateListValue.value;
			const matchFormat = "yyyyMM";
			return [h("div", { class: ["vxe-calendar--view-wrapper", `type--${datePanelType}`] }, [h("div", { class: "vxe-calendar--view-body" }, monthDatas.map((rows) => {
				return h("div", {
					class: "vxe-calendar--view-row",
					style: { height: `${100 / monthDatas.length}%` }
				}, rows.map((item) => {
					const isSelected = multiple ? dateListValue.some((val) => import_xe_utils.default.isDateSame(val, item.date, matchFormat)) : import_xe_utils.default.isDateSame(dateValue, item.date, matchFormat);
					return h("div", {
						class: ["vxe-calendar--view-item", {
							"is--prev": item.isPrev,
							"is--current": item.isCurrent,
							"is--now": item.isNow,
							"is--next": item.isNext,
							"is--disabled": isDateDisabled(item),
							"is--selected": isSelected,
							"is--hover": import_xe_utils.default.isDateSame(datePanelValue, item.date, matchFormat)
						}],
						style: Object.assign({}, import_xe_utils.default.isFunction(cellStyle) ? cellStyle({
							type: datePanelType,
							viewType: datePanelType,
							date: item.date,
							$calendar: $xeCalendar
						}) : cellStyle, { width: `${100 / rows.length}%` }),
						onClick: (evnt) => dateClickEvent(evnt, item),
						onContextmenu: (evnt) => datContextmenuEvent(evnt, item),
						onMouseenter: () => dateMouseenterEvent(item),
						onMouseleave: dateMouseleaveEvent
					}, [h("div", { class: "vxe-calendar--view-item-inner" }, renderDateLabel(item, getI18n$2(`vxe.input.date.months.m${item.month}`)))]);
				}));
			}))])];
		};
		const renderDateQuarterTable = () => {
			const { multiple, cellStyle } = props;
			const { datePanelType, datePanelValue } = reactData;
			const dateValue = computeDateValue.value;
			const quarterDatas = computeQuarterDatas.value;
			const dateListValue = computeDateListValue.value;
			const matchFormat = "yyyyq";
			return [h("div", { class: ["vxe-calendar--view-wrapper", `type--${datePanelType}`] }, [h("div", { class: "vxe-calendar--view-body" }, quarterDatas.map((rows) => {
				return h("div", {
					class: "vxe-calendar--view-row",
					style: { height: `${100 / quarterDatas.length}%` }
				}, rows.map((item) => {
					const isSelected = multiple ? dateListValue.some((val) => import_xe_utils.default.isDateSame(val, item.date, matchFormat)) : import_xe_utils.default.isDateSame(dateValue, item.date, matchFormat);
					return h("div", {
						class: ["vxe-calendar--view-item", {
							"is--prev": item.isPrev,
							"is--current": item.isCurrent,
							"is--now": item.isNow,
							"is--next": item.isNext,
							"is--disabled": isDateDisabled(item),
							"is--selected": isSelected,
							"is--hover": import_xe_utils.default.isDateSame(datePanelValue, item.date, matchFormat)
						}],
						style: Object.assign({}, import_xe_utils.default.isFunction(cellStyle) ? cellStyle({
							type: datePanelType,
							viewType: datePanelType,
							date: item.date,
							$calendar: $xeCalendar
						}) : cellStyle, { width: `${100 / rows.length}%` }),
						onClick: (evnt) => dateClickEvent(evnt, item),
						onContextmenu: (evnt) => datContextmenuEvent(evnt, item),
						onMouseenter: () => dateMouseenterEvent(item),
						onMouseleave: dateMouseleaveEvent
					}, [h("div", { class: "vxe-calendar--view-item-inner" }, renderDateLabel(item, getI18n$2(`vxe.input.date.quarters.q${item.quarter}`)))]);
				}));
			}))])];
		};
		const renderDateYearTable = () => {
			const { multiple, cellStyle } = props;
			const { datePanelType, datePanelValue } = reactData;
			const dateValue = computeDateValue.value;
			const yearDatas = computeYearDatas.value;
			const dateListValue = computeDateListValue.value;
			const matchFormat = "yyyy";
			return [h("div", { class: ["vxe-calendar--view-wrapper", `type--${datePanelType}`] }, [h("div", { class: "vxe-calendar--view-body" }, yearDatas.map((rows) => {
				return h("div", {
					class: "vxe-calendar--view-row",
					style: { height: `${100 / yearDatas.length}%` }
				}, rows.map((item) => {
					const isSelected = multiple ? dateListValue.some((val) => import_xe_utils.default.isDateSame(val, item.date, matchFormat)) : import_xe_utils.default.isDateSame(dateValue, item.date, matchFormat);
					return h("div", {
						class: ["vxe-calendar--view-item", {
							"is--prev": item.isPrev,
							"is--current": item.isCurrent,
							"is--now": item.isNow,
							"is--next": item.isNext,
							"is--disabled": isDateDisabled(item),
							"is--selected": isSelected,
							"is--hover": import_xe_utils.default.isDateSame(datePanelValue, item.date, matchFormat)
						}],
						style: Object.assign({}, import_xe_utils.default.isFunction(cellStyle) ? cellStyle({
							type: datePanelType,
							viewType: datePanelType,
							date: item.date,
							$calendar: $xeCalendar
						}) : cellStyle, { width: `${100 / rows.length}%` }),
						onClick: (evnt) => dateClickEvent(evnt, item),
						onContextmenu: (evnt) => datContextmenuEvent(evnt, item),
						onMouseenter: () => dateMouseenterEvent(item),
						onMouseleave: dateMouseleaveEvent
					}, [h("div", { class: "vxe-calendar--view-item-inner" }, renderDateLabel(item, item.year))]);
				}));
			}))])];
		};
		const renderDateTable = () => {
			const { datePanelType } = reactData;
			switch (datePanelType) {
				case "week": return renderDateWeekTable();
				case "month": return renderDateMonthTable();
				case "quarter": return renderDateQuarterTable();
				case "year": return renderDateYearTable();
			}
			return renderDateDayTable();
		};
		const renderDatePanel = () => {
			const { multiple } = props;
			const { datePanelType } = reactData;
			const isDisabledPrevDateBtn = computeIsDisabledPrevDateBtn.value;
			const isDisabledNextDateBtn = computeIsDisabledNextDateBtn.value;
			const selectDatePanelObj = computeSelectDatePanelObj.value;
			return [h("div", { class: "vxe-calendar--header" }, [h("div", { class: "vxe-calendar--type-wrapper" }, [datePanelType === "year" ? h(VxeButtonComponent, {
				class: "vxe-calendar--date-picker-label",
				disabled: datePanelType === "year",
				content: selectDatePanelObj.y
			}) : h("span", { class: "vxe-calendar--date-picker-btns" }, [h(VxeButtonComponent, {
				class: "vxe-calendar--date-picker-btn",
				content: selectDatePanelObj.y,
				onClick: dateToggleYearTypeEvent
			}), selectDatePanelObj.m ? h(VxeButtonComponent, {
				class: "vxe-calendar--date-picker-btn",
				content: selectDatePanelObj.m,
				onClick: dateToggleMonthTypeEvent
			}) : renderEmptyElement($xeCalendar)])]), h("div", { class: "vxe-calendar--btn-wrapper" }, [
				h(VxeButtonComponent, {
					disabled: isDisabledPrevDateBtn,
					icon: "vxe-icon-caret-left",
					onClick: datePrevEvent
				}),
				h(VxeButtonComponent, {
					icon: "vxe-icon-dot",
					onClick: dateTodayMonthEvent
				}),
				h(VxeButtonComponent, {
					disabled: isDisabledNextDateBtn,
					icon: "vxe-icon-caret-right",
					onClick: dateNextEvent
				}),
				multiple && computeSupportMultiples.value ? h("span", { class: "vxe-calendar--btn vxe-calendar--confirm-btn" }, [h("button", {
					class: "vxe-calendar--confirm",
					type: "button",
					onClick: dateConfirmEvent
				}, getI18n$2("vxe.button.confirm"))]) : null
			])]), h("div", { class: "vxe-calendar--body" }, renderDateTable())];
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $input: $xeCalendar }, params));
		};
		Object.assign($xeCalendar, { dispatchEvent });
		const renderVN = () => {
			const { className, type } = props;
			const vSize = computeSize.value;
			const isDisabled = computeIsDisabled.value;
			const calendarStyle = computeCalendarStyle.value;
			return h("div", {
				ref: refElem,
				class: [
					"vxe-calendar",
					`type--${type}`,
					className,
					{
						[`size--${vSize}`]: vSize,
						"is--disabled": isDisabled
					}
				],
				style: calendarStyle
			}, [renderDatePanel()]);
		};
		$xeCalendar.renderVN = renderVN;
		watch(() => props.modelValue, (val) => {
			reactData.inputValue = val;
			changeValue();
		});
		watch(() => props.type, () => {
			Object.assign(reactData, {
				selectValue: null,
				inputValue: null,
				datePanelValue: null,
				datePanelLabel: "",
				datePanelType: "day",
				selectMonth: null,
				currentDate: null
			});
			initValue();
			dateOpenPanel();
		});
		watch(computeDateLabelFormat, () => {
			if (computeIsCalendarType.value) {
				dateParseValue(reactData.datePanelValue);
				reactData.inputValue = props.multiple ? computeDateMultipleLabel.value : reactData.datePanelLabel;
			}
		});
		onMounted(() => {
			const { menuConfig } = props;
			const VxeUIContextMenu = VxeUI.getComponent("VxeContextMenu");
			if (menuConfig && !VxeUIContextMenu) errLog("vxe.error.reqComp", ["vxe-context-menu"]);
			dateOpenPanel();
		});
		initValue();
		return $xeCalendar;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/calendar/index.js
var VxeCalendar = Object.assign({}, calendar_default, { install(app) {
	app.component(calendar_default.name, calendar_default);
} });
dynamicApp.use(VxeCalendar);
VxeUI.component(calendar_default);
var Calendar = VxeCalendar;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/card/src/card.js
var card_default = defineVxeComponent({
	name: "VxeCard",
	props: {
		title: String,
		showTitleOverflow: {
			type: Boolean,
			default: () => getConfig().card.showTitleOverflow
		},
		width: [String, Number],
		height: [String, Number],
		border: {
			type: Boolean,
			default: () => getConfig().card.border
		},
		loading: Boolean,
		shadow: {
			type: Boolean,
			default: () => getConfig().card.shadow
		},
		padding: {
			type: Boolean,
			default: () => getConfig().card.padding
		},
		size: {
			type: String,
			default: () => getConfig().card.size || getConfig().size
		}
	},
	emits: [],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const { computeSize } = useSize(props);
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeCardStyle = computed(() => {
			const { height, width } = props;
			const stys = {};
			if (width) stys.width = toCssUnit(width);
			if (height) stys.height = toCssUnit(height);
			return stys;
		});
		const computeMaps = { computeSize };
		const $xeCard = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $card: $xeCard }, params));
		};
		Object.assign($xeCard, { dispatchEvent }, {});
		const renderVN = () => {
			const { title, border, shadow, padding, loading, showTitleOverflow } = props;
			const defaultSlot = slots.default;
			const headerSlot = slots.header;
			const titleSlot = slots.title;
			const extraSlot = slots.extra;
			const footerSlot = slots.footer;
			const leftSlot = slots.left;
			const rightSlot = slots.right;
			const vSize = computeSize.value;
			const cardStyle = computeCardStyle.value;
			return h("div", {
				ref: refElem,
				class: ["vxe-card", {
					[`size--${vSize}`]: vSize,
					"is--border": border,
					"is--shadow": shadow,
					"is--padding": padding
				}],
				style: cardStyle
			}, [
				title || titleSlot || headerSlot ? h("div", { class: "vxe-card--header" }, headerSlot ? getSlotVNs$1(headerSlot({})) : [h("div", { class: ["vxe-card--header-title", { "is--ellipsis": showTitleOverflow }] }, titleSlot ? getSlotVNs$1(titleSlot({})) : `${title || ""}`), extraSlot ? h("div", { class: "vxe-card--header-extra" }, getSlotVNs$1(extraSlot({}))) : renderEmptyElement($xeCard)]) : renderEmptyElement($xeCard),
				h("div", { class: "vxe-card--body" }, [
					leftSlot ? h("div", { class: "vxe-card--body-left" }, getSlotVNs$1(leftSlot({}))) : renderEmptyElement($xeCard),
					h("div", { class: "vxe-card--body-content" }, defaultSlot ? getSlotVNs$1(defaultSlot({})) : []),
					rightSlot ? h("div", { class: "vxe-card--body-right" }, getSlotVNs$1(rightSlot({}))) : renderEmptyElement($xeCard)
				]),
				footerSlot ? h("div", { class: "vxe-card--footer" }, getSlotVNs$1(footerSlot({}))) : renderEmptyElement($xeCard),
				h(loading_default, {
					class: "vxe-card--loading",
					modelValue: loading
				})
			]);
		};
		$xeCard.renderVN = renderVN;
		return $xeCard;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/card/index.js
var VxeCard = Object.assign({}, card_default, { install(app) {
	app.component(card_default.name, card_default);
} });
dynamicApp.use(VxeCard);
VxeUI.component(card_default);
var Card = VxeCard;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/carousel/src/carousel.js
var carousel_default = defineVxeComponent({
	name: "VxeCarousel",
	props: {
		modelValue: [String, Number],
		options: Array,
		loading: Boolean,
		height: {
			type: [Number, String],
			default: () => getConfig().carousel.height
		},
		width: {
			type: [Number, String],
			default: () => getConfig().carousel.width
		},
		autoPlay: {
			type: Boolean,
			default: () => getConfig().carousel.autoPlay
		},
		interval: {
			type: [Number, String],
			default: () => getConfig().carousel.interval
		},
		loop: {
			type: Boolean,
			default: () => getConfig().carousel.loop
		},
		vertical: {
			type: Boolean,
			default: () => getConfig().carousel.vertical
		},
		showIndicators: {
			type: Boolean,
			default: () => getConfig().carousel.showIndicators
		}
	},
	emits: ["update:modelValue", "change"],
	setup(props, context) {
		const { emit, slots } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const refWrapperElem = ref();
		const reactData = reactive({
			activeName: "",
			staticItems: [],
			itemWidth: 0,
			itemHeight: 0
		});
		const internalData = {
			apTimeout: void 0,
			stopFlag: false
		};
		const refMaps = { refElem };
		const computeListStyle = computed(() => {
			const { vertical, options } = props;
			const { activeName, itemWidth, itemHeight, staticItems } = reactData;
			const list = (staticItems && staticItems.length ? staticItems : options) || [];
			const activeIndex = Math.max(0, import_xe_utils.default.findIndexOf(list, (item) => item.name === activeName));
			const stys = {};
			if (vertical) stys.transform = `translateY(-${activeIndex * itemHeight}px)`;
			else {
				stys.width = `${itemWidth * list.length}px`;
				stys.transform = `translateX(-${activeIndex * itemWidth}px)`;
			}
			return stys;
		});
		const computeMaps = {};
		const $xeCarousel = {
			xID,
			props,
			context,
			reactData,
			internalData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const updateStyle = () => {
			nextTick(() => {
				const wrapperElem = refWrapperElem.value;
				if (wrapperElem) {
					reactData.itemWidth = wrapperElem.clientWidth;
					reactData.itemHeight = wrapperElem.clientHeight;
				}
			});
		};
		const clickItemEvent = (evnt, item) => {
			const value = item.name;
			reactData.activeName = item.name;
			emit("update:modelValue", value);
			emit("change", { value }, evnt);
			updateStyle();
		};
		const initDefaultActive = (list) => {
			let activeName = null;
			if (list && list.length) {
				let validVal = false;
				activeName = props.modelValue;
				list.forEach((item) => {
					if (activeName === item.name) validVal = true;
				});
				if (!validVal) {
					activeName = list[0].name;
					emit("update:modelValue", activeName);
				}
			}
			reactData.activeName = activeName;
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $carousel: $xeCarousel }, params));
		};
		const handlePrevNext = (isNext) => {
			const { options, loop } = props;
			const { activeName, staticItems } = reactData;
			const list = (staticItems && staticItems.length ? staticItems : options) || [];
			const index = Math.max(0, import_xe_utils.default.findIndexOf(list, (item) => item.name === activeName));
			if (index > -1) {
				let item = null;
				if (isNext) {
					if (index < list.length - 1) item = list[index + 1];
					else if (loop) item = list[0];
				} else if (index > 0) item = list[index - 1];
				else if (loop) item = list[list.length - 1];
				if (item) {
					const name = item.name;
					const value = name;
					reactData.activeName = name;
					emit("update:modelValue", value);
					return true;
				}
			}
			return false;
		};
		const carouselMethods = {
			dispatchEvent,
			prev() {
				if (handlePrevNext(false)) handleAutoPlay();
				return nextTick();
			},
			next() {
				if (handlePrevNext(true)) handleAutoPlay();
				return nextTick();
			}
		};
		const prevEvent = (evnt) => {
			if (handlePrevNext(false)) {
				const value = reactData.activeName;
				emit("change", { value }, evnt);
			}
		};
		const nextEvent = (evnt) => {
			if (handlePrevNext(true)) {
				const value = reactData.activeName;
				emit("change", { value }, evnt);
			}
		};
		const stopAutoPlay = () => {
			const { apTimeout } = internalData;
			internalData.stopFlag = true;
			if (apTimeout) {
				clearTimeout(apTimeout);
				internalData.apTimeout = void 0;
			}
		};
		const handleAutoPlay = () => {
			const { autoPlay, interval } = props;
			const { stopFlag } = internalData;
			stopAutoPlay();
			if (autoPlay) {
				internalData.stopFlag = false;
				internalData.apTimeout = setTimeout(() => {
					if (!stopFlag) handlePrevNext(true);
				}, import_xe_utils.default.toNumber(interval) || 300);
			}
		};
		const mouseenterEvent = () => {
			stopAutoPlay();
		};
		const mouseleaveEvent = () => {
			handleAutoPlay();
		};
		const carouselPrivateMethods = {};
		const callSlot = (slotFunc, params) => {
			if (slotFunc) {
				if (import_xe_utils.default.isString(slotFunc)) slotFunc = slots[slotFunc] || null;
				if (import_xe_utils.default.isFunction(slotFunc)) return getSlotVNs$1(slotFunc(params));
			}
			return [];
		};
		Object.assign($xeCarousel, carouselMethods, carouselPrivateMethods);
		const renderItemWrapper = (list) => {
			const { height } = props;
			const { activeName } = reactData;
			const listStyle = computeListStyle.value;
			return h("div", {
				class: "vxe-carousel--list",
				style: listStyle
			}, list.map((item) => {
				const { name, url, slots } = item;
				const defaultSlot = slots ? slots.default : null;
				return h("div", {
					key: `${name}`,
					class: ["vxe-carousel--item-inner", { "is--active": activeName === name }],
					style: height ? { height: toCssUnit(height) } : null
				}, defaultSlot ? callSlot(defaultSlot, {}) : [h("img", {
					class: "vxe-carousel--item-img",
					src: url
				})]);
			}));
		};
		const renderIndicators = (list) => {
			const { activeName } = reactData;
			return h("div", { class: "vxe-carousel--indicators" }, list.map((item) => {
				const { name } = item;
				return h("div", {
					key: `${name}`,
					class: ["vxe-carousel--indicators-item", { "is--active": activeName === name }],
					onClick(evnt) {
						clickItemEvent(evnt, item);
					}
				});
			}));
		};
		const renderVN = () => {
			const { loading, height, width, showIndicators, vertical, options } = props;
			const { staticItems } = reactData;
			const defaultSlot = slots.default;
			const list = (staticItems && staticItems.length ? staticItems : options) || [];
			return h("div", {
				ref: refElem,
				class: ["vxe-carousel", `is--${vertical ? "vertical" : "horizontal"}`],
				style: width ? { width: toCssUnit(width) } : null,
				onMouseenter: mouseenterEvent,
				onMouseleave: mouseleaveEvent
			}, [
				h("div", { class: "vxe-carousel--slots" }, defaultSlot ? defaultSlot({}) : []),
				h("div", {
					ref: refWrapperElem,
					class: "vxe-carousel--item-wrapper",
					style: height ? { height: toCssUnit(height) } : null
				}, [renderItemWrapper(list)]),
				showIndicators ? renderIndicators(list) : renderEmptyElement($xeCarousel),
				h("div", { class: "vxe-carousel--btn-wrapper" }, [h("div", {
					class: "vxe-carousel--previous-btn",
					onClick: prevEvent
				}, [h("i", { class: vertical ? getIcon().CAROUSEL_VERTICAL_PREVIOUS : getIcon().CAROUSEL_HORIZONTAL_PREVIOUS })]), h("div", {
					class: "vxe-carousel--next-btn",
					onClick: nextEvent
				}, [h("i", { class: vertical ? getIcon().CAROUSEL_VERTICAL_NEXT : getIcon().CAROUSEL_HORIZONTAL_NEXT })])]),
				h(loading_default, {
					class: "vxe-carousel--loading",
					modelValue: loading
				})
			]);
		};
		const optsFlag = ref(0);
		watch(() => props.options ? props.options.length : -1, () => {
			optsFlag.value++;
		});
		watch(() => props.options, () => {
			optsFlag.value++;
		});
		watch(optsFlag, () => {
			initDefaultActive(props.options);
		});
		const stFlag = ref(0);
		watch(() => reactData.staticItems ? reactData.staticItems.length : -1, () => {
			stFlag.value++;
		});
		watch(() => reactData.staticItems, () => {
			stFlag.value++;
		});
		watch(stFlag, () => {
			initDefaultActive(reactData.staticItems);
		});
		watch(() => props.autoPlay, () => {
			handleAutoPlay();
		});
		initDefaultActive(reactData.staticItems.length ? reactData.staticItems : props.options);
		onMounted(() => {
			handleAutoPlay();
			updateStyle();
		});
		onUnmounted(() => {
			stopAutoPlay();
		});
		provide("$xeCarousel", $xeCarousel);
		$xeCarousel.renderVN = renderVN;
		return $xeCarousel;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/carousel/index.js
var VxeCarousel = Object.assign({}, carousel_default, { install(app) {
	app.component(carousel_default.name, carousel_default);
} });
dynamicApp.use(VxeCarousel);
VxeUI.component(carousel_default);
var Carousel = VxeCarousel;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/carousel/src/util.js
function assembleCarouselItem($xeCarousel, elem, itemConfig) {
	const staticItems = $xeCarousel.reactData.staticItems;
	const parentElem = elem.parentNode;
	if (parentElem) staticItems.splice(import_xe_utils.default.arrayIndexOf(parentElem.children, elem), 0, itemConfig);
	$xeCarousel.reactData.staticItems = staticItems.slice(0);
}
function destroyCarouselItem($xeCarousel, itemConfig) {
	const staticItems = $xeCarousel.reactData.staticItems;
	const index = import_xe_utils.default.findIndexOf(staticItems, (item) => item.id === itemConfig.id);
	if (index > -1) staticItems.splice(index, 1);
	$xeCarousel.reactData.staticItems = staticItems.slice(0);
}
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/carousel/src/carousel-item.js
var carousel_item_default = defineVxeComponent({
	name: "VxeCarouselItem",
	props: {
		name: [String, Number],
		className: String,
		url: String
	},
	emits: [],
	setup(props, context) {
		const { slots, emit } = context;
		const $xeCarousel = inject("$xeCarousel", null);
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const reactData = reactive({});
		const itemConfig = reactive({
			id: xID,
			name: props.name,
			url: props.url,
			className: props.className,
			slots
		});
		const refMaps = { refElem };
		const computeMaps = {};
		const $xeCarouselItem = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $carouselItem: $xeCarouselItem }, params));
		};
		Object.assign($xeCarouselItem, { dispatchEvent }, {});
		const renderVN = () => {
			return h("div", { ref: refElem });
		};
		watch(() => props.name, (val) => {
			itemConfig.name = val;
		});
		watch(() => props.url, (val) => {
			itemConfig.url = val;
		});
		onMounted(() => {
			const elem = refElem.value;
			if ($xeCarousel && elem) assembleCarouselItem($xeCarousel, elem, itemConfig);
		});
		onUnmounted(() => {
			if ($xeCarousel) destroyCarouselItem($xeCarousel, itemConfig);
		});
		$xeCarouselItem.renderVN = renderVN;
		return $xeCarouselItem;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/carousel-item/index.js
var VxeCarouselItem = Object.assign({}, carousel_item_default, { install(app) {
	app.component(carousel_item_default.name, carousel_item_default);
} });
dynamicApp.use(VxeCarouselItem);
VxeUI.component(carousel_item_default);
var CarouselItem = VxeCarouselItem;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/tree/src/util.js
function enNodeValue(nodeid) {
	return import_xe_utils.default.eqNull(nodeid) ? "" : encodeURIComponent(`${nodeid}`);
}
function deNodeValue(nodeid) {
	return nodeid ? decodeURIComponent(`${nodeid}`) : nodeid;
}
function countTreeExpand($xeTree, prevNode) {
	let count = 1;
	if (!prevNode) return count;
	const { transform } = $xeTree.props;
	const { updateExpandedFlag } = $xeTree.reactData;
	const { treeExpandedMaps } = $xeTree.internalData;
	const { computeChildrenField, computeMapChildrenField } = $xeTree.getComputeMaps();
	const childrenField = computeChildrenField.value;
	const mapChildrenField = computeMapChildrenField.value;
	const nodeChildren = prevNode[transform ? mapChildrenField : childrenField];
	if (nodeChildren && updateExpandedFlag && treeExpandedMaps[$xeTree.getNodeId(prevNode)]) for (let index = 0; index < nodeChildren.length; index++) count += countTreeExpand($xeTree, nodeChildren[index]);
	return count;
}
function calcTreeLine($xeTree, node, prevNode) {
	const { scrollYStore } = $xeTree.internalData;
	const { rowHeight } = scrollYStore;
	let expandSize = 1;
	if (prevNode) expandSize = countTreeExpand($xeTree, prevNode);
	return (rowHeight || 28) * expandSize - (prevNode ? 1 : 12);
}
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/cascader/src/cascader.js
function getOptUniqueId$1() {
	return import_xe_utils.default.uniqueId("node_");
}
function createInternalData$9() {
	return {
		fullOptionList: [],
		fullNodeMaps: {}
	};
}
var cascader_default = defineVxeComponent({
	name: "VxeCascader",
	props: {
		modelValue: [
			String,
			Number,
			Array
		],
		clearable: Boolean,
		placeholder: {
			type: String,
			default: () => import_xe_utils.default.eqNull(getConfig().cascader.placeholder) ? getI18n("vxe.base.pleaseSelect") : getConfig().cascader.placeholder
		},
		readonly: {
			type: Boolean,
			default: null
		},
		loading: Boolean,
		disabled: {
			type: Boolean,
			default: null
		},
		filterable: Boolean,
		filterConfig: Object,
		multiple: Boolean,
		className: [String, Function],
		prefixIcon: String,
		placement: String,
		transform: Boolean,
		lazyOptions: Array,
		options: Array,
		optionProps: Object,
		zIndex: Number,
		size: {
			type: String,
			default: () => getConfig().cascader.size || getConfig().size
		},
		remote: Boolean,
		remoteConfig: Function,
		popupConfig: Object,
		autoClose: {
			type: Boolean,
			default: () => getConfig().cascader.autoClose
		},
		showTotalButoon: {
			type: Boolean,
			default: () => getConfig().cascader.showTotalButoon
		},
		showCheckedButoon: {
			type: Boolean,
			default: () => getConfig().cascader.showCheckedButoon
		},
		showClearButton: {
			type: Boolean,
			default: () => getConfig().cascader.showClearButton
		},
		transfer: {
			type: Boolean,
			default: null
		},
		/**
		* 已废弃，被 remote-config.queryMethod 替换
		* @deprecated
		*/
		remoteMethod: Function
	},
	emits: [
		"update:modelValue",
		"change",
		"all-change",
		"clear",
		"blur",
		"focus",
		"click",
		"node-click"
	],
	setup(props, context) {
		const { emit, slots } = context;
		const $xeModal = inject("$xeModal", null);
		const $xeDrawer = inject("$xeDrawer", null);
		const $xeTable = inject("$xeTable", null);
		const $xeForm = inject("$xeForm", null);
		const formItemInfo = inject("xeFormItemInfo", null);
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const refElem = ref();
		const refInput = ref();
		const refInpSearch = ref();
		const refTreeWrapper = ref();
		const refOptionPanel = ref();
		const refTree = ref();
		const reactData = reactive({
			initialized: false,
			searchValue: "",
			searchLoading: false,
			panelIndex: 0,
			panelStyle: {},
			panelPlacement: null,
			triggerFocusPanel: false,
			visiblePanel: false,
			isAniVisible: false,
			isActivated: false
		});
		const internalData = createInternalData$9();
		const refMaps = { refElem };
		const computeFormReadonly = computed(() => {
			const { readonly } = props;
			if (readonly === null) {
				if ($xeForm) return $xeForm.props.readonly;
				return false;
			}
			return readonly;
		});
		const computeIsDisabled = computed(() => {
			const { disabled } = props;
			if (disabled === null) {
				if ($xeForm) return $xeForm.props.disabled;
				return false;
			}
			return disabled;
		});
		const computeBtnTransfer = computed(() => {
			const { transfer } = props;
			if (transfer === null) {
				const globalTransfer = getConfig().select.transfer;
				if (import_xe_utils.default.isBoolean(globalTransfer)) return globalTransfer;
				if ($xeTable || $xeModal || $xeDrawer || $xeForm) return true;
			}
			return transfer;
		});
		const computePopupOpts = computed(() => {
			return Object.assign({}, getConfig().cascader.popupConfig, props.popupConfig);
		});
		const computePropsOpts = computed(() => {
			return props.optionProps || {};
		});
		const computeNodeKeyField = computed(() => {
			return computeValueField.value;
		});
		const computeLabelField = computed(() => {
			return computePropsOpts.value.label || "label";
		});
		const computeValueField = computed(() => {
			return computePropsOpts.value.value || "value";
		});
		const computeChildrenField = computed(() => {
			return computePropsOpts.value.children || "children";
		});
		const computeRemoteOpts = computed(() => {
			return Object.assign({}, getConfig().cascader.remoteConfig, props.remoteConfig);
		});
		const computeSelectLabel = computed(() => {
			const { modelValue, lazyOptions } = props;
			const { fullNodeMaps } = internalData;
			const valueField = computeValueField.value;
			const labelField = computeLabelField.value;
			return (import_xe_utils.default.eqNull(modelValue) ? [] : import_xe_utils.default.isArray(modelValue) ? modelValue : [modelValue]).map((val) => {
				const cacheItem = fullNodeMaps[val];
				if (cacheItem) return cacheItem.item[labelField];
				if (lazyOptions) {
					const lazyItem = lazyOptions.find((item) => item[valueField] === val);
					if (lazyItem) return lazyItem[labelField];
				}
				return val;
			}).join(", ");
		});
		const computePopupWrapperStyle = computed(() => {
			const { height, width } = computePopupOpts.value;
			const stys = {};
			if (width) stys.width = toCssUnit(width);
			if (height) {
				stys.height = toCssUnit(height);
				stys.maxHeight = toCssUnit(height);
			}
			return stys;
		});
		const computeMaps = {};
		const $xeCascader = {
			xID,
			props,
			context,
			reactData,
			internalData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $cascader: $xeCascader }, params));
		};
		const emitModel = (value) => {
			emit("update:modelValue", value);
		};
		const cascaderMethods = { dispatchEvent };
		const getNodeid = (option) => {
			const nodeid = option[computeNodeKeyField.value];
			return nodeid ? encodeURIComponent(nodeid) : "";
		};
		const cacheDataMap = () => {
			const { transform, options } = props;
			const nodeKeyField = computeNodeKeyField.value;
			const childrenField = computeChildrenField.value;
			const valueField = computeValueField.value;
			const nodeMaps = {};
			const keyMaps = {};
			const handleOptNode = (item, index, items, path, parent, nodes) => {
				let nodeid = getNodeid(item);
				if (!nodeid) nodeid = getOptUniqueId$1();
				if (keyMaps[nodeid]) errLog("vxe.error.repeatKey", [`[tree-select] ${nodeKeyField}`, nodeid]);
				keyMaps[nodeid] = true;
				const value = item[valueField];
				if (nodeMaps[value]) errLog("vxe.error.repeatKey", [`[tree-select] ${valueField}`, value]);
				nodeMaps[value] = {
					item,
					index,
					items,
					parent,
					nodes
				};
			};
			if (options) if (transform) options.forEach((item, index, items) => {
				handleOptNode(item, index, items, [], null, []);
			});
			else import_xe_utils.default.eachTree(options, handleOptNode, { children: childrenField });
			internalData.fullOptionList = options || [];
			internalData.fullNodeMaps = nodeMaps;
		};
		const updateZindex = () => {
			const { zIndex } = props;
			if (zIndex) reactData.panelIndex = zIndex;
			else if (reactData.panelIndex < getLastZIndex()) reactData.panelIndex = nextZIndex();
		};
		const updatePlacement = () => {
			const { placement } = props;
			const { panelIndex } = reactData;
			const targetElem = refElem.value;
			const panelElem = refOptionPanel.value;
			const btnTransfer = computeBtnTransfer.value;
			const popupOpts = computePopupOpts.value;
			const handleStyle = () => {
				const ppObj = updatePanelPlacement(targetElem, panelElem, {
					placement: popupOpts.placement || placement,
					defaultPlacement: popupOpts.defaultPlacement,
					teleportTo: btnTransfer
				});
				reactData.panelStyle = Object.assign(ppObj.style, { zIndex: panelIndex });
				reactData.panelPlacement = ppObj.placement;
			};
			handleStyle();
			return nextTick().then(handleStyle);
		};
		const showOptionPanel = () => {
			const { loading, remote, filterable } = props;
			const { fullOptionList } = internalData;
			const isDisabled = computeIsDisabled.value;
			const remoteOpts = computeRemoteOpts.value;
			if (!loading && !isDisabled) {
				clearTimeout(internalData.hpTimeout);
				if (!reactData.initialized) reactData.initialized = true;
				reactData.isActivated = true;
				reactData.isAniVisible = true;
				if (filterable) {
					if (remote && remoteOpts.enabled && remoteOpts.autoLoad && !fullOptionList.length) handleSearchEvent();
				}
				setTimeout(() => {
					reactData.visiblePanel = true;
					handleFocusSearch();
				}, 10);
				updateZindex();
				updatePlacement();
			}
		};
		const hideOptionPanel = () => {
			reactData.visiblePanel = false;
			internalData.hpTimeout = setTimeout(() => {
				reactData.isAniVisible = false;
			}, 350);
		};
		const changeEvent = (evnt, selectValue, node) => {
			const value = import_xe_utils.default.isArray(selectValue) ? selectValue.map(deNodeValue) : deNodeValue(selectValue);
			emitModel(value);
			if (value !== props.modelValue) {
				dispatchEvent("change", {
					value,
					node,
					option: node
				}, evnt);
				if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, value);
			}
		};
		const clearValueEvent = (evnt, selectValue) => {
			changeEvent(evnt, selectValue, null);
			dispatchEvent("clear", { value: selectValue }, evnt);
		};
		const clearEvent = (params, evnt) => {
			clearValueEvent(evnt, null);
			hideOptionPanel();
		};
		const allCheckedPanelEvent = (params) => {
			const { $event } = params;
			const { multiple, autoClose } = props;
			const $tree = refTree.value;
			if (multiple) {
				if ($tree) $tree.setAllCheckboxNode(true).then(({ checkNodeKeys, checkNodes }) => {
					changeEvent($event, checkNodeKeys, checkNodes[0]);
					dispatchEvent("all-change", { value: checkNodeKeys }, $event);
					if (autoClose) hideOptionPanel();
				});
			}
		};
		const clearCheckedPanelEvent = (params) => {
			const { $event } = params;
			const { multiple, autoClose } = props;
			const $tree = refTree.value;
			if ($tree) {
				const value = multiple ? [] : null;
				$tree.clearCheckboxNode().then(() => {
					if (autoClose) hideOptionPanel();
				});
				changeEvent($event, value, null);
				dispatchEvent("clear", { value }, $event);
			}
		};
		const handleGlobalMousewheelEvent = (evnt) => {
			const { visiblePanel } = reactData;
			if (!computeIsDisabled.value) {
				if (visiblePanel) {
					const panelElem = refOptionPanel.value;
					if (getEventTargetNode(evnt, panelElem).flag) updatePlacement();
					else hideOptionPanel();
				}
			}
		};
		const handleGlobalMousedownEvent = (evnt) => {
			const { visiblePanel } = reactData;
			if (!computeIsDisabled.value) {
				const el = refElem.value;
				const panelElem = refOptionPanel.value;
				reactData.isActivated = getEventTargetNode(evnt, el).flag || getEventTargetNode(evnt, panelElem).flag;
				if (visiblePanel && !reactData.isActivated) hideOptionPanel();
			}
		};
		const handleGlobalBlurEvent = () => {
			const { visiblePanel, isActivated } = reactData;
			if (visiblePanel) hideOptionPanel();
			if (isActivated) reactData.isActivated = false;
			if (visiblePanel || isActivated) {
				const $input = refInput.value;
				if ($input) $input.blur();
			}
		};
		const handleGlobalResizeEvent = () => {
			const { visiblePanel } = reactData;
			if (visiblePanel) updatePlacement();
		};
		const handleFocusSearch = () => {
			if (props.filterable) nextTick(() => {
				const inpSearch = refInpSearch.value;
				if (inpSearch) inpSearch.focus();
			});
		};
		const focusEvent = (evnt) => {
			if (!computeIsDisabled.value) {
				if (!reactData.visiblePanel) {
					reactData.triggerFocusPanel = true;
					showOptionPanel();
					setTimeout(() => {
						reactData.triggerFocusPanel = false;
					}, 150);
				}
			}
			dispatchEvent("focus", {}, evnt);
		};
		const clickEvent = (evnt) => {
			togglePanelEvent(evnt);
			dispatchEvent("click", {}, evnt);
		};
		const blurEvent = (evnt) => {
			reactData.isActivated = false;
			dispatchEvent("blur", {}, evnt);
		};
		const modelSearchEvent = (value) => {
			reactData.searchValue = value;
		};
		const handleSearchEvent = () => {
			const { modelValue, remote, remoteMethod } = props;
			const { searchValue } = reactData;
			const remoteOpts = computeRemoteOpts.value;
			const queryMethod = remoteOpts.queryMethod || remoteMethod;
			if (remote && queryMethod && remoteOpts.enabled) {
				reactData.searchLoading = true;
				Promise.resolve(queryMethod({
					$cascader: $xeCascader,
					searchValue,
					value: modelValue
				})).then(() => nextTick()).catch(() => nextTick()).finally(() => {
					reactData.searchLoading = false;
				});
			}
		};
		const togglePanelEvent = (params) => {
			const { $event } = params;
			$event.preventDefault();
			if (reactData.triggerFocusPanel) reactData.triggerFocusPanel = false;
			else if (reactData.visiblePanel) hideOptionPanel();
			else showOptionPanel();
		};
		Object.assign($xeCascader, cascaderMethods, {});
		const renderVN = () => {
			const { className, modelValue, multiple, loading, filterable, showTotalButoon, showCheckedButoon, showClearButton } = props;
			const { initialized, isActivated, isAniVisible, visiblePanel, searchValue } = reactData;
			const vSize = computeSize.value;
			const isDisabled = computeIsDisabled.value;
			const selectLabel = computeSelectLabel.value;
			const btnTransfer = computeBtnTransfer.value;
			const formReadonly = computeFormReadonly.value;
			const popupWrapperStyle = computePopupWrapperStyle.value;
			const headerSlot = slots.header;
			const footerSlot = slots.footer;
			const prefixSlot = slots.prefix;
			const popupClassName = computePopupOpts.value.className;
			if (formReadonly) return h("div", {
				ref: refElem,
				class: ["vxe-cascader--readonly", className]
			}, [h("span", { class: "vxe-cascader-label" }, selectLabel)]);
			const selectVals = import_xe_utils.default.eqNull(modelValue) ? [] : import_xe_utils.default.isArray(modelValue) ? modelValue : [modelValue];
			return h("div", {
				ref: refElem,
				class: [
					"vxe-cascader",
					className ? import_xe_utils.default.isFunction(className) ? className({ $cascader: $xeCascader }) : className : "",
					{
						[`size--${vSize}`]: vSize,
						"is--filterable": filterable,
						"is--visible": visiblePanel,
						"is--disabled": isDisabled,
						"is--loading": loading,
						"is--active": isActivated
					}
				]
			}, [h(input_default, {
				ref: refInput,
				clearable: props.clearable,
				placeholder: loading ? getI18n("vxe.select.loadingText") : props.placeholder,
				editable: false,
				disabled: isDisabled,
				type: "text",
				prefixIcon: props.prefixIcon,
				suffixIcon: loading ? getIcon().TREE_SELECT_LOADED : visiblePanel ? getIcon().TREE_SELECT_OPEN : getIcon().TREE_SELECT_CLOSE,
				modelValue: loading ? "" : selectLabel,
				title: selectLabel,
				onClear: clearEvent,
				onClick: clickEvent,
				onFocus: focusEvent,
				onBlur: blurEvent,
				onSuffixClick: togglePanelEvent
			}, prefixSlot ? { prefix: () => prefixSlot({}) } : {}), h(Teleport, {
				to: "body",
				disabled: btnTransfer ? !initialized : true
			}, [h("div", {
				ref: refOptionPanel,
				class: [
					"vxe-table--ignore-clear vxe-cascader--panel",
					popupClassName ? import_xe_utils.default.isFunction(popupClassName) ? popupClassName({ $cascader: $xeCascader }) : popupClassName : "",
					{
						[`size--${vSize}`]: vSize,
						"is--transfer": btnTransfer,
						"ani--leave": !loading && isAniVisible,
						"ani--enter": !loading && visiblePanel
					}
				],
				placement: reactData.panelPlacement,
				style: reactData.panelStyle
			}, initialized ? [h("div", { class: "vxe-cascader--panel-wrapper" }, [
				filterable ? h("div", { class: "vxe-cascader--panel-search" }, [h(input_default, {
					ref: refInpSearch,
					class: "vxe-cascader-search--input",
					modelValue: searchValue,
					clearable: true,
					disabled: false,
					readonly: false,
					placeholder: getI18n("vxe.cascader.search"),
					prefixIcon: getIcon().INPUT_SEARCH,
					"onUpdate:modelValue": modelSearchEvent
				})]) : renderEmptyElement($xeCascader),
				showTotalButoon || showCheckedButoon && multiple || showClearButton || headerSlot ? h("div", { class: "vxe-cascader--panel-header" }, headerSlot ? headerSlot({}) : [h("div", { class: "vxe-cascader--header-button" }, [showTotalButoon ? h("div", { class: "vxe-cascader--header-total" }, getI18n("vxe.cascader.total", [selectVals.length])) : renderEmptyElement($xeCascader), h("div", { class: "vxe-cascader--header-btns" }, [showCheckedButoon && multiple ? h(VxeButtonComponent, {
					content: getI18n("vxe.cascader.allChecked"),
					mode: "text",
					onClick: allCheckedPanelEvent
				}) : renderEmptyElement($xeCascader), showClearButton ? h(VxeButtonComponent, {
					content: getI18n("vxe.cascader.clearChecked"),
					mode: "text",
					onClick: clearCheckedPanelEvent
				}) : renderEmptyElement($xeCascader)])])]) : renderEmptyElement($xeCascader),
				h("div", { class: "vxe-cascader--panel-body" }, [h("div", {
					ref: refTreeWrapper,
					class: "vxe-cascader-tree--wrapper",
					style: popupWrapperStyle
				}, [])]),
				footerSlot ? h("div", { class: "vxe-cascader--panel-footer" }, footerSlot({})) : renderEmptyElement($xeCascader)
			])] : [])])]);
		};
		watch(() => props.options, () => {
			cacheDataMap();
		});
		cacheDataMap();
		onMounted(() => {
			globalEvents.on($xeCascader, "mousewheel", handleGlobalMousewheelEvent);
			globalEvents.on($xeCascader, "mousedown", handleGlobalMousedownEvent);
			globalEvents.on($xeCascader, "blur", handleGlobalBlurEvent);
			globalEvents.on($xeCascader, "resize", handleGlobalResizeEvent);
		});
		onUnmounted(() => {
			globalEvents.off($xeCascader, "mousewheel");
			globalEvents.off($xeCascader, "mousedown");
			globalEvents.off($xeCascader, "blur");
			globalEvents.off($xeCascader, "resize");
			import_xe_utils.default.assign(internalData, createInternalData$9());
		});
		provide("$xeCascader", $xeCascader);
		$xeCascader.renderVN = renderVN;
		return $xeCascader;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/cascader/index.js
var VxeCascader = Object.assign({}, cascader_default, { install(app) {
	app.component(cascader_default.name, cascader_default);
} });
dynamicApp.use(VxeCascader);
VxeUI.component(cascader_default);
var Cascader = VxeCascader;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/checkbox/src/button.js
var button_default = defineVxeComponent({
	name: "VxeCheckboxButton",
	props: {
		modelValue: [
			String,
			Number,
			Boolean
		],
		label: {
			type: [
				String,
				Number,
				Boolean
			],
			default: null
		},
		title: [String, Number],
		checkedValue: {
			type: [
				String,
				Number,
				Boolean
			],
			default: true
		},
		uncheckedValue: {
			type: [
				String,
				Number,
				Boolean
			],
			default: false
		},
		content: [String, Number],
		disabled: {
			type: Boolean,
			default: null
		},
		size: {
			type: String,
			default: () => getConfig().checkboxButton.size || getConfig().size
		}
	},
	emits: ["update:modelValue", "change"],
	setup(props, context) {
		const { slots, emit } = context;
		const $xeForm = inject("$xeForm", null);
		const formItemInfo = inject("xeFormItemInfo", null);
		const $xeCheckboxGroup = inject("$xeCheckboxGroup", null);
		const xID = import_xe_utils.default.uniqueId();
		const reactData = reactive({});
		const { computeSize } = useSize(props);
		const $xeCheckboxButton = {
			xID,
			props,
			context,
			reactData
		};
		const computeIsChecked = computed(() => {
			if ($xeCheckboxGroup) return import_xe_utils.default.includes($xeCheckboxGroup.props.modelValue, props.label);
			return props.modelValue === props.checkedValue;
		});
		const computeIsDisabled = computed(() => {
			const { disabled } = props;
			const isChecked = computeIsChecked.value;
			if (disabled === null) {
				if ($xeCheckboxGroup) {
					const { computeIsDisabled: computeIsGroupDisabled, computeIsMaximize: computeIsGroupMaximize } = $xeCheckboxGroup.getComputeMaps();
					const isGroupDisabled = computeIsGroupDisabled.value;
					const isGroupMaximize = computeIsGroupMaximize.value;
					return isGroupDisabled || isGroupMaximize && !isChecked;
				}
			}
			return disabled;
		});
		const changeEvent = (evnt) => {
			const { checkedValue, uncheckedValue } = props;
			if (!computeIsDisabled.value) {
				const checked = evnt.target.checked;
				const value = checked ? checkedValue : uncheckedValue;
				const params = {
					checked,
					value,
					label: props.label
				};
				if ($xeCheckboxGroup) $xeCheckboxGroup.handleChecked(params, evnt);
				else {
					emit("update:modelValue", value);
					$xeCheckboxButton.dispatchEvent("change", params, evnt);
					if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, value);
				}
			}
		};
		Object.assign($xeCheckboxButton, { dispatchEvent(type, params, evnt) {
			emit(type, createEvent(evnt, { $checkboxButton: $xeCheckboxButton }, params));
		} }, {});
		const renderVN = () => {
			const { label } = props;
			const vSize = computeSize.value;
			const isDisabled = computeIsDisabled.value;
			const isChecked = computeIsChecked.value;
			return h("label", {
				key: label,
				class: ["vxe-checkbox vxe-checkbox--button", {
					[`size--${vSize}`]: vSize,
					"is--disabled": isDisabled
				}],
				title: props.title
			}, [h("input", {
				class: "vxe-checkbox--input",
				type: "checkbox",
				disabled: isDisabled,
				checked: isChecked,
				onChange: changeEvent
			}), h("span", { class: "vxe-checkbox--label" }, slots.default ? slots.default({}) : getFuncText(props.content))]);
		};
		$xeCheckboxButton.renderVN = renderVN;
		return renderVN;
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/checkbox-button/index.js
var VxeCheckboxButton = Object.assign(button_default, { install: function(app) {
	app.component(button_default.name, button_default);
} });
dynamicApp.use(VxeCheckboxButton);
VxeUI.component(button_default);
var CheckboxButton = VxeCheckboxButton;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/checkbox/src/group.js
function createInternalData$8() {
	return {};
}
var group_default$1 = defineVxeComponent({
	name: "VxeCheckboxGroup",
	props: {
		modelValue: Array,
		options: Array,
		optionProps: Object,
		disabled: {
			type: Boolean,
			default: null
		},
		max: {
			type: [String, Number],
			default: null
		},
		size: {
			type: String,
			default: () => getConfig().checkboxGroup.size || getConfig().size
		},
		defaultConfig: Object
	},
	emits: [
		"update:modelValue",
		"change",
		"default-change"
	],
	setup(props, context) {
		const { slots, emit } = context;
		const $xeForm = inject("$xeForm", null);
		const formItemInfo = inject("xeFormItemInfo", null);
		const xID = import_xe_utils.default.uniqueId();
		const reactData = reactive({});
		const internalData = createInternalData$8();
		const computeIsDisabled = computed(() => {
			const { disabled } = props;
			if (disabled === null) {
				if ($xeForm) return $xeForm.props.readonly || $xeForm.props.disabled;
				return false;
			}
			return disabled;
		});
		const computeIsMaximize = computed(() => {
			const { modelValue, max } = props;
			if (max) return (modelValue ? modelValue.length : 0) >= import_xe_utils.default.toNumber(max);
			return false;
		});
		const computeDefaultOpts = computed(() => {
			return Object.assign({}, props.defaultConfig);
		});
		const computePropsOpts = computed(() => {
			return Object.assign({}, props.optionProps);
		});
		const computeLabelField = computed(() => {
			return computePropsOpts.value.label || "label";
		});
		const computeValueField = computed(() => {
			return computePropsOpts.value.value || "value";
		});
		const computeDisabledField = computed(() => {
			return computePropsOpts.value.disabled || "disabled";
		});
		const computeMaps = {
			computeIsMaximize,
			computeIsDisabled
		};
		const $xeCheckboxGroup = {
			xID,
			props,
			context,
			reactData,
			getComputeMaps: () => computeMaps
		};
		useSize(props);
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $checkboxGroup: $xeCheckboxGroup }, params));
		};
		const emitModel = (value) => {
			emit("update:modelValue", value);
		};
		const emitDefaultValue = (value) => {
			emitModel(value);
			dispatchEvent("default-change", { value }, null);
		};
		const checkboxGroupMethods = { dispatchEvent };
		const checkboxGroupPrivateMethods = { handleChecked(params, evnt) {
			const { checked, label } = params;
			const checklist = props.modelValue || [];
			const checkIndex = checklist.indexOf(label);
			if (checked) {
				if (checkIndex === -1) checklist.push(label);
			} else checklist.splice(checkIndex, 1);
			emitModel(checklist);
			$xeCheckboxGroup.dispatchEvent("change", Object.assign({}, params, {
				checklist,
				value: checklist
			}), evnt);
			if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, checklist);
		} };
		const loadData = (datas) => {
			const { isLoaded } = internalData;
			const defaultOpts = computeDefaultOpts.value;
			const valueField = computeValueField.value;
			if (!isLoaded) {
				const { selectMode } = defaultOpts;
				if (datas.length > 0 && import_xe_utils.default.eqNull(props.modelValue)) {
					if (selectMode === "all") nextTick(() => {
						emitDefaultValue(datas.map((item) => item[valueField]));
					});
					else if (selectMode === "first" || selectMode === "last") {
						const selectItem = import_xe_utils.default[selectMode](datas);
						if (selectItem) nextTick(() => {
							if (import_xe_utils.default.eqNull(props.modelValue)) emitDefaultValue([selectItem[valueField]]);
						});
					}
					internalData.isLoaded = true;
				}
			}
			return nextTick();
		};
		Object.assign($xeCheckboxGroup, checkboxGroupMethods, checkboxGroupPrivateMethods);
		const renderVN = () => {
			const { options } = props;
			const defaultSlot = slots.default;
			const valueField = computeValueField.value;
			const labelField = computeLabelField.value;
			const disabledField = computeDisabledField.value;
			return h("div", { class: "vxe-checkbox-group" }, defaultSlot ? defaultSlot({}) : options ? options.map((item) => {
				return h(checkbox_default, {
					key: item[valueField],
					label: item[valueField],
					content: item[labelField],
					disabled: item[disabledField]
				});
			}) : []);
		};
		watch(() => props.options, (val) => {
			loadData(val || []);
		});
		onMounted(() => {
			nextTick(() => {
				const { options } = props;
				if (options) loadData(options);
			});
		});
		onUnmounted(() => {
			import_xe_utils.default.assign(internalData, createInternalData$8());
		});
		provide("$xeCheckboxGroup", $xeCheckboxGroup);
		$xeCheckboxGroup.renderVN = renderVN;
		return renderVN;
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/checkbox-group/index.js
var VxeCheckboxGroup = Object.assign(group_default$1, { install(app) {
	app.component(group_default$1.name, group_default$1);
} });
dynamicApp.use(VxeCheckboxGroup);
VxeUI.component(group_default$1);
var CheckboxGroup = VxeCheckboxGroup;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/row/src/col.js
var col_default = defineVxeComponent({
	name: "VxeCol",
	props: {
		span: [Number, String],
		align: String,
		width: [Number, String],
		fill: Boolean,
		ellipsis: Boolean,
		size: {
			type: String,
			default: () => getConfig().col.size || getConfig().size
		}
	},
	emits: [],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		useSize(props);
		const refElem = ref();
		const reactData = reactive({});
		const refMaps = { refElem };
		const $xeRow = inject("$xeRow", null);
		const computeRowGutter = computed(() => {
			if ($xeRow) return $xeRow.props.gutter;
			return null;
		});
		const computeRowVertical = computed(() => {
			if ($xeRow) return $xeRow.props.vertical;
			return null;
		});
		const computeColStyle = computed(() => {
			const { width } = props;
			const rowGutter = computeRowGutter.value;
			const rowVertical = computeRowVertical.value;
			const style = {};
			if (rowGutter) {
				let [lrGutter, tbGutter] = import_xe_utils.default.isArray(rowGutter) ? rowGutter : [rowGutter];
				if (rowVertical) {
					tbGutter = lrGutter;
					lrGutter = "";
				}
				if (lrGutter) {
					const padding = import_xe_utils.default.isNumber(lrGutter) ? toCssUnit(lrGutter / 2) : `calc(${toCssUnit(lrGutter)} / 2)`;
					style.paddingLeft = padding;
					style.paddingRight = padding;
				}
				if (tbGutter) {
					const padding = import_xe_utils.default.isNumber(tbGutter) ? toCssUnit(tbGutter / 2) : `calc(${toCssUnit(tbGutter)} / 2)`;
					style.paddingTop = padding;
					style.paddingBottom = padding;
				}
			}
			if (width) style.width = toCssUnit(width);
			return style;
		});
		const computeMaps = {};
		const $xeCol = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $col: $xeCol }, params));
		};
		Object.assign($xeCol, { dispatchEvent }, {});
		const renderVN = () => {
			const { span, fill, align, width, ellipsis } = props;
			const colStyle = computeColStyle.value;
			const defaultSlot = slots.default;
			return h("div", {
				ref: refElem,
				class: [
					"vxe-col",
					span ? `span${span}` : "",
					align ? `align--${align}` : "",
					{
						"is--span": !!span,
						"is--width": !!width,
						"is--fill": fill,
						"is--ellipsis": ellipsis
					}
				],
				style: colStyle
			}, [h("div", { class: "vxe-col--inner" }, defaultSlot ? defaultSlot({}) : [])]);
		};
		$xeCol.renderVN = renderVN;
		return $xeCol;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/col/index.js
var VxeCol = Object.assign({}, col_default, { install(app) {
	app.component(col_default.name, col_default);
} });
dynamicApp.use(VxeCol);
VxeUI.component(col_default);
var Col = VxeCol;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/collapse/src/collapse.js
var collapse_default = defineVxeComponent({
	name: "VxeCollapse",
	props: {
		modelValue: Array,
		options: Array,
		padding: {
			type: Boolean,
			default: () => getConfig().collapse.padding
		},
		expandConfig: Object,
		size: {
			type: String,
			default: () => getConfig().collapse.size || getConfig().size
		}
	},
	emits: [
		"update:modelValue",
		"load",
		"change",
		"toggle-expand"
	],
	setup(props, context) {
		const { emit, slots } = context;
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const refElem = ref();
		const reactData = reactive({
			staticPanes: [],
			activeNames: [],
			initNames: [],
			cachePaneMaps: {}
		});
		const refMaps = { refElem };
		const computeItemOptions = computed(() => {
			const { options } = props;
			return (options || []).filter((item) => handleFilterItem(item));
		});
		const computeItemStaticOptions = computed(() => {
			const { staticPanes } = reactData;
			return staticPanes.filter((item) => handleFilterItem(item));
		});
		const computeExpandOpts = computed(() => {
			return Object.assign({}, getConfig().collapse.expandConfig, props.expandConfig);
		});
		const computeMaps = {};
		const $xeCollapse = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const handleFilterItem = (item) => {
			const { permissionCode } = item;
			if (permissionCode) {
				if (!permission.checkVisible(permissionCode)) return false;
			}
			return true;
		};
		const addInitName = (name) => {
			const { initNames } = reactData;
			if (name && !initNames.includes(name)) {
				initNames.push(name);
				dispatchEvent("load", { name }, null);
				return true;
			}
			return false;
		};
		const initDefaultName = (list) => {
			const { activeNames } = reactData;
			const nameMaps = {};
			if (list && list.length) list.forEach((item) => {
				const { name, preload } = item || {};
				if (name) {
					const isActive = activeNames.includes(name);
					nameMaps[`${name}`] = { loading: false };
					if (isActive) addInitName(name);
					if (preload) {
						if (!isActive) activeNames.push(name);
					}
				}
			});
			reactData.activeNames = activeNames ? activeNames.slice(0) : [];
			reactData.cachePaneMaps = nameMaps;
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $collapse: $xeCollapse }, params));
		};
		const collapseMethods = { dispatchEvent };
		const callSlot = (slotFunc, params) => {
			if (slotFunc) {
				if (import_xe_utils.default.isString(slotFunc)) slotFunc = slots[slotFunc] || null;
				if (import_xe_utils.default.isFunction(slotFunc)) return getSlotVNs$1(slotFunc(params));
			}
			return [];
		};
		const handleClickEvent = (evnt, item) => {
			const { activeNames } = reactData;
			const { name } = item;
			if (name) {
				const aIndex = activeNames.indexOf(name);
				let expanded = false;
				if (aIndex === -1) {
					expanded = true;
					activeNames.push(name);
				} else activeNames.splice(aIndex, 1);
				addInitName(name);
				dispatchEvent("change", {
					value: activeNames,
					name
				}, evnt);
				dispatchEvent("toggle-expand", {
					value: activeNames,
					name,
					expanded
				}, evnt);
			}
		};
		Object.assign($xeCollapse, collapseMethods, {});
		const renderList = (itemList) => {
			const { activeNames, initNames } = reactData;
			const expandOpts = computeExpandOpts.value;
			return itemList.map((item) => {
				const { icon, name, title, slots } = item;
				const titleSlot = slots ? slots.title : null;
				const defaultSlot = slots ? slots.default : null;
				const isActive = name && activeNames.includes(name);
				return h("div", { class: "vxe-collapse-item" }, [h("div", {
					class: "vxe-collapse--item-header",
					onClick(evnt) {
						handleClickEvent(evnt, item);
					}
				}, [
					expandOpts.showIcon ? h("span", { class: "vxe-collapse--item-switch" }, [h("i", { class: isActive ? getIcon().COLLAPSE_OPEN : getIcon().COLLAPSE_CLOSE })]) : renderEmptyElement($xeCollapse),
					icon ? h("span", { class: "vxe-collapse--item-icon" }, [h("i", { class: icon })]) : renderEmptyElement($xeCollapse),
					h("span", { class: "vxe-collapse--item-name" }, titleSlot ? callSlot(titleSlot, {
						name,
						title
					}) : `${title}`)
				]), h("div", { class: ["vxe-collapse--item-content", { "is--visible": isActive }] }, [name && initNames.includes(name) ? h("div", { class: "vxe-collapse--item-inner" }, [defaultSlot ? callSlot(defaultSlot, {
					name,
					title
				}) : ""]) : renderEmptyElement($xeCollapse)])]);
			});
		};
		const renderVN = () => {
			const { padding } = props;
			const vSize = computeSize.value;
			const itemOptions = computeItemOptions.value;
			const itemStaticOptions = computeItemStaticOptions.value;
			const defaultSlot = slots.default;
			const itemList = defaultSlot ? itemStaticOptions : itemOptions;
			return h("div", {
				ref: refElem,
				class: ["vxe-collapse", {
					[`size--${vSize}`]: vSize,
					"is--padding": padding
				}]
			}, [h("div", { class: "vxe-collapse-slots" }, defaultSlot ? defaultSlot({}) : []), renderList(itemList)]);
		};
		watch(() => props.modelValue, (val) => {
			reactData.activeNames = val || [];
		});
		const optsFlag = ref(0);
		watch(() => props.options ? props.options.length : -1, () => {
			optsFlag.value++;
		});
		watch(() => props.options, () => {
			optsFlag.value++;
		});
		watch(optsFlag, () => {
			initDefaultName(props.options);
		});
		const stFlag = ref(0);
		watch(() => reactData.staticPanes ? reactData.staticPanes.length : -1, () => {
			stFlag.value++;
		});
		watch(() => reactData.staticPanes, () => {
			stFlag.value++;
		});
		watch(stFlag, () => {
			initDefaultName(reactData.staticPanes);
		});
		reactData.activeNames = props.modelValue || [];
		initDefaultName(reactData.staticPanes.length ? reactData.staticPanes : props.options);
		provide("$xeCollapse", $xeCollapse);
		$xeCollapse.renderVN = renderVN;
		return $xeCollapse;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/collapse/index.js
var VxeCollapse = Object.assign({}, collapse_default, { install(app) {
	app.component(collapse_default.name, collapse_default);
} });
dynamicApp.use(VxeCollapse);
VxeUI.component(collapse_default);
var Collapse = VxeCollapse;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/collapse/src/util.js
function assembleCollapseItem($xeCollapse, elem, collapseConfig) {
	const staticPanes = $xeCollapse.reactData.staticPanes;
	const parentElem = elem.parentNode;
	if (parentElem) {
		staticPanes.splice(import_xe_utils.default.arrayIndexOf(parentElem.children, elem), 0, collapseConfig);
		$xeCollapse.reactData.staticPanes = staticPanes.slice(0);
	}
}
function destroyCollapseItem($xeCollapse, collapseConfig) {
	const staticPanes = $xeCollapse.reactData.staticPanes;
	const matchObj = import_xe_utils.default.findTree(staticPanes, (item) => item.id === collapseConfig.id, { children: "children" });
	if (matchObj) matchObj.items.splice(matchObj.index, 1);
	$xeCollapse.reactData.staticPanes = staticPanes.slice(0);
}
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/collapse/src/collapse-pane.js
var collapse_pane_default = defineVxeComponent({
	name: "VxeCollapsePane",
	props: {
		title: [String, Number],
		name: [String, Number],
		icon: String,
		preload: Boolean,
		permissionCode: [String, Number]
	},
	emits: [],
	setup(props, context) {
		const { emit, slots } = context;
		const xID = import_xe_utils.default.uniqueId();
		const $xeCollapse = inject("$xeCollapse", null);
		const refElem = ref();
		const reactData = reactive({});
		const collapseConfig = reactive({
			id: xID,
			title: props.title,
			name: props.name,
			icon: props.icon,
			preload: props.preload,
			permissionCode: props.permissionCode,
			slots
		});
		const refMaps = { refElem };
		const computeMaps = {};
		const $xeCollapsePane = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $collapsePane: $xeCollapsePane }, params));
		};
		Object.assign($xeCollapsePane, { dispatchEvent }, {});
		const renderVN = () => {
			return h("div", { ref: refElem }, []);
		};
		watch(() => props.title, (val) => {
			collapseConfig.title = val;
		});
		watch(() => props.name, (val) => {
			collapseConfig.name = val;
		});
		watch(() => props.icon, (val) => {
			collapseConfig.icon = val;
		});
		watch(() => props.permissionCode, (val) => {
			collapseConfig.permissionCode = val;
		});
		onMounted(() => {
			const elem = refElem.value;
			if ($xeCollapse && elem) assembleCollapseItem($xeCollapse, elem, collapseConfig);
		});
		onUnmounted(() => {
			if ($xeCollapse) destroyCollapseItem($xeCollapse, collapseConfig);
		});
		$xeCollapsePane.renderVN = renderVN;
		return $xeCollapsePane;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/collapse-pane/index.js
var VxeCollapsePane = Object.assign({}, collapse_pane_default, { install(app) {
	app.component(collapse_pane_default.name, collapse_pane_default);
} });
dynamicApp.use(VxeCollapsePane);
VxeUI.component(collapse_pane_default);
var CollapsePane = VxeCollapsePane;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/color-picker/src/util.js
var hexAlphaKeyMaps = {};
var hexAlphaNumMaps = {};
"FF,FC,FA,F7,F5,F2,F0,ED,EB,E8,E6,E3,E0,DE,DB,D9,D6,D4,D1,CF,CC,C9,C7,C4,C2,BF,BD,BA,B8,B5,B3,B0,AD,AB,A8,A6,A3,A1,9E,9C,99,96,94,91,8F,8C,8A,87,85,82,80,7D,7A,78,75,73,70,6E,6B,69,66,63,61,5E,5C,59,57,54,52,4F,4D,4A,47,45,42,40,3D,3B,38,36,33,30,2E,2B,29,26,24,21,1F,1C,1A,17,14,12,0F,0D,0A,08,05,03,00".split(",").forEach((key, i) => {
	const num = (100 - i) / 100;
	hexAlphaKeyMaps[key] = num;
	hexAlphaNumMaps[num] = key;
});
var rgbRE = /^rgb(a?)\((\d{1,3})[,\s]+(\d{1,3})[,\s]+(\d{1,3})([,//\s]+([0-9.]{1,4})(%?))?\)$/;
var hexRE = /^(#?)([0-9A-Fa-f]{6})([0-9A-Fa-f]{2})?$/;
function parseColor(value) {
	const rest = {
		value: "",
		type: "",
		hex: "",
		hexV: "",
		hexA: "",
		r: 0,
		g: 0,
		b: 0,
		a: 1
	};
	if (value) {
		const hexRest = value.match(hexRE);
		if (hexRest) {
			rest.value = value;
			rest.type = "hex";
			rest.hex = value;
			rest.hexV = hexRest[2];
			rest.hexA = hexRest[3] || "";
			rest.a = (hexRest[3] ? hexAlphaKeyMaps[hexRest[3].toUpperCase()] : 1) || 1;
		}
		const rgbRest = value.match(rgbRE);
		if (rgbRest) {
			rest.value = value;
			rest.type = rgbRest[6] ? "rgba" : "rgb";
			rest.r = parseInt(rgbRest[2]);
			rest.g = parseInt(rgbRest[3]);
			rest.b = parseInt(rgbRest[4]);
			rest.a = rgbRest[7] ? parseInt(rgbRest[6]) / 100 : rgbRest[6] ? parseFloat(rgbRest[6]) : 1;
		}
	}
	return rest;
}
function updateColorAlpha(value, alpha = 1) {
	const { type, hexV, r, g, b } = parseColor(value);
	if (type === "rgb" || type === "rgba") return toRgb(r, g, b, alpha);
	if (type === "hex") return `#${hexV}${alpha === 1 ? "" : hexAlphaNumMaps[alpha] || ""}`;
	return "";
}
function toRgb(r, g, b, a) {
	if (import_xe_utils.default.eqNull(a)) return `rgb(${r},${g},${b})`;
	return `rgba(${r},${g},${b},${a})`;
}
function rgbToHex(rgbRest) {
	if (!rgbRest) return "";
	const { r, g, b, a = 1 } = rgbRest;
	return `#${r.toString(16).padStart(2, "0")}${g.toString(16).padStart(2, "0")}${b.toString(16).padStart(2, "0")}${a === 1 ? "" : hexAlphaNumMaps[a] || ""}`.toUpperCase();
}
function rgbToHsv(rgbRest) {
	if (!rgbRest) return null;
	const { r, g, b } = rgbRest;
	const rAbs = r / 255;
	const gAbs = g / 255;
	const bAbs = b / 255;
	let h = 0;
	let s = 0;
	const val = Math.max(rAbs, gAbs, bAbs);
	const num = val - Math.min(rAbs, gAbs, bAbs);
	const handleDiff = (c) => (val - c) / 6 / num + 1 / 2;
	if (num === 0) h = s = 0;
	else {
		s = num / val;
		const rr = handleDiff(rAbs);
		const gg = handleDiff(gAbs);
		const bb = handleDiff(bAbs);
		if (rAbs === val) h = bb - gg;
		else if (gAbs === val) h = 1 / 3 + rr - bb;
		else if (bAbs === val) h = 2 / 3 + gg - rr;
		if (h < 0) h += 1;
		else if (h > 1) h -= 1;
	}
	return {
		h: Math.round(h * 360),
		s: import_xe_utils.default.round(s, 2),
		v: import_xe_utils.default.round(val, 2)
	};
}
function hsvToRgb(h, s, v) {
	let r = 0;
	let g = 0;
	let b = 0;
	const i = Math.floor(h / 60);
	const f = h / 60 - i;
	const p = v * (1 - s);
	const q = v * (1 - f * s);
	const t = v * (1 - (1 - f) * s);
	switch (i) {
		case 0:
			r = v;
			g = t;
			b = p;
			break;
		case 1:
			r = q;
			g = v;
			b = p;
			break;
		case 2:
			r = p;
			g = v;
			b = t;
			break;
		case 3:
			r = p;
			g = q;
			b = v;
			break;
		case 4:
			r = t;
			g = p;
			b = v;
			break;
		case 5:
			r = v;
			g = p;
			b = q;
			break;
	}
	return {
		r: Math.round(r * 255),
		g: Math.round(g * 255),
		b: Math.round(b * 255)
	};
}
function hexToHsv(str) {
	const rgbRest = hexToRgb(str);
	if (rgbRest) return rgbToHsv(rgbRest);
	return null;
}
function hexToRgb(str) {
	if (str) {
		const rests = str.match(/^(#?)([0-9A-Fa-f]{2})([0-9A-Fa-f]{2})([0-9A-Fa-f]{2})([0-9A-Fa-f]{2})?$/);
		if (rests) return {
			r: parseInt(rests[2], 16),
			g: parseInt(rests[3], 16),
			b: parseInt(rests[4], 16),
			a: (rests[5] ? hexAlphaKeyMaps[rests[5].toUpperCase()] : 1) || 1
		};
	}
	return null;
}
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/color-picker/src/color-picker.js
var color_picker_default = defineVxeComponent({
	name: "VxeColorPicker",
	props: {
		modelValue: String,
		placeholder: String,
		clearable: {
			type: Boolean,
			default: () => getConfig().colorPicker.clearable
		},
		type: {
			type: String,
			default: () => getConfig().colorPicker.type
		},
		size: {
			type: String,
			default: () => getConfig().colorPicker.size || getConfig().size
		},
		className: [String, Function],
		popupClassName: [String, Function],
		colors: {
			type: Array,
			default: () => import_xe_utils.default.clone(getConfig().colorPicker.colors, true) || []
		},
		showAlpha: {
			type: Boolean,
			default: () => getConfig().colorPicker.showAlpha
		},
		showEyeDropper: {
			type: Boolean,
			default: () => getConfig().colorPicker.showEyeDropper
		},
		showColorExtractor: {
			type: Boolean,
			default: () => getConfig().colorPicker.showColorExtractor
		},
		showQuick: {
			type: Boolean,
			default: () => getConfig().colorPicker.showQuick
		},
		readonly: {
			type: Boolean,
			default: null
		},
		disabled: {
			type: Boolean,
			default: null
		},
		clickToCopy: {
			type: Boolean,
			default: () => getConfig().colorPicker.clickToCopy
		},
		placement: String,
		transfer: {
			type: Boolean,
			default: null
		},
		popupConfig: Object
	},
	emits: [
		"update:modelValue",
		"change",
		"clear",
		"click"
	],
	setup(props, context) {
		const { emit } = context;
		const $xeModal = inject("$xeModal", null);
		const $xeDrawer = inject("$xeDrawer", null);
		const $xeTable = inject("$xeTable", null);
		const $xeForm = inject("$xeForm", null);
		const formItemInfo = inject("xeFormItemInfo", null);
		const WinEyeDropper = typeof window !== "undefined" ? window.EyeDropper : null;
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const refElem = ref();
		const refInputTarget = ref();
		const refOptionPanel = ref();
		const refHueSliderElem = ref();
		const refHueSliderBtnElem = ref();
		const refAlphaSliderElem = ref();
		const refAlphaSliderBtnElem = ref();
		const refColorPanelElem = ref();
		const refColorActiveElem = ref();
		const reactData = reactive({
			initialized: false,
			selectTyle: "hex",
			selectColor: `${props.modelValue || ""}`,
			showTypePopup: false,
			panelColor: "",
			hexValue: "",
			rValue: 0,
			gValue: 0,
			bValue: 0,
			aValue: 0,
			panelIndex: 0,
			panelStyle: {},
			panelPlacement: null,
			visiblePanel: false,
			isAniVisible: false,
			isActivated: false
		});
		const typeList = [{
			label: "HEX",
			value: "hex"
		}, {
			label: "RGB",
			value: "rgb"
		}];
		const internalData = {};
		const computeFormReadonly = computed(() => {
			const { readonly } = props;
			if (readonly === null) {
				if ($xeForm) return $xeForm.props.readonly;
				return false;
			}
			return readonly;
		});
		const computeIsDisabled = computed(() => {
			const { disabled } = props;
			if (disabled === null) {
				if ($xeForm) return $xeForm.props.disabled;
				return false;
			}
			return disabled;
		});
		const computeBtnTransfer = computed(() => {
			const { transfer } = props;
			if (transfer === null) {
				const globalTransfer = getConfig().colorPicker.transfer;
				if (import_xe_utils.default.isBoolean(globalTransfer)) return globalTransfer;
				if ($xeTable || $xeModal || $xeDrawer || $xeForm) return true;
			}
			return transfer;
		});
		const computeColorList = computed(() => {
			const { colors } = props;
			if (colors) return colors.map((item) => {
				if (import_xe_utils.default.isString(item)) return {
					label: item,
					value: item
				};
				return {
					label: import_xe_utils.default.eqNull(item.label) ? item.value : item.label,
					value: item.value
				};
			});
			return [];
		});
		const computePopupOpts = computed(() => {
			return Object.assign({}, getConfig().colorPicker.popupConfig, props.popupConfig);
		});
		const computeIsRgb = computed(() => {
			const { selectTyle } = reactData;
			return selectTyle === "rgb";
		});
		const computeSelectTypeItem = computed(() => {
			const { selectTyle } = reactData;
			return typeList.find((item) => item.value === selectTyle);
		});
		const refMaps = { refElem };
		const computeMaps = {};
		const $xeColorPicker = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const emitModel = (value) => {
			emit("update:modelValue", value);
		};
		const updateMode = () => {
			const { modelValue } = props;
			reactData.selectColor = import_xe_utils.default.toValueString(modelValue);
			updateModelColor();
		};
		const updateType = () => {
			const { type } = props;
			let selectTyle = "hex";
			if (type === "rgb" || type === "rgba") selectTyle = "rgb";
			reactData.selectTyle = selectTyle;
			updateMode();
		};
		const updateModelColor = () => {
			const { selectColor, isAniVisible } = reactData;
			const isRgb = computeIsRgb.value;
			const hueSliderEl = refHueSliderElem.value;
			const alphaSliderEl = refAlphaSliderElem.value;
			const colorRest = parseColor(selectColor);
			reactData.hexValue = colorRest.hex;
			reactData.rValue = colorRest.r;
			reactData.gValue = colorRest.g;
			reactData.bValue = colorRest.b;
			reactData.aValue = colorRest.a;
			if (colorRest.value) {
				if (isRgb) {
					if (colorRest.type === "hex") {
						const rgbRest = hexToRgb(colorRest.hex);
						if (rgbRest) {
							reactData.rValue = rgbRest.r;
							reactData.gValue = rgbRest.g;
							reactData.bValue = rgbRest.b;
							reactData.aValue = rgbRest.a;
						}
					}
				} else if (colorRest.type !== "hex") reactData.hexValue = rgbToHex(colorRest);
			}
			if (isAniVisible) {
				const hsvRest = colorRest.type === "hex" ? hexToHsv(colorRest.hex) : rgbToHsv(colorRest);
				const colorPanelEl = refColorPanelElem.value;
				if (hsvRest) {
					if (colorPanelEl) {
						const offsetTop = colorPanelEl.clientHeight * (1 - hsvRest.v);
						handlePanelColor(colorPanelEl.clientWidth * hsvRest.s, offsetTop);
					}
					if (hueSliderEl) handleHueColor(import_xe_utils.default.ceil((1 - hsvRest.h / 360) * hueSliderEl.clientWidth));
				}
				if (alphaSliderEl) handleAlphaColor(alphaSliderEl.clientWidth * colorRest.a);
			}
		};
		const updateZindex = () => {
			if (reactData.panelIndex < getLastZIndex()) reactData.panelIndex = nextZIndex();
		};
		const updatePlacement = () => {
			const { placement } = props;
			const { panelIndex } = reactData;
			const targetElem = refElem.value;
			const panelElem = refOptionPanel.value;
			const btnTransfer = computeBtnTransfer.value;
			const popupOpts = computePopupOpts.value;
			const handleStyle = () => {
				const ppObj = updatePanelPlacement(targetElem, panelElem, {
					placement: popupOpts.placement || placement,
					defaultPlacement: popupOpts.defaultPlacement,
					teleportTo: btnTransfer
				});
				reactData.panelStyle = Object.assign(ppObj.style, { zIndex: panelIndex });
				reactData.panelPlacement = ppObj.placement;
			};
			handleStyle();
			return nextTick().then(handleStyle);
		};
		const showOptionPanel = () => {
			const { hpTimeout } = internalData;
			if (!computeIsDisabled.value) {
				if (hpTimeout) {
					clearTimeout(hpTimeout);
					internalData.hpTimeout = void 0;
				}
				if (!reactData.initialized) reactData.initialized = true;
				reactData.isActivated = true;
				reactData.isAniVisible = true;
				setTimeout(() => {
					updateModelColor();
					reactData.visiblePanel = true;
				}, 10);
				updateZindex();
				updatePlacement();
			}
		};
		const hideOptionPanel = () => {
			reactData.visiblePanel = false;
			internalData.hpTimeout = setTimeout(() => {
				reactData.isAniVisible = false;
			}, 350);
		};
		const changeEvent = (evnt, value) => {
			reactData.selectColor = value;
			if (value !== props.modelValue) {
				emitModel(value);
				dispatchEvent("change", { value }, evnt);
				if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, value);
			}
		};
		const clearValueEvent = (evnt, selectValue) => {
			changeEvent(evnt, selectValue);
			dispatchEvent("clear", { value: selectValue }, evnt);
		};
		const focusEvent = () => {
			if (!computeIsDisabled.value) {
				if (!reactData.visiblePanel) showOptionPanel();
			}
		};
		const blurEvent = () => {
			reactData.isActivated = false;
		};
		const clearEvent = (evnt) => {
			clearValueEvent(evnt, null);
			hideOptionPanel();
		};
		const confirmEvent = (evnt) => {
			const { selectColor } = reactData;
			changeEvent(evnt, selectColor);
			hideOptionPanel();
		};
		const togglePanelEvent = (evnt) => {
			evnt.preventDefault();
			if (reactData.visiblePanel) hideOptionPanel();
			else showOptionPanel();
		};
		const clickEvent = (evnt) => {
			togglePanelEvent(evnt);
			dispatchEvent("click", {}, evnt);
		};
		const handlePanelClickEvent = () => {
			reactData.showTypePopup = false;
		};
		const toggleTypeVisibleEvent = (evnt) => {
			evnt.stopPropagation();
			reactData.showTypePopup = !reactData.showTypePopup;
		};
		const handleChangeType = (type) => {
			const { selectTyle } = reactData;
			if (type !== selectTyle) {
				reactData.selectTyle = type;
				updateModelColor();
			}
			reactData.showTypePopup = false;
		};
		const handleHueColor = (offsetLeft) => {
			const hueSliderEl = refHueSliderElem.value;
			const hueSliderBtnEl = refHueSliderBtnElem.value;
			if (hueSliderEl && hueSliderBtnEl) {
				if (offsetLeft < 0) offsetLeft = 0;
				const barWidth = import_xe_utils.default.toInteger(hueSliderEl.clientWidth);
				const itemNum = 255;
				const countNum = itemNum * 6;
				const offsetX = import_xe_utils.default.ceil(countNum / barWidth * offsetLeft);
				const offsetNum = offsetX % itemNum;
				let rNum = 0;
				let gNum = 0;
				let bNum = 0;
				switch (Math.ceil(offsetX / itemNum)) {
					case 1:
						rNum = itemNum;
						bNum = offsetNum;
						break;
					case 2:
						rNum = itemNum - offsetNum;
						bNum = itemNum;
						break;
					case 3:
						gNum = offsetNum;
						bNum = itemNum;
						break;
					case 4:
						gNum = itemNum;
						bNum = itemNum - offsetNum;
						break;
					case 5:
						rNum = offsetNum;
						gNum = itemNum;
						break;
					case 6:
						rNum = itemNum;
						gNum = itemNum - offsetNum;
						break;
				}
				reactData.panelColor = toRgb(rNum, gNum, bNum);
				hueSliderBtnEl.style.left = toCssUnit(offsetLeft);
			}
		};
		const handleHueBarEvent = (evnt) => {
			const hueSliderEl = refHueSliderElem.value;
			const hueSliderBtnEl = refHueSliderBtnElem.value;
			if (hueSliderEl && hueSliderBtnEl) {
				const hueSliderRect = hueSliderEl.getBoundingClientRect();
				const barWidth = import_xe_utils.default.toInteger(hueSliderEl.clientWidth);
				handleHueColor(import_xe_utils.default.ceil(Math.min(barWidth - 1, Math.max(1, evnt.clientX - hueSliderRect.x))));
			}
		};
		const handleHueSliderMousedownEvent = (evnt) => {
			evnt.preventDefault();
			document.onmousemove = (evnt) => {
				evnt.preventDefault();
				handleHueBarEvent(evnt);
			};
			document.onmouseup = (evnt) => {
				document.onmousemove = null;
				document.onmouseup = null;
				handleHueBarEvent(evnt);
			};
		};
		const handleAlphaColor = (offsetLeft) => {
			const { selectColor } = reactData;
			const alphaSliderEl = refAlphaSliderElem.value;
			const alphaSliderBtnEl = refAlphaSliderBtnElem.value;
			if (alphaSliderEl && alphaSliderBtnEl) {
				const barWidth = alphaSliderEl.getBoundingClientRect().width;
				if (offsetLeft < 0) offsetLeft = 0;
				if (offsetLeft > barWidth) offsetLeft = barWidth;
				const alpha = import_xe_utils.default.ceil(100 / barWidth * offsetLeft / 100, 2);
				reactData.aValue = alpha;
				alphaSliderBtnEl.style.left = toCssUnit(offsetLeft);
				reactData.selectColor = updateColorAlpha(selectColor, alpha);
			}
		};
		const handleAlphaBarEvent = (evnt) => {
			const alphaSliderEl = refAlphaSliderElem.value;
			const alphaSliderBtnEl = refAlphaSliderBtnElem.value;
			if (alphaSliderEl && alphaSliderBtnEl) {
				const alphaSliderRect = alphaSliderEl.getBoundingClientRect();
				const barWidth = alphaSliderRect.width;
				handleAlphaColor(Math.min(barWidth, Math.max(0, evnt.clientX - alphaSliderRect.x)));
				updateModelColor();
			}
		};
		const handleAlphaSliderMousedownEvent = (evnt) => {
			evnt.preventDefault();
			document.onmousemove = (evnt) => {
				evnt.preventDefault();
				handleAlphaBarEvent(evnt);
			};
			document.onmouseup = (evnt) => {
				document.onmousemove = null;
				document.onmouseup = null;
				handleAlphaBarEvent(evnt);
			};
		};
		const handleInputRgbEvent = () => {
			const { rValue, gValue, bValue, aValue } = reactData;
			reactData.selectColor = toRgb(rValue, gValue, bValue, aValue);
			updateModelColor();
		};
		const handleInputAlphaEvent = () => {
			const { aValue } = reactData;
			const alphaSliderEl = refAlphaSliderElem.value;
			const alphaSliderBtnEl = refAlphaSliderBtnElem.value;
			if (alphaSliderEl && alphaSliderBtnEl) handleAlphaColor(alphaSliderEl.getBoundingClientRect().width * aValue);
		};
		const handleQuickEvent = (evnt, item) => {
			reactData.selectColor = item.value;
			updateModelColor();
		};
		const handlePanelColor = (offsetLeft, offsetTop) => {
			const colorActiveEl = refColorActiveElem.value;
			if (colorActiveEl) {
				colorActiveEl.style.top = toCssUnit(offsetTop);
				colorActiveEl.style.left = toCssUnit(offsetLeft);
			}
		};
		const handleEyeDropperEvent = () => {
			if (WinEyeDropper) try {
				new WinEyeDropper().open().then((rest) => {
					if (rest && rest.sRGBHex) {
						reactData.selectColor = rest.sRGBHex;
						updateModelColor();
					}
				}).catch(() => {});
			} catch (e) {}
		};
		const handleSelectColorByXY = (clientX, clientY) => {
			const { showAlpha } = props;
			const { panelColor, aValue } = reactData;
			const colorPanelEl = refColorPanelElem.value;
			const colorActiveEl = refColorActiveElem.value;
			if (colorPanelEl && colorActiveEl) {
				const { clientWidth, clientHeight } = colorPanelEl;
				const colorPanelRect = colorPanelEl.getBoundingClientRect();
				const offsetTop = Math.min(clientHeight, Math.max(0, clientY - colorPanelRect.y));
				const offsetLeft = Math.min(clientWidth, Math.max(0, clientX - colorPanelRect.x));
				const colorRest = parseColor(panelColor);
				if (colorRest) {
					const hsvRest = colorRest.type === "hex" ? hexToHsv(colorRest.hex) : rgbToHsv(colorRest);
					if (hsvRest) {
						const ragRest = hsvToRgb(hsvRest.h, offsetLeft / clientWidth, 1 - offsetTop / clientHeight);
						reactData.selectColor = toRgb(ragRest.r, ragRest.g, ragRest.b, showAlpha ? aValue : null);
						updateModelColor();
					}
				}
				handlePanelColor(offsetLeft, offsetTop);
			}
		};
		const handleSelectColorMousedownEvent = (evnt) => {
			evnt.stopPropagation();
			evnt.preventDefault();
			handleSelectColorByXY(evnt.clientX, evnt.clientY);
			document.onmousemove = (evnt) => {
				handleSelectColorByXY(evnt.clientX, evnt.clientY);
			};
			document.onmouseup = () => {
				document.onmousemove = null;
				document.onmouseup = null;
			};
		};
		const handleCopyColorEvent = () => {
			const { selectColor } = reactData;
			if (selectColor) {
				if (VxeUI.clipboard.copy(selectColor)) {
					if (VxeUI.modal) VxeUI.modal.message({
						content: getI18n("vxe.colorPicker.copySuccess", [selectColor]),
						status: "success"
					});
				}
			}
		};
		const handleGlobalMousewheelEvent = (evnt) => {
			const { visiblePanel } = reactData;
			if (!computeIsDisabled.value) {
				if (visiblePanel) {
					const panelElem = refOptionPanel.value;
					if (getEventTargetNode(evnt, panelElem).flag) updatePlacement();
					else hideOptionPanel();
				}
			}
		};
		const handleGlobalMousedownEvent = (evnt) => {
			const { visiblePanel } = reactData;
			if (!computeIsDisabled.value) {
				const el = refElem.value;
				const panelElem = refOptionPanel.value;
				reactData.isActivated = getEventTargetNode(evnt, el).flag || getEventTargetNode(evnt, panelElem).flag;
				if (visiblePanel && !reactData.isActivated) hideOptionPanel();
			}
		};
		const handleGlobalBlurEvent = () => {
			const { visiblePanel, isActivated } = reactData;
			if (visiblePanel) hideOptionPanel();
			if (isActivated) reactData.isActivated = false;
			if (visiblePanel || isActivated) {
				const targetElem = refInputTarget.value;
				if (targetElem) targetElem.blur();
			}
		};
		const handleGlobalResizeEvent = () => {
			const { visiblePanel } = reactData;
			if (visiblePanel) updatePlacement();
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $colorPicker: $xeColorPicker }, params));
		};
		Object.assign($xeColorPicker, { dispatchEvent }, {});
		const renderColorWrapper = () => {
			const { showColorExtractor } = props;
			const { panelColor } = reactData;
			if (showColorExtractor) return h("div", {
				ref: refColorPanelElem,
				class: "vxe-color-picker--color-wrapper",
				onMousedown: handleSelectColorMousedownEvent
			}, [
				h("div", {
					class: "vxe-color-picker--color-bg",
					style: { backgroundColor: panelColor }
				}),
				h("div", { class: "vxe-color-picker--white-bg" }),
				h("div", { class: "vxe-color-picker--black-bg" }),
				h("div", {
					ref: refColorActiveElem,
					class: "vxe-color-picker--color-active"
				})
			]);
			return renderEmptyElement($xeColorPicker);
		};
		const renderColorBar = () => {
			const { showAlpha, clickToCopy, showEyeDropper } = props;
			const { selectTyle, showTypePopup, hexValue, rValue, gValue, bValue, aValue, selectColor, panelColor } = reactData;
			const isRgb = computeIsRgb.value;
			const selectTypeItem = computeSelectTypeItem.value;
			return h("div", { class: "vxe-color-picker--bar-wrapper" }, [h("div", { class: "vxe-color-picker--slider-wrapper" }, [
				showEyeDropper && WinEyeDropper ? h("div", { class: "vxe-color-picker--color-dropper" }, [h("span", {
					class: "vxe-color-picker--color-dropper-btn",
					onClick: handleEyeDropperEvent
				}, [h("i", { class: getIcon().COLOR_PICKER_EYE_DROPPER })])]) : renderEmptyElement($xeColorPicker),
				h("div", { class: "vxe-color-picker--slider-preview" }, [h("div", { class: "vxe-color-picker--preview-btn" }, [h("div", {
					class: "vxe-color-picker--preview-color",
					style: { backgroundColor: selectColor }
				}, clickToCopy ? [h("span", {
					class: "vxe-color-picker--preview-copy-btn",
					onClick: handleCopyColorEvent
				}, [h("i", { class: getIcon().COLOR_PICKER_COLOR_COPY })])] : [])])]),
				h("div", { class: "vxe-color-picker--slider-handle" }, [h("div", {
					ref: refHueSliderElem,
					class: "vxe-color-picker--bar-hue-slider",
					onClick: handleHueBarEvent
				}, [h("div", {
					ref: refHueSliderBtnElem,
					class: "vxe-color-picker--bar-hue-btn",
					onMousedown: handleHueSliderMousedownEvent
				})]), showAlpha ? h("div", {
					ref: refAlphaSliderElem,
					class: "vxe-color-picker--bar-alpha-slider",
					onClick: handleAlphaBarEvent
				}, [h("div", {
					class: "vxe-color-picker--bar-alpha-bg",
					style: { background: `linear-gradient(to right, rgba(0, 0, 0, 0), ${panelColor})` }
				}), h("div", {
					ref: refAlphaSliderBtnElem,
					class: "vxe-color-picker--bar-alpha-btn",
					onMousedown: handleAlphaSliderMousedownEvent
				})]) : renderEmptyElement($xeColorPicker)])
			]), h("div", { class: "vxe-color-picker--custom-wrapper" }, [h("div", { class: "vxe-color-picker--type-switch" }, [h("div", {
				class: "vxe-color-picker--type-label",
				onClick: toggleTypeVisibleEvent
			}, [h("span", `${selectTypeItem ? selectTypeItem.label : selectTyle}`), h("span", { class: "vxe-color-picker--type-icon" }, [h("i", { class: showTypePopup ? getIcon().COLOR_PICKER_TPTY_OPEN : getIcon().COLOR_PICKER_TPTY_CLOSE })])]), h("div", { class: ["vxe-color-picker--type-popup", { "is--visible": showTypePopup }] }, typeList.map((item) => {
				return h("div", {
					class: "vxe-color-picker--type-item",
					onClick(evnt) {
						evnt.stopPropagation();
						handleChangeType(item.value);
					}
				}, item.label);
			}))]), h("div", { class: `vxe-color-picker--${selectTyle}-wrapper` }, isRgb ? [h("div", { class: "vxe-color-picker--input-wrapper" }, [
				h(number_input_default, {
					type: "integer",
					size: "mini",
					align: "center",
					min: 0,
					max: 255,
					maxLength: 3,
					placeholder: "",
					modelValue: rValue,
					controlConfig: { showButton: false },
					"onUpdate:modelValue"(val) {
						reactData.rValue = val;
					},
					onChange: handleInputRgbEvent
				}),
				h(number_input_default, {
					type: "integer",
					size: "mini",
					align: "center",
					min: 0,
					max: 255,
					maxLength: 3,
					placeholder: "",
					modelValue: gValue,
					controlConfig: { showButton: false },
					"onUpdate:modelValue"(val) {
						reactData.gValue = val;
					},
					onChange: handleInputRgbEvent
				}),
				h(number_input_default, {
					type: "integer",
					size: "mini",
					align: "center",
					min: 0,
					max: 255,
					maxLength: 3,
					placeholder: "",
					modelValue: bValue,
					controlConfig: { showButton: false },
					"onUpdate:modelValue"(val) {
						reactData.bValue = val;
					},
					onChange: handleInputRgbEvent
				}),
				h(number_input_default, {
					type: "number",
					size: "mini",
					align: "center",
					min: 0,
					max: 1,
					step: .01,
					maxLength: 4,
					placeholder: "",
					modelValue: aValue,
					controlConfig: { showButton: false },
					"onUpdate:modelValue"(val) {
						reactData.aValue = val;
					},
					onChange: handleInputAlphaEvent
				})
			]), h("div", { class: "vxe-color-picker--input-title" }, [
				h("span", "R"),
				h("span", "G"),
				h("span", "B"),
				h("span", "A")
			])] : [h("div", { class: "vxe-color-picker--input-wrapper" }, [h(input_default, {
				type: "text",
				size: "mini",
				align: "center",
				maxLength: 9,
				placeholder: "",
				modelValue: hexValue,
				"onUpdate:modelValue"(val) {
					reactData.hexValue = val;
				},
				onChange() {
					const colorRest = parseColor(reactData.hexValue);
					if (colorRest) {
						if (colorRest.value) {
							reactData.selectColor = colorRest.value;
							updateModelColor();
						}
					}
				}
			})]), h("div", { class: "vxe-color-picker--input-title" }, getI18n("vxe.colorPicker.hex"))])])]);
		};
		const renderQuickWrapper = () => {
			const { showQuick } = props;
			const colorList = computeColorList.value;
			if (showQuick && colorList.length) return h("div", { class: "vxe-color-picker--quick-wrapper" }, colorList.map((item, i) => {
				return h("div", {
					key: i,
					class: "vxe-color-picker--quick-item",
					title: item.label || "",
					style: { backgroundColor: item.value },
					onClick(evnt) {
						handleQuickEvent(evnt, item);
					}
				});
			}));
			return renderEmptyElement($xeColorPicker);
		};
		const renderVN = () => {
			const { className, popupClassName, clearable, modelValue } = props;
			const { initialized, isActivated, isAniVisible, visiblePanel } = reactData;
			const vSize = computeSize.value;
			const isDisabled = computeIsDisabled.value;
			const btnTransfer = computeBtnTransfer.value;
			if (computeFormReadonly.value) return h("div", {
				ref: refElem,
				class: ["vxe-color-picker--readonly", className]
			}, [h("div", {
				class: "vxe-color-picker--readonly-color",
				style: { backgroundColor: modelValue }
			})]);
			return h("div", {
				ref: refElem,
				class: [
					"vxe-color-picker",
					className ? import_xe_utils.default.isFunction(className) ? className({ $colorPicker: $xeColorPicker }) : className : "",
					{
						[`size--${vSize}`]: vSize,
						"is--selected": !!modelValue,
						"is--visible": visiblePanel,
						"is--disabled": isDisabled,
						"is--active": isActivated
					}
				]
			}, [
				h("input", {
					ref: refInputTarget,
					class: "vxe-color-picker--input",
					onFocus: focusEvent,
					onBlur: blurEvent
				}),
				h("div", {
					class: "vxe-color-picker--inner",
					onClick: clickEvent
				}, [h("div", {
					class: "vxe-color-picker--inner-color",
					style: { backgroundColor: modelValue }
				})]),
				h(Teleport, {
					to: "body",
					disabled: btnTransfer ? !initialized : true
				}, [h("div", {
					ref: refOptionPanel,
					class: [
						"vxe-table--ignore-clear vxe-color-picker--panel",
						popupClassName ? import_xe_utils.default.isFunction(popupClassName) ? popupClassName({ $colorPicker: $xeColorPicker }) : popupClassName : "",
						{
							[`size--${vSize}`]: vSize,
							"is--transfer": btnTransfer,
							"ani--leave": isAniVisible,
							"ani--enter": visiblePanel
						}
					],
					placement: reactData.panelPlacement,
					style: reactData.panelStyle
				}, [initialized && (visiblePanel || isAniVisible) ? h("div", {
					class: "vxe-color-picker--panel-wrapper",
					onClick: handlePanelClickEvent
				}, [
					renderColorWrapper(),
					renderColorBar(),
					renderQuickWrapper(),
					h("div", { class: "vxe-color-picker--footer-wrapper" }, [clearable ? h(VxeButtonComponent, {
						content: getI18n("vxe.colorPicker.clear"),
						size: "mini",
						onClick: clearEvent
					}) : renderEmptyElement($xeColorPicker), h(VxeButtonComponent, {
						content: getI18n("vxe.colorPicker.confirm"),
						size: "mini",
						status: "primary",
						onClick: confirmEvent
					})])
				]) : renderEmptyElement($xeColorPicker)])])
			]);
		};
		watch(() => props.modelValue, () => {
			updateMode();
		});
		watch(() => props.type, () => {
			updateType();
		});
		onMounted(() => {
			globalEvents.on($xeColorPicker, "mousewheel", handleGlobalMousewheelEvent);
			globalEvents.on($xeColorPicker, "mousedown", handleGlobalMousedownEvent);
			globalEvents.on($xeColorPicker, "blur", handleGlobalBlurEvent);
			globalEvents.on($xeColorPicker, "resize", handleGlobalResizeEvent);
		});
		onUnmounted(() => {
			globalEvents.off($xeColorPicker, "mousewheel");
			globalEvents.off($xeColorPicker, "mousedown");
			globalEvents.off($xeColorPicker, "blur");
			globalEvents.off($xeColorPicker, "resize");
		});
		updateType();
		provide("$xeColorPicker", $xeColorPicker);
		$xeColorPicker.renderVN = renderVN;
		return $xeColorPicker;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/color-picker/index.js
var VxeColorPicker = Object.assign({}, color_picker_default, { install(app) {
	app.component(color_picker_default.name, color_picker_default);
} });
dynamicApp.use(VxeColorPicker);
VxeUI.component(color_picker_default);
var ColorPicker = VxeColorPicker;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/context-menu/src/context-menu.js
function createInternalData$7() {
	return {};
}
function createReactData$6() {
	return {
		visible: false,
		activeOption: null,
		activeChildOption: null,
		popupStyle: {
			top: "",
			left: "",
			zIndex: 0
		},
		childOffsetX: 0
	};
}
var context_menu_default = defineVxeComponent({
	name: "VxeContextMenu",
	props: {
		modelValue: Boolean,
		className: String,
		size: {
			type: String,
			default: () => getConfig().contextMenu.size || getConfig().size
		},
		options: Array,
		x: [Number, String],
		y: [Number, String],
		autoLocate: {
			type: Boolean,
			default: () => getConfig().contextMenu.autoLocate
		},
		zIndex: [Number, String],
		position: {
			type: String,
			default: () => getConfig().contextMenu.position
		},
		destroyOnClose: {
			type: Boolean,
			default: () => getConfig().contextMenu.destroyOnClose
		},
		transfer: {
			type: Boolean,
			default: () => getConfig().contextMenu.transfer
		}
	},
	emits: [
		"update:modelValue",
		"option-click",
		"change",
		"show",
		"hide"
	],
	setup(props, context) {
		const { emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const { computeSize } = useSize(props);
		const internalData = createInternalData$7();
		const reactData = reactive(createReactData$6());
		const refMaps = { refElem };
		const computeMenuGroups = computed(() => {
			const { options } = props;
			return options || [];
		});
		const computeAllFirstMenuList = computed(() => {
			const menuGroups = computeMenuGroups.value;
			const firstList = [];
			for (let i = 0; i < menuGroups.length; i++) {
				const list = menuGroups[i];
				for (let j = 0; j < list.length; j++) {
					const firstItem = list[j];
					if (hasValidItem(firstItem)) firstList.push(firstItem);
				}
			}
			return firstList;
		});
		const computeTopAndLeft = computed(() => {
			const { x, y } = props;
			return `${x}${y}`;
		});
		const computeMaps = {};
		const $xeContextMenu = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $contextMenu: $xeContextMenu }, params));
		};
		const emitModel = (value) => {
			emit("update:modelValue", value);
		};
		const openMenu = () => {
			const { modelValue } = props;
			const { visible } = reactData;
			const value = true;
			reactData.visible = value;
			handleLocate();
			updateZindex();
			if (modelValue !== value) {
				emitModel(value);
				dispatchEvent("change", { value }, null);
			}
			if (visible !== value) dispatchEvent("show", { visible: value }, null);
			return nextTick().then(() => {
				updateLocate();
			});
		};
		const closeMenu = () => {
			const { modelValue } = props;
			const { visible } = reactData;
			const value = false;
			reactData.visible = value;
			if (modelValue !== value) {
				emitModel(value);
				dispatchEvent("change", { value }, null);
			}
			if (visible !== value) dispatchEvent("hide", { visible: value }, null);
			return nextTick();
		};
		const handleLocate = () => {
			const { x, y } = props;
			const { popupStyle } = reactData;
			popupStyle.left = toCssUnit(x || 0);
			popupStyle.top = toCssUnit(y || 0);
			updateLocate();
		};
		const updateZindex = () => {
			const { zIndex, transfer } = props;
			const { popupStyle } = reactData;
			const menuZIndex = popupStyle.zIndex;
			if (zIndex) popupStyle.zIndex = import_xe_utils.default.toNumber(zIndex);
			else if (menuZIndex < getLastZIndex()) popupStyle.zIndex = transfer ? nextSubZIndex() : nextZIndex();
		};
		const updateLocate = () => {
			const { autoLocate, position } = props;
			const { popupStyle } = reactData;
			if (autoLocate) {
				const wrapperEl = refElem.value;
				if (wrapperEl) {
					const { visibleWidth, visibleHeight } = getDomNode();
					const wrapperStyle = getComputedStyle(wrapperEl);
					const offsetTop = import_xe_utils.default.toNumber(wrapperStyle.top);
					const offsetLeft = import_xe_utils.default.toNumber(wrapperStyle.left);
					const wrapperWidth = wrapperEl.offsetWidth;
					const wrapperHeight = wrapperEl.offsetHeight;
					if (position === "absolute") {} else {
						if (offsetTop + wrapperHeight > visibleHeight) popupStyle.top = `${Math.max(0, offsetTop - wrapperHeight)}px`;
						if (offsetLeft + wrapperWidth > visibleWidth) popupStyle.left = `${Math.max(0, offsetLeft - wrapperWidth)}px`;
					}
				}
			}
			updateChildLocate();
		};
		const updateChildLocate = () => {
			const wrapperEl = refElem.value;
			if (wrapperEl) {
				const { visibleWidth } = getDomNode();
				const owSize = 4;
				const handleStyle = () => {
					const wrapperStyle = getComputedStyle(wrapperEl);
					const offsetLeft = import_xe_utils.default.toNumber(wrapperStyle.left);
					const wrapperWidth = wrapperEl.offsetWidth;
					const childEl = wrapperEl.querySelector(".vxe-context-menu--children-wrapper");
					const childWidth = childEl ? childEl.offsetWidth : wrapperWidth;
					if (offsetLeft + wrapperWidth > visibleWidth - childWidth) reactData.childOffsetX = -childWidth + owSize;
					else reactData.childOffsetX = wrapperWidth - owSize;
				};
				handleStyle();
				nextTick(() => {
					handleStyle();
				});
			}
		};
		const handleVisible = () => {
			const { modelValue } = props;
			if (modelValue) openMenu();
			else closeMenu();
		};
		const tagMethods = {
			dispatchEvent,
			open: openMenu,
			close: closeMenu
		};
		const hasChildMenu = (item) => {
			const { children } = item;
			return children && children.some((child) => child.visible !== false);
		};
		const handleItemClickEvent = (evnt, item) => {
			evnt.preventDefault();
			evnt.stopPropagation();
			if (!item.disabled && !item.loading && !hasChildMenu(item)) {
				dispatchEvent("option-click", { option: item }, evnt);
				closeMenu();
			}
		};
		const handleItemMouseenterEvent = (evnt, item, parentitem) => {
			const { leaveTime } = internalData;
			if (leaveTime) clearTimeout(leaveTime);
			reactData.activeOption = parentitem || item;
			if (parentitem) {
				reactData.activeOption = parentitem;
				reactData.activeChildOption = item;
			} else {
				reactData.activeOption = item;
				if (hasChildMenu(item)) {
					reactData.activeChildOption = findFirstChildItem(item);
					nextTick(() => {
						updateChildLocate();
					});
				} else reactData.activeChildOption = null;
			}
		};
		const handleItemMouseleaveEvent = () => {
			const { leaveTime } = internalData;
			if (leaveTime) clearTimeout(leaveTime);
			internalData.leaveTime = setTimeout(() => {
				internalData.leaveTime = null;
				reactData.activeOption = null;
				reactData.activeChildOption = null;
			}, 300);
		};
		const hasValidItem = (item) => {
			return !item.loading && !item.disabled && item.visible !== false;
		};
		const findNextFirstItem = (allFirstList, firstItem) => {
			for (let i = 0; i < allFirstList.length; i++) if (firstItem === allFirstList[i]) {
				const nextItem = allFirstList[i + 1];
				if (nextItem) return nextItem;
			}
			return import_xe_utils.default.first(allFirstList);
		};
		const findPrevFirstItem = (allFirstList, firstItem) => {
			for (let i = 0; i < allFirstList.length; i++) if (firstItem === allFirstList[i]) {
				if (i > 0) return allFirstList[i - 1];
			}
			return import_xe_utils.default.last(allFirstList);
		};
		const findFirstChildItem = (firstItem) => {
			const { children } = firstItem;
			if (children) for (let i = 0; i < children.length; i++) {
				const item = children[i];
				if (hasValidItem(item)) return item;
			}
			return null;
		};
		const findPrevChildItem = (firstItem, childItem) => {
			const { children } = firstItem;
			let prevValidItem = null;
			if (children) {
				for (let i = 0; i < children.length; i++) {
					const item = children[i];
					if (childItem === item) break;
					if (hasValidItem(item)) prevValidItem = item;
				}
				if (!prevValidItem) for (let len = children.length - 1; len >= 0; len--) {
					const item = children[len];
					if (hasValidItem(item)) return item;
				}
			}
			return prevValidItem;
		};
		const findNextChildItem = (firstItem, childItem) => {
			const { children } = firstItem;
			let firstValidItem = null;
			if (children) {
				let isMetch = false;
				for (let i = 0; i < children.length; i++) {
					const item = children[i];
					if (!firstValidItem) {
						if (hasValidItem(item)) firstValidItem = item;
					}
					if (isMetch) {
						if (hasValidItem(item)) return item;
					} else isMetch = childItem === item;
				}
			}
			return firstValidItem;
		};
		const handleGlobalMousewheelEvent = (evnt) => {
			const { visible } = reactData;
			if (visible) {
				const el = refElem.value;
				if (!getEventTargetNode(evnt, el, "").flag) closeMenu();
			}
		};
		const handleGlobalKeydownEvent = (evnt) => {
			const { visible, activeOption, activeChildOption } = reactData;
			const allFirstMenuList = computeAllFirstMenuList.value;
			if (visible) {
				const isUpArrow = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.ARROW_UP);
				const isDwArrow = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.ARROW_DOWN);
				const isLeftArrow = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.ARROW_LEFT);
				const isRightArrow = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.ARROW_RIGHT);
				const isEnter = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.ENTER);
				if (globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.ESCAPE)) {
					closeMenu();
					return;
				}
				if (isEnter) {
					if (activeOption || activeChildOption) {
						evnt.preventDefault();
						evnt.stopPropagation();
						if (!activeChildOption && hasChildMenu(activeOption)) {
							reactData.activeChildOption = findFirstChildItem(activeOption);
							updateChildLocate();
							return;
						}
						handleItemClickEvent(evnt, activeChildOption || activeOption);
						return;
					}
				}
				if (activeChildOption) {
					if (isUpArrow) {
						evnt.preventDefault();
						reactData.activeChildOption = findPrevChildItem(activeOption, activeChildOption);
						updateChildLocate();
					} else if (isDwArrow) {
						evnt.preventDefault();
						reactData.activeChildOption = findNextChildItem(activeOption, activeChildOption);
						updateChildLocate();
					} else if (isLeftArrow) {
						evnt.preventDefault();
						reactData.activeChildOption = null;
					}
				} else if (activeOption) {
					evnt.preventDefault();
					if (isUpArrow) reactData.activeOption = findPrevFirstItem(allFirstMenuList, activeOption);
					else if (isDwArrow) reactData.activeOption = findNextFirstItem(allFirstMenuList, activeOption);
					else if (hasChildMenu(activeOption)) {
						if (isRightArrow) {
							reactData.activeChildOption = findFirstChildItem(activeOption);
							updateChildLocate();
						}
					}
				} else {
					evnt.preventDefault();
					reactData.activeOption = import_xe_utils.default.first(allFirstMenuList);
				}
			}
		};
		const handleGlobalMousedownEvent = (evnt) => {
			const { visible } = reactData;
			if (visible) {
				const el = refElem.value;
				if (!getEventTargetNode(evnt, el, "").flag) closeMenu();
			}
		};
		const handleGlobalBlurEvent = () => {
			const { visible } = reactData;
			if (visible) closeMenu();
		};
		Object.assign($xeContextMenu, tagMethods, {});
		const renderMenuItem = (item, parentItem, hasChildMenus) => {
			const { visible, disabled, loading } = item;
			if (visible === false) return renderEmptyElement($xeContextMenu);
			const prefixOpts = Object.assign({}, item.prefixConfig);
			const prefixIcon = prefixOpts.icon || item.prefixIcon;
			const suffixOpts = Object.assign({}, item.suffixConfig);
			const suffixIcon = suffixOpts.icon || item.suffixIcon;
			const menuContent = loading ? getI18n("vxe.contextMenu.loadingText") : getFuncText(item.name);
			return h("div", {
				class: ["vxe-context-menu--item-inner", {
					"is--disabled": disabled,
					"is--loading": loading
				}],
				onClick(evnt) {
					handleItemClickEvent(evnt, item);
				},
				onMouseenter(evnt) {
					handleItemMouseenterEvent(evnt, item, parentItem);
				},
				onMouseleave: handleItemMouseleaveEvent
			}, [
				h("div", { class: ["vxe-context-menu--item-prefix", prefixOpts.className || ""] }, loading ? [h("span", { key: "1" }, [h("i", { class: getIcon().CONTEXT_MENU_OPTION_LOADING })])] : [prefixIcon && import_xe_utils.default.isFunction(prefixIcon) ? h("span", { key: "2" }, getSlotVNs$1(prefixIcon({}))) : h("span", { key: "3" }, [h("i", { class: prefixIcon })]), prefixOpts.content ? h("span", { key: "4" }, `${prefixOpts.content || ""}`) : renderEmptyElement($xeContextMenu)]),
				h("div", { class: "vxe-context-menu--item-label" }, menuContent),
				!loading && (suffixIcon || suffixOpts.content) ? h("div", { class: ["vxe-context-menu--item-suffix", suffixOpts.className || ""] }, [suffixIcon && import_xe_utils.default.isFunction(suffixIcon) ? h("span", { key: "2" }, getSlotVNs$1(suffixIcon({}))) : h("span", { key: "3" }, [h("i", { class: suffixIcon })]), suffixOpts.content ? h("span", { key: "4" }, `${suffixOpts.content || ""}`) : renderEmptyElement($xeContextMenu)]) : renderEmptyElement($xeContextMenu),
				hasChildMenus ? h("div", { class: "vxe-context-menu--item-subicon" }, [h("i", { class: getIcon().CONTEXT_MENU_CHILDREN })]) : renderEmptyElement($xeContextMenu)
			]);
		};
		const renderMenus = () => {
			const { activeOption, activeChildOption, childOffsetX } = reactData;
			const menuGroups = computeMenuGroups.value;
			const mgVNs = [];
			menuGroups.forEach((menuList, gIndex) => {
				const moVNs = [];
				menuList.forEach((firstItem, i) => {
					const { children } = firstItem;
					const hasChildMenus = children && children.some((child) => child.visible !== false);
					const isActiveFirst = activeOption === firstItem;
					const showChild = isActiveFirst && !!activeChildOption;
					moVNs.push(h("div", {
						key: `${gIndex}_${i}`,
						class: [
							"vxe-context-menu--item-wrapper vxe-context-menu--first-item",
							firstItem.className || "",
							{
								"is--active": isActiveFirst,
								"is--subactive": isActiveFirst && !!activeChildOption
							}
						]
					}, [hasChildMenus && showChild ? h("div", {
						class: "vxe-context-menu--children-wrapper",
						style: { transform: `translate(${childOffsetX}px, -2px)` }
					}, children.map((twoItem) => {
						return h("div", { class: [
							"vxe-context-menu--item-wrapper vxe-context-menu--child-item",
							twoItem.className || "",
							{ "is--active": activeChildOption === twoItem }
						] }, [renderMenuItem(twoItem, firstItem)]);
					})) : renderEmptyElement($xeContextMenu), renderMenuItem(firstItem, null, hasChildMenus)]));
				});
				mgVNs.push(h("div", {
					key: gIndex,
					class: "vxe-context-menu--group-wrapper"
				}, moVNs));
			});
			return mgVNs;
		};
		const renderVN = () => {
			const { className, position, destroyOnClose } = props;
			const { visible, popupStyle } = reactData;
			const vSize = computeSize.value;
			return h("div", {
				ref: refElem,
				class: [
					"vxe-context-menu vxe-context-menu--wrapper",
					position === "absolute" ? "is--" + position : "is--fixed",
					className || "",
					{
						[`size--${vSize}`]: vSize,
						"is--visible": visible
					}
				],
				style: popupStyle
			}, (destroyOnClose ? visible : true) ? renderMenus() : []);
		};
		watch(computeTopAndLeft, () => {
			handleLocate();
			nextTick(() => {
				updateLocate();
			});
		});
		watch(() => props.modelValue, () => {
			handleVisible();
		});
		handleVisible();
		onMounted(() => {
			globalEvents.on($xeContextMenu, "mousewheel", handleGlobalMousewheelEvent);
			globalEvents.on($xeContextMenu, "keydown", handleGlobalKeydownEvent);
			globalEvents.on($xeContextMenu, "mousedown", handleGlobalMousedownEvent);
			globalEvents.on($xeContextMenu, "blur", handleGlobalBlurEvent);
		});
		onBeforeUnmount(() => {
			const { leaveTime } = internalData;
			if (leaveTime) clearTimeout(leaveTime);
			globalEvents.off($xeContextMenu, "mousewheel");
			globalEvents.off($xeContextMenu, "keydown");
			globalEvents.off($xeContextMenu, "mousedown");
			globalEvents.off($xeContextMenu, "blur");
			import_xe_utils.default.assign(reactData, createReactData$6());
			import_xe_utils.default.assign(internalData, createInternalData$7());
		});
		$xeContextMenu.renderVN = renderVN;
		return $xeContextMenu;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/context-menu/index.js
var VxeContextMenu = Object.assign({}, context_menu_default, { install(app) {
	app.component(context_menu_default.name, context_menu_default);
} });
function openMenu(opts, x, y) {
	dynamicStore.globalContextMenu = {
		modelValue: true,
		options: opts.options,
		className: opts.className,
		size: opts.size,
		zIndex: opts.zIndex,
		x,
		y,
		position: "fixed",
		destroyOnClose: true,
		transfer: false,
		events: opts.events
	};
	checkDynamic();
}
var ContextMenuController = {
	open(options) {
		const opts = Object.assign({
			x: 0,
			y: 0
		}, options);
		openMenu(opts, opts.x, opts.y);
	},
	openByEvent(evnt, options) {
		evnt.preventDefault();
		evnt.stopPropagation();
		openMenu(Object.assign({}, options), evnt.clientX + 1, evnt.clientY + 1);
	},
	close() {
		dynamicStore.globalContextMenu = null;
	}
};
dynamicApp.use(VxeContextMenu);
VxeUI.component(context_menu_default);
VxeUI.contextMenu = ContextMenuController;
var ContextMenu = VxeContextMenu;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/text/src/text.js
var text_default = defineVxeComponent({
	name: "VxeText",
	props: {
		status: String,
		title: [String, Number],
		icon: String,
		prefixIcon: String,
		suffixIcon: String,
		loading: Boolean,
		content: [String, Number],
		clickToCopy: {
			type: Boolean,
			default: () => getConfig().text.clickToCopy
		},
		copyConfig: Object,
		size: {
			type: String,
			default: () => getConfig().text.size || getConfig().size
		}
	},
	emits: [
		"click",
		"dblclick",
		"prefix-click",
		"suffix-click",
		"copy-success",
		"copy-error"
	],
	setup(props, context) {
		const { emit, slots } = context;
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const refElem = ref();
		const refContentElem = ref();
		const reactData = reactive({});
		const computeCopyOpts = computed(() => {
			return Object.assign({}, getConfig().text.copyConfig, props.copyConfig);
		});
		const refMaps = { refElem };
		const computeMaps = {};
		const handleCopy = (evnt) => {
			const { content } = props;
			const { showMessage } = computeCopyOpts.value;
			const contentEl = refContentElem.value;
			const copyVal = (contentEl ? contentEl.textContent : "") || content;
			if (copyVal) if (VxeUI.clipboard.copy(copyVal)) {
				if (showMessage && VxeUI.modal) VxeUI.modal.message({
					content: getI18n("vxe.text.copySuccess"),
					status: "success"
				});
				dispatchEvent("copy-success", {}, evnt);
			} else {
				if (showMessage && VxeUI.modal) VxeUI.modal.message({
					content: getI18n("vxe.text.copyError"),
					status: "error"
				});
				dispatchEvent("copy-error", {}, evnt);
			}
		};
		const clickIconEvent = (evnt) => {
			const { clickToCopy } = props;
			const copyOpts = computeCopyOpts.value;
			if (clickToCopy && copyOpts.trigger !== "dblclick") handleCopy(evnt);
		};
		const dblclickIconEvent = (evnt) => {
			const { clickToCopy } = props;
			const copyOpts = computeCopyOpts.value;
			if (clickToCopy && copyOpts.trigger === "dblclick") handleCopy(evnt);
		};
		const $xeText = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $text: $xeText }, params));
		};
		const textMethods = { dispatchEvent };
		const clickEvent = (evnt) => {
			const { loading } = props;
			if (!loading) dispatchEvent("click", {}, evnt);
		};
		const dblclickEvent = (evnt) => {
			const { loading } = props;
			if (!loading) dispatchEvent("dblclick", {}, evnt);
		};
		const prefixEvent = (evnt) => {
			const { loading } = props;
			if (!loading) dispatchEvent("prefix-click", {}, evnt);
		};
		const suffixEvent = (evnt) => {
			const { loading } = props;
			if (!loading) dispatchEvent("suffix-click", {}, evnt);
		};
		Object.assign($xeText, textMethods, {});
		const renderCopyIcon = () => {
			const { icon, status } = computeCopyOpts.value;
			return h("span", {
				key: "ci",
				class: ["vxe-text--copy-icon", { [`theme--${status}`]: status }],
				onClick: clickIconEvent,
				onDblclick: dblclickIconEvent
			}, [h("i", { class: icon || getIcon().TEXT_COPY })]);
		};
		const renderContent = () => {
			const { loading, icon, prefixIcon, suffixIcon, clickToCopy, content } = props;
			const copyOpts = computeCopyOpts.value;
			const defaultSlot = slots.default;
			const prefixIconSlot = slots.prefixIcon || slots["prefix-icon"] || slots.icon;
			const suffixIconSlot = slots.suffixIcon || slots["suffix-icon"];
			const copyToRight = copyOpts.layout === "right";
			const contVNs = [];
			if (loading) contVNs.push(h("span", {
				key: "lg",
				class: "vxe-text--loading"
			}, [h("i", { class: getIcon().TEXT_LOADING })]));
			else if (clickToCopy && !copyToRight) contVNs.push(renderCopyIcon());
			if (prefixIcon || icon) contVNs.push(h("span", {
				key: "si",
				class: "vxe-text--prefix-icon",
				onClick: prefixEvent
			}, prefixIconSlot ? prefixIconSlot({}) : [h("i", { class: prefixIcon || icon })]));
			contVNs.push(h("span", {
				key: "ct",
				ref: refContentElem,
				class: "vxe-text--content",
				onClick: clickEvent,
				onDblclick: dblclickEvent
			}, defaultSlot ? defaultSlot({}) : import_xe_utils.default.toValueString(content)));
			if (suffixIcon) contVNs.push(h("span", {
				key: "si",
				class: "vxe-text--suffix-icon",
				onClick: suffixEvent
			}, suffixIconSlot ? suffixIconSlot({}) : [h("i", { class: suffixIcon })]));
			if (clickToCopy && copyToRight && !loading) contVNs.push(renderCopyIcon());
			return contVNs;
		};
		const renderVN = () => {
			const { loading, status, title, clickToCopy } = props;
			const vSize = computeSize.value;
			return h("span", {
				ref: refElem,
				title,
				class: ["vxe-text", {
					[`size--${vSize}`]: vSize,
					[`theme--${status}`]: status,
					"is--copy": clickToCopy,
					"is--loading": loading
				}]
			}, renderContent());
		};
		$xeText.renderVN = renderVN;
		return $xeText;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/countdown/src/countdown.js
var countdown_default = defineVxeComponent({
	name: "VxeCountdown",
	props: {
		modelValue: [Number, String],
		format: String,
		prefixConfig: Object,
		suffixConfig: Object,
		size: {
			type: String,
			default: () => getConfig().countdown.size || getConfig().size
		}
	},
	emits: [
		"update:modelValue",
		"start",
		"end"
	],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const { computeSize } = useSize(props);
		const reactData = reactive({
			currNum: 0,
			secondNum: 0
		});
		const internalData = { dnTimeout: void 0 };
		const refMaps = { refElem };
		const computeTimeFormats = computed(() => {
			const { secondNum } = reactData;
			if (secondNum >= 316224e5) return [
				"yyyy",
				"MM",
				"dd",
				"HH",
				"mm",
				"ss"
			];
			if (secondNum >= 26784e5) return [
				"MM",
				"dd",
				"HH",
				"mm",
				"ss"
			];
			if (secondNum >= 864e5) return [
				"dd",
				"HH",
				"mm",
				"ss"
			];
			if (secondNum >= 36e5) return [
				"HH",
				"mm",
				"ss"
			];
			if (secondNum >= 6e4) return ["mm", "ss"];
			return ["ss"];
		});
		const computeDiffConf = computed(() => {
			const { currNum } = reactData;
			const diffRest = import_xe_utils.default.getDateDiff(Date.now(), Date.now() + currNum);
			return Object.assign(diffRest, { done: !(import_xe_utils.default.isBoolean(diffRest.status) ? diffRest.status : diffRest.done) });
		});
		const computeFormatLabel = computed(() => {
			const { format } = props;
			const diffConf = computeDiffConf.value;
			let rest = "";
			if (format) {
				rest = `${format}`;
				import_xe_utils.default.each(diffConf, (val, key) => {
					rest = rest.replace(new RegExp(key, "g"), import_xe_utils.default.padStart(val, key.length, "0"));
				});
				return rest;
			}
			return rest;
		});
		const computePrefixOpts = computed(() => {
			return Object.assign({}, props.prefixConfig, getConfig().countdown.prefixConfig);
		});
		const computeSuffixOpts = computed(() => {
			return Object.assign({}, props.suffixConfig, getConfig().countdown.suffixConfig);
		});
		const computeMaps = { computeSize };
		const $xeCountdown = {
			xID,
			props,
			context,
			reactData,
			internalData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $carousel: $xeCountdown }, params));
		};
		const updateCount = () => {
			const secondNum = import_xe_utils.default.toNumber(props.modelValue || 0);
			reactData.secondNum = secondNum;
			reactData.currNum = secondNum;
		};
		const handleTime = () => {
			const { currNum } = reactData;
			if (currNum > 1e3) {
				reactData.currNum -= 1e3;
				internalData.dnTimeout = setTimeout(() => {
					handleTime();
				}, 1e3);
			} else {
				reactData.currNum = 0;
				handleStop();
			}
		};
		const countdownMethods = { dispatchEvent };
		const handleStart = () => {
			dispatchEvent("start", {}, null);
			handleTime();
		};
		const handleStop = () => {
			const { dnTimeout } = internalData;
			if (dnTimeout) {
				clearTimeout(dnTimeout);
				internalData.dnTimeout = void 0;
				dispatchEvent("end", {}, null);
			}
		};
		Object.assign($xeCountdown, countdownMethods, {});
		const renderDefaultContentVNs = () => {
			const { format } = props;
			const timeFormats = computeTimeFormats.value;
			const diffConf = computeDiffConf.value;
			const formatLabel = computeFormatLabel.value;
			if (format) return [h("div", {
				key: "format",
				class: "vxe-countdown--content-format"
			}, formatLabel)];
			return timeFormats.map((key, index) => {
				return h("div", {
					key: index,
					class: "vxe-countdown--content-item"
				}, [h("div", { class: "vxe-countdown--content-num" }, `${diffConf[key] || 0}`), h("div", { class: "vxe-countdown--content-unit" }, getI18n(`vxe.countdown.formats.${key}`))]);
			});
		};
		const renderVN = () => {
			const { prefixConfig, suffixConfig } = props;
			const { currNum } = reactData;
			const vSize = computeSize.value;
			const diffConf = computeDiffConf.value;
			const prefixOpts = computePrefixOpts.value;
			const suffixOpts = computeSuffixOpts.value;
			const prefixSlot = slots.prefix;
			const suffixSlot = slots.suffix;
			const defaultSlot = slots.default;
			return h("div", {
				ref: refElem,
				class: [
					"vxe-countdown",
					diffConf.done ? "is--progress" : "is-end",
					{ [`size--${vSize}`]: vSize }
				]
			}, [
				prefixSlot || prefixConfig ? h("div", { class: "vxe-countdown--prefix" }, prefixSlot ? getSlotVNs$1(prefixSlot({
					currentValue: currNum,
					diffConf
				})) : [h(text_default, {
					content: prefixOpts.content,
					icon: prefixOpts.icon,
					status: prefixOpts.status
				})]) : renderEmptyElement($xeCountdown),
				h("div", { class: "vxe-countdown--content" }, defaultSlot ? getSlotVNs$1(defaultSlot({
					currentValue: currNum,
					diffConf
				})) : renderDefaultContentVNs()),
				suffixSlot || suffixConfig ? h("div", { class: "vxe-countdown--suffix" }, suffixSlot ? getSlotVNs$1(suffixSlot({
					currentValue: currNum,
					diffConf
				})) : [h(text_default, {
					content: suffixOpts.content,
					icon: suffixOpts.icon,
					status: suffixOpts.status
				})]) : renderEmptyElement($xeCountdown)
			]);
		};
		watch(() => props.modelValue, () => {
			updateCount();
			handleStop();
			handleStart();
		});
		onUnmounted(() => {
			handleStop();
		});
		onMounted(() => {
			handleStart();
		});
		updateCount();
		$xeCountdown.renderVN = renderVN;
		return $xeCountdown;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/countdown/index.js
var VxeCountdown = Object.assign({}, countdown_default, { install(app) {
	app.component(countdown_default.name, countdown_default);
} });
dynamicApp.use(VxeCountdown);
VxeUI.component(countdown_default);
var Countdown = VxeCountdown;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/date-panel/src/date-panel.js
var date_panel_default = defineVxeComponent({
	name: "VxeDatePanel",
	props: {
		modelValue: [
			String,
			Number,
			Date
		],
		type: {
			type: String,
			default: "date"
		},
		className: String,
		size: {
			type: String,
			default: () => getConfig().datePanel.size || getConfig().size
		},
		multiple: Boolean,
		limitCount: {
			type: [String, Number],
			default: () => getConfig().datePanel.limitCount
		},
		startDate: {
			type: [
				String,
				Number,
				Date
			],
			default: () => getConfig().datePanel.startDate
		},
		endDate: {
			type: [
				String,
				Number,
				Date
			],
			default: () => getConfig().datePanel.endDate
		},
		defaultDate: [
			String,
			Number,
			Date
		],
		defaultTime: [
			String,
			Number,
			Date
		],
		minDate: [
			String,
			Number,
			Date
		],
		maxDate: [
			String,
			Number,
			Date
		],
		startDay: {
			type: [String, Number],
			default: () => getConfig().datePanel.startDay
		},
		labelFormat: String,
		valueFormat: String,
		timeFormat: String,
		festivalMethod: {
			type: Function,
			default: () => getConfig().datePanel.festivalMethod
		},
		disabledMethod: {
			type: Function,
			default: () => getConfig().datePanel.disabledMethod
		},
		timeConfig: Object,
		selectDay: {
			type: [String, Number],
			default: () => getConfig().datePanel.selectDay
		}
	},
	emits: [
		"update:modelValue",
		"change",
		"click",
		"clear",
		"date-prev",
		"date-today",
		"date-next",
		"confirm"
	],
	setup(props, context) {
		const { emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const reactData = reactive({
			visiblePanel: false,
			isAniVisible: false,
			isActivated: false,
			inputValue: "",
			inputLabel: "",
			datetimePanelValue: null,
			datePanelValue: null,
			datePanelLabel: "",
			datePanelType: "day",
			selectMonth: null,
			currentDate: null
		});
		const internalData = {
			yearSize: 12,
			monthSize: 20,
			quarterSize: 8,
			hpTimeout: void 0
		};
		const refElem = ref();
		const refPanelWrapper = ref();
		const refInputTimeBody = ref();
		const refMaps = { refElem };
		const $xeDatePanel = {
			xID,
			props,
			context,
			reactData,
			internalData,
			getRefMaps: () => refMaps
		};
		const computeIsDateTimeType = computed(() => {
			const { type } = props;
			return type === "time" || type === "datetime";
		});
		const computeIsDatePanelType = computed(() => {
			return computeIsDateTimeType.value || [
				"date",
				"week",
				"month",
				"quarter",
				"year"
			].indexOf(props.type) > -1;
		});
		const computeDateStartTime = computed(() => {
			return props.startDate ? import_xe_utils.default.toStringDate(props.startDate) : null;
		});
		const computeDateEndTime = computed(() => {
			return props.endDate ? import_xe_utils.default.toStringDate(props.endDate) : null;
		});
		const computeDateListValue = computed(() => {
			const { modelValue, multiple } = props;
			const isDatePanelType = computeIsDatePanelType.value;
			const dateValueFormat = computeDateValueFormat.value;
			if (multiple && modelValue && isDatePanelType) return import_xe_utils.default.toValueString(modelValue).split(",").map((item) => {
				const date = parseDate(item, dateValueFormat);
				if (import_xe_utils.default.isValidDate(date)) return date;
				return date;
			});
			return [];
		});
		const computeDateMultipleValue = computed(() => {
			const dateListValue = computeDateListValue.value;
			const dateValueFormat = computeDateValueFormat.value;
			return dateListValue.map((date) => import_xe_utils.default.toDateString(date, dateValueFormat));
		});
		const computeDateMultipleLabel = computed(() => {
			const dateListValue = computeDateListValue.value;
			const dateLabelFormat = computeDateLabelFormat.value;
			return dateListValue.map((date) => import_xe_utils.default.toDateString(date, dateLabelFormat)).join(", ");
		});
		const computeLimitMaxCount = computed(() => {
			return props.multiple ? import_xe_utils.default.toNumber(props.limitCount) : 0;
		});
		const computeOverCount = computed(() => {
			const { multiple } = props;
			const limitMaxCount = computeLimitMaxCount.value;
			const dateMultipleValue = computeDateMultipleValue.value;
			if (multiple && limitMaxCount) return dateMultipleValue.length >= limitMaxCount;
			return false;
		});
		const computeTimeOpts = computed(() => {
			return Object.assign({}, getConfig().datePanel.timeConfig, props.timeConfig);
		});
		const computeDateValueFormat = computed(() => {
			const { type, valueFormat } = props;
			return handleValueFormat(type, valueFormat);
		});
		const computeDateValue = computed(() => {
			const { modelValue } = props;
			const isDatePanelType = computeIsDatePanelType.value;
			const dateValueFormat = computeDateValueFormat.value;
			let val = null;
			if (modelValue && isDatePanelType) {
				const date = parseDate(modelValue, dateValueFormat);
				if (import_xe_utils.default.isValidDate(date)) val = date;
			}
			return val;
		});
		const computeIsDisabledPrevDateBtn = computed(() => {
			const dateStartTime = computeDateStartTime.value;
			const { selectMonth } = reactData;
			if (selectMonth && dateStartTime) return selectMonth <= dateStartTime;
			return false;
		});
		const computeIsDisabledNextDateBtn = computed(() => {
			const dateEndTime = computeDateEndTime.value;
			const { selectMonth } = reactData;
			if (selectMonth && dateEndTime) return import_xe_utils.default.getWhatMonth(selectMonth, 0, "last") >= dateEndTime;
			return false;
		});
		const computeDateTimeLabel = computed(() => {
			const { datetimePanelValue } = reactData;
			const hasTimeSecond = computeHasTimeSecond.value;
			const hasTimeMinute = computeHasTimeMinute.value;
			if (datetimePanelValue) return import_xe_utils.default.toDateString(datetimePanelValue, hasTimeMinute && hasTimeSecond ? "HH:mm:ss" : hasTimeMinute ? "HH:mm" : "HH");
			return "";
		});
		const computeDateHMSTime = computed(() => {
			const dateValue = computeDateValue.value;
			const isDateTimeType = computeIsDateTimeType.value;
			return dateValue && isDateTimeType ? (dateValue.getHours() * 3600 + dateValue.getMinutes() * 60 + dateValue.getSeconds()) * 1e3 : 0;
		});
		const computeDateLabelFormat = computed(() => {
			const { labelFormat } = props;
			if (computeIsDatePanelType.value) return labelFormat || getI18n(`vxe.input.date.labelFormat.${props.type}`);
			return "";
		});
		const computeYearList = computed(() => {
			const { yearSize } = internalData;
			const { selectMonth, currentDate } = reactData;
			const years = [];
			if (selectMonth && currentDate) {
				const currFullYear = currentDate.getFullYear();
				const selectFullYear = selectMonth.getFullYear();
				const startYearDate = new Date(selectFullYear - selectFullYear % yearSize, 0, 1);
				for (let index = -4; index < yearSize + 4; index++) {
					const date = import_xe_utils.default.getWhatYear(startYearDate, index, "first");
					const itemFullYear = date.getFullYear();
					years.push({
						date,
						isCurrent: true,
						isPrev: index < 0,
						isNow: currFullYear === itemFullYear,
						isNext: index >= yearSize,
						year: itemFullYear
					});
				}
			}
			return years;
		});
		const computeSelectDatePanelObj = computed(() => {
			const isDatePanelType = computeIsDatePanelType.value;
			let y = "";
			let m = "";
			if (isDatePanelType) {
				const { datePanelType, selectMonth } = reactData;
				const yearList = computeYearList.value;
				let year = "";
				let month;
				if (selectMonth) {
					year = selectMonth.getFullYear();
					month = selectMonth.getMonth() + 1;
				}
				if (datePanelType === "quarter" || datePanelType === "month") y = getI18n("vxe.datePicker.yearTitle", [year]);
				else if (datePanelType === "year") y = yearList.length ? `${yearList[0].year} - ${yearList[yearList.length - 1].year}` : "";
				else {
					y = getI18n("vxe.datePicker.yearTitle", [year]);
					m = month ? getI18n(`vxe.input.date.m${month}`) : "-";
				}
			}
			return {
				y,
				m
			};
		});
		const computeFirstDayOfWeek = computed(() => {
			const { startDay } = props;
			return import_xe_utils.default.toNumber(startDay);
		});
		const computeWeekDatas = computed(() => {
			const weeks = [];
			if (computeIsDatePanelType.value) {
				let sWeek = computeFirstDayOfWeek.value;
				weeks.push(sWeek);
				for (let index = 0; index < 6; index++) {
					if (sWeek >= 6) sWeek = 0;
					else sWeek++;
					weeks.push(sWeek);
				}
			}
			return weeks;
		});
		const computeDateHeaders = computed(() => {
			if (computeIsDatePanelType.value) return computeWeekDatas.value.map((day) => {
				return {
					value: day,
					label: getI18n(`vxe.input.date.weeks.w${day}`)
				};
			});
			return [];
		});
		const computeWeekHeaders = computed(() => {
			if (computeIsDatePanelType.value) {
				const dateHeaders = computeDateHeaders.value;
				return [{ label: getI18n("vxe.input.date.weeks.w") }].concat(dateHeaders);
			}
			return [];
		});
		const computeYearDatas = computed(() => {
			const yearList = computeYearList.value;
			return import_xe_utils.default.chunk(yearList, 4);
		});
		const computeQuarterList = computed(() => {
			const { quarterSize } = internalData;
			const { selectMonth, currentDate } = reactData;
			const quarters = [];
			if (selectMonth && currentDate) {
				const currFullYear = currentDate.getFullYear();
				const currQuarter = getDateQuarter(currentDate);
				const firstYear = import_xe_utils.default.getWhatYear(selectMonth, 0, "first");
				const selFullYear = firstYear.getFullYear();
				for (let index = -2; index < quarterSize - 2; index++) {
					const date = import_xe_utils.default.getWhatQuarter(firstYear, index);
					const itemFullYear = date.getFullYear();
					const itemQuarter = getDateQuarter(date);
					const isPrev = itemFullYear < selFullYear;
					quarters.push({
						date,
						isPrev,
						isCurrent: itemFullYear === selFullYear,
						isNow: itemFullYear === currFullYear && itemQuarter === currQuarter,
						isNext: !isPrev && itemFullYear > selFullYear,
						quarter: itemQuarter
					});
				}
			}
			return quarters;
		});
		const computeQuarterDatas = computed(() => {
			const quarterList = computeQuarterList.value;
			return import_xe_utils.default.chunk(quarterList, 2);
		});
		const computeMonthList = computed(() => {
			const { monthSize } = internalData;
			const { selectMonth, currentDate } = reactData;
			const months = [];
			if (selectMonth && currentDate) {
				const currFullYear = currentDate.getFullYear();
				const currMonth = currentDate.getMonth();
				const selFullYear = import_xe_utils.default.getWhatYear(selectMonth, 0, "first").getFullYear();
				for (let index = -4; index < monthSize - 4; index++) {
					const date = import_xe_utils.default.getWhatYear(selectMonth, 0, index);
					const itemFullYear = date.getFullYear();
					const itemMonth = date.getMonth();
					const isPrev = itemFullYear < selFullYear;
					months.push({
						date,
						isPrev,
						isCurrent: itemFullYear === selFullYear,
						isNow: itemFullYear === currFullYear && itemMonth === currMonth,
						isNext: !isPrev && itemFullYear > selFullYear,
						month: itemMonth
					});
				}
			}
			return months;
		});
		const computeMonthDatas = computed(() => {
			const monthList = computeMonthList.value;
			return import_xe_utils.default.chunk(monthList, 4);
		});
		const computeDayList = computed(() => {
			const { selectMonth, currentDate } = reactData;
			const days = [];
			if (selectMonth && currentDate) {
				const dateHMSTime = computeDateHMSTime.value;
				const weekDatas = computeWeekDatas.value;
				const currFullYear = currentDate.getFullYear();
				const currMonth = currentDate.getMonth();
				const currDate = currentDate.getDate();
				const selFullYear = selectMonth.getFullYear();
				const selMonth = selectMonth.getMonth();
				const selDay = selectMonth.getDay();
				const prevOffsetDate = -weekDatas.indexOf(selDay);
				const startDayDate = new Date(import_xe_utils.default.getWhatDay(selectMonth, prevOffsetDate).getTime() + dateHMSTime);
				for (let index = 0; index < 42; index++) {
					const date = import_xe_utils.default.getWhatDay(startDayDate, index);
					const itemFullYear = date.getFullYear();
					const itemMonth = date.getMonth();
					const itemDate = date.getDate();
					const isPrev = date < selectMonth;
					days.push({
						date,
						isPrev,
						isCurrent: itemFullYear === selFullYear && itemMonth === selMonth,
						isNow: itemFullYear === currFullYear && itemMonth === currMonth && itemDate === currDate,
						isNext: !isPrev && selMonth !== itemMonth,
						label: itemDate
					});
				}
			}
			return days;
		});
		const computeDayDatas = computed(() => {
			const dayList = computeDayList.value;
			return import_xe_utils.default.chunk(dayList, 7);
		});
		const computeWeekDates = computed(() => {
			const dayDatas = computeDayDatas.value;
			const firstDayOfWeek = computeFirstDayOfWeek.value;
			return dayDatas.map((list) => {
				const firstItem = list[0];
				return [{
					date: firstItem.date,
					isWeekNumber: true,
					isPrev: false,
					isCurrent: false,
					isNow: false,
					isNext: false,
					label: import_xe_utils.default.getYearWeek(firstItem.date, firstDayOfWeek)
				}].concat(list);
			});
		});
		const computeHourList = computed(() => {
			const { hours: hourOptions, hourDisabledMethod } = computeTimeOpts.value;
			const list = [];
			if (computeIsDateTimeType.value) if (hourOptions && hourOptions.length) hourOptions.forEach((item) => {
				if (import_xe_utils.default.isNumber(item) || import_xe_utils.default.isString(item)) {
					const hour = import_xe_utils.default.toNumber(item);
					list.push({
						value: hour,
						label: ("" + hour).padStart(2, "0"),
						disabled: !!(hourDisabledMethod && hourDisabledMethod({ hour }))
					});
				} else if (item) {
					const hour = import_xe_utils.default.toNumber(item.value);
					list.push({
						value: hour,
						label: ("" + (item.label || hour)).padStart(2, "0"),
						disabled: import_xe_utils.default.isBoolean(item.disabled) ? item.disabled : !!(hourDisabledMethod && hourDisabledMethod({ hour }))
					});
				}
			});
			else for (let index = 0; index < 24; index++) {
				const hour = index;
				list.push({
					value: hour,
					label: ("" + hour).padStart(2, "0"),
					disabled: !!(hourDisabledMethod && hourDisabledMethod({ hour }))
				});
			}
			return list;
		});
		const computeMinuteList = computed(() => {
			const { minutes: minuteOptions, minuteDisabledMethod } = computeTimeOpts.value;
			const list = [];
			if (computeIsDateTimeType.value) if (minuteOptions && minuteOptions.length) minuteOptions.forEach((item) => {
				if (import_xe_utils.default.isNumber(item) || import_xe_utils.default.isString(item)) {
					const minute = import_xe_utils.default.toNumber(item);
					list.push({
						value: minute,
						label: ("" + minute).padStart(2, "0"),
						disabled: !!(minuteDisabledMethod && minuteDisabledMethod({ minute }))
					});
				} else if (item) {
					const minute = import_xe_utils.default.toNumber(item.value);
					list.push({
						value: minute,
						label: ("" + (item.label || minute)).padStart(2, "0"),
						disabled: import_xe_utils.default.isBoolean(item.disabled) ? item.disabled : !!(minuteDisabledMethod && minuteDisabledMethod({ minute }))
					});
				}
			});
			else for (let index = 0; index < 60; index++) {
				const minute = index;
				list.push({
					value: minute,
					label: ("" + minute).padStart(2, "0"),
					disabled: !!(minuteDisabledMethod && minuteDisabledMethod({ minute }))
				});
			}
			return list;
		});
		const computeHasTimeMinute = computed(() => {
			const { timeFormat } = props;
			const dateValueFormat = computeDateValueFormat.value;
			return !/HH/.test(timeFormat || dateValueFormat) || /mm/.test(timeFormat || dateValueFormat);
		});
		const computeHasTimeSecond = computed(() => {
			const { timeFormat } = props;
			const dateValueFormat = computeDateValueFormat.value;
			return !/HH/.test(timeFormat || dateValueFormat) || /ss/.test(timeFormat || dateValueFormat);
		});
		const computeSecondList = computed(() => {
			const { seconds: secondOptions, secondDisabledMethod } = computeTimeOpts.value;
			const list = [];
			if (computeIsDateTimeType.value) if (secondOptions && secondOptions.length) secondOptions.forEach((item) => {
				if (import_xe_utils.default.isNumber(item) || import_xe_utils.default.isString(item)) {
					const second = import_xe_utils.default.toNumber(item);
					list.push({
						value: second,
						label: ("" + second).padStart(2, "0"),
						disabled: !!(secondDisabledMethod && secondDisabledMethod({ second }))
					});
				} else if (item) {
					const second = import_xe_utils.default.toNumber(item.value);
					list.push({
						value: second,
						label: ("" + (item.label || second)).padStart(2, "0"),
						disabled: import_xe_utils.default.isBoolean(item.disabled) ? item.disabled : !!(secondDisabledMethod && secondDisabledMethod({ second }))
					});
				}
			});
			else for (let index = 0; index < 60; index++) {
				const second = index;
				list.push({
					value: second,
					label: ("" + second).padStart(2, "0"),
					disabled: !!(secondDisabledMethod && secondDisabledMethod({ second }))
				});
			}
			return list;
		});
		const updateModelValue = (modelValue) => {
			const { type } = props;
			const dateValueFormat = computeDateValueFormat.value;
			const inpDate = parseDateValue(modelValue, type, { valueFormat: dateValueFormat });
			reactData.inputValue = inpDate;
			reactData.inputLabel = inpDate;
			dateOpenPanel();
		};
		const parseDate = (value, format) => {
			const { type, multiple } = props;
			if (type === "time") return toStringTimeDate(value);
			if (import_xe_utils.default.isArray(value)) return import_xe_utils.default.toStringDate(value[0], format);
			if (import_xe_utils.default.isString(value)) return import_xe_utils.default.toStringDate(multiple ? import_xe_utils.default.last(value.split(",")) : value, format);
			return import_xe_utils.default.toStringDate(value, format);
		};
		const dateRevert = () => {
			reactData.inputLabel = props.multiple ? computeDateMultipleLabel.value : reactData.datePanelLabel;
		};
		const afterCheckValue = (inpVal) => {
			const { type } = props;
			const { inputLabel, datetimePanelValue } = reactData;
			const dateLabelFormat = computeDateLabelFormat.value;
			if (inpVal) {
				let inpDateVal = parseDate(inpVal, dateLabelFormat);
				if (import_xe_utils.default.isValidDate(inpDateVal)) if (type === "time") {
					inpDateVal = import_xe_utils.default.toDateString(inpDateVal, dateLabelFormat);
					if (inputLabel !== inpDateVal) handleChange(inpDateVal, { type: "check" });
					reactData.inputLabel = inpDateVal;
				} else {
					let isChange = false;
					const firstDayOfWeek = computeFirstDayOfWeek.value;
					if (type === "datetime") {
						const dateValue = computeDateValue.value;
						if (inpVal !== import_xe_utils.default.toDateString(dateValue, dateLabelFormat) || inpVal !== import_xe_utils.default.toDateString(inpDateVal, dateLabelFormat)) {
							isChange = true;
							if (datetimePanelValue) {
								datetimePanelValue.setHours(inpDateVal.getHours());
								datetimePanelValue.setMinutes(inpDateVal.getMinutes());
								datetimePanelValue.setSeconds(inpDateVal.getSeconds());
							}
						}
					} else isChange = true;
					reactData.inputLabel = import_xe_utils.default.toDateString(inpDateVal, dateLabelFormat, { firstDay: firstDayOfWeek });
					if (isChange) dateChange(inpDateVal);
				}
				else dateRevert();
			} else handleChange("", { type: "check" });
		};
		const emitModel = (value) => {
			reactData.inputValue = value;
			emit("update:modelValue", value);
		};
		const handleChange = (value, evnt) => {
			const { type, modelValue, valueFormat } = props;
			const dateValueFormat = computeDateValueFormat.value;
			reactData.inputLabel = value;
			if (hasTimestampValueType(valueFormat)) {
				const dateVal = parseDateValue(value, type, { valueFormat: dateValueFormat });
				const timeNum = dateVal ? dateVal.getTime() : null;
				emitModel(timeNum);
				if (modelValue !== timeNum) dispatchEvent("change", { value: timeNum }, evnt);
			} else if (hasDateValueType(valueFormat)) {
				const dateVal = parseDateValue(value, type, { valueFormat: dateValueFormat });
				emitModel(dateVal);
				if (modelValue && dateVal ? import_xe_utils.default.toStringDate(modelValue).getTime() !== dateVal.getTime() : modelValue !== dateVal) dispatchEvent("change", { value: dateVal }, evnt);
			} else {
				emitModel(value);
				if (import_xe_utils.default.toValueString(modelValue) !== value) dispatchEvent("change", { value }, evnt);
			}
		};
		const hidePanel = () => {
			return new Promise((resolve) => {
				reactData.visiblePanel = false;
				internalData.hpTimeout = setTimeout(() => {
					reactData.isAniVisible = false;
					resolve();
				}, 350);
			});
		};
		const dateParseValue = (val) => {
			const { type } = props;
			const dateLabelFormat = computeDateLabelFormat.value;
			const dateValueFormat = computeDateValueFormat.value;
			const firstDayOfWeek = computeFirstDayOfWeek.value;
			const dateObj = parseDateObj(val, type, {
				valueFormat: dateValueFormat,
				labelFormat: dateLabelFormat,
				firstDay: firstDayOfWeek
			});
			reactData.datePanelValue = dateObj.value;
			reactData.datePanelLabel = dateObj.label;
		};
		/**
		* 值变化时处理
		*/
		const changeValue = () => {
			const isDatePanelType = computeIsDatePanelType.value;
			const { inputLabel } = reactData;
			if (isDatePanelType) {
				dateParseValue(inputLabel);
				reactData.inputLabel = props.multiple ? computeDateMultipleLabel.value : reactData.datePanelLabel;
			}
		};
		/**
		* 检查初始值
		*/
		const initValue = () => {
			const { modelValue } = props;
			const isDatePanelType = computeIsDatePanelType.value;
			updateModelValue(modelValue);
			if (isDatePanelType) changeValue();
		};
		const dateCheckMonth = (date) => {
			const firstDayOfWeek = computeFirstDayOfWeek.value;
			const weekNum = import_xe_utils.default.getYearWeek(date, firstDayOfWeek);
			const weekStartDate = import_xe_utils.default.getWhatWeek(date, 0, firstDayOfWeek, firstDayOfWeek);
			const month = import_xe_utils.default.getWhatMonth(weekNum === 1 ? import_xe_utils.default.getWhatDay(weekStartDate, 6) : date, 0, "first");
			if (!import_xe_utils.default.isEqual(month, reactData.selectMonth)) reactData.selectMonth = month;
		};
		const dateChange = (date, isReload) => {
			const { modelValue, multiple } = props;
			const { datetimePanelValue } = reactData;
			const isDateTimeType = computeIsDateTimeType.value;
			const dateValueFormat = computeDateValueFormat.value;
			const firstDayOfWeek = computeFirstDayOfWeek.value;
			if (props.type === "week") {
				const sWeek = import_xe_utils.default.toNumber(props.selectDay);
				date = import_xe_utils.default.getWhatWeek(date, 0, sWeek, firstDayOfWeek);
			} else if (isDateTimeType) {
				if (datetimePanelValue) {
					date.setHours(datetimePanelValue.getHours());
					date.setMinutes(datetimePanelValue.getMinutes());
					date.setSeconds(datetimePanelValue.getSeconds());
				}
			}
			const inpVal = import_xe_utils.default.toDateString(date, dateValueFormat, { firstDay: firstDayOfWeek });
			dateCheckMonth(date);
			if (multiple) {
				const overCount = computeOverCount.value;
				if (isDateTimeType) {
					const dateListValue = isReload ? [] : [...computeDateListValue.value];
					const datetimeRest = [];
					const eqIndex = import_xe_utils.default.findIndexOf(dateListValue, (val) => import_xe_utils.default.isDateSame(date, val, "yyyyMMdd"));
					if (eqIndex === -1) {
						if (overCount) return;
						dateListValue.push(date);
					} else dateListValue.splice(eqIndex, 1);
					dateListValue.forEach((item) => {
						if (item) {
							if (datetimePanelValue) {
								item.setHours(datetimePanelValue.getHours());
								item.setMinutes(datetimePanelValue.getMinutes());
								item.setSeconds(datetimePanelValue.getSeconds());
							}
							datetimeRest.push(item);
						}
					});
					handleChange(datetimeRest.map((date) => import_xe_utils.default.toDateString(date, dateValueFormat)).join(","), { type: "update" });
				} else {
					const dateMultipleValue = isReload ? [] : computeDateMultipleValue.value;
					if (dateMultipleValue.some((val) => import_xe_utils.default.isEqual(val, inpVal))) handleChange(dateMultipleValue.filter((val) => !import_xe_utils.default.isEqual(val, inpVal)).join(","), { type: "update" });
					else {
						if (overCount) return;
						handleChange(dateMultipleValue.concat([inpVal]).join(","), { type: "update" });
					}
				}
			} else if (!import_xe_utils.default.isEqual(modelValue, inpVal)) handleChange(inpVal, { type: "update" });
		};
		const dateMonthHandle = (date, offsetMonth) => {
			const firstDayOfWeek = computeFirstDayOfWeek.value;
			const weekNum = import_xe_utils.default.getYearWeek(date, firstDayOfWeek);
			const weekStartDate = import_xe_utils.default.getWhatWeek(date, 0, firstDayOfWeek, firstDayOfWeek);
			reactData.selectMonth = import_xe_utils.default.getWhatMonth(weekNum === 1 ? import_xe_utils.default.getWhatDay(weekStartDate, 6) : date, offsetMonth, "first");
		};
		const dateNowHandle = () => {
			const { type } = props;
			const firstDayOfWeek = computeFirstDayOfWeek.value;
			let currentDate = /* @__PURE__ */ new Date();
			switch (type) {
				case "week":
					currentDate = import_xe_utils.default.getWhatWeek(currentDate, 0, firstDayOfWeek);
					break;
				case "datetime":
					currentDate = /* @__PURE__ */ new Date();
					reactData.datetimePanelValue = /* @__PURE__ */ new Date();
					break;
				default:
					currentDate = import_xe_utils.default.getWhatDay(Date.now(), 0, "first");
					break;
			}
			reactData.currentDate = currentDate;
			dateMonthHandle(currentDate, 0);
		};
		const dateToggleYearTypeEvent = () => {
			reactData.datePanelType = "year";
		};
		const dateToggleMonthTypeEvent = () => {
			let { datePanelType } = reactData;
			if (datePanelType === "month" || datePanelType === "quarter") datePanelType = "year";
			else datePanelType = "month";
			reactData.datePanelType = datePanelType;
		};
		const datePrevEvent = (evnt) => {
			const { type } = props;
			const { datePanelType, selectMonth, inputLabel } = reactData;
			const { yearSize } = internalData;
			const value = inputLabel;
			if (!computeIsDisabledPrevDateBtn.value) {
				let viewDate;
				if (type === "year") viewDate = import_xe_utils.default.getWhatYear(selectMonth, -yearSize, "first");
				else if (type === "month" || type === "quarter") if (datePanelType === "year") viewDate = import_xe_utils.default.getWhatYear(selectMonth, -yearSize, "first");
				else viewDate = import_xe_utils.default.getWhatYear(selectMonth, -1, "first");
				else if (datePanelType === "year") viewDate = import_xe_utils.default.getWhatYear(selectMonth, -yearSize, "first");
				else if (datePanelType === "month") viewDate = import_xe_utils.default.getWhatYear(selectMonth, -1, "first");
				else viewDate = import_xe_utils.default.getWhatMonth(selectMonth, -1, "first");
				reactData.selectMonth = viewDate;
				dispatchEvent("date-prev", {
					viewType: datePanelType,
					viewDate,
					value,
					type
				}, evnt);
			}
		};
		const dateTodayMonthEvent = (evnt) => {
			dateNowHandle();
			dateChange(reactData.currentDate, true);
			if (!props.multiple) hidePanel();
			dispatchEvent("date-today", { type: props.type }, evnt);
		};
		const dateNextEvent = (evnt) => {
			const { type } = props;
			const { datePanelType, selectMonth, inputLabel } = reactData;
			const { yearSize } = internalData;
			const value = inputLabel;
			if (!computeIsDisabledNextDateBtn.value) {
				let viewDate;
				if (type === "year") viewDate = import_xe_utils.default.getWhatYear(selectMonth, yearSize, "first");
				else if (type === "month" || type === "quarter") if (datePanelType === "year") viewDate = import_xe_utils.default.getWhatYear(selectMonth, yearSize, "first");
				else viewDate = import_xe_utils.default.getWhatYear(selectMonth, 1, "first");
				else if (datePanelType === "year") viewDate = import_xe_utils.default.getWhatYear(selectMonth, yearSize, "first");
				else if (datePanelType === "month") viewDate = import_xe_utils.default.getWhatYear(selectMonth, 1, "first");
				else viewDate = import_xe_utils.default.getWhatMonth(selectMonth, 1, "first");
				reactData.selectMonth = viewDate;
				dispatchEvent("date-next", {
					viewType: datePanelType,
					value,
					type
				}, evnt);
			}
		};
		const isRangeDisabled = (item) => {
			const dateStartTime = computeDateStartTime.value;
			const dateEndTime = computeDateEndTime.value;
			const { date } = item;
			if (dateStartTime && dateStartTime.getTime() > date.getTime()) return true;
			if (dateEndTime && dateEndTime.getTime() < date.getTime()) return true;
			return false;
		};
		const isDateDisabled = (item) => {
			const { disabledMethod } = props;
			const { datePanelType } = reactData;
			const { date } = item;
			if (disabledMethod) return disabledMethod({
				type: datePanelType,
				viewType: datePanelType,
				date,
				$datePanel: $xeDatePanel
			});
			return false;
		};
		const hasAllDisabled = (item) => {
			return isRangeDisabled(item) || isDateDisabled(item);
		};
		const dateSelectItem = (date) => {
			const { type, multiple } = props;
			const { datePanelType } = reactData;
			if (type === "month") if (datePanelType === "year") {
				reactData.datePanelType = "month";
				dateCheckMonth(date);
			} else {
				dateChange(date);
				if (!multiple) hidePanel();
			}
			else if (type === "year") {
				dateChange(date);
				if (!multiple) hidePanel();
			} else if (type === "quarter") if (datePanelType === "year") {
				reactData.datePanelType = "quarter";
				dateCheckMonth(date);
			} else {
				dateChange(date);
				if (!multiple) hidePanel();
			}
			else if (datePanelType === "month") {
				reactData.datePanelType = type === "week" ? type : "day";
				dateCheckMonth(date);
			} else if (datePanelType === "year") {
				reactData.datePanelType = "month";
				dateCheckMonth(date);
			} else {
				dateChange(date);
				if (type === "datetime") {} else if (!multiple) hidePanel();
			}
		};
		const dateSelectEvent = (item) => {
			if (!hasAllDisabled(item)) dateSelectItem(item.date);
		};
		const dateMoveDay = (offsetDay) => {
			if (!hasAllDisabled({ date: offsetDay })) {
				if (!computeDayList.value.some((item) => import_xe_utils.default.isDateSame(item.date, offsetDay, "yyyyMMdd"))) dateCheckMonth(offsetDay);
				dateParseValue(offsetDay);
			}
		};
		const dateMoveYear = (offsetYear) => {
			if (!hasAllDisabled({ date: offsetYear })) {
				if (!computeYearList.value.some((item) => import_xe_utils.default.isDateSame(item.date, offsetYear, "yyyy"))) dateCheckMonth(offsetYear);
				dateParseValue(offsetYear);
			}
		};
		const dateMoveQuarter = (offsetQuarter) => {
			if (!hasAllDisabled({ date: offsetQuarter })) {
				if (!computeQuarterList.value.some((item) => import_xe_utils.default.isDateSame(item.date, offsetQuarter, "yyyyq"))) dateCheckMonth(offsetQuarter);
				dateParseValue(offsetQuarter);
			}
		};
		const dateMoveMonth = (offsetMonth) => {
			if (!hasAllDisabled({ date: offsetMonth })) {
				if (!computeMonthList.value.some((item) => import_xe_utils.default.isDateSame(item.date, offsetMonth, "yyyyMM"))) dateCheckMonth(offsetMonth);
				dateParseValue(offsetMonth);
			}
		};
		const dateMouseenterEvent = (item) => {
			if (!hasAllDisabled(item)) {
				const { datePanelType } = reactData;
				if (datePanelType === "month") dateMoveMonth(item.date);
				else if (datePanelType === "quarter") dateMoveQuarter(item.date);
				else if (datePanelType === "year") dateMoveYear(item.date);
				else dateMoveDay(item.date);
			}
		};
		const dateMouseleaveEvent = () => {
			reactData.datePanelValue = null;
		};
		const updateTimePos = (liElem) => {
			if (liElem) {
				const height = liElem.offsetHeight;
				const ulElem = liElem.parentNode;
				ulElem.scrollTop = liElem.offsetTop - height * 4;
			}
		};
		const dateTimeChangeEvent = (evnt) => {
			const { datetimePanelValue } = reactData;
			reactData.datetimePanelValue = datetimePanelValue ? new Date(datetimePanelValue.getTime()) : /* @__PURE__ */ new Date();
			updateTimePos(evnt.currentTarget);
		};
		const dateHourEvent = (evnt, item) => {
			const { datetimePanelValue } = reactData;
			if (!item.disabled) {
				if (datetimePanelValue) datetimePanelValue.setHours(item.value);
				dateTimeChangeEvent(evnt);
			}
		};
		const dateConfirmEvent = (evnt) => {
			const { multiple } = props;
			const { datetimePanelValue } = reactData;
			const dateValue = computeDateValue.value;
			const isDateTimeType = computeIsDateTimeType.value;
			if (isDateTimeType) {
				const dateValueFormat = computeDateValueFormat.value;
				if (multiple) {
					const dateMultipleValue = computeDateMultipleValue.value;
					if (isDateTimeType) {
						const dateListValue = [...computeDateListValue.value];
						const datetimeRest = [];
						dateListValue.forEach((item) => {
							if (item) {
								if (datetimePanelValue) {
									item.setHours(datetimePanelValue.getHours());
									item.setMinutes(datetimePanelValue.getMinutes());
									item.setSeconds(datetimePanelValue.getSeconds());
								}
								datetimeRest.push(item);
							}
						});
						handleChange(datetimeRest.map((date) => import_xe_utils.default.toDateString(date, dateValueFormat)).join(","), { type: "update" });
					} else handleChange(dateMultipleValue.join(","), { type: "update" });
				} else dateChange(dateValue || reactData.currentDate);
			}
			hidePanel();
			dispatchEvent("confirm", {}, evnt);
		};
		const dateMinuteEvent = (evnt, item) => {
			const { datetimePanelValue } = reactData;
			if (!item.disabled) {
				if (datetimePanelValue) datetimePanelValue.setMinutes(item.value);
				dateTimeChangeEvent(evnt);
			}
		};
		const dateSecondEvent = (evnt, item) => {
			const { datetimePanelValue } = reactData;
			if (!item.disabled) {
				if (datetimePanelValue) datetimePanelValue.setSeconds(item.value);
				dateTimeChangeEvent(evnt);
			}
		};
		const dateOpenPanel = () => {
			const { type, defaultDate, defaultTime } = props;
			const isDateTimeType = computeIsDateTimeType.value;
			const dateValue = computeDateValue.value;
			if ([
				"year",
				"quarter",
				"month",
				"week"
			].indexOf(type) > -1) reactData.datePanelType = type;
			else reactData.datePanelType = "day";
			reactData.currentDate = import_xe_utils.default.getWhatDay(Date.now(), 0, "first");
			if (dateValue) {
				dateMonthHandle(dateValue, 0);
				dateParseValue(dateValue);
			} else if (defaultDate) {
				const defDate = parseDate(defaultDate, "yyyy-MM-dd");
				if (import_xe_utils.default.isValidDate(defDate)) dateMonthHandle(defDate, 0);
				else dateNowHandle();
			} else dateNowHandle();
			if (isDateTimeType) {
				let dtPanelValue = reactData.datePanelValue;
				if (!dtPanelValue) {
					dtPanelValue = import_xe_utils.default.getWhatDay(Date.now(), 0, "first");
					if (defaultTime) {
						const defTime = toStringTimeDate(defaultTime);
						if (import_xe_utils.default.isValidDate(defTime)) {
							dtPanelValue.setHours(defTime.getHours());
							dtPanelValue.setMinutes(defTime.getMinutes());
							dtPanelValue.setSeconds(defTime.getSeconds());
						}
					}
				}
				reactData.datetimePanelValue = dtPanelValue;
				nextTick(() => {
					const timeBodyElem = refInputTimeBody.value;
					import_xe_utils.default.arrayEach(timeBodyElem.querySelectorAll("li.is--selected"), (elem) => {
						updateTimePos(elem);
					});
				});
			}
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $datePanel: $xeDatePanel }, params));
		};
		Object.assign($xeDatePanel, {
			dispatchEvent,
			getModelValue() {
				return reactData.inputValue;
			},
			setPanelDate(date) {
				if (date) dateCheckMonth(date);
			},
			getPanelDate() {
				return reactData.selectMonth;
			},
			checkValue(value) {
				afterCheckValue(value);
			},
			confirmByEvent(evnt) {
				dateConfirmEvent(evnt);
			}
		});
		const renderDateLabel = (item, label) => {
			const { festivalMethod } = props;
			const labelVNs = [];
			if (festivalMethod) {
				const { datePanelType } = reactData;
				const festivalRest = festivalMethod({
					type: datePanelType,
					viewType: datePanelType,
					date: item.date,
					$datePanel: $xeDatePanel
				});
				const festivalItem = festivalRest ? import_xe_utils.default.isString(festivalRest) ? { label: festivalRest } : festivalRest : {};
				const extraItem = festivalItem.extra ? import_xe_utils.default.isString(festivalItem.extra) ? { label: festivalItem.extra } : festivalItem.extra : null;
				labelVNs.push(h("div", { class: ["vxe-date-panel--label", { "is-notice": festivalItem.notice }] }, extraItem && extraItem.label ? [h("div", { class: "vxe-date-panel--label--number" }, `${label}`), h("div", {
					class: [
						"vxe-date-panel--label--extra",
						extraItem.important ? "is-important" : "",
						extraItem.className
					],
					style: extraItem.style
				}, import_xe_utils.default.toValueString(extraItem.label))] : `${label}`));
				const festivalLabel = festivalItem.label;
				if (festivalLabel) {
					const festivalLabels = import_xe_utils.default.toValueString(festivalLabel).split(",");
					labelVNs.push(h("div", {
						class: [
							"vxe-date-panel--festival",
							festivalItem.important ? "is-important" : "",
							festivalItem.className
						],
						style: festivalItem.style
					}, [festivalLabels.length > 1 ? h("div", { class: ["vxe-date-panel--festival--overlap", `overlap--${festivalLabels.length}`] }, festivalLabels.map((label) => h("div", label.substring(0, 3)))) : h("div", { class: "vxe-date-panel--festival--label" }, festivalLabels[0].substring(0, 3))]));
				}
			}
			return labelVNs;
		};
		const renderDateDayTable = () => {
			const { multiple } = props;
			const { datePanelType, datePanelValue } = reactData;
			const dateValue = computeDateValue.value;
			const dateHeaders = computeDateHeaders.value;
			const dayDatas = computeDayDatas.value;
			const dateListValue = computeDateListValue.value;
			const overCount = computeOverCount.value;
			const matchFormat = "yyyyMMdd";
			return [h("div", { class: ["vxe-date-panel--view-wrapper", `type--${datePanelType}`] }, [h("div", { class: "vxe-date-panel--view-header" }, [h("div", { class: "vxe-date-panel--view-row" }, dateHeaders.map((item) => {
				return h("div", {
					class: "vxe-date-panel--view-item",
					style: { width: `${100 / dateHeaders.length}%` }
				}, [h("div", { class: "vxe-date-panel--view-item-inner" }, [h("div", { class: "vxe-date-panel--view-item-label" }, item.label)])]);
			}))]), h("div", { class: "vxe-date-panel--view-body" }, dayDatas.map((rows) => {
				return h("div", {
					class: "vxe-date-panel--view-row",
					style: { height: `${100 / dayDatas.length}%` }
				}, rows.map((item) => {
					const isSelected = multiple ? dateListValue.some((val) => import_xe_utils.default.isDateSame(val, item.date, matchFormat)) : import_xe_utils.default.isDateSame(dateValue, item.date, matchFormat);
					return h("div", {
						class: ["vxe-date-panel--view-item", {
							"is--prev": item.isPrev,
							"is--current": item.isCurrent,
							"is--now": item.isNow,
							"is--next": item.isNext,
							"is--range-disabled": isRangeDisabled(item),
							"is--disabled": isDateDisabled(item),
							"is--selected": isSelected,
							"is--over": overCount && !isSelected,
							"is--hover": !overCount && import_xe_utils.default.isDateSame(datePanelValue, item.date, matchFormat)
						}],
						style: { width: `${100 / rows.length}%` },
						onClick: () => dateSelectEvent(item),
						onMouseenter: () => dateMouseenterEvent(item),
						onMouseleave: dateMouseleaveEvent
					}, [h("div", { class: "vxe-date-panel--view-item-inner" }, renderDateLabel(item, item.label))]);
				}));
			}))])];
		};
		const renderDateWeekTable = () => {
			const { multiple } = props;
			const { datePanelType, datePanelValue } = reactData;
			const dateValue = computeDateValue.value;
			const weekHeaders = computeWeekHeaders.value;
			const weekDates = computeWeekDates.value;
			const dateListValue = computeDateListValue.value;
			const overCount = computeOverCount.value;
			const matchFormat = "yyyyMMdd";
			return [h("div", { class: ["vxe-date-panel--view-wrapper", `type--${datePanelType}`] }, [h("div", { class: "vxe-date-panel--view-header" }, [h("div", { class: "vxe-date-panel--view-row" }, weekHeaders.map((item, rIndex) => {
				return h("div", {
					class: "vxe-date-panel--view-item",
					style: { width: `${rIndex ? 13 : 9}%` }
				}, [h("div", { class: "vxe-date-panel--view-item-inner" }, [h("div", { class: "vxe-date-panel--view-item-label" }, item.label)])]);
			}))]), h("div", { class: "vxe-date-panel--view-body" }, weekDates.map((rows) => {
				const isSelected = multiple ? rows.some((item) => dateListValue.some((val) => import_xe_utils.default.isDateSame(val, item.date, matchFormat))) : rows.some((item) => import_xe_utils.default.isDateSame(dateValue, item.date, matchFormat));
				const isHover = rows.some((item) => import_xe_utils.default.isDateSame(datePanelValue, item.date, matchFormat));
				const isNowWeek = rows.some((item) => item.isNow);
				return h("div", {
					class: "vxe-date-panel--view-row",
					style: { height: `${100 / weekDates.length}%` }
				}, rows.map((item, rIndex) => {
					return h("div", {
						class: ["vxe-date-panel--view-item", {
							"is--prev": item.isPrev,
							"is--current": item.isCurrent,
							"is--now": rIndex ? item.isNow : isNowWeek,
							"is--next": item.isNext,
							"is--range-disabled": isRangeDisabled(item),
							"is--disabled": isDateDisabled(item),
							"is--selected": isSelected,
							"is--over": overCount && !isSelected,
							"is--hover": !overCount && isHover
						}],
						style: { width: `${rIndex ? 13 : 9}%` },
						onClick: () => dateSelectEvent(item),
						onMouseenter: () => dateMouseenterEvent(item),
						onMouseleave: dateMouseleaveEvent
					}, [h("div", { class: "vxe-date-panel--view-item-inner" }, renderDateLabel(item, item.label))]);
				}));
			}))])];
		};
		const renderDateMonthTable = () => {
			const { multiple } = props;
			const { datePanelType, datePanelValue } = reactData;
			const dateValue = computeDateValue.value;
			const monthDatas = computeMonthDatas.value;
			const dateListValue = computeDateListValue.value;
			const overCount = computeOverCount.value;
			const matchFormat = "yyyyMM";
			return [h("div", { class: ["vxe-date-panel--view-wrapper", `type--${datePanelType}`] }, [h("div", { class: "vxe-date-panel--view-body" }, monthDatas.map((rows) => {
				return h("div", {
					class: "vxe-date-panel--view-row",
					style: { height: `${100 / monthDatas.length}%` }
				}, rows.map((item) => {
					const isSelected = multiple ? dateListValue.some((val) => import_xe_utils.default.isDateSame(val, item.date, matchFormat)) : import_xe_utils.default.isDateSame(dateValue, item.date, matchFormat);
					return h("div", {
						class: ["vxe-date-panel--view-item", {
							"is--prev": item.isPrev,
							"is--current": item.isCurrent,
							"is--now": item.isNow,
							"is--next": item.isNext,
							"is--range-disabled": isRangeDisabled(item),
							"is--disabled": isDateDisabled(item),
							"is--selected": isSelected,
							"is--over": overCount && !isSelected,
							"is--hover": !overCount && import_xe_utils.default.isDateSame(datePanelValue, item.date, matchFormat)
						}],
						style: { width: `${100 / rows.length}%` },
						onClick: () => dateSelectEvent(item),
						onMouseenter: () => dateMouseenterEvent(item),
						onMouseleave: dateMouseleaveEvent
					}, [h("div", { class: "vxe-date-panel--view-item-inner" }, renderDateLabel(item, getI18n(`vxe.input.date.months.m${item.month}`)))]);
				}));
			}))])];
		};
		const renderDateQuarterTable = () => {
			const { multiple } = props;
			const { datePanelType, datePanelValue } = reactData;
			const dateValue = computeDateValue.value;
			const quarterDatas = computeQuarterDatas.value;
			const dateListValue = computeDateListValue.value;
			const overCount = computeOverCount.value;
			const matchFormat = "yyyyq";
			return [h("div", { class: ["vxe-date-panel--view-wrapper", `type--${datePanelType}`] }, [h("div", { class: "vxe-date-panel--view-body" }, quarterDatas.map((rows) => {
				return h("div", {
					class: "vxe-date-panel--view-row",
					style: { height: `${100 / quarterDatas.length}%` }
				}, rows.map((item) => {
					const isSelected = multiple ? dateListValue.some((val) => import_xe_utils.default.isDateSame(val, item.date, matchFormat)) : import_xe_utils.default.isDateSame(dateValue, item.date, matchFormat);
					return h("div", {
						class: ["vxe-date-panel--view-item", {
							"is--prev": item.isPrev,
							"is--current": item.isCurrent,
							"is--now": item.isNow,
							"is--next": item.isNext,
							"is--range-disabled": isRangeDisabled(item),
							"is--disabled": isDateDisabled(item),
							"is--selected": isSelected,
							"is--over": overCount && !isSelected,
							"is--hover": !overCount && import_xe_utils.default.isDateSame(datePanelValue, item.date, matchFormat)
						}],
						style: { width: `${100 / rows.length}%` },
						onClick: () => dateSelectEvent(item),
						onMouseenter: () => dateMouseenterEvent(item),
						onMouseleave: dateMouseleaveEvent
					}, [h("div", { class: "vxe-date-panel--view-item-inner" }, renderDateLabel(item, getI18n(`vxe.input.date.quarters.q${item.quarter}`)))]);
				}));
			}))])];
		};
		const renderDateYearTable = () => {
			const { multiple } = props;
			const { datePanelType, datePanelValue } = reactData;
			const dateValue = computeDateValue.value;
			const yearDatas = computeYearDatas.value;
			const dateListValue = computeDateListValue.value;
			const overCount = computeOverCount.value;
			const matchFormat = "yyyy";
			return [h("div", { class: ["vxe-date-panel--view-wrapper", `type--${datePanelType}`] }, [h("div", { class: "vxe-date-panel--view-body" }, yearDatas.map((rows) => {
				return h("div", {
					class: "vxe-date-panel--view-row",
					style: { height: `${100 / yearDatas.length}%` }
				}, rows.map((item) => {
					const isSelected = multiple ? dateListValue.some((val) => import_xe_utils.default.isDateSame(val, item.date, matchFormat)) : import_xe_utils.default.isDateSame(dateValue, item.date, matchFormat);
					return h("div", {
						class: ["vxe-date-panel--view-item", {
							"is--prev": item.isPrev,
							"is--current": item.isCurrent,
							"is--now": item.isNow,
							"is--next": item.isNext,
							"is--range-disabled": isRangeDisabled(item),
							"is--disabled": isDateDisabled(item),
							"is--selected": isSelected,
							"is--over": overCount && !isSelected,
							"is--hover": !overCount && import_xe_utils.default.isDateSame(datePanelValue, item.date, matchFormat)
						}],
						style: { width: `${100 / rows.length}%` },
						onClick: () => dateSelectEvent(item),
						onMouseenter: () => dateMouseenterEvent(item),
						onMouseleave: dateMouseleaveEvent
					}, [h("div", { class: "vxe-date-panel--view-item-inner" }, renderDateLabel(item, item.year))]);
				}));
			}))])];
		};
		const renderDateTable = () => {
			const { datePanelType } = reactData;
			switch (datePanelType) {
				case "week": return renderDateWeekTable();
				case "month": return renderDateMonthTable();
				case "quarter": return renderDateQuarterTable();
				case "year": return renderDateYearTable();
			}
			return renderDateDayTable();
		};
		const renderDatePanel = () => {
			const { datePanelType } = reactData;
			const isDisabledPrevDateBtn = computeIsDisabledPrevDateBtn.value;
			const isDisabledNextDateBtn = computeIsDisabledNextDateBtn.value;
			const selectDatePanelObj = computeSelectDatePanelObj.value;
			return [h("div", { class: "vxe-date-panel--picker-header" }, [h("div", { class: "vxe-date-panel--picker-type-wrapper" }, [datePanelType === "year" ? h("span", { class: "vxe-date-panel--picker-label" }, selectDatePanelObj.y) : h("span", { class: "vxe-date-panel--picker-btns" }, [h("span", {
				class: "vxe-date-panel--picker-btn",
				onClick: dateToggleYearTypeEvent
			}, selectDatePanelObj.y), selectDatePanelObj.m ? h("span", {
				class: "vxe-date-panel--picker-btn",
				onClick: dateToggleMonthTypeEvent
			}, selectDatePanelObj.m) : renderEmptyElement($xeDatePanel)])]), h("div", { class: "vxe-date-panel--picker-btn-wrapper" }, [
				h("span", {
					class: ["vxe-date-panel--picker-btn vxe-date-panel--picker-prev-btn", { "is--disabled": isDisabledPrevDateBtn }],
					onClick: datePrevEvent
				}, [h("i", { class: "vxe-icon-caret-left" })]),
				h("span", {
					class: "vxe-date-panel--picker-btn vxe-date-panel--picker-current-btn",
					onClick: dateTodayMonthEvent
				}, [h("i", { class: "vxe-icon-dot" })]),
				h("span", {
					class: ["vxe-date-panel--picker-btn vxe-date-panel--picker-next-btn", { "is--disabled": isDisabledNextDateBtn }],
					onClick: dateNextEvent
				}, [h("i", { class: "vxe-icon-caret-right" })])
			])]), h("div", { class: "vxe-date-panel--picker-body" }, renderDateTable())];
		};
		const renderTimePanel = () => {
			const { type } = props;
			const { datetimePanelValue } = reactData;
			const dateTimeLabel = computeDateTimeLabel.value;
			const hourList = computeHourList.value;
			const hasTimeMinute = computeHasTimeMinute.value;
			const minuteList = computeMinuteList.value;
			const hasTimeSecond = computeHasTimeSecond.value;
			const secondList = computeSecondList.value;
			return [type === "time" ? renderEmptyElement($xeDatePanel) : h("div", { class: "vxe-date-panel--time-header" }, [h("div", { class: "vxe-date-panel--time-title" }, dateTimeLabel)]), h("div", {
				ref: refInputTimeBody,
				class: "vxe-date-panel--time-body"
			}, [
				h("ul", { class: "vxe-date-panel--time-hour-list" }, hourList.map((item, index) => {
					return h("li", {
						key: index,
						class: {
							"is--disabled": item.disabled,
							"is--selected": datetimePanelValue && datetimePanelValue.getHours() === item.value
						},
						onClick: (evnt) => dateHourEvent(evnt, item)
					}, item.label);
				})),
				hasTimeMinute ? h("ul", { class: "vxe-date-panel--time-minute-list" }, minuteList.map((item, index) => {
					return h("li", {
						key: index,
						class: {
							"is--disabled": item.disabled,
							"is--selected": datetimePanelValue && datetimePanelValue.getMinutes() === item.value
						},
						onClick: (evnt) => dateMinuteEvent(evnt, item)
					}, item.label);
				})) : renderEmptyElement($xeDatePanel),
				hasTimeMinute && hasTimeSecond ? h("ul", { class: "vxe-date-panel--time-second-list" }, secondList.map((item, index) => {
					return h("li", {
						key: index,
						class: {
							"is--disabled": item.disabled,
							"is--selected": datetimePanelValue && datetimePanelValue.getSeconds() === item.value
						},
						onClick: (evnt) => dateSecondEvent(evnt, item)
					}, item.label);
				})) : renderEmptyElement($xeDatePanel)
			])];
		};
		const renderPickerPanel = () => {
			const { type } = props;
			if (type === "datetime") return h("div", {
				key: type,
				ref: refPanelWrapper,
				class: "vxe-date-panel--time-layout-wrapper"
			}, [h("div", { class: "vxe-date-panel--time-left-wrapper" }, renderDatePanel()), h("div", { class: "vxe-date-panel--time-right-wrapper" }, renderTimePanel())]);
			else if (type === "time") return h("div", {
				key: type,
				ref: refPanelWrapper,
				class: "vxe-date-panel--wrapper"
			}, renderTimePanel());
			return h("div", {
				key: type || "default",
				ref: refPanelWrapper,
				class: "vxe-date-panel--wrapper"
			}, renderDatePanel());
		};
		const renderVN = () => {
			const { type } = props;
			const vSize = computeSize.value;
			return h("div", { class: [
				"vxe-date-panel",
				`type--${type}`,
				{ [`size--${vSize}`]: vSize }
			] }, [renderPickerPanel()]);
		};
		watch(() => props.modelValue, (val) => {
			updateModelValue(val);
			changeValue();
		});
		watch(() => props.type, () => {
			Object.assign(reactData, {
				inputLabel: "",
				datetimePanelValue: null,
				datePanelValue: null,
				datePanelLabel: "",
				datePanelType: "day",
				selectMonth: null,
				currentDate: null
			});
			initValue();
		});
		watch(computeDateLabelFormat, () => {
			if (computeIsDatePanelType.value) {
				dateParseValue(reactData.datePanelValue);
				reactData.inputLabel = props.multiple ? computeDateMultipleLabel.value : reactData.datePanelLabel;
			}
		});
		initValue();
		dateOpenPanel();
		$xeDatePanel.renderVN = renderVN;
		return $xeDatePanel;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/date-panel/index.js
var VxeDatePanel = Object.assign({}, date_panel_default, { install(app) {
	app.component(date_panel_default.name, date_panel_default);
} });
dynamicApp.use(VxeDatePanel);
VxeUI.component(date_panel_default);
var DatePanel = VxeDatePanel;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/date-picker/src/date-picker.js
var date_picker_default = defineVxeComponent({
	name: "VxeDatePicker",
	props: {
		modelValue: [
			String,
			Number,
			Date
		],
		immediate: {
			type: Boolean,
			default: true
		},
		name: String,
		type: {
			type: String,
			default: "date"
		},
		clearable: {
			type: Boolean,
			default: () => getConfig().datePicker.clearable
		},
		readonly: {
			type: Boolean,
			default: null
		},
		disabled: {
			type: Boolean,
			default: null
		},
		placeholder: String,
		autoComplete: {
			type: String,
			default: "off"
		},
		form: String,
		className: String,
		zIndex: Number,
		size: {
			type: String,
			default: () => getConfig().datePicker.size || getConfig().size
		},
		multiple: Boolean,
		limitCount: {
			type: [String, Number],
			default: () => getConfig().datePicker.limitCount
		},
		startDate: {
			type: [
				String,
				Number,
				Date
			],
			default: () => getConfig().datePicker.startDate
		},
		endDate: {
			type: [
				String,
				Number,
				Date
			],
			default: () => getConfig().datePicker.endDate
		},
		defaultDate: [
			String,
			Number,
			Date
		],
		defaultTime: [
			String,
			Number,
			Date
		],
		minDate: [
			String,
			Number,
			Date
		],
		maxDate: [
			String,
			Number,
			Date
		],
		startDay: {
			type: [String, Number],
			default: () => getConfig().datePicker.startDay
		},
		labelFormat: String,
		valueFormat: String,
		timeFormat: String,
		editable: {
			type: Boolean,
			default: true
		},
		festivalMethod: {
			type: Function,
			default: () => getConfig().datePicker.festivalMethod
		},
		disabledMethod: {
			type: Function,
			default: () => getConfig().datePicker.disabledMethod
		},
		selectDay: {
			type: [String, Number],
			default: () => getConfig().datePicker.selectDay
		},
		showClearButton: {
			type: Boolean,
			default: () => getConfig().datePicker.showClearButton
		},
		showConfirmButton: {
			type: Boolean,
			default: () => getConfig().datePicker.showConfirmButton
		},
		autoClose: {
			type: Boolean,
			default: () => getConfig().datePicker.autoClose
		},
		prefixIcon: String,
		suffixIcon: String,
		placement: String,
		transfer: {
			type: Boolean,
			default: null
		},
		timeConfig: Object,
		popupConfig: Object,
		shortcutConfig: Object,
		startWeek: Number
	},
	emits: [
		"update:modelValue",
		"input",
		"change",
		"keydown",
		"keyup",
		"click",
		"focus",
		"blur",
		"clear",
		"confirm",
		"prefix-click",
		"suffix-click",
		"date-prev",
		"date-today",
		"date-next",
		"shortcut-click"
	],
	setup(props, context) {
		const { slots, emit } = context;
		const $xeModal = inject("$xeModal", null);
		const $xeDrawer = inject("$xeDrawer", null);
		const $xeTable = inject("$xeTable", null);
		const $xeForm = inject("$xeForm", null);
		const formItemInfo = inject("xeFormItemInfo", null);
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const reactData = reactive({
			initialized: false,
			panelIndex: 0,
			visiblePanel: false,
			isAniVisible: false,
			panelStyle: {},
			panelPlacement: "",
			isActivated: false,
			inputValue: "",
			inputLabel: ""
		});
		const internalData = { hpTimeout: void 0 };
		const refElem = ref();
		const refInputTarget = ref();
		const refInputPanel = ref();
		const refPanelWrapper = ref();
		const refDatePanel = ref();
		const refMaps = {
			refElem,
			refInput: refInputTarget
		};
		const $xeDatePicker = {
			xID,
			props,
			context,
			reactData,
			internalData,
			getRefMaps: () => refMaps
		};
		const computeBtnTransfer = computed(() => {
			const { transfer } = props;
			const popupOpts = computePopupOpts.value;
			if (import_xe_utils.default.isBoolean(popupOpts.transfer)) return popupOpts.transfer;
			if (transfer === null) {
				const globalTransfer = getConfig().datePicker.transfer;
				if (import_xe_utils.default.isBoolean(globalTransfer)) return globalTransfer;
				if ($xeTable || $xeModal || $xeDrawer || $xeForm) return true;
			}
			return transfer;
		});
		const computeFormReadonly = computed(() => {
			const { readonly } = props;
			if (readonly === null) {
				if ($xeForm) return $xeForm.props.readonly;
				return false;
			}
			return readonly;
		});
		const computeIsDisabled = computed(() => {
			const { disabled } = props;
			if (disabled === null) {
				if ($xeForm) return $xeForm.props.disabled;
				return false;
			}
			return disabled;
		});
		const computeIsDateTimeType = computed(() => {
			const { type } = props;
			return type === "time" || type === "datetime";
		});
		const computeIsDatePickerType = computed(() => {
			return computeIsDateTimeType.value || [
				"date",
				"week",
				"month",
				"quarter",
				"year"
			].indexOf(props.type) > -1;
		});
		const computeIsClearable = computed(() => {
			return props.clearable;
		});
		const computeInputReadonly = computed(() => {
			const { type, editable, multiple } = props;
			return computeFormReadonly.value || multiple || !editable || type === "week" || type === "quarter";
		});
		const computeInpPlaceholder = computed(() => {
			const { placeholder } = props;
			if (placeholder) return getFuncText(placeholder);
			const globalPlaceholder = getConfig().datePicker.placeholder;
			if (globalPlaceholder) return getFuncText(globalPlaceholder);
			return getI18n("vxe.base.pleaseSelect");
		});
		const computeInpImmediate = computed(() => {
			const { immediate } = props;
			return immediate;
		});
		const computeTimeOpts = computed(() => {
			return Object.assign({}, getConfig().datePicker.timeConfig, props.timeConfig);
		});
		const computePopupOpts = computed(() => {
			return Object.assign({}, getConfig().datePicker.popupConfig, props.popupConfig);
		});
		const computeShortcutOpts = computed(() => {
			return Object.assign({}, getConfig().datePicker.shortcutConfig, props.shortcutConfig);
		});
		const computeShortcutList = computed(() => {
			const { options } = computeShortcutOpts.value;
			if (options) return options.map((option, index) => {
				return Object.assign({ name: `${option.name || option.code || index}` }, option);
			});
			return [];
		});
		const computeDateLabelFormat = computed(() => {
			const { labelFormat } = props;
			return labelFormat || getI18n(`vxe.input.date.labelFormat.${props.type}`);
		});
		const computeDateValueFormat = computed(() => {
			const { type, valueFormat } = props;
			return handleValueFormat(type, valueFormat);
		});
		const computeFirstDayOfWeek = computed(() => {
			const { startDay } = props;
			return import_xe_utils.default.toNumber(startDay);
		});
		const computePanelLabel = computed(() => {
			const { type, multiple } = props;
			const { inputValue } = reactData;
			const dateLabelFormat = computeDateLabelFormat.value;
			const dateValueFormat = computeDateValueFormat.value;
			const firstDayOfWeek = computeFirstDayOfWeek.value;
			return (inputValue ? multiple ? inputValue.split(",") : [inputValue] : []).map((val) => {
				return parseDateObj(val, type, {
					valueFormat: dateValueFormat,
					labelFormat: dateLabelFormat,
					firstDay: firstDayOfWeek
				}).label;
			}).join(", ");
		});
		const updateModelValue = () => {
			const { modelValue } = props;
			let val = "";
			if (modelValue) if (import_xe_utils.default.isNumber(modelValue) && /^[0-9]{11,15}$/.test(`${modelValue}`)) val = new Date(modelValue);
			else val = modelValue;
			reactData.inputValue = val;
		};
		const triggerEvent = (evnt) => {
			const { inputValue } = reactData;
			dispatchEvent(evnt.type, { value: inputValue }, evnt);
		};
		const handleChange = (value, evnt) => {
			const { type, modelValue, valueFormat } = props;
			const dateValueFormat = computeDateValueFormat.value;
			reactData.inputValue = value;
			if (hasTimestampValueType(valueFormat)) {
				const dateVal = parseDateValue(value, type, { valueFormat: dateValueFormat });
				const timeNum = dateVal ? dateVal.getTime() : null;
				emit("update:modelValue", timeNum);
				if (modelValue !== timeNum) {
					dispatchEvent("change", { value: timeNum }, evnt);
					if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, timeNum);
				}
			} else if (hasDateValueType(valueFormat)) {
				const dateVal = parseDateValue(value, type, { valueFormat: dateValueFormat });
				emit("update:modelValue", dateVal);
				if (modelValue && dateVal ? import_xe_utils.default.toStringDate(modelValue).getTime() !== dateVal.getTime() : modelValue !== dateVal) {
					dispatchEvent("change", { value: dateVal }, evnt);
					if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, dateVal);
				}
			} else {
				emit("update:modelValue", value);
				if (import_xe_utils.default.toValueString(modelValue) !== value) {
					dispatchEvent("change", { value }, evnt);
					if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, value);
				}
			}
		};
		const inputEvent = (evnt) => {
			const value = evnt.target.value;
			reactData.inputLabel = value;
			dispatchEvent("input", { value }, evnt);
		};
		const changeEvent = (evnt) => {
			if (!computeInpImmediate.value) triggerEvent(evnt);
		};
		const focusEvent = (evnt) => {
			const { trigger } = computePopupOpts.value;
			reactData.isActivated = true;
			if (!trigger || trigger === "default") datePickerOpenEvent(evnt);
			triggerEvent(evnt);
		};
		const clickPrefixEvent = (evnt) => {
			if (!computeIsDisabled.value) {
				const { inputValue } = reactData;
				dispatchEvent("prefix-click", { value: inputValue }, evnt);
			}
		};
		const hidePanel = () => {
			return new Promise((resolve) => {
				reactData.visiblePanel = false;
				internalData.hpTimeout = setTimeout(() => {
					reactData.isAniVisible = false;
					resolve();
				}, 350);
			});
		};
		const clearValueEvent = (evnt, value) => {
			if (computeIsDatePickerType.value) hidePanel();
			handleChange("", evnt);
			dispatchEvent("clear", { value }, evnt);
		};
		const clickSuffixEvent = (evnt) => {
			if (!computeIsDisabled.value) {
				const { inputValue } = reactData;
				dispatchEvent("suffix-click", { value: inputValue }, evnt);
			}
		};
		const blurEvent = (evnt) => {
			const $datePanel = refDatePanel.value;
			const { inputValue } = reactData;
			const inpImmediate = computeInpImmediate.value;
			const value = inputValue;
			if (!inpImmediate) handleChange(value, evnt);
			if (!reactData.visiblePanel) {
				reactData.isActivated = false;
				if ($datePanel) $datePanel.checkValue(reactData.inputLabel);
			}
			dispatchEvent("blur", { value }, evnt);
			if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, value);
		};
		const keydownEvent = (evnt) => {
			triggerEvent(evnt);
		};
		const keyupEvent = (evnt) => {
			triggerEvent(evnt);
		};
		const confirmEvent = (evnt) => {
			const $datePanel = refDatePanel.value;
			if ($datePanel) $datePanel.confirmByEvent(evnt);
			hidePanel();
		};
		const panelChangeEvent = (params) => {
			const { multiple, autoClose } = props;
			const { value, $event } = params;
			const isDateTimeType = computeIsDateTimeType.value;
			handleChange(value, $event);
			if (!multiple && !isDateTimeType) {
				if (autoClose) hidePanel();
			}
		};
		const panelConfirmEvent = (params) => {
			dispatchEvent("confirm", params, params.$event);
		};
		const handleGlobalMousedownEvent = (evnt) => {
			const $datePanel = refDatePanel.value;
			const { visiblePanel, isActivated } = reactData;
			const el = refElem.value;
			const panelWrapperElem = refPanelWrapper.value;
			if (!computeIsDisabled.value && isActivated) {
				reactData.isActivated = getEventTargetNode(evnt, el).flag || getEventTargetNode(evnt, panelWrapperElem).flag;
				if (!reactData.isActivated) {
					if (visiblePanel) {
						hidePanel();
						if ($datePanel) $datePanel.checkValue(reactData.inputLabel);
					}
				}
			}
		};
		const handleGlobalKeydownEvent = (evnt) => {
			const { visiblePanel } = reactData;
			if (!computeIsDisabled.value) {
				const isTab = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.TAB);
				const isEsc = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.ESCAPE);
				if (isTab) reactData.isActivated = false;
				if (visiblePanel) {
					if (isEsc || isTab) hidePanel();
				}
			}
		};
		const handleGlobalMousewheelEvent = (evnt) => {
			const { visiblePanel } = reactData;
			if (!computeIsDisabled.value) {
				if (visiblePanel) {
					const panelWrapperElem = refPanelWrapper.value;
					if (getEventTargetNode(evnt, panelWrapperElem).flag) updatePlacement();
					else hidePanel();
				}
			}
		};
		const handleGlobalBlurEvent = () => {
			const $datePanel = refDatePanel.value;
			const { isActivated, visiblePanel } = reactData;
			if (visiblePanel) hidePanel();
			if (isActivated) reactData.isActivated = false;
			if (visiblePanel || isActivated) {
				if ($datePanel) $datePanel.checkValue(reactData.inputLabel);
				const targetElem = refInputTarget.value;
				if (targetElem) targetElem.blur();
			}
		};
		const handleGlobalResizeEvent = () => {
			const { visiblePanel } = reactData;
			if (visiblePanel) updatePlacement();
		};
		const updateZindex = () => {
			const customZIndex = computePopupOpts.value.zIndex || props.zIndex;
			if (customZIndex) reactData.panelIndex = import_xe_utils.default.toNumber(customZIndex);
			else if (reactData.panelIndex < getLastZIndex()) reactData.panelIndex = nextZIndex();
		};
		const updatePlacement = () => {
			const { placement } = props;
			const { panelIndex } = reactData;
			const targetElem = refInputTarget.value;
			const panelElem = refInputPanel.value;
			const btnTransfer = computeBtnTransfer.value;
			const popupOpts = computePopupOpts.value;
			const handleStyle = () => {
				const ppObj = updatePanelPlacement(targetElem, panelElem, {
					placement: popupOpts.placement || placement,
					defaultPlacement: popupOpts.defaultPlacement,
					teleportTo: btnTransfer
				});
				reactData.panelStyle = Object.assign(ppObj.style, { zIndex: panelIndex });
				reactData.panelPlacement = ppObj.placement;
			};
			handleStyle();
			return nextTick().then(handleStyle);
		};
		const showPanel = () => {
			const { visiblePanel } = reactData;
			if (!computeIsDisabled.value && !visiblePanel) {
				if (!reactData.initialized) reactData.initialized = true;
				if (internalData.hpTimeout) {
					clearTimeout(internalData.hpTimeout);
					internalData.hpTimeout = void 0;
				}
				reactData.isActivated = true;
				reactData.isAniVisible = true;
				setTimeout(() => {
					reactData.visiblePanel = true;
					updatePlacement();
				}, 10);
				updateZindex();
				return updatePlacement();
			}
			return nextTick();
		};
		const datePickerOpenEvent = (evnt) => {
			if (!computeFormReadonly.value) {
				evnt.preventDefault();
				showPanel();
			}
		};
		const clickIconEvent = (evnt) => {
			const { trigger } = computePopupOpts.value;
			if (!trigger || trigger === "default" || trigger === "icon") datePickerOpenEvent(evnt);
		};
		const clickEvent = (evnt) => {
			triggerEvent(evnt);
		};
		const handleShortcutEvent = ({ option, $event }) => {
			const { type } = props;
			const { inputValue } = reactData;
			const shortcutOpts = computeShortcutOpts.value;
			const { autoClose } = shortcutOpts;
			const { code, clickMethod } = option;
			let value = inputValue;
			const shortcutParams = {
				$datePicker: $xeDatePicker,
				option,
				value,
				code
			};
			if (!clickMethod && code) {
				const gCommandOpts = commands.get(code);
				const dpCommandMethod = gCommandOpts ? gCommandOpts.datePickerCommandMethod : null;
				if (dpCommandMethod) dpCommandMethod(shortcutParams);
				else {
					const dateValueFormat = computeDateValueFormat.value;
					const firstDayOfWeek = computeFirstDayOfWeek.value;
					switch (code) {
						case "now":
						case "prev":
						case "next":
						case "minus":
						case "plus":
							value = getDateByCode(code, value, type, {
								valueFormat: dateValueFormat,
								firstDay: firstDayOfWeek
							}).value;
							shortcutParams.value = value;
							handleChange(value, $event);
							break;
						default:
							errLog("vxe.error.notCommands", [`[date-picker] ${code}`]);
							break;
					}
				}
			} else {
				const optClickMethod = clickMethod || shortcutOpts.clickMethod;
				if (optClickMethod) optClickMethod(shortcutParams);
			}
			if (autoClose) hidePanel();
			dispatchEvent("shortcut-click", shortcutParams, $event);
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $datePicker: $xeDatePicker }, params));
		};
		Object.assign($xeDatePicker, {
			dispatchEvent,
			setModelValue(value) {
				reactData.inputValue = value;
				emit("update:modelValue", value);
			},
			setModelValueByEvent(evnt, value) {
				handleChange(value || "", evnt);
			},
			focus() {
				const inputElem = refInputTarget.value;
				reactData.isActivated = true;
				inputElem.focus();
				return nextTick();
			},
			blur() {
				refInputTarget.value.blur();
				reactData.isActivated = false;
				return nextTick();
			},
			select() {
				refInputTarget.value.select();
				reactData.isActivated = false;
				return nextTick();
			},
			showPanel,
			hidePanel,
			updatePlacement
		});
		const renderShortcutBtn = (pos, isVertical) => {
			const shortcutOpts = computeShortcutOpts.value;
			const { position, align, mode } = shortcutOpts;
			const shortcutList = computeShortcutList.value;
			if (isEnableConf(shortcutOpts) && shortcutList.length && (position || "left") === pos) return h("div", { class: `vxe-date-picker--layout-${pos}-wrapper` }, [h(button_group_default, {
				options: shortcutList,
				mode,
				align,
				vertical: isVertical,
				onClick: handleShortcutEvent
			})]);
			return renderEmptyElement($xeDatePicker);
		};
		const renderPanel = () => {
			const { type, multiple, showClearButton, showConfirmButton } = props;
			const { initialized, isAniVisible, visiblePanel, panelPlacement, panelStyle, inputValue } = reactData;
			const vSize = computeSize.value;
			const btnTransfer = computeBtnTransfer.value;
			const shortcutOpts = computeShortcutOpts.value;
			const isClearable = computeIsClearable.value;
			const isDateTimeType = computeIsDateTimeType.value;
			const shortcutList = computeShortcutList.value;
			const timeOpts = computeTimeOpts.value;
			const popupOpts = computePopupOpts.value;
			const { position } = shortcutOpts;
			const headerSlot = slots.header;
			const footerSlot = slots.footer;
			const topSlot = slots.top;
			const bottomSlot = slots.bottom;
			const leftSlot = slots.left;
			const rightSlot = slots.right;
			const ppClassName = popupOpts.className;
			const hasShortcutBtn = shortcutList.length > 0;
			const showConfirmBtn = showConfirmButton === null ? isDateTimeType || multiple : showConfirmButton;
			const showClearBtn = showClearButton === null ? isClearable && showConfirmBtn && type !== "time" : showClearButton;
			return h(Teleport, {
				to: "body",
				disabled: btnTransfer ? !initialized : true
			}, [h("div", {
				ref: refInputPanel,
				class: [
					"vxe-table--ignore-clear vxe-date-picker--panel",
					`type--${type}`,
					ppClassName ? import_xe_utils.default.isFunction(ppClassName) ? ppClassName({ $datePicker: $xeDatePicker }) : ppClassName : "",
					{
						[`size--${vSize}`]: vSize,
						"is--transfer": btnTransfer,
						"ani--leave": isAniVisible,
						"ani--enter": visiblePanel,
						"show--top": !!(topSlot || headerSlot || hasShortcutBtn && (position === "top" || position === "header")),
						"show--bottom": !!(bottomSlot || footerSlot || hasShortcutBtn && (position === "bottom" || position === "footer")),
						"show--left": !!(leftSlot || hasShortcutBtn && position === "left"),
						"show--right": !!(rightSlot || hasShortcutBtn && position === "right")
					}
				],
				placement: panelPlacement,
				style: panelStyle
			}, initialized && (visiblePanel || isAniVisible) ? [h("div", {
				ref: refPanelWrapper,
				class: [
					"vxe-date-picker--layout-all-wrapper",
					`type--${type}`,
					{ [`size--${vSize}`]: vSize }
				]
			}, [
				topSlot ? h("div", { class: "vxe-date-picker--layout-top-wrapper" }, topSlot({})) : renderShortcutBtn("top"),
				h("div", { class: "vxe-date-picker--layout-body-layout-wrapper" }, [
					leftSlot ? h("div", { class: "vxe-date-picker--layout-left-wrapper" }, leftSlot({})) : renderShortcutBtn("left", true),
					h("div", { class: "vxe-date-picker--layout-body-content-wrapper" }, [
						headerSlot ? h("div", { class: "vxe-date-picker--layout-header-wrapper" }, headerSlot({})) : renderShortcutBtn("header"),
						h("div", { class: "vxe-date-picker--layout-body-wrapper" }, [h(date_panel_default, {
							ref: refDatePanel,
							modelValue: reactData.inputValue,
							type: props.type,
							className: props.className,
							multiple: props.multiple,
							limitCount: props.limitCount,
							startDate: props.startDate,
							endDate: props.endDate,
							defaultDate: props.defaultDate,
							defaultTime: props.defaultTime,
							minDate: props.minDate,
							maxDate: props.maxDate,
							startDay: props.startDay,
							labelFormat: props.labelFormat,
							valueFormat: props.valueFormat,
							timeFormat: props.timeFormat,
							timeConfig: timeOpts,
							festivalMethod: props.festivalMethod,
							disabledMethod: props.disabledMethod,
							selectDay: props.selectDay,
							onChange: panelChangeEvent,
							onConfirm: panelConfirmEvent
						})]),
						h("div", { class: "vxe-date-picker--layout-footer-wrapper" }, [h("div", { class: "vxe-date-picker--layout-footer-custom" }, footerSlot ? footerSlot({}) : [renderShortcutBtn("footer")]), showClearBtn || showConfirmBtn ? h("div", { class: "vxe-date-picker--layout-footer-btns" }, [showClearBtn ? h(VxeButtonComponent, {
							size: "mini",
							disabled: inputValue === "" || import_xe_utils.default.eqNull(inputValue),
							content: getI18n("vxe.button.clear"),
							onClick: clearValueEvent
						}) : renderEmptyElement($xeDatePicker), showConfirmBtn ? h(VxeButtonComponent, {
							size: "mini",
							status: "primary",
							content: getI18n("vxe.button.confirm"),
							onClick: confirmEvent
						}) : renderEmptyElement($xeDatePicker)]) : renderEmptyElement($xeDatePicker)])
					]),
					rightSlot ? h("div", { class: "vxe-date-picker--layout-right-wrapper" }, rightSlot({})) : renderShortcutBtn("right", true)
				]),
				bottomSlot ? h("div", { class: "vxe-date-picker--layout-bottom-wrapper" }, bottomSlot({})) : renderShortcutBtn("bottom")
			])] : [])]);
		};
		const renderPrefixIcon = () => {
			const { prefixIcon } = props;
			const prefixSlot = slots.prefix;
			return prefixSlot || prefixIcon ? h("div", {
				class: "vxe-date-picker--prefix",
				onClick: clickPrefixEvent
			}, [h("div", { class: "vxe-date-picker--prefix-icon" }, prefixSlot ? getSlotVNs$1(prefixSlot({})) : [h("i", { class: prefixIcon })])]) : null;
		};
		const renderSuffixIcon = () => {
			const { suffixIcon } = props;
			const { inputValue } = reactData;
			const suffixSlot = slots.suffix;
			const isDisabled = computeIsDisabled.value;
			const isClearable = computeIsClearable.value;
			return h("div", { class: ["vxe-date-picker--suffix", { "is--clear": isClearable && !isDisabled && !(inputValue === "" || import_xe_utils.default.eqNull(inputValue)) }] }, [
				isClearable ? h("div", {
					class: "vxe-date-picker--clear-icon",
					onClick: clearValueEvent
				}, [h("i", { class: getIcon().INPUT_CLEAR })]) : renderEmptyElement($xeDatePicker),
				renderExtraSuffixIcon(),
				suffixSlot || suffixIcon ? h("div", {
					class: "vxe-date-picker--suffix-icon",
					onClick: clickSuffixEvent
				}, suffixSlot ? getSlotVNs$1(suffixSlot({})) : [h("i", { class: suffixIcon })]) : renderEmptyElement($xeDatePicker)
			]);
		};
		const renderExtraSuffixIcon = () => {
			return h("div", {
				class: "vxe-date-picker--control-icon",
				onClick: clickIconEvent
			}, [h("i", { class: ["vxe-date-picker--date-picker-icon", getIcon().DATE_PICKER_DATE] })]);
		};
		const renderVN = () => {
			const { className, type, name, autoComplete } = props;
			const { inputValue, inputLabel, visiblePanel, isActivated } = reactData;
			const vSize = computeSize.value;
			const isDisabled = computeIsDisabled.value;
			const formReadonly = computeFormReadonly.value;
			const panelLabel = computePanelLabel.value;
			if (formReadonly) return h("div", {
				ref: refElem,
				class: [
					"vxe-date-picker--readonly",
					`type--${type}`,
					className
				]
			}, panelLabel);
			const inputReadonly = computeInputReadonly.value;
			const inpPlaceholder = computeInpPlaceholder.value;
			const isClearable = computeIsClearable.value;
			const prefix = renderPrefixIcon();
			const suffix = renderSuffixIcon();
			return h("div", {
				ref: refElem,
				class: [
					"vxe-date-picker",
					`type--${type}`,
					className,
					{
						[`size--${vSize}`]: vSize,
						"is--prefix": !!prefix,
						"is--suffix": !!suffix,
						"is--visible": visiblePanel,
						"is--disabled": isDisabled,
						"is--active": isActivated,
						"show--clear": isClearable && !isDisabled && !(inputValue === "" || import_xe_utils.default.eqNull(inputValue))
					}
				],
				spellcheck: false
			}, [
				prefix || renderEmptyElement($xeDatePicker),
				h("div", { class: "vxe-date-picker--wrapper" }, [h("input", {
					ref: refInputTarget,
					class: "vxe-date-picker--inner",
					value: inputLabel,
					name,
					type: "text",
					placeholder: inpPlaceholder,
					readonly: inputReadonly,
					disabled: isDisabled,
					autocomplete: autoComplete,
					onKeydown: keydownEvent,
					onKeyup: keyupEvent,
					onClick: clickEvent,
					onInput: inputEvent,
					onChange: changeEvent,
					onFocus: focusEvent,
					onBlur: blurEvent
				})]),
				suffix || renderEmptyElement($xeDatePicker),
				renderPanel()
			]);
		};
		watch(computePanelLabel, (val) => {
			reactData.inputLabel = val;
		});
		watch(() => props.modelValue, () => {
			updateModelValue();
		});
		nextTick(() => {
			globalEvents.on($xeDatePicker, "mousewheel", handleGlobalMousewheelEvent);
			globalEvents.on($xeDatePicker, "mousedown", handleGlobalMousedownEvent);
			globalEvents.on($xeDatePicker, "keydown", handleGlobalKeydownEvent);
			globalEvents.on($xeDatePicker, "blur", handleGlobalBlurEvent);
			globalEvents.on($xeDatePicker, "resize", handleGlobalResizeEvent);
		});
		onUnmounted(() => {
			globalEvents.off($xeDatePicker, "mousewheel");
			globalEvents.off($xeDatePicker, "mousedown");
			globalEvents.off($xeDatePicker, "blur");
			globalEvents.off($xeDatePicker, "resize");
		});
		updateModelValue();
		provide("$xeDatePicker", $xeDatePicker);
		$xeDatePicker.renderVN = renderVN;
		return $xeDatePicker;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/date-picker/index.js
var VxeDatePicker = Object.assign({}, date_picker_default, { install(app) {
	app.component(date_picker_default.name, date_picker_default);
} });
dynamicApp.use(VxeDatePicker);
VxeUI.component(date_picker_default);
var DatePicker = VxeDatePicker;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/date-range-picker/src/date-range-picker.js
var date_range_picker_default = defineVxeComponent({
	name: "VxeDateRangePicker",
	props: {
		modelValue: [
			String,
			Number,
			Date,
			Array
		],
		startValue: [
			String,
			Number,
			Date
		],
		endValue: [
			String,
			Number,
			Date
		],
		immediate: {
			type: Boolean,
			default: true
		},
		name: String,
		type: {
			type: String,
			default: "date"
		},
		clearable: {
			type: Boolean,
			default: () => getConfig().dateRangePicker.clearable
		},
		readonly: {
			type: Boolean,
			default: null
		},
		disabled: {
			type: Boolean,
			default: null
		},
		placeholder: String,
		autoComplete: {
			type: String,
			default: "off"
		},
		form: String,
		className: String,
		zIndex: Number,
		size: {
			type: String,
			default: () => getConfig().dateRangePicker.size || getConfig().size
		},
		minDate: [
			String,
			Number,
			Date
		],
		maxDate: [
			String,
			Number,
			Date
		],
		defaultDate: [
			String,
			Number,
			Date,
			Array
		],
		defaultTime: [
			String,
			Number,
			Date,
			Array
		],
		startDay: {
			type: [String, Number],
			default: () => getConfig().dateRangePicker.startDay
		},
		labelFormat: String,
		valueFormat: String,
		timeFormat: String,
		valueType: String,
		editable: {
			type: Boolean,
			default: true
		},
		festivalMethod: {
			type: Function,
			default: () => getConfig().dateRangePicker.festivalMethod
		},
		disabledMethod: {
			type: Function,
			default: () => getConfig().dateRangePicker.disabledMethod
		},
		separator: {
			type: [String, Number],
			default: () => getConfig().dateRangePicker.separator
		},
		selectDay: {
			type: [String, Number],
			default: () => getConfig().dateRangePicker.selectDay
		},
		showClearButton: {
			type: Boolean,
			default: () => getConfig().dateRangePicker.showClearButton
		},
		showConfirmButton: {
			type: Boolean,
			default: () => getConfig().dateRangePicker.showConfirmButton
		},
		autoClose: {
			type: Boolean,
			default: () => getConfig().dateRangePicker.autoClose
		},
		prefixIcon: String,
		suffixIcon: String,
		placement: String,
		transfer: {
			type: Boolean,
			default: null
		},
		timeConfig: Object,
		popupConfig: Object,
		shortcutConfig: Object
	},
	emits: [
		"update:modelValue",
		"update:startValue",
		"update:endValue",
		"input",
		"change",
		"keydown",
		"keyup",
		"click",
		"focus",
		"blur",
		"clear",
		"confirm",
		"prefix-click",
		"suffix-click",
		"date-prev",
		"date-today",
		"date-next",
		"shortcut-click"
	],
	setup(props, context) {
		const { slots, emit } = context;
		const $xeModal = inject("$xeModal", null);
		const $xeDrawer = inject("$xeDrawer", null);
		const $xeTable = inject("$xeTable", null);
		const $xeForm = inject("$xeForm", null);
		const formItemInfo = inject("xeFormItemInfo", null);
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const reactData = reactive({
			initialized: false,
			panelIndex: 0,
			visiblePanel: false,
			isAniVisible: false,
			panelStyle: {},
			panelPlacement: "",
			isActivated: false,
			startValue: "",
			endValue: ""
		});
		const internalData = {};
		const refElem = ref();
		const refInputTarget = ref();
		const refInputPanel = ref();
		const refPanelWrapper = ref();
		const refStartDatePanel = ref();
		const refEndDatePanel = ref();
		const refMaps = {
			refElem,
			refInput: refInputTarget
		};
		const $xeDateRangePicker = {
			xID,
			props,
			context,
			reactData,
			internalData,
			getRefMaps: () => refMaps
		};
		let dateRangePickerMethods = {};
		const computeBtnTransfer = computed(() => {
			const { transfer } = props;
			const popupOpts = computePopupOpts.value;
			if (import_xe_utils.default.isBoolean(popupOpts.transfer)) return popupOpts.transfer;
			if (transfer === null) {
				const globalTransfer = getConfig().dateRangePicker.transfer;
				if (import_xe_utils.default.isBoolean(globalTransfer)) return globalTransfer;
				if ($xeTable || $xeModal || $xeDrawer || $xeForm) return true;
			}
			return transfer;
		});
		const computeFormReadonly = computed(() => {
			const { readonly } = props;
			if (readonly === null) {
				if ($xeForm) return $xeForm.props.readonly;
				return false;
			}
			return readonly;
		});
		const computeIsDisabled = computed(() => {
			const { disabled } = props;
			if (disabled === null) {
				if ($xeForm) return $xeForm.props.disabled;
				return false;
			}
			return disabled;
		});
		const computeDefaultDates = computed(() => {
			const { defaultDate } = props;
			if (defaultDate) {
				if (import_xe_utils.default.isArray(defaultDate)) return defaultDate;
				if (`${defaultDate}`.indexOf(",") > -1) return `${defaultDate}`.split(",");
				return [defaultDate, defaultDate];
			}
			return [];
		});
		const computeDefaultTimes = computed(() => {
			const { defaultTime } = props;
			if (defaultTime) {
				if (import_xe_utils.default.isArray(defaultTime)) return defaultTime;
				if (`${defaultTime}`.indexOf(",") > -1) return `${defaultTime}`.split(",");
				return [defaultTime, defaultTime];
			}
			return [];
		});
		const computeMVal = computed(() => {
			const { startValue, endValue } = props;
			return `${startValue || ""}${endValue || ""}`;
		});
		const computeIsDateTimeType = computed(() => {
			const { type } = props;
			return type === "time" || type === "datetime";
		});
		const computeIsDatePickerType = computed(() => {
			return [
				"date",
				"week",
				"month",
				"quarter",
				"year"
			].indexOf(props.type) > -1;
		});
		const computeIsClearable = computed(() => {
			return props.clearable;
		});
		const computeInpPlaceholder = computed(() => {
			const { placeholder } = props;
			if (placeholder) return getFuncText(placeholder);
			const globalPlaceholder = getConfig().dateRangePicker.placeholder;
			if (globalPlaceholder) return getFuncText(globalPlaceholder);
			return getI18n("vxe.dateRangePicker.pleaseRange");
		});
		const computeInpImmediate = computed(() => {
			const { immediate } = props;
			return immediate;
		});
		const computeTimeOpts = computed(() => {
			return Object.assign({}, getConfig().dateRangePicker.timeConfig, props.timeConfig);
		});
		const computePopupOpts = computed(() => {
			return Object.assign({}, getConfig().dateRangePicker.popupConfig, props.popupConfig);
		});
		const computeShortcutOpts = computed(() => {
			return Object.assign({}, getConfig().dateRangePicker.shortcutConfig, props.shortcutConfig);
		});
		const computeShortcutList = computed(() => {
			const { options } = computeShortcutOpts.value;
			if (options) return options.map((option, index) => {
				return Object.assign({ name: `${option.name || option.code || index}` }, option);
			});
			return [];
		});
		const computeDateLabelFormat = computed(() => {
			const { labelFormat } = props;
			return labelFormat || getI18n(`vxe.input.date.labelFormat.${props.type}`);
		});
		const computeDateValueFormat = computed(() => {
			const { type, valueFormat } = props;
			return handleValueFormat(type, valueFormat);
		});
		const computeFirstDayOfWeek = computed(() => {
			const { startDay } = props;
			return import_xe_utils.default.toNumber(startDay);
		});
		const computePanelLabelObj = computed(() => {
			const { startValue, endValue } = reactData;
			return formatRangeLabel(startValue || endValue ? [startValue || "", endValue || ""] : []);
		});
		const computeInputLabel = computed(() => {
			return computePanelLabelObj.value.label;
		});
		const formatRangeLabel = (vals) => {
			const { type, separator } = props;
			const dateLabelFormat = computeDateLabelFormat.value;
			const dateValueFormat = computeDateValueFormat.value;
			const firstDayOfWeek = computeFirstDayOfWeek.value;
			const startRest = vals[0] ? parseDateObj(vals[0], type, {
				valueFormat: dateValueFormat,
				labelFormat: dateLabelFormat,
				firstDay: firstDayOfWeek
			}) : null;
			const endRest = vals[1] ? parseDateObj(vals[1], type, {
				valueFormat: dateValueFormat,
				labelFormat: dateLabelFormat,
				firstDay: firstDayOfWeek
			}) : null;
			const startLabel = startRest ? startRest.label : "";
			const endLabel = endRest ? endRest.label : "";
			return {
				label: (startLabel || endLabel ? [startLabel, endLabel] : []).join(`${separator || " ~ "}`),
				startLabel,
				endLabel
			};
		};
		const getRangeValue = (sValue, eValue) => {
			const { modelValue, valueType } = props;
			let isArr = import_xe_utils.default.isArray(modelValue);
			if (valueType) switch (valueType) {
				case "array":
					isArr = true;
					break;
				case "string":
					isArr = false;
					break;
			}
			if (sValue || eValue) {
				const rest = [sValue || "", eValue || ""];
				if (isArr) return rest;
				return rest.join(",");
			}
			return isArr ? [] : "";
		};
		const paraeUpdateModel = () => {
			const { type, modelValue } = props;
			const dateValueFormat = computeDateValueFormat.value;
			let sValue = "";
			let eValue = "";
			if (import_xe_utils.default.isArray(modelValue)) {
				const date1 = parseDateString(modelValue[0], type, { valueFormat: dateValueFormat });
				const date2 = parseDateString(modelValue[1], type, { valueFormat: dateValueFormat });
				if (date1 || date2) {
					sValue = date1 || "";
					eValue = date2 || "";
				}
			} else if (import_xe_utils.default.isString(modelValue)) {
				const strArr = modelValue.split(",");
				if (strArr[0] || strArr[1]) {
					sValue = strArr[0] || "";
					eValue = strArr[1] || "";
				}
			}
			return {
				sValue,
				eValue
			};
		};
		const parseUpdateData = () => {
			const { type, startValue, endValue } = props;
			const dateValueFormat = computeDateValueFormat.value;
			let sValue = "";
			let eValue = "";
			sValue = parseDateString(startValue, type, { valueFormat: dateValueFormat });
			eValue = parseDateString(endValue, type, { valueFormat: dateValueFormat });
			return {
				sValue,
				eValue
			};
		};
		const updateModelValue = (isModel) => {
			const { modelValue, startValue, endValue } = props;
			let restObj = {
				sValue: "",
				eValue: ""
			};
			if (isModel) if (modelValue) restObj = paraeUpdateModel();
			else restObj = parseUpdateData();
			else if (startValue || endValue) restObj = parseUpdateData();
			else restObj = paraeUpdateModel();
			reactData.startValue = restObj.sValue;
			reactData.endValue = restObj.eValue;
		};
		const triggerEvent = (evnt) => {
			const { startValue, endValue } = reactData;
			const value = getRangeValue(startValue, endValue);
			dispatchEvent(evnt.type, {
				value,
				startValue,
				endValue
			}, evnt);
		};
		const handleChange = (sValue, eValue, evnt) => {
			const { modelValue } = props;
			reactData.startValue = sValue;
			reactData.endValue = eValue;
			const value = getRangeValue(sValue, eValue);
			const isFinish = sValue && eValue || !sValue && !eValue;
			emit("update:modelValue", value);
			emit("update:startValue", sValue || "");
			emit("update:endValue", eValue || "");
			if (import_xe_utils.default.toValueString(modelValue) !== value) {
				dispatchEvent("change", {
					value,
					startValue: sValue,
					endValue: eValue,
					isFinish
				}, evnt);
				if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, value);
			}
		};
		const changeEvent = (evnt) => {
			if (!computeInpImmediate.value) triggerEvent(evnt);
		};
		const focusEvent = (evnt) => {
			const { trigger } = computePopupOpts.value;
			reactData.isActivated = true;
			if (!trigger || trigger === "default") dateRangePickerOpenEvent(evnt);
			triggerEvent(evnt);
		};
		const clickPrefixEvent = (evnt) => {
			if (!computeIsDisabled.value) {
				const { startValue, endValue } = reactData;
				dispatchEvent("prefix-click", {
					value: getRangeValue(startValue, endValue),
					startValue,
					endValue
				}, evnt);
			}
		};
		const hidePanel = () => {
			return new Promise((resolve) => {
				reactData.visiblePanel = false;
				internalData.hpTimeout = setTimeout(() => {
					reactData.isAniVisible = false;
					resolve();
				}, 350);
			});
		};
		const clearValueEvent = (evnt, value) => {
			if (computeIsDatePickerType.value) hidePanel();
			const startValue = "";
			const endValue = "";
			handleChange(startValue, endValue, evnt);
			dispatchEvent("clear", {
				value,
				startValue,
				endValue
			}, evnt);
		};
		const checkValue = () => {
			const $startDatePanel = refStartDatePanel.value;
			const $endDatePanel = refEndDatePanel.value;
			if ($startDatePanel && $endDatePanel) {
				const startValue = $startDatePanel.getModelValue();
				const endValue = $endDatePanel.getModelValue();
				if (!startValue || !endValue) handleChange("", "", { type: "check" });
			}
		};
		const handleSelectClose = () => {
			const { autoClose } = props;
			const { startValue, endValue } = reactData;
			const { selectStatus } = internalData;
			const isDatePickerType = computeIsDatePickerType.value;
			if (autoClose) {
				if (selectStatus && isDatePickerType) {
					if (startValue && endValue) hidePanel();
				}
			} else if (startValue && endValue) internalData.selectStatus = false;
		};
		const clickSuffixEvent = (evnt) => {
			if (!computeIsDisabled.value) {
				const { startValue, endValue } = reactData;
				dispatchEvent("suffix-click", {
					value: getRangeValue(startValue, endValue),
					startValue,
					endValue
				}, evnt);
			}
		};
		const blurEvent = (evnt) => {
			const { startValue, endValue } = reactData;
			const inpImmediate = computeInpImmediate.value;
			const value = "";
			if (!inpImmediate) handleChange(startValue, endValue, evnt);
			if (!reactData.visiblePanel) reactData.isActivated = false;
			dispatchEvent("blur", {
				value,
				startValue,
				endValue
			}, evnt);
			if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, value);
		};
		const keydownEvent = (evnt) => {
			triggerEvent(evnt);
		};
		const keyupEvent = (evnt) => {
			triggerEvent(evnt);
		};
		const confirmEvent = (evnt) => {
			const $startDatePanel = refStartDatePanel.value;
			const $endDatePanel = refEndDatePanel.value;
			if ($startDatePanel && $endDatePanel) {
				const startValue = $startDatePanel.getModelValue();
				const endValue = $endDatePanel.getModelValue();
				if (startValue && !endValue || !startValue && endValue) handleChange("", "", evnt);
				else {
					$startDatePanel.confirmByEvent(evnt);
					$endDatePanel.confirmByEvent(evnt);
				}
				dispatchEvent("confirm", {
					value: getRangeValue(startValue, endValue),
					startValue,
					endValue
				}, evnt);
			}
			hidePanel();
		};
		const startPanelChangeEvent = (params) => {
			const { selectStatus } = internalData;
			const { value, $event } = params;
			const endValue = selectStatus ? reactData.endValue : "";
			handleChange(value, endValue, $event);
			handleSelectClose();
			if (!selectStatus) internalData.selectStatus = true;
			nextTick(() => {
				const $startDatePanel = refStartDatePanel.value;
				const $endDatePanel = refEndDatePanel.value;
				if ($startDatePanel && $endDatePanel) {
					const startValue = $startDatePanel.getModelValue();
					if (!endValue && startValue) $endDatePanel.setPanelDate(import_xe_utils.default.toStringDate(startValue));
				}
			});
		};
		const endPanelChangeEvent = (params) => {
			const { selectStatus } = internalData;
			const { value, $event } = params;
			const startValue = selectStatus ? reactData.startValue : "";
			handleChange(startValue, value, $event);
			handleSelectClose();
			if (!selectStatus) internalData.selectStatus = true;
			nextTick(() => {
				const $startDatePanel = refStartDatePanel.value;
				const $endDatePanel = refEndDatePanel.value;
				if ($startDatePanel && $endDatePanel) {
					const endValue = $endDatePanel.getModelValue();
					if (!startValue && endValue) $startDatePanel.setPanelDate(import_xe_utils.default.toStringDate(endValue));
				}
			});
		};
		const handleGlobalMousedownEvent = (evnt) => {
			const { visiblePanel, isActivated } = reactData;
			const el = refElem.value;
			const panelWrapperElem = refPanelWrapper.value;
			if (!computeIsDisabled.value && isActivated) {
				reactData.isActivated = getEventTargetNode(evnt, el).flag || getEventTargetNode(evnt, panelWrapperElem).flag;
				if (!reactData.isActivated) {
					if (visiblePanel) {
						checkValue();
						hidePanel();
					}
				}
			}
		};
		const handleGlobalKeydownEvent = (evnt) => {
			const { visiblePanel } = reactData;
			if (!computeIsDisabled.value) {
				const isTab = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.TAB);
				const isEsc = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.ESCAPE);
				if (isTab) reactData.isActivated = false;
				if (visiblePanel) {
					if (isEsc || isTab) {
						checkValue();
						hidePanel();
					}
				}
			}
		};
		const handleGlobalMousewheelEvent = (evnt) => {
			const { visiblePanel } = reactData;
			if (!computeIsDisabled.value) {
				if (visiblePanel) {
					const panelWrapperElem = refPanelWrapper.value;
					if (getEventTargetNode(evnt, panelWrapperElem).flag) updatePlacement();
					else {
						checkValue();
						hidePanel();
					}
				}
			}
		};
		const handleGlobalBlurEvent = () => {
			const { visiblePanel, isActivated } = reactData;
			if (visiblePanel) {
				checkValue();
				hidePanel();
			}
			if (isActivated) reactData.isActivated = false;
			if (visiblePanel || isActivated) {
				const targetElem = refInputTarget.value;
				if (targetElem) targetElem.blur();
			}
		};
		const handleGlobalResizeEvent = () => {
			const { visiblePanel } = reactData;
			if (visiblePanel) updatePlacement();
		};
		const updateZindex = () => {
			const customZIndex = computePopupOpts.value.zIndex || props.zIndex;
			if (customZIndex) reactData.panelIndex = import_xe_utils.default.toNumber(customZIndex);
			else if (reactData.panelIndex < getLastZIndex()) reactData.panelIndex = nextZIndex();
		};
		const updatePlacement = () => {
			const { placement } = props;
			const { panelIndex } = reactData;
			const targetElem = refInputTarget.value;
			const panelElem = refInputPanel.value;
			const btnTransfer = computeBtnTransfer.value;
			const popupOpts = computePopupOpts.value;
			const handleStyle = () => {
				const ppObj = updatePanelPlacement(targetElem, panelElem, {
					placement: popupOpts.placement || placement,
					defaultPlacement: popupOpts.defaultPlacement,
					teleportTo: btnTransfer
				});
				reactData.panelStyle = Object.assign(ppObj.style, { zIndex: panelIndex });
				reactData.panelPlacement = ppObj.placement;
			};
			handleStyle();
			return nextTick().then(handleStyle);
		};
		const showPanel = () => {
			const { visiblePanel } = reactData;
			if (!computeIsDisabled.value && !visiblePanel) {
				if (!reactData.initialized) reactData.initialized = true;
				if (internalData.hpTimeout) {
					clearTimeout(internalData.hpTimeout);
					internalData.hpTimeout = void 0;
				}
				internalData.selectStatus = false;
				reactData.isActivated = true;
				reactData.isAniVisible = true;
				setTimeout(() => {
					reactData.visiblePanel = true;
					updatePlacement();
				}, 10);
				updateZindex();
				return updatePlacement();
			}
			return nextTick();
		};
		const dateRangePickerOpenEvent = (evnt) => {
			if (!computeFormReadonly.value) {
				evnt.preventDefault();
				showPanel();
			}
		};
		const clickIconEvent = (evnt) => {
			const { trigger } = computePopupOpts.value;
			if (!trigger || trigger === "default" || trigger === "icon") dateRangePickerOpenEvent(evnt);
		};
		const clickEvent = (evnt) => {
			triggerEvent(evnt);
		};
		const handleShortcutEvent = ({ option, $event }) => {
			const { type } = props;
			const shortcutOpts = computeShortcutOpts.value;
			const { autoClose } = shortcutOpts;
			const { code, clickMethod } = option;
			let startValue = reactData.startValue;
			let endValue = reactData.endValue;
			let value = getRangeValue(startValue, endValue);
			const shortcutParams = {
				$dateRangePicker: $xeDateRangePicker,
				option,
				value,
				startValue,
				endValue,
				code
			};
			if (!clickMethod && code) {
				const gCommandOpts = commands.get(code);
				const drpCommandMethod = gCommandOpts ? gCommandOpts.dateRangePickerCommandMethod : null;
				if (drpCommandMethod) drpCommandMethod(shortcutParams);
				else {
					const dateValueFormat = computeDateValueFormat.value;
					const firstDayOfWeek = computeFirstDayOfWeek.value;
					switch (code) {
						case "last1":
						case "last3":
						case "last7":
						case "last30":
						case "last60":
						case "last90":
						case "last180": {
							const restObj = getRangeDateByCode(code, value, type, {
								valueFormat: dateValueFormat,
								firstDay: firstDayOfWeek
							});
							startValue = restObj.startValue;
							endValue = restObj.endValue;
							value = getRangeValue(startValue, endValue);
							shortcutParams.value = value;
							shortcutParams.startValue = startValue;
							shortcutParams.endValue = endValue;
							handleChange(startValue, endValue, $event);
							break;
						}
						default:
							errLog("vxe.error.notCommands", [`[date-range-picker] ${code}`]);
							break;
					}
				}
			} else {
				const optClickMethod = clickMethod || shortcutOpts.clickMethod;
				if (optClickMethod) optClickMethod(shortcutParams);
			}
			if (autoClose) hidePanel();
			dispatchEvent("shortcut-click", shortcutParams, $event);
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $dateRangePicker: $xeDateRangePicker }, params));
		};
		dateRangePickerMethods = {
			dispatchEvent,
			setModelValue(startValue, endValue) {
				reactData.startValue = startValue || "";
				reactData.endValue = endValue || "";
				emit("update:modelValue", getRangeValue(startValue, endValue));
			},
			setModelValueByEvent(evnt, startValue, endValue) {
				handleChange(startValue || "", endValue || "", evnt);
			},
			focus() {
				const inputElem = refInputTarget.value;
				reactData.isActivated = true;
				inputElem.focus();
				return nextTick();
			},
			blur() {
				refInputTarget.value.blur();
				reactData.isActivated = false;
				return nextTick();
			},
			select() {
				refInputTarget.value.select();
				reactData.isActivated = false;
				return nextTick();
			},
			showPanel,
			hidePanel,
			updatePlacement
		};
		Object.assign($xeDateRangePicker, dateRangePickerMethods);
		const renderShortcutBtn = (pos, isVertical) => {
			const shortcutOpts = computeShortcutOpts.value;
			const { position, align, mode } = shortcutOpts;
			const shortcutList = computeShortcutList.value;
			if (isEnableConf(shortcutOpts) && shortcutList.length && (position || "left") === pos) return h("div", { class: `vxe-date-range-picker--layout-${pos}-wrapper` }, [h(button_group_default, {
				options: shortcutList,
				mode,
				align,
				vertical: isVertical,
				onClick: handleShortcutEvent
			})]);
			return renderEmptyElement($xeDateRangePicker);
		};
		const renderPanel = () => {
			const { type, separator, autoClose, showConfirmButton, showClearButton } = props;
			const { initialized, isAniVisible, visiblePanel, panelPlacement, panelStyle, startValue, endValue } = reactData;
			const vSize = computeSize.value;
			const btnTransfer = computeBtnTransfer.value;
			const shortcutOpts = computeShortcutOpts.value;
			const isClearable = computeIsClearable.value;
			const panelLabelObj = computePanelLabelObj.value;
			const shortcutList = computeShortcutList.value;
			const isDateTimeType = computeIsDateTimeType.value;
			const defaultDates = computeDefaultDates.value;
			const defaultTimes = computeDefaultTimes.value;
			const timeOpts = computeTimeOpts.value;
			const popupOpts = computePopupOpts.value;
			const { startLabel, endLabel } = panelLabelObj;
			const { position } = shortcutOpts;
			const headerSlot = slots.header;
			const footerSlot = slots.footer;
			const topSlot = slots.top;
			const bottomSlot = slots.bottom;
			const leftSlot = slots.left;
			const rightSlot = slots.right;
			const ppClassName = popupOpts.className;
			const [sdDate, edDate] = defaultDates;
			const [sdTime, edTime] = defaultTimes;
			const hasShortcutBtn = shortcutList.length > 0;
			const showConfirmBtn = showConfirmButton === null ? isDateTimeType || !autoClose : showConfirmButton;
			const showClearBtn = showClearButton === null ? isClearable : showClearButton;
			return h(Teleport, {
				to: "body",
				disabled: btnTransfer ? !initialized : true
			}, [h("div", {
				ref: refInputPanel,
				class: [
					"vxe-table--ignore-clear vxe-date-range-picker--panel",
					`type--${type}`,
					ppClassName ? import_xe_utils.default.isFunction(ppClassName) ? ppClassName({ $dateRangePicker: $xeDateRangePicker }) : ppClassName : "",
					{
						[`size--${vSize}`]: vSize,
						"is--transfer": btnTransfer,
						"ani--leave": isAniVisible,
						"ani--enter": visiblePanel,
						"show--top": !!(topSlot || headerSlot || hasShortcutBtn && (position === "top" || position === "header")),
						"show--bottom": !!(bottomSlot || footerSlot || hasShortcutBtn && (position === "bottom" || position === "footer")),
						"show--left": !!(leftSlot || hasShortcutBtn && position === "left"),
						"show--right": !!(rightSlot || hasShortcutBtn && position === "right")
					}
				],
				placement: panelPlacement,
				style: panelStyle
			}, initialized && (visiblePanel || isAniVisible) ? [h("div", {
				ref: refPanelWrapper,
				class: [
					"vxe-date-range-picker--layout-all-wrapper",
					`type--${type}`,
					{ [`size--${vSize}`]: vSize }
				]
			}, [
				topSlot ? h("div", { class: "vxe-date-range-picker--layout-top-wrapper" }, topSlot({})) : renderShortcutBtn("top"),
				h("div", { class: "vxe-date-range-picker--layout-body-layout-wrapper" }, [
					leftSlot ? h("div", { class: "vxe-date-range-picker--layout-left-wrapper" }, leftSlot({})) : renderShortcutBtn("left", true),
					h("div", { class: "vxe-date-range-picker--layout-body-content-wrapper" }, [
						headerSlot ? h("div", { class: "vxe-date-range-picker--layout-header-wrapper" }, headerSlot({})) : renderShortcutBtn("header"),
						h("div", { class: "vxe-date-range-picker--layout-body-wrapper" }, [h(date_panel_default, {
							ref: refStartDatePanel,
							modelValue: startValue,
							type: props.type,
							className: props.className,
							minDate: props.minDate,
							maxDate: props.maxDate,
							endDate: endValue,
							startDay: props.startDay,
							labelFormat: props.labelFormat,
							valueFormat: props.valueFormat,
							timeFormat: props.timeFormat,
							defaultDate: sdDate,
							defaultTime: sdTime,
							timeConfig: timeOpts,
							festivalMethod: props.festivalMethod,
							disabledMethod: props.disabledMethod,
							selectDay: props.selectDay,
							onChange: startPanelChangeEvent
						}), h(date_panel_default, {
							ref: refEndDatePanel,
							modelValue: endValue,
							type: props.type,
							className: props.className,
							minDate: props.minDate,
							maxDate: props.maxDate,
							startDate: startValue,
							startDay: props.startDay,
							labelFormat: props.labelFormat,
							valueFormat: props.valueFormat,
							timeFormat: props.timeFormat,
							defaultDate: edDate,
							defaultTime: edTime,
							timeConfig: timeOpts,
							festivalMethod: props.festivalMethod,
							disabledMethod: props.disabledMethod,
							selectDay: props.selectDay,
							onChange: endPanelChangeEvent
						})]),
						h("div", { class: "vxe-date-range-picker--layout-footer-wrapper" }, [
							h("div", { class: "vxe-date-range-picker--layout-footer-label" }, startLabel || endLabel ? [
								h("span", startLabel),
								h("span", `${separator || ""}`),
								h("span", endLabel)
							] : `${separator || ""}`),
							h("div", { class: "vxe-date-range-picker--layout-footer-custom" }, footerSlot ? footerSlot({}) : [renderShortcutBtn("footer")]),
							h("div", { class: "vxe-date-range-picker--layout-footer-btns" }, [showClearBtn ? h(VxeButtonComponent, {
								size: "mini",
								disabled: !(startValue || endValue),
								content: getI18n("vxe.button.clear"),
								onClick: clearValueEvent
							}) : renderEmptyElement($xeDateRangePicker), showConfirmBtn ? h(VxeButtonComponent, {
								size: "mini",
								status: "primary",
								content: getI18n("vxe.button.confirm"),
								onClick: confirmEvent
							}) : renderEmptyElement($xeDateRangePicker)])
						])
					]),
					rightSlot ? h("div", { class: "vxe-date-range-picker--layout-right-wrapper" }, rightSlot({})) : renderShortcutBtn("right", true)
				]),
				bottomSlot ? h("div", { class: "vxe-date-range-picker--layout-bottom-wrapper" }, bottomSlot({})) : renderShortcutBtn("bottom")
			])] : [])]);
		};
		const renderPrefixIcon = () => {
			const { prefixIcon } = props;
			const prefixSlot = slots.prefix;
			return prefixSlot || prefixIcon ? h("div", {
				class: "vxe-date-range-picker--prefix",
				onClick: clickPrefixEvent
			}, [h("div", { class: "vxe-date-range-picker--prefix-icon" }, prefixSlot ? getSlotVNs$1(prefixSlot({})) : [h("i", { class: prefixIcon })])]) : null;
		};
		const renderSuffixIcon = () => {
			const { suffixIcon } = props;
			const { startValue, endValue } = reactData;
			const suffixSlot = slots.suffix;
			const isDisabled = computeIsDisabled.value;
			const isClearable = computeIsClearable.value;
			return h("div", { class: ["vxe-date-range-picker--suffix", { "is--clear": isClearable && !isDisabled && (startValue || endValue) }] }, [
				isClearable ? h("div", {
					class: "vxe-date-range-picker--clear-icon",
					onClick: clearValueEvent
				}, [h("i", { class: getIcon().INPUT_CLEAR })]) : renderEmptyElement($xeDateRangePicker),
				renderExtraSuffixIcon(),
				suffixSlot || suffixIcon ? h("div", {
					class: "vxe-date-range-picker--suffix-icon",
					onClick: clickSuffixEvent
				}, suffixSlot ? getSlotVNs$1(suffixSlot({})) : [h("i", { class: suffixIcon })]) : renderEmptyElement($xeDateRangePicker)
			]);
		};
		const renderExtraSuffixIcon = () => {
			return h("div", {
				class: "vxe-date-range-picker--control-icon",
				onClick: clickIconEvent
			}, [h("i", { class: ["vxe-date-range-picker--date-picker-icon", getIcon().DATE_PICKER_DATE] })]);
		};
		const renderVN = () => {
			const { className, type, name, autoComplete } = props;
			const { startValue, endValue, visiblePanel, isActivated } = reactData;
			const vSize = computeSize.value;
			const isDisabled = computeIsDisabled.value;
			const formReadonly = computeFormReadonly.value;
			const inputLabel = computeInputLabel.value;
			if (formReadonly) return h("div", {
				ref: refElem,
				class: [
					"vxe-date-range-picker--readonly",
					`type--${type}`,
					className
				]
			}, inputLabel);
			const inpPlaceholder = computeInpPlaceholder.value;
			const isClearable = computeIsClearable.value;
			const prefix = renderPrefixIcon();
			const suffix = renderSuffixIcon();
			return h("div", {
				ref: refElem,
				class: [
					"vxe-date-range-picker",
					`type--${type}`,
					className,
					{
						[`size--${vSize}`]: vSize,
						"is--prefix": !!prefix,
						"is--suffix": !!suffix,
						"is--visible": visiblePanel,
						"is--disabled": isDisabled,
						"is--active": isActivated,
						"show--clear": isClearable && !isDisabled && (startValue || endValue)
					}
				],
				spellcheck: false
			}, [
				prefix || renderEmptyElement($xeDateRangePicker),
				h("div", { class: "vxe-date-range-picker--wrapper" }, [h("input", {
					ref: refInputTarget,
					class: "vxe-date-range-picker--inner",
					value: inputLabel,
					name,
					type: "text",
					placeholder: inpPlaceholder,
					editable: false,
					disabled: isDisabled,
					autocomplete: autoComplete,
					onKeydown: keydownEvent,
					onKeyup: keyupEvent,
					onClick: clickEvent,
					onChange: changeEvent,
					onFocus: focusEvent,
					onBlur: blurEvent
				})]),
				suffix || renderEmptyElement($xeDateRangePicker),
				renderPanel()
			]);
		};
		watch(() => props.modelValue, () => {
			updateModelValue(true);
		});
		watch(computeMVal, () => {
			updateModelValue(false);
		});
		updateModelValue(true);
		nextTick(() => {
			globalEvents.on($xeDateRangePicker, "mousewheel", handleGlobalMousewheelEvent);
			globalEvents.on($xeDateRangePicker, "mousedown", handleGlobalMousedownEvent);
			globalEvents.on($xeDateRangePicker, "keydown", handleGlobalKeydownEvent);
			globalEvents.on($xeDateRangePicker, "blur", handleGlobalBlurEvent);
			globalEvents.on($xeDateRangePicker, "resize", handleGlobalResizeEvent);
		});
		onDeactivated(() => {
			checkValue();
		});
		onUnmounted(() => {
			globalEvents.off($xeDateRangePicker, "mousewheel");
			globalEvents.off($xeDateRangePicker, "mousedown");
			globalEvents.off($xeDateRangePicker, "blur");
			globalEvents.off($xeDateRangePicker, "resize");
		});
		onBeforeUnmount(() => {
			checkValue();
		});
		provide("$xeDateRangePicker", $xeDateRangePicker);
		$xeDateRangePicker.renderVN = renderVN;
		return $xeDateRangePicker;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/date-range-picker/index.js
var VxeDateRangePicker = Object.assign({}, date_range_picker_default, { install(app) {
	app.component(date_range_picker_default.name, date_range_picker_default);
} });
dynamicApp.use(VxeDateRangePicker);
VxeUI.component(date_range_picker_default);
var DateRangePicker = VxeDateRangePicker;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/drawer/src/drawer.js
var allActiveDrawers = [];
var drawer_default = defineVxeComponent({
	name: "VxeDrawer",
	props: {
		modelValue: Boolean,
		id: String,
		title: String,
		loading: {
			type: Boolean,
			default: null
		},
		className: String,
		position: {
			type: [String, Object],
			default: () => getConfig().drawer.position
		},
		lockView: {
			type: Boolean,
			default: () => getConfig().drawer.lockView
		},
		lockScroll: Boolean,
		mask: {
			type: Boolean,
			default: () => getConfig().drawer.mask
		},
		maskClosable: {
			type: Boolean,
			default: () => getConfig().drawer.maskClosable
		},
		escClosable: {
			type: Boolean,
			default: () => getConfig().drawer.escClosable
		},
		cancelClosable: {
			type: Boolean,
			default: () => getConfig().drawer.cancelClosable
		},
		confirmClosable: {
			type: Boolean,
			default: () => getConfig().drawer.confirmClosable
		},
		showHeader: {
			type: Boolean,
			default: () => getConfig().drawer.showHeader
		},
		showFooter: {
			type: Boolean,
			default: () => getConfig().drawer.showFooter
		},
		showClose: {
			type: Boolean,
			default: () => getConfig().drawer.showClose
		},
		content: [Number, String],
		showCancelButton: {
			type: Boolean,
			default: null
		},
		cancelButtonText: {
			type: String,
			default: () => getConfig().drawer.cancelButtonText
		},
		showConfirmButton: {
			type: Boolean,
			default: () => getConfig().drawer.showConfirmButton
		},
		confirmButtonText: {
			type: String,
			default: () => getConfig().drawer.confirmButtonText
		},
		destroyOnClose: {
			type: Boolean,
			default: () => getConfig().drawer.destroyOnClose
		},
		showTitleOverflow: {
			type: Boolean,
			default: () => getConfig().drawer.showTitleOverflow
		},
		width: [Number, String],
		height: [Number, String],
		resize: {
			type: Boolean,
			default: () => getConfig().drawer.resize
		},
		zIndex: Number,
		transfer: {
			type: Boolean,
			default: () => getConfig().drawer.transfer
		},
		padding: {
			type: Boolean,
			default: () => getConfig().drawer.padding
		},
		size: {
			type: String,
			default: () => getConfig().drawer.size || getConfig().size
		},
		beforeHideMethod: {
			type: Function,
			default: () => getConfig().drawer.beforeHideMethod
		},
		slots: Object
	},
	emits: [
		"update:modelValue",
		"show",
		"hide",
		"before-hide",
		"close",
		"confirm",
		"cancel",
		"resize"
	],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const $xeModal = inject("$xeModal", null);
		const $xeParentDrawer = inject("$xeDrawer", null);
		const $xeTable = inject("$xeTable", null);
		const $xeForm = inject("$xeForm", null);
		const { computeSize } = useSize(props);
		const refElem = ref();
		const refDrawerBox = ref();
		const refConfirmBtn = ref();
		const refCancelBtn = ref();
		const reactData = reactive({
			initialized: false,
			visible: false,
			contentVisible: false,
			drawerZIndex: 0,
			resizeFlag: 1
		});
		const refMaps = { refElem };
		const computeBtnTransfer = computed(() => {
			const { transfer } = props;
			if (transfer === null) {
				const globalTransfer = getConfig().modal.transfer;
				if (import_xe_utils.default.isBoolean(globalTransfer)) return globalTransfer;
				if ($xeTable || $xeModal || $xeParentDrawer || $xeForm) return true;
			}
			return transfer;
		});
		const computeDragType = computed(() => {
			switch (props.position) {
				case "top": return "sb";
				case "bottom": return "st";
				case "left": return "wr";
			}
			return "wl";
		});
		const computeMaps = {};
		const $xeDrawer = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const getBox = () => {
			return refDrawerBox.value;
		};
		const recalculate = () => {
			const { width, height } = props;
			const boxElem = getBox();
			if (boxElem) {
				boxElem.style.width = toCssUnit(width);
				boxElem.style.height = toCssUnit(height);
			}
			return nextTick();
		};
		const updateZindex = () => {
			const { zIndex } = props;
			const { drawerZIndex } = reactData;
			if (zIndex) reactData.drawerZIndex = zIndex;
			else if (drawerZIndex < getLastZIndex()) reactData.drawerZIndex = nextZIndex();
		};
		const removeActiveQueue = () => {
			if (allActiveDrawers.indexOf($xeDrawer) > -1) import_xe_utils.default.remove(allActiveDrawers, (item) => item === $xeDrawer);
		};
		const closeDrawer = (type) => {
			const { beforeHideMethod } = props;
			const { visible } = reactData;
			const params = { type };
			if (visible) Promise.resolve(beforeHideMethod ? beforeHideMethod(params) : null).then((rest) => {
				if (!import_xe_utils.default.isError(rest)) {
					reactData.contentVisible = false;
					removeActiveQueue();
					dispatchEvent("before-hide", params, null);
					setTimeout(() => {
						reactData.visible = false;
						emit("update:modelValue", false);
						dispatchEvent("hide", params, null);
					}, 200);
				}
			}).catch((e) => e);
			return nextTick();
		};
		const closeEvent = (evnt) => {
			const type = "close";
			dispatchEvent(type, { type }, evnt);
			closeDrawer(type);
		};
		const confirmEvent = (evnt) => {
			const { confirmClosable } = props;
			const type = "confirm";
			dispatchEvent(type, { type }, evnt);
			if (confirmClosable) closeDrawer(type);
		};
		const cancelEvent = (evnt) => {
			const { cancelClosable } = props;
			const type = "cancel";
			dispatchEvent(type, { type }, evnt);
			if (cancelClosable) closeDrawer(type);
		};
		const openDrawer = () => {
			const { showFooter } = props;
			const { initialized, visible } = reactData;
			if (!initialized) reactData.initialized = true;
			if (!visible) {
				reactData.visible = true;
				reactData.contentVisible = false;
				updateZindex();
				allActiveDrawers.push($xeDrawer);
				setTimeout(() => {
					recalculate();
					reactData.contentVisible = true;
					nextTick(() => {
						if (showFooter) {
							const confirmBtn = refConfirmBtn.value;
							const cancelBtn = refCancelBtn.value;
							const operBtn = confirmBtn || cancelBtn;
							if (operBtn) operBtn.focus();
						}
						const params = { type: "" };
						emit("update:modelValue", true);
						dispatchEvent("show", params, null);
					});
				}, 10);
			}
			return nextTick();
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $drawer: $xeDrawer }, params));
		};
		const drawerMethods = {
			dispatchEvent,
			open: openDrawer,
			close() {
				return closeDrawer("close");
			},
			getBox
		};
		const selfClickEvent = (evnt) => {
			const el = refElem.value;
			if (props.maskClosable && evnt.target === el) closeDrawer("mask");
		};
		const handleGlobalKeydownEvent = (evnt) => {
			if (globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.ESCAPE)) {
				const lastDrawer = import_xe_utils.default.max(allActiveDrawers, (item) => item.reactData.drawerZIndex);
				if (lastDrawer) setTimeout(() => {
					if (lastDrawer === $xeDrawer && lastDrawer.props.escClosable) {
						const type = "exit";
						dispatchEvent("close", { type }, evnt);
						closeDrawer(type);
					}
				}, 10);
			}
		};
		const boxMousedownEvent = () => {
			const { drawerZIndex } = reactData;
			if (allActiveDrawers.some((comp) => comp.reactData.visible && comp.reactData.drawerZIndex > drawerZIndex)) updateZindex();
		};
		const dragEvent = (evnt) => {
			evnt.preventDefault();
			const { visibleHeight, visibleWidth } = getDomNode();
			const marginSize = 0;
			const type = evnt.target.getAttribute("type");
			const minWidth = 0;
			const minHeight = 0;
			const maxWidth = visibleWidth;
			const maxHeight = visibleHeight;
			const boxElem = getBox();
			const clientWidth = boxElem.clientWidth;
			const clientHeight = boxElem.clientHeight;
			const disX = evnt.clientX;
			const disY = evnt.clientY;
			const offsetTop = boxElem.offsetTop;
			const offsetLeft = boxElem.offsetLeft;
			const params = { type: "resize" };
			document.onmousemove = (evnt) => {
				evnt.preventDefault();
				let dragLeft;
				let dragTop;
				let width;
				let height;
				switch (type) {
					case "wl":
						dragLeft = disX - evnt.clientX;
						width = dragLeft + clientWidth;
						if (offsetLeft - dragLeft > marginSize) {
							if (width > minWidth) boxElem.style.width = `${width < maxWidth ? width : maxWidth}px`;
						}
						break;
					case "st":
						dragTop = disY - evnt.clientY;
						height = clientHeight + dragTop;
						if (offsetTop - dragTop > marginSize) {
							if (height > minHeight) boxElem.style.height = `${height < maxHeight ? height : maxHeight}px`;
						}
						break;
					case "wr":
						dragLeft = evnt.clientX - disX;
						width = dragLeft + clientWidth;
						if (offsetLeft + width + marginSize < visibleWidth) {
							if (width > minWidth) boxElem.style.width = `${width < maxWidth ? width : maxWidth}px`;
						}
						break;
					case "sb":
						dragTop = evnt.clientY - disY;
						height = dragTop + clientHeight;
						if (offsetTop + height + marginSize < visibleHeight) {
							if (height > minHeight) boxElem.style.height = `${height < maxHeight ? height : maxHeight}px`;
						}
						break;
				}
				boxElem.className = boxElem.className.replace(/\s?is--drag/, "") + " is--drag";
				dispatchEvent("resize", params, evnt);
				reactData.resizeFlag++;
			};
			document.onmouseup = () => {
				document.onmousemove = null;
				document.onmouseup = null;
				reactData.resizeFlag++;
				setTimeout(() => {
					boxElem.className = boxElem.className.replace(/\s?is--drag/, "");
				}, 50);
			};
		};
		Object.assign($xeDrawer, drawerMethods, {});
		const renderTitles = () => {
			const { slots: propSlots = {}, showClose, title } = props;
			const titleSlot = slots.title || propSlots.title;
			const cornerSlot = slots.corner || propSlots.corner;
			return [h("div", { class: "vxe-drawer--header-title" }, titleSlot ? getSlotVNs$1(titleSlot({ $drawer: $xeDrawer })) : title ? getFuncText(title) : getI18n("vxe.alert.title")), h("div", { class: "vxe-drawer--header-right" }, [cornerSlot ? h("div", { class: "vxe-drawer--corner-wrapper" }, getSlotVNs$1(cornerSlot({ $drawer: $xeDrawer }))) : renderEmptyElement($xeDrawer), showClose ? h("div", {
				class: ["vxe-drawer--close-btn", "trigger--btn"],
				title: getI18n("vxe.drawer.close"),
				onClick: closeEvent
			}, [h("i", { class: getIcon().DRAWER_CLOSE })]) : renderEmptyElement($xeDrawer)])];
		};
		const renderHeader = () => {
			const { slots: propSlots = {}, showTitleOverflow } = props;
			const headerSlot = slots.header || propSlots.header;
			if (props.showHeader) return h("div", { class: ["vxe-drawer--header", { "is--ellipsis": showTitleOverflow }] }, headerSlot ? getSlotVNs$1(headerSlot({ $drawer: $xeDrawer })) : renderTitles());
			return renderEmptyElement($xeDrawer);
		};
		const renderBody = () => {
			const { slots: propSlots = {}, content } = props;
			const defaultSlot = slots.default || propSlots.default;
			const leftSlot = slots.left || propSlots.left;
			const rightSlot = slots.right || propSlots.right;
			return h("div", { class: "vxe-drawer--body" }, [
				leftSlot ? h("div", { class: "vxe-drawer--body-left" }, getSlotVNs$1(leftSlot({ $drawer: $xeDrawer }))) : renderEmptyElement($xeDrawer),
				h("div", { class: "vxe-drawer--body-default" }, [h("div", { class: "vxe-drawer--content" }, defaultSlot ? getSlotVNs$1(defaultSlot({ $drawer: $xeDrawer })) : getFuncText(content))]),
				rightSlot ? h("div", { class: "vxe-drawer--body-right" }, getSlotVNs$1(rightSlot({ $drawer: $xeDrawer }))) : renderEmptyElement($xeDrawer),
				h(VxeLoading, {
					class: "vxe-drawer--loading",
					modelValue: props.loading
				})
			]);
		};
		const renderDefaultFooter = () => {
			const { slots: propSlots = {}, showCancelButton, showConfirmButton, loading } = props;
			const lfSlot = slots.leftfoot || propSlots.leftfoot;
			const rfSlot = slots.rightfoot || propSlots.rightfoot;
			const btnVNs = [];
			if (showCancelButton) btnVNs.push(h(VxeButtonComponent, {
				key: 1,
				ref: refCancelBtn,
				content: props.cancelButtonText || getI18n("vxe.button.cancel"),
				onClick: cancelEvent
			}));
			if (showConfirmButton) btnVNs.push(h(VxeButtonComponent, {
				key: 2,
				ref: refConfirmBtn,
				loading,
				status: "primary",
				content: props.confirmButtonText || getI18n("vxe.button.confirm"),
				onClick: confirmEvent
			}));
			return h("div", { class: "vxe-drawer--footer-wrapper" }, [h("div", { class: "vxe-drawer--footer-left" }, lfSlot ? getSlotVNs$1(lfSlot({ $drawer: $xeDrawer })) : []), h("div", { class: "vxe-drawer--footer-right" }, rfSlot ? getSlotVNs$1(rfSlot({ $drawer: $xeDrawer })) : btnVNs)]);
		};
		const renderFooter = () => {
			const { slots: propSlots = {} } = props;
			const footerSlot = slots.footer || propSlots.footer;
			if (props.showFooter) return h("div", { class: "vxe-drawer--footer" }, footerSlot ? getSlotVNs$1(footerSlot({ $drawer: $xeDrawer })) : [renderDefaultFooter()]);
			return renderEmptyElement($xeDrawer);
		};
		const renderVN = () => {
			const { slots: propSlots = {}, className, position, loading, lockScroll, padding, lockView, mask, resize, destroyOnClose } = props;
			const { initialized, contentVisible, visible } = reactData;
			const asideSlot = slots.aside || propSlots.aside;
			const vSize = computeSize.value;
			const dragType = computeDragType.value;
			const btnTransfer = computeBtnTransfer.value;
			return h(Teleport, {
				to: "body",
				disabled: btnTransfer ? !initialized : true
			}, [h("div", {
				ref: refElem,
				class: [
					"vxe-drawer--wrapper",
					`pos--${position}`,
					className || "",
					{
						[`size--${vSize}`]: vSize,
						"is--padding": padding,
						"lock--scroll": lockScroll,
						"lock--view": lockView,
						"is--resize": resize,
						"is--mask": mask,
						"is--visible": contentVisible,
						"is--active": visible,
						"is--loading": loading
					}
				],
				style: { zIndex: reactData.drawerZIndex },
				onClick: selfClickEvent
			}, [h("div", {
				ref: refDrawerBox,
				class: "vxe-drawer--box",
				onMousedown: boxMousedownEvent
			}, [asideSlot ? h("div", { class: "vxe-drawer--aside" }, getSlotVNs$1(asideSlot({ $drawer: $xeDrawer }))) : renderEmptyElement($xeDrawer), h("div", { class: "vxe-drawer--container" }, !reactData.initialized || destroyOnClose && !reactData.visible ? [] : [
				renderHeader(),
				renderBody(),
				renderFooter(),
				resize ? h("span", { class: "vxe-drawer--resize" }, [h("span", {
					class: `${dragType}-resize`,
					type: dragType,
					onMousedown: dragEvent
				})]) : renderEmptyElement($xeDrawer)
			])])])]);
		};
		watch(() => props.width, recalculate);
		watch(() => props.height, recalculate);
		watch(() => props.modelValue, (value) => {
			if (value) openDrawer();
			else closeDrawer("model");
		});
		onMounted(() => {
			nextTick(() => {
				if (props.modelValue) openDrawer();
				recalculate();
			});
			if (props.escClosable) globalEvents.on($xeDrawer, "keydown", handleGlobalKeydownEvent);
		});
		onUnmounted(() => {
			globalEvents.off($xeDrawer, "keydown");
			removeActiveQueue();
		});
		provide("$xeDrawer", $xeDrawer);
		$xeDrawer.renderVN = renderVN;
		return $xeDrawer;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/drawer/index.js
function handleDrawer(options) {
	checkDynamic();
	return new Promise((resolve) => {
		if (options && options.id && allActiveDrawers.some((comp) => comp.props.id === options.id)) resolve("exist");
		else {
			const _onHide = options.onHide;
			const drawerOpts = Object.assign(options, {
				key: import_xe_utils.default.uniqueId(),
				modelValue: true,
				onHide(params) {
					const drawerList = dynamicStore.drawers;
					if (_onHide) _onHide(params);
					dynamicStore.drawers = drawerList.filter((item) => item.key !== drawerOpts.key);
					resolve(params.type);
				}
			});
			dynamicStore.drawers.push(drawerOpts);
		}
	});
}
function getDrawer(id) {
	return import_xe_utils.default.find(allActiveDrawers, ($drawer) => $drawer.props.id === id);
}
/**
* 全局关闭动态的活动窗口（只能用于关闭动态的创建的活动窗口）
* 如果传 id 则关闭指定的窗口
* 如果不传则关闭所有窗口
*/
function closeDrawer(id) {
	const drawers = id ? [getDrawer(id)] : allActiveDrawers;
	const restPromises = [];
	drawers.forEach(($drawer) => {
		if ($drawer) restPromises.push($drawer.close());
	});
	return Promise.all(restPromises);
}
function openDrawer(options) {
	return handleDrawer(Object.assign({}, options));
}
var DrawerController = {
	get: getDrawer,
	close: closeDrawer,
	open: openDrawer
};
var VxeDrawer = Object.assign(drawer_default, { install: function(app) {
	app.component(drawer_default.name, drawer_default);
} });
VxeUI.drawer = DrawerController;
dynamicApp.use(VxeDrawer);
VxeUI.component(drawer_default);
var Drawer = VxeDrawer;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/empty/src/empty.js
var empty_default = defineVxeComponent({
	name: "VxeEmpty",
	props: {
		imageUrl: String,
		imageStyle: Object,
		icon: String,
		status: String,
		content: [String, Number]
	},
	emits: [],
	setup(props, context) {
		const { emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeMaps = {};
		const $xeEmpty = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $empty: $xeEmpty }, params));
		};
		Object.assign($xeEmpty, { dispatchEvent }, {});
		const renderVN = () => {
			const { imageUrl, imageStyle, icon, status, content } = props;
			return h("div", {
				ref: refElem,
				class: ["vxe-empty", { [`theme--${status}`]: status }]
			}, [h("div", { class: "vxe-empty--inner" }, [imageUrl ? h("div", { class: "vxe-empty--img-wrapper" }, [h("img", {
				src: imageUrl,
				style: imageStyle
			})]) : h("div", { class: "vxe-empty--icon-wrapper" }, [h("i", { class: icon || getIcon().EMPTY_DEFAULT })]), h("div", { class: "vxe-empty--content-wrapper" }, `${content || getI18n("vxe.empty.defText")}`)])]);
		};
		$xeEmpty.renderVN = renderVN;
		return $xeEmpty;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/empty/index.js
var VxeEmpty = Object.assign({}, empty_default, { install(app) {
	app.component(empty_default.name, empty_default);
} });
dynamicApp.use(VxeEmpty);
VxeUI.component(empty_default);
var Empty = VxeEmpty;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/form/src/itemInfo.js
var ItemInfo = class {
	constructor($xeForm, item) {
		const { field, itemRender, formatter } = item;
		if (formatter) {
			if (import_xe_utils.default.isString(formatter)) {
				const gFormatOpts = formats.get(formatter);
				if (!gFormatOpts || !gFormatOpts.formItemFormatMethod) errLog("vxe.error.notFormats", [`[form] ${formatter}`]);
			} else if (import_xe_utils.default.isArray(formatter)) {
				const gFormatOpts = formats.get(formatter[0]);
				if (!gFormatOpts || !gFormatOpts.formItemFormatMethod) errLog("vxe.error.notFormats", [`[form] ${formatter[0]}`]);
			}
		}
		if (field && itemRender) {
			if (itemRender.startField && `${itemRender.startField}`.indexOf(field) >= 0) errLog("vxe.error.modelConflicts", [`[form] field=${field}`, `item-render.startField=${itemRender.startField}`]);
			if (itemRender.endField && `${itemRender.endField}`.indexOf(field) >= 0) errLog("vxe.error.modelConflicts", [`[form] field=${field}`, `item-render.endField=${itemRender.endField}`]);
		}
		Object.assign(this, {
			id: import_xe_utils.default.uniqueId("item_"),
			title: item.title,
			field,
			span: item.span,
			align: item.align,
			verticalAlign: item.verticalAlign,
			titleBackground: item.titleBackground,
			titleBold: item.titleBold,
			titleAlign: item.titleAlign,
			titleWidth: item.titleWidth,
			titleColon: item.titleColon,
			vertical: item.vertical,
			padding: item.padding,
			titleAsterisk: item.titleAsterisk,
			titlePrefix: item.titlePrefix,
			titleSuffix: item.titleSuffix,
			titleOverflow: item.titleOverflow,
			showTitle: item.showTitle,
			resetValue: item.resetValue,
			visibleMethod: item.visibleMethod,
			visible: item.visible,
			showContent: item.showContent,
			folding: item.folding,
			collapseNode: item.collapseNode,
			className: item.className,
			contentClassName: item.contentClassName,
			contentStyle: item.contentStyle,
			titleClassName: item.titleClassName,
			titleStyle: item.titleStyle,
			itemRender,
			rules: item.rules,
			formatter,
			params: item.params,
			showError: false,
			showIconMsg: false,
			errRule: null,
			slots: item.slots,
			children: []
		});
	}
	update(name, value) {
		this[name] = value;
	}
};
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/form/src/util.js
function isFormItem(item) {
	return item instanceof ItemInfo;
}
function createItem($xeForm, _vm) {
	return isFormItem(_vm) ? _vm : new ItemInfo($xeForm, _vm);
}
function handleFieldOrItem($xeForm, fieldOrItem) {
	if (fieldOrItem) return import_xe_utils.default.isString(fieldOrItem) ? $xeForm.getItemByField(fieldOrItem) : fieldOrItem;
	return null;
}
function isHiddenItem($xeForm, formItem) {
	const { reactData } = $xeForm;
	const { collapseAll } = reactData;
	const { folding, visible } = formItem;
	return visible === false || folding && collapseAll;
}
function isActiveItem($xeForm, formItem) {
	const $xeGrid = $xeForm.xeGrid;
	let { visibleMethod, itemRender, visible, field } = formItem;
	if (visible === false) return visible;
	const compConf = isEnableConf(itemRender) ? renderer.get(itemRender.name) : null;
	const fiVisibleMethod = compConf ? compConf.formItemVisibleMethod || compConf.itemVisibleMethod : null;
	if (!visibleMethod && fiVisibleMethod) visibleMethod = fiVisibleMethod;
	if (!visibleMethod) return true;
	const { data } = $xeForm.props;
	return visibleMethod({
		data,
		field,
		property: field,
		item: formItem,
		$form: $xeForm,
		$grid: $xeGrid
	});
}
function watchItem(props, formItem) {
	Object.keys(props).forEach((name) => {
		watch(() => props[name], (value) => {
			formItem.update(name, value);
		});
	});
}
function assembleItem($xeForm, el, formItem, formGroup) {
	const { reactData } = $xeForm;
	const { customLayout } = $xeForm.props;
	const { staticItems } = reactData;
	if (customLayout) {
		if (!staticItems.some((item) => item.id === formItem.id)) staticItems.push(formItem);
	} else if (el) {
		const parentElem = el.parentNode;
		const parentItem = formGroup ? formGroup.formItem : null;
		const parentItems = parentItem ? parentItem.children : staticItems;
		if (parentElem) {
			parentItems.splice(import_xe_utils.default.arrayIndexOf(parentElem.children, el), 0, formItem);
			reactData.staticItems = staticItems.slice(0);
		}
	}
}
function destroyItem($xeForm, formItem) {
	const { reactData } = $xeForm;
	const { staticItems } = reactData;
	const index = import_xe_utils.default.findIndexOf(staticItems, (item) => item.id === formItem.id);
	if (index > -1) staticItems.splice(index, 1);
	reactData.staticItems = staticItems.slice(0);
}
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/form/src/render.js
function renderPrefixIcon(titlePrefix) {
	return h("span", { class: "vxe-form--item-title-tip-prefix" }, [h(icon_default, {
		class: titlePrefix.icon || getIcon().FORM_PREFIX,
		status: titlePrefix.iconStatus
	})]);
}
function renderSuffixIcon(titleSuffix) {
	return h("span", { class: "vxe-form--item-title-tip-suffix" }, [h(icon_default, {
		class: titleSuffix.icon || getIcon().FORM_SUFFIX,
		status: titleSuffix.iconStatus
	})]);
}
function getItemClass($xeForm, item, isGroup) {
	const formProps = $xeForm.props;
	const formReactData = $xeForm.reactData;
	const $xeGrid = $xeForm.xeGrid;
	const { computeSize, computeValidOpts } = $xeForm.getComputeMaps();
	const { data, rules, readonly, disabled, span: allSpan, titleBackground: allTitleBackground, titleBold: allTitleBold, titleColon: allTitleColon, titleAsterisk: allTitleAsterisk, vertical: allVertical, padding: allPadding } = formProps;
	const { collapseAll } = formReactData;
	const { folding, field, itemRender, showError, className, vertical, padding, children, showContent } = item;
	const vSize = computeSize.value;
	const { showErrorMessage, showMessage, showErrorBackground, showErrorIcon } = computeValidOpts.value;
	const compConf = isEnableConf(itemRender) ? renderer.get(itemRender.name) : null;
	const itemClassName = compConf ? compConf.formItemClassName || compConf.itemClassName : "";
	const span = item.span || allSpan;
	const itemPadding = import_xe_utils.default.eqNull(padding) ? allPadding : padding;
	const itemVertical = import_xe_utils.default.eqNull(vertical) ? allVertical : vertical;
	const titleBackground = import_xe_utils.default.eqNull(item.titleBackground) ? allTitleBackground : item.titleBackground;
	const titleBold = import_xe_utils.default.eqNull(item.titleBold) ? allTitleBold : item.titleBold;
	const titleColon = import_xe_utils.default.eqNull(item.titleColon) ? allTitleColon : item.titleColon;
	const titleAsterisk = import_xe_utils.default.eqNull(item.titleAsterisk) ? allTitleAsterisk : item.titleAsterisk;
	const params = {
		data,
		disabled,
		readonly,
		field,
		property: field,
		item,
		$form: $xeForm,
		$grid: $xeGrid
	};
	const hasGroup = children && children.length > 0;
	let isRequired = false;
	let isValid = false;
	if (!readonly && rules) {
		const itemRules = rules[field];
		if (itemRules && itemRules.length) {
			isValid = true;
			isRequired = itemRules.some((rule) => rule.required);
		}
	}
	const showErrMsg = import_xe_utils.default.isBoolean(showErrorMessage) ? showErrorMessage : showMessage;
	return [
		isGroup || hasGroup ? "vxe-form--group" : "",
		"vxe-form--item",
		item.id,
		span ? `vxe-form--item-col_${span} is--span` : "",
		`${showErrMsg ? "show" : "hide"}--err-msg`,
		className ? import_xe_utils.default.isFunction(className) ? className(params) : className : "",
		itemClassName ? import_xe_utils.default.isFunction(itemClassName) ? itemClassName(params) : itemClassName : "",
		{
			[`size--${vSize}`]: vSize,
			"is--colon": titleColon,
			"is--tbg": titleBackground,
			"is--bold": titleBold,
			"is--padding": itemPadding,
			"is--vertical": itemVertical,
			"is--asterisk": titleAsterisk,
			"hide--content": showContent === false,
			"is--valid": isValid,
			"is--required": isRequired,
			"is--hidden": folding && collapseAll,
			"is--active": isActiveItem($xeForm, item),
			"err--icon": showErrorIcon,
			"err--bg": showErrorBackground,
			"is--error": showError
		}
	];
}
function getItemContentClass($xeForm, item, isGroup) {
	const formProps = $xeForm.props;
	const $xeGrid = $xeForm.xeGrid;
	const { data, readonly, disabled, align: allAlign, verticalAlign: allVerticalAlign } = formProps;
	const { field, itemRender, contentClassName, children } = item;
	const hasGroup = children && children.length > 0;
	const compConf = isEnableConf(itemRender) ? renderer.get(itemRender.name) : null;
	const itemContentClassName = compConf ? compConf.formItemContentClassName || compConf.itemContentClassName : "";
	const align = hasGroup ? item.align : import_xe_utils.default.eqNull(item.align) ? allAlign : item.align;
	const verticalAlign = hasGroup ? item.verticalAlign : import_xe_utils.default.eqNull(item.verticalAlign) ? allVerticalAlign : item.verticalAlign;
	const params = {
		data,
		disabled,
		readonly,
		field,
		property: field,
		item,
		$form: $xeForm,
		$grid: $xeGrid
	};
	return [
		isGroup || hasGroup ? "vxe-form--group-content vxe-form--item-row" : "",
		"vxe-form--item-content",
		align ? `align--${align}` : "",
		verticalAlign ? `vertical-align--${verticalAlign}` : "",
		itemContentClassName ? import_xe_utils.default.isFunction(itemContentClassName) ? itemContentClassName(params) : itemContentClassName : "",
		contentClassName ? import_xe_utils.default.isFunction(contentClassName) ? contentClassName(params) : contentClassName : ""
	];
}
function renderTitle($xeForm, item, isGroup) {
	const formProps = $xeForm.props;
	const $xeGrid = $xeForm.xeGrid;
	const { data, readonly, disabled, titleAlign: allTitleAlign, titleWidth: allTitleWidth, titleOverflow: allTitleOverflow, vertical: allVertical } = formProps;
	const { slots, title, field, itemRender, titleOverflow, vertical, showTitle, titleClassName, titleStyle, titlePrefix, titleSuffix, children, showContent } = item;
	const { computeTooltipOpts } = $xeForm.getComputeMaps();
	const tooltipOpts = computeTooltipOpts.value;
	const compConf = isEnableConf(itemRender) ? renderer.get(itemRender.name) : null;
	const itemTitleClassName = compConf ? compConf.formItemTitleClassName || compConf.itemTitleClassName : "";
	const itemTitleStyle = compConf ? compConf.formItemTitleStyle || compConf.itemTitleStyle : null;
	const itemVertical = import_xe_utils.default.eqNull(vertical) ? allVertical : vertical;
	const titleAlign = import_xe_utils.default.eqNull(item.titleAlign) ? allTitleAlign : item.titleAlign;
	const titleWidth = itemVertical ? null : import_xe_utils.default.eqNull(item.titleWidth) ? allTitleWidth : item.titleWidth;
	const itemOverflow = import_xe_utils.default.eqNull(titleOverflow) ? allTitleOverflow : titleOverflow;
	const ovEllipsis = itemOverflow === "ellipsis";
	const ovTitle = itemOverflow === "title";
	const ovTooltip = itemOverflow === true || itemOverflow === "tooltip";
	const hasEllipsis = ovTitle || ovTooltip || ovEllipsis;
	const params = {
		data,
		disabled,
		readonly,
		field,
		property: field,
		item,
		$form: $xeForm,
		$grid: $xeGrid
	};
	const titleSlot = slots ? slots.title : null;
	const prefixSlot = slots ? slots.prefix : null;
	const suffixSlot = slots ? slots.suffix || slots.extra : null;
	const isTitle = showTitle !== false && (title || titleSlot);
	const hasGroup = children && children.length > 0;
	const titVNs = [];
	if (prefixSlot) titVNs.push(h("span", {
		key: "pt",
		class: "vxe-form--item-title-prefix"
	}, $xeForm.callSlot(prefixSlot, params)));
	if (titlePrefix) titVNs.push(titlePrefix.content || titlePrefix.message ? h(tooltip_default, Object.assign(Object.assign(Object.assign({ key: "pm" }, tooltipOpts), titlePrefix), { content: getFuncText(titlePrefix.content || titlePrefix.message) }), { default: () => renderPrefixIcon(titlePrefix) }) : renderPrefixIcon(titlePrefix));
	const rftTitle = compConf ? compConf.renderFormItemTitle || compConf.renderItemTitle : null;
	titVNs.push(h("span", {
		key: "pl",
		class: "vxe-form--item-title-label"
	}, titleSlot ? $xeForm.callSlot(titleSlot, params) : rftTitle ? getSlotVNs$1(rftTitle(itemRender, params)) : getFuncText(item.title)));
	const fixVNs = [];
	if (titleSuffix) fixVNs.push(titleSuffix.content || titleSuffix.message ? h(tooltip_default, Object.assign(Object.assign(Object.assign({ key: "sm" }, tooltipOpts), titleSuffix), { content: getFuncText(titleSuffix.content || titleSuffix.message) }), { default: () => renderSuffixIcon(titleSuffix) }) : renderSuffixIcon(titleSuffix));
	if (suffixSlot) fixVNs.push(h("span", {
		key: "st",
		class: "vxe-form--item-title-suffix"
	}, $xeForm.callSlot(suffixSlot, params)));
	const ons = ovTooltip ? {
		onMouseenter(evnt) {
			$xeForm.triggerTitleTipEvent(evnt, params);
		},
		onMouseleave: $xeForm.handleTitleTipLeaveEvent
	} : {};
	const itStyle = Object.assign({}, import_xe_utils.default.isFunction(itemTitleStyle) ? itemTitleStyle(params) : itemTitleStyle, import_xe_utils.default.isFunction(titleStyle) ? titleStyle(params) : titleStyle);
	if (titleWidth && titleWidth !== "auto" && showContent !== false) itStyle.width = toCssUnit(titleWidth);
	return isTitle ? h("div", Object.assign({
		class: [
			isGroup || hasGroup ? "vxe-form--group-title" : "",
			"vxe-form--item-title",
			titleAlign ? `align--${titleAlign}` : "",
			hasEllipsis ? "is--ellipsis" : "",
			itemTitleClassName ? import_xe_utils.default.isFunction(itemTitleClassName) ? itemTitleClassName(params) : itemTitleClassName : "",
			titleClassName ? import_xe_utils.default.isFunction(titleClassName) ? titleClassName(params) : titleClassName : ""
		],
		style: itStyle,
		itemid: item.id,
		title: ovTitle ? getFuncText(title) : null
	}, ons), [h("div", { class: "vxe-form--item-title-content" }, titVNs), h("div", { class: "vxe-form--item-title-postfix" }, fixVNs)]) : renderEmptyElement($xeForm);
}
function renderItemErrorIcon($xeForm, item) {
	const { computeValidOpts } = $xeForm.getComputeMaps();
	const validOpts = computeValidOpts.value;
	const { showErrorIcon, errorIcon } = validOpts;
	const { errRule, showIconMsg } = item;
	if (!showErrorIcon) return renderEmptyElement($xeForm);
	return h("div", {
		key: "emi",
		class: ["vxe-form-item--valid-error-icon-wrapper", {
			"is--show": showIconMsg,
			"is--hide": !showIconMsg
		}]
	}, [h("span", {
		class: "vxe-form-item--valid-error-icon-btn",
		onClick(evnt) {
			$xeForm.handleValidIconEvent(evnt, { item });
		}
	}, [h("i", { class: errorIcon || getIcon().FORM_VALID_ERROR_ICON })]), h("div", { class: "vxe-form-item--valid-error-icon-msg-tip" }, errRule ? [h("div", { class: `vxe-form-item--valid-error-icon-msg vxe-form-item--valid-error-icon-theme-${validOpts.theme || "normal"}` }, errRule.content || errRule.message)] : [])]);
}
function renderItemContent($xeForm, item) {
	const formProps = $xeForm.props;
	const formReactData = $xeForm.reactData;
	const formInternalData = $xeForm.internalData;
	const $xeGrid = $xeForm.xeGrid;
	const { computeCollapseOpts, computeValidOpts } = $xeForm.getComputeMaps();
	const { itemFormatCache } = formInternalData;
	const { data, readonly, disabled } = formProps;
	const { collapseAll } = formReactData;
	const { slots, field, itemRender, collapseNode, errRule, formatter } = item;
	const defaultSlot = slots ? slots.default : null;
	const validSlot = slots ? slots.valid : null;
	const collapseOpts = computeCollapseOpts.value;
	const validOpts = computeValidOpts.value;
	const { showErrorMessage, showMessage } = validOpts;
	const compConf = isEnableConf(itemRender) ? renderer.get(itemRender.name) : null;
	const itemValue = import_xe_utils.default.get(data, field);
	const params = {
		data,
		disabled,
		readonly,
		field,
		property: field,
		item,
		itemValue,
		$form: $xeForm,
		$grid: $xeGrid
	};
	let contentVNs = [];
	const rftContent = compConf ? compConf.renderFormItemContent || compConf.renderItemContent : null;
	if (defaultSlot) contentVNs = $xeForm.callSlot(defaultSlot, params);
	else if (rftContent) contentVNs = getSlotVNs$1(rftContent(itemRender, params));
	else if (field) {
		let itemLabel = itemValue;
		if (formatter) {
			let formatData;
			if (field) {
				const itemRest = itemFormatCache[field];
				if (itemRest) {
					formatData = itemRest.formatData;
					if (formatData) {
						if (formatData.value === itemValue) return formatData.label;
					} else formatData = itemRest.formatData = {};
				} else itemFormatCache[field] = { field };
			}
			if (import_xe_utils.default.isString(formatter)) {
				const gFormatOpts = formats.get(formatter);
				const fiFormatMethod = gFormatOpts ? gFormatOpts.formItemFormatMethod : null;
				itemLabel = fiFormatMethod ? fiFormatMethod(params) : "";
			} else if (import_xe_utils.default.isArray(formatter)) {
				const gFormatOpts = formats.get(formatter[0]);
				const fiFormatMethod = gFormatOpts ? gFormatOpts.formItemFormatMethod : null;
				itemLabel = fiFormatMethod ? fiFormatMethod(params, ...formatter.slice(1)) : "";
			} else itemLabel = formatter(params);
			if (formatData) {
				formatData.value = itemValue;
				formatData.label = itemLabel;
			}
		}
		contentVNs = [eqEmptyValue(itemLabel) ? "" : `${itemLabel}`];
	}
	if (collapseNode) contentVNs.push(h("div", {
		class: "vxe-form--item-trigger-node",
		onClick: $xeForm.toggleCollapseEvent
	}, [h("span", { class: "vxe-form--item-trigger-text" }, collapseAll ? collapseOpts.unfoldButtonText || getI18n("vxe.form.unfolding") : collapseOpts.foldButtonText || getI18n("vxe.form.folding")), h("i", { class: ["vxe-form--item-trigger-icon", collapseAll ? collapseOpts.foldIcon || getIcon().FORM_FOLDING : collapseOpts.unfoldIcon || getIcon().FORM_UNFOLDING] })]));
	if (errRule && (import_xe_utils.default.isBoolean(showErrorMessage) ? showErrorMessage : showMessage)) {
		const validParams = Object.assign(Object.assign({}, params), { rule: errRule });
		contentVNs.push(h("div", {
			class: "vxe-form-item--valid-error-tip",
			style: errRule.maxWidth ? { width: toCssUnit(errRule.maxWidth) } : null
		}, [h("div", { class: `vxe-form-item--valid-error-wrapper vxe-form-item--valid-error-theme-${validOpts.theme || "normal"}` }, [validSlot ? $xeForm.callSlot(validSlot, validParams) : [h("span", { class: "vxe-form--item--valid-error-msg" }, errRule.content || errRule.message)]])]));
	}
	return h("div", {
		key: "ct",
		class: "vxe-form--item-inner"
	}, contentVNs);
}
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/form/src/form-config-item.js
var VxeFormConfigItem = defineVxeComponent({
	name: "VxeFormConfigItem",
	props: { itemConfig: Object },
	setup(props) {
		const $xeForm = inject("$xeForm", {});
		provide("xeFormItemInfo", { itemConfig: props.itemConfig });
		const renderItem = ($xeForm, item) => {
			const formProps = $xeForm.props;
			const $xeGrid = $xeForm.xeGrid;
			const { data, readonly, disabled } = formProps;
			const { visible, field, itemRender, contentStyle, children, showContent } = item;
			const compConf = isEnableConf(itemRender) ? renderer.get(itemRender.name) : null;
			const itemStyle = compConf ? compConf.formItemStyle || compConf.itemStyle : null;
			const itemContentStyle = compConf ? compConf.formItemContentStyle || compConf.itemContentStyle : null;
			const params = {
				data,
				disabled,
				readonly,
				field,
				property: field,
				item,
				$form: $xeForm,
				$grid: $xeGrid
			};
			const hasGroup = children && children.length > 0;
			if (visible === false) return renderEmptyElement($xeForm);
			return h("div", {
				key: item.id,
				itemid: item.id,
				class: getItemClass($xeForm, item),
				style: import_xe_utils.default.isFunction(itemStyle) ? itemStyle(params) : itemStyle
			}, [renderTitle($xeForm, item), showContent === false ? renderEmptyElement($xeForm) : h("div", {
				class: getItemContentClass($xeForm, item),
				style: Object.assign({}, import_xe_utils.default.isFunction(itemContentStyle) ? itemContentStyle(params) : itemContentStyle, import_xe_utils.default.isFunction(contentStyle) ? contentStyle(params) : contentStyle)
			}, hasGroup ? children.map((childItem) => renderItem($xeForm, childItem)) : [renderItemContent($xeForm, item), renderItemErrorIcon($xeForm, item)])]);
		};
		const renderVN = () => {
			return renderItem($xeForm, props.itemConfig);
		};
		const $xeFormconfigitem = { renderVN };
		provide("$xeFormGroup", null);
		return $xeFormconfigitem;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/form/render/index.js
var componentDefaultModelProp = "modelValue";
/**
* 已废弃
* @deprecated
*/
function getOldComponentName(name) {
	return `vxe-${`${name || ""}`.replace("$", "")}`;
}
function getDefaultComponent(renderOpts) {
	const { name } = renderOpts;
	return getComponent(name) || resolveComponent(`${name}`);
}
/**
* 已废弃
* @deprecated
*/
function getOldComponent({ name }) {
	return resolveComponent(getOldComponentName(name));
}
function getNativeAttrs(renderOpts) {
	let { name, attrs } = renderOpts;
	if (name === "input") attrs = Object.assign({ type: "text" }, attrs);
	return attrs;
}
function getComponentFormItemProps(renderOpts, params, value, defaultProps) {
	return import_xe_utils.default.assign({}, defaultProps, renderOpts.props, { [componentDefaultModelProp]: value });
}
/**
* 原生事件处理
* @param renderOpts
* @param params
* @param modelFunc
* @param changeFunc
*/
function getNativeElementOns(renderOpts, params, modelFunc, changeFunc) {
	const { events } = renderOpts;
	const modelEvent = getModelEvent(renderOpts);
	const changeEvent = getChangeEvent(renderOpts);
	const isSameEvent = changeEvent === modelEvent;
	const ons = {};
	if (events) import_xe_utils.default.objectEach(events, (func, key) => {
		ons[getOnName(key)] = function(...args) {
			func(params, ...args);
		};
	});
	if (modelFunc) ons[getOnName(modelEvent)] = function(targetEvnt) {
		modelFunc(targetEvnt);
		if (isSameEvent && changeFunc) changeFunc(targetEvnt);
		if (events && events[modelEvent]) events[modelEvent](params, targetEvnt);
	};
	if (!isSameEvent && changeFunc) ons[getOnName(changeEvent)] = function(...args) {
		changeFunc(...args);
		if (events && events[changeEvent]) events[changeEvent](params, ...args);
	};
	return ons;
}
/**
* 组件事件处理
* @param renderOpts
* @param params
* @param modelFunc
* @param changeFunc
*/
function getComponentOns(renderOpts, params, eFns, eventOns) {
	const { events } = renderOpts;
	const { model: modelFunc, change: changeFunc } = eFns || {};
	const modelEvent = getModelEvent(renderOpts);
	const changeEvent = getChangeEvent(renderOpts);
	const ons = {};
	import_xe_utils.default.objectEach(events, (func, key) => {
		ons[getOnName(key)] = function(...args) {
			if (!import_xe_utils.default.isFunction(func)) errLog("vxe.error.errFunc", [`[form] ${func}`]);
			func(params, ...args);
		};
	});
	if (modelFunc) ons[getOnName(modelEvent)] = function(targetEvnt) {
		modelFunc(targetEvnt);
		if (events && events[modelEvent]) events[modelEvent](params, targetEvnt);
	};
	if (changeFunc) ons[getOnName(changeEvent)] = function(...args) {
		changeFunc(...args);
		if (events && events[changeEvent]) events[changeEvent](params, ...args);
	};
	return eventOns ? Object.assign(ons, eventOns) : ons;
}
function getItemOns(renderOpts, params) {
	const { $form, data, field } = params;
	return getComponentOns(renderOpts, params, {
		model(value) {
			import_xe_utils.default.set(data, field, value);
		},
		change() {
			$form.updateStatus(params);
			if (renderOpts.changeToSubmit) $form.handleSubmitEvent(new Event("change"));
		}
	});
}
function getNativeItemOns(renderOpts, params) {
	const { $form, data, field } = params;
	return getNativeElementOns(renderOpts, params, (evnt) => {
		const itemValue = evnt.target.value;
		import_xe_utils.default.set(data, field, itemValue);
	}, () => {
		$form.updateStatus(params);
	});
}
function renderNativeOptgroup(renderOpts, params, renderOptionsMethods) {
	const { optionGroups, optionGroupProps = {} } = renderOpts;
	const groupOptions = optionGroupProps.options || "options";
	const groupLabel = optionGroupProps.label || "label";
	if (optionGroups) return optionGroups.map((group, gIndex) => {
		return h("optgroup", {
			key: gIndex,
			label: group[groupLabel]
		}, renderOptionsMethods(group[groupOptions], renderOpts, params));
	});
	return [];
}
/**
* 渲染表单-项
* 用于渲染原生的标签
*/
function nativeItemRender(renderOpts, params) {
	const { data, field } = params;
	const { name } = renderOpts;
	const attrs = getNativeAttrs(renderOpts);
	const itemValue = import_xe_utils.default.get(data, field);
	return [h(`${name}`, Object.assign(Object.assign(Object.assign({ class: `vxe-default-${name}` }, attrs), { value: attrs && name === "input" && (attrs.type === "submit" || attrs.type === "reset") ? null : itemValue }), getNativeItemOns(renderOpts, params)))];
}
function defaultItemRender(renderOpts, params) {
	const { data, field } = params;
	const itemValue = import_xe_utils.default.get(data, field);
	return [h(getDefaultComponent(renderOpts), Object.assign(Object.assign({}, getComponentFormItemProps(renderOpts, params, itemValue)), getItemOns(renderOpts, params)))];
}
/**
* 已废弃
* @deprecated
*/
function oldItemRender(renderOpts, params) {
	const { data, field } = params;
	const itemValue = import_xe_utils.default.get(data, field);
	return [h(getOldComponent(renderOpts), Object.assign(Object.assign({}, getComponentFormItemProps(renderOpts, params, itemValue)), getItemOns(renderOpts, params)))];
}
/**
* 已废弃
* @deprecated
*/
function oldButtonItemRender(renderOpts, params) {
	return [h(resolveComponent("vxe-button"), Object.assign(Object.assign({}, getComponentFormItemProps(renderOpts, params, null)), getComponentOns(renderOpts, params)))];
}
/**
* 已废弃
* @deprecated
*/
function oldButtonsItemRender(renderOpts, params) {
	const { children } = renderOpts;
	return children ? children.map((childRenderOpts) => oldButtonItemRender(childRenderOpts, params)[0]) : [];
}
/**
* 渲染原生的 select 标签
*/
function renderNativeFormOptions(options, renderOpts, params) {
	const { data, field } = params;
	const { optionProps = {} } = renderOpts;
	const labelProp = optionProps.label || "label";
	const valueProp = optionProps.value || "value";
	const disabledProp = optionProps.disabled || "disabled";
	const cellValue = import_xe_utils.default.get(data, field);
	if (options) return options.map((item, oIndex) => {
		return h("option", {
			key: oIndex,
			value: item[valueProp],
			disabled: item[disabledProp],
			selected: item[valueProp] == cellValue
		}, item[labelProp]);
	});
	return [];
}
/**
* 渲染表单-项
*/
function defaultFormItemRender(renderOpts, params) {
	const { data, field } = params;
	const itemValue = import_xe_utils.default.get(data, field);
	return [h(getDefaultComponent(renderOpts), Object.assign(Object.assign({}, getComponentFormItemProps(renderOpts, params, itemValue)), getItemOns(renderOpts, params)))];
}
function formItemRadioAndCheckboxRender(renderOpts, params) {
	const { options, optionProps } = renderOpts;
	const { data, field } = params;
	const itemValue = import_xe_utils.default.get(data, field);
	return [h(getDefaultComponent(renderOpts), Object.assign(Object.assign({
		options,
		optionProps
	}, getComponentFormItemProps(renderOpts, params, itemValue)), getItemOns(renderOpts, params)))];
}
/**
* 已废弃
* @deprecated
*/
function oldFormItemRadioAndCheckboxRender(renderOpts, params) {
	const { name, options, optionProps = {} } = renderOpts;
	const { data, field } = params;
	const labelProp = optionProps.label || "label";
	const valueProp = optionProps.value || "value";
	const disabledProp = optionProps.disabled || "disabled";
	const itemValue = import_xe_utils.default.get(data, field);
	const compName = getOldComponentName(name);
	if (options) return [h(resolveComponent(`${compName}-group`), Object.assign(Object.assign({}, getComponentFormItemProps(renderOpts, params, itemValue)), getItemOns(renderOpts, params)), { default: () => {
		return options.map((item, index) => {
			return h(resolveComponent(compName), {
				key: index,
				label: item[valueProp],
				content: item[labelProp],
				disabled: item[disabledProp]
			});
		});
	} })];
	return [h(resolveComponent(compName), Object.assign(Object.assign({}, getComponentFormItemProps(renderOpts, params, itemValue)), getItemOns(renderOpts, params)))];
}
/**
* 表单 - 渲染器
*/
renderer.mixin({
	input: {
		formItemAutoFocus: "input",
		renderFormItemContent: nativeItemRender
	},
	textarea: {
		formItemAutoFocus: "textarea",
		renderFormItemContent: nativeItemRender
	},
	select: {
		formItemAutoFocus: "input",
		renderFormItemContent(renderOpts, params) {
			return [h("select", Object.assign(Object.assign({ class: "vxe-default-select" }, getNativeAttrs(renderOpts)), getNativeItemOns(renderOpts, params)), renderOpts.optionGroups ? renderNativeOptgroup(renderOpts, params, renderNativeFormOptions) : renderNativeFormOptions(renderOpts.options, renderOpts, params))];
		}
	},
	VxeInput: {
		formItemAutoFocus: "input",
		renderFormItemContent: defaultItemRender
	},
	VxeNumberInput: {
		formItemAutoFocus: "input",
		renderFormItemContent: defaultItemRender
	},
	VxePasswordInput: {
		formItemAutoFocus: "input",
		renderFormItemContent: defaultItemRender
	},
	VxeTextarea: {
		formItemAutoFocus: "textarea",
		renderFormItemContent: defaultItemRender
	},
	VxeDatePicker: {
		formItemAutoFocus: "input",
		renderFormItemContent: defaultItemRender
	},
	VxeDateRangePicker: {
		formItemAutoFocus: "input",
		renderFormItemContent(renderOpts, params) {
			const { startField, endField } = renderOpts;
			const { $form, data, field } = params;
			const itemValue = import_xe_utils.default.get(data, field);
			const seProps = {};
			const seOs = {};
			if (startField && endField) {
				seProps.startValue = import_xe_utils.default.get(data, startField);
				seProps.endValue = import_xe_utils.default.get(data, endField);
				seOs["onUpdate:startValue"] = (value) => {
					if (startField) import_xe_utils.default.set(data, startField, value);
				};
				seOs["onUpdate:endValue"] = (value) => {
					if (endField) import_xe_utils.default.set(data, endField, value);
				};
			}
			return [h(getDefaultComponent(renderOpts), Object.assign(Object.assign({}, getComponentFormItemProps(renderOpts, params, itemValue, seProps)), getComponentOns(renderOpts, params, {
				model(value) {
					import_xe_utils.default.set(data, field, value);
				},
				change({ $event, isFinish }) {
					$form.updateStatus(params);
					if (renderOpts.changeToSubmit && isFinish) $form.handleSubmitEvent($event);
				}
			}, seOs)))];
		}
	},
	VxeButton: { renderFormItemContent: defaultFormItemRender },
	VxeButtonGroup: { renderFormItemContent(renderOpts, params) {
		const { options } = renderOpts;
		const { data, field } = params;
		const itemValue = import_xe_utils.default.get(data, field);
		return [h(getDefaultComponent(renderOpts), Object.assign(Object.assign({ options }, getComponentFormItemProps(renderOpts, params, itemValue)), getItemOns(renderOpts, params)))];
	} },
	VxeSelect: {
		formItemAutoFocus: "input",
		renderFormItemContent(renderOpts, params) {
			const { data, field } = params;
			const { options, optionProps, optionGroups, optionGroupProps } = renderOpts;
			const itemValue = import_xe_utils.default.get(data, field);
			return [h(getDefaultComponent(renderOpts), Object.assign(Object.assign({}, getComponentFormItemProps(renderOpts, params, itemValue, {
				options,
				optionProps,
				optionGroups,
				optionGroupProps
			})), getItemOns(renderOpts, params)))];
		}
	},
	VxeTreeSelect: {
		formItemAutoFocus: "input",
		renderFormItemContent(renderOpts, params) {
			const { data, field } = params;
			const { options, optionProps } = renderOpts;
			const itemValue = import_xe_utils.default.get(data, field);
			return [h(getDefaultComponent(renderOpts), Object.assign(Object.assign({}, getComponentFormItemProps(renderOpts, params, itemValue, {
				options,
				optionProps
			})), getItemOns(renderOpts, params)))];
		}
	},
	VxeTableSelect: {
		formItemAutoFocus: "input",
		renderFormItemContent(renderOpts, params) {
			const { data, field } = params;
			const { options, optionProps } = renderOpts;
			const itemValue = import_xe_utils.default.get(data, field);
			return [h(getDefaultComponent(renderOpts), Object.assign(Object.assign({}, getComponentFormItemProps(renderOpts, params, itemValue, {
				options,
				optionProps
			})), getItemOns(renderOpts, params)))];
		}
	},
	VxeColorPicker: {
		formItemAutoFocus: "input",
		renderFormItemContent(renderOpts, params) {
			const { data, field } = params;
			const { options } = renderOpts;
			const itemValue = import_xe_utils.default.get(data, field);
			return [h(getDefaultComponent(renderOpts), Object.assign(Object.assign({}, getComponentFormItemProps(renderOpts, params, itemValue, { colors: options })), getItemOns(renderOpts, params)))];
		}
	},
	VxeIconPicker: {
		formItemAutoFocus: "input",
		renderFormItemContent(renderOpts, params) {
			const { data, field } = params;
			const { options } = renderOpts;
			const itemValue = import_xe_utils.default.get(data, field);
			return [h(getDefaultComponent(renderOpts), Object.assign(Object.assign({}, getComponentFormItemProps(renderOpts, params, itemValue, { icons: options })), getItemOns(renderOpts, params)))];
		}
	},
	VxeRadio: { renderFormItemContent: defaultFormItemRender },
	VxeRadioGroup: { renderFormItemContent: formItemRadioAndCheckboxRender },
	VxeCheckbox: { renderFormItemContent: defaultFormItemRender },
	VxeCheckboxGroup: { renderFormItemContent: formItemRadioAndCheckboxRender },
	VxeSwitch: { renderFormItemContent: defaultItemRender },
	VxeRate: { renderFormItemContent: defaultItemRender },
	VxeSlider: { renderFormItemContent: defaultItemRender },
	VxeImage: { renderFormItemContent(renderOpts, params) {
		const { data, field } = params;
		const { props } = renderOpts;
		const itemValue = import_xe_utils.default.get(data, field);
		return [h(getDefaultComponent(renderOpts), Object.assign(Object.assign(Object.assign({}, props), { src: itemValue }), getItemOns(renderOpts, params)))];
	} },
	VxeImageGroup: { renderFormItemContent(renderOpts, params) {
		const { data, field } = params;
		const { props } = renderOpts;
		const itemValue = import_xe_utils.default.get(data, field);
		return [h(getDefaultComponent(renderOpts), Object.assign(Object.assign(Object.assign({}, props), { urlList: itemValue }), getItemOns(renderOpts, params)))];
	} },
	VxeUpload: { renderFormItemContent: defaultItemRender },
	$input: {
		formItemAutoFocus: "input",
		renderFormItemContent: oldItemRender
	},
	$textarea: {
		formItemAutoFocus: "textarea",
		renderFormItemContent: oldItemRender
	},
	$button: { renderFormItemContent: oldButtonItemRender },
	$buttons: { renderFormItemContent: oldButtonsItemRender },
	$select: {
		formItemAutoFocus: "input",
		renderFormItemContent(renderOpts, params) {
			const { data, field } = params;
			const { options, optionProps, optionGroups, optionGroupProps } = renderOpts;
			const itemValue = import_xe_utils.default.get(data, field);
			return [h(getOldComponent(renderOpts), Object.assign(Object.assign({}, getComponentFormItemProps(renderOpts, params, itemValue, {
				options,
				optionProps,
				optionGroups,
				optionGroupProps
			})), getItemOns(renderOpts, params)))];
		}
	},
	$radio: { renderFormItemContent: oldFormItemRadioAndCheckboxRender },
	$checkbox: { renderFormItemContent: oldFormItemRadioAndCheckboxRender },
	$switch: { renderFormItemContent: oldItemRender }
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/form/src/form.js
var Rule = class {
	constructor(rule) {
		Object.assign(this, {
			$options: rule,
			required: rule.required,
			min: rule.min,
			max: rule.min,
			type: rule.type,
			pattern: rule.pattern,
			validator: rule.validator,
			trigger: rule.trigger,
			maxWidth: rule.maxWidth
		});
	}
	get content() {
		return getFuncText(this.$options.content || this.$options.message);
	}
	get message() {
		return this.content;
	}
};
function validREValue(pattern, val) {
	if (pattern && !(import_xe_utils.default.isRegExp(pattern) ? pattern : new RegExp(pattern)).test(val)) return false;
	return true;
}
function validMaxValue(max, num) {
	if (!import_xe_utils.default.eqNull(max) && num > import_xe_utils.default.toNumber(max)) return false;
	return true;
}
function validMinValue(min, num) {
	if (!import_xe_utils.default.eqNull(min) && num < import_xe_utils.default.toNumber(min)) return false;
	return true;
}
function validRuleValue(rule, val, required) {
	const { type, min, max, pattern } = rule;
	const isArrType = type === "array";
	const isNumType = type === "number";
	const isStrType = type === "string";
	const strVal = `${val}`;
	if (!validREValue(pattern, strVal)) return false;
	if (isArrType) {
		if (!import_xe_utils.default.isArray(val)) return false;
		if (required) {
			if (!val.length) return false;
		}
		if (!validMinValue(min, val.length)) return false;
		if (!validMaxValue(max, val.length)) return false;
	} else if (isNumType) {
		const numVal = Number(val);
		if (isNaN(numVal)) return false;
		if (!validMinValue(min, numVal)) return false;
		if (!validMaxValue(max, numVal)) return false;
	} else {
		if (isStrType) {
			if (!import_xe_utils.default.isString(val)) return false;
		}
		if (required) {
			if (!strVal) return false;
		}
		if (!validMinValue(min, strVal.length)) return false;
		if (!validMaxValue(max, strVal.length)) return false;
	}
	return true;
}
function checkRuleStatus(rule, data, val) {
	const { required, field } = rule;
	const currVal = field ? import_xe_utils.default.get(data, field) : val;
	const isEmptyVal = import_xe_utils.default.isArray(currVal) ? !currVal.length : eqEmptyValue(currVal);
	if (required) {
		if (isEmptyVal) return false;
		if (!validRuleValue(rule, currVal, required)) return false;
	} else if (!isEmptyVal) {
		if (!validRuleValue(rule, currVal, required)) return false;
	}
	return true;
}
function createInternalData$6() {
	return {
		meTimeout: void 0,
		stTimeout: void 0,
		tooltipStore: {
			item: null,
			visible: false
		},
		itemFormatCache: {}
	};
}
var form_default = defineVxeComponent({
	name: "VxeForm",
	props: {
		collapseStatus: {
			type: Boolean,
			default: true
		},
		loading: Boolean,
		data: Object,
		size: {
			type: String,
			default: () => getConfig().form.size || getConfig().size
		},
		span: {
			type: [String, Number],
			default: () => getConfig().form.span
		},
		align: {
			type: String,
			default: () => getConfig().form.align
		},
		verticalAlign: {
			type: String,
			default: () => getConfig().form.verticalAlign
		},
		border: {
			type: Boolean,
			default: () => getConfig().form.border
		},
		titleBackground: {
			type: Boolean,
			default: () => getConfig().form.titleBackground
		},
		titleBold: {
			type: Boolean,
			default: () => getConfig().form.titleBold
		},
		titleAlign: {
			type: String,
			default: () => getConfig().form.titleAlign
		},
		titleWidth: {
			type: [String, Number],
			default: () => getConfig().form.titleWidth
		},
		titleColon: {
			type: Boolean,
			default: () => getConfig().form.titleColon
		},
		titleAsterisk: {
			type: Boolean,
			default: () => getConfig().form.titleAsterisk
		},
		titleOverflow: {
			type: [Boolean, String],
			default: () => getConfig().form.titleOverflow
		},
		vertical: {
			type: Boolean,
			default: () => getConfig().form.vertical
		},
		padding: {
			type: Boolean,
			default: () => getConfig().form.padding
		},
		className: [String, Function],
		readonly: Boolean,
		disabled: Boolean,
		items: Array,
		rules: Object,
		preventSubmit: {
			type: Boolean,
			default: () => getConfig().form.preventSubmit
		},
		validConfig: Object,
		tooltipConfig: Object,
		collapseConfig: Object,
		customLayout: {
			type: Boolean,
			default: () => getConfig().form.customLayout
		},
		params: Object
	},
	emits: [
		"update:collapseStatus",
		"collapse",
		"toggle-collapse",
		"submit",
		"submit-invalid",
		"reset"
	],
	setup(props, context) {
		const { slots, emit } = context;
		const $xeGrid = inject("$xeGrid", null);
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const reactData = reactive({
			collapseAll: props.collapseStatus,
			staticItems: [],
			formItems: [],
			itemWidth: 0
		});
		const internalData = createInternalData$6();
		const refElem = ref();
		const refTooltip = ref();
		let formMethods = {};
		const computeValidOpts = computed(() => {
			return Object.assign({}, getConfig().form.validConfig, props.validConfig);
		});
		const computeTooltipOpts = computed(() => {
			return Object.assign({}, getConfig().tooltip, getConfig().form.tooltipConfig, props.tooltipConfig);
		});
		const computeCollapseOpts = computed(() => {
			return Object.assign({}, getConfig().form.collapseConfig, props.collapseConfig);
		});
		const computeAutoItemWidthList = computed(() => {
			const { titleWidth: allTitleWidth, vertical: allVertical } = props;
			const { formItems } = reactData;
			const itemList = [];
			import_xe_utils.default.eachTree(formItems, (item) => {
				const { titleWidth, vertical } = item;
				if (titleWidth === "auto") itemList.push(item);
				else if (((import_xe_utils.default.eqNull(vertical) ? allVertical : vertical) ? null : import_xe_utils.default.eqNull(titleWidth) ? allTitleWidth : titleWidth) === "auto" && (!item.children || !item.children.length)) itemList.push(item);
			}, { children: "children" });
			return itemList;
		});
		const refMaps = { refElem };
		const computeMaps = {
			computeSize,
			computeValidOpts,
			computeTooltipOpts,
			computeCollapseOpts,
			computeAutoItemWidthList
		};
		const $xeForm = {
			xID,
			props,
			context,
			reactData,
			internalData,
			xeGrid: $xeGrid,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const callSlot = (slotFunc, params) => {
			if (slotFunc) {
				if (import_xe_utils.default.isString(slotFunc)) slotFunc = slots[slotFunc] || null;
				if (import_xe_utils.default.isFunction(slotFunc)) return getSlotVNs$1(slotFunc(params));
			}
			return [];
		};
		const loadItem = (list) => {
			if (list.length) list.forEach((item) => {
				if (item.slots) import_xe_utils.default.each(item.slots, (func) => {
					if (!import_xe_utils.default.isFunction(func)) {
						if (!slots[func]) errLog("vxe.error.notSlot", [`[form] ${func}`]);
					}
				});
			});
			reactData.staticItems = import_xe_utils.default.mapTree(list, (item) => createItem($xeForm, item), { children: "children" });
			internalData.itemFormatCache = {};
			return nextTick().then(() => {
				return recalculate();
			});
		};
		const getItems = () => {
			const itemList = [];
			import_xe_utils.default.eachTree(reactData.formItems, (item) => {
				itemList.push(item);
			}, { children: "children" });
			return itemList;
		};
		const getItemByField = (field) => {
			const rest = import_xe_utils.default.findTree(reactData.formItems, (item) => item.field === field, { children: "children" });
			return rest ? rest.item : null;
		};
		const getCollapseStatus = () => {
			return reactData.collapseAll;
		};
		const toggleCollapse = () => {
			const status = !getCollapseStatus();
			reactData.collapseAll = status;
			emit("update:collapseStatus", status);
			return nextTick();
		};
		const toggleCollapseEvent = (evnt) => {
			const actionRest = toggleCollapse();
			const status = getCollapseStatus();
			formMethods.dispatchEvent("toggle-collapse", {
				status,
				collapse: status,
				data: props.data
			}, evnt);
			formMethods.dispatchEvent("collapse", {
				status,
				collapse: status,
				data: props.data
			}, evnt);
			actionRest.then(() => {
				recalculate().then(() => {
					if ($xeGrid) $xeGrid.recalculate(true);
				});
				if ($xeGrid) $xeGrid.recalculate();
			});
		};
		const clearValidate = (fieldOrItem) => {
			if (fieldOrItem) {
				let fields = fieldOrItem;
				if (!import_xe_utils.default.isArray(fieldOrItem)) fields = [fieldOrItem];
				fields.forEach((field) => {
					if (field) {
						const item = handleFieldOrItem($xeForm, field);
						if (item) {
							item.showError = false;
							item.showIconMsg = false;
						}
					}
				});
			} else getItems().forEach((item) => {
				item.showError = false;
				item.showIconMsg = false;
			});
			return nextTick();
		};
		const getResetValue = (item, data, itemValue) => {
			const { field, resetValue } = item;
			if (import_xe_utils.default.isFunction(resetValue)) return resetValue({
				field,
				item,
				data,
				$form: $xeForm,
				$grid: $xeGrid
			});
			else if (import_xe_utils.default.eqNull(resetValue)) {
				if (import_xe_utils.default.isArray(itemValue)) return [];
			}
			return resetValue;
		};
		const reset = () => {
			const { data } = props;
			const itemList = getItems();
			if (data) itemList.forEach((item) => {
				const { field, itemRender } = item;
				if (isEnableConf(itemRender)) {
					const { name, startField, endField } = itemRender;
					const compConf = renderer.get(name);
					const fiResetMethod = compConf ? compConf.formItemResetMethod || compConf.itemResetMethod : null;
					if (compConf && fiResetMethod) fiResetMethod({
						data,
						field,
						property: field,
						item,
						$form: $xeForm,
						$grid: $xeGrid
					});
					else if (field) {
						const itemValue = import_xe_utils.default.get(data, field);
						import_xe_utils.default.set(data, field, getResetValue(item, data, itemValue));
					}
					if (startField && endField) {
						import_xe_utils.default.set(data, startField, getResetValue(item, data, import_xe_utils.default.get(data, startField)));
						import_xe_utils.default.set(data, endField, getResetValue(item, data, import_xe_utils.default.get(data, endField)));
					}
				}
			});
			internalData.itemFormatCache = {};
			clearValidate();
			return recalculate();
		};
		const resetEvent = (evnt) => {
			evnt.preventDefault();
			reset();
			formMethods.dispatchEvent("reset", { data: props.data }, evnt);
		};
		const handleFocus = (fields) => {
			const el = refElem.value;
			if (el) for (let i = 0; i < fields.length; i++) {
				const field = fields[i];
				const item = getItemByField(field);
				if (item && isEnableConf(item.itemRender)) {
					const { itemRender } = item;
					const compConf = renderer.get(itemRender.name);
					if (!i) scrollToView(el.querySelector(`.${item.id}`));
					let inputElem = null;
					const autoFocus = itemRender.autoFocus || itemRender.autofocus || (compConf ? compConf.formItemAutoFocus : null);
					if (import_xe_utils.default.isFunction(autoFocus)) inputElem = autoFocus({
						$form: $xeForm,
						$grid: $xeGrid,
						item,
						data: props.data,
						field
					});
					else if (autoFocus === true) inputElem = el.querySelector(`.${item.id} input,textarea`);
					else if (autoFocus) inputElem = el.querySelector(`.${item.id} ${autoFocus}`);
					if (inputElem) {
						inputElem.focus();
						break;
					}
				}
			}
		};
		/**
		* 校验数据
		* 按表格行、列顺序依次校验（同步或异步）
		* 校验规则根据索引顺序依次校验，如果是异步则会等待校验完成才会继续校验下一列
		* 如果校验失败则，触发回调或者 Promise<(ErrMap 校验不通过列的信息)>
		* 如果是传回调方式这返回一个 (ErrMap 校验不通过列的信息)
		*
		* rule 配置：
		*  required=Boolean 是否必填
		*  min=Number 最小长度
		*  max=Number 最大长度
		*  validator=Function({ itemValue, rule, rules, data, property }) 自定义校验，接收一个 Promise
		*  trigger=change 触发方式
		*/
		const validItemRules = (validType, fields, val) => {
			const { data, rules: formRules } = props;
			const errorMaps = {};
			if (!import_xe_utils.default.isArray(fields)) fields = [fields];
			return Promise.all(fields.map((property) => {
				const errorRules = [];
				const syncVailds = [];
				if (property && formRules) {
					const rules = import_xe_utils.default.get(formRules, property);
					if (rules) {
						const itemValue = import_xe_utils.default.isUndefined(val) ? import_xe_utils.default.get(data, property) : val;
						rules.forEach((rule) => {
							const { trigger, validator } = rule;
							if (validType === "all" || !trigger || validType === trigger) {
								if (validator) {
									const validParams = {
										itemValue,
										rule,
										rules,
										data,
										field: property,
										property,
										$form: $xeForm
									};
									let customValid;
									if (import_xe_utils.default.isString(validator)) {
										const gvItem = validators.get(validator);
										if (gvItem) {
											const validatorMethod = gvItem.formItemValidatorMethod || gvItem.itemValidatorMethod;
											if (validatorMethod) customValid = validatorMethod(validParams);
											else warnLog("vxe.error.notValidators", [`[form] ${validator}`]);
										} else errLog("vxe.error.notValidators", [`[form] ${validator}`]);
									} else customValid = validator(validParams);
									if (customValid) {
										if (import_xe_utils.default.isError(customValid)) errorRules.push(new Rule({
											type: "custom",
											trigger,
											content: customValid.message,
											rule: new Rule(rule)
										}));
										else if (customValid.catch) syncVailds.push(customValid.catch((e) => {
											errorRules.push(new Rule({
												type: "custom",
												trigger,
												content: e ? e.message : rule.content || rule.message,
												rule: new Rule(rule)
											}));
										}));
									}
								} else if (!checkRuleStatus(rule, data, itemValue)) errorRules.push(new Rule(rule));
							}
						});
					}
				}
				return Promise.all(syncVailds).then(() => {
					if (errorRules.length) errorMaps[property] = errorRules.map((rule) => {
						return {
							$form: $xeForm,
							rule,
							data,
							field: property,
							property
						};
					});
				});
			})).then(() => {
				if (!import_xe_utils.default.isEmpty(errorMaps)) return Promise.reject(errorMaps);
			});
		};
		const beginValidate = (itemList, type, callback) => {
			const { data, rules: formRules } = props;
			const validOpts = computeValidOpts.value;
			const validRest = {};
			const validFields = [];
			const itemValids = [];
			clearTimeout(internalData.meTimeout);
			if (data && formRules) {
				itemList.forEach((item) => {
					const { field } = item;
					if (field && !isHiddenItem($xeForm, item) && isActiveItem($xeForm, item)) itemValids.push(validItemRules(type || "all", field).then(() => {
						item.errRule = null;
					}).catch((errorMaps) => {
						const rest = errorMaps[field];
						if (!validRest[field]) validRest[field] = [];
						validRest[field].push(rest);
						validFields.push(field);
						item.errRule = rest[0].rule;
						return Promise.reject(rest);
					}));
				});
				return Promise.all(itemValids).then(() => {
					if (callback) callback();
				}).catch(() => {
					return new Promise((resolve) => {
						internalData.meTimeout = setTimeout(() => {
							itemList.forEach((item) => {
								if (item.errRule) item.showError = true;
							});
						}, 20);
						if (validOpts.autoPos !== false) nextTick(() => {
							handleFocus(validFields);
						});
						if (callback) {
							callback(validRest);
							resolve();
						} else resolve(validRest);
					});
				});
			}
			if (callback) callback();
			return Promise.resolve();
		};
		const validate = (callback) => {
			const { readonly } = props;
			clearValidate();
			if (readonly) return nextTick();
			return beginValidate(getItems(), "", callback).then((params) => {
				recalculate();
				return params;
			});
		};
		const validateField = (fieldOrItem, callback) => {
			const { readonly } = props;
			if (readonly) return nextTick();
			let fields = [];
			if (fieldOrItem) if (import_xe_utils.default.isArray(fieldOrItem)) fields = fieldOrItem;
			else fields = [fieldOrItem];
			return beginValidate(fields.map((field) => handleFieldOrItem($xeForm, field)), "", callback).then((params) => {
				recalculate();
				return params;
			});
		};
		const handleSubmitEvent = (evnt) => {
			const { readonly } = props;
			clearValidate();
			if (readonly) {
				$xeForm.dispatchEvent("submit", { data: props.data }, evnt);
				recalculate();
				return;
			}
			beginValidate(getItems()).then((errMap) => {
				if (errMap) $xeForm.dispatchEvent("submit-invalid", {
					data: props.data,
					errMap
				}, evnt);
				else $xeForm.dispatchEvent("submit", { data: props.data }, evnt);
				recalculate();
			});
		};
		const submitEvent = (evnt) => {
			evnt.preventDefault();
			if (!props.preventSubmit) handleSubmitEvent(evnt);
		};
		const closeTooltip = () => {
			const { tooltipStore } = internalData;
			const $tooltip = refTooltip.value;
			if (tooltipStore.visible) {
				Object.assign(tooltipStore, {
					item: null,
					visible: false
				});
				if ($tooltip) $tooltip.close();
			}
			return nextTick();
		};
		const triggerTitleTipEvent = (evnt, params) => {
			const { item } = params;
			const { tooltipStore } = internalData;
			const $tooltip = refTooltip.value;
			const overflowElem = evnt.currentTarget.children[0];
			const content = (overflowElem.textContent || "").trim();
			const isCellOverflow = overflowElem.scrollWidth > overflowElem.clientWidth;
			clearTimeout(internalData.stTimeout);
			if (tooltipStore.item !== item) closeTooltip();
			if (content && isCellOverflow) {
				Object.assign(tooltipStore, {
					item,
					visible: true
				});
				if ($tooltip) $tooltip.open(overflowElem, content);
			}
		};
		const handleTitleTipLeaveEvent = () => {
			const tooltipOpts = computeTooltipOpts.value;
			let $tooltip = refTooltip.value;
			if ($tooltip) $tooltip.setActived(false);
			if (tooltipOpts.enterable) internalData.stTimeout = setTimeout(() => {
				$tooltip = refTooltip.value;
				if ($tooltip && !$tooltip.isActived()) closeTooltip();
			}, tooltipOpts.leaveDelay);
			else closeTooltip();
		};
		const triggerItemEvent = (evnt, field, itemValue) => {
			if (field) return validItemRules(evnt ? ["blur"].includes(evnt.type) ? "blur" : "change" : "all", field, itemValue).then(() => {
				clearValidate(field);
			}).catch((errorMaps) => {
				const rest = errorMaps[field];
				const item = getItemByField(field);
				if (rest && item) {
					item.showError = true;
					item.errRule = rest[0].rule;
				}
			});
			return nextTick();
		};
		/**
		* 更新项状态
		* 如果组件值 v-model 发生 change 时，调用改函数用于更新某一项编辑状态
		* 如果单元格配置了校验规则，则会进行校验
		*/
		const updateStatus = (scope, itemValue) => {
			const { field } = scope;
			return triggerItemEvent(new Event("change"), field, itemValue);
		};
		const recalculate = () => {
			const autoItemWidthList = computeAutoItemWidthList.value;
			const el = refElem.value;
			if (el && autoItemWidthList.length) {
				const itemElList = el.querySelectorAll(autoItemWidthList.map((item) => `.vxe-form--item-title[itemid="${item.id}"]`).join(","));
				let maxItemWidth = 0;
				import_xe_utils.default.arrayEach(itemElList, (itemEl) => {
					itemEl.style.width = "";
					maxItemWidth = Math.max(maxItemWidth, Math.ceil(itemEl.clientWidth + 2));
				});
				import_xe_utils.default.arrayEach(itemElList, (itemEl) => {
					itemEl.style.width = `${maxItemWidth}px`;
				});
			}
			return nextTick();
		};
		const handleGlobalResizeEvent = () => {
			recalculate();
		};
		formMethods = {
			dispatchEvent(type, params, evnt) {
				emit(type, createEvent(evnt, {
					$form: $xeForm,
					$grid: $xeGrid
				}, params));
			},
			reset,
			validate,
			validateField,
			clearValidate,
			updateStatus,
			toggleCollapse,
			getItems,
			getItemByField,
			closeTooltip,
			recalculate
		};
		Object.assign($xeForm, formMethods, {
			callSlot,
			triggerItemEvent,
			toggleCollapseEvent,
			triggerTitleTipEvent,
			handleTitleTipLeaveEvent,
			handleValidIconEvent(evnt, params) {
				const { item } = params;
				item.showIconMsg = !item.showIconMsg;
			},
			handleSubmitEvent
		});
		const renderVN = () => {
			const { loading, border, className, data, customLayout } = props;
			const { formItems } = reactData;
			const vSize = computeSize.value;
			const tooltipOpts = computeTooltipOpts.value;
			const defaultSlot = slots.default;
			return h("form", {
				ref: refElem,
				class: [
					"vxe-form",
					className ? import_xe_utils.default.isFunction(className) ? className({
						items: formItems,
						data,
						$form: $xeForm
					}) : className : "",
					{
						[`size--${vSize}`]: vSize,
						"is--border": border,
						"custom--layout": customLayout,
						"is--loading": loading
					}
				],
				onSubmit: submitEvent,
				onReset: resetEvent
			}, [
				h("div", { class: "vxe-form--wrapper vxe-form--item-row" }, customLayout ? defaultSlot ? defaultSlot({}) : [] : formItems.map((item, index) => {
					return h(VxeFormConfigItem, {
						key: index,
						itemConfig: item
					});
				})),
				h("div", {
					class: "vxe-form-slots",
					ref: "hideItem"
				}, customLayout ? [] : defaultSlot ? defaultSlot({}) : []),
				h(VxeLoading, {
					class: "vxe-form--loading",
					modelValue: loading
				}),
				h(VxeTooltip, Object.assign({ ref: refTooltip }, tooltipOpts))
			]);
		};
		const recalcFlag = ref(0);
		watch(() => props.vertical, () => {
			recalcFlag.value++;
		});
		watch(() => props.titleWidth, () => {
			recalcFlag.value++;
		});
		watch(recalcFlag, () => {
			nextTick().then(() => {
				recalculate();
			});
		});
		const staticItemFlag = ref(0);
		watch(() => reactData.staticItems.length, () => {
			staticItemFlag.value++;
		});
		watch(() => reactData.staticItems, () => {
			staticItemFlag.value++;
		});
		watch(staticItemFlag, () => {
			reactData.formItems = reactData.staticItems;
			recalcFlag.value++;
		});
		const itemFlag = ref(0);
		watch(() => props.items ? props.items.length : -1, () => {
			itemFlag.value++;
		});
		watch(() => props.items, () => {
			itemFlag.value++;
		});
		watch(itemFlag, () => {
			loadItem(props.items || []);
		});
		watch(() => props.collapseStatus, (value) => {
			reactData.collapseAll = !!value;
		});
		watch(() => props.readonly, () => {
			clearValidate();
		});
		watch(() => props.disabled, () => {
			clearValidate();
		});
		onMounted(() => {
			nextTick(() => {
				if (props.customLayout && props.items) errLog("vxe.error.errConflicts", ["[form] custom-layout", "items"]);
			});
			globalEvents.on($xeForm, "resize", handleGlobalResizeEvent);
		});
		onUnmounted(() => {
			globalEvents.off($xeForm, "resize");
			import_xe_utils.default.assign(internalData, createInternalData$6());
		});
		if (props.items) loadItem(props.items);
		provide("xeFormItemInfo", null);
		provide("$xeForm", $xeForm);
		provide("$xeFormGroup", null);
		provide("$xeFormItem", null);
		$xeForm.renderVN = renderVN;
		return $xeForm;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/form/index.js
var VxeForm = Object.assign(form_default, { install(app) {
	app.component(form_default.name, form_default);
} });
dynamicApp.use(VxeForm);
VxeUI.component(form_default);
var Form = VxeForm;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/form/src/form-item.js
var formItemProps = {
	title: String,
	field: String,
	span: {
		type: [String, Number],
		default: null
	},
	align: {
		type: String,
		default: null
	},
	verticalAlign: {
		type: String,
		default: null
	},
	titleBackground: {
		type: Boolean,
		default: null
	},
	titleBold: {
		type: Boolean,
		default: null
	},
	titleAlign: {
		type: String,
		default: null
	},
	titleWidth: {
		type: [String, Number],
		default: null
	},
	titleColon: {
		type: Boolean,
		default: null
	},
	titleAsterisk: {
		type: Boolean,
		default: null
	},
	showTitle: {
		type: Boolean,
		default: true
	},
	vertical: {
		type: Boolean,
		default: null
	},
	padding: {
		type: Boolean,
		default: null
	},
	formatter: [String, Function],
	className: [String, Function],
	contentClassName: [String, Function],
	contentStyle: [Object, Function],
	titleClassName: [String, Function],
	titleStyle: [Object, Function],
	titleOverflow: {
		type: [Boolean, String],
		default: null
	},
	titlePrefix: Object,
	titleSuffix: Object,
	resetValue: { default: null },
	visibleMethod: Function,
	visible: {
		type: Boolean,
		default: null
	},
	showContent: {
		type: Boolean,
		default: null
	},
	folding: Boolean,
	collapseNode: Boolean,
	itemRender: Object,
	rules: Array,
	params: Object
};
var form_item_default = defineVxeComponent({
	name: "VxeFormItem",
	props: formItemProps,
	setup(props, { slots }) {
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const $xeForm = inject("$xeForm", {});
		const $xeFormGroup = inject("$xeFormGroup", null);
		const formItem = reactive(createItem($xeForm, props));
		formItem.slots = slots;
		provide("xeFormItemInfo", { itemConfig: formItem });
		const renderItem = ($xeForm, item) => {
			const formProps = $xeForm.props;
			const $xeGrid = $xeForm.xeGrid;
			const { data, readonly, disabled } = formProps;
			const { visible, field, itemRender, contentStyle, showContent } = item;
			const compConf = isEnableConf(itemRender) ? renderer.get(itemRender.name) : null;
			const itemStyle = compConf ? compConf.formItemStyle || compConf.itemStyle : null;
			const itemContentStyle = compConf ? compConf.formItemContentStyle || compConf.itemContentStyle : null;
			const params = {
				data,
				disabled,
				readonly,
				field,
				property: field,
				item,
				$form: $xeForm,
				$grid: $xeGrid
			};
			if (visible === false) return renderEmptyElement($xeFormItem);
			return h("div", {
				ref: refElem,
				key: item.id,
				itemid: item.id,
				class: getItemClass($xeForm, item),
				style: import_xe_utils.default.isFunction(itemStyle) ? itemStyle(params) : itemStyle
			}, [renderTitle($xeForm, item), showContent === false ? renderEmptyElement($xeFormItem) : h("div", {
				class: getItemContentClass($xeForm, item),
				style: Object.assign({}, import_xe_utils.default.isFunction(itemContentStyle) ? itemContentStyle(params) : itemContentStyle, import_xe_utils.default.isFunction(contentStyle) ? contentStyle(params) : contentStyle)
			}, [renderItemContent($xeForm, item), renderItemErrorIcon($xeForm, item)])]);
		};
		const renderVN = () => {
			return ($xeForm ? $xeForm.props.customLayout : false) ? renderItem($xeForm, formItem) : h("div", { ref: refElem });
		};
		const $xeFormItem = {
			xID,
			formItem,
			renderVN
		};
		watchItem(props, formItem);
		onMounted(() => {
			const elem = refElem.value;
			assembleItem($xeForm, elem, formItem, $xeFormGroup);
		});
		onUnmounted(() => {
			destroyItem($xeForm, formItem);
		});
		provide("$xeFormItem", $xeFormItem);
		provide("$xeFormGroup", null);
		return $xeFormItem;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/form/src/form-group.js
var form_group_default = defineVxeComponent({
	name: "VxeFormGroup",
	props: formItemProps,
	setup(props, context) {
		const { slots } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const $xeForm = inject("$xeForm", {});
		const $xeParentFormGroup = inject("$xeFormGroup", null);
		const formItem = reactive(createItem($xeForm, props));
		formItem.slots = slots;
		formItem.children = [];
		provide("xeFormItemInfo", { itemConfig: formItem });
		const renderVN = () => {
			const formProps = $xeForm.props;
			const $xeGrid = $xeForm.xeGrid;
			const item = formItem;
			const { data, readonly, disabled } = formProps;
			const { visible, field, itemRender, contentStyle } = item;
			const compConf = isEnableConf(itemRender) ? renderer.get(itemRender.name) : null;
			const itemStyle = compConf ? compConf.formItemStyle || compConf.itemStyle : null;
			const itemContentStyle = compConf ? compConf.formItemContentStyle || compConf.itemContentStyle : null;
			const defaultSlot = slots ? slots.default : null;
			const params = {
				data,
				disabled,
				readonly,
				field,
				property: field,
				item,
				$form: $xeForm,
				$grid: $xeGrid
			};
			if (visible === false) return renderEmptyElement($xeFormGroup);
			return h("div", {
				ref: refElem,
				key: item.id,
				itemid: item.id,
				class: getItemClass($xeForm, item, true),
				style: import_xe_utils.default.isFunction(itemStyle) ? itemStyle(params) : itemStyle
			}, [renderTitle($xeForm, item, true), h("div", {
				class: getItemContentClass($xeForm, item, true),
				style: Object.assign({}, import_xe_utils.default.isFunction(itemContentStyle) ? itemContentStyle(params) : itemContentStyle, import_xe_utils.default.isFunction(contentStyle) ? contentStyle(params) : contentStyle)
			}, defaultSlot ? defaultSlot({}) : [])]);
		};
		const $xeFormGroup = {
			xID,
			formItem,
			renderVN
		};
		watchItem(props, formItem);
		onMounted(() => {
			const elem = refElem.value;
			assembleItem($xeForm, elem, formItem, $xeParentFormGroup);
		});
		onUnmounted(() => {
			destroyItem($xeForm, formItem);
		});
		provide("$xeFormGroup", $xeFormGroup);
		provide("$xeFormItem", null);
		return $xeFormGroup;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/form-gather/index.js
var VxeFormGatherComponent = Object.assign({}, form_group_default, { name: "VxeFormGather" });
/**
* 已废弃，被 VxeFormGather 替换
* @deprecated
*/
var VxeFormGather = Object.assign(VxeFormGatherComponent, { install(app) {
	app.component(VxeFormGatherComponent.name, VxeFormGatherComponent);
} });
dynamicApp.use(VxeFormGather);
VxeUI.component(VxeFormGatherComponent);
/**
* 已废弃，被 FormGroup 替换
* @deprecated
*/
var FormGather = VxeFormGather;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/form-group/index.js
var VxeFormGroup = Object.assign(form_group_default, { install(app) {
	app.component(form_group_default.name, form_group_default);
} });
dynamicApp.use(VxeFormGroup);
VxeUI.component(form_group_default);
var FormGroup = VxeFormGroup;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/form-item/index.js
var VxeFormItem = Object.assign(form_item_default, { install(app) {
	app.component(form_item_default.name, form_item_default);
} });
dynamicApp.use(VxeFormItem);
VxeUI.component(form_item_default);
var FormItem = VxeFormItem;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/icon-picker/src/icon-picker.js
var icon_picker_default = defineVxeComponent({
	name: "VxeIconPicker",
	props: {
		modelValue: String,
		placeholder: String,
		clearable: Boolean,
		size: {
			type: String,
			default: () => getConfig().iconPicker.size || getConfig().size
		},
		className: [String, Function],
		/**
		* 已废弃，请使用 popupConfig.className
		* @deprecated
		*/
		popupClassName: [String, Function],
		showIconTitle: {
			type: Boolean,
			default: () => getConfig().iconPicker.showIconTitle
		},
		readonly: {
			type: Boolean,
			default: null
		},
		disabled: {
			type: Boolean,
			default: null
		},
		icons: Array,
		placement: String,
		popupConfig: Object,
		transfer: {
			type: Boolean,
			default: null
		}
	},
	emits: [
		"update:modelValue",
		"change",
		"clear",
		"click"
	],
	setup(props, context) {
		const { emit } = context;
		const $xeModal = inject("$xeModal", null);
		const $xeDrawer = inject("$xeDrawer", null);
		const $xeTable = inject("$xeTable", null);
		const $xeForm = inject("$xeForm", null);
		const formItemInfo = inject("xeFormItemInfo", null);
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const reactData = reactive({
			initialized: false,
			selectIcon: `${props.modelValue || ""}`,
			panelIndex: 0,
			panelStyle: {},
			panelPlacement: null,
			visiblePanel: false,
			isAniVisible: false,
			isActivated: false
		});
		const internalData = {};
		const refElem = ref();
		const refInput = ref();
		const refOptionPanel = ref();
		const refMaps = { refElem };
		const $xeIconPicker = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps
		};
		let iconPickerMethods = {};
		const computeFormReadonly = computed(() => {
			const { readonly } = props;
			if (readonly === null) {
				if ($xeForm) return $xeForm.props.readonly;
				return false;
			}
			return readonly;
		});
		const computeIsDisabled = computed(() => {
			const { disabled } = props;
			if (disabled === null) {
				if ($xeForm) return $xeForm.props.disabled;
				return false;
			}
			return disabled;
		});
		const computeBtnTransfer = computed(() => {
			const { transfer } = props;
			const popupOpts = computePopupOpts.value;
			if (import_xe_utils.default.isBoolean(popupOpts.transfer)) return popupOpts.transfer;
			if (transfer === null) {
				const globalTransfer = getConfig().iconPicker.transfer;
				if (import_xe_utils.default.isBoolean(globalTransfer)) return globalTransfer;
				if ($xeTable || $xeModal || $xeDrawer || $xeForm) return true;
			}
			return transfer;
		});
		const computeInpPlaceholder = computed(() => {
			const { placeholder } = props;
			if (placeholder) return getFuncText(placeholder);
			const globalPlaceholder = getConfig().select.placeholder;
			if (globalPlaceholder) return getFuncText(globalPlaceholder);
			return getI18n("vxe.base.pleaseSelect");
		});
		const computeIconList = computed(() => {
			let { icons } = props;
			if (!icons || !icons.length) icons = getConfig().iconPicker.icons || [];
			return icons.map((item) => {
				if (import_xe_utils.default.isString(item)) return {
					title: item,
					icon: `vxe-icon-${`${item || ""}`.replace(/^vxe-icon-/, "")}`
				};
				return {
					title: `${item.title || ""}`,
					icon: item.icon || "",
					iconRender: item.iconRender
				};
			});
		});
		const computePopupOpts = computed(() => {
			return Object.assign({}, getConfig().iconPicker.popupConfig, props.popupConfig);
		});
		const computeIconGroupList = computed(() => {
			const iconList = computeIconList.value;
			return import_xe_utils.default.chunk(iconList, 4);
		});
		const computeSelectIconItem = computed(() => {
			const { selectIcon } = reactData;
			const iconList = computeIconList.value;
			return selectIcon ? iconList.find((item) => item.icon === selectIcon) : null;
		});
		const updateZindex = () => {
			if (reactData.panelIndex < getLastZIndex()) reactData.panelIndex = nextZIndex();
		};
		const updatePlacement = () => {
			const { placement } = props;
			const { panelIndex } = reactData;
			const targetElem = refElem.value;
			const panelElem = refOptionPanel.value;
			const btnTransfer = computeBtnTransfer.value;
			const popupOpts = computePopupOpts.value;
			const handleStyle = () => {
				const ppObj = updatePanelPlacement(targetElem, panelElem, {
					placement: popupOpts.placement || placement,
					defaultPlacement: popupOpts.defaultPlacement,
					teleportTo: btnTransfer
				});
				reactData.panelStyle = Object.assign(ppObj.style, { zIndex: panelIndex });
				reactData.panelPlacement = ppObj.placement;
			};
			handleStyle();
			return nextTick().then(handleStyle);
		};
		const showOptionPanel = () => {
			const { hpTimeout } = internalData;
			if (!computeIsDisabled.value) {
				if (hpTimeout) {
					clearTimeout(hpTimeout);
					internalData.hpTimeout = void 0;
				}
				if (!reactData.initialized) reactData.initialized = true;
				reactData.isActivated = true;
				reactData.isAniVisible = true;
				setTimeout(() => {
					reactData.visiblePanel = true;
					updatePlacement();
				}, 10);
				updateZindex();
				updatePlacement();
			}
		};
		const hideOptionPanel = () => {
			reactData.visiblePanel = false;
			internalData.hpTimeout = setTimeout(() => {
				reactData.isAniVisible = false;
			}, 350);
		};
		const changeEvent = (evnt, selectValue) => {
			reactData.selectIcon = selectValue;
			if (selectValue !== props.modelValue) {
				emit("update:modelValue", selectValue);
				dispatchEvent("change", { value: selectValue }, evnt);
				if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, selectValue);
			}
		};
		const focusEvent = () => {
			if (!computeIsDisabled.value) {
				if (!reactData.visiblePanel) showOptionPanel();
			}
		};
		const blurEvent = () => {
			reactData.isActivated = false;
		};
		const clearValueEvent = (evnt, selectValue) => {
			changeEvent(evnt, selectValue);
			dispatchEvent("clear", { value: selectValue }, evnt);
		};
		const clearEvent = (params, evnt) => {
			clearValueEvent(evnt, null);
			hideOptionPanel();
		};
		const togglePanelEvent = (evnt) => {
			evnt.preventDefault();
			if (reactData.visiblePanel) hideOptionPanel();
			else showOptionPanel();
		};
		const clickEvent = (evnt) => {
			togglePanelEvent(evnt);
			dispatchEvent("click", {}, evnt);
		};
		const handleGlobalMousewheelEvent = (evnt) => {
			const { visiblePanel } = reactData;
			if (!computeIsDisabled.value) {
				if (visiblePanel) {
					const panelElem = refOptionPanel.value;
					if (getEventTargetNode(evnt, panelElem).flag) updatePlacement();
					else hideOptionPanel();
				}
			}
		};
		const handleGlobalMousedownEvent = (evnt) => {
			const { visiblePanel } = reactData;
			if (!computeIsDisabled.value) {
				const el = refElem.value;
				const panelElem = refOptionPanel.value;
				reactData.isActivated = getEventTargetNode(evnt, el).flag || getEventTargetNode(evnt, panelElem).flag;
				if (visiblePanel && !reactData.isActivated) hideOptionPanel();
			}
		};
		const handleGlobalKeydownEvent = (evnt) => {
			const { clearable } = props;
			const { visiblePanel } = reactData;
			if (!computeIsDisabled.value) {
				const isTab = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.TAB);
				const isEnter = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.ENTER);
				const isEsc = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.ESCAPE);
				const isUpArrow = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.ARROW_UP);
				const isDwArrow = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.ARROW_DOWN);
				const isDel = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.DELETE);
				const isSpacebar = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.SPACEBAR);
				if (isTab) reactData.isActivated = false;
				if (visiblePanel) {
					if (isEsc || isTab) hideOptionPanel();
					else if (isEnter) {
						evnt.preventDefault();
						evnt.stopPropagation();
					} else if (isUpArrow || isDwArrow) evnt.preventDefault();
					else if (isSpacebar) evnt.preventDefault();
				} else if ((isUpArrow || isDwArrow || isEnter || isSpacebar) && reactData.isActivated) {
					evnt.preventDefault();
					showOptionPanel();
				}
				if (reactData.isActivated) {
					if (isDel && clearable) clearValueEvent(evnt, null);
				}
			}
		};
		const handleGlobalBlurEvent = () => {
			const { visiblePanel, isActivated } = reactData;
			if (visiblePanel) hideOptionPanel();
			if (isActivated) reactData.isActivated = false;
			if (visiblePanel || isActivated) {
				const $input = refInput.value;
				if ($input) $input.blur();
			}
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $iconPicker: $xeIconPicker }, params));
		};
		iconPickerMethods = {
			dispatchEvent,
			isPanelVisible() {
				return reactData.visiblePanel;
			},
			togglePanel() {
				if (reactData.visiblePanel) hideOptionPanel();
				else showOptionPanel();
				return nextTick();
			},
			hidePanel() {
				if (reactData.visiblePanel) hideOptionPanel();
				return nextTick();
			},
			showPanel() {
				if (!reactData.visiblePanel) showOptionPanel();
				return nextTick();
			},
			focus() {
				const $input = refInput.value;
				reactData.isActivated = true;
				if ($input) $input.blur();
				return nextTick();
			},
			blur() {
				const $input = refInput.value;
				if ($input) $input.blur();
				reactData.isActivated = false;
				return nextTick();
			}
		};
		const handleClickIconEvent = (evnt, item) => {
			const value = item.icon;
			changeEvent(evnt, value);
			hideOptionPanel();
		};
		Object.assign($xeIconPicker, iconPickerMethods);
		const renderIconWrapper = () => {
			const { showIconTitle } = props;
			const { selectIcon } = reactData;
			const iconGroupList = computeIconGroupList.value;
			const isDisabled = computeIsDisabled.value;
			return h("div", { class: "vxe-ico-picker--list-wrapper" }, iconGroupList.map((list) => {
				return h("div", { class: "vxe-ico-picker--list" }, list.map((item) => {
					const { iconRender } = item;
					const compConf = iconRender ? renderer.get(iconRender.name) : null;
					const oIconMethod = compConf ? compConf.renderIconPickerOptionIcon : null;
					return h("div", {
						class: ["vxe-ico-picker--item", { "is--selected": selectIcon === item.icon }],
						onClick(evnt) {
							if (!isDisabled) handleClickIconEvent(evnt, item);
						}
					}, [h("div", { class: "vxe-ico-picker--item-icon" }, oIconMethod && iconRender ? getSlotVNs$1(oIconMethod(iconRender, {
						$iconPicker: $xeIconPicker,
						option: item
					})) : [h("i", { class: item.icon || "" })]), showIconTitle ? h("div", { class: "vxe-ico-picker--item-title" }, `${item.title || ""}`) : renderEmptyElement($xeIconPicker)]);
				}));
			}));
		};
		const renderIconView = () => {
			const { selectIcon } = reactData;
			const selectIconItem = computeSelectIconItem.value;
			if (selectIconItem) {
				const { iconRender } = selectIconItem;
				const compConf = iconRender ? renderer.get(iconRender.name) : null;
				const oIconMethod = compConf ? compConf.renderIconPickerOptionIcon : null;
				if (oIconMethod && iconRender) return h("div", {
					key: "inc",
					class: "vxe-ico-picker--icon"
				}, getSlotVNs$1(oIconMethod(iconRender, {
					$iconPicker: $xeIconPicker,
					option: selectIconItem
				})));
			}
			return h("div", {
				key: "ind",
				class: "vxe-ico-picker--icon"
			}, [h("i", { class: selectIcon })]);
		};
		const renderVN = () => {
			const { className, clearable } = props;
			const { initialized, isActivated, isAniVisible, visiblePanel, selectIcon } = reactData;
			const vSize = computeSize.value;
			const isDisabled = computeIsDisabled.value;
			const btnTransfer = computeBtnTransfer.value;
			const formReadonly = computeFormReadonly.value;
			const inpPlaceholder = computeInpPlaceholder.value;
			const ppClassName = computePopupOpts.value.className || props.popupClassName;
			if (formReadonly) return h("div", {
				ref: refElem,
				class: ["vxe-ico-picker--readonly", className]
			}, [h("i", { class: selectIcon })]);
			return h("div", {
				ref: refElem,
				class: [
					"vxe-ico-picker",
					className ? import_xe_utils.default.isFunction(className) ? className({ $iconPicker: $xeIconPicker }) : className : "",
					{
						[`size--${vSize}`]: vSize,
						"show--clear": clearable && !isDisabled && !!selectIcon,
						"is--visible": visiblePanel,
						"is--disabled": isDisabled,
						"is--active": isActivated
					}
				]
			}, [h("div", {
				class: "vxe-ico-picker--inner",
				onClick: clickEvent
			}, [
				h("input", {
					ref: refInput,
					class: "vxe-ico-picker--input",
					onFocus: focusEvent,
					onBlur: blurEvent
				}),
				selectIcon ? renderIconView() : h("div", { class: "vxe-ico-picker--placeholder" }, inpPlaceholder),
				h("div", { class: "vxe-ico-picker--suffix" }, [h("div", {
					class: "vxe-ico-picker--clear-icon",
					onClick: clearEvent
				}, [h("i", { class: getIcon().INPUT_CLEAR })]), h("div", { class: "vxe-ico-picker--suffix-icon" }, [h("i", { class: visiblePanel ? getIcon().ICON_PICKER_OPEN : getIcon().ICON_PICKER_CLOSE })])])
			]), h(Teleport, {
				to: "body",
				disabled: btnTransfer ? !initialized : true
			}, [h("div", {
				ref: refOptionPanel,
				class: [
					"vxe-table--ignore-clear vxe-ico-picker--panel",
					ppClassName ? import_xe_utils.default.isFunction(ppClassName) ? ppClassName({ $iconPicker: $xeIconPicker }) : ppClassName : "",
					{
						[`size--${vSize}`]: vSize,
						"is--transfer": btnTransfer,
						"ani--leave": isAniVisible,
						"ani--enter": visiblePanel
					}
				],
				placement: reactData.panelPlacement,
				style: reactData.panelStyle
			}, [initialized && (visiblePanel || isAniVisible) ? h("div", { class: "vxe-ico-picker--panel-wrapper" }, [renderIconWrapper()]) : renderEmptyElement($xeIconPicker)])])]);
		};
		watch(() => props.modelValue, (val) => {
			reactData.selectIcon = `${val || ""}`;
		});
		onMounted(() => {
			globalEvents.on($xeIconPicker, "mousewheel", handleGlobalMousewheelEvent);
			globalEvents.on($xeIconPicker, "mousedown", handleGlobalMousedownEvent);
			globalEvents.on($xeIconPicker, "keydown", handleGlobalKeydownEvent);
			globalEvents.on($xeIconPicker, "blur", handleGlobalBlurEvent);
		});
		onUnmounted(() => {
			globalEvents.off($xeIconPicker, "mousewheel");
			globalEvents.off($xeIconPicker, "mousedown");
			globalEvents.off($xeIconPicker, "keydown");
			globalEvents.off($xeIconPicker, "blur");
		});
		provide("$xeIconPicker", $xeIconPicker);
		$xeIconPicker.renderVN = renderVN;
		return $xeIconPicker;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/icon-picker/index.js
var VxeIconPicker = Object.assign(icon_picker_default, { install: function(app) {
	app.component(icon_picker_default.name, icon_picker_default);
} });
dynamicApp.use(VxeIconPicker);
VxeUI.component(icon_picker_default);
var IconPicker = VxeIconPicker;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/image/src/preview.js
var preview_default = defineVxeComponent({
	name: "VxeImagePreview",
	props: {
		modelValue: Number,
		urlList: Array,
		urlField: {
			type: String,
			default: () => getConfig().imagePreview.urlField
		},
		maskClosable: {
			type: Boolean,
			default: () => getConfig().imagePreview.maskClosable
		},
		marginSize: {
			type: String,
			default: () => getConfig().imagePreview.marginSize
		},
		showPrintButton: {
			type: Boolean,
			default: () => getConfig().imagePreview.showPrintButton
		},
		showDownloadButton: {
			type: Boolean,
			default: () => getConfig().imagePreview.showDownloadButton
		},
		zIndex: Number,
		toolbarConfig: Object,
		beforeDownloadMethod: Function,
		downloadMethod: Function
	},
	emits: [
		"update:modelValue",
		"change",
		"download",
		"download-fail",
		"rotate",
		"close"
	],
	setup(props, context) {
		const { emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const refMaps = { refElem };
		const reactData = reactive({
			activeIndex: props.modelValue || 0,
			offsetPct11: false,
			offsetScale: 0,
			offsetRotate: 0,
			offsetLeft: 0,
			offsetTop: 0
		});
		const computeUrlProp = computed(() => {
			return props.urlField || "url";
		});
		const computeMarginSize = computed(() => {
			return import_xe_utils.default.toNumber(props.marginSize || 0) || 16;
		});
		const computeToolbarOpts = computed(() => {
			return Object.assign({}, getConfig().imagePreview.toolbarConfig, props.toolbarConfig);
		});
		const computeRotateText = computed(() => {
			const { offsetRotate } = reactData;
			if (offsetRotate) return `${offsetRotate}°`;
			return "0°";
		});
		const computeScaleText = computed(() => {
			const { offsetScale } = reactData;
			if (offsetScale) return `${import_xe_utils.default.ceil((1 + offsetScale) * 100)}%`;
			return "100%";
		});
		const computeImgList = computed(() => {
			const { urlList } = props;
			const urlProp = computeUrlProp.value;
			if (urlList && urlList.length) return urlList.map((item) => {
				if (import_xe_utils.default.isString(item)) return item;
				if (item[urlProp]) return item[urlProp];
				return "";
			});
			return [];
		});
		const computeImgTransform = computed(() => {
			let { offsetScale, offsetRotate, offsetLeft, offsetTop } = reactData;
			const stys = [];
			let targetScale = 1;
			if (offsetScale) {
				targetScale = 1 + offsetScale;
				stys.push(`scale(${targetScale})`);
			}
			if (offsetRotate) stys.push(`rotate(${offsetRotate}deg)`);
			if (offsetLeft || offsetTop) {
				offsetLeft /= targetScale;
				offsetTop /= targetScale;
				let targetOffsetLeft = offsetLeft;
				let targetOffsetTop = offsetTop;
				if (offsetRotate) switch (offsetRotate % 360) {
					case 90:
					case -270:
						targetOffsetLeft = offsetTop;
						targetOffsetTop = -offsetLeft;
						break;
					case 180:
					case -180:
						targetOffsetLeft = -offsetLeft;
						targetOffsetTop = -offsetTop;
						break;
					case 270:
					case -90:
						targetOffsetLeft = -offsetTop;
						targetOffsetTop = offsetLeft;
						break;
				}
				stys.push(`translate(${targetOffsetLeft}px, ${targetOffsetTop}px)`);
			}
			return stys.length ? stys.join(" ") : "";
		});
		const computeMaps = { computeImgList };
		const $xeImagePreview = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $imagePreview: $xeImagePreview }, params));
		};
		const imagePreviewMethods = { dispatchEvent };
		const emitModel = (value) => {
			reactData.activeIndex = value;
			emit("update:modelValue", value);
		};
		const handleCloseEvent = (evnt) => {
			dispatchEvent("close", {}, evnt);
		};
		const imagePreviewPrivateMethods = {};
		const resetStyle = () => {
			const elem = refElem.value;
			removeClass(elem, "is--move");
			Object.assign(reactData, {
				offsetPct11: false,
				offsetScale: 0,
				offsetRotate: 0,
				offsetLeft: 0,
				offsetTop: 0
			});
		};
		const getOffsetZoomStep = () => {
			const { offsetScale } = reactData;
			let stepNum = .02;
			if (offsetScale >= -.6) {
				stepNum = .04;
				if (offsetScale >= -.4) {
					stepNum = .07;
					if (offsetScale >= 0) {
						stepNum = .1;
						if (offsetScale >= 3) {
							stepNum = .25;
							if (offsetScale >= 8) {
								stepNum = .4;
								if (offsetScale >= 16) {
									stepNum = .6;
									if (offsetScale >= 24) {
										stepNum = .9;
										if (offsetScale >= 32) {
											stepNum = 1.3;
											if (offsetScale >= 39) {
												stepNum = 1.9;
												if (offsetScale >= 45) stepNum = 2.5;
											}
										}
									}
								}
							}
						}
					}
				}
			}
			return stepNum;
		};
		const handleZoom = (isAdd) => {
			const { offsetScale } = reactData;
			const stepNum = getOffsetZoomStep();
			if (isAdd) reactData.offsetScale = Number(Math.min(49, offsetScale + stepNum).toFixed(2));
			else reactData.offsetScale = Number(Math.max(-.9, offsetScale - stepNum).toFixed(2));
		};
		const handleChangeEvent = (evnt, isNext) => {
			let activeIndex = reactData.activeIndex || 0;
			const imgList = computeImgList.value;
			if (isNext) if (activeIndex >= imgList.length - 1) activeIndex = 0;
			else activeIndex++;
			else if (activeIndex <= 0) activeIndex = imgList.length - 1;
			else activeIndex--;
			const imgUrl = imgList[activeIndex || 0];
			reactData.activeIndex = activeIndex;
			resetStyle();
			emitModel(activeIndex);
			dispatchEvent("change", {
				url: imgUrl,
				activeIndex
			}, evnt);
		};
		const handleRotateImgEvent = (evnt, isRight) => {
			const imgList = computeImgList.value;
			const { activeIndex } = reactData;
			const imgUrl = imgList[activeIndex || 0];
			let offsetRotate = reactData.offsetRotate;
			if (isRight) offsetRotate += 90;
			else offsetRotate -= 90;
			reactData.offsetRotate = offsetRotate;
			dispatchEvent("rotate", {
				url: imgUrl,
				rotateValue: offsetRotate
			}, evnt);
		};
		const handlePct11 = () => {
			resetStyle();
			reactData.offsetPct11 = true;
		};
		const handlePrintImg = () => {
			const { activeIndex } = reactData;
			const imgUrl = computeImgList.value[activeIndex || 0];
			if (VxeUI.print) VxeUI.print({
				align: "center",
				pageBreaks: [{ bodyHtml: `<img src="${imgUrl}" style="max-width:100%;max-height:100%;">` }]
			});
		};
		const handleDownloadEvent = (evnt, imgUrl) => {
			dispatchEvent("download", { url: imgUrl }, evnt);
		};
		const handleDefaultDownload = (evnt, imgUrl) => {
			if (VxeUI.saveFile) fetch(imgUrl).then((res) => {
				return res.blob().then((blob) => {
					VxeUI.saveFile({
						filename: imgUrl,
						content: blob
					});
					handleDownloadEvent(evnt, imgUrl);
				});
			}).catch(() => {
				if (VxeUI.modal) VxeUI.modal.message({
					content: getI18n("vxe.error.downErr"),
					status: "error"
				});
			});
		};
		const handleDownloadImg = (evnt) => {
			const { activeIndex } = reactData;
			const btnConf = computeToolbarOpts.value.download;
			const btnOpts = import_xe_utils.default.isBoolean(btnConf) ? {} : btnConf ? Object.assign({}, btnConf) : {};
			const imgUrl = computeImgList.value[activeIndex || 0];
			const beforeDownloadFn = props.beforeDownloadMethod || btnOpts.beforeDownloadMethod || getConfig().imagePreview.beforeDownloadMethod;
			const downloadFn = props.downloadMethod || btnOpts.downloadMethod || getConfig().imagePreview.downloadMethod;
			Promise.resolve(beforeDownloadFn ? beforeDownloadFn({
				$imagePreview: $xeImagePreview,
				url: imgUrl,
				index: activeIndex || 0
			}) : true).then((status) => {
				if (status) if (downloadFn) Promise.resolve(downloadFn({
					$imagePreview: $xeImagePreview,
					url: imgUrl,
					index: activeIndex || 0
				})).then(() => {
					handleDownloadEvent(evnt, imgUrl);
				}).catch((e) => e);
				else handleDefaultDownload(evnt, imgUrl);
			});
		};
		const handleOperationBtn = (evnt, code) => {
			const { activeIndex } = reactData;
			if (computeImgList.value[activeIndex || 0]) switch (code) {
				case "zoomOut":
					handleZoom(false);
					break;
				case "zoomIn":
					handleZoom(true);
					break;
				case "pctFull":
					resetStyle();
					break;
				case "pct11":
					handlePct11();
					break;
				case "rotateLeft":
					handleRotateImgEvent(evnt, false);
					break;
				case "rotateRight":
					handleRotateImgEvent(evnt, true);
					break;
				case "print":
					handlePrintImg();
					break;
				case "download":
					handleDownloadImg(evnt);
					break;
			}
		};
		const wheelEvent = (evnt) => {
			const delta = evnt.deltaY;
			if (delta > 0) {
				evnt.preventDefault();
				handleZoom(false);
			} else if (delta < 0) {
				evnt.preventDefault();
				handleZoom(true);
			}
		};
		const moveEvent = (evnt) => {
			const { offsetTop, offsetLeft } = reactData;
			const elem = refElem.value;
			evnt.preventDefault();
			const domMousemove = document.onmousemove;
			const domMouseup = document.onmouseup;
			const startX = evnt.pageX;
			const startY = evnt.pageY;
			const marginSize = computeMarginSize.value;
			document.onmousemove = (et) => {
				const { pageX, pageY } = et;
				const { visibleHeight, visibleWidth } = getDomNode();
				et.preventDefault();
				addClass(elem, "is--move");
				if (pageX > marginSize && pageY > marginSize && pageX < visibleWidth - marginSize && pageY < visibleHeight - marginSize) {
					reactData.offsetLeft = offsetLeft + pageX - startX;
					reactData.offsetTop = offsetTop + pageY - startY;
				}
			};
			document.onmouseup = () => {
				document.onmousemove = domMousemove;
				document.onmouseup = domMouseup;
				removeClass(elem, "is--move");
			};
		};
		const handleGlobalKeydownEvent = (evnt) => {
			const isControlKey = hasControlKey(evnt);
			const hasShiftKey = evnt.shiftKey;
			const isUpArrow = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.ARROW_UP);
			const isDownArrow = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.ARROW_DOWN);
			const isLeftArrow = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.ARROW_LEFT);
			const isRightArrow = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.ARROW_RIGHT);
			const isR = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.R);
			const isP = globalEvents.hasKey(evnt, GLOBAL_EVENT_KEYS.P);
			if (isUpArrow) {
				evnt.preventDefault();
				if (hasShiftKey) reactData.offsetTop -= 1;
				else handleZoom(true);
			} else if (isDownArrow) {
				evnt.preventDefault();
				if (hasShiftKey) reactData.offsetTop += 1;
				else handleZoom(false);
			} else if (isLeftArrow) {
				evnt.preventDefault();
				if (hasShiftKey) reactData.offsetLeft -= 1;
				else handleChangeEvent(evnt, false);
			} else if (isRightArrow) {
				evnt.preventDefault();
				if (hasShiftKey) reactData.offsetLeft += 1;
				else handleChangeEvent(evnt, true);
			} else if (isR && isControlKey) {
				evnt.preventDefault();
				if (hasShiftKey) handleRotateImgEvent(evnt, false);
				else handleRotateImgEvent(evnt, true);
			} else if (isP && isControlKey) {
				evnt.preventDefault();
				handlePrintImg();
			}
		};
		const handleClickMaskEvent = (evnt) => {
			if (props.maskClosable) {
				if (evnt.target === evnt.currentTarget) dispatchEvent("close", {}, evnt);
			}
		};
		Object.assign($xeImagePreview, imagePreviewMethods, imagePreviewPrivateMethods);
		const renderImgWrapper = () => {
			const { activeIndex } = reactData;
			const imgList = computeImgList.value;
			const imgTransform = computeImgTransform.value;
			return h("div", {
				class: "vxe-image-preview--img-list",
				onClick: handleClickMaskEvent
			}, imgList.map((url, index) => {
				const isActive = activeIndex === index;
				return h("img", {
					class: ["vxe-image-preview--img-item", { "is--active": isActive }],
					src: url,
					style: isActive ? { transform: imgTransform } : null,
					onMousedown(evnt) {
						moveEvent(evnt);
					}
				});
			}));
		};
		const renderOperationBtn = (code, icon) => {
			const btnConf = computeToolbarOpts.value[code];
			const btnOpts = import_xe_utils.default.isBoolean(btnConf) ? {} : btnConf ? Object.assign({}, btnConf) : {};
			return btnConf !== false ? h("div", {
				class: "vxe-image-preview--operation-btn",
				title: getI18n(`vxe.imagePreview.operBtn.${code}`),
				onClick(evnt) {
					handleOperationBtn(evnt, code);
				}
			}, [h("i", { class: btnOpts.icon || getIcon()[icon] })]) : renderEmptyElement($xeImagePreview);
		};
		const renderBtnWrapper = () => {
			const { showPrintButton, showDownloadButton } = props;
			const { activeIndex } = reactData;
			const imgList = computeImgList.value;
			const rotateText = computeRotateText.value;
			const scaleText = computeScaleText.value;
			const toolbarOpts = computeToolbarOpts.value;
			return h("div", { class: "vxe-image-preview--btn-wrapper" }, [
				h("div", { class: "vxe-image-preview--close-wrapper" }, [h("div", {
					class: "vxe-image-preview--close-btn",
					onClick: handleCloseEvent
				}, [h("i", { class: getIcon().IMAGE_PREVIEW_CLOSE })]), h("div", { class: "vxe-image-preview--close-bg" })]),
				imgList.length > 1 ? h("div", {
					class: "vxe-image-preview--previous-btn",
					onClick(evnt) {
						handleChangeEvent(evnt, false);
					}
				}, [h("i", { class: getIcon().IMAGE_PREVIEW_PREVIOUS })]) : renderEmptyElement($xeImagePreview),
				imgList.length > 1 ? h("div", {
					class: "vxe-image-preview--next-btn",
					onClick(evnt) {
						handleChangeEvent(evnt, true);
					}
				}, [h("i", { class: getIcon().IMAGE_PREVIEW_NEXT })]) : renderEmptyElement($xeImagePreview),
				h("div", { class: "vxe-image-preview--operation-info" }, [h("div", { class: "vxe-image-preview--operation-deg" }, rotateText), h("div", { class: "vxe-image-preview--operation-pct" }, scaleText)]),
				h("div", { class: "vxe-image-preview--operation-wrapper" }, [
					h("div", { class: "vxe-image-preview--operation-active-count" }, [h("span", { class: "vxe-image-preview--operation-active-current" }, `${(activeIndex || 0) + 1}`), h("span", { class: "vxe-image-preview--operation-active-total" }, `/${imgList.length}`)]),
					renderOperationBtn("zoomOut", "IMAGE_PREVIEW_ZOOM_OUT"),
					renderOperationBtn("zoomIn", "IMAGE_PREVIEW_ZOOM_IN"),
					renderOperationBtn("pctFull", "IMAGE_PREVIEW_PCT_FULL"),
					renderOperationBtn("pct11", "IMAGE_PREVIEW_PCT_1_1"),
					renderOperationBtn("rotateLeft", "IMAGE_PREVIEW_ROTATE_LEFT"),
					renderOperationBtn("rotateRight", "IMAGE_PREVIEW_ROTATE_RIGHT"),
					showPrintButton || toolbarOpts.print ? renderOperationBtn("print", "IMAGE_PREVIEW_PRINT") : renderEmptyElement($xeImagePreview),
					showDownloadButton || toolbarOpts.download ? renderOperationBtn("download", "IMAGE_PREVIEW_DOWNLOAD") : renderEmptyElement($xeImagePreview)
				])
			]);
		};
		const renderVN = () => {
			const { offsetPct11 } = reactData;
			return h("div", {
				ref: refElem,
				class: ["vxe-image-preview", { "is--pct11": offsetPct11 }]
			}, [renderImgWrapper(), renderBtnWrapper()]);
		};
		watch(() => props.modelValue, (val) => {
			reactData.activeIndex = val;
			resetStyle();
		});
		onMounted(() => {
			const elem = refElem.value;
			if (elem) elem.addEventListener("wheel", wheelEvent, { passive: false });
			globalEvents.on($xeImagePreview, "keydown", handleGlobalKeydownEvent);
		});
		onBeforeUnmount(() => {
			const elem = refElem.value;
			if (elem) {
				elem.removeEventListener("wheel", wheelEvent);
				removeClass(elem, "is--move");
			}
		});
		onUnmounted(() => {
			globalEvents.off($xeImagePreview, "keydown");
		});
		provide("$xeImagePreview", $xeImagePreview);
		$xeImagePreview.renderVN = renderVN;
		return renderVN;
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/image/src/util.js
var openPreviewImage = (options) => {
	if (VxeUI.modal) {
		const opts = Object.assign({ escClosable: true }, options);
		const { urlList, activeIndex, zIndex } = opts;
		const { rotate, change } = opts.events || {};
		const modalId = import_xe_utils.default.uniqueId("image-preview");
		VxeUI.modal.open({
			id: modalId,
			title: "预览",
			width: "100%",
			height: "100%",
			showHeader: false,
			showFooter: false,
			padding: false,
			escClosable: opts.escClosable,
			zIndex,
			className: "vxe-image-preview-popup-wrapper",
			slots: { default() {
				return h(preview_default, {
					modelValue: activeIndex,
					urlList,
					urlField: opts.urlField,
					marginSize: opts.marginSize,
					maskClosable: opts.maskClosable,
					toolbarConfig: opts.toolbarConfig,
					showPrintButton: opts.showPrintButton,
					showDownloadButton: opts.showDownloadButton,
					beforeDownloadMethod: opts.beforeDownloadMethod,
					downloadMethod: opts.downloadMethod,
					onClose() {
						VxeUI.modal.close(modalId);
					},
					onChange(eventParams) {
						if (change) change.call(this, eventParams);
					},
					onRotate(eventParams) {
						if (rotate) rotate.call(this, eventParams);
					}
				});
			} }
		});
	}
	return Promise.resolve();
};
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/image/src/image.js
function createInternalData$5() {
	return {};
}
function createReactData$5() {
	return {};
}
var image_default = defineVxeComponent({
	name: "VxeImage",
	props: {
		src: [String, Array],
		alt: [String, Number],
		loading: String,
		title: [String, Number],
		width: [String, Number],
		height: [String, Number],
		circle: Boolean,
		draggable: {
			type: Boolean,
			default: () => getConfig().image.draggable
		},
		zIndex: Number,
		maskClosable: {
			type: Boolean,
			default: () => getConfig().image.maskClosable
		},
		toolbarConfig: Object,
		showPreview: {
			type: Boolean,
			default: () => getConfig().image.showPreview
		},
		showPrintButton: {
			type: Boolean,
			default: () => getConfig().image.showPrintButton
		},
		showDownloadButton: {
			type: Boolean,
			default: () => getConfig().image.showDownloadButton
		},
		size: {
			type: String,
			default: () => getConfig().image.size || getConfig().size
		},
		getThumbnailUrlMethod: Function
	},
	emits: [
		"click",
		"change",
		"rotate",
		"dragstart",
		"drag",
		"dragend"
	],
	setup(props, context) {
		const { emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const $xeImageGroup = inject("$xeImageGroup", null);
		const refElem = ref();
		const { computeSize } = useSize(props);
		const internalData = createInternalData$5();
		const reactData = reactive(createReactData$5());
		const refMaps = { refElem };
		const computeWrapperStyle = computed(() => {
			const { width, height, circle } = props;
			const stys = {};
			if (width) stys.width = toCssUnit(width);
			else if (circle) {
				if (height) stys.width = toCssUnit(height);
			}
			if (height) stys.height = toCssUnit(height);
			else if (circle) {
				if (width) stys.height = toCssUnit(width);
			}
			return stys;
		});
		const computeImgStyle = computed(() => {
			const { width, height } = props;
			const stys = {};
			if (width && height) {
				stys.maxWidth = toCssUnit(width);
				stys.maxHeight = toCssUnit(height);
			} else {
				if (width) stys.width = toCssUnit(width);
				if (height) stys.height = toCssUnit(height);
			}
			return stys;
		});
		const computeImgList = computed(() => {
			const { src } = props;
			if (src) return (import_xe_utils.default.isArray(src) ? src : [src]).map((item) => {
				if (import_xe_utils.default.isString(item)) return {
					url: item,
					alt: ""
				};
				return {
					url: item.url,
					alt: item.alt
				};
			});
			return [];
		});
		const computeImgItem = computed(() => {
			return computeImgList.value[0];
		});
		const computeImgUrl = computed(() => {
			const imgItem = computeImgItem.value;
			return imgItem ? `${imgItem.url || ""}` : "";
		});
		const computeImgThumbnailUrl = computed(() => {
			const getThumbnailUrlFn = props.getThumbnailUrlMethod || getConfig().image.getThumbnailUrlMethod;
			const imgUrl = computeImgUrl.value;
			return getThumbnailUrlFn ? getThumbnailUrlFn({
				url: imgUrl,
				$image: $xeImage
			}) : "";
		});
		const computeMaps = { computeSize };
		const $xeImage = {
			xID,
			props,
			context,
			reactData,
			internalData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $image: $xeImage }, params));
		};
		const imageMethods = { dispatchEvent };
		const clickEvent = (evnt) => {
			const { showPreview, toolbarConfig, showPrintButton, showDownloadButton, maskClosable, zIndex } = props;
			const { mdTime, dgTime } = internalData;
			if (mdTime && dgTime && dgTime > mdTime) return;
			const imgList = computeImgList.value;
			const imgUrl = computeImgUrl.value;
			if ($xeImageGroup) $xeImageGroup.handleClickImgEvent(evnt, { url: imgUrl });
			else {
				if (showPreview && imgUrl) openPreviewImage({
					urlList: imgList,
					toolbarConfig,
					showPrintButton,
					showDownloadButton,
					maskClosable,
					zIndex,
					events: {
						change(eventParams) {
							dispatchEvent("change", eventParams, eventParams.$event);
						},
						rotate(eventParams) {
							dispatchEvent("rotate", eventParams, eventParams.$event);
						}
					}
				});
				dispatchEvent("click", { url: imgUrl }, evnt);
			}
		};
		const mousedownEvent = () => {
			internalData.mdTime = Date.now();
		};
		const handleDragstartEvent = (evnt) => {
			internalData.dgTime = Date.now();
			dispatchEvent("dragstart", {}, evnt);
		};
		const handleDragEvent = (evnt) => {
			dispatchEvent(evnt.type, {}, evnt);
		};
		Object.assign($xeImage, imageMethods, {});
		const renderVN = () => {
			const { alt, loading, circle, draggable } = props;
			const wrapperStyle = computeWrapperStyle.value;
			const imgStyle = computeImgStyle.value;
			const imgUrl = computeImgUrl.value;
			const imgThumbnailUrl = computeImgThumbnailUrl.value;
			const vSize = computeSize.value;
			let imgDrag;
			if (!import_xe_utils.default.eqNull(draggable)) imgDrag = !!(draggable === true || draggable === "true");
			return h("div", {
				class: ["vxe-image", {
					[`size--${vSize}`]: vSize,
					"is--circle": circle
				}],
				style: wrapperStyle
			}, [h("img", {
				ref: refElem,
				class: "vxe-image-img",
				src: imgThumbnailUrl || imgUrl,
				alt,
				loading,
				draggable: imgDrag,
				style: imgStyle,
				onMousedown: mousedownEvent,
				onClick: clickEvent,
				onDragstart: handleDragstartEvent,
				onDrag: handleDragEvent,
				onDragend: handleDragEvent
			})]);
		};
		onBeforeUnmount(() => {
			import_xe_utils.default.assign(reactData, createReactData$5());
			import_xe_utils.default.assign(internalData, createInternalData$5());
		});
		$xeImage.renderVN = renderVN;
		return $xeImage;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/image/index.js
var VxeImage = Object.assign({}, image_default, { install(app) {
	app.component(image_default.name, image_default);
} });
dynamicApp.use(VxeImage);
VxeUI.component(image_default);
var Image = VxeImage;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/image/src/group.js
var group_default = defineVxeComponent({
	name: "VxeImageGroup",
	props: {
		urlList: [Array, String],
		showPreview: {
			type: Boolean,
			default: () => getConfig().imageGroup.showPreview
		},
		imageStyle: Object,
		zIndex: Number,
		size: {
			type: String,
			default: () => getConfig().imageGroup.size || getConfig().size
		},
		toolbarConfig: Object,
		showPrintButton: {
			type: Boolean,
			default: () => getConfig().imageGroup.showPrintButton
		},
		showDownloadButton: {
			type: Boolean,
			default: () => getConfig().imageGroup.showDownloadButton
		},
		getThumbnailUrlMethod: Function
	},
	emits: [
		"click",
		"change",
		"rotate"
	],
	setup(props, context) {
		const { emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const computeImgList = computed(() => {
			const { urlList } = props;
			if (urlList) return (import_xe_utils.default.isArray(urlList) ? urlList : [urlList]).map((item) => {
				if (import_xe_utils.default.isString(item)) return {
					url: item,
					alt: ""
				};
				return {
					url: item.url,
					alt: item.alt
				};
			});
			return [];
		});
		const computeImgStyleOpts = computed(() => {
			return Object.assign({}, getConfig().imageGroup.imageStyle, props.imageStyle);
		});
		const computeGetThumbnailUrlMethod = computed(() => {
			return props.getThumbnailUrlMethod || getConfig().imageGroup.getThumbnailUrlMethod;
		});
		const computeMaps = { computeSize };
		const $xeImageGroup = {
			xID,
			props,
			context,
			getComputeMaps: () => computeMaps
		};
		Object.assign($xeImageGroup, { dispatchEvent(type, params, evnt) {
			emit(type, createEvent(evnt, { $imageGroup: $xeImageGroup }, params));
		} }, { handleClickImgEvent(evnt, params) {
			const { showPreview, toolbarConfig, showPrintButton, showDownloadButton, zIndex } = props;
			const { url } = params;
			const imgList = computeImgList.value;
			if (showPreview && url) openPreviewImage({
				activeIndex: Math.max(0, import_xe_utils.default.findIndexOf(imgList, (item) => item.url === url)),
				urlList: imgList,
				toolbarConfig,
				showPrintButton,
				showDownloadButton,
				zIndex,
				events: {
					change(eventParams) {
						$xeImageGroup.dispatchEvent("change", eventParams, eventParams.$event);
					},
					rotate(eventParams) {
						$xeImageGroup.dispatchEvent("rotate", eventParams, eventParams.$event);
					}
				}
			});
			$xeImageGroup.dispatchEvent("click", {
				url,
				urlList: imgList
			}, evnt);
		} });
		const renderVN = () => {
			const imgList = computeImgList.value;
			const vSize = computeSize.value;
			const imgStyleOpts = computeImgStyleOpts.value;
			const getThumbnailUrlMethod = computeGetThumbnailUrlMethod.value;
			return h("div", { class: ["vxe-image-group", { [`size--${vSize}`]: vSize }] }, imgList ? imgList.map((item, index) => {
				return h(image_default, {
					key: index,
					src: item.url,
					alt: item.alt,
					width: imgStyleOpts.width,
					height: imgStyleOpts.height,
					getThumbnailUrlMethod
				});
			}) : []);
		};
		$xeImageGroup.renderVN = renderVN;
		provide("$xeImageGroup", $xeImageGroup);
		return $xeImageGroup;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/image-group/index.js
var VxeImageGroup = Object.assign({}, group_default, { install(app) {
	app.component(group_default.name, group_default);
} });
dynamicApp.use(VxeImageGroup);
VxeUI.component(group_default);
var ImageGroup = VxeImageGroup;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/image-preview/index.js
var VxeImagePreview = Object.assign(preview_default, { install(app) {
	app.component(preview_default.name, preview_default);
} });
VxeUI.previewImage = openPreviewImage;
dynamicApp.use(VxeImagePreview);
VxeUI.component(preview_default);
var ImagePreview = VxeImagePreview;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/layout-aside/src/layout-aside.js
var layout_aside_default = defineVxeComponent({
	name: "VxeLayoutAside",
	props: {
		width: [String, Number],
		collapsed: Boolean,
		collapseWidth: [String, Number],
		loading: Boolean,
		padding: Boolean,
		size: {
			type: String,
			default: () => getConfig().layoutAside.size || getConfig().size
		}
	},
	emits: [],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const { computeSize } = useSize(props);
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeWrapperWidth = computed(() => {
			const { width, collapsed, collapseWidth } = props;
			if (collapsed) {
				if (collapseWidth) return toCssUnit(collapseWidth);
			} else if (width) return toCssUnit(width);
			return "";
		});
		const computeMaps = { computeSize };
		const $xeLayoutAside = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $layoutAside: $xeLayoutAside }, params));
		};
		Object.assign($xeLayoutAside, { dispatchEvent }, {});
		const renderVN = () => {
			const { width, collapsed, loading, padding } = props;
			const wrapperWidth = computeWrapperWidth.value;
			const vSize = computeSize.value;
			const defaultSlot = slots.default;
			return h("aside", {
				ref: refElem,
				class: ["vxe-layout-aside", {
					[`size--${vSize}`]: vSize,
					"is--padding": padding,
					"is--default-width": !width,
					"is--collapse": collapsed,
					"is--loading": loading
				}],
				style: wrapperWidth ? { width: wrapperWidth } : null
			}, [h("div", { class: "vxe-layout-aside--inner" }, defaultSlot ? defaultSlot({}) : []), h(loading_default, {
				class: "vxe-list-view--loading",
				modelValue: loading
			})]);
		};
		provide("$xeLayoutAside", $xeLayoutAside);
		$xeLayoutAside.renderVN = renderVN;
		return $xeLayoutAside;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/layout-aside/index.js
var VxeLayoutAside = Object.assign({}, layout_aside_default, { install(app) {
	app.component(layout_aside_default.name, layout_aside_default);
} });
dynamicApp.use(VxeLayoutAside);
VxeUI.component(layout_aside_default);
var LayoutAside = VxeLayoutAside;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/layout-body/src/layout-body.js
function createInternalData$4() {
	return {};
}
function createReactData$4() {
	return {};
}
var layout_body_default = defineVxeComponent({
	name: "VxeLayoutBody",
	props: {
		loading: Boolean,
		padding: Boolean,
		showBacktop: {
			type: Boolean,
			default: () => getConfig().layoutBody.showBacktop
		},
		backtopConfig: Object,
		size: {
			type: String,
			default: () => getConfig().layoutBody.size || getConfig().size
		}
	},
	emits: [],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const backtopId = `vxe_layout_body_backtop_${xID}`;
		const refElem = ref();
		const { computeSize } = useSize(props);
		const internalData = createInternalData$4();
		const reactData = reactive(createReactData$4());
		const refMaps = { refElem };
		const computeBacktopOpts = computed(() => {
			return Object.assign({}, getConfig().layoutBody.backtopConfig, props.backtopConfig, { target: "#" + backtopId });
		});
		const computeMaps = { computeSize };
		const $xeLayoutBody = {
			xID,
			props,
			context,
			internalData,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $layoutBody: $xeLayoutBody }, params));
		};
		Object.assign($xeLayoutBody, { dispatchEvent }, {});
		const renderVN = () => {
			const { loading, padding, showBacktop } = props;
			const backtopOpts = computeBacktopOpts.value;
			const vSize = computeSize.value;
			const defaultSlot = slots.default;
			const backtopSlot = slots.backtop;
			const backtopTopSlot = slots.backtopTop || slots["backtop-top"];
			const backtopBottomSlot = slots.backtopBottom || slots["backtop-bottom"];
			const backtopScopeSlots = {};
			if (backtopSlot) backtopScopeSlots.default = backtopSlot;
			if (backtopTopSlot) backtopScopeSlots.top = backtopTopSlot;
			if (backtopBottomSlot) backtopScopeSlots.bottom = backtopBottomSlot;
			return h("div", {
				ref: refElem,
				class: ["vxe-layout-body", {
					[`size--${vSize}`]: vSize,
					"is--loading": loading,
					"is--padding": padding
				}]
			}, [
				h("div", {
					id: showBacktop ? backtopId : "",
					class: "vxe-layout-body--inner"
				}, defaultSlot ? defaultSlot({}) : []),
				h(VxeLoading, {
					class: "vxe-list-view--loading",
					modelValue: loading
				}),
				showBacktop ? h(VxeBacktop, backtopOpts, backtopScopeSlots) : renderEmptyElement($xeLayoutBody)
			]);
		};
		$xeLayoutBody.renderVN = renderVN;
		return $xeLayoutBody;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/layout-body/index.js
var VxeLayoutBody = Object.assign({}, layout_body_default, { install(app) {
	app.component(layout_body_default.name, layout_body_default);
} });
dynamicApp.use(VxeLayoutBody);
VxeUI.component(layout_body_default);
var LayoutBody = VxeLayoutBody;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/layout-container/src/layout-container.js
var layout_container_default = defineVxeComponent({
	name: "VxeLayoutContainer",
	props: {
		vertical: Boolean,
		size: {
			type: String,
			default: () => getConfig().layoutContainer.size || getConfig().size
		}
	},
	emits: [],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const { computeSize } = useSize(props);
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeMaps = { computeSize };
		const $xeLayoutContainer = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $layoutContainer: $xeLayoutContainer }, params));
		};
		Object.assign($xeLayoutContainer, { dispatchEvent }, {});
		const renderVN = () => {
			const { vertical } = props;
			const vSize = computeSize.value;
			const defaultSlot = slots.default;
			return h("div", {
				ref: refElem,
				class: ["vxe-layout-container", {
					[`size--${vSize}`]: vSize,
					"is--vertical": vertical
				}]
			}, defaultSlot ? defaultSlot({}) : []);
		};
		$xeLayoutContainer.renderVN = renderVN;
		return $xeLayoutContainer;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/layout-container/index.js
var VxeLayoutContainer = Object.assign({}, layout_container_default, { install(app) {
	app.component(layout_container_default.name, layout_container_default);
} });
dynamicApp.use(VxeLayoutContainer);
VxeUI.component(layout_container_default);
var LayoutContainer = VxeLayoutContainer;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/layout-footer/src/layout-footer.js
var layout_footer_default = defineVxeComponent({
	name: "VxeLayoutFooter",
	props: {
		fixed: Boolean,
		align: String
	},
	emits: [],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeMaps = {};
		const $xeLayoutFooter = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $layoutFooter: $xeLayoutFooter }, params));
		};
		Object.assign($xeLayoutFooter, { dispatchEvent }, {});
		const renderVN = () => {
			const { fixed, align } = props;
			const defaultSlot = slots.default;
			return h("footer", {
				ref: refElem,
				class: [
					"vxe-layout-footer",
					align ? `align--${align}` : "",
					{ "is--fixed": fixed }
				]
			}, defaultSlot ? defaultSlot({}) : []);
		};
		$xeLayoutFooter.renderVN = renderVN;
		return $xeLayoutFooter;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/layout-footer/index.js
var VxeLayoutFooter = Object.assign({}, layout_footer_default, { install(app) {
	app.component(layout_footer_default.name, layout_footer_default);
} });
dynamicApp.use(VxeLayoutFooter);
VxeUI.component(layout_footer_default);
var LayoutFooter = VxeLayoutFooter;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/layout-header/src/layout-header.js
var layout_header_default = defineVxeComponent({
	name: "VxeLayoutHeader",
	props: { fixed: Boolean },
	emits: [],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeMaps = {};
		const $xeLayoutHeader = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $layoutHeader: $xeLayoutHeader }, params));
		};
		Object.assign($xeLayoutHeader, { dispatchEvent }, {});
		const renderVN = () => {
			const { fixed } = props;
			const defaultSlot = slots.default;
			return h("header", {
				ref: refElem,
				class: ["vxe-layout-header", { "is--fixed": fixed }]
			}, defaultSlot ? defaultSlot({}) : []);
		};
		$xeLayoutHeader.renderVN = renderVN;
		return $xeLayoutHeader;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/layout-header/index.js
var VxeLayoutHeader = Object.assign({}, layout_header_default, { install(app) {
	app.component(layout_header_default.name, layout_header_default);
} });
dynamicApp.use(VxeLayoutHeader);
VxeUI.component(layout_header_default);
var LayoutHeader = VxeLayoutHeader;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/link/src/link.js
var link_default = defineVxeComponent({
	name: "VxeLink",
	props: {
		href: String,
		target: String,
		status: String,
		title: [String, Number],
		disabled: Boolean,
		icon: String,
		routerLink: Object,
		underline: {
			type: Boolean,
			default: () => getConfig().link.underline
		},
		/**
		* 权限码
		*/
		permissionCode: [String, Number],
		content: [String, Number],
		size: {
			type: String,
			default: () => getConfig().link.size || getConfig().size
		}
	},
	emits: ["click"],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const { computePermissionInfo } = usePermission(props);
		const refElem = ref();
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeMaps = {};
		const $xeLink = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $link: $xeLink }, params));
		};
		const linkMethods = { dispatchEvent };
		const linkPrivateMethods = {};
		const clickEvent = (evnt) => {
			const { disabled } = props;
			if (!disabled) dispatchEvent("click", {}, evnt);
		};
		Object.assign($xeLink, linkMethods, linkPrivateMethods);
		const renderContent = () => {
			const { icon, content } = props;
			const defaultSlot = slots.default;
			const iconSlot = slots.icon;
			const textContent = import_xe_utils.default.toValueString(content);
			return [iconSlot || icon ? h("span", { class: "vxe-link--icon" }, iconSlot ? getSlotVNs$1(iconSlot({})) : [h("i", { class: icon })]) : renderEmptyElement($xeLink), defaultSlot || textContent ? h("span", { class: "vxe-link--content" }, defaultSlot ? defaultSlot({}) : textContent) : renderEmptyElement($xeLink)];
		};
		const renderVN = () => {
			const { status, target, href, title, underline, disabled, routerLink } = props;
			const permissionInfo = computePermissionInfo.value;
			const vSize = computeSize.value;
			if (!permissionInfo.visible) return renderEmptyElement($xeLink);
			if (routerLink && !disabled) return h(resolveComponent("router-link"), {
				class: ["vxe-link", {
					[`size--${vSize}`]: vSize,
					[`theme--${status}`]: status,
					"is--disabled": disabled,
					"is--underline": underline
				}],
				title,
				target,
				to: disabled ? null : routerLink,
				onClick: clickEvent
			}, { default() {
				return renderContent();
			} });
			return h("a", {
				ref: refElem,
				class: ["vxe-link", {
					[`size--${vSize}`]: vSize,
					[`theme--${status}`]: status,
					"is--disabled": disabled,
					"is--underline": underline
				}],
				href: disabled ? null : href,
				target,
				title,
				onClick: clickEvent
			}, renderContent());
		};
		$xeLink.renderVN = renderVN;
		return $xeLink;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/link/index.js
var VxeLink = Object.assign({}, link_default, { install(app) {
	app.component(link_default.name, link_default);
} });
dynamicApp.use(VxeLink);
VxeUI.component(link_default);
var Link = VxeLink;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/list/src/list.js
function createReactData$3() {
	return {
		scrollYLoad: false,
		bodyHeight: 0,
		customHeight: 0,
		customMaxHeight: 0,
		parentHeight: 0,
		topSpaceHeight: 0,
		items: []
	};
}
function createInternalData$3() {
	return {
		resizeObserver: void 0,
		fullData: [],
		lastScrollLeft: 0,
		lastScrollTop: 0,
		scrollYStore: {
			startIndex: 0,
			endIndex: 0,
			visibleSize: 0,
			offsetSize: 0,
			rowHeight: 0
		}
	};
}
var list_default = defineVxeComponent({
	name: "VxeList",
	props: {
		data: Array,
		height: [Number, String],
		maxHeight: [Number, String],
		loading: Boolean,
		className: [String, Function],
		size: {
			type: String,
			default: () => getConfig().list.size || getConfig().size
		},
		autoResize: {
			type: Boolean,
			default: () => getConfig().list.autoResize
		},
		syncResize: [
			Boolean,
			String,
			Number
		],
		virtualYConfig: Object,
		scrollY: Object
	},
	emits: ["scroll"],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const browseObj = import_xe_utils.default.browse();
		const { computeSize } = useSize(props);
		const reactData = reactive(createReactData$3());
		const internalData = createInternalData$3();
		const refElem = ref();
		const refVirtualWrapper = ref();
		const refVirtualBody = ref();
		const refMaps = { refElem };
		const $xeList = {
			xID,
			props,
			context,
			reactData,
			internalData,
			getRefMaps: () => refMaps
		};
		const computeSYOpts = computed(() => {
			return Object.assign({}, getConfig().list.virtualYConfig || getConfig().list.scrollY, props.virtualYConfig || props.scrollY);
		});
		const computeStyles = computed(() => {
			const { height, maxHeight } = props;
			const { customHeight, customMaxHeight } = reactData;
			const style = {};
			if (height) style.height = `${customHeight}px`;
			else if (maxHeight) {
				style.height = "auto";
				style.maxHeight = `${customMaxHeight}px`;
			}
			return style;
		});
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $list: $xeList }, params));
		};
		const calcTableHeight = (key) => {
			const { parentHeight } = reactData;
			const val = props[key];
			let num = 0;
			if (val) if (val === "100%" || val === "auto") num = parentHeight;
			else {
				if (isScale(val)) num = Math.floor((import_xe_utils.default.toInteger(val) || 1) / 100 * parentHeight);
				else num = import_xe_utils.default.toNumber(val);
				num = Math.max(40, num);
			}
			return num;
		};
		const updateHeight = () => {
			reactData.customHeight = calcTableHeight("height");
			reactData.customMaxHeight = calcTableHeight("maxHeight");
		};
		const updateYSpace = () => {
			const { scrollYLoad } = reactData;
			const { scrollYStore, fullData } = internalData;
			reactData.bodyHeight = scrollYLoad ? fullData.length * scrollYStore.rowHeight : 0;
			reactData.topSpaceHeight = scrollYLoad ? Math.max(scrollYStore.startIndex * scrollYStore.rowHeight, 0) : 0;
		};
		const handleData = () => {
			const { scrollYLoad } = reactData;
			const { fullData, scrollYStore } = internalData;
			reactData.items = scrollYLoad ? fullData.slice(scrollYStore.startIndex, scrollYStore.endIndex) : fullData.slice(0);
			return nextTick();
		};
		const updateYData = () => {
			handleData();
			updateYSpace();
		};
		const computeScrollLoad = () => {
			return nextTick().then(() => {
				const { scrollYLoad } = reactData;
				const { scrollYStore } = internalData;
				const virtualBodyElem = refVirtualBody.value;
				const sYOpts = computeSYOpts.value;
				let rowHeight = 0;
				let firstItemElem;
				if (virtualBodyElem) {
					if (sYOpts.sItem) firstItemElem = virtualBodyElem.querySelector(sYOpts.sItem);
					if (!firstItemElem) firstItemElem = virtualBodyElem.children[0];
				}
				if (firstItemElem) rowHeight = firstItemElem.offsetHeight;
				rowHeight = Math.max(12, rowHeight);
				scrollYStore.rowHeight = rowHeight;
				if (scrollYLoad) {
					const scrollBodyElem = refVirtualWrapper.value;
					const visibleYSize = Math.max(8, Math.ceil(scrollBodyElem.clientHeight / rowHeight));
					const offsetYSize = sYOpts.oSize ? import_xe_utils.default.toNumber(sYOpts.oSize) : browseObj.edge ? 10 : 0;
					scrollYStore.offsetSize = offsetYSize;
					scrollYStore.visibleSize = visibleYSize;
					scrollYStore.endIndex = Math.max(scrollYStore.startIndex + visibleYSize + offsetYSize, scrollYStore.endIndex);
					updateYData();
				} else updateYSpace();
			});
		};
		/**
		* 清除滚动条
		*/
		const clearScroll = () => {
			const scrollBodyElem = refVirtualWrapper.value;
			if (scrollBodyElem) scrollBodyElem.scrollTop = 0;
			return nextTick();
		};
		/**
		* 如果有滚动条，则滚动到对应的位置
		*/
		const scrollTo = (scrollLeft, scrollTop) => {
			const scrollBodyElem = refVirtualWrapper.value;
			if (scrollLeft) {
				if (!import_xe_utils.default.isNumber(scrollLeft)) {
					scrollTop = scrollLeft.top;
					scrollLeft = scrollLeft.left;
				}
			}
			if (import_xe_utils.default.isNumber(scrollLeft)) scrollBodyElem.scrollLeft = scrollLeft;
			if (import_xe_utils.default.isNumber(scrollTop)) scrollBodyElem.scrollTop = scrollTop;
			if (reactData.scrollYLoad) return new Promise((resolve) => {
				setTimeout(() => {
					nextTick(() => {
						resolve();
					});
				}, 50);
			});
			return nextTick();
		};
		/**
		* 刷新滚动条
		*/
		const refreshScroll = () => {
			const { lastScrollLeft, lastScrollTop } = internalData;
			return clearScroll().then(() => {
				if (lastScrollLeft || lastScrollTop) {
					internalData.lastScrollLeft = 0;
					internalData.lastScrollTop = 0;
					return scrollTo(lastScrollLeft, lastScrollTop);
				}
			});
		};
		/**
		* 重新计算列表
		*/
		const recalculate = () => {
			const el = refElem.value;
			if (el) {
				const parentEl = el.parentElement;
				reactData.parentHeight = parentEl ? parentEl.clientHeight : 0;
				updateHeight();
				if (el.clientWidth && el.clientHeight) return computeScrollLoad();
			}
			return nextTick();
		};
		const loadYData = (evnt) => {
			const { scrollYStore } = internalData;
			const { startIndex, endIndex, visibleSize, offsetSize, rowHeight } = scrollYStore;
			const scrollTop = evnt.target.scrollTop;
			const toVisibleIndex = Math.floor(scrollTop / rowHeight);
			const offsetStartIndex = Math.max(0, toVisibleIndex - 1 - offsetSize);
			const offsetEndIndex = toVisibleIndex + visibleSize + offsetSize;
			if (toVisibleIndex <= startIndex || toVisibleIndex >= endIndex - visibleSize - 1) {
				if (startIndex !== offsetStartIndex || endIndex !== offsetEndIndex) {
					scrollYStore.startIndex = offsetStartIndex;
					scrollYStore.endIndex = offsetEndIndex;
					updateYData();
				}
			}
		};
		const scrollEvent = (evnt) => {
			const scrollBodyElem = evnt.target;
			const scrollTop = scrollBodyElem.scrollTop;
			const scrollLeft = scrollBodyElem.scrollLeft;
			const isX = scrollLeft !== internalData.lastScrollLeft;
			const isY = scrollTop !== internalData.lastScrollTop;
			internalData.lastScrollTop = scrollTop;
			internalData.lastScrollLeft = scrollLeft;
			if (reactData.scrollYLoad) loadYData(evnt);
			dispatchEvent("scroll", {
				scrollLeft,
				scrollTop,
				isX,
				isY
			}, evnt);
		};
		/**
		* 加载数据
		* @param {Array} datas 数据
		*/
		const loadData = (datas) => {
			const { scrollYStore } = internalData;
			const sYOpts = computeSYOpts.value;
			const fullData = datas || [];
			Object.assign(scrollYStore, {
				startIndex: 0,
				endIndex: 1,
				visibleSize: 0
			});
			internalData.fullData = fullData;
			reactData.scrollYLoad = !!sYOpts.enabled && sYOpts.gt > -1 && (sYOpts.gt === 0 || sYOpts.gt <= fullData.length);
			handleData();
			return computeScrollLoad().then(() => {
				refreshScroll();
			});
		};
		Object.assign($xeList, {
			dispatchEvent,
			loadData,
			/**
			* 重新加载数据
			* @param {Array} datas 数据
			*/
			reloadData(datas) {
				clearScroll();
				return loadData(datas);
			},
			recalculate,
			scrollTo,
			refreshScroll,
			clearScroll
		});
		const renderVN = () => {
			const { className, loading } = props;
			const { bodyHeight, topSpaceHeight, items } = reactData;
			const defaultSlot = slots.default;
			const vSize = computeSize.value;
			const styles = computeStyles.value;
			return h("div", {
				ref: refElem,
				class: [
					"vxe-list",
					className ? import_xe_utils.default.isFunction(className) ? className({ $list: $xeList }) : className : "",
					{
						[`size--${vSize}`]: vSize,
						"is--loading": loading
					}
				]
			}, [h("div", {
				ref: refVirtualWrapper,
				class: "vxe-list--virtual-wrapper",
				style: styles,
				onScroll: scrollEvent
			}, [h("div", {
				class: "vxe-list--y-space",
				style: { height: bodyHeight ? `${bodyHeight}px` : "" }
			}), h("div", {
				ref: refVirtualBody,
				class: "vxe-list--body",
				style: { marginTop: topSpaceHeight ? `${topSpaceHeight}px` : "" }
			}, defaultSlot ? defaultSlot({
				items,
				$list: $xeList
			}) : [])]), h(loading_default, {
				class: "vxe-list--loading",
				modelValue: loading
			})]);
		};
		const dataFlag = ref(0);
		watch(() => props.data ? props.data.length : -1, () => {
			dataFlag.value++;
		});
		watch(() => props.data, () => {
			dataFlag.value++;
		});
		watch(dataFlag, () => {
			loadData(props.data || []);
		});
		watch(() => props.height, () => {
			recalculate();
		});
		watch(() => props.maxHeight, () => {
			recalculate();
		});
		watch(() => props.syncResize, (value) => {
			if (value) {
				recalculate();
				nextTick(() => setTimeout(() => recalculate()));
			}
		});
		onActivated(() => {
			recalculate().then(() => refreshScroll());
		});
		nextTick(() => {
			loadData(props.data || []);
		});
		onMounted(() => {
			recalculate();
			if (props.autoResize) {
				const el = refElem.value;
				const resizeObserver = globalResize.create(() => recalculate());
				resizeObserver.observe(el);
				if (el) resizeObserver.observe(el.parentElement);
				internalData.resizeObserver = resizeObserver;
			}
			globalEvents.on($xeList, "resize", recalculate);
		});
		onBeforeUnmount(() => {
			const { resizeObserver } = internalData;
			if (resizeObserver) resizeObserver.disconnect();
			globalEvents.off($xeList, "resize");
			import_xe_utils.default.assign(reactData, createReactData$3());
			import_xe_utils.default.assign(internalData, createInternalData$3());
		});
		$xeList.renderVN = renderVN;
		return $xeList;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/list/index.js
var VxeList = Object.assign(list_default, { install(app) {
	app.component(list_default.name, list_default);
} });
dynamicApp.use(VxeList);
VxeUI.component(list_default);
var List = VxeList;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/menu/src/menu.js
var { menus: menus$2, getConfig: getConfig$2, getIcon: getIcon$2 } = VxeUI;
var menu_default = defineVxeComponent({
	name: "VxeMenu",
	props: {
		modelValue: [String, Number],
		expandAll: Boolean,
		accordion: {
			type: Boolean,
			default: () => getConfig$2().menu.accordion
		},
		collapsed: {
			type: Boolean,
			default: null
		},
		collapseFixed: Boolean,
		loading: Boolean,
		options: {
			type: Array,
			default: () => []
		},
		size: {
			type: String,
			default: () => getConfig$2().menu.size || getConfig$2().size
		},
		menuConfig: Object
	},
	emits: [
		"update:modelValue",
		"click",
		"option-menu",
		"menu-click"
	],
	setup(props, context) {
		const { emit, slots } = context;
		const xID = import_xe_utils.default.uniqueId();
		const $xeLayoutAside = inject("$xeLayoutAside", null);
		const refElem = ref();
		const refCollapseElem = ref();
		const { computeSize } = useSize(props);
		const reactData = reactive({
			initialized: !!props.collapsed,
			isEnterCollapse: false,
			collapseStyle: {},
			collapseZindex: 0,
			activeName: props.modelValue,
			menuList: [],
			itemHeight: 1
		});
		const refMaps = { refElem };
		const computeMenuOpts = computed(() => {
			return Object.assign({}, getConfig$2().menu.menuConfig, props.menuConfig);
		});
		const computeIsCollapsed = computed(() => {
			const { collapsed } = props;
			if (import_xe_utils.default.isBoolean(collapsed)) return collapsed;
			if ($xeLayoutAside) return !!$xeLayoutAside.props.collapsed;
			return false;
		});
		const computeCollapseWidth = computed(() => {
			let collapseWidth = "";
			if ($xeLayoutAside) collapseWidth = $xeLayoutAside.props.collapseWidth || "";
			return collapseWidth;
		});
		const computeCollapseEnterWidth = computed(() => {
			let width = "";
			if ($xeLayoutAside) width = $xeLayoutAside.props.width || "";
			return width;
		});
		const computeMaps = { computeSize };
		const $xeMenu = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const getMenuTitle = (item) => {
			return `${item.title || item.name}`;
		};
		const updateZindex = () => {
			if (reactData.collapseZindex < getLastZIndex()) reactData.collapseZindex = nextZIndex();
		};
		const updateActiveMenu = (isDefExpand) => {
			const { activeName } = reactData;
			import_xe_utils.default.eachTree(reactData.menuList, (item, index, items, path, parent, nodes) => {
				if (item.itemKey === activeName) {
					nodes.forEach((obj) => {
						obj.isActive = true;
						if (isDefExpand) obj.isExpand = true;
					});
					item.isExactActive = true;
				} else {
					item.isExactActive = false;
					item.isActive = false;
				}
			}, { children: "childList" });
		};
		const updateMenuConfig = () => {
			const { options, expandAll } = props;
			reactData.menuList = import_xe_utils.default.mapTree(options, (item, index, items, path, parent) => {
				return Object.assign(Object.assign({}, item), {
					parentKey: parent ? parent.name || path.slice(0, path.length - 1).join(",") : "",
					level: path.length,
					itemKey: item.name || path.join(","),
					isExactActive: false,
					isActive: false,
					isExpand: import_xe_utils.default.isBoolean(item.expanded) ? item.expanded : !!expandAll,
					hasChild: item.children && item.children.length > 0
				});
			}, {
				children: "children",
				mapChildren: "childList"
			});
		};
		const updateCollapseStyle = () => {
			const { collapseFixed } = props;
			if (collapseFixed) nextTick(() => {
				const { isEnterCollapse } = reactData;
				const isCollapsed = computeIsCollapsed.value;
				const collapseEnterWidth = computeCollapseEnterWidth.value;
				const collapseWidth = computeCollapseWidth.value;
				const el = refElem.value;
				if (el) {
					const clientRect = el.getBoundingClientRect();
					const parentNode = el.parentNode;
					reactData.collapseStyle = isCollapsed ? {
						top: toCssUnit(clientRect.top),
						left: toCssUnit(clientRect.left),
						height: toCssUnit(parentNode.clientHeight),
						width: isEnterCollapse ? collapseEnterWidth ? toCssUnit(collapseEnterWidth) : "" : collapseWidth ? toCssUnit(collapseWidth) : "",
						zIndex: reactData.collapseZindex
					} : {};
				}
			});
		};
		const handleCollapseMenu = () => {
			const { collapseFixed } = props;
			if (collapseFixed) {
				const { initialized } = reactData;
				if (computeIsCollapsed.value) {
					if (!initialized) {
						reactData.initialized = true;
						nextTick(() => {
							const collapseEl = refCollapseElem.value;
							if (collapseEl) document.body.appendChild(collapseEl);
						});
					}
				}
				reactData.isEnterCollapse = false;
				updateZindex();
				updateCollapseStyle();
			}
		};
		const handleClickIconCollapse = (evnt, item, itemList) => {
			const { accordion } = props;
			const { hasChild, isExpand } = item;
			if (hasChild) {
				evnt.stopPropagation();
				evnt.preventDefault();
				if (accordion) itemList.forEach((obj) => {
					if (obj !== item) obj.isExpand = false;
				});
				item.isExpand = !isExpand;
			}
		};
		const emitModel = (value) => {
			reactData.activeName = value;
			emit("update:modelValue", value);
		};
		const handleContextmenuEvent = (evnt, item) => {
			const { menuConfig } = props;
			const menuOpts = computeMenuOpts.value;
			if (menuConfig ? isEnableConf(menuOpts) : menuOpts.enabled) {
				const { options, visibleMethod } = menuOpts;
				if (!visibleMethod || visibleMethod({
					$menu: $xeMenu,
					options,
					currentMenu: item
				})) {
					if (VxeUI.contextMenu) VxeUI.contextMenu.openByEvent(evnt, {
						options,
						events: { optionClick(eventParams) {
							const { option } = eventParams;
							const gMenuOpts = menus$2.get(option.code);
							const mmMethod = gMenuOpts ? gMenuOpts.menuMenuMethod : null;
							const params = {
								menu: option,
								currentMenu: item,
								$event: evnt,
								$menu: $xeMenu
							};
							if (mmMethod) mmMethod(params, evnt);
							dispatchEvent("menu-click", params, eventParams.$event);
						} }
					});
				}
			}
			dispatchEvent("option-menu", { currentMenu: item }, evnt);
		};
		const handleClickMenu = (evnt, item, itemList) => {
			const { itemKey, routerLink, hasChild } = item;
			if (routerLink) {
				emitModel(itemKey);
				handleMenuMouseleave();
			} else if (hasChild) handleClickIconCollapse(evnt, item, itemList);
			else {
				emitModel(itemKey);
				handleMenuMouseleave();
			}
			dispatchEvent("click", {
				currentMenu: item,
				menu: item
			}, evnt);
		};
		const handleMenuMouseenter = () => {
			const { collapseStyle } = reactData;
			const collapseEnterWidth = computeCollapseEnterWidth.value;
			reactData.collapseStyle = Object.assign({}, collapseStyle, { width: collapseEnterWidth ? toCssUnit(collapseEnterWidth) : "" });
			reactData.isEnterCollapse = true;
		};
		const handleMenuMouseover = () => {
			const { isEnterCollapse } = reactData;
			if (!isEnterCollapse) handleMenuMouseenter();
		};
		const handleMenuMouseleave = () => {
			const { collapseStyle } = reactData;
			const el = refElem.value;
			reactData.collapseStyle = Object.assign({}, collapseStyle, { width: el ? toCssUnit(el.offsetWidth) : "" });
			reactData.isEnterCollapse = false;
		};
		const callSlot = (slotFunc, params) => {
			if (slotFunc) {
				if (import_xe_utils.default.isString(slotFunc)) slotFunc = slots[slotFunc] || null;
				if (import_xe_utils.default.isFunction(slotFunc)) return getSlotVNs$1(slotFunc(params));
			}
			return [];
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $menu: $xeMenu }, params));
		};
		Object.assign($xeMenu, { dispatchEvent }, {});
		const renderMenuTitle = (item, itemList) => {
			const { icon, isExpand, hasChild } = item;
			const itemSlots = item.slots || {};
			const optionSlot = itemSlots.default || slots.option;
			const titleSlot = itemSlots.title || slots.optionTitle || slots["option-title"];
			const iconSlot = itemSlots.icon || slots.optionIcon || slots["option-icon"];
			const title = getMenuTitle(item);
			const params = {
				currentMenu: item,
				collapsed: computeIsCollapsed.value,
				option: item
			};
			return [
				optionSlot ? renderEmptyElement($xeMenu) : h("div", { class: "vxe-menu--item-link-icon" }, iconSlot ? callSlot(iconSlot, params) : icon ? [h("i", { class: icon })] : []),
				optionSlot ? h("div", { class: "vxe-menu--item-custom-title" }, callSlot(optionSlot, params)) : h("div", {
					class: "vxe-menu--item-link-title",
					title
				}, titleSlot ? callSlot(titleSlot, params) : title),
				hasChild ? h("div", {
					class: "vxe-menu--item-link-collapse",
					onClick(evnt) {
						handleClickIconCollapse(evnt, item, itemList);
					}
				}, [h("i", { class: isExpand ? getIcon$2().MENU_ITEM_EXPAND_OPEN : getIcon$2().MENU_ITEM_EXPAND_CLOSE })]) : renderEmptyElement($xeMenu)
			];
		};
		const renderDefaultChildren = (item, itemList) => {
			const { itemKey, level, hasChild, isActive, isExactActive, isExpand, routerLink, childList } = item;
			const { isEnterCollapse } = reactData;
			const isCollapsed = computeIsCollapsed.value;
			if (item.permissionCode) {
				if (!permission.checkVisible(item.permissionCode)) return renderEmptyElement($xeMenu);
			}
			return h("div", {
				key: itemKey,
				class: [
					"vxe-menu--item-wrapper",
					`vxe-menu--item-level${level}`,
					{
						"is--exact-active": isExactActive,
						"is--active": isActive,
						"is--expand": (!isCollapsed || isEnterCollapse) && isExpand
					}
				]
			}, [routerLink ? h(resolveComponent("router-link"), {
				class: "vxe-menu--item-link",
				to: routerLink,
				onContextmenu(evnt) {
					handleContextmenuEvent(evnt, item);
				},
				onClick(evnt) {
					handleClickMenu(evnt, item, itemList);
				}
			}, { default: () => renderMenuTitle(item, itemList) }) : h("div", {
				class: "vxe-menu--item-link",
				onContextmenu(evnt) {
					handleContextmenuEvent(evnt, item);
				},
				onClick(evnt) {
					handleClickMenu(evnt, item, itemList);
				}
			}, renderMenuTitle(item, itemList)), hasChild ? h("div", { class: "vxe-menu--item-group" }, childList.map((child) => renderDefaultChildren(child, childList))) : renderEmptyElement($xeMenu)]);
		};
		const renderCollapseChildren = (item, itemList) => {
			const { itemKey, level, hasChild, isActive, isExactActive, routerLink, childList } = item;
			if (item.permissionCode) {
				if (!permission.checkVisible(item.permissionCode)) return renderEmptyElement($xeMenu);
			}
			return h("div", {
				key: itemKey,
				class: [
					"vxe-menu--item-wrapper",
					`vxe-menu--item-level${level}`,
					{
						"is--exact-active": isExactActive,
						"is--active": isActive
					}
				]
			}, [routerLink ? h(resolveComponent("router-link"), {
				class: "vxe-menu--item-link",
				to: routerLink,
				onContextmenu(evnt) {
					handleContextmenuEvent(evnt, item);
				},
				onClick(evnt) {
					handleClickMenu(evnt, item, itemList);
				}
			}, { default: () => renderMenuTitle(item, itemList) }) : h("div", {
				class: "vxe-menu--item-link",
				onContextmenu(evnt) {
					handleContextmenuEvent(evnt, item);
				},
				onClick(evnt) {
					handleClickMenu(evnt, item, itemList);
				}
			}, renderMenuTitle(item, itemList)), hasChild ? h("div", { class: "vxe-menu--item-group" }, childList.map((child) => renderDefaultChildren(child, childList))) : renderEmptyElement($xeMenu)]);
		};
		const renderVN = () => {
			const { loading, collapseFixed } = props;
			const { initialized, menuList, collapseStyle, isEnterCollapse } = reactData;
			const vSize = computeSize.value;
			const isCollapsed = computeIsCollapsed.value;
			let ons = {};
			if (collapseFixed) ons = {
				onMouseenter: handleMenuMouseenter,
				onMouseover: handleMenuMouseover,
				onMouseleave: handleMenuMouseleave
			};
			return h("div", {
				ref: refElem,
				class: ["vxe-menu", {
					[`size--${vSize}`]: vSize,
					"is--collapsed": isCollapsed,
					"is--loading": loading
				}]
			}, [
				h("div", { class: "vxe-menu--item-list" }, menuList.map((child) => isCollapsed ? renderCollapseChildren(child, menuList) : renderDefaultChildren(child, menuList))),
				initialized ? h("div", Object.assign({
					ref: refCollapseElem,
					class: ["vxe-menu--collapse-wrapper", {
						[`size--${vSize}`]: vSize,
						"is--collapsed": isCollapsed,
						"is--enter": isEnterCollapse,
						"is--loading": loading
					}],
					style: collapseStyle
				}, ons), [isCollapsed ? h("div", { class: "vxe-menu--item-list" }, menuList.map((child) => renderDefaultChildren(child, menuList))) : renderEmptyElement($xeMenu)]) : renderEmptyElement($xeMenu),
				h(VxeLoading, {
					class: "vxe-list-view--loading",
					modelValue: loading
				})
			]);
		};
		const optFlag = ref(0);
		watch(() => props.options ? props.options.length : -1, () => {
			optFlag.value++;
		});
		watch(() => props.options, () => {
			optFlag.value++;
		});
		watch(optFlag, () => {
			updateMenuConfig();
			updateActiveMenu(true);
		});
		watch(() => props.modelValue, (val) => {
			reactData.activeName = val;
		});
		watch(() => reactData.activeName, () => {
			updateActiveMenu(true);
		});
		watch(computeIsCollapsed, () => {
			handleCollapseMenu();
		});
		onMounted(() => {
			const { menuConfig } = props;
			const VxeUIContextMenu = VxeUI.getComponent("VxeContextMenu");
			if (menuConfig && !VxeUIContextMenu) errLog("vxe.error.reqComp", ["vxe-context-menu"]);
			globalEvents.on($xeMenu, "resize", updateCollapseStyle);
			updateCollapseStyle();
		});
		onBeforeUnmount(() => {
			globalEvents.off($xeMenu, "resize");
			const collapseEl = refCollapseElem.value;
			if (collapseEl) {
				const parentNode = collapseEl.parentNode;
				if (parentNode) parentNode.removeChild(collapseEl);
			}
		});
		updateMenuConfig();
		updateActiveMenu(true);
		$xeMenu.renderVN = renderVN;
		return $xeMenu;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/menu/index.js
var VxeMenu = Object.assign({}, menu_default, { install(app) {
	app.component(menu_default.name, menu_default);
} });
dynamicApp.use(VxeMenu);
VxeUI.component(menu_default);
var Menu = VxeMenu;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/notice-bar/src/notice-bar.js
var notice_bar_default = defineVxeComponent({
	name: "VxeNoticeBar",
	props: {
		duration: [String, Number],
		direction: {
			type: String,
			default: () => getConfig().noticeBar.direction
		},
		speed: {
			type: String,
			default: () => getConfig().noticeBar.speed
		},
		content: String,
		vertical: Boolean,
		loop: { type: Boolean },
		size: {
			type: String,
			default: () => getConfig().noticeBar.size || getConfig().size
		}
	},
	emits: ["start", "end"],
	setup(props, context) {
		const { slots, emit } = context;
		const $xeTabs = inject("$xeTabs", null);
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const refElem = ref();
		const refContentElem = ref();
		const reactData = reactive({
			animationStatus: false,
			animationDuration: 0
		});
		const refMaps = { refElem };
		const computeNoticeText = computed(() => {
			const { content } = props;
			return `${content || ""}`;
		});
		const computeTabsResizeFlag = computed(() => {
			return $xeTabs ? $xeTabs.reactData.resizeFlag : null;
		});
		const computeMaps = {};
		const $xeNoticeBar = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $noticeBar: $xeNoticeBar }, params));
		};
		const noticeBarMethods = { dispatchEvent };
		const noticeBarPrivateMethods = {};
		const updateAnimationStyle = () => {
			const { speed } = props;
			const contEl = refContentElem.value;
			if (contEl) {
				let sRate = 46;
				if (speed === "fast") sRate = 118;
				else if (speed === "slow") sRate = 18;
				reactData.animationDuration = Math.ceil(contEl.scrollWidth / sRate);
			}
		};
		const animationStartEvent = (evnt) => {
			reactData.animationStatus = true;
			dispatchEvent("start", { status: true }, evnt);
		};
		const animationEndEvent = (evnt) => {
			reactData.animationStatus = false;
			dispatchEvent("end", { status: false }, evnt);
		};
		Object.assign($xeNoticeBar, noticeBarMethods, noticeBarPrivateMethods);
		const renderVN = () => {
			const { vertical, duration, direction, loop } = props;
			const { animationDuration, animationStatus } = reactData;
			const vSize = computeSize.value;
			const noticeText = computeNoticeText.value;
			const defaultSlot = slots.default;
			const prefixSlot = slots.prefix;
			const suffixSlot = slots.suffix;
			return h("div", {
				ref: refElem,
				class: [
					"vxe-notice-bar",
					`is--${vertical ? "vertical" : "horizontal"}`,
					`dir--${direction || "left"}`,
					{
						[`size--${vSize}`]: vSize,
						"is--loop": loop
					}
				]
			}, [
				prefixSlot ? h("div", { class: "vxe-notice-bar--prefix" }, prefixSlot({})) : renderEmptyElement($xeNoticeBar),
				h("div", { class: "vxe-notice-bar--content" }, [h("div", {
					ref: refContentElem,
					class: "vxe-notice-bar--inner"
				}, [h("div", {
					class: ["vxe-notice-bar--wrapper", `is--${animationStatus ? "progress" : "end"}`],
					style: { animationDuration: duration ? toCssUnit(duration, "s") : `${animationDuration}s` },
					onAnimationstart: animationStartEvent,
					onAnimationend: animationEndEvent
				}, defaultSlot ? defaultSlot({}) : noticeText)])]),
				suffixSlot ? h("div", { class: "vxe-notice-bar--suffix" }, suffixSlot({})) : renderEmptyElement($xeNoticeBar)
			]);
		};
		watch(computeTabsResizeFlag, () => {
			updateAnimationStyle();
		});
		onMounted(() => {
			globalEvents.on($xeNoticeBar, "resize", updateAnimationStyle);
			updateAnimationStyle();
		});
		onBeforeUnmount(() => {
			globalEvents.off($xeNoticeBar, "resize");
		});
		$xeNoticeBar.renderVN = renderVN;
		return $xeNoticeBar;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/notice-bar/index.js
var VxeNoticeBar = Object.assign({}, notice_bar_default, { install(app) {
	app.component(notice_bar_default.name, notice_bar_default);
} });
dynamicApp.use(VxeNoticeBar);
VxeUI.component(notice_bar_default);
var NoticeBar = VxeNoticeBar;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/select/src/option-info.js
var OptionInfo = class {
	constructor($xeselect, _vm) {
		Object.assign(this, {
			id: import_xe_utils.default.uniqueId("option_"),
			value: _vm.value,
			label: _vm.label,
			visible: _vm.visible,
			className: _vm.className,
			disabled: _vm.disabled
		});
	}
	update(name, value) {
		this[name] = value;
	}
};
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/select/src/util.js
function isOption(option) {
	return option instanceof OptionInfo;
}
function createOption($xeSelect, _vm) {
	return isOption(_vm) ? _vm : new OptionInfo($xeSelect, _vm);
}
function watchOption(props, option) {
	Object.keys(props).forEach((name) => {
		watch(() => props[name], (value) => {
			option.update(name, value);
		});
	});
}
function assembleOption($xeSelect, el, option, $xeOptgroup) {
	const { reactData } = $xeSelect;
	const { staticOptions } = reactData;
	const parentElem = el.parentNode;
	const parentOption = $xeOptgroup ? $xeOptgroup.optionConfig : null;
	const parentCols = parentOption ? parentOption.options : staticOptions;
	if (parentElem && parentCols) {
		parentCols.splice(import_xe_utils.default.arrayIndexOf(parentElem.children, el), 0, option);
		reactData.staticOptions = staticOptions.slice(0);
	}
}
function destroyOption($xeSelect, option) {
	const { reactData } = $xeSelect;
	const { staticOptions } = reactData;
	const matchObj = import_xe_utils.default.findTree(staticOptions, (item) => item.id === option.id, { children: "options" });
	if (matchObj) matchObj.items.splice(matchObj.index, 1);
	reactData.staticOptions = staticOptions.slice(0);
}
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/select/src/optgroup.js
var optgroup_default = defineVxeComponent({
	name: "VxeOptgroup",
	props: {
		label: {
			type: [
				String,
				Number,
				Boolean
			],
			default: ""
		},
		visible: {
			type: Boolean,
			default: null
		},
		className: [String, Function],
		disabled: Boolean
	},
	emits: [],
	setup(props, { slots }) {
		const elem = ref();
		const $xeSelect = inject("$xeSelect", {});
		const optionConfig = createOption($xeSelect, props);
		const $xeOptgroup = { optionConfig };
		optionConfig.options = [];
		provide("$xeOptgroup", $xeOptgroup);
		watchOption(props, optionConfig);
		onMounted(() => {
			const el = elem.value;
			assembleOption($xeSelect, el, optionConfig);
		});
		onUnmounted(() => {
			destroyOption($xeSelect, optionConfig);
		});
		return () => {
			const defaultSlot = slots.default;
			return h("div", { ref: elem }, defaultSlot ? defaultSlot({}) : []);
		};
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/optgroup/index.js
var VxeOptgroup = Object.assign(optgroup_default, { install: function(app) {
	app.component(optgroup_default.name, optgroup_default);
} });
dynamicApp.use(VxeOptgroup);
VxeUI.component(optgroup_default);
var Optgroup = VxeOptgroup;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/select/src/option.js
var option_default = defineVxeComponent({
	name: "VxeOption",
	props: {
		value: [
			String,
			Number,
			Boolean
		],
		label: {
			type: [
				String,
				Number,
				Boolean
			],
			default: ""
		},
		visible: {
			type: Boolean,
			default: null
		},
		className: [String, Function],
		disabled: Boolean
	},
	emits: [],
	setup(props, { slots }) {
		const elem = ref();
		const $xeSelect = inject("$xeSelect", {});
		const $xeOptgroup = inject("$xeOptgroup", null);
		const optionConfig = createOption($xeSelect, props);
		optionConfig.slots = slots;
		watchOption(props, optionConfig);
		onMounted(() => {
			const el = elem.value;
			assembleOption($xeSelect, el, optionConfig, $xeOptgroup);
		});
		onUnmounted(() => {
			destroyOption($xeSelect, optionConfig);
		});
		return () => {
			return h("div", { ref: elem });
		};
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/option/index.js
var VxeOption = Object.assign(option_default, { install: function(app) {
	app.component(option_default.name, option_default);
} });
dynamicApp.use(VxeOption);
VxeUI.component(option_default);
var Option = VxeOption;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/password-input/src/password-input.js
var password_input_default = defineVxeComponent({
	name: "VxePasswordInput",
	props: {
		modelValue: String,
		immediate: {
			type: Boolean,
			default: true
		},
		name: String,
		clearable: {
			type: Boolean,
			default: () => getConfig().passwordInput.clearable
		},
		readonly: Boolean,
		disabled: Boolean,
		maxLength: [String, Number],
		placeholder: String,
		autoComplete: {
			type: String,
			default: "off"
		},
		className: String,
		size: {
			type: String,
			default: () => getConfig().passwordInput.size || getConfig().size
		},
		prefixIcon: String,
		suffixIcon: String,
		controls: {
			type: Boolean,
			default: () => getConfig().passwordInput.controls
		},
		autocomplete: String
	},
	emits: [
		"update:modelValue",
		"input",
		"change",
		"click",
		"focus",
		"blur",
		"clear",
		"lazy-change",
		"toggle-visible",
		"prefix-click",
		"suffix-click"
	],
	setup(props, context) {
		const { emit, slots } = context;
		const $xeForm = inject("$xeForm", null);
		const formItemInfo = inject("xeFormItemInfo", null);
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const reactData = reactive({
			showPwd: false,
			isActivated: false,
			inputValue: props.modelValue
		});
		const refElem = ref();
		const refInputTarget = ref();
		const refMaps = {
			refElem,
			refInput: refInputTarget
		};
		const $xePasswordInput = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps
		};
		let passwordInputMethods = {};
		const computeIsClearable = computed(() => {
			return props.clearable;
		});
		const computeInpReadonly = computed(() => {
			const { readonly } = props;
			return readonly;
		});
		const computeInpPlaceholder = computed(() => {
			const { placeholder } = props;
			if (placeholder) return getFuncText(placeholder);
			const globalPlaceholder = getConfig().passwordInput.placeholder;
			if (globalPlaceholder) return getFuncText(globalPlaceholder);
			return getI18n("vxe.base.pleaseInput");
		});
		const computeInputType = computed(() => {
			const { showPwd } = reactData;
			if (showPwd) return "text";
			return "password";
		});
		const computeInpImmediate = computed(() => {
			const { immediate } = props;
			return immediate;
		});
		const triggerEvent = (evnt) => {
			const { inputValue } = reactData;
			passwordInputMethods.dispatchEvent(evnt.type, { value: inputValue }, evnt);
		};
		const emitInputEvent = (value, evnt) => {
			const inpImmediate = computeInpImmediate.value;
			reactData.inputValue = value;
			if (inpImmediate) handleChange(value, evnt);
			else passwordInputMethods.dispatchEvent("input", { value }, evnt);
		};
		const inputEvent = (evnt) => {
			const value = evnt.target.value;
			emitInputEvent(value, evnt);
		};
		const handleChange = (value, evnt) => {
			reactData.inputValue = value;
			emit("update:modelValue", value);
			passwordInputMethods.dispatchEvent("input", { value }, evnt);
			if (import_xe_utils.default.toValueString(props.modelValue) !== value) {
				passwordInputMethods.dispatchEvent("change", { value }, evnt);
				if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, value);
			}
		};
		const changeEvent = (evnt) => {
			triggerEvent(evnt);
			$xePasswordInput.dispatchEvent("lazy-change", { value: reactData.inputValue }, evnt);
			if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, reactData.inputValue);
		};
		const focusEvent = (evnt) => {
			reactData.isActivated = true;
			triggerEvent(evnt);
		};
		const blurEvent = (evnt) => {
			const { inputValue } = reactData;
			const value = inputValue;
			$xePasswordInput.dispatchEvent("blur", { value }, evnt);
			if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, value);
		};
		const passwordToggleEvent = (evnt) => {
			const { readonly, disabled } = props;
			const { showPwd } = reactData;
			if (!disabled && !readonly) reactData.showPwd = !showPwd;
			$xePasswordInput.dispatchEvent("toggle-visible", { visible: reactData.showPwd }, evnt);
		};
		const clickEvent = (evnt) => {
			triggerEvent(evnt);
		};
		const clearValueEvent = (evnt, value) => {
			focus();
			handleChange("", evnt);
			$xePasswordInput.dispatchEvent("clear", { value }, evnt);
			$xePasswordInput.dispatchEvent("lazy-change", { value: reactData.inputValue }, evnt);
		};
		const clickSuffixEvent = (evnt) => {
			const { disabled } = props;
			if (!disabled) {
				const { inputValue } = reactData;
				$xePasswordInput.dispatchEvent("suffix-click", { value: inputValue }, evnt);
			}
		};
		const clickPrefixEvent = (evnt) => {
			const { disabled } = props;
			if (!disabled) {
				const { inputValue } = reactData;
				$xePasswordInput.dispatchEvent("prefix-click", { value: inputValue }, evnt);
			}
		};
		const renderPasswordIcon = () => {
			const { showPwd } = reactData;
			return h("div", {
				class: "vxe-password-input--control-icon",
				onClick: passwordToggleEvent
			}, [h("i", { class: ["vxe-password-input--password-icon", showPwd ? getIcon().PASSWORD_INPUT_SHOW_PWD : getIcon().PASSWORD_INPUT_HIDE_PWD] })]);
		};
		const renderPrefixIcon = () => {
			const { prefixIcon } = props;
			const prefixSlot = slots.prefix;
			return prefixSlot || prefixIcon ? h("div", {
				class: "vxe-password-input--prefix",
				onClick: clickPrefixEvent
			}, [h("div", { class: "vxe-password-input--prefix-icon" }, prefixSlot ? getSlotVNs$1(prefixSlot({})) : [h("i", { class: prefixIcon })])]) : null;
		};
		const renderSuffixIcon = () => {
			const { disabled, suffixIcon, controls } = props;
			const { inputValue } = reactData;
			const suffixSlot = slots.suffix;
			const isClearable = computeIsClearable.value;
			return isClearable || controls || suffixSlot || suffixIcon ? h("div", { class: ["vxe-password-input--suffix", { "is--clear": isClearable && !disabled && !(inputValue === "" || import_xe_utils.default.eqNull(inputValue)) }] }, [
				isClearable ? h("div", {
					class: "vxe-password-input--clear-icon",
					onClick: clearValueEvent
				}, [h("i", { class: getIcon().INPUT_CLEAR })]) : renderEmptyElement($xePasswordInput),
				controls ? renderPasswordIcon() : renderEmptyElement($xePasswordInput),
				suffixSlot || suffixIcon ? h("div", {
					class: "vxe-password-input--suffix-icon",
					onClick: clickSuffixEvent
				}, suffixSlot ? getSlotVNs$1(suffixSlot({})) : [h("i", { class: suffixIcon })]) : renderEmptyElement($xePasswordInput)
			]) : null;
		};
		passwordInputMethods = {
			dispatchEvent(type, params, evnt) {
				emit(type, createEvent(evnt, { $passwordInput: $xePasswordInput }, params));
			},
			focus() {
				const inputElem = refInputTarget.value;
				reactData.isActivated = true;
				inputElem.focus();
				return nextTick();
			},
			blur() {
				refInputTarget.value.blur();
				reactData.isActivated = false;
				return nextTick();
			},
			select() {
				refInputTarget.value.select();
				reactData.isActivated = false;
				return nextTick();
			}
		};
		Object.assign($xePasswordInput, passwordInputMethods);
		watch(() => props.modelValue, (val) => {
			reactData.inputValue = val;
		});
		const renderVN = () => {
			const { className, name, disabled, readonly, autocomplete, autoComplete, maxLength } = props;
			const { inputValue, isActivated } = reactData;
			const vSize = computeSize.value;
			const inpReadonly = computeInpReadonly.value;
			const inputType = computeInputType.value;
			const inpPlaceholder = computeInpPlaceholder.value;
			const isClearable = computeIsClearable.value;
			const prefix = renderPrefixIcon();
			const suffix = renderSuffixIcon();
			return h("div", {
				ref: refElem,
				class: [
					"vxe-password-input",
					className,
					{
						[`size--${vSize}`]: vSize,
						"is--prefix": !!prefix,
						"is--suffix": !!suffix,
						"is--readonly": readonly,
						"is--disabled": disabled,
						"is--active": isActivated,
						"show--clear": isClearable && !disabled && !(inputValue === "" || import_xe_utils.default.eqNull(inputValue))
					}
				],
				spellcheck: false
			}, [
				prefix || renderEmptyElement($xePasswordInput),
				h("div", { class: "vxe-password-input--wrapper" }, [h("input", {
					ref: refInputTarget,
					class: "vxe-password-input--inner",
					value: inputValue,
					name,
					type: inputType,
					placeholder: inpPlaceholder,
					readonly: inpReadonly,
					disabled,
					autocomplete: autocomplete || autoComplete,
					maxlength: maxLength,
					onClick: clickEvent,
					onInput: inputEvent,
					onChange: changeEvent,
					onFocus: focusEvent,
					onBlur: blurEvent
				})]),
				suffix || renderEmptyElement($xePasswordInput)
			]);
		};
		$xePasswordInput.renderVN = renderVN;
		return $xePasswordInput;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/password-input/index.js
var VxePasswordInput = Object.assign({}, password_input_default, { install(app) {
	app.component(password_input_default.name, password_input_default);
} });
dynamicApp.use(VxePasswordInput);
VxeUI.component(password_input_default);
var PasswordInput = VxePasswordInput;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/print/src/util.js
var browseObj = import_xe_utils.default.browse();
var printFrame;
var defaultHtmlStyle = "body{padding:0;font-family:\"Microsoft YaHei\",微软雅黑,\"MicrosoftJhengHei\",华文细黑,STHeiti,MingLiu}body *{-webkit-box-sizing:border-box;box-sizing:border-box}.vxe-table{border-collapse:collapse;text-align:left;border-spacing:0}.vxe-table:not(.is--print){table-layout:fixed}.vxe-table,.vxe-table th,.vxe-table td,.vxe-table td{border-color:#D0D0D0;border-style:solid;border-width:0}.vxe-table.is--print{width:100%}.border--default,.border--full,.border--outer{border-top-width:1px}.border--default,.border--full,.border--outer{border-left-width:1px}.border--outer,.border--default th,.border--default td,.border--full th,.border--full td,.border--outer th,.border--inner th,.border--inner td{border-bottom-width:1px}.border--default,.border--outer,.border--full th,.border--full td{border-right-width:1px}.border--default th,.border--full th,.border--outer th{background-color:#f8f8f9}.vxe-table td>div,.vxe-table th>div{padding:.5em .4em}.col--center{text-align:center}.col--right{text-align:right}.vxe-table:not(.is--print) .col--ellipsis>div{overflow:hidden;text-overflow:ellipsis;white-space:nowrap;word-break:break-all}.vxe-table--tree-node{text-align:left}.vxe-table--tree-node-wrapper{position:relative}.vxe-table--tree-icon-wrapper{position:absolute;top:50%;width:1em;height:1em;text-align:center;-webkit-transform:translateY(-50%);transform:translateY(-50%);-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none;cursor:pointer}.vxe-table--tree-unfold-icon,.vxe-table--tree-fold-icon{position:absolute;width:0;height:0;border-style:solid;border-width:.5em;border-right-color:transparent;border-bottom-color:transparent}.vxe-table--tree-unfold-icon{left:.3em;top:0;border-left-color:#939599;border-top-color:transparent}.vxe-table--tree-fold-icon{left:0;top:.3em;border-left-color:transparent;border-top-color:#939599}.vxe-table--tree-cell{display:block;padding-left:1.5em}.vxe-table input[type=\"checkbox\"]{margin:0}.vxe-table input[type=\"checkbox\"],.vxe-table input[type=\"radio\"],.vxe-table input[type=\"checkbox\"]+span,.vxe-table input[type=\"radio\"]+span{vertical-align:middle;padding-left:0.4em}";
function trimHtml(html) {
	return `${html}`.replace(/(<!---->)/, "");
}
function createPrintFrame() {
	const frame = document.createElement("iframe");
	frame.className = "vxe-table--print-frame";
	return frame;
}
function appendPrintFrame() {
	if (!printFrame.parentNode) document.body.appendChild(printFrame);
}
function afterPrintEvent() {
	requestAnimationFrame(removeFrame);
}
function removeFrame() {
	if (printFrame) {
		if (printFrame.parentNode) {
			try {
				printFrame.contentDocument.write("");
			} catch (e) {}
			printFrame.parentNode.removeChild(printFrame);
		}
		printFrame = null;
	}
}
function getExportBlobByString(str, type) {
	return new Blob([str], { type: `text/${type};charset=utf-8;` });
}
var defaultPrintMargin = 50;
var defaultFontColor = "#000000";
function parsePageStyle(val) {
	const styOpts = Object.assign({}, val);
	const headStyOpts = Object.assign({}, styOpts.header);
	const titStyOpts = Object.assign({}, styOpts.title);
	const footStyOpts = Object.assign({}, styOpts.footer);
	const pnStyOpts = Object.assign({}, styOpts.pageNumber);
	let mVal = defaultPrintMargin;
	let marginTop = mVal;
	let marginBottom = mVal;
	let marginLeft = mVal;
	let marginRight = mVal;
	if (import_xe_utils.default.isNumber(styOpts.margin) || import_xe_utils.default.isString(styOpts.margin)) {
		mVal = styOpts.margin;
		marginTop = mVal;
		marginBottom = mVal;
		marginLeft = mVal;
		marginRight = mVal;
	}
	return {
		marginTop: toCssUnit(styOpts.marginTop || marginTop),
		marginBottom: toCssUnit(styOpts.marginBottom || marginBottom),
		marginLeft: toCssUnit(styOpts.marginLeft || marginLeft),
		marginRight: toCssUnit(styOpts.marginRight || marginRight),
		fontSize: toCssUnit(styOpts.fontSize),
		color: styOpts.color,
		textAlign: styOpts.textAlign,
		header: {
			height: toCssUnit(headStyOpts.height),
			textAlign: headStyOpts.textAlign
		},
		title: {
			color: titStyOpts.color,
			fontSize: toCssUnit(titStyOpts.fontSize),
			textAlign: titStyOpts.textAlign
		},
		footer: {
			height: toCssUnit(footStyOpts.height),
			textAlign: footStyOpts.textAlign
		},
		pageNumber: {
			color: pnStyOpts.color,
			fontSize: toCssUnit(pnStyOpts.fontSize),
			textAlign: pnStyOpts.textAlign
		}
	};
}
function createCssLink(urls) {
	if (urls) return urls.map((url) => `<link rel="stylesheet" href="${url}">`).join("\n");
	return "";
}
function createHtmlPage(opts, printHtml) {
	const { pageStyle, customStyle, styleUrls } = opts;
	const pageStyObj = parsePageStyle(pageStyle);
	const headStyOpts = pageStyObj.header;
	const titStyOpts = pageStyObj.title;
	const footStyOpts = pageStyObj.header;
	const pnStyOpts = pageStyObj.pageNumber;
	const isPbMode = opts._pageBreaks || opts.pageBreaks && opts.pageBreaks.length;
	return [
		"<!DOCTYPE html><html>",
		"<head>",
		"<meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1,minimum-scale=1,maximum-scale=1,user-scalable=no,minimal-ui\">",
		`<title>${opts.title || ""}</title>`,
		createCssLink(styleUrls),
		`<style media="print">@page{size:auto;${isPbMode ? "margin: 0mm;" : ""}}</style>`,
		`<style>body{font-size:${pageStyObj.fontSize || "14px"};color:${pageStyObj.color || defaultFontColor};text-align:${pageStyObj.textAlign || "left"};}</style>`,
		"<style>",
		".vxe-print-slots{display:none;}",
		".vxe-print-page-break.align--center{text-align:center;}",
		".vxe-print-page-break.align--left{text-align:left;}",
		".vxe-print-page-break.align--right{text-align:right;}",
		".vxe-print-page-break{break-before:always;page-break-before:always;display:flex;flex-direction:column;height:100vh;overflow:hidden;}",
		".vxe-print-page-break--body{display:flex;flex-direction:row;flex-grow:1;overflow:hidden;}",
		".vxe-print-page-break--left,.vxe-print-page-break--right{flex-shrink:0;height:100%;}",
		`.vxe-print-page-break--left{width:${pageStyObj.marginLeft};}`,
		`.vxe-print-page-break--right{width:${pageStyObj.marginRight};}`,
		".vxe-print-page-break--header,.vxe-print-page-break--footer{display:flex;justify-content:center;flex-direction:column;flex-shrink:0;width:100%;}",
		`.vxe-print-page-break--header{height:${headStyOpts.height || pageStyObj.marginTop};padding:0 ${pageStyObj.marginLeft} 0 ${pageStyObj.marginRight};text-align:${headStyOpts.textAlign || "left"};}`,
		`.vxe-print-page-break--header-title{font-size:${titStyOpts.fontSize || "1.6em"};color:${titStyOpts.color || defaultFontColor};text-align:${opts.headerAlign || pnStyOpts.textAlign || "center"};}`,
		`.vxe-print-page-break--footer{height:${footStyOpts.height || pageStyObj.marginBottom};padding:0 ${pageStyObj.marginLeft} 0 ${pageStyObj.marginRight};text-align:${footStyOpts.textAlign || "left"};}`,
		".vxe-print-page-break--content{flex-grow:1;overflow:hidden;}",
		`.vxe-print-page-break--footer-page-number{font-size:${pnStyOpts.fontSize || "1.2em"};color:${pnStyOpts.color || defaultFontColor};text-align:${opts.footerAlign || pnStyOpts.textAlign || "center"};}`,
		"</style>",
		"<style>.vxe-table{white-space:pre;}</style>",
		`<style>${defaultHtmlStyle}</style>`,
		isPbMode ? "<style>body{margin:0;}</style>" : "",
		customStyle ? `<style>${customStyle}</style>` : "",
		"</head>",
		"<body>",
		`${printHtml}`,
		"</body>",
		"</html>"
	].join("");
}
function handlePrint(opts, printHtml = "") {
	const { beforeMethod, styleUrls } = opts;
	if (styleUrls && styleUrls.length) styleUrls.forEach((url) => {
		if (!/.css$/.test(url)) warnLog("vxe.error.errProp", [url, `${url}.css`]);
	});
	if (beforeMethod) printHtml = beforeMethod({
		content: printHtml,
		html: printHtml,
		options: opts
	}) || "";
	printHtml = createHtmlPage(opts, printHtml);
	const blob = getExportBlobByString(printHtml, "html");
	return new Promise((resolve) => {
		if (browseObj.msie) {
			removeFrame();
			printFrame = createPrintFrame();
			appendPrintFrame();
			const contentDocument = printFrame.contentDocument;
			if (contentDocument) {
				contentDocument.write(printHtml);
				contentDocument.execCommand("print");
			}
			setTimeout(() => {
				resolve({ status: true });
			}, 300);
		} else {
			if (!printFrame) {
				printFrame = createPrintFrame();
				printFrame.onload = (evnt) => {
					const frameEl = evnt.target;
					if (frameEl.src) try {
						const contentWindow = frameEl.contentWindow;
						if (contentWindow) {
							contentWindow.onafterprint = afterPrintEvent;
							contentWindow.print();
						}
					} catch (e) {}
					resolve({ status: true });
				};
				printFrame.onerror = () => {
					resolve({ status: false });
				};
			}
			appendPrintFrame();
			printFrame.src = URL.createObjectURL(blob);
		}
	});
}
function createPageBreak(opts) {
	const { title, showPageNumber, align, headerAlign, footerAlign, showAllPageTitle } = opts;
	const pageBreaks = opts.pageBreaks || [];
	const pageCount = pageBreaks.length;
	return pageBreaks.map((item, index) => {
		const bodyHtml = item.bodyHtml;
		const headerHtml = item.headerHtml || opts.headerHtml;
		const footerHtml = item.footerHtml || opts.footerHtml;
		const leftHtml = item.leftHtml || opts.leftHtml;
		const rightHtml = item.rightHtml || opts.rightHtml;
		const currentPage = index + 1;
		const params = {
			currentPage,
			pageCount
		};
		return [
			`<div class="${["vxe-print-page-break", align ? `align--${align}` : ""].join(" ")}">`,
			`<div class="${["vxe-print-page-break--header", headerAlign ? `align--${headerAlign}` : ""].join(" ")}">`,
			headerHtml ? `${import_xe_utils.default.isFunction(headerHtml) ? headerHtml(params) : headerHtml || ""}` : title && (showAllPageTitle || !index) ? `<div class="vxe-print-page-break--header-title">${title || ""}</div>` : "",
			"</div>",
			"<div class=\"vxe-print-page-break--body\">",
			`<div class="vxe-print-page-break--left">${import_xe_utils.default.isFunction(leftHtml) ? leftHtml(params) : leftHtml || ""}</div>`,
			`<div class="vxe-print-page-break--content">${import_xe_utils.default.isFunction(bodyHtml) ? bodyHtml(params) : bodyHtml || ""}</div>`,
			`<div class="vxe-print-page-break--right">${import_xe_utils.default.isFunction(rightHtml) ? rightHtml(params) : rightHtml || ""}</div>`,
			"</div>",
			`<div class="${["vxe-print-page-break--footer", footerAlign ? `align--${footerAlign}` : ""].join(" ")}">`,
			footerHtml ? `${import_xe_utils.default.isFunction(footerHtml) ? footerHtml(params) : footerHtml || ""}` : showPageNumber ? `<div class="vxe-print-page-break--footer-page-number">${currentPage}/${pageCount}</div>` : "",
			"</div>",
			"</div>"
		].join("");
	}).join("");
}
var printHtml = (options) => {
	const opts = Object.assign({
		_pageBreaks: false,
		customLayout: true
	}, options);
	if (opts.sheetName) opts.title = opts.title || opts.sheetName;
	if (opts.style) opts.customStyle = opts.customStyle || opts.style;
	if (opts.beforePrintMethod) opts.beforeMethod = opts.beforeMethod || opts.beforePrintMethod;
	if (opts.pageBreaks && opts.pageBreaks.length) return handlePrint(opts, createPageBreak(opts));
	return handlePrint(opts, opts.html || opts.content);
};
function assemblePageBreak($xePageBreak, elem, pageBreakConfig) {
	const staticPageBreaks = $xePageBreak.reactData.staticPageBreaks;
	const parentElem = elem.parentNode;
	if (parentElem && staticPageBreaks) {
		staticPageBreaks.splice(import_xe_utils.default.arrayIndexOf(parentElem.children, elem), 0, pageBreakConfig);
		$xePageBreak.reactData.staticPageBreaks = staticPageBreaks.slice(0);
	}
}
function destroyPageBreak($xePageBreak, pageBreakConfig) {
	$xePageBreak.reactData.staticPageBreaks = $xePageBreak.reactData.staticPageBreaks.filter((item) => item.id !== pageBreakConfig.id);
}
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/print/src/page-break.js
var page_break_default = defineVxeComponent({
	name: "VxePrintPageBreak",
	props: {},
	emits: [],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const $xePrint = inject("$xePrint", null);
		const refElem = ref();
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeMaps = {};
		const pageBreakConfig = reactive({
			id: xID,
			slots
		});
		const $xePrintPageBreak = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $print: $xePrint }, params));
		};
		Object.assign($xePrintPageBreak, { dispatchEvent }, {});
		if (!$xePrint) {
			$xePrintPageBreak.renderVN = () => {
				return createCommentVNode();
			};
			return $xePrintPageBreak;
		}
		const renderVN = () => {
			return h("div", { ref: refElem });
		};
		onMounted(() => {
			const elem = refElem.value;
			if ($xePrint && elem) assemblePageBreak($xePrint, elem, pageBreakConfig);
		});
		onUnmounted(() => {
			if ($xePrint) destroyPageBreak($xePrint, pageBreakConfig);
		});
		$xePrintPageBreak.renderVN = renderVN;
		return $xePrintPageBreak;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/print-page-break/index.js
var VxePrintPageBreak = Object.assign({}, page_break_default, { install(app) {
	app.component(page_break_default.name, page_break_default);
} });
dynamicApp.use(VxePrintPageBreak);
VxeUI.component(page_break_default);
var PrintPageBreak = VxePrintPageBreak;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/print/src/print.js
var print_default = defineVxeComponent({
	name: "VxePrint",
	props: {
		align: {
			type: String,
			default: () => getConfig().print.align
		},
		title: String,
		headerAlign: {
			type: String,
			default: () => getConfig().print.headerAlign
		},
		footerAlign: {
			type: String,
			default: () => getConfig().print.footerAlign
		},
		showPageNumber: {
			type: Boolean,
			default: () => getConfig().print.showPageNumber
		},
		customLayout: Boolean,
		pageBreaks: Array,
		content: String,
		html: String,
		headerHtml: String,
		footerHtml: String,
		leftHtml: String,
		rightHtml: String,
		showAllPageTitle: {
			type: Boolean,
			default: () => getConfig().print.showAllPageTitle
		},
		pageStyle: {
			type: Object,
			default: () => getConfig().print.pageStyle
		},
		customStyle: {
			type: String,
			default: () => getConfig().print.customStyle
		},
		styleUrls: {
			type: Array,
			default: () => getConfig().print.styleUrls
		},
		beforeMethod: Function
	},
	emits: [],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const reactData = reactive({ staticPageBreaks: [] });
		const refMaps = { refElem };
		const computeMaps = {};
		const $xePrint = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $print: $xePrint }, params));
		};
		Object.assign($xePrint, {
			dispatchEvent,
			print() {
				const { align, title, headerAlign, footerAlign, showPageNumber, customLayout, pageBreaks, content, headerHtml, footerHtml, leftHtml, rightHtml, showAllPageTitle, pageStyle, customStyle, styleUrls, beforeMethod } = props;
				const elem = refElem.value;
				return printHtml(Object.assign({}, {
					align,
					title,
					headerAlign,
					footerAlign,
					showPageNumber,
					customLayout,
					pageBreaks,
					content,
					headerHtml,
					footerHtml,
					leftHtml,
					rightHtml,
					showAllPageTitle,
					pageStyle,
					customStyle,
					styleUrls,
					beforeMethod
				}, {
					_pageBreaks: !!reactData.staticPageBreaks.length,
					html: trimHtml(elem ? elem.outerHTML : "") || props.html || props.content || ""
				}));
			}
		}, {});
		const renderPageConfigLayouts = () => {
			const { title, showPageNumber, showAllPageTitle, align, headerAlign, footerAlign } = props;
			const pageBreaks = props.pageBreaks || [];
			const pageCount = pageBreaks.length;
			return pageBreaks.map((item, index) => {
				const bodyHtml = item.bodyHtml;
				const headerHtml = item.headerHtml || props.headerHtml;
				const footerHtml = item.footerHtml || props.footerHtml;
				const leftHtml = item.leftHtml || props.leftHtml;
				const rightHtml = item.rightHtml || props.rightHtml;
				const currentPage = index + 1;
				const params = {
					currentPage,
					pageCount
				};
				return h("div", { class: ["vxe-print-page-break", align ? `align--${align}` : ""] }, [
					h("div", { class: ["vxe-print-page-break--header", headerAlign ? `align--${headerAlign}` : ""] }, headerHtml ? `${import_xe_utils.default.isFunction(headerHtml) ? headerHtml(params) : headerHtml || ""}` : [title && (showAllPageTitle || !index) ? h("div", { class: "vxe-print-page-break--header-title" }, `${title || ""}`) : renderEmptyElement($xePrint)]),
					h("div", { class: "vxe-print-page-break--body" }, [
						h("div", { class: "vxe-print-page-break--left" }, `${import_xe_utils.default.isFunction(leftHtml) ? leftHtml(params) : leftHtml || ""}`),
						h("div", { class: "vxe-print-page-break--content" }, `${import_xe_utils.default.isFunction(bodyHtml) ? bodyHtml(params) : bodyHtml || ""}`),
						h("div", { class: "vxe-print-page-break--right" }, `${import_xe_utils.default.isFunction(rightHtml) ? rightHtml(params) : rightHtml || ""}`)
					]),
					h("div", { class: ["vxe-print-page-break--footer", footerAlign ? `align--${footerAlign}` : ""] }, footerHtml ? `${import_xe_utils.default.isFunction(footerHtml) ? footerHtml(params) : footerHtml || ""}` : [showPageNumber ? h("div", { class: "vxe-print-page-break--footer-page-number" }, `${currentPage}/${pageCount}`) : renderEmptyElement($xePrint)])
				]);
			});
		};
		const renderPageStaticLayouts = () => {
			const { title, showPageNumber, showAllPageTitle, align, headerAlign, footerAlign } = props;
			const { staticPageBreaks } = reactData;
			const pageCount = staticPageBreaks.length;
			return staticPageBreaks.map((item, index) => {
				const itemSlots = item.slots || {};
				const currentPage = index + 1;
				const defaultSlot = itemSlots.default;
				const headerSlot = itemSlots.header || slots.header;
				const footerSlot = itemSlots.footer || slots.footer;
				const leftSlot = itemSlots.left || slots.left;
				const rightSlot = itemSlots.right || slots.right;
				const params = {
					currentPage,
					pageCount
				};
				return h("div", { class: ["vxe-print-page-break", align ? `align--${align}` : ""] }, [
					h("div", { class: ["vxe-print-page-break--header", headerAlign ? `align--${headerAlign}` : ""] }, headerSlot ? getSlotVNs$1(headerSlot(params)) : [title && (showAllPageTitle || !index) ? h("div", { class: "vxe-print-page-break--header-title" }, `${title || ""}`) : renderEmptyElement($xePrint)]),
					h("div", { class: "vxe-print-page-break--body" }, [
						h("div", { class: "vxe-print-page-break--left" }, leftSlot ? getSlotVNs$1(leftSlot(params)) : []),
						h("div", { class: "vxe-print-page-break--content" }, defaultSlot ? getSlotVNs$1(defaultSlot(params)) : []),
						h("div", { class: "vxe-print-page-break--right" }, rightSlot ? getSlotVNs$1(rightSlot(params)) : [])
					]),
					h("div", { class: ["vxe-print-page-break--footer", footerAlign ? `align--${footerAlign}` : ""] }, footerSlot ? getSlotVNs$1(footerSlot(params)) : [showPageNumber ? h("div", { class: "vxe-print-page-break--footer-page-number" }, `${currentPage}/${pageCount}`) : renderEmptyElement($xePrint)])
				]);
			});
		};
		const renderVN = () => {
			const { customLayout } = props;
			const { staticPageBreaks } = reactData;
			const defaultSlot = slots.default;
			return h("div", {
				ref: refElem,
				class: ["vxe-print"]
			}, customLayout ? defaultSlot ? getSlotVNs$1(defaultSlot({})) : [] : [h("div", {
				key: "slot",
				class: "vxe-print-slots"
			}, defaultSlot ? getSlotVNs$1(defaultSlot({})) : [])].concat(staticPageBreaks.length ? renderPageStaticLayouts() : renderPageConfigLayouts()));
		};
		$xePrint.renderVN = renderVN;
		provide("$xePrint", $xePrint);
		return $xePrint;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/print/index.js
var VxePrint = Object.assign({}, print_default, { install(app) {
	app.component(print_default.name, print_default);
} });
dynamicApp.use(VxePrint);
VxeUI.component(print_default);
VxeUI.print = printHtml;
var Print = VxePrint;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/pulldown/src/pulldown.js
var pulldown_default = defineVxeComponent({
	name: "VxePulldown",
	props: {
		modelValue: Boolean,
		disabled: Boolean,
		placement: String,
		/**
		* 已废弃，请使用 popup-config.trigger
		* @deprecated
		*/
		trigger: {
			type: String,
			default: getConfig().pulldown.trigger
		},
		/**
		* 已废弃，请使用 popupConfig.zIndex
		* @deprecated
		*/
		zIndex: Number,
		size: {
			type: String,
			default: () => getConfig().pulldown.size || getConfig().size
		},
		options: Array,
		className: {
			type: [String, Function],
			default: getConfig().pulldown.className
		},
		/**
		* 已废弃，请使用 popupConfig.className
		* @deprecated
		*/
		popupClassName: [String, Function],
		showPopupShadow: Boolean,
		popupConfig: Object,
		destroyOnClose: {
			type: Boolean,
			default: getConfig().pulldown.destroyOnClose
		},
		transfer: {
			type: Boolean,
			default: null
		}
	},
	emits: [
		"update:modelValue",
		"click",
		"option-click",
		"show-panel",
		"hide-panel",
		"visible-change"
	],
	setup(props, context) {
		const { slots, emit } = context;
		const $xeModal = inject("$xeModal", null);
		const $xeDrawer = inject("$xeDrawer", null);
		const $xeTable = inject("$xeTable", null);
		const $xeForm = inject("$xeForm", null);
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const reactData = reactive({
			initialized: false,
			panelIndex: 0,
			panelStyle: {},
			panelPlacement: null,
			visiblePanel: false,
			isAniVisible: false,
			isActivated: false
		});
		const internalData = { hpTimeout: void 0 };
		const refElem = ref();
		const refPulldownContent = ref();
		const refPulldownPanel = ref();
		const computeBtnTransfer = computed(() => {
			const { transfer } = props;
			const popupOpts = computePopupOpts.value;
			if (import_xe_utils.default.isBoolean(popupOpts.transfer)) return popupOpts.transfer;
			if (transfer === null) {
				const globalTransfer = getConfig().pulldown.transfer;
				if (import_xe_utils.default.isBoolean(globalTransfer)) return globalTransfer;
				if ($xeTable || $xeModal || $xeDrawer || $xeForm) return true;
			}
			return transfer;
		});
		const computePopupOpts = computed(() => {
			return Object.assign({}, getConfig().pulldown.popupConfig, props.popupConfig);
		});
		const refMaps = { refElem };
		const $xePulldown = {
			xID,
			props,
			context,
			reactData,
			internalData,
			getRefMaps: () => refMaps
		};
		let pulldownMethods = {};
		const updateZindex = () => {
			const customZIndex = computePopupOpts.value.zIndex || props.zIndex;
			if (customZIndex) reactData.panelIndex = import_xe_utils.default.toNumber(customZIndex);
			else if (reactData.panelIndex < getLastZIndex()) reactData.panelIndex = nextZIndex();
		};
		const isPanelVisible = () => {
			return reactData.visiblePanel;
		};
		/**
		* 手动更新位置
		*/
		const updatePlacement = () => {
			const { placement } = props;
			const { panelIndex } = reactData;
			const targetElem = refPulldownContent.value;
			const panelElem = refPulldownPanel.value;
			const btnTransfer = computeBtnTransfer.value;
			const popupOpts = computePopupOpts.value;
			const handleStyle = () => {
				const ppObj = updatePanelPlacement(targetElem, panelElem, {
					placement: popupOpts.placement || placement,
					defaultPlacement: popupOpts.defaultPlacement,
					teleportTo: btnTransfer
				});
				reactData.panelStyle = Object.assign(ppObj.style, { zIndex: panelIndex });
				reactData.panelPlacement = ppObj.placement;
			};
			handleStyle();
			return nextTick().then(handleStyle);
		};
		/**
		* 显示下拉面板
		*/
		const showPanel = () => {
			if (!reactData.initialized) reactData.initialized = true;
			return new Promise((resolve) => {
				if (!props.disabled) {
					if (internalData.hpTimeout) clearTimeout(internalData.hpTimeout);
					reactData.isActivated = true;
					reactData.isAniVisible = true;
					setTimeout(() => {
						reactData.visiblePanel = true;
						emit("update:modelValue", true);
						updatePlacement();
						setTimeout(() => {
							resolve(updatePlacement());
						}, 40);
					}, 10);
					updateZindex();
					dispatchEvent("visible-change", { visible: true }, null);
				} else nextTick(() => {
					resolve();
				});
			});
		};
		/**
		* 隐藏下拉面板
		*/
		const hideOptionPanel = () => {
			reactData.visiblePanel = false;
			dispatchEvent("visible-change", { visible: false }, null);
			emit("update:modelValue", false);
			return new Promise((resolve) => {
				if (reactData.isAniVisible) internalData.hpTimeout = setTimeout(() => {
					reactData.isAniVisible = false;
					nextTick(() => {
						resolve();
					});
				}, 350);
				else nextTick(() => {
					resolve();
				});
			});
		};
		/**
		* 切换下拉面板
		*/
		const togglePanel = () => {
			if (reactData.visiblePanel) return hideOptionPanel();
			return showPanel();
		};
		const handleOptionEvent = (evnt, option) => {
			if (!option.disabled) {
				if (reactData.visiblePanel) {
					hideOptionPanel();
					dispatchEvent("hide-panel", {}, evnt);
				}
				dispatchEvent("option-click", { option }, evnt);
			}
		};
		const clickTargetEvent = (evnt) => {
			const { trigger } = props;
			const popupOpts = computePopupOpts.value;
			if ((trigger || popupOpts.trigger) === "click") if (reactData.visiblePanel) {
				hideOptionPanel();
				dispatchEvent("hide-panel", {}, evnt);
			} else {
				showPanel();
				dispatchEvent("show-panel", {}, evnt);
			}
			dispatchEvent("click", { $pulldown: $xePulldown }, evnt);
		};
		const handleGlobalMousewheelEvent = (evnt) => {
			const { trigger, disabled } = props;
			const { visiblePanel } = reactData;
			const panelElem = refPulldownPanel.value;
			const popupOpts = computePopupOpts.value;
			const currTrigger = trigger || popupOpts.trigger;
			if (!disabled) {
				if (visiblePanel) {
					if (getEventTargetNode(evnt, panelElem).flag) updatePlacement();
					else if (currTrigger !== "manual") {
						hideOptionPanel();
						dispatchEvent("hide-panel", {}, evnt);
					}
				}
			}
		};
		const handleGlobalMousedownEvent = (evnt) => {
			const { trigger, disabled } = props;
			const { visiblePanel } = reactData;
			const popupOpts = computePopupOpts.value;
			const currTrigger = trigger || popupOpts.trigger;
			const el = refElem.value;
			const panelElem = refPulldownPanel.value;
			if (!disabled) {
				reactData.isActivated = getEventTargetNode(evnt, el).flag || getEventTargetNode(evnt, panelElem).flag;
				if (visiblePanel && !reactData.isActivated) {
					if (currTrigger !== "manual") {
						hideOptionPanel();
						dispatchEvent("hide-panel", {}, evnt);
					}
				}
			}
		};
		const handleGlobalBlurEvent = (evnt) => {
			const { trigger } = props;
			const { visiblePanel, isActivated } = reactData;
			const popupOpts = computePopupOpts.value;
			const currTrigger = trigger || popupOpts.trigger;
			if (visiblePanel) {
				if (currTrigger !== "manual") {
					hideOptionPanel();
					dispatchEvent("hide-panel", {}, evnt);
				}
			}
			if (isActivated) reactData.isActivated = false;
		};
		const handleGlobalResizeEvent = () => {
			const { visiblePanel } = reactData;
			if (visiblePanel) updatePlacement();
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $pulldown: $xePulldown }, params));
		};
		pulldownMethods = {
			dispatchEvent,
			isPanelVisible,
			togglePanel,
			showPanel,
			hidePanel: hideOptionPanel
		};
		Object.assign($xePulldown, pulldownMethods);
		watch(() => props.modelValue, (value) => {
			reactData.isActivated = !!value;
			if (value) showPanel();
			else hideOptionPanel();
		});
		nextTick(() => {
			if (props.modelValue) showPanel();
			globalEvents.on($xePulldown, "mousewheel", handleGlobalMousewheelEvent);
			globalEvents.on($xePulldown, "mousedown", handleGlobalMousedownEvent);
			globalEvents.on($xePulldown, "blur", handleGlobalBlurEvent);
			globalEvents.on($xePulldown, "resize", handleGlobalResizeEvent);
		});
		onUnmounted(() => {
			globalEvents.off($xePulldown, "mousewheel");
			globalEvents.off($xePulldown, "mousedown");
			globalEvents.off($xePulldown, "blur");
			globalEvents.off($xePulldown, "resize");
		});
		const renderDefaultPanel = (options) => {
			const optionSlot = slots.option;
			return h("div", { class: "vxe-pulldown--panel-list" }, options ? options.map((item) => {
				return h("div", {
					class: "vxe-pulldown--panel-item",
					onClick(evnt) {
						handleOptionEvent(evnt, item);
					}
				}, optionSlot ? optionSlot({
					$pulldown: $xePulldown,
					option: item
				}) : `${item.label || ""}`);
			}) : []);
		};
		const renderVN = () => {
			const { className, options, showPopupShadow, destroyOnClose, disabled } = props;
			const { initialized, isActivated, isAniVisible, visiblePanel, panelStyle, panelPlacement } = reactData;
			const btnTransfer = computeBtnTransfer.value;
			const vSize = computeSize.value;
			const popupOpts = computePopupOpts.value;
			const defaultSlot = slots.default;
			const headerSlot = slots.header;
			const footerSlot = slots.footer;
			const dropdownSlot = slots.dropdown;
			const ppClassName = popupOpts.className || props.popupClassName;
			return h("div", {
				ref: refElem,
				class: [
					"vxe-pulldown",
					className ? import_xe_utils.default.isFunction(className) ? className({ $pulldown: $xePulldown }) : className : "",
					{
						[`size--${vSize}`]: vSize,
						"is--visible": visiblePanel,
						"is--disabled": disabled,
						"is--active": isActivated
					}
				]
			}, [h("div", {
				ref: refPulldownContent,
				class: "vxe-pulldown--content",
				onClick: clickTargetEvent
			}, defaultSlot ? defaultSlot({ $pulldown: $xePulldown }) : []), h(Teleport, {
				to: "body",
				disabled: btnTransfer ? !initialized : true
			}, [h("div", {
				ref: refPulldownPanel,
				class: [
					"vxe-table--ignore-clear vxe-pulldown--panel",
					ppClassName ? import_xe_utils.default.isFunction(ppClassName) ? ppClassName({ $pulldown: $xePulldown }) : ppClassName : "",
					{
						[`size--${vSize}`]: vSize,
						"is--transfer": btnTransfer,
						"ani--leave": isAniVisible,
						"ani--enter": visiblePanel
					}
				],
				placement: panelPlacement,
				style: panelStyle
			}, [h("div", { class: ["vxe-pulldown--panel-wrapper", { "is--shadow": showPopupShadow }] }, initialized && (destroyOnClose ? visiblePanel || isAniVisible : true) ? [
				headerSlot ? h("div", { class: "vxe-pulldown--panel-header" }, headerSlot({ $pulldown: $xePulldown })) : renderEmptyElement($xePulldown),
				h("div", { class: "vxe-pulldown--panel-body" }, dropdownSlot ? dropdownSlot({ $pulldown: $xePulldown }) : [renderDefaultPanel(options)]),
				footerSlot ? h("div", { class: "vxe-pulldown--panel-footer" }, footerSlot({ $pulldown: $xePulldown })) : renderEmptyElement($xePulldown)
			] : [])])])]);
		};
		$xePulldown.renderVN = renderVN;
		return $xePulldown;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/pulldown/index.js
var VxePulldown = Object.assign(pulldown_default, { install: function(app) {
	app.component(pulldown_default.name, pulldown_default);
} });
dynamicApp.use(VxePulldown);
VxeUI.component(pulldown_default);
var Pulldown = VxePulldown;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/radio/index.js
var VxeRadio = Object.assign(radio_default, { install: function(app) {
	app.component(radio_default.name, radio_default);
} });
dynamicApp.use(VxeRadio);
VxeUI.component(radio_default);
var Radio = VxeRadio;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/radio-button/index.js
var VxeRadioButton = Object.assign(button_default$1, { install: function(app) {
	app.component(button_default$1.name, button_default$1);
} });
dynamicApp.use(VxeRadioButton);
VxeUI.component(button_default$1);
var RadioButton = VxeRadioButton;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/rate/src/rate.js
var rate_default = defineVxeComponent({
	name: "VxeRate",
	props: {
		modelValue: [Number, String],
		disabled: {
			type: Boolean,
			default: null
		},
		readonly: {
			type: Boolean,
			default: null
		},
		size: {
			type: String,
			default: () => getConfig().rate.size || getConfig().size
		},
		status: String
	},
	emits: ["update:modelValue", "change"],
	setup(props, context) {
		const { emit } = context;
		const $xeForm = inject("$xeForm", null);
		const formItemInfo = inject("xeFormItemInfo", null);
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const refElem = ref();
		const reactData = reactive({ activeValue: null });
		const refMaps = { refElem };
		const computeIsDisabled = computed(() => {
			const { disabled } = props;
			if (disabled === null) {
				if ($xeForm) return $xeForm.props.readonly || $xeForm.props.disabled;
				return false;
			}
			return disabled;
		});
		const computeIsReadonly = computed(() => {
			const { readonly } = props;
			if (readonly === null) {
				if ($xeForm) return $xeForm.props.readonly || $xeForm.props.disabled;
				return false;
			}
			return readonly;
		});
		const computeNumVal = computed(() => {
			const { modelValue } = props;
			const { activeValue } = reactData;
			return import_xe_utils.default.toNumber(activeValue === null ? modelValue : activeValue);
		});
		const computeItemList = computed(() => {
			return [
				1,
				2,
				3,
				4,
				5
			].map((num) => {
				return {
					value: num,
					label: num
				};
			});
		});
		const computeMaps = {};
		const $xeRate = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $rate: $xeRate }, params));
		};
		const collapsePaneMethods = { dispatchEvent };
		const collapsePanePrivateMethods = {};
		const emitModel = (value) => {
			emit("update:modelValue", value);
		};
		const mouseenterEvent = (evnt, item) => {
			const isDisabled = computeIsDisabled.value;
			const isReadonly = computeIsReadonly.value;
			if (!(isDisabled || isReadonly)) reactData.activeValue = item.value;
		};
		const mouseleaveEvent = () => {
			reactData.activeValue = null;
		};
		const clickEvent = (evnt, item) => {
			const isDisabled = computeIsDisabled.value;
			const isReadonly = computeIsReadonly.value;
			if (!(isDisabled || isReadonly)) {
				const value = item.value;
				emitModel(value);
				dispatchEvent("change", { value }, evnt);
				if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, value);
			}
		};
		Object.assign($xeRate, collapsePaneMethods, collapsePanePrivateMethods);
		const renderVN = () => {
			const { status } = props;
			const isDisabled = computeIsDisabled.value;
			const isReadonly = computeIsReadonly.value;
			const itemList = computeItemList.value;
			const vSize = computeSize.value;
			const numVal = computeNumVal.value;
			return h("div", {
				ref: refElem,
				class: ["vxe-rate", {
					[`size--${vSize}`]: vSize,
					[`theme--${status}`]: status,
					"is--disabled": isDisabled,
					"is--readonly": isReadonly
				}]
			}, itemList.map((item) => {
				const isChecked = numVal >= item.value;
				return h("div", {
					class: ["vxe-rte--item", { "is--checked": isChecked }],
					onMouseenter(evnt) {
						if (!(isDisabled || isReadonly)) mouseenterEvent(evnt, item);
					},
					onMouseleave: mouseleaveEvent,
					onClick(evnt) {
						if (!(isDisabled || isReadonly)) clickEvent(evnt, item);
					}
				}, [h("i", { class: isChecked ? getIcon().RATE_CHECKED : getIcon().RATE_UNCHECKED })]);
			}));
		};
		$xeRate.renderVN = renderVN;
		return $xeRate;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/rate/index.js
var VxeRate = Object.assign({}, rate_default, { install(app) {
	app.component(rate_default.name, rate_default);
} });
dynamicApp.use(VxeRate);
VxeUI.component(rate_default);
var Rate = VxeRate;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/result/src/result.js
var result_default = defineVxeComponent({
	name: "VxeResult",
	props: {
		imageUrl: String,
		imageStyle: Object,
		icon: String,
		type: [String, Number],
		status: [String, Number],
		title: [String, Number],
		content: [String, Number]
	},
	emits: [],
	setup(props, context) {
		const { emit, slots } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeMaps = {};
		const $xeResult = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $result: $xeResult }, params));
		};
		Object.assign($xeResult, { dispatchEvent }, {});
		const renderVN = () => {
			const { imageUrl, imageStyle, icon, title, type, content } = props;
			const status = props.status || type;
			const slotExtra = slots.extra;
			return h("div", {
				ref: "refElem",
				class: ["vxe-result", { [`theme--${status}`]: status }]
			}, [h("div", { class: "vxe-result--inner" }, [
				imageUrl ? h("div", { class: "vxe-result--img-wrapper" }, [h("img", {
					src: imageUrl,
					style: imageStyle
				})]) : h("div", { class: "vxe-result--icon-wrapper" }, [h("i", { class: [icon, type ? getIcon()[`RESULT_${type}`.toLocaleUpperCase()] : ""] })]),
				h("div", { class: "vxe-result--title-wrapper" }, `${title || ""}`),
				h("div", { class: "vxe-result--content-wrapper" }, `${content || ""}`),
				slotExtra ? h("div", { class: "vxe-result--extra-wrapper" }, slotExtra({})) : renderEmptyElement($xeResult)
			])]);
		};
		$xeResult.renderVN = renderVN;
		return $xeResult;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/result/index.js
var VxeResult = Object.assign({}, result_default, { install(app) {
	app.component(result_default.name, result_default);
} });
dynamicApp.use(VxeResult);
VxeUI.component(result_default);
var Result = VxeResult;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/row/src/row.js
var row_default = defineVxeComponent({
	name: "VxeRow",
	props: {
		gutter: [
			Number,
			String,
			Array
		],
		wrap: {
			type: Boolean,
			default: () => getConfig().row.wrap
		},
		vertical: Boolean,
		size: {
			type: String,
			default: () => getConfig().row.size || getConfig().size
		}
	},
	emits: ["click"],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		useSize(props);
		const refElem = ref();
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeRowStyle = computed(() => {
			const { gutter, vertical } = props;
			const style = {};
			if (gutter) {
				let [lrGutter, tbGutter] = import_xe_utils.default.isArray(gutter) ? gutter : [gutter];
				if (vertical) {
					tbGutter = lrGutter;
					lrGutter = "";
				}
				if (lrGutter) {
					const offsetSize = import_xe_utils.default.isNumber(lrGutter) ? toCssUnit(-(lrGutter / 2)) : `calc(${toCssUnit(lrGutter)} / 2 * -1)`;
					style.marginLeft = offsetSize;
					style.marginRight = offsetSize;
				}
				if (tbGutter) {
					const offsetSize = import_xe_utils.default.isNumber(tbGutter) ? toCssUnit(-(tbGutter / 2)) : `calc(${toCssUnit(tbGutter)} / 2 * -1)`;
					style.marginTop = offsetSize;
					style.marginBottom = offsetSize;
				}
			}
			return style;
		});
		const computeMaps = {};
		const $xeRow = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const clickEvent = (evnt) => {
			dispatchEvent("click", {}, evnt);
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $row: $xeRow }, params));
		};
		Object.assign($xeRow, { dispatchEvent }, {});
		const renderVN = () => {
			const { vertical, wrap } = props;
			const rowStyle = computeRowStyle.value;
			const defaultSlot = slots.default;
			return h("div", {
				ref: refElem,
				class: ["vxe-row", {
					"is--vertical": vertical,
					"is--wrap": wrap
				}],
				style: rowStyle,
				onClick: clickEvent
			}, defaultSlot ? defaultSlot({}) : []);
		};
		provide("$xeRow", $xeRow);
		$xeRow.renderVN = renderVN;
		return $xeRow;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/row/index.js
var VxeRow = Object.assign({}, row_default, { install(app) {
	app.component(row_default.name, row_default);
} });
dynamicApp.use(VxeRow);
VxeUI.component(row_default);
var Row = VxeRow;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/segmented/src/segmented.js
function createReactData$2() {
	return {
		groupName: import_xe_utils.default.uniqueId("xe_group_"),
		selectStyle: {
			display: "",
			left: "",
			width: ""
		}
	};
}
var segmented_default = defineVxeComponent({
	name: "VxeSegmented",
	props: {
		modelValue: [String, Number],
		name: [String, Number],
		disabled: Boolean,
		options: Array,
		type: {
			type: String,
			default: () => getConfig().segmented.type
		},
		status: String,
		width: [String, Number],
		height: [String, Number],
		optionConfig: Object,
		size: {
			type: String,
			default: () => getConfig().segmented.size || getConfig().size
		}
	},
	emits: ["update:modelValue", "change"],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const refElem = ref();
		const refWrapperElem = ref();
		const refSelectedElem = ref();
		const reactData = reactive(createReactData$2());
		const refMaps = { refElem };
		const computeItemType = computed(() => {
			const { type } = props;
			if (type === "round") return type;
			if (type === "inside") return type;
			return "default";
		});
		const computeWrapperStyle = computed(() => {
			const { width, height } = props;
			const stys = {};
			if (width) stys.width = toCssUnit(width);
			if (height) stys.height = toCssUnit(height);
			return stys;
		});
		const computeOptionOpts = computed(() => {
			return Object.assign({}, getConfig().segmented.optionConfig, props.optionConfig);
		});
		const computeItemList = computed(() => {
			return props.options || [];
		});
		const computeSelectIndex = computed(() => {
			const { modelValue } = props;
			const itemList = computeItemList.value;
			return import_xe_utils.default.findIndexOf(itemList, (item) => item.value === modelValue);
		});
		const comIsFullWidth = computed(() => {
			return computeWrapperStyle.value.width === "100%";
		});
		const computeMaps = {};
		const $xeSegmented = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $segmented: $xeSegmented }, params));
		};
		const emitModel = (value) => {
			emit("update:modelValue", value);
		};
		const changeEvent = (evnt, item) => {
			const value = item.value;
			emitModel(value);
			dispatchEvent("change", { value }, evnt);
			updateStyle();
		};
		const updateStyle = () => {
			nextTick(() => {
				const selectIndex = computeSelectIndex.value;
				const wrapperElem = refWrapperElem.value;
				const atStyle = {
					display: "",
					left: "",
					width: ""
				};
				if (wrapperElem) {
					const itemEl = wrapperElem.children[selectIndex];
					if (itemEl) {
						atStyle.display = "block";
						atStyle.left = toCssUnit(itemEl.offsetLeft);
						atStyle.width = toCssUnit(itemEl.clientWidth);
					}
				}
				reactData.selectStyle = atStyle;
			});
		};
		Object.assign($xeSegmented, { dispatchEvent }, {});
		const renderItems = () => {
			const { modelValue, name } = props;
			const { groupName } = reactData;
			const optionOpts = computeOptionOpts.value;
			const itemList = computeItemList.value;
			const isFullWidth = comIsFullWidth.value;
			const fullItemWidth = isFullWidth ? Math.max(0, 100 / itemList.length) : 0;
			const defaultSlot = slots.default;
			const itemVNs = [];
			itemList.forEach((item, i) => {
				const { icon, width } = item;
				const itemWidth = width || optionOpts.width;
				itemVNs.push(h("label", {
					key: i,
					class: ["vxe-segmented--item", { "is--checked": modelValue === item.value }],
					style: isFullWidth || itemWidth ? { width: itemWidth ? toCssUnit(itemWidth) : fullItemWidth ? `${fullItemWidth}%` : "" } : void 0
				}, [h("input", {
					class: "vxe-segmented--input",
					type: "radio",
					name: name || groupName,
					onChange(evnt) {
						changeEvent(evnt, item);
					}
				}), h("div", { class: "vxe-segmented--content" }, [icon ? h("div", { class: "vxe-segmented--icon" }, [h("i", { class: icon })]) : renderEmptyElement($xeSegmented), h("div", { class: "vxe-segmented--label" }, defaultSlot ? defaultSlot({ option: item }) : import_xe_utils.default.eqNull(item.label) ? "" : `${item.label}`)])]));
			});
			return itemVNs;
		};
		const renderVN = () => {
			const { status } = props;
			const { selectStyle } = reactData;
			const itemType = computeItemType.value;
			const wrapperStyle = computeWrapperStyle.value;
			const vSize = computeSize.value;
			return h("div", {
				ref: refElem,
				class: [
					"vxe-segmented",
					`type--${itemType}`,
					{
						[`size--${vSize}`]: vSize,
						[`theme--${status}`]: status
					}
				]
			}, [h("div", {
				class: "vxe-segmented--group",
				style: wrapperStyle
			}, [h("div", {
				ref: refSelectedElem,
				class: "vxe-segmented--selected",
				style: selectStyle
			}), h("div", {
				ref: refWrapperElem,
				class: "vxe-segmented--inner"
			}, renderItems())])]);
		};
		watch(computeSelectIndex, () => {
			updateStyle();
		});
		onMounted(() => {
			updateStyle();
		});
		onBeforeUnmount(() => {
			import_xe_utils.default.assign(reactData, createReactData$2());
		});
		$xeSegmented.renderVN = renderVN;
		return $xeSegmented;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/segmented/index.js
var VxeSegmented = Object.assign({}, segmented_default, { install(app) {
	app.component(segmented_default.name, segmented_default);
} });
dynamicApp.use(VxeSegmented);
VxeUI.component(segmented_default);
var Segmented = VxeSegmented;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/splitter/src/splitter.js
var splitter_default = defineVxeComponent({
	name: "VxeSplitter",
	props: {
		width: [Number, String],
		height: [Number, String],
		vertical: {
			type: Boolean,
			default: () => getConfig().splitter.vertical
		},
		border: {
			type: Boolean,
			default: () => getConfig().splitter.border
		},
		padding: {
			type: Boolean,
			default: () => getConfig().splitter.padding
		},
		resize: {
			type: Boolean,
			default: () => getConfig().splitter.resize
		},
		items: Array,
		itemConfig: Object,
		barConfig: Object,
		resizeConfig: Object,
		actionConfig: Object,
		size: {
			type: String,
			default: () => getConfig().splitter.size || getConfig().size
		}
	},
	emits: [
		"action-dblclick",
		"action-click",
		"toggle-expand",
		"resize-start",
		"resize-drag",
		"resize-end"
	],
	setup(props, context) {
		const { emit, slots } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const refBarInfoElem = ref();
		const refResizableSplitterTip = ref();
		const { computeSize } = useSize(props);
		const reactData = reactive({
			staticItems: [],
			itemList: [],
			barWidth: 0,
			barHeight: 0
		});
		const internalData = {
			wrapperWidth: 0,
			wrapperHeight: 0
		};
		const computeItemOpts = computed(() => {
			return Object.assign({}, getConfig().splitter.itemConfig, props.itemConfig);
		});
		const computeBarOpts = computed(() => {
			return Object.assign({}, getConfig().splitter.barConfig, props.barConfig);
		});
		const computeResizeOpts = computed(() => {
			return Object.assign({}, getConfig().splitter.resizeConfig, props.resizeConfig);
		});
		const computeActionOpts = computed(() => {
			return Object.assign({}, getConfig().splitter.actionConfig, props.actionConfig);
		});
		const computeVisibleItems = computed(() => {
			return reactData.itemList.filter((item) => item.isExpand);
		});
		const computeAutoItems = computed(() => {
			const { vertical } = props;
			const autoItems = [];
			let heightCount = 0;
			let widthCount = 0;
			reactData.itemList.forEach(vertical ? (item) => {
				const { renderHeight, resizeHeight, foldHeight, isExpand, height } = item;
				const itemHeight = isExpand ? foldHeight || resizeHeight || renderHeight : 0;
				if (!height) autoItems.push(item);
				heightCount += itemHeight;
			} : (item) => {
				const { renderWidth, resizeWidth, foldWidth, isExpand, width } = item;
				const itemWidth = isExpand ? foldWidth || resizeWidth || renderWidth : 0;
				if (!width) autoItems.push(item);
				widthCount += itemWidth;
			});
			return {
				autoItems,
				heightCount,
				heightRatio: heightCount / 100,
				widthCount,
				widthRatio: widthCount / 100
			};
		});
		const computeBarStyle = computed(() => {
			const { width, height } = computeBarOpts.value;
			const stys = {};
			if (height) stys.height = toCssUnit(height);
			if (width) stys.width = toCssUnit(width);
			return stys;
		});
		const computeMaps = {
			computeItemOpts,
			computeBarOpts,
			computeActionOpts
		};
		const refMaps = { refElem };
		const $xeSplitter = {
			xID,
			props,
			context,
			reactData,
			internalData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $splitter: $xeSplitter }, params));
		};
		const callSlot = (slotFunc, params) => {
			if (slotFunc) {
				if (import_xe_utils.default.isString(slotFunc)) slotFunc = slots[slotFunc] || null;
				if (import_xe_utils.default.isFunction(slotFunc)) return getSlotVNs$1(slotFunc(params));
			}
			return [];
		};
		const getActionIcon = (prevItem, nextItem, isNext) => {
			const { vertical } = props;
			const topIcon = "SPLIT_TOP_ACTION";
			const bottomIcon = "SPLIT_BOTTOM_ACTION";
			const leftIcon = "SPLIT_LEFT_ACTION";
			const rightIcon = "SPLIT_RIGHT_ACTION";
			let iconName = "";
			if (vertical) if (isNext) iconName = nextItem.isExpand ? bottomIcon : topIcon;
			else iconName = prevItem.isExpand ? topIcon : bottomIcon;
			else if (isNext) iconName = nextItem.isExpand ? rightIcon : leftIcon;
			else iconName = prevItem.isExpand ? leftIcon : rightIcon;
			if (iconName) return getIcon()[iconName];
			return "";
		};
		const reset = () => {
			const { itemList } = reactData;
			itemList.forEach((item) => {
				item.isExpand = true;
				item.foldHeight = 0;
				item.foldWidth = 0;
				item.resizeHeight = 0;
				item.resizeWidth = 0;
			});
			return nextTick();
		};
		const handleLoadItem = (list, isReset) => {
			const { staticItems } = reactData;
			const { showPrevButton, showNextButton } = computeActionOpts.value;
			const itemDef = {
				isExpand: true,
				renderWidth: 0,
				resizeWidth: 0,
				foldWidth: 0,
				renderHeight: 0,
				resizeHeight: 0,
				foldHeight: 0
			};
			reactData.itemList = list.map((item) => {
				if (item.showAction) warnLog("vxe.error.removeProp", ["[splitter] show-action"]);
				if (item.slots) import_xe_utils.default.each(item.slots, (func) => {
					if (!import_xe_utils.default.isFunction(func)) {
						if (!slots[func]) errLog("vxe.error.notSlot", [`[splitter] ${func}`]);
					}
				});
				return Object.assign({}, isReset ? null : itemDef, item, isReset ? itemDef : null, { id: import_xe_utils.default.uniqueId() });
			});
			if (staticItems.length) errLog("vxe.error.errConflicts", ["<vxe-splitter-panel ...>", "items"]);
			if ((showPrevButton || showNextButton) && reactData.itemList.length > 2) errLog("vxe.error.errConflicts", ["action-config.showPrevButton | action-config.showNextButton", "Only supports 2 item"]);
			return recalculate();
		};
		const loadItem = (list) => {
			return handleLoadItem(list || [], false);
		};
		const reloadItem = (list) => {
			return handleLoadItem(list || [], true);
		};
		const handleItemByName = (name) => {
			const { itemList } = reactData;
			let index = -1;
			let currItem = null;
			let prevItem = null;
			let nextItem = null;
			for (let i = 0; i < itemList.length; i++) {
				const item = itemList[i];
				if (item.name === name) {
					index = i;
					currItem = item;
					prevItem = itemList[i - 1] || null;
					nextItem = itemList[i + 1] || null;
					break;
				}
			}
			return {
				index,
				currItem,
				prevItem,
				nextItem
			};
		};
		const setItemExpand = (name, expanded) => {
			const restItem = handleItemByName(name);
			if (restItem) {
				const { currItem, prevItem, nextItem } = restItem;
				if (currItem) {
					if (expanded ? !currItem.isExpand : currItem.isExpand) {
						if (nextItem) {
							if (nextItem.isExpand) handleItemActionEvent(null, currItem, nextItem, false);
						} else if (prevItem) {
							if (prevItem.isExpand) handleItemActionEvent(null, prevItem, currItem, true);
						}
					}
				}
			}
			return nextTick();
		};
		const toggleItemExpand = (name) => {
			const restItem = handleItemByName(name);
			if (restItem) {
				const { currItem } = restItem;
				if (currItem) return setItemExpand(name, !currItem.isExpand);
			}
			return nextTick();
		};
		const getItemExpand = (name) => {
			const restItem = handleItemByName(name);
			if (restItem) {
				const { currItem } = restItem;
				if (currItem) return currItem.isExpand;
			}
			return false;
		};
		const recalculate = () => {
			return nextTick().then(() => {
				const { vertical } = props;
				const { itemList } = reactData;
				const el = refElem.value;
				const barInfoElem = refBarInfoElem.value;
				if (!el) return;
				const wWidth = el.clientWidth;
				const wHeight = el.clientHeight;
				if (!wWidth || !wHeight) return;
				if (barInfoElem) {
					reactData.barWidth = barInfoElem.offsetWidth;
					reactData.barHeight = barInfoElem.offsetHeight;
				}
				const contentWidth = wWidth - (vertical ? 0 : reactData.barWidth * (itemList.length - 1));
				const contentHeight = wHeight - (vertical ? reactData.barHeight * (itemList.length - 1) : 0);
				const itemOpts = computeItemOpts.value;
				const allMinWidth = import_xe_utils.default.toNumber(itemOpts.minWidth);
				const allMinHeight = import_xe_utils.default.toNumber(itemOpts.minHeight);
				const residueItems = [];
				if (vertical) {
					let countHeight = 0;
					itemList.forEach((item) => {
						const { height } = item;
						let itemHeight = 0;
						if (height) {
							if (isScale(height)) itemHeight = contentHeight * import_xe_utils.default.toNumber(height) / 100;
							else itemHeight = import_xe_utils.default.toNumber(height);
							item.renderHeight = itemHeight;
						} else residueItems.push(item);
						countHeight += itemHeight;
					});
					if (residueItems.length) {
						const reMeanHeight = (contentHeight - countHeight) / residueItems.length;
						residueItems.forEach((item) => {
							item.renderHeight = Math.max(import_xe_utils.default.toNumber(getGlobalDefaultConfig(item.minHeight, allMinHeight)), reMeanHeight);
						});
					}
				} else {
					let countWidth = 0;
					itemList.forEach((item) => {
						const { width } = item;
						let itemWidth = 0;
						if (width) {
							if (isScale(width)) itemWidth = contentWidth * import_xe_utils.default.toNumber(width) / 100;
							else itemWidth = import_xe_utils.default.toNumber(width);
							item.renderWidth = itemWidth;
						} else residueItems.push(item);
						countWidth += itemWidth;
					});
					if (residueItems.length) {
						const reMeanWidth = (contentWidth - countWidth) / residueItems.length;
						residueItems.forEach((item) => {
							item.renderWidth = Math.max(import_xe_utils.default.toNumber(getGlobalDefaultConfig(item.minWidth, allMinWidth)), reMeanWidth);
						});
					}
				}
				internalData.wrapperWidth = contentWidth;
				internalData.wrapperHeight = contentHeight;
			});
		};
		const dragEvent = (evnt) => {
			const { resize, vertical } = props;
			const { itemList } = reactData;
			if (!resize) return;
			evnt.preventDefault();
			const barEl = evnt.currentTarget;
			const handleEl = barEl.parentElement;
			const el = refElem.value;
			if (!el) return;
			const prevEl = handleEl.previousElementSibling;
			const nextEl = handleEl.nextElementSibling;
			if (!prevEl || !nextEl) return;
			const prevId = prevEl.getAttribute("itemid");
			const nextId = nextEl.getAttribute("itemid");
			const prevItem = itemList.find((item) => item.id === prevId);
			const nextItem = itemList.find((item) => item.id === nextId);
			if (!prevItem || !nextItem) return;
			const containerRect = el.getBoundingClientRect();
			const barRect = barEl.getBoundingClientRect();
			const rsSplitterLineEl = refResizableSplitterTip.value;
			const rsSplitterTipEl = rsSplitterLineEl ? rsSplitterLineEl.children[0] : null;
			const itemOpts = computeItemOpts.value;
			const { immediate } = computeResizeOpts.value;
			const allMinWidth = import_xe_utils.default.toNumber(itemOpts.minWidth);
			const allMinHeight = import_xe_utils.default.toNumber(itemOpts.minHeight);
			const barOffsetX = Math.ceil(barRect.width - (evnt.clientX - barRect.left));
			const barOffsetY = Math.ceil(evnt.clientY - barRect.top);
			const prevWidth = prevEl.offsetWidth;
			const nextWidth = nextEl.offsetWidth;
			const prevMinWidth = import_xe_utils.default.toNumber(prevItem ? getGlobalDefaultConfig(prevItem.minWidth, allMinWidth) : allMinWidth);
			const nextMinWidth = import_xe_utils.default.toNumber(nextItem ? getGlobalDefaultConfig(nextItem.minWidth, allMinWidth) : allMinWidth);
			const minOffsetLeft = prevEl.offsetLeft + prevMinWidth - barOffsetX;
			const maxOffsetLeft = nextEl.offsetLeft + nextEl.offsetWidth - nextMinWidth - barOffsetX;
			const startOffsetLeft = evnt.clientX - containerRect.left;
			let targetOffsetWidth = -1;
			let prevResizeWidth = 0;
			let nextResizeWidth = 0;
			let offsetLeft = startOffsetLeft;
			const prevHeight = prevEl.offsetHeight;
			const nextHeight = nextEl.offsetHeight;
			const prevMinHeight = import_xe_utils.default.toNumber(prevItem ? getGlobalDefaultConfig(prevItem.minHeight, allMinHeight) : allMinHeight);
			const nextMinHeight = import_xe_utils.default.toNumber(nextItem ? getGlobalDefaultConfig(nextItem.minHeight, allMinHeight) : allMinHeight);
			const minOffsetTop = prevEl.offsetTop + prevMinHeight + barOffsetY;
			const maxOffsetTop = nextEl.offsetTop + nextEl.offsetHeight - nextMinHeight + barOffsetY;
			const startOffsetTop = evnt.clientY - containerRect.top;
			let targetOffsetHeight = -1;
			let prevResizeHeight = 0;
			let nextResizeHeight = 0;
			let offsetTop = startOffsetTop;
			const handleReStyle = (evnt) => {
				if (!rsSplitterLineEl) return;
				const rsNumPrevEl = rsSplitterTipEl ? rsSplitterTipEl.children[0] : null;
				const rsNumNextEl = rsSplitterTipEl ? rsSplitterTipEl.children[1] : null;
				if (vertical) {
					let tipWidth = 0;
					if (rsNumPrevEl) if (targetOffsetHeight < 0) rsNumPrevEl.style.display = "none";
					else {
						rsNumPrevEl.textContent = `${Math.floor(prevResizeHeight)}px`;
						rsNumPrevEl.style.display = "block";
						tipWidth = rsNumPrevEl.offsetWidth;
					}
					if (rsNumNextEl) if (targetOffsetHeight < 0) {
						rsNumNextEl.textContent = `${Math.floor(nextResizeHeight)}px`;
						rsNumNextEl.style.display = "block";
						tipWidth = rsNumNextEl.offsetWidth;
					} else rsNumNextEl.style.display = "none";
					let rsLeft = Math.max(1, evnt.clientX - containerRect.left - tipWidth / 2);
					if (rsLeft > containerRect.width - tipWidth - 1) rsLeft = containerRect.width - tipWidth - 1;
					rsSplitterLineEl.style.left = "0";
					rsSplitterLineEl.style.top = `${offsetTop}px`;
					if (rsSplitterTipEl) rsSplitterTipEl.style.left = `${rsLeft}px`;
				} else {
					let tipHeight = 0;
					if (rsNumPrevEl) if (targetOffsetWidth < 0) rsNumPrevEl.style.display = "none";
					else {
						rsNumPrevEl.textContent = `${Math.floor(prevResizeWidth)}px`;
						rsNumPrevEl.style.display = "block";
						tipHeight = rsNumPrevEl.offsetHeight;
					}
					if (rsNumNextEl) if (targetOffsetWidth < 0) {
						rsNumNextEl.textContent = `${Math.floor(nextResizeWidth)}px`;
						rsNumNextEl.style.display = "block";
						tipHeight = rsNumNextEl.offsetHeight;
					} else rsNumNextEl.style.display = "none";
					let rsTop = Math.max(1, evnt.clientY - containerRect.top - tipHeight / 2);
					if (rsTop > containerRect.height - tipHeight - 1) rsTop = containerRect.height - tipHeight - 1;
					rsSplitterLineEl.style.top = "0";
					rsSplitterLineEl.style.left = `${offsetLeft}px`;
					if (rsSplitterTipEl) rsSplitterTipEl.style.top = `${rsTop}px`;
				}
			};
			const handleUpdate = () => {
				if (vertical) {
					prevItem.resizeHeight = prevResizeHeight;
					nextItem.resizeHeight = nextResizeHeight;
				} else {
					prevItem.resizeWidth = prevResizeWidth;
					nextItem.resizeWidth = nextResizeWidth;
				}
			};
			const handleDrag = (evnt) => {
				if (vertical) {
					offsetTop = evnt.clientY - containerRect.top;
					if (offsetTop < minOffsetTop) offsetTop = minOffsetTop;
					if (offsetTop > maxOffsetTop) offsetTop = maxOffsetTop;
					targetOffsetHeight = offsetTop - startOffsetTop;
					prevResizeHeight = prevHeight + targetOffsetHeight;
					nextResizeHeight = nextHeight - targetOffsetHeight;
				} else {
					offsetLeft = evnt.clientX - containerRect.left;
					if (offsetLeft < minOffsetLeft) offsetLeft = minOffsetLeft;
					if (offsetLeft > maxOffsetLeft) offsetLeft = maxOffsetLeft;
					targetOffsetWidth = offsetLeft - startOffsetLeft;
					prevResizeWidth = prevWidth + targetOffsetWidth;
					nextResizeWidth = nextWidth - targetOffsetWidth;
				}
				if (immediate) if (vertical) {
					prevEl.style.height = toCssUnit(prevResizeHeight);
					nextEl.style.height = toCssUnit(nextResizeHeight);
				} else {
					prevEl.style.width = toCssUnit(prevResizeWidth);
					nextEl.style.width = toCssUnit(nextResizeWidth);
				}
				if (rsSplitterLineEl) handleReStyle(evnt);
				dispatchEvent("resize-drag", {
					prevItem,
					nextItem,
					offsetHeight: targetOffsetHeight,
					offsetWidth: targetOffsetWidth
				}, evnt);
			};
			document.onmousemove = (evnt) => {
				evnt.preventDefault();
				handleDrag(evnt);
			};
			document.onmouseup = (evnt) => {
				document.onmousemove = null;
				document.onmouseup = null;
				if (rsSplitterLineEl) rsSplitterLineEl.style.display = "";
				handleUpdate();
				removeClass(el, "is--drag");
				dispatchEvent("resize-end", {
					prevItem,
					nextItem,
					offsetHeight: targetOffsetHeight,
					offsetWidth: targetOffsetWidth
				}, evnt);
				recalculate();
			};
			if (rsSplitterLineEl) {
				rsSplitterLineEl.style.display = "block";
				handleReStyle(evnt);
			}
			handleDrag(evnt);
			addClass(el, "is--drag");
			dispatchEvent("resize-start", {
				prevItem,
				nextItem
			}, evnt);
		};
		const handleItemActionEvent = (evnt, prevItem, nextItem, isNext) => {
			const { vertical } = props;
			let expanded = false;
			let item = prevItem;
			if (isNext) {
				item = nextItem;
				expanded = !nextItem.isExpand;
				nextItem.isExpand = expanded;
			} else {
				expanded = !prevItem.isExpand;
				prevItem.isExpand = expanded;
			}
			if (vertical) if (prevItem.isExpand && nextItem.isExpand) {
				prevItem.foldHeight = 0;
				nextItem.foldHeight = 0;
			} else if (prevItem.isExpand) {
				nextItem.foldHeight = 0;
				prevItem.foldHeight = (prevItem.resizeHeight || prevItem.renderHeight) + (nextItem.resizeHeight || nextItem.renderHeight);
			} else {
				prevItem.foldHeight = 0;
				nextItem.foldHeight = (prevItem.resizeHeight || prevItem.renderHeight) + (nextItem.resizeHeight || nextItem.renderHeight);
			}
			else if (prevItem.isExpand && nextItem.isExpand) {
				prevItem.foldWidth = 0;
				nextItem.foldWidth = 0;
			} else if (prevItem.isExpand) {
				nextItem.foldWidth = 0;
				prevItem.foldWidth = (prevItem.resizeWidth || prevItem.renderWidth) + (nextItem.resizeWidth || nextItem.renderWidth);
			} else {
				prevItem.foldWidth = 0;
				nextItem.foldWidth = (prevItem.resizeWidth || prevItem.renderWidth) + (nextItem.resizeWidth || nextItem.renderWidth);
			}
			if (evnt) dispatchEvent("toggle-expand", {
				prevItem,
				nextItem,
				expanded,
				item
			}, evnt);
			recalculate();
		};
		const handlePrevActionDblclickEvent = (evnt) => {
			const { itemList } = reactData;
			const actionOpts = computeActionOpts.value;
			const handleEl = evnt.currentTarget.parentElement.parentElement;
			const prevId = handleEl.previousElementSibling.getAttribute("itemid");
			const prevItem = itemList.find((item) => item.id === prevId);
			const nextId = handleEl.nextElementSibling.getAttribute("itemid");
			const nextItem = itemList.find((item) => item.id === nextId);
			if (actionOpts.trigger === "dblclick") {
				if (prevItem && nextItem && nextItem.isExpand) handleItemActionEvent(evnt, prevItem, nextItem, false);
			}
			dispatchEvent("action-dblclick", {
				prevItem,
				nextItem
			}, evnt);
		};
		const handlePrevActionClickEvent = (evnt) => {
			const { itemList } = reactData;
			const actionOpts = computeActionOpts.value;
			const handleEl = evnt.currentTarget.parentElement.parentElement;
			const prevId = handleEl.previousElementSibling.getAttribute("itemid");
			const prevItem = itemList.find((item) => item.id === prevId);
			const nextId = handleEl.nextElementSibling.getAttribute("itemid");
			const nextItem = itemList.find((item) => item.id === nextId);
			if (actionOpts.trigger !== "dblclick") {
				if (prevItem && nextItem && nextItem.isExpand) handleItemActionEvent(evnt, prevItem, nextItem, false);
			}
			dispatchEvent("action-click", {
				prevItem,
				nextItem
			}, evnt);
		};
		const handleNextActionDblclickEvent = (evnt) => {
			const { itemList } = reactData;
			const actionOpts = computeActionOpts.value;
			const handleEl = evnt.currentTarget.parentElement.parentElement;
			const prevId = handleEl.previousElementSibling.getAttribute("itemid");
			const prevItem = itemList.find((item) => item.id === prevId);
			const nextId = handleEl.nextElementSibling.getAttribute("itemid");
			const nextItem = itemList.find((item) => item.id === nextId);
			if (actionOpts.trigger === "dblclick") {
				if (prevItem && nextItem && prevItem.isExpand) handleItemActionEvent(evnt, prevItem, nextItem, true);
			}
			dispatchEvent("action-dblclick", {
				prevItem,
				nextItem
			}, evnt);
		};
		const handleNextActionClickEvent = (evnt) => {
			const { itemList } = reactData;
			const actionOpts = computeActionOpts.value;
			const handleEl = evnt.currentTarget.parentElement.parentElement;
			const prevId = handleEl.previousElementSibling.getAttribute("itemid");
			const prevItem = itemList.find((item) => item.id === prevId);
			const nextId = handleEl.nextElementSibling.getAttribute("itemid");
			const nextItem = itemList.find((item) => item.id === nextId);
			if (actionOpts.trigger !== "dblclick") {
				if (prevItem && nextItem && prevItem.isExpand) handleItemActionEvent(evnt, prevItem, nextItem, true);
			}
			dispatchEvent("action-click", {
				prevItem,
				nextItem
			}, evnt);
		};
		const handleGlobalResizeEvent = () => {
			recalculate();
		};
		Object.assign($xeSplitter, {
			dispatchEvent,
			setItemExpand,
			toggleItemExpand,
			getItemExpand,
			recalculate,
			reset,
			loadItem,
			reloadItem
		}, {});
		const renderHandleBar = (prevItem, nextItem) => {
			const { border, resize, vertical } = props;
			const { itemList } = reactData;
			const barStyle = computeBarStyle.value;
			const actionOpts = computeActionOpts.value;
			const { direction } = actionOpts;
			const showPrevButton = import_xe_utils.default.isBoolean(actionOpts.showPrevButton) ? actionOpts.showPrevButton : itemList.some((item) => item.showAction);
			const showNextButton = import_xe_utils.default.isBoolean(actionOpts.showNextButton) ? actionOpts.showNextButton : direction === "next" && itemList.some((item) => item.showAction);
			const { immediate } = computeResizeOpts.value;
			return h("div", { class: [
				"vxe-splitter-panel-handle",
				vertical ? "is--vertical" : "is--horizontal",
				immediate ? "is-resize--immediate" : "is-resize--lazy",
				{
					"is--resize": resize,
					"is--border": border
				}
			] }, [h("div", {
				class: "vxe-splitter-panel-handle-bar",
				style: barStyle,
				onMousedown: dragEvent
			}), itemList.length === 2 ? h("div", { class: "vxe-splitter-panel-action-btn-wrapper" }, [showPrevButton && nextItem.isExpand ? h("div", {
				class: "vxe-splitter-panel-action-btn",
				onDblclick: handlePrevActionDblclickEvent,
				onClick: handlePrevActionClickEvent
			}, [h("i", { class: getActionIcon(prevItem, nextItem, false) })]) : renderEmptyElement($xeSplitter), showNextButton && prevItem.isExpand ? h("div", {
				class: "vxe-splitter-panel-action-btn",
				onDblclick: handleNextActionDblclickEvent,
				onClick: handleNextActionClickEvent
			}, [h("i", { class: getActionIcon(prevItem, nextItem, true) })]) : renderEmptyElement($xeSplitter)]) : renderEmptyElement($xeSplitter)]);
		};
		const renderItems = () => {
			const { border, padding, resize, vertical } = props;
			const { itemList } = reactData;
			const vSize = computeSize.value;
			const { immediate } = computeResizeOpts.value;
			const visibleItems = computeVisibleItems.value;
			const { autoItems } = computeAutoItems.value;
			const itemVNs = [];
			itemList.forEach((prevItem, index) => {
				const { id, name, slots, renderHeight, resizeHeight, foldHeight, renderWidth, resizeWidth, foldWidth, isExpand } = prevItem;
				const nextItem = itemList[index + 1];
				const defaultSlot = slots ? slots.default : null;
				const stys = {};
				let itemWidth = isExpand ? foldWidth || resizeWidth || renderWidth : 0;
				let itemHeight = isExpand ? foldHeight || resizeHeight || renderHeight : 0;
				if (autoItems.length === 1) {
					if (vertical) {
						if (!prevItem.height) itemHeight = 0;
					} else if (!prevItem.width) itemWidth = 0;
				}
				let isFill = true;
				if (vertical) {
					if (itemHeight && visibleItems.length > 1) {
						isFill = false;
						stys.height = toCssUnit(itemHeight);
					}
				} else if (itemWidth && visibleItems.length > 1) {
					isFill = false;
					stys.width = toCssUnit(itemWidth);
				}
				itemVNs.push(h("div", {
					itemid: id,
					class: [
						"vxe-splitter-panel",
						vertical ? "is--vertical" : "is--horizontal",
						immediate ? "is-resize--immediate" : "is-resize--lazy",
						{
							[`size--${vSize}`]: vSize,
							"is--resize": resize,
							"is--padding": padding,
							"is--border": border,
							"is--height": itemHeight,
							"is--width": itemWidth,
							"is--visible": isExpand,
							"is--hidden": !isExpand,
							"is--fill": isExpand && isFill
						}
					],
					style: stys
				}, [h("div", {
					itemid: id,
					class: "vxe-splitter-panel--wrapper"
				}, [h("div", { class: "vxe-splitter-panel--inner" }, defaultSlot ? callSlot(defaultSlot, {
					name,
					isExpand
				}) : [])])]));
				if (nextItem) itemVNs.push(renderHandleBar(prevItem, nextItem));
			});
			return h("div", { class: "vxe-splitter-wrapper" }, itemVNs);
		};
		const renderVN = () => {
			const { vertical, width, height } = props;
			const vSize = computeSize.value;
			const { immediate, showTip } = computeResizeOpts.value;
			const defaultSlot = slots.default;
			const stys = {};
			if (height) stys.height = toCssUnit(height);
			if (width) stys.width = toCssUnit(width);
			return h("div", {
				ref: refElem,
				class: [
					"vxe-splitter",
					vertical ? "is--vertical" : "is--horizontal",
					immediate ? "is-resize--immediate" : "is-resize--lazy",
					{ [`size--${vSize}`]: vSize }
				],
				style: stys
			}, [
				h("div", { class: "vxe-splitter-slots" }, defaultSlot ? defaultSlot({}) : []),
				renderItems(),
				h("div", {
					ref: refResizableSplitterTip,
					class: [
						"vxe-splitter--resizable-splitter-tip",
						vertical ? "is--vertical" : "is--horizontal",
						immediate ? "is-resize--immediate" : "is-resize--lazy"
					]
				}, showTip ? [h("div", { class: "vxe-splitter--resizable-splitter-tip-number" }, [h("div", { class: "vxe-splitter--resizable-splitter-number-prev" }), h("div", { class: "vxe-splitter--resizable-splitter-number-next" })])] : []),
				h("div", { class: "vxe-splitter--render-vars" }, [h("div", {
					ref: refBarInfoElem,
					class: "vxe-splitter--handle-bar-info"
				})])
			]);
		};
		const itemFlag = ref(0);
		watch(() => props.items ? props.items.length : -1, () => {
			itemFlag.value++;
		});
		watch(() => props.items, () => {
			itemFlag.value++;
		});
		watch(itemFlag, () => {
			loadItem(props.items || []);
		});
		watch(() => reactData.staticItems, (val) => {
			const { showPrevButton, showNextButton } = computeActionOpts.value;
			if (props.items && props.items.length) errLog("vxe.error.errConflicts", ["<vxe-splitter-panel ...>", "items"]);
			reactData.itemList = val || [];
			if ((showPrevButton || showNextButton) && reactData.itemList.length > 2) errLog("vxe.error.modelConflicts", ["[splitter] action-config.showPrevButton | action-config.showNextButton", "<vxe-splitter-panel ...> Only supports 2 panel"]);
			reactData.itemList.forEach((item) => {
				if (item.showAction) warnLog("vxe.error.removeProp", ["[splitter] showAction"]);
			});
			recalculate();
		});
		let resizeObserver;
		onMounted(() => {
			nextTick(() => {
				recalculate();
			});
			const el = refElem.value;
			if (el) {
				resizeObserver = globalResize.create(() => {
					recalculate();
				});
				resizeObserver.observe(el);
			}
			if (computeActionOpts.value.direction) errLog("vxe.error.delProp", ["[splitter] action-config.direction", "action-config.showPrevButton | action-config.showNextButton"]);
			globalEvents.on($xeSplitter, "resize", handleGlobalResizeEvent);
		});
		onUnmounted(() => {
			if (resizeObserver) resizeObserver.disconnect();
			globalEvents.off($xeSplitter, "resize");
		});
		onActivated(() => {
			recalculate();
		});
		if (props.items) loadItem(props.items);
		provide("$xeSplitter", $xeSplitter);
		$xeSplitter.renderVN = renderVN;
		return $xeSplitter;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/splitter/index.js
var VxeSplitter = Object.assign({}, splitter_default, { install(app) {
	app.component(splitter_default.name, splitter_default);
} });
dynamicApp.use(VxeSplitter);
VxeUI.component(splitter_default);
var Splitter = VxeSplitter;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/splitter/src/util.js
function assembleSplitterItem($xeSplitter, elem, paneConfig) {
	const staticItems = $xeSplitter.reactData.staticItems;
	const parentElem = elem.parentNode;
	if (parentElem) {
		staticItems.splice(import_xe_utils.default.arrayIndexOf(parentElem.children, elem), 0, paneConfig);
		$xeSplitter.reactData.staticItems = staticItems.slice(0);
	}
}
function destroySplitterItem($xeSplitter, paneConfig) {
	const staticItems = $xeSplitter.reactData.staticItems;
	const index = import_xe_utils.default.findIndexOf(staticItems, (item) => item.id === paneConfig.id);
	if (index > -1) staticItems.splice(index, 1);
	$xeSplitter.reactData.staticItems = staticItems.slice(0);
}
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/splitter/src/splitter-panel.js
var splitter_panel_default = defineVxeComponent({
	name: "VxeSplitterPanel",
	props: {
		name: [Number, String],
		width: [Number, String],
		height: [Number, String],
		minWidth: {
			type: [Number, String],
			default: () => null
		},
		minHeight: {
			type: [Number, String],
			default: () => null
		},
		showAction: Boolean
	},
	emits: [],
	setup(props, context) {
		const { emit, slots } = context;
		const xID = import_xe_utils.default.uniqueId();
		const $xeSplitter = inject("$xeSplitter", null);
		const refElem = ref();
		const paneConfig = reactive({
			id: xID,
			name: props.name,
			width: props.width,
			height: props.height,
			minWidth: props.minWidth,
			minHeight: props.minHeight,
			showAction: props.showAction,
			isExpand: true,
			renderWidth: 0,
			resizeWidth: 0,
			foldWidth: 0,
			renderHeight: 0,
			resizeHeight: 0,
			foldHeight: 0,
			slots
		});
		const reactData = reactive({});
		const internalData = {};
		const computeMaps = {};
		const refMaps = { refElem };
		const $xeSplitterItem = {
			xID,
			props,
			context,
			reactData,
			internalData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $splitterPanel: $xeSplitterItem }, params));
		};
		Object.assign($xeSplitterItem, { dispatchEvent }, {});
		const renderVN = () => {
			return h("div", { ref: refElem });
		};
		watch(() => props.name, (val) => {
			paneConfig.name = val;
		});
		watch(() => props.width, (val) => {
			paneConfig.width = val;
		});
		watch(() => props.height, (val) => {
			paneConfig.height = val;
		});
		watch(() => props.minWidth, (val) => {
			paneConfig.minWidth = val;
		});
		watch(() => props.minHeight, (val) => {
			paneConfig.minHeight = val;
		});
		watch(() => props.showAction, (val) => {
			paneConfig.showAction = val;
		});
		onMounted(() => {
			const elem = refElem.value;
			if ($xeSplitter && elem) assembleSplitterItem($xeSplitter, elem, paneConfig);
		});
		onUnmounted(() => {
			if ($xeSplitter) destroySplitterItem($xeSplitter, paneConfig);
		});
		provide("$xeSplitterItem", $xeSplitterItem);
		$xeSplitterItem.renderVN = renderVN;
		return $xeSplitterItem;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/splitter-panel/index.js
var VxeSplitterPanel = Object.assign({}, splitter_panel_default, { install(app) {
	app.component(splitter_panel_default.name, splitter_panel_default);
} });
dynamicApp.use(VxeSplitterPanel);
VxeUI.component(splitter_panel_default);
var SplitterPanel = VxeSplitterPanel;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/split/index.js
var VxeSplit = Object.assign({}, splitter_default, { install(app) {
	app.component("VxeSplit", splitter_default);
} });
dynamicApp.use(VxeSplit);
VxeUI.component(splitter_default);
var Split = VxeSplit;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/split-pane/index.js
var VxeSplitPane = Object.assign({}, splitter_panel_default, { install(app) {
	app.component("VxeSplitPane", splitter_panel_default);
	app.component("VxeSplitItem", splitter_panel_default);
} });
dynamicApp.use(VxeSplitPane);
VxeUI.component(splitter_panel_default);
var SplitPane = VxeSplitPane;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/slider/src/slider.js
var slider_default = defineVxeComponent({
	name: "VxeSlider",
	props: {
		modelValue: [
			String,
			Number,
			Array
		],
		vertical: Boolean,
		max: {
			type: [String, Number],
			default: () => getConfig().slider.max
		},
		min: {
			type: [String, Number],
			default: () => getConfig().slider.min
		},
		step: {
			type: [String, Number],
			default: () => getConfig().slider.step
		},
		size: {
			type: String,
			default: () => getConfig().slider.size || getConfig().size
		},
		range: {
			type: Boolean,
			default: () => getConfig().slider.range
		},
		readonly: {
			type: Boolean,
			default: null
		},
		disabled: {
			type: Boolean,
			default: null
		}
	},
	emits: [
		"update:modelValue",
		"change",
		"track-dragstart",
		"track-dragover",
		"track-dragend"
	],
	setup(props, context) {
		const { emit } = context;
		const $xeForm = inject("$xeForm", null);
		const formItemInfo = inject("xeFormItemInfo", null);
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const refElem = ref();
		const refBarElem = ref();
		const refTrackElem = ref();
		const refStartBtnElem = ref();
		const refEndBtnElem = ref();
		const reactData = reactive({
			startValue: 0,
			endValue: 0
		});
		const refMaps = { refElem };
		const computeFormReadonly = computed(() => {
			const { readonly } = props;
			if (readonly === null) {
				if ($xeForm) return $xeForm.props.readonly;
				return false;
			}
			return readonly;
		});
		const computeIsDisabled = computed(() => {
			const { disabled } = props;
			if (disabled === null) {
				if ($xeForm) return $xeForm.props.disabled;
				return false;
			}
			return disabled;
		});
		const computeMaxNum = computed(() => {
			return import_xe_utils.default.toNumber(props.max || 0);
		});
		const computeMinNum = computed(() => {
			return import_xe_utils.default.toNumber(props.min || 0);
		});
		const computeMaps = {};
		const $xeSlider = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const emitModel = (value) => {
			emit("update:modelValue", value);
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $slider: $xeSlider }, params));
		};
		const collapsePaneMethods = { dispatchEvent };
		const getStartPercent = (startValue) => {
			const { range } = props;
			const maxNum = computeMaxNum.value;
			const minNum = computeMinNum.value;
			return range ? import_xe_utils.default.floor((startValue - minNum) / import_xe_utils.default.toNumber(maxNum - minNum) * 100) : 0;
		};
		const getEndPercent = (startValue, endValue) => {
			const { range } = props;
			const maxNum = computeMaxNum.value;
			const minNum = computeMinNum.value;
			return import_xe_utils.default.floor((endValue - (range ? startValue : 0) - minNum) / import_xe_utils.default.toNumber(maxNum - minNum) * 100);
		};
		const updateModel = () => {
			const { modelValue } = props;
			if (import_xe_utils.default.isArray(modelValue)) {
				const [sVal, eVal] = import_xe_utils.default.clone(modelValue, true).sort();
				reactData.startValue = import_xe_utils.default.floor(import_xe_utils.default.toNumber(sVal || 0));
				reactData.endValue = import_xe_utils.default.floor(import_xe_utils.default.toNumber(eVal || 0));
			} else {
				reactData.startValue = 0;
				reactData.endValue = import_xe_utils.default.floor(import_xe_utils.default.toNumber(modelValue || 0));
			}
		};
		const updateBarStyle = () => {
			const { startValue, endValue } = reactData;
			const trackElem = refTrackElem.value;
			const startBtnElem = refStartBtnElem.value;
			const endBtnElem = refEndBtnElem.value;
			let startPercent = 0;
			let endPercent = 0;
			if (startValue > endValue) {
				startPercent = getStartPercent(endValue);
				endPercent = getEndPercent(endValue, startValue);
			} else {
				startPercent = getStartPercent(startValue);
				endPercent = getEndPercent(startValue, endValue);
			}
			if (trackElem) {
				trackElem.style.left = `${startPercent}%`;
				trackElem.style.width = `${endPercent}%`;
			}
			if (startBtnElem) startBtnElem.style.left = `${startPercent}%`;
			if (endBtnElem) endBtnElem.style.left = `${import_xe_utils.default.floor(startPercent + endPercent)}%`;
		};
		const changeEvent = (evnt) => {
			const { range } = props;
			const { startValue, endValue } = reactData;
			const value = range ? [startValue, endValue].sort() : endValue;
			emitModel(value);
			dispatchEvent("change", { value }, evnt);
			if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, value);
		};
		const handleMousedownEvent = (evnt, isEnd) => {
			const formReadonly = computeFormReadonly.value;
			const isDisabled = computeIsDisabled.value;
			const maxNum = computeMaxNum.value;
			const minNum = computeMinNum.value;
			if (!(formReadonly || isDisabled)) {
				evnt.preventDefault();
				document.onmousemove = (evnt) => {
					evnt.preventDefault();
					const el = refElem.value;
					const barElem = refBarElem.value;
					if (el && barElem) {
						const barRect = barElem.getBoundingClientRect();
						const trackWidth = (evnt.clientX - barRect.left) / barRect.width;
						if (isEnd) reactData.endValue = import_xe_utils.default.floor(Math.max(minNum, Math.min(maxNum, trackWidth * (maxNum - minNum) + minNum)));
						else reactData.startValue = import_xe_utils.default.floor(Math.max(minNum, Math.min(maxNum, trackWidth * (maxNum - minNum))));
						dispatchEvent("track-dragover", {
							startValue: reactData.startValue,
							endValue: reactData.endValue
						}, evnt);
					}
					updateBarStyle();
				};
				document.onmouseup = (evnt) => {
					document.onmousemove = null;
					document.onmouseup = null;
					dispatchEvent("track-dragend", {
						startValue: reactData.startValue,
						endValue: reactData.endValue
					}, evnt);
					changeEvent(evnt);
					updateBarStyle();
				};
				dispatchEvent("track-dragstart", {
					startValue: reactData.startValue,
					endValue: reactData.endValue
				}, evnt);
			}
		};
		const handleStartMousedownEvent = (evnt) => {
			const endBtnElem = refEndBtnElem.value;
			const startBtnElem = evnt.currentTarget;
			handleMousedownEvent(evnt, endBtnElem ? endBtnElem.offsetLeft < startBtnElem.offsetLeft : false);
		};
		const handleEndMousedownEvent = (evnt) => {
			const startBtnElem = refStartBtnElem.value;
			const endBtnElem = evnt.currentTarget;
			handleMousedownEvent(evnt, startBtnElem ? endBtnElem.offsetLeft > startBtnElem.offsetLeft : true);
		};
		Object.assign($xeSlider, collapsePaneMethods, {});
		const renderVN = () => {
			const { vertical, range } = props;
			const vSize = computeSize.value;
			const formReadonly = computeFormReadonly.value;
			const isDisabled = computeIsDisabled.value;
			return h("div", {
				ref: refElem,
				class: ["vxe-slider", {
					[`size--${vSize}`]: vSize,
					"is--vertical": vertical,
					"is--readonly": formReadonly,
					"is--disabled": isDisabled
				}]
			}, [h("div", { class: "vxe-slider--inner" }, [
				h("div", {
					ref: refBarElem,
					class: "vxe-slider--bar-wrapper"
				}),
				h("div", {
					ref: refTrackElem,
					class: "vxe-slider--bar-track"
				}),
				formReadonly || !range ? renderEmptyElement($xeSlider) : h("div", {
					ref: refStartBtnElem,
					class: "vxe-slider--bar-btn vxe-slider--start-btn",
					onMousedown: handleStartMousedownEvent
				}),
				formReadonly ? renderEmptyElement($xeSlider) : h("div", {
					ref: refEndBtnElem,
					class: "vxe-slider--bar-btn vxe-slider--end-btn",
					onMousedown: handleEndMousedownEvent
				})
			])]);
		};
		watch(() => props.modelValue, () => {
			updateModel();
		});
		onMounted(() => {
			updateBarStyle();
		});
		updateModel();
		$xeSlider.renderVN = renderVN;
		return $xeSlider;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/slider/index.js
var VxeSlider = Object.assign({}, slider_default, { install(app) {
	app.component(slider_default.name, slider_default);
} });
dynamicApp.use(VxeSlider);
VxeUI.component(slider_default);
var Slider = VxeSlider;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/steps/src/steps.js
var steps_default = defineVxeComponent({
	name: "VxeSteps",
	props: {},
	emits: [],
	setup(props, context) {
		const { emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeMaps = {};
		const $xeSteps = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $steps: $xeSteps }, params));
		};
		Object.assign($xeSteps, { dispatchEvent }, {});
		const renderVN = () => {
			return h("div", {
				ref: refElem,
				class: "vxe-steps"
			}, []);
		};
		$xeSteps.renderVN = renderVN;
		return $xeSteps;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/steps/index.js
var VxeSteps = Object.assign({}, steps_default, { install(app) {
	app.component(steps_default.name, steps_default);
} });
dynamicApp.use(VxeSteps);
VxeUI.component(steps_default);
var Steps = VxeSteps;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/switch/src/switch.js
var switch_default = defineVxeComponent({
	name: "VxeSwitch",
	props: {
		modelValue: [
			String,
			Number,
			Boolean
		],
		disabled: {
			type: Boolean,
			default: null
		},
		readonly: {
			type: Boolean,
			default: null
		},
		size: {
			type: String,
			default: () => getConfig().switch.size || getConfig().size
		},
		openLabel: String,
		closeLabel: String,
		openValue: {
			type: [
				String,
				Number,
				Boolean
			],
			default: true
		},
		closeValue: {
			type: [
				String,
				Number,
				Boolean
			],
			default: false
		},
		openIcon: String,
		closeIcon: String,
		openActiveIcon: String,
		closeActiveIcon: String
	},
	emits: [
		"update:modelValue",
		"change",
		"focus",
		"blur"
	],
	setup(props, context) {
		const { emit } = context;
		const $xeForm = inject("$xeForm", null);
		const formItemInfo = inject("xeFormItemInfo", null);
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const reactData = reactive({
			isActivated: false,
			hasAnimat: false,
			offsetLeft: 0
		});
		const internalData = {};
		const $xeSwitch = {
			xID,
			props,
			context,
			reactData,
			internalData
		};
		const refButton = ref();
		let switchMethods = {};
		const computeIsDisabled = computed(() => {
			const { disabled } = props;
			if (disabled === null) {
				if ($xeForm) return $xeForm.props.readonly || $xeForm.props.disabled;
				return false;
			}
			return disabled;
		});
		const computeIsReadonly = computed(() => {
			const { readonly } = props;
			if (readonly === null) {
				if ($xeForm) return $xeForm.props.readonly || $xeForm.props.disabled;
				return false;
			}
			return readonly;
		});
		const computeOnShowLabel = computed(() => {
			return getFuncText(props.openLabel);
		});
		const computeOffShowLabel = computed(() => {
			return getFuncText(props.closeLabel);
		});
		const computeIsChecked = computed(() => {
			return props.modelValue === props.openValue;
		});
		const emitModel = (value) => {
			emit("update:modelValue", value);
		};
		const clickEvent = (evnt) => {
			const isDisabled = computeIsDisabled.value;
			const isReadonly = computeIsReadonly.value;
			if (!(isDisabled || isReadonly)) {
				const isChecked = computeIsChecked.value;
				clearTimeout(internalData.atTimeout);
				const value = isChecked ? props.closeValue : props.openValue;
				reactData.hasAnimat = true;
				emitModel(value);
				switchMethods.dispatchEvent("change", { value }, evnt);
				if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, value);
				internalData.atTimeout = setTimeout(() => {
					reactData.hasAnimat = false;
					internalData.atTimeout = void 0;
				}, 400);
			}
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $switch: $xeSwitch }, params));
		};
		const focusEvent = (evnt) => {
			reactData.isActivated = true;
			switchMethods.dispatchEvent("focus", { value: props.modelValue }, evnt);
		};
		const blurEvent = (evnt) => {
			reactData.isActivated = false;
			switchMethods.dispatchEvent("blur", { value: props.modelValue }, evnt);
		};
		switchMethods = {
			dispatchEvent,
			focus() {
				const btnElem = refButton.value;
				reactData.isActivated = true;
				if (btnElem) btnElem.focus();
				return nextTick();
			},
			blur() {
				const btnElem = refButton.value;
				if (btnElem) btnElem.blur();
				reactData.isActivated = false;
				return nextTick();
			}
		};
		Object.assign($xeSwitch, switchMethods);
		const renderVN = () => {
			const { openIcon, closeIcon, openActiveIcon, closeActiveIcon } = props;
			const vSize = computeSize.value;
			const isChecked = computeIsChecked.value;
			const onShowLabel = computeOnShowLabel.value;
			const offShowLabel = computeOffShowLabel.value;
			const isDisabled = computeIsDisabled.value;
			const isReadonly = computeIsReadonly.value;
			return h("div", { class: [
				"vxe-switch",
				isChecked ? "is--on" : "is--off",
				{
					[`size--${vSize}`]: vSize,
					"is--disabled": isDisabled,
					"is--readonly": isReadonly,
					"is--animat": reactData.hasAnimat
				}
			] }, [h("button", {
				ref: refButton,
				class: "vxe-switch--button",
				type: "button",
				disabled: isDisabled || isReadonly,
				onClick: clickEvent,
				onFocus: focusEvent,
				onBlur: blurEvent
			}, [
				h("span", { class: "vxe-switch--label vxe-switch--label-on" }, [openIcon ? h("i", { class: ["vxe-switch--label-icon", openIcon] }) : renderEmptyElement($xeSwitch), onShowLabel]),
				h("span", { class: "vxe-switch--label vxe-switch--label-off" }, [closeIcon ? h("i", { class: ["vxe-switch--label-icon", closeIcon] }) : renderEmptyElement($xeSwitch), offShowLabel]),
				h("span", { class: ["vxe-switch--icon"] }, openActiveIcon || closeActiveIcon ? [h("i", { class: isChecked ? openActiveIcon : closeActiveIcon })] : [])
			])]);
		};
		$xeSwitch.renderVN = renderVN;
		return $xeSwitch;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/switch/index.js
var VxeSwitch = Object.assign(switch_default, { install: function(app) {
	app.component(switch_default.name, switch_default);
} });
dynamicApp.use(VxeSwitch);
VxeUI.component(switch_default);
var Switch = VxeSwitch;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/tabs/src/util.js
function assembleTabItem($xeTabs, elem, tabConfig) {
	const staticTabs = $xeTabs.reactData.staticTabs;
	const parentElem = elem.parentNode;
	if (parentElem) {
		staticTabs.splice(import_xe_utils.default.arrayIndexOf(parentElem.children, elem), 0, tabConfig);
		$xeTabs.reactData.staticTabs = staticTabs.slice(0);
	}
}
function destroyTabItem($xeTabs, tabConfig) {
	const staticTabs = $xeTabs.reactData.staticTabs;
	const matchObj = import_xe_utils.default.findTree(staticTabs, (item) => item.id === tabConfig.id, { children: "children" });
	if (matchObj) matchObj.items.splice(matchObj.index, 1);
	$xeTabs.reactData.staticTabs = staticTabs.slice(0);
}
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/tabs/src/tab-pane.js
var tab_pane_default = defineVxeComponent({
	name: "VxeTabPane",
	props: {
		title: [String, Number],
		name: [String, Number],
		icon: String,
		titleWidth: [String, Number],
		titleAlign: [String, Number],
		preload: Boolean,
		permissionCode: [String, Number]
	},
	emits: [],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const $xeTabs = inject("$xeTabs", null);
		const reactData = reactive({});
		const tabConfig = reactive({
			id: xID,
			title: props.title,
			name: props.name,
			icon: props.icon,
			titleWidth: props.titleWidth,
			titleAlign: props.titleAlign,
			preload: props.preload,
			permissionCode: props.permissionCode,
			slots
		});
		const refMaps = { refElem };
		const computeMaps = {};
		const $xeTabPane = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $tabPane: $xeTabPane }, params));
		};
		Object.assign($xeTabPane, { dispatchEvent }, {});
		watch(() => props.title, (val) => {
			tabConfig.title = val;
		});
		watch(() => props.name, (val) => {
			tabConfig.name = val;
		});
		watch(() => props.icon, (val) => {
			tabConfig.icon = val;
		});
		watch(() => props.permissionCode, (val) => {
			tabConfig.permissionCode = val;
		});
		onMounted(() => {
			const elem = refElem.value;
			if ($xeTabs && elem) assembleTabItem($xeTabs, elem, tabConfig);
		});
		onUnmounted(() => {
			if ($xeTabs) destroyTabItem($xeTabs, tabConfig);
		});
		const renderVN = () => {
			return h("div", { ref: refElem }, []);
		};
		$xeTabPane.renderVN = renderVN;
		return $xeTabPane;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/tab-pane/index.js
var VxeTabPane = Object.assign({}, tab_pane_default, { install(app) {
	app.component(tab_pane_default.name, tab_pane_default);
} });
dynamicApp.use(VxeTabPane);
VxeUI.component(tab_pane_default);
var TabPane = VxeTabPane;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/table-select/src/table-select.js
function getRowUniqueId() {
	return import_xe_utils.default.uniqueId("row_");
}
function createInternalData$2() {
	return { fullRowMaps: {} };
}
var table_select_default = defineVxeComponent({
	name: "VxeTableSelect",
	props: {
		modelValue: [
			String,
			Number,
			Array
		],
		clearable: Boolean,
		placeholder: {
			type: String,
			default: () => import_xe_utils.default.eqNull(getConfig().tableSelect.placeholder) ? getI18n("vxe.base.pleaseSelect") : getConfig().tableSelect.placeholder
		},
		readonly: {
			type: Boolean,
			default: null
		},
		loading: Boolean,
		disabled: {
			type: Boolean,
			default: null
		},
		multiple: Boolean,
		className: [String, Function],
		prefixIcon: String,
		placement: String,
		columns: Array,
		options: Array,
		optionProps: Object,
		lazyOptions: Array,
		/**
		* 已废弃，请使用 popupConfig.zIndex
		* @deprecated
		*/
		zIndex: Number,
		size: {
			type: String,
			default: () => getConfig().tableSelect.size || getConfig().size
		},
		popupConfig: Object,
		gridConfig: Object,
		transfer: {
			type: Boolean,
			default: null
		}
	},
	emits: [
		"update:modelValue",
		"change",
		"clear",
		"blur",
		"focus",
		"click",
		"form-submit",
		"form-reset",
		"form-collapse",
		"page-change"
	],
	setup(props, context) {
		const { emit, slots } = context;
		const VxeTableGridComponent = VxeUI.getComponent("vxe-grid");
		const $xeModal = inject("$xeModal", null);
		const $xeDrawer = inject("$xeDrawer", null);
		const $xeTable = inject("$xeTable", null);
		const $xeForm = inject("$xeForm", null);
		const formItemInfo = inject("xeFormItemInfo", null);
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const refElem = ref();
		const refInput = ref();
		const refGridWrapper = ref();
		const refOptionPanel = ref();
		const refGrid = ref();
		const reactData = reactive({
			initialized: false,
			tableColumns: [],
			fullOptionList: [],
			panelIndex: 0,
			panelStyle: {},
			panelPlacement: null,
			triggerFocusPanel: false,
			visiblePanel: false,
			isAniVisible: false,
			isActivated: false
		});
		const internalData = createInternalData$2();
		const refMaps = { refElem };
		const computeFormReadonly = computed(() => {
			const { readonly } = props;
			if (readonly === null) {
				if ($xeForm) return $xeForm.props.readonly;
				return false;
			}
			return readonly;
		});
		const computeIsDisabled = computed(() => {
			const { disabled } = props;
			if (disabled === null) {
				if ($xeForm) return $xeForm.props.disabled;
				return false;
			}
			return disabled;
		});
		const computeBtnTransfer = computed(() => {
			const { transfer } = props;
			const popupOpts = computePopupOpts.value;
			if (import_xe_utils.default.isBoolean(popupOpts.transfer)) return popupOpts.transfer;
			if (transfer === null) {
				const globalTransfer = getConfig().tableSelect.transfer;
				if (import_xe_utils.default.isBoolean(globalTransfer)) return globalTransfer;
				if ($xeTable || $xeModal || $xeDrawer || $xeForm) return true;
			}
			return transfer;
		});
		const computePropsOpts = computed(() => {
			return props.optionProps || {};
		});
		const computeRowOpts = computed(() => {
			const gridOpts = computeGridOpts.value;
			return Object.assign({}, gridOpts.rowConfig, { isCurrent: true });
		});
		const computeRowKeyField = computed(() => {
			return computeRowOpts.value.keyField || "_X_ROW_KEY";
		});
		const computeLabelField = computed(() => {
			return computePropsOpts.value.label || "label";
		});
		const computeValueField = computed(() => {
			return computePropsOpts.value.value || "value";
		});
		const computePopupOpts = computed(() => {
			return Object.assign({}, getConfig().tableSelect.popupConfig, props.popupConfig);
		});
		const computeGridOpts = computed(() => {
			return Object.assign({}, getConfig().tableSelect.gridConfig, props.gridConfig, { data: void 0 });
		});
		const computeSelectGridOpts = computed(() => {
			const gridOpts = computeGridOpts.value;
			const { pagerConfig, proxyConfig } = gridOpts;
			if (proxyConfig) {
				const proxyAjax = proxyConfig.ajax;
				if (proxyAjax && proxyAjax.query) {
					const newProxyConfig = import_xe_utils.default.clone(proxyConfig, true);
					const ajaxMethods = proxyAjax.query;
					if (ajaxMethods) {
						const resConfigs = proxyConfig.response || proxyConfig.props || {};
						Object.assign(newProxyConfig.ajax, { query(params, ...args) {
							return Promise.resolve(ajaxMethods(params, ...args)).then((rest) => {
								let tableData = [];
								if (pagerConfig) {
									const resultProp = resConfigs.result;
									tableData = (import_xe_utils.default.isFunction(resultProp) ? resultProp({
										data: rest,
										$table: null,
										$grid: null,
										$gantt: null
									}) : import_xe_utils.default.get(rest, resultProp || "result")) || [];
								} else {
									const listProp = resConfigs.list;
									tableData = (listProp ? import_xe_utils.default.isFunction(listProp) ? listProp({
										data: rest,
										$table: null,
										$grid: null,
										$gantt: null
									}) : import_xe_utils.default.get(rest, listProp) : rest) || [];
								}
								cacheDataMap(tableData || []);
								return rest;
							});
						} });
					}
					return Object.assign({}, gridOpts, { proxyConfig: newProxyConfig });
				}
			}
			return gridOpts;
		});
		const computeSelectLabel = computed(() => {
			const { modelValue, lazyOptions } = props;
			const { fullOptionList } = reactData;
			const { fullRowMaps } = internalData;
			const valueField = computeValueField.value;
			const labelField = computeLabelField.value;
			if (!fullOptionList) return "";
			return (import_xe_utils.default.isArray(modelValue) ? modelValue : [modelValue]).map((val) => {
				const cacheItem = fullRowMaps[val];
				if (cacheItem) return cacheItem.item[labelField];
				if (lazyOptions) {
					const lazyItem = lazyOptions.find((item) => item[valueField] === val);
					if (lazyItem) return lazyItem[labelField];
				}
				return val;
			}).join(", ");
		});
		const computePopupWrapperStyle = computed(() => {
			const { height, width } = computePopupOpts.value;
			const stys = {};
			if (width) stys.width = toCssUnit(width);
			if (height) stys.height = toCssUnit(height);
			return stys;
		});
		const computeMaps = {};
		const $xeTableSelect = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const gridEventKeys = [
			"form-submit",
			"form-reset",
			"form-collapse",
			"page-change"
		];
		const gridEvents = {};
		gridEventKeys.forEach((name) => {
			gridEvents[getOnName(import_xe_utils.default.camelCase(name))] = (params) => {
				dispatchEvent(name, params, params.$event);
			};
		});
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $tableSelect: $xeTableSelect }, params));
		};
		const emitModel = (value) => {
			emit("update:modelValue", value);
		};
		const tableSelectMethods = { dispatchEvent };
		const tableSelectPrivateMethods = {};
		const getRowid = (option) => {
			const rowid = option[computeRowKeyField.value];
			return rowid ? encodeURIComponent(rowid) : "";
		};
		const getRowsByValue = (modelValue) => {
			const { fullRowMaps } = internalData;
			const rows = [];
			(import_xe_utils.default.eqNull(modelValue) ? [] : import_xe_utils.default.isArray(modelValue) ? modelValue : [modelValue]).forEach((val) => {
				const cacheItem = fullRowMaps[val];
				if (cacheItem) rows.push(cacheItem.item);
			});
			return rows;
		};
		const updateModel = (modelValue) => {
			const { multiple } = props;
			nextTick(() => {
				const $grid = refGrid.value;
				if ($grid) {
					const selectList = getRowsByValue(modelValue);
					if (selectList.length) if (multiple) $grid.setCheckboxRow(selectList, true);
					else $grid.setRadioRow(selectList[0]);
				}
			});
		};
		const loadTableColumn = (columns) => {
			if (!columns || !columns.length) return;
			const { multiple } = props;
			const tableCols = [];
			let hasRadioCol = false;
			let hasCheckboxCol = false;
			columns.forEach((column) => {
				if (!hasRadioCol && column.type === "radio") hasRadioCol = true;
				else if (!hasCheckboxCol && column.type === "checkbox") hasCheckboxCol = true;
				tableCols.push(column);
			});
			if (multiple) {
				if (!hasCheckboxCol) tableCols.unshift({
					type: "checkbox",
					width: 70
				});
			} else if (!hasRadioCol) tableCols.unshift({
				type: "radio",
				width: 70
			});
			reactData.tableColumns = tableCols;
		};
		const cacheDataMap = (dataList) => {
			const { options } = props;
			const rowKeyField = computeRowKeyField.value;
			const valueField = computeValueField.value;
			const { treeConfig, pagerConfig } = computeGridOpts.value;
			const rowMaps = {};
			const keyMaps = {};
			if (treeConfig) {} else import_xe_utils.default.arrayEach(dataList || options || [], (item, index, items) => {
				let rowid = getRowid(item);
				if (!rowid) rowid = getRowUniqueId();
				if (keyMaps[rowid]) errLog("vxe.error.repeatKey", [`[table-select] ${rowKeyField}`, rowid]);
				keyMaps[rowid] = true;
				const value = item[valueField];
				if (rowMaps[value]) errLog("vxe.error.repeatKey", [`[table-select] ${valueField}`, value]);
				rowMaps[value] = {
					item,
					index,
					items,
					parent: null,
					nodes: []
				};
			});
			reactData.fullOptionList = dataList || options || [];
			internalData.fullRowMaps = pagerConfig ? Object.assign({}, internalData.fullRowMaps, rowMaps) : rowMaps;
			updateModel(props.modelValue);
		};
		const updateZindex = () => {
			const customZIndex = computePopupOpts.value.zIndex || props.zIndex;
			if (customZIndex) reactData.panelIndex = import_xe_utils.default.toNumber(customZIndex);
			else if (reactData.panelIndex < getLastZIndex()) reactData.panelIndex = nextZIndex();
		};
		const updatePlacement = () => {
			const { placement } = props;
			const { panelIndex } = reactData;
			const targetElem = refElem.value;
			const panelElem = refOptionPanel.value;
			const btnTransfer = computeBtnTransfer.value;
			const popupOpts = computePopupOpts.value;
			const handleStyle = () => {
				const ppObj = updatePanelPlacement(targetElem, panelElem, {
					placement: popupOpts.placement || placement,
					defaultPlacement: popupOpts.defaultPlacement,
					teleportTo: btnTransfer
				});
				reactData.panelStyle = Object.assign(ppObj.style, { zIndex: panelIndex });
				reactData.panelPlacement = ppObj.placement;
			};
			handleStyle();
			return nextTick().then(handleStyle);
		};
		const showOptionPanel = () => {
			const { loading } = props;
			const isDisabled = computeIsDisabled.value;
			if (!loading && !isDisabled) {
				if (internalData.vpTimeout) clearTimeout(internalData.vpTimeout);
				if (internalData.hpTimeout) clearTimeout(internalData.hpTimeout);
				if (!reactData.initialized) reactData.initialized = true;
				reactData.isActivated = true;
				reactData.isAniVisible = true;
				internalData.vpTimeout = setTimeout(() => {
					reactData.visiblePanel = true;
					updateModel(props.modelValue);
					internalData.vpTimeout = void 0;
					updatePlacement();
				}, 10);
				updateZindex();
				updatePlacement();
			}
		};
		const hideOptionPanel = () => {
			reactData.visiblePanel = false;
			if (internalData.vpTimeout) clearTimeout(internalData.vpTimeout);
			if (internalData.hpTimeout) clearTimeout(internalData.hpTimeout);
			internalData.hpTimeout = setTimeout(() => {
				reactData.isAniVisible = false;
				internalData.hpTimeout = void 0;
			}, 350);
		};
		const changeEvent = (evnt, selectValue, row) => {
			emitModel(selectValue);
			if (selectValue !== props.modelValue) {
				dispatchEvent("change", {
					value: selectValue,
					row,
					option: row
				}, evnt);
				if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, selectValue);
			}
		};
		const clearValueEvent = (evnt, selectValue) => {
			changeEvent(evnt, selectValue, null);
			dispatchEvent("clear", { value: selectValue }, evnt);
		};
		const clearEvent = (params, evnt) => {
			clearValueEvent(evnt, null);
			hideOptionPanel();
		};
		const handleGlobalMousewheelEvent = (evnt) => {
			const { visiblePanel } = reactData;
			if (!computeIsDisabled.value) {
				if (visiblePanel) {
					const panelElem = refOptionPanel.value;
					if (getEventTargetNode(evnt, panelElem).flag) updatePlacement();
					else hideOptionPanel();
				}
			}
		};
		const handleGlobalMousedownEvent = (evnt) => {
			const { visiblePanel } = reactData;
			if (!computeIsDisabled.value) {
				const el = refElem.value;
				const panelElem = refOptionPanel.value;
				reactData.isActivated = getEventTargetNode(evnt, el).flag || getEventTargetNode(evnt, panelElem).flag;
				if (visiblePanel && !reactData.isActivated) hideOptionPanel();
			}
		};
		const handleGlobalBlurEvent = () => {
			const { visiblePanel, isActivated } = reactData;
			if (visiblePanel) hideOptionPanel();
			if (isActivated) reactData.isActivated = false;
			if (visiblePanel || isActivated) {
				const $input = refInput.value;
				if ($input) $input.blur();
			}
		};
		const handleGlobalResizeEvent = () => {
			const { visiblePanel } = reactData;
			if (visiblePanel) updatePlacement();
		};
		const focusEvent = (evnt) => {
			if (!computeIsDisabled.value) {
				if (!reactData.visiblePanel) {
					reactData.triggerFocusPanel = true;
					showOptionPanel();
					setTimeout(() => {
						reactData.triggerFocusPanel = false;
					}, 150);
				}
			}
			dispatchEvent("focus", {}, evnt);
		};
		const clickEvent = (evnt) => {
			togglePanelEvent(evnt);
			dispatchEvent("click", {}, evnt);
		};
		const blurEvent = (evnt) => {
			reactData.isActivated = false;
			dispatchEvent("blur", {}, evnt);
		};
		const togglePanelEvent = (params) => {
			const { $event } = params;
			$event.preventDefault();
			if (reactData.triggerFocusPanel) reactData.triggerFocusPanel = false;
			else if (reactData.visiblePanel) hideOptionPanel();
			else showOptionPanel();
		};
		const radioChangeEvent = (params) => {
			const { $event, row } = params;
			const value = row[computeValueField.value];
			changeEvent($event, value, row);
			hideOptionPanel();
		};
		const checkboxChangeEvent = (params) => {
			const { $grid, $event, row } = params;
			const valueField = computeValueField.value;
			if ($grid) changeEvent($event, $grid.getCheckboxRecords().map((row) => {
				return row[valueField];
			}), row);
		};
		const checkboxAllEvent = (params) => {
			checkboxChangeEvent(params);
		};
		Object.assign($xeTableSelect, tableSelectMethods, tableSelectPrivateMethods);
		const renderVN = () => {
			const { className, options, loading } = props;
			const { initialized, isActivated, isAniVisible, visiblePanel, tableColumns } = reactData;
			const vSize = computeSize.value;
			const isDisabled = computeIsDisabled.value;
			const selectLabel = computeSelectLabel.value;
			const btnTransfer = computeBtnTransfer.value;
			const formReadonly = computeFormReadonly.value;
			const popupOpts = computePopupOpts.value;
			const selectGridOpts = computeSelectGridOpts.value;
			const rowOpts = computeRowOpts.value;
			const popupWrapperStyle = computePopupWrapperStyle.value;
			const headerSlot = slots.header;
			const footerSlot = slots.footer;
			const prefixSlot = slots.prefix;
			const ppClassName = popupOpts.className;
			if (formReadonly) return h("div", {
				ref: refElem,
				class: ["vxe-table-select--readonly", className]
			}, [h("span", { class: "vxe-table-select-label" }, selectLabel)]);
			return h("div", {
				ref: refElem,
				class: [
					"vxe-table-select",
					className ? import_xe_utils.default.isFunction(className) ? className({ $tableSelect: $xeTableSelect }) : className : "",
					{
						[`size--${vSize}`]: vSize,
						"is--visible": visiblePanel,
						"is--disabled": isDisabled,
						"is--loading": loading,
						"is--active": isActivated
					}
				]
			}, [h(input_default, {
				ref: refInput,
				clearable: props.clearable,
				placeholder: loading ? getI18n("vxe.select.loadingText") : props.placeholder,
				editable: false,
				disabled: isDisabled,
				type: "text",
				prefixIcon: props.prefixIcon,
				suffixIcon: loading ? getIcon().TABLE_SELECT_LOADED : visiblePanel ? getIcon().TABLE_SELECT_OPEN : getIcon().TABLE_SELECT_CLOSE,
				modelValue: loading ? "" : selectLabel,
				onClear: clearEvent,
				onClick: clickEvent,
				onFocus: focusEvent,
				onBlur: blurEvent,
				onSuffixClick: togglePanelEvent
			}, prefixSlot ? { prefix: () => prefixSlot({}) } : {}), h(Teleport, {
				to: "body",
				disabled: btnTransfer ? !initialized : true
			}, [h("div", {
				ref: refOptionPanel,
				class: [
					"vxe-table--ignore-clear vxe-table-select--panel",
					ppClassName ? import_xe_utils.default.isFunction(ppClassName) ? ppClassName({ $tableSelect: $xeTableSelect }) : ppClassName : "",
					{
						[`size--${vSize}`]: vSize,
						"is--transfer": btnTransfer,
						"ani--leave": !loading && isAniVisible,
						"ani--enter": !loading && visiblePanel
					}
				],
				placement: reactData.panelPlacement,
				style: reactData.panelStyle
			}, initialized ? [h("div", { class: "vxe-table-select--panel-wrapper" }, [
				headerSlot ? h("div", { class: "vxe-table-select--panel-header" }, headerSlot({})) : renderEmptyElement($xeTableSelect),
				h("div", { class: "vxe-table-select--panel-body" }, [h("div", {
					ref: refGridWrapper,
					class: "vxe-table-select-grid--wrapper",
					style: popupWrapperStyle
				}, [VxeTableGridComponent ? h(VxeTableGridComponent, Object.assign(Object.assign(Object.assign({}, selectGridOpts), gridEvents), {
					class: "vxe-table-select--grid",
					ref: refGrid,
					rowConfig: rowOpts,
					data: options,
					columns: tableColumns.length ? tableColumns : selectGridOpts.columns,
					height: "100%",
					autoResize: true,
					onRadioChange: radioChangeEvent,
					onCheckboxChange: checkboxChangeEvent,
					onCheckboxAll: checkboxAllEvent
				}), Object.assign({}, slots, {
					header: void 0,
					footer: void 0,
					prefixSlot: void 0
				})) : renderEmptyElement($xeTableSelect)])]),
				footerSlot ? h("div", { class: "vxe-table-select--panel-footer" }, footerSlot({})) : renderEmptyElement($xeTableSelect)
			])] : [])])]);
		};
		watch(() => props.options, () => {
			cacheDataMap();
		});
		watch(() => props.columns, (val) => {
			loadTableColumn(val);
		});
		watch(() => props.modelValue, (val) => {
			updateModel(val);
		});
		loadTableColumn(props.columns);
		cacheDataMap();
		onMounted(() => {
			const { gridConfig } = props;
			if (gridConfig && gridConfig.proxyConfig) {
				if (gridConfig.proxyConfig.autoLoad !== false) reactData.initialized = true;
			}
			globalEvents.on($xeTableSelect, "mousewheel", handleGlobalMousewheelEvent);
			globalEvents.on($xeTableSelect, "mousedown", handleGlobalMousedownEvent);
			globalEvents.on($xeTableSelect, "blur", handleGlobalBlurEvent);
			globalEvents.on($xeTableSelect, "resize", handleGlobalResizeEvent);
		});
		onUnmounted(() => {
			globalEvents.off($xeTableSelect, "mousewheel");
			globalEvents.off($xeTableSelect, "mousedown");
			globalEvents.off($xeTableSelect, "blur");
			globalEvents.off($xeTableSelect, "resize");
			import_xe_utils.default.assign(internalData, createInternalData$2());
		});
		nextTick(() => {
			if (!VxeTableGridComponent) errLog("vxe.error.reqComp", ["[table-select] vxe-grid"]);
		});
		provide("$xeTableSelect", $xeTableSelect);
		$xeTableSelect.renderVN = renderVN;
		return $xeTableSelect;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/table-select/index.js
var VxeTableSelect = Object.assign({}, table_select_default, { install(app) {
	app.component(table_select_default.name, table_select_default);
} });
dynamicApp.use(VxeTableSelect);
VxeUI.component(table_select_default);
var TableSelect = VxeTableSelect;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/tabs/src/tabs.js
var scrollbarOffsetSize = 20;
var tabs_default = defineVxeComponent({
	name: "VxeTabs",
	props: {
		modelValue: [
			String,
			Number,
			Boolean
		],
		options: Array,
		width: [String, Number],
		height: [String, Number],
		destroyOnClose: {
			type: Boolean,
			default: () => getConfig().tabs.destroyOnClose
		},
		titleWidth: [String, Number],
		titleAlign: [String, Number],
		type: {
			type: String,
			default: () => getConfig().tabs.type
		},
		position: {
			type: String,
			default: () => getConfig().tabs.position
		},
		showClose: Boolean,
		showBody: {
			type: Boolean,
			default: true
		},
		padding: {
			type: Boolean,
			default: () => getConfig().tabs.padding
		},
		trigger: String,
		beforeChangeMethod: Function,
		closeConfig: Object,
		refreshConfig: Object,
		size: {
			type: String,
			default: () => getConfig().tabs.size || getConfig().size
		},
		beforeCloseMethod: Function
	},
	emits: [
		"update:modelValue",
		"change",
		"tab-change",
		"tab-change-fail",
		"tab-close",
		"tab-close-fail",
		"tab-click",
		"tab-load"
	],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const $xeParentTabs = inject("$xeTabs", null);
		const { computeSize } = useSize(props);
		const refElem = ref();
		const refHeadWrapperElem = ref();
		const refHeadPrevElem = ref();
		const refHeadNextElem = ref();
		const reactData = reactive({
			staticTabs: [],
			activeName: null,
			initNames: [],
			lintLeft: 0,
			lintTop: 0,
			lintWidth: 0,
			lintHeight: 0,
			scrollbarWidth: 0,
			scrollbarHeight: 0,
			isTabOver: false,
			resizeFlag: 1,
			cacheTabMaps: {}
		});
		const internalData = { slTimeout: void 0 };
		const refMaps = { refElem };
		const computeTabType = computed(() => {
			const { type } = props;
			return type || "default";
		});
		const computeTabPosition = computed(() => {
			const { position } = props;
			return position || "top";
		});
		const computeLrPosition = computed(() => {
			const tabPosition = computeTabPosition.value;
			return tabPosition === "left" || tabPosition === "right";
		});
		const computeLineStyle = computed(() => {
			const { lintLeft, lintTop, lintWidth, lintHeight } = reactData;
			return computeLrPosition.value ? {
				top: `${lintTop}px`,
				height: `${lintHeight}px`
			} : {
				left: `${lintLeft}px`,
				width: `${lintWidth}px`
			};
		});
		const computeWrapperStyle = computed(() => {
			const { width } = props;
			const stys = {};
			if (width) stys.width = toCssUnit(width);
			return stys;
		});
		const computeCloseOpts = computed(() => {
			return Object.assign({}, getConfig().tabs.closeConfig, props.closeConfig);
		});
		const computeRefreshOpts = computed(() => {
			return Object.assign({}, getConfig().tabs.refreshConfig, props.refreshConfig);
		});
		const computeTabOptions = computed(() => {
			const { options } = props;
			return (options || []).filter((item) => handleFilterTab(item));
		});
		const computeTabStaticOptions = computed(() => {
			const { staticTabs } = reactData;
			return staticTabs.filter((item) => handleFilterTab(item));
		});
		const computeParentTabsResizeFlag = computed(() => {
			return $xeParentTabs ? $xeParentTabs.reactData.resizeFlag : null;
		});
		const computeMaps = {};
		const $xeTabs = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const handleFilterTab = (item) => {
			const { permissionCode } = item;
			if (permissionCode) {
				if (!permission.checkVisible(permissionCode)) return false;
			}
			return true;
		};
		const callSlot = (slotFunc, params) => {
			if (slotFunc) {
				if (import_xe_utils.default.isString(slotFunc)) slotFunc = slots[slotFunc] || null;
				if (import_xe_utils.default.isFunction(slotFunc)) return getSlotVNs$1(slotFunc(params));
			}
			return [];
		};
		const checkScrolling = () => {
			const lrPosition = computeLrPosition.value;
			const headerWrapperEl = refHeadWrapperElem.value;
			const headPrevEl = refHeadPrevElem.value;
			const headNextEl = refHeadNextElem.value;
			if (headerWrapperEl) {
				const { scrollLeft, scrollTop, clientWidth, clientHeight, scrollWidth, scrollHeight } = headerWrapperEl;
				if (headPrevEl) if ((lrPosition ? scrollTop : scrollLeft) > 0) addClass(headPrevEl, "scrolling--middle");
				else removeClass(headPrevEl, "scrolling--middle");
				if (headNextEl) if (lrPosition ? clientHeight < scrollHeight - Math.ceil(scrollTop) : clientWidth < scrollWidth - Math.ceil(scrollLeft)) addClass(headNextEl, "scrolling--middle");
				else removeClass(headNextEl, "scrolling--middle");
			}
		};
		const updateTabStyle = () => {
			const handleStyle = () => {
				const { activeName } = reactData;
				const tabType = computeTabType.value;
				const tabOptions = computeTabOptions.value;
				const tabStaticOptions = computeTabStaticOptions.value;
				const headerWrapperEl = refHeadWrapperElem.value;
				const lrPosition = computeLrPosition.value;
				let lintWidth = 0;
				let lintHeight = 0;
				let lintLeft = 0;
				let lintTop = 0;
				let sBarWidth = 0;
				let sBarHeight = 0;
				let isOver = false;
				if (headerWrapperEl) {
					const index = import_xe_utils.default.findIndexOf(tabStaticOptions.length ? tabStaticOptions : tabOptions, (item) => item.name === activeName);
					const { children, offsetWidth, scrollWidth, offsetHeight, scrollHeight, clientWidth, clientHeight } = headerWrapperEl;
					sBarWidth = offsetWidth - clientWidth;
					sBarHeight = offsetHeight - clientHeight;
					if (lrPosition) {
						isOver = scrollHeight !== clientHeight;
						if (index > -1) {
							const tabEl = children[index];
							if (tabEl) {
								const tabHeight = tabEl.clientHeight;
								const tabWidth = tabEl.clientWidth;
								if (tabType === "card") {
									lintWidth = tabWidth;
									lintHeight = tabHeight;
									lintTop = tabEl.offsetTop;
								} else if (tabType === "border-card") {
									lintWidth = tabWidth;
									lintHeight = tabHeight;
									lintTop = tabEl.offsetTop - 1;
								} else {
									lintHeight = Math.max(4, Math.floor(tabHeight * .6));
									lintTop = tabEl.offsetTop + Math.floor((tabHeight - lintHeight) / 2);
								}
							}
						}
					} else {
						isOver = scrollWidth !== clientWidth;
						if (index > -1) {
							const tabEl = children[index];
							if (tabEl) {
								const tabWidth = tabEl.clientWidth;
								if (tabType === "card") {
									lintWidth = tabWidth + 1;
									lintLeft = tabEl.offsetLeft;
								} else if (tabType === "border-card") {
									lintWidth = tabWidth;
									lintLeft = tabEl.offsetLeft - 1;
								} else {
									lintWidth = Math.max(4, Math.floor(tabWidth * .6));
									lintLeft = tabEl.offsetLeft + Math.floor((tabWidth - lintWidth) / 2);
								}
							}
						}
					}
				}
				reactData.scrollbarWidth = sBarWidth;
				reactData.scrollbarHeight = sBarHeight;
				reactData.lintLeft = lintLeft;
				reactData.lintTop = lintTop;
				reactData.lintWidth = lintWidth;
				reactData.lintHeight = lintHeight;
				reactData.isTabOver = isOver;
				checkScrolling();
			};
			handleStyle();
			nextTick(handleStyle);
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $tabs: $xeTabs }, params));
		};
		const emitModel = (value) => {
			emit("update:modelValue", value);
		};
		const addInitName = (name, evnt) => {
			const { initNames } = reactData;
			if (name && !initNames.includes(name)) {
				dispatchEvent("tab-load", { name }, evnt);
				initNames.push(name);
				return true;
			}
			return false;
		};
		const initDefaultName = (list) => {
			let activeName = null;
			const nameMaps = {};
			if (list && list.length) {
				let validVal = false;
				activeName = props.modelValue;
				list.forEach((item) => {
					const { name, preload } = item || {};
					if (name) {
						nameMaps[`${name}`] = { loading: false };
						if (activeName === name) validVal = true;
						if (preload) addInitName(name, null);
					}
				});
				if (!validVal) {
					activeName = list[0].name;
					addInitName(activeName, null);
					emitModel(activeName);
				}
			}
			reactData.activeName = activeName;
			reactData.cacheTabMaps = nameMaps;
		};
		const clickEvent = (evnt, item) => {
			const { trigger } = props;
			const beforeMethod = props.beforeChangeMethod || getConfig().tabs.beforeChangeMethod;
			const { activeName } = reactData;
			const { name } = item;
			const value = name;
			dispatchEvent("tab-click", { name }, evnt);
			if (trigger === "manual") return;
			if (name !== activeName) Promise.resolve(!beforeMethod || beforeMethod({
				$tabs: $xeTabs,
				name,
				oldName: activeName,
				newName: name,
				option: item
			})).then((status) => {
				if (status) {
					reactData.activeName = name;
					emitModel(value);
					addInitName(name, evnt);
					dispatchEvent("change", {
						value,
						name,
						oldName: activeName,
						newName: name,
						option: item
					}, evnt);
					dispatchEvent("tab-change", {
						value,
						name,
						oldName: activeName,
						newName: name,
						option: item
					}, evnt);
				} else dispatchEvent("tab-change-fail", {
					value,
					name,
					oldName: activeName,
					newName: name,
					option: item
				}, evnt);
			}).catch(() => {
				dispatchEvent("tab-change-fail", {
					value,
					name,
					oldName: activeName,
					newName: name,
					option: item
				}, evnt);
			});
		};
		const handleRefreshTabEvent = (evnt, item) => {
			evnt.stopPropagation();
			const { activeName, cacheTabMaps } = reactData;
			const { name } = item;
			const { queryMethod } = computeRefreshOpts.value;
			const cacheItem = name ? cacheTabMaps[`${name}`] : null;
			if (cacheItem) if (queryMethod) {
				if (cacheItem.loading) return;
				cacheItem.loading = true;
				Promise.resolve(queryMethod({
					$tabs: $xeTabs,
					value: activeName,
					name,
					option: item
				})).finally(() => {
					cacheItem.loading = false;
				});
			} else errLog("vxe.error.notFunc", ["refresh-config.queryMethod"]);
		};
		const handleCloseTabEvent = (evnt, item, index, list) => {
			evnt.stopPropagation();
			const { activeName } = reactData;
			const beforeMethod = computeCloseOpts.value.beforeMethod || props.beforeCloseMethod || getConfig().tabs.beforeCloseMethod;
			const { name } = item;
			const value = activeName;
			let nextName = value;
			if (activeName === name) {
				const nextItem = index < list.length - 1 ? list[index + 1] : list[index - 1];
				nextName = nextItem ? nextItem.name : null;
			}
			Promise.resolve(!beforeMethod || beforeMethod({
				$tabs: $xeTabs,
				value,
				name,
				nextName,
				option: item
			})).then((status) => {
				if (status) dispatchEvent("tab-close", {
					value,
					name,
					nextName
				}, evnt);
				else dispatchEvent("tab-close-fail", {
					value,
					name,
					nextName
				}, evnt);
			}).catch(() => {
				dispatchEvent("tab-close-fail", {
					value,
					name,
					nextName
				}, evnt);
			});
		};
		const startScrollAnimation = (offsetPos, offsetSize) => {
			const { slTimeout } = internalData;
			const lrPosition = computeLrPosition.value;
			let offsetLeft = lrPosition ? 0 : offsetSize;
			let offsetTop = lrPosition ? offsetSize : 0;
			let scrollCount = 6;
			let delayNum = 35;
			if (slTimeout) {
				clearTimeout(slTimeout);
				internalData.slTimeout = void 0;
			}
			const scrollAnimate = () => {
				const headerWrapperEl = refHeadWrapperElem.value;
				if (scrollCount > 0) {
					scrollCount--;
					if (headerWrapperEl) {
						const { clientWidth, clientHeight, scrollWidth, scrollHeight, scrollLeft, scrollTop } = headerWrapperEl;
						if (lrPosition) {
							offsetTop = Math.floor(offsetTop / 2);
							if (offsetPos > 0) {
								if (clientHeight + scrollTop < scrollHeight) {
									headerWrapperEl.scrollTop += offsetTop;
									delayNum -= 4;
									internalData.slTimeout = setTimeout(scrollAnimate, delayNum);
								}
							} else if (scrollTop > 0) {
								headerWrapperEl.scrollTop -= offsetTop;
								delayNum -= 4;
								internalData.slTimeout = setTimeout(scrollAnimate, delayNum);
							}
						} else {
							offsetLeft = Math.floor(offsetLeft / 2);
							if (offsetPos > 0) {
								if (clientWidth + scrollLeft < scrollWidth) {
									headerWrapperEl.scrollLeft += offsetLeft;
									delayNum -= 4;
									internalData.slTimeout = setTimeout(scrollAnimate, delayNum);
								}
							} else if (scrollLeft > 0) {
								headerWrapperEl.scrollLeft -= offsetLeft;
								delayNum -= 4;
								internalData.slTimeout = setTimeout(scrollAnimate, delayNum);
							}
						}
						updateTabStyle();
					}
				}
			};
			scrollAnimate();
		};
		const handleScrollToLeft = (offsetPos) => {
			const lrPosition = computeLrPosition.value;
			const headerWrapperEl = refHeadWrapperElem.value;
			if (headerWrapperEl) {
				const { clientWidth, clientHeight } = headerWrapperEl;
				startScrollAnimation(offsetPos, Math.floor((lrPosition ? clientHeight : clientWidth) * .75));
			}
		};
		const scrollLeftEvent = () => {
			handleScrollToLeft(-1);
		};
		const scrollRightEvent = () => {
			handleScrollToLeft(1);
		};
		const scrollToTab = (name) => {
			const tabOptions = computeTabOptions.value;
			const tabStaticOptions = computeTabStaticOptions.value;
			const lrPosition = computeLrPosition.value;
			return nextTick().then(() => {
				const headerWrapperEl = refHeadWrapperElem.value;
				if (headerWrapperEl) {
					const index = import_xe_utils.default.findIndexOf(tabStaticOptions.length ? tabStaticOptions : tabOptions, (item) => item.name === name);
					if (index > -1) {
						const { scrollLeft, scrollTop, clientWidth, clientHeight, children } = headerWrapperEl;
						const tabEl = children[index];
						if (tabEl) if (lrPosition) {
							const tabOffsetTop = tabEl.offsetTop;
							const overSize = tabOffsetTop + tabEl.clientHeight - (scrollTop + clientHeight);
							if (overSize > 0) headerWrapperEl.scrollTop += overSize;
							if (tabOffsetTop < scrollTop) headerWrapperEl.scrollTop = tabOffsetTop;
						} else {
							const tabOffsetLeft = tabEl.offsetLeft;
							const overSize = tabOffsetLeft + tabEl.clientWidth - (scrollLeft + clientWidth);
							if (overSize > 0) headerWrapperEl.scrollLeft += overSize;
							if (tabOffsetLeft < scrollLeft) headerWrapperEl.scrollLeft = tabOffsetLeft;
						}
					}
					updateTabStyle();
				}
			});
		};
		const handlePrevNext = (isNext) => {
			const { activeName } = reactData;
			const tabOptions = computeTabOptions.value;
			const tabStaticOptions = computeTabStaticOptions.value;
			const list = tabStaticOptions.length ? tabStaticOptions : tabOptions;
			const index = import_xe_utils.default.findIndexOf(list, (item) => item.name === activeName);
			if (index > -1) {
				let item = null;
				if (isNext) {
					if (index < list.length - 1) item = list[index + 1];
				} else if (index > 0) item = list[index - 1];
				if (item) {
					const name = item.name;
					const value = name;
					reactData.activeName = name;
					emitModel(value);
					addInitName(name, null);
				}
			}
			return nextTick();
		};
		const tabsMethods = {
			dispatchEvent,
			scrollToTab,
			prev() {
				return handlePrevNext(false);
			},
			next() {
				return handlePrevNext(true);
			},
			prevTab() {
				warnLog("vxe.error.delFunc", ["[tabs] prevTab", "prev"]);
				return tabsMethods.prev();
			},
			nextTab() {
				warnLog("vxe.error.delFunc", ["[tabs] nextTab", "next"]);
				return tabsMethods.next();
			}
		};
		Object.assign($xeTabs, tabsMethods, {});
		const renderTabHeader = (tabList) => {
			const { titleWidth: allTitleWidth, titleAlign: allTitleAlign, showClose, closeConfig, refreshConfig } = props;
			const { activeName, scrollbarWidth, scrollbarHeight, isTabOver, cacheTabMaps } = reactData;
			const tabType = computeTabType.value;
			const tabPosition = computeTabPosition.value;
			const lrPosition = computeLrPosition.value;
			const lineStyle = computeLineStyle.value;
			const tabPrefixSlot = slots.tabPrefix || slots["tab-prefix"] || slots.prefix;
			const tabSuffixSlot = slots.tabSuffix || slots["tab-suffix"] || slots.suffix || slots.extra;
			const closeOpts = computeCloseOpts.value;
			const closeVisibleMethod = closeOpts.visibleMethod;
			const refreshOpts = computeRefreshOpts.value;
			const refreshVisibleMethod = refreshOpts.visibleMethod;
			return h("div", {
				key: "th",
				class: [
					"vxe-tabs-header",
					`type--${tabType}`,
					`pos--${tabPosition}`
				]
			}, [
				tabPrefixSlot ? h("div", { class: [
					"vxe-tabs-header--prefix",
					`type--${tabType}`,
					`pos--${tabPosition}`
				] }, callSlot(tabPrefixSlot, { name: activeName })) : renderEmptyElement($xeTabs),
				isTabOver ? h("div", {
					ref: refHeadPrevElem,
					class: [
						"vxe-tabs-header--bar vxe-tabs-header--prev-bar",
						`type--${tabType}`,
						`pos--${tabPosition}`
					],
					onClick: scrollLeftEvent
				}, [h("span", { class: lrPosition ? getIcon().TABS_TAB_BUTTON_TOP : getIcon().TABS_TAB_BUTTON_LEFT })]) : renderEmptyElement($xeTabs),
				h("div", { class: [
					"vxe-tabs-header--wrapper",
					`type--${tabType}`,
					`pos--${tabPosition}`
				] }, [h("div", {
					ref: refHeadWrapperElem,
					class: "vxe-tabs-header--item-wrapper",
					style: lrPosition ? {
						marginRight: `-${scrollbarWidth + scrollbarOffsetSize}px`,
						paddingRight: `${scrollbarOffsetSize}px`
					} : {
						marginBottom: `-${scrollbarHeight + scrollbarOffsetSize}px`,
						paddingBottom: `${scrollbarOffsetSize}px`
					},
					onScroll: checkScrolling
				}, tabList.map((item, index) => {
					const { title, titleWidth, titleAlign, icon, name } = item;
					const itemSlots = item.slots || {};
					const titleSlot = itemSlots.title || itemSlots.tab;
					const titlePrefixSlot = itemSlots.titlePrefix || itemSlots["title-prefix"];
					const titleSuffixSlot = itemSlots.titleSuffix || itemSlots["title-suffix"];
					const itemWidth = titleWidth || allTitleWidth;
					const itemAlign = titleAlign || allTitleAlign;
					const params = {
						$tabs: $xeTabs,
						value: activeName,
						name,
						option: item
					};
					const isActive = activeName === name;
					const cacheItem = name ? cacheTabMaps[`${name}`] : null;
					const isLoading = cacheItem ? cacheItem.loading : false;
					return h("div", {
						key: `${name}`,
						class: [
							"vxe-tabs-header--item",
							`type--${tabType}`,
							`pos--${tabPosition}`,
							itemAlign ? `align--${itemAlign}` : "",
							{ "is--active": isActive }
						],
						style: itemWidth ? { width: toCssUnit(itemWidth) } : void 0,
						onClick(evnt) {
							clickEvent(evnt, item);
						}
					}, [h("div", { class: "vxe-tabs-header--item-inner" }, [
						h("div", { class: "vxe-tabs-header--item-content" }, [
							icon ? h("span", { class: "vxe-tabs-header--item-icon" }, [h("i", { class: icon })]) : renderEmptyElement($xeTabs),
							titlePrefixSlot ? h("span", { class: "vxe-tabs-header--item-prefix" }, callSlot(titlePrefixSlot, {
								name,
								title
							})) : renderEmptyElement($xeTabs),
							h("span", { class: "vxe-tabs-header--item-name" }, titleSlot ? callSlot(titleSlot, {
								name,
								title
							}) : `${title}`),
							titleSuffixSlot ? h("span", { class: "vxe-tabs-header--item-suffix" }, callSlot(titleSuffixSlot, {
								name,
								title
							})) : renderEmptyElement($xeTabs)
						]),
						(isEnableConf(refreshConfig) || refreshOpts.enabled) && (refreshVisibleMethod ? refreshVisibleMethod(params) : true) ? h("div", {
							class: ["vxe-tabs-header--refresh-btn", {
								"is--active": isActive,
								"is--loading": isLoading,
								"is--disabled": isLoading
							}],
							onClick(evnt) {
								handleRefreshTabEvent(evnt, item);
							}
						}, [h("i", { class: isLoading ? getIcon().TABS_TAB_REFRESH_LOADING : getIcon().TABS_TAB_REFRESH })]) : renderEmptyElement($xeTabs),
						(showClose || isEnableConf(closeConfig) || closeOpts.enabled) && (!closeVisibleMethod || closeVisibleMethod(params)) ? h("div", {
							class: ["vxe-tabs-header--close-btn", { "is--active": isActive }],
							onClick(evnt) {
								handleCloseTabEvent(evnt, item, index, tabList);
							}
						}, [h("i", { class: getIcon().TABS_TAB_CLOSE })]) : renderEmptyElement($xeTabs)
					])]);
				}).concat([h("span", {
					key: "line",
					class: [
						"vxe-tabs-header--active-line",
						`type--${tabType}`,
						`pos--${tabPosition}`
					],
					style: lineStyle
				})]))]),
				isTabOver ? h("div", {
					ref: refHeadNextElem,
					class: [
						"vxe-tabs-header--bar vxe-tabs-header--next-bar",
						`type--${tabType}`,
						`pos--${tabPosition}`
					],
					onClick: scrollRightEvent
				}, [h("span", { class: lrPosition ? getIcon().TABS_TAB_BUTTON_BOTTOM : getIcon().TABS_TAB_BUTTON_RIGHT })]) : renderEmptyElement($xeTabs),
				tabSuffixSlot ? h("div", { class: [
					"vxe-tabs-header--suffix",
					`type--${tabType}`,
					`pos--${tabPosition}`
				] }, callSlot(tabSuffixSlot, { name: activeName })) : renderEmptyElement($xeTabs)
			]);
		};
		const renderTabPane = (item) => {
			const { initNames, activeName } = reactData;
			const { name, slots } = item;
			const defaultSlot = slots ? slots.default : null;
			return name && initNames.includes(name) ? h("div", {
				key: `${name}`,
				class: ["vxe-tabs-pane--item", { "is--visible": activeName === name }]
			}, defaultSlot ? callSlot(defaultSlot, { name }) : []) : renderEmptyElement($xeTabs);
		};
		const renderTabContent = (tabList) => {
			const { destroyOnClose } = props;
			const { activeName } = reactData;
			if (destroyOnClose) {
				const activeTab = tabList.find((item) => item.name === activeName);
				return [activeTab ? renderTabPane(activeTab) : renderEmptyElement($xeTabs)];
			}
			return tabList.map((item) => renderTabPane(item));
		};
		const rendetTabBody = (tabList) => {
			const { height, padding, showBody } = props;
			const { activeName, cacheTabMaps } = reactData;
			const vSize = computeSize.value;
			const tabType = computeTabType.value;
			const tabPosition = computeTabPosition.value;
			const { showLoading } = computeRefreshOpts.value;
			const headerpSlot = slots.header;
			const footerSlot = slots.footer;
			if (!showBody) return renderEmptyElement($xeTabs);
			const cacheItem = activeName ? cacheTabMaps[`${activeName}`] : null;
			const isLoading = cacheItem ? cacheItem.loading : false;
			const defParams = { name: activeName };
			return h("div", {
				key: "tb",
				class: [
					"vxe-tabs-pane--wrapper",
					`type--${tabType}`,
					`pos--${tabPosition}`,
					{ "is--content": showBody }
				]
			}, [
				headerpSlot ? h("div", { class: "vxe-tabs-pane--header" }, callSlot(headerpSlot, defParams)) : renderEmptyElement($xeTabs),
				h("div", { class: [
					"vxe-tabs-pane--body",
					`type--${tabType}`,
					`pos--${tabPosition}`,
					{
						[`size--${vSize}`]: vSize,
						"is--padding": padding,
						"is--height": height
					}
				] }, renderTabContent(tabList)),
				footerSlot ? h("div", { class: "vxe-tabs-pane--footer" }, callSlot(footerSlot, defParams)) : renderEmptyElement($xeTabs),
				showLoading && isLoading ? renderEmptyElement($xeTabs) : h(loading_default, {
					class: "vxe-tabs--loading",
					modelValue: isLoading
				})
			]);
		};
		const renderVN = () => {
			const { height, padding, trigger } = props;
			const { activeName } = reactData;
			const vSize = computeSize.value;
			const tabOptions = computeTabOptions.value;
			const tabStaticOptions = computeTabStaticOptions.value;
			const tabType = computeTabType.value;
			const tabPosition = computeTabPosition.value;
			const wrapperStyle = computeWrapperStyle.value;
			const defaultSlot = slots.default;
			const tabList = defaultSlot ? tabStaticOptions : tabOptions;
			const vns = [h("div", {
				key: "ts",
				class: "vxe-tabs-slots"
			}, defaultSlot ? defaultSlot({ name: activeName }) : [])];
			if (tabPosition === "right" || tabPosition === "bottom") vns.push(rendetTabBody(tabList), renderTabHeader(tabList));
			else vns.push(renderTabHeader(tabList), rendetTabBody(tabList));
			return h("div", {
				ref: refElem,
				class: [
					"vxe-tabs",
					`pos--${tabPosition}`,
					`vxe-tabs--${tabType}`,
					`trigger--${trigger === "manual" ? "trigger" : "default"}`,
					{
						[`size--${vSize}`]: vSize,
						"is--padding": padding,
						"is--height": height
					}
				],
				style: wrapperStyle
			}, vns);
		};
		watch(() => props.position, () => {
			reactData.resizeFlag++;
		});
		watch(() => props.modelValue, (val) => {
			addInitName(val, null);
			reactData.activeName = val;
		});
		watch(() => reactData.activeName, (val) => {
			scrollToTab(val);
		});
		const optsFlag = ref(0);
		watch(() => props.options ? props.options.length : -1, () => {
			optsFlag.value++;
		});
		watch(() => props.options, () => {
			optsFlag.value++;
		});
		watch(optsFlag, () => {
			initDefaultName(props.options);
			reactData.resizeFlag++;
		});
		const stFlag = ref(0);
		watch(() => reactData.staticTabs ? reactData.staticTabs.length : -1, () => {
			stFlag.value++;
		});
		watch(() => reactData.staticTabs, () => {
			stFlag.value++;
		});
		watch(stFlag, () => {
			initDefaultName(reactData.staticTabs);
			reactData.resizeFlag++;
		});
		watch(computeParentTabsResizeFlag, () => {
			reactData.resizeFlag++;
		});
		watch(() => reactData.resizeFlag, () => {
			nextTick(() => {
				updateTabStyle();
			});
		});
		onMounted(() => {
			updateTabStyle();
			globalEvents.on($xeTabs, "resize", updateTabStyle);
		});
		onUnmounted(() => {
			globalEvents.off($xeTabs, "resize");
		});
		provide("$xeTabs", $xeTabs);
		addInitName(props.modelValue, null);
		initDefaultName(reactData.staticTabs.length ? reactData.staticTabs : props.options);
		$xeTabs.renderVN = renderVN;
		return $xeTabs;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/tabs/index.js
var VxeTabs = Object.assign({}, tabs_default, { install(app) {
	app.component(tabs_default.name, tabs_default);
} });
dynamicApp.use(VxeTabs);
VxeUI.component(tabs_default);
var Tabs = VxeTabs;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/tag/src/tag.js
var tag_default = defineVxeComponent({
	name: "VxeTag",
	props: {
		border: {
			type: Boolean,
			default: () => getConfig().tag.border
		},
		visible: {
			type: Boolean,
			default: null
		},
		status: String,
		title: [String, Number],
		icon: String,
		closeIcon: {
			type: String,
			default: () => getConfig().tag.closeIcon
		},
		content: [String, Number],
		round: Boolean,
		closable: {
			type: Boolean,
			default: () => getConfig().tag.closable
		},
		color: String,
		loading: Boolean,
		size: {
			type: String,
			default: () => getConfig().tag.size || getConfig().size
		}
	},
	emits: [
		"click",
		"dblclick",
		"close",
		"update:visible"
	],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const refElem = ref();
		const reactData = reactive({ showTag: props.visible !== false });
		const refMaps = { refElem };
		const computeMaps = {};
		const $xeTag = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $tag: $xeTag }, params));
		};
		const tagMethods = { dispatchEvent };
		const tagPrivateMethods = {};
		const updateVisible = () => {
			reactData.showTag = props.visible !== false;
		};
		const clickEvent = (evnt) => {
			const { loading } = props;
			if (!loading) dispatchEvent("click", {}, evnt);
		};
		const dblclickEvent = (evnt) => {
			const { loading } = props;
			if (!loading) dispatchEvent("dblclick", {}, evnt);
		};
		const closeEvent = (evnt) => {
			const { loading } = props;
			if (!loading) {
				const visible = !reactData.showTag;
				reactData.showTag = visible;
				emit("update:visible", visible);
				dispatchEvent("close", { visible }, evnt);
			}
		};
		Object.assign($xeTag, tagMethods, tagPrivateMethods);
		const renderContent = () => {
			const { icon, content, closable, closeIcon, loading } = props;
			const defaultSlot = slots.default;
			const iconSlot = slots.icon;
			const closeIconSlot = slots.closeIcon || slots["close-icon"];
			return [
				iconSlot || icon ? h("span", { class: "vxe-tag--icon" }, iconSlot ? iconSlot({}) : [h("i", { class: icon })]) : renderEmptyElement($xeTag),
				h("span", { class: "vxe-tag--content" }, defaultSlot ? defaultSlot({}) : import_xe_utils.default.toValueString(content)),
				loading || closable ? h("span", {
					class: loading ? "vxe-tag--loading" : "vxe-tag--close",
					onClick: closeEvent
				}, !loading && closeIconSlot ? closeIconSlot({}) : [h("i", { class: loading ? getIcon().TAG_LOADING : closeIcon || getIcon().TAG_CLOSE })]) : renderEmptyElement($xeTag)
			];
		};
		const renderVN = () => {
			const { status, color, title, round, border, loading } = props;
			const { showTag } = reactData;
			const vSize = computeSize.value;
			if (!showTag) return renderEmptyElement($xeTag);
			return h("span", {
				ref: refElem,
				class: ["vxe-tag", {
					[`size--${vSize}`]: vSize,
					[`theme--${status}`]: status && !color,
					[`color--${color}`]: color && !status,
					"is--round": round,
					"is--border": border,
					"is--loading": loading
				}],
				title,
				onClick: clickEvent,
				onDblclick: dblclickEvent
			}, renderContent());
		};
		watch(() => props.visible, () => {
			updateVisible();
		});
		updateVisible();
		$xeTag.renderVN = renderVN;
		return $xeTag;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/tag/index.js
var VxeTag = Object.assign({}, tag_default, { install(app) {
	app.component(tag_default.name, tag_default);
} });
dynamicApp.use(VxeTag);
VxeUI.component(tag_default);
var Tag = VxeTag;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/text-ellipsis/src/text-ellipsis.js
var text_ellipsis_default = defineVxeComponent({
	name: "VxeTextEllipsis",
	props: {
		href: String,
		target: String,
		content: [String, Number],
		lineClamp: [String, Number],
		status: String,
		title: [String, Number],
		loading: Boolean,
		offsetLength: [String, Number],
		routerLink: Object,
		underline: {
			type: Boolean,
			default: () => getConfig().textEllipsis.underline
		},
		/**
		* 权限码
		*/
		permissionCode: [String, Number],
		size: {
			type: String,
			default: () => getConfig().textEllipsis.size || getConfig().size
		}
	},
	emits: ["click"],
	setup(props, context) {
		const { emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const { computePermissionInfo } = usePermission(props);
		const refElem = ref();
		const realityElem = ref();
		const reactData = reactive({
			resizeObserver: null,
			visibleLen: 0
		});
		const refMaps = { refElem };
		const computeTextLineClamp = computed(() => {
			return import_xe_utils.default.toNumber(props.lineClamp);
		});
		const computeTextContent = computed(() => {
			return import_xe_utils.default.toValueString(props.content);
		});
		const computeTextOffsetLength = computed(() => {
			return props.offsetLength ? import_xe_utils.default.toNumber(props.offsetLength) : 0;
		});
		const computeVisibleContent = computed(() => {
			const { visibleLen } = reactData;
			const textLineClamp = computeTextLineClamp.value;
			const textContent = computeTextContent.value;
			const textOffsetLength = computeTextOffsetLength.value;
			if (textLineClamp > 1) {
				if (textContent.length > visibleLen) return `${textContent.slice(0, Math.max(1, visibleLen - 3 + textOffsetLength))}...`;
				return textContent;
			}
			return textContent;
		});
		const computeMaps = {};
		const $xeTextEllipsis = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $textEllipsis: $xeTextEllipsis }, params));
		};
		const calculateFont = (targetWidth) => {
			const el = refElem.value;
			const ryEl = realityElem.value;
			if (el && ryEl) {
				let fontSize = 12;
				try {
					fontSize = Math.max(10, import_xe_utils.default.toNumber(getComputedStyle(ryEl).fontSize));
				} catch (e) {}
				const textContent = computeTextContent.value;
				let currIndex = Math.floor(targetWidth / fontSize);
				let currStr = textContent.slice(0, currIndex);
				ryEl.textContent = currStr;
				reactData.visibleLen = currStr.length;
				let maxCount = 0;
				while (targetWidth > ryEl.clientWidth && maxCount < 30) {
					maxCount++;
					const offsetIndex = Math.floor((targetWidth - ryEl.clientWidth) / fontSize);
					if (offsetIndex) {
						currIndex += offsetIndex;
						currStr = textContent.slice(0, currIndex);
						ryEl.textContent = currStr;
						reactData.visibleLen = currStr.length;
					} else break;
				}
				ryEl.textContent = "";
				ryEl.style.display = "";
				ryEl.style.position = "";
				ryEl.style.top = "";
				ryEl.style.left = "";
			}
		};
		const updateStyle = () => {
			const el = refElem.value;
			const ryEl = realityElem.value;
			const textContent = computeTextContent.value;
			const textLineClamp = computeTextLineClamp.value;
			if (el && ryEl) {
				const cWidth = el.clientWidth;
				ryEl.style.display = "block";
				ryEl.style.position = "absolute";
				ryEl.style.top = "-3000px";
				ryEl.style.left = "-3000px";
				ryEl.textContent = textContent;
				const sWidth = ryEl.offsetWidth;
				const targetWidth = Math.floor(cWidth * textLineClamp);
				if (targetWidth > sWidth) reactData.visibleLen = textContent.length;
				else calculateFont(targetWidth);
			} else reactData.visibleLen = textContent.length;
		};
		const textEllipsisMethods = { dispatchEvent };
		const clickEvent = () => {
			emit("click", {});
		};
		const initObserver = () => {
			const { resizeObserver } = reactData;
			const textLineClamp = computeTextLineClamp.value;
			if (!resizeObserver) {
				const el = refElem.value;
				if (el && textLineClamp > 1) {
					if (window.ResizeObserver) {
						const observerObj = new window.ResizeObserver(import_xe_utils.default.throttle(() => {
							updateStyle();
						}, 300, {
							leading: true,
							trailing: true
						}));
						observerObj.observe(el);
						reactData.resizeObserver = observerObj;
					}
				}
			}
		};
		Object.assign($xeTextEllipsis, textEllipsisMethods, {});
		const renderContent = () => {
			const { routerLink, href, target, title } = props;
			const visibleContent = computeVisibleContent.value;
			if (routerLink) return h(resolveComponent("router-link"), {
				class: "vxe-text-ellipsis--link",
				title,
				target,
				to: routerLink
			}, { default() {
				return renderContent();
			} });
			if (href) return h("a", {
				class: "vxe-text-ellipsis--link",
				href,
				target,
				title
			}, visibleContent);
			return h("span", { class: "vxe-text-ellipsis--content" }, visibleContent);
		};
		const renderVN = () => {
			const { loading, status, title, underline } = props;
			const permissionInfo = computePermissionInfo.value;
			const vSize = computeSize.value;
			const textLineClamp = computeTextLineClamp.value;
			if (!permissionInfo.visible) return renderEmptyElement($xeTextEllipsis);
			return h("div", {
				ref: refElem,
				class: [
					"vxe-text-ellipsis",
					textLineClamp > 1 ? "is--multi" : "is--single",
					{
						[`size--${vSize}`]: vSize,
						[`theme--${status}`]: status,
						"is--underline": underline,
						"is--loading": loading
					}
				],
				title,
				onClick: clickEvent
			}, [h("span", {
				ref: realityElem,
				class: "vxe-text-ellipsis--reality"
			}), renderContent()]);
		};
		watch(() => props.content, () => {
			updateStyle();
		});
		watch(() => props.lineClamp, () => {
			initObserver();
			updateStyle();
		});
		onMounted(() => {
			initObserver();
			updateStyle();
		});
		onBeforeUnmount(() => {
			const { resizeObserver } = reactData;
			const el = refElem.value;
			const ryEl = realityElem.value;
			if (ryEl) ryEl.textContent = "";
			if (resizeObserver) {
				if (el) resizeObserver.unobserve(el);
				resizeObserver.disconnect();
				reactData.resizeObserver = null;
			}
		});
		$xeTextEllipsis.renderVN = renderVN;
		return $xeTextEllipsis;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/text-ellipsis/index.js
var VxeTextEllipsis = Object.assign({}, text_ellipsis_default, { install(app) {
	app.component(text_ellipsis_default.name, text_ellipsis_default);
} });
dynamicApp.use(VxeTextEllipsis);
VxeUI.component(text_ellipsis_default);
var TextEllipsis = VxeTextEllipsis;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/text/index.js
var VxeText = Object.assign({}, text_default, { install(app) {
	app.component(text_default.name, text_default);
} });
dynamicApp.use(VxeText);
VxeUI.component(text_default);
var Text = VxeText;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/textarea/src/textarea.js
var autoTxtElem;
var textarea_default = defineVxeComponent({
	name: "VxeTextarea",
	props: {
		modelValue: [String, Number],
		className: String,
		immediate: {
			type: Boolean,
			default: true
		},
		name: String,
		readonly: {
			type: Boolean,
			default: null
		},
		editable: {
			type: Boolean,
			default: true
		},
		disabled: {
			type: Boolean,
			default: null
		},
		placeholder: String,
		maxLength: [String, Number],
		trim: {
			type: Boolean,
			default: () => getConfig().textarea.trim
		},
		rows: {
			type: [String, Number],
			default: null
		},
		cols: {
			type: [String, Number],
			default: null
		},
		showWordCount: Boolean,
		countMethod: Function,
		autosize: [Boolean, Object],
		form: String,
		resize: {
			type: String,
			default: () => getConfig().textarea.resize
		},
		size: {
			type: String,
			default: () => getConfig().textarea.size || getConfig().size
		},
		maxlength: [String, Number]
	},
	emits: [
		"update:modelValue",
		"input",
		"keydown",
		"keyup",
		"click",
		"change",
		"focus",
		"blur",
		"lazy-change"
	],
	setup(props, context) {
		const { emit } = context;
		const $xeForm = inject("$xeForm", null);
		const formItemInfo = inject("xeFormItemInfo", null);
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const reactData = reactive({ inputValue: props.modelValue });
		const refElem = ref();
		const refTextarea = ref();
		const refMaps = {
			refElem,
			refTextarea
		};
		const $xeTextarea = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps
		};
		let textareaMethods = {};
		const computeFormReadonly = computed(() => {
			const { readonly } = props;
			if (readonly === null) {
				if ($xeForm) return $xeForm.props.readonly;
				return false;
			}
			return readonly;
		});
		const computeIsDisabled = computed(() => {
			const { disabled } = props;
			if (disabled === null) {
				if ($xeForm) return $xeForm.props.disabled;
				return false;
			}
			return disabled;
		});
		const computeInputReadonly = computed(() => {
			const { editable, readonly } = props;
			return readonly || !editable;
		});
		const computeInpPlaceholder = computed(() => {
			const { placeholder } = props;
			if (placeholder) return getFuncText(placeholder);
			const globalPlaceholder = getConfig().textarea.placeholder;
			if (globalPlaceholder) return getFuncText(globalPlaceholder);
			return getI18n("vxe.base.pleaseInput");
		});
		const computeInpMaxLength = computed(() => {
			const { maxLength, maxlength } = props;
			return maxLength || maxlength;
		});
		const computeInputCount = computed(() => {
			return import_xe_utils.default.getSize(reactData.inputValue);
		});
		const computeIsCountError = computed(() => {
			const inputCount = computeInputCount.value;
			const inpMaxLength = computeInpMaxLength.value;
			return inpMaxLength && inputCount > import_xe_utils.default.toNumber(inpMaxLength);
		});
		const computeSizeOpts = computed(() => {
			return Object.assign({
				minRows: 1,
				maxRows: 10
			}, getConfig().textarea.autosize, props.autosize);
		});
		const updateAutoTxt = () => {
			const { size, autosize } = props;
			const { inputValue } = reactData;
			if (autosize) {
				if (!autoTxtElem) autoTxtElem = document.createElement("div");
				if (!autoTxtElem.parentNode) document.body.appendChild(autoTxtElem);
				const textElem = refTextarea.value;
				if (!textElem) return;
				const textStyle = getComputedStyle(textElem);
				autoTxtElem.className = ["vxe-textarea--autosize", size ? `size--${size}` : ""].join(" ");
				autoTxtElem.style.width = `${textElem.clientWidth}px`;
				autoTxtElem.style.padding = textStyle.padding;
				autoTxtElem.innerText = ("" + (inputValue || "　")).replace(/\n$/, "\n　");
			}
		};
		const handleResize = () => {
			if (props.autosize) nextTick(() => {
				const { minRows, maxRows } = computeSizeOpts.value;
				const textElem = refTextarea.value;
				if (!textElem) return;
				const sizeHeight = autoTxtElem.clientHeight;
				const textStyle = getComputedStyle(textElem);
				const lineHeight = import_xe_utils.default.toNumber(textStyle.lineHeight);
				const paddingTop = import_xe_utils.default.toNumber(textStyle.paddingTop);
				const paddingBottom = import_xe_utils.default.toNumber(textStyle.paddingBottom);
				const borderTopWidth = import_xe_utils.default.toNumber(textStyle.borderTopWidth);
				const borderBottomWidth = import_xe_utils.default.toNumber(textStyle.borderBottomWidth);
				const intervalHeight = paddingTop + paddingBottom + borderTopWidth + borderBottomWidth;
				const rowNum = (sizeHeight - intervalHeight) / lineHeight;
				const textRows = rowNum && /[0-9]/.test("" + rowNum) ? rowNum : Math.floor(rowNum) + 1;
				let vaildRows = textRows;
				if (textRows < minRows) vaildRows = minRows;
				else if (textRows > maxRows) vaildRows = maxRows;
				textElem.style.height = `${vaildRows * lineHeight + intervalHeight}px`;
			});
		};
		const triggerEvent = (evnt) => {
			const value = reactData.inputValue;
			$xeTextarea.dispatchEvent(evnt.type, { value }, evnt);
		};
		const handleChange = (value, evnt) => {
			if (props.trim) value = `${value || ""}`.trim();
			reactData.inputValue = value;
			emit("update:modelValue", value);
			if (import_xe_utils.default.toValueString(props.modelValue) !== value) {
				textareaMethods.dispatchEvent("change", { value }, evnt);
				if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, value);
			}
		};
		const inputEvent = (evnt) => {
			const { immediate } = props;
			const value = evnt.target.value;
			reactData.inputValue = value;
			if (immediate) handleChange(value, evnt);
			$xeTextarea.dispatchEvent("input", { value }, evnt);
			handleResize();
		};
		const changeEvent = (evnt) => {
			const { immediate } = props;
			if (immediate) triggerEvent(evnt);
			else handleChange(reactData.inputValue, evnt);
			$xeTextarea.dispatchEvent("lazy-change", { value: reactData.inputValue }, evnt);
		};
		const blurEvent = (evnt) => {
			const { immediate } = props;
			const { inputValue } = reactData;
			if (!immediate) handleChange(inputValue, evnt);
			$xeTextarea.dispatchEvent("blur", { value: inputValue }, evnt);
		};
		textareaMethods = {
			dispatchEvent(type, params, evnt) {
				emit(type, createEvent(evnt, { $textarea: $xeTextarea }, params));
			},
			focus() {
				refTextarea.value.focus();
				return nextTick();
			},
			blur() {
				refTextarea.value.blur();
				return nextTick();
			}
		};
		Object.assign($xeTextarea, textareaMethods);
		watch(() => props.modelValue, (val) => {
			reactData.inputValue = val;
			updateAutoTxt();
		});
		watch(computeSizeOpts, () => {
			updateAutoTxt();
			handleResize();
		});
		nextTick(() => {
			const { autosize } = props;
			if (autosize) {
				updateAutoTxt();
				handleResize();
			}
		});
		const renderVN = () => {
			const { className, resize, autosize, showWordCount, countMethod, rows, cols } = props;
			const { inputValue } = reactData;
			const vSize = computeSize.value;
			const isDisabled = computeIsDisabled.value;
			const isCountError = computeIsCountError.value;
			const inputCount = computeInputCount.value;
			const inputReadonly = computeInputReadonly.value;
			const formReadonly = computeFormReadonly.value;
			const inpPlaceholder = computeInpPlaceholder.value;
			const inpMaxLength = computeInpMaxLength.value;
			if (formReadonly) return h("div", {
				ref: refElem,
				class: ["vxe-textarea--readonly", className]
			}, inputValue);
			return h("div", {
				ref: refElem,
				class: [
					"vxe-textarea",
					className,
					{
						[`size--${vSize}`]: vSize,
						"is--autosize": autosize,
						"is--count": showWordCount,
						"is--disabled": isDisabled,
						"is--rows": !import_xe_utils.default.eqNull(rows),
						"is--cols": !import_xe_utils.default.eqNull(cols)
					}
				],
				spellcheck: false
			}, [h("textarea", {
				ref: refTextarea,
				class: "vxe-textarea--inner",
				value: inputValue,
				name: props.name,
				placeholder: inpPlaceholder,
				maxlength: inpMaxLength,
				readonly: inputReadonly,
				disabled: isDisabled,
				rows,
				cols,
				style: resize ? { resize } : null,
				onInput: inputEvent,
				onChange: changeEvent,
				onKeydown: triggerEvent,
				onKeyup: triggerEvent,
				onClick: triggerEvent,
				onFocus: triggerEvent,
				onBlur: blurEvent
			}), showWordCount ? h("span", { class: ["vxe-textarea--count", { "is--error": isCountError }] }, countMethod ? `${countMethod({ value: inputValue })}` : `${inputCount}${inpMaxLength ? `/${inpMaxLength}` : ""}`) : null]);
		};
		$xeTextarea.renderVN = renderVN;
		return $xeTextarea;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/textarea/index.js
var VxeTextarea = Object.assign(textarea_default, { install: function(app) {
	app.component(textarea_default.name, textarea_default);
} });
dynamicApp.use(VxeTextarea);
VxeUI.component(textarea_default);
var Textarea = VxeTextarea;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/timeline/src/timeline.js
var timeline_default = defineVxeComponent({
	name: "VxeTimeline",
	props: { size: {
		type: String,
		default: () => getConfig().timeline.size || getConfig().size
	} },
	emits: [],
	setup(props, context) {
		const { emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeMaps = {};
		const $xeTimeline = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $timeline: $xeTimeline }, params));
		};
		Object.assign($xeTimeline, { dispatchEvent }, {});
		const renderVN = () => {
			return h("div", {
				ref: refElem,
				class: "vxe-timeline"
			});
		};
		$xeTimeline.renderVN = renderVN;
		return $xeTimeline;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/timeline/index.js
var VxeTimeline = Object.assign({}, timeline_default, { install(app) {
	app.component(timeline_default.name, timeline_default);
} });
dynamicApp.use(VxeTimeline);
VxeUI.component(timeline_default);
var Timeline = VxeTimeline;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/timeline/src/timeline-item.js
var timeline_item_default = defineVxeComponent({
	name: "VxeTimelineItem",
	props: {},
	emits: [],
	setup(props, context) {
		const { emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeMaps = {};
		const $xeTimelineItem = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $timelineItem: $xeTimelineItem }, params));
		};
		Object.assign($xeTimelineItem, { dispatchEvent }, {});
		const renderVN = () => {
			return h("div", {
				ref: refElem,
				class: "vxe-timeline-item"
			});
		};
		$xeTimelineItem.renderVN = renderVN;
		return $xeTimelineItem;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/timeline-item/index.js
var VxeTimelineItem = Object.assign({}, timeline_item_default, { install(app) {
	app.component(timeline_item_default.name, timeline_item_default);
} });
dynamicApp.use(VxeTimelineItem);
VxeUI.component(timeline_item_default);
var TimelineItem = VxeTimelineItem;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/tip/src/tip.js
var tip_default = defineVxeComponent({
	name: "VxeTip",
	props: {
		title: {
			type: [String, Number],
			default: () => getConfig().tip.title
		},
		content: [String, Number],
		status: String,
		icon: {
			type: String,
			default: () => getConfig().tip.icon
		},
		/**
		* 权限码
		*/
		permissionCode: [String, Number],
		size: {
			type: String,
			default: () => getConfig().tip.size || getConfig().size
		}
	},
	emits: [],
	setup(props, context) {
		const { slots, emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const { computePermissionInfo } = usePermission(props);
		const refElem = ref();
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeMaps = {};
		const $xeTip = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $tip: $xeTip }, params));
		};
		Object.assign($xeTip, { dispatchEvent }, {});
		const renderVN = () => {
			const { status, content, icon, title } = props;
			const defaultSlot = slots.default;
			const titleSlot = slots.title;
			const iconSlot = slots.icon;
			const permissionInfo = computePermissionInfo.value;
			const vSize = computeSize.value;
			if (!permissionInfo.visible) return renderEmptyElement($xeTip);
			return h("div", {
				ref: refElem,
				class: ["vxe-tip", {
					[`size--${vSize}`]: vSize,
					[`theme--${status}`]: status,
					"has--title": !!(titleSlot || title)
				}]
			}, [iconSlot || icon ? h("div", { class: "vxe-tip--icon" }, iconSlot ? getSlotVNs$1(iconSlot({})) : [h("i", { class: icon })]) : renderEmptyElement($xeTip), h("div", { class: "vxe-tip--body" }, [titleSlot || title ? h("div", { class: "vxe-tip--title" }, titleSlot ? getSlotVNs$1(titleSlot({})) : import_xe_utils.default.toValueString(title)) : renderEmptyElement($xeTip), h("div", { class: "vxe-tip--content" }, defaultSlot ? getSlotVNs$1(defaultSlot({})) : import_xe_utils.default.toValueString(content))])]);
		};
		$xeTip.renderVN = renderVN;
		return $xeTip;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/tip/index.js
var VxeTip = Object.assign({}, tip_default, { install(app) {
	app.component(tip_default.name, tip_default);
	app.component("VxeTipsComponent", tip_default);
} });
dynamicApp.use(VxeTip);
VxeUI.component(tip_default);
var Tips = VxeTip;
var Tip = VxeTip;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/tour/src/tour.js
var tour_default = defineVxeComponent({
	name: "VxeTour",
	props: { size: {
		type: String,
		default: () => getConfig().tour.size || getConfig().size
	} },
	emits: [],
	setup(props, context) {
		const { emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const reactData = reactive({});
		const refMaps = { refElem };
		const computeMaps = {};
		const $xeTour = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $tour: $xeTour }, params));
		};
		Object.assign($xeTour, { dispatchEvent }, {});
		const renderVN = () => {
			return h("div", {
				ref: refElem,
				class: "vxe-tour"
			});
		};
		$xeTour.renderVN = renderVN;
		return $xeTour;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/tour/index.js
var VxeTour = Object.assign({}, tour_default, { install(app) {
	app.component(tour_default.name, tour_default);
} });
dynamicApp.use(VxeTour);
VxeUI.component(tour_default);
var Tour = VxeTour;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/tree/src/store.js
var crossTreeDragNodeInfo = reactive({ node: null });
function getCrossTreeDragNodeInfo() {
	return crossTreeDragNodeInfo;
}
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/ui/src/anime.js
var rowMoveCls = "row--drag-move";
/**
* 上下拖拽
*/
function moveRowAnimateToTb(elemList, offsetTop) {
	import_xe_utils.default.arrayEach(elemList, (trEl) => {
		trEl.style.transform = `translateY(${offsetTop}px)`;
	});
	requestAnimationFrame(() => {
		import_xe_utils.default.arrayEach(elemList, (trEl) => {
			addClass(trEl, rowMoveCls);
			trEl.style.transform = "";
		});
	});
}
function clearRowAnimate(elem, clss) {
	setTimeout(() => {
		if (elem) import_xe_utils.default.arrayEach(elem.querySelectorAll(clss.map((cls) => `${cls}.${rowMoveCls}`).join(",")), (elem) => removeClass(elem, rowMoveCls));
	}, 500);
}
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/tree/src/tree.js
var { menus: menus$1, getConfig: getConfig$1, getI18n: getI18n$1, getIcon: getIcon$1 } = VxeUI;
/**
* 生成节点的唯一主键
*/
function getNodeUniqueId() {
	return import_xe_utils.default.uniqueId("node_");
}
function createInternalData$1() {
	return {
		treeFullData: [],
		afterTreeList: [],
		afterVisibleList: [],
		nodeMaps: {},
		selectCheckboxMaps: {},
		indeterminateRowMaps: {},
		treeExpandedMaps: {},
		treeExpandLazyLoadedMaps: {},
		lastScrollLeft: 0,
		lastScrollTop: 0,
		scrollYStore: {
			startIndex: 0,
			endIndex: 0,
			visibleSize: 0,
			offsetSize: 0,
			rowHeight: 0
		},
		lastScrollTime: 0
	};
}
function createReactData$1() {
	return {
		parentHeight: 0,
		customHeight: 0,
		customMinHeight: 0,
		customMaxHeight: 0,
		currentNode: null,
		scrollYLoad: false,
		bodyHeight: 0,
		topSpaceHeight: 0,
		selectRadioKey: null,
		treeList: [],
		updateExpandedFlag: 1,
		updateCheckboxFlag: 1,
		dragNode: null,
		dragTipText: ""
	};
}
var tree_default = defineVxeComponent({
	name: "VxeTree",
	props: {
		data: Array,
		autoResize: {
			type: Boolean,
			default: () => getConfig$1().tree.autoResize
		},
		height: [String, Number],
		maxHeight: {
			type: [String, Number],
			default: () => getConfig$1().tree.maxHeight
		},
		minHeight: {
			type: [String, Number],
			default: () => getConfig$1().tree.minHeight
		},
		loading: Boolean,
		loadingConfig: Object,
		accordion: {
			type: Boolean,
			default: () => getConfig$1().tree.accordion
		},
		childrenField: {
			type: String,
			default: () => getConfig$1().tree.childrenField
		},
		valueField: {
			type: String,
			default: () => getConfig$1().tree.valueField
		},
		keyField: {
			type: String,
			default: () => getConfig$1().tree.keyField
		},
		parentField: {
			type: String,
			default: () => getConfig$1().tree.parentField
		},
		titleField: {
			type: String,
			default: () => getConfig$1().tree.titleField
		},
		hasChildField: {
			type: String,
			default: () => getConfig$1().tree.hasChildField
		},
		mapChildrenField: {
			type: String,
			default: () => getConfig$1().tree.mapChildrenField
		},
		transform: Boolean,
		isCurrent: Boolean,
		isHover: Boolean,
		expandAll: Boolean,
		expandNodeKeys: Array,
		showLine: {
			type: Boolean,
			default: () => getConfig$1().tree.showLine
		},
		trigger: String,
		indent: {
			type: Number,
			default: () => getConfig$1().tree.indent
		},
		showRadio: {
			type: Boolean,
			default: () => getConfig$1().tree.showRadio
		},
		checkNodeKey: {
			type: [String, Number],
			default: () => getConfig$1().tree.checkNodeKey
		},
		radioConfig: Object,
		showCheckbox: {
			type: Boolean,
			default: () => getConfig$1().tree.showCheckbox
		},
		checkNodeKeys: {
			type: Array,
			default: () => getConfig$1().tree.checkNodeKeys
		},
		checkboxConfig: Object,
		nodeConfig: Object,
		lazy: Boolean,
		toggleMethod: Function,
		loadMethod: Function,
		drag: {
			type: Boolean,
			default: () => getConfig$1().tree.drag
		},
		dragConfig: Object,
		menuConfig: Object,
		showIcon: {
			type: Boolean,
			default: true
		},
		iconOpen: {
			type: String,
			default: () => getConfig$1().tree.iconOpen
		},
		iconClose: {
			type: String,
			default: () => getConfig$1().tree.iconClose
		},
		iconLoaded: {
			type: String,
			default: () => getConfig$1().tree.iconLoaded
		},
		filterValue: [String, Number],
		filterConfig: Object,
		size: {
			type: String,
			default: () => getConfig$1().tree.size || getConfig$1().size
		},
		virtualYConfig: Object
	},
	emits: [
		"update:modelValue",
		"update:checkNodeKey",
		"update:checkNodeKeys",
		"node-click",
		"node-dblclick",
		"current-change",
		"radio-change",
		"checkbox-change",
		"load-success",
		"load-error",
		"scroll",
		"node-dragstart",
		"node-dragover",
		"node-dragend",
		"node-expand",
		"node-menu",
		"menu-click"
	],
	setup(props, context) {
		const { emit, slots } = context;
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const refElem = ref();
		const refHeaderWrapperElem = ref();
		const refFooterWrapperElem = ref();
		const refVirtualWrapper = ref();
		const refVirtualBody = ref();
		const refDragNodeLineElem = ref();
		const refDragTipElem = ref();
		const crossTreeDragNodeInfo = getCrossTreeDragNodeInfo();
		const internalData = createInternalData$1();
		const reactData = reactive(createReactData$1());
		const refMaps = { refElem };
		const computeTitleField = computed(() => {
			return props.titleField || "title";
		});
		const computeKeyField = computed(() => {
			return props.keyField || "id";
		});
		const computeValueField = computed(() => {
			const keyField = computeKeyField.value;
			return props.valueField || keyField;
		});
		const computeParentField = computed(() => {
			return props.parentField || "parentId";
		});
		const computeChildrenField = computed(() => {
			return props.childrenField || "children";
		});
		const computeMapChildrenField = computed(() => {
			return props.mapChildrenField || "mapChildren";
		});
		const computeHasChildField = computed(() => {
			return props.hasChildField || "hasChild";
		});
		const computeVirtualYOpts = computed(() => {
			return Object.assign({}, getConfig$1().tree.virtualYConfig, props.virtualYConfig);
		});
		const computeIsRowCurrent = computed(() => {
			const { isCurrent } = computeNodeOpts.value;
			if (import_xe_utils.default.isBoolean(isCurrent)) return isCurrent;
			return props.isCurrent;
		});
		const computeIsRowHover = computed(() => {
			const { isHover } = computeNodeOpts.value;
			if (import_xe_utils.default.isBoolean(isHover)) return isHover;
			return props.isHover;
		});
		const computeRadioOpts = computed(() => {
			return Object.assign({ showIcon: true }, getConfig$1().tree.radioConfig, props.radioConfig);
		});
		const computeCheckboxOpts = computed(() => {
			return Object.assign({ showIcon: true }, getConfig$1().tree.checkboxConfig, props.checkboxConfig);
		});
		const computeNodeOpts = computed(() => {
			return Object.assign({}, getConfig$1().tree.nodeConfig, props.nodeConfig);
		});
		const computeLoadingOpts = computed(() => {
			return Object.assign({}, getConfig$1().tree.loadingConfig, props.loadingConfig);
		});
		const computeDragOpts = computed(() => {
			return Object.assign({}, getConfig$1().tree.dragConfig, props.dragConfig);
		});
		const computeMenuOpts = computed(() => {
			return Object.assign({}, getConfig$1().tree.menuConfig, props.menuConfig);
		});
		const computeTreeStyle = computed(() => {
			const { indent } = props;
			const { customHeight, customMinHeight, customMaxHeight } = reactData;
			const stys = {};
			if (customHeight) stys.height = toCssUnit(customHeight);
			if (customMinHeight) stys.minHeight = toCssUnit(customMinHeight);
			if (customMaxHeight) stys.maxHeight = toCssUnit(customMaxHeight);
			if (indent) stys["--vxe-ui-tree-node-indent"] = toCssUnit(indent);
			return stys;
		});
		const computeFilterOpts = computed(() => {
			return Object.assign({}, getConfig$1().tree.filterConfig, props.filterConfig);
		});
		const computeMaps = {
			computeKeyField,
			computeParentField,
			computeChildrenField,
			computeMapChildrenField,
			computeRadioOpts,
			computeCheckboxOpts,
			computeNodeOpts,
			computeDragOpts
		};
		const $xeTree = {
			xID,
			props,
			context,
			internalData,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const getNodeId = (node) => {
			const valueField = computeValueField.value;
			return enNodeValue(import_xe_utils.default.get(node, valueField));
		};
		const isExpandByNode = (node) => {
			const { updateExpandedFlag } = reactData;
			const { treeExpandedMaps } = internalData;
			const nodeid = getNodeId(node);
			return !!(updateExpandedFlag && treeExpandedMaps[nodeid]);
		};
		const isCheckedByRadioNodeId = (nodeid) => {
			const { selectRadioKey } = reactData;
			return selectRadioKey === nodeid;
		};
		const isCheckedByRadioNode = (node) => {
			return isCheckedByRadioNodeId(getNodeId(node));
		};
		const isCheckedByCheckboxNodeId = (nodeid) => {
			const { updateCheckboxFlag } = reactData;
			const { selectCheckboxMaps } = internalData;
			return !!(updateCheckboxFlag && selectCheckboxMaps[nodeid]);
		};
		const isCheckedByCheckboxNode = (node) => {
			return isCheckedByCheckboxNodeId(getNodeId(node));
		};
		const isIndeterminateByCheckboxNodeid = (nodeid) => {
			const { updateCheckboxFlag } = reactData;
			const { indeterminateRowMaps } = internalData;
			return !!(updateCheckboxFlag && indeterminateRowMaps[nodeid]);
		};
		const isIndeterminateByCheckboxNode = (node) => {
			return isIndeterminateByCheckboxNodeid(getNodeId(node));
		};
		const emitCheckboxMode = (value) => {
			emit("update:checkNodeKeys", value);
		};
		const emitRadioMode = (value) => {
			emit("update:checkNodeKey", value);
		};
		const handleSetCheckboxByNodeId = (nodeKeys, checked) => {
			const { nodeMaps } = internalData;
			if (nodeKeys) {
				if (!import_xe_utils.default.isArray(nodeKeys)) nodeKeys = [nodeKeys];
				const nodeList = [];
				nodeKeys.forEach((nodeKey) => {
					const nodeItem = nodeMaps[enNodeValue(nodeKey)];
					if (nodeItem) nodeList.push(nodeItem.item);
				});
				handleCheckedCheckboxNode(nodeList, checked);
			}
			return nextTick();
		};
		const handleCheckedCheckboxNode = (nodeList, checked) => {
			const { transform } = props;
			const { selectCheckboxMaps } = internalData;
			const mapChildrenField = computeMapChildrenField.value;
			const childrenField = computeChildrenField.value;
			const { checkStrictly } = computeCheckboxOpts.value;
			const handleSelect = (node) => {
				const nodeid = getNodeId(node);
				if (checked) {
					if (!selectCheckboxMaps[nodeid]) selectCheckboxMaps[nodeid] = node;
				} else if (selectCheckboxMaps[nodeid]) delete selectCheckboxMaps[nodeid];
			};
			if (checkStrictly) nodeList.forEach(handleSelect);
			else import_xe_utils.default.eachTree(nodeList, handleSelect, { children: transform ? mapChildrenField : childrenField });
			reactData.updateCheckboxFlag++;
			updateCheckboxStatus();
		};
		const updateCheckboxChecked = (nodeKeys) => {
			internalData.selectCheckboxMaps = {};
			handleSetCheckboxByNodeId(nodeKeys, true);
		};
		const handleSetExpand = (nodeid, expanded, expandedMaps) => {
			if (expanded) {
				if (!expandedMaps[nodeid]) expandedMaps[nodeid] = true;
			} else if (expandedMaps[nodeid]) delete expandedMaps[nodeid];
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $tree: $xeTree }, params));
		};
		const getParentElem = () => {
			const el = refElem.value;
			return el ? el.parentElement : null;
		};
		const calcTreeHeight = (key) => {
			const { parentHeight } = reactData;
			const val = props[key];
			let num = 0;
			if (val) if (val === "100%" || val === "auto") num = parentHeight;
			else {
				if (isScale(val)) num = Math.floor((import_xe_utils.default.toInteger(val) || 1) / 100 * parentHeight);
				else num = import_xe_utils.default.toNumber(val);
				num = Math.max(40, num);
			}
			return num;
		};
		const updateHeight = () => {
			reactData.customHeight = calcTreeHeight("height");
			reactData.customMinHeight = calcTreeHeight("minHeight");
			reactData.customMaxHeight = calcTreeHeight("maxHeight");
			if (reactData.scrollYLoad && !(reactData.customHeight || reactData.customMinHeight)) reactData.customHeight = 300;
		};
		const createNode = (records) => {
			const valueField = computeValueField.value;
			return Promise.resolve(records.map((obj) => {
				const item = Object.assign({}, obj);
				let nodeid = getNodeId(item);
				if (!nodeid) {
					nodeid = getNodeUniqueId();
					import_xe_utils.default.set(item, valueField, nodeid);
				}
				return item;
			}));
		};
		const cacheNodeMap = () => {
			const { treeFullData } = internalData;
			const valueField = computeValueField.value;
			const childrenField = computeChildrenField.value;
			const keyMaps = {};
			import_xe_utils.default.eachTree(treeFullData, (item, index, items, path, parent, nodes) => {
				let nodeid = getNodeId(item);
				if (!nodeid) {
					nodeid = getNodeUniqueId();
					import_xe_utils.default.set(item, valueField, nodeid);
				}
				keyMaps[nodeid] = {
					item,
					index,
					$index: -1,
					_index: -1,
					items,
					parent,
					nodes,
					level: nodes.length - 1,
					treeIndex: index,
					lineCount: 0,
					treeLoaded: false
				};
			}, { children: childrenField });
			internalData.nodeMaps = keyMaps;
		};
		const updateAfterDataIndex = () => {
			const { transform } = props;
			const { afterTreeList, nodeMaps } = internalData;
			const childrenField = computeChildrenField.value;
			const mapChildrenField = computeMapChildrenField.value;
			let vtIndex = 0;
			import_xe_utils.default.eachTree(afterTreeList, (item, index, items) => {
				const nodeid = getNodeId(item);
				const nodeItem = nodeMaps[nodeid];
				if (nodeItem) {
					nodeItem.items = items;
					nodeItem.treeIndex = index;
					nodeItem._index = vtIndex;
				} else nodeMaps[nodeid] = {
					item,
					index,
					$index: -1,
					_index: vtIndex,
					items,
					parent,
					nodes: [],
					level: 0,
					treeIndex: index,
					lineCount: 0,
					treeLoaded: false
				};
				vtIndex++;
			}, { children: transform ? mapChildrenField : childrenField });
		};
		const updateAfterFullData = () => {
			const { transform, filterValue } = props;
			const { treeFullData, lastFilterValue } = internalData;
			const titleField = computeTitleField.value;
			const childrenField = computeChildrenField.value;
			const mapChildrenField = computeMapChildrenField.value;
			const { autoExpandAll, beforeFilterMethod, filterMethod, afterFilterMethod } = computeFilterOpts.value;
			let fullList = treeFullData;
			let treeList = fullList;
			let filterStr = "";
			if (filterValue || filterValue === 0) {
				filterStr = `${filterValue}`;
				const handleSearch = filterMethod ? (item) => {
					return filterMethod({
						$tree: $xeTree,
						node: item,
						filterValue: filterStr
					});
				} : (item) => {
					return String(item[titleField]).toLowerCase().indexOf(filterStr.toLowerCase()) > -1;
				};
				const bafParams = {
					$tree: $xeTree,
					filterValue: filterStr
				};
				if (beforeFilterMethod) beforeFilterMethod(bafParams);
				if (transform) {
					treeList = import_xe_utils.default.searchTree(treeFullData, handleSearch, {
						original: true,
						isEvery: true,
						children: childrenField,
						mapChildren: mapChildrenField
					});
					fullList = treeList;
				} else fullList = treeFullData.filter(handleSearch);
				internalData.lastFilterValue = filterStr;
				nextTick(() => {
					if (autoExpandAll) $xeTree.setAllExpandNode(true).then(() => {
						if (afterFilterMethod) afterFilterMethod(bafParams);
					});
					else if (afterFilterMethod) afterFilterMethod(bafParams);
				});
			} else {
				if (transform) {
					treeList = import_xe_utils.default.searchTree(treeFullData, () => true, {
						original: true,
						isEvery: true,
						children: childrenField,
						mapChildren: mapChildrenField
					});
					fullList = treeList;
					if (lastFilterValue) {
						const bafParams = {
							$tree: $xeTree,
							filterValue: filterStr
						};
						if (beforeFilterMethod) beforeFilterMethod(bafParams);
						nextTick(() => {
							if (autoExpandAll) $xeTree.clearAllExpandNode().then(() => {
								if (afterFilterMethod) afterFilterMethod(bafParams);
							});
							else if (afterFilterMethod) afterFilterMethod(bafParams);
						});
					}
				}
				internalData.lastFilterValue = "";
			}
			internalData.afterVisibleList = fullList;
			internalData.afterTreeList = treeList;
			updateAfterDataIndex();
		};
		/**
		* 如果为虚拟树、则将树结构拍平
		*/
		const handleTreeToList = () => {
			const { transform } = props;
			const { afterTreeList, treeExpandedMaps } = internalData;
			const mapChildrenField = computeMapChildrenField.value;
			const expandMaps = {};
			if (transform) {
				const fullData = [];
				import_xe_utils.default.eachTree(afterTreeList, (item, index, items, path, parentRow) => {
					const nodeid = getNodeId(item);
					const parentNodeid = getNodeId(parentRow);
					if (!parentRow || expandMaps[parentNodeid] && treeExpandedMaps[parentNodeid]) {
						expandMaps[nodeid] = 1;
						fullData.push(item);
					}
				}, { children: mapChildrenField });
				updateScrollYStatus(fullData);
				internalData.afterVisibleList = fullData;
				return fullData;
			}
			return internalData.afterVisibleList;
		};
		const handleData = (force) => {
			const { scrollYLoad } = reactData;
			const { scrollYStore, nodeMaps } = internalData;
			let fullList = internalData.afterVisibleList;
			if (force) {
				updateAfterFullData();
				fullList = handleTreeToList();
			}
			const treeList = scrollYLoad ? fullList.slice(scrollYStore.startIndex, scrollYStore.endIndex) : fullList.slice(0);
			treeList.forEach((item, $index) => {
				const itemRest = nodeMaps[getNodeId(item)];
				if (itemRest) itemRest.$index = $index;
			});
			reactData.treeList = treeList;
		};
		const triggerSearchEvent = import_xe_utils.default.debounce(() => handleData(true), 350, { trailing: true });
		const loadData = (list) => {
			const { expandAll, expandNodeKeys, transform } = props;
			const { initialized, scrollYStore } = internalData;
			const keyField = computeKeyField.value;
			const parentField = computeParentField.value;
			const childrenField = computeChildrenField.value;
			const fullData = transform ? import_xe_utils.default.toArrayTree(list, {
				key: keyField,
				parentKey: parentField,
				mapChildren: childrenField
			}) : list ? list.slice(0) : [];
			internalData.treeFullData = fullData;
			Object.assign(scrollYStore, {
				startIndex: 0,
				endIndex: 1,
				visibleSize: 0
			});
			const sYLoad = updateScrollYStatus(fullData);
			cacheNodeMap();
			handleData(true);
			if (sYLoad) {
				if (!(props.height || props.maxHeight)) errLog("vxe.error.reqProp", ["[tree] height | max-height | virtual-y-config.enabled=false"]);
			}
			return computeScrollLoad().then(() => {
				if (!initialized) {
					if (list && list.length) {
						internalData.initialized = true;
						if (expandAll) $xeTree.setAllExpandNode(true);
						else if (expandNodeKeys && expandNodeKeys.length) $xeTree.setExpandByNodeId(expandNodeKeys, true);
						handleSetCheckboxByNodeId(props.checkNodeKeys || [], true);
					}
				}
				updateHeight();
				refreshScroll();
			});
		};
		const updateScrollYStatus = (fullData) => {
			const { transform } = props;
			const virtualYOpts = computeVirtualYOpts.value;
			const allList = fullData || internalData.treeFullData;
			const scrollYLoad = !!transform && !!virtualYOpts.enabled && virtualYOpts.gt > -1 && (virtualYOpts.gt === 0 || virtualYOpts.gt < allList.length);
			reactData.scrollYLoad = scrollYLoad;
			return scrollYLoad;
		};
		const updateYSpace = () => {
			const { scrollYLoad } = reactData;
			const { scrollYStore, afterVisibleList } = internalData;
			reactData.bodyHeight = scrollYLoad ? afterVisibleList.length * scrollYStore.rowHeight : 0;
			reactData.topSpaceHeight = scrollYLoad ? Math.max(scrollYStore.startIndex * scrollYStore.rowHeight, 0) : 0;
		};
		const updateYData = () => {
			handleData();
			updateYSpace();
		};
		const computeScrollLoad = () => {
			return nextTick().then(() => {
				const { scrollYLoad } = reactData;
				const { scrollYStore } = internalData;
				const virtualBodyElem = refVirtualBody.value;
				const virtualYOpts = computeVirtualYOpts.value;
				let rowHeight = 0;
				let firstItemElem;
				if (virtualBodyElem) {
					if (!firstItemElem) firstItemElem = virtualBodyElem.children[0];
				}
				if (firstItemElem) rowHeight = firstItemElem.offsetHeight;
				rowHeight = Math.max(20, rowHeight);
				scrollYStore.rowHeight = rowHeight;
				if (scrollYLoad) {
					const scrollBodyElem = refVirtualWrapper.value;
					const visibleYSize = Math.max(8, scrollBodyElem ? Math.ceil(scrollBodyElem.clientHeight / rowHeight) : 0);
					const offsetYSize = Math.max(0, Math.min(2, import_xe_utils.default.toNumber(virtualYOpts.oSize)));
					scrollYStore.offsetSize = offsetYSize;
					scrollYStore.visibleSize = visibleYSize;
					scrollYStore.endIndex = Math.max(scrollYStore.startIndex, visibleYSize + offsetYSize, scrollYStore.endIndex);
					updateYData();
				} else updateYSpace();
			});
		};
		/**
		* 如果有滚动条，则滚动到对应的位置
		*/
		const handleScrollTo = (scrollLeft, scrollTop) => {
			const scrollBodyElem = refVirtualWrapper.value;
			if (scrollLeft) {
				if (!import_xe_utils.default.isNumber(scrollLeft)) {
					scrollTop = scrollLeft.top;
					scrollLeft = scrollLeft.left;
				}
			}
			if (scrollBodyElem) {
				if (import_xe_utils.default.isNumber(scrollLeft)) scrollBodyElem.scrollLeft = scrollLeft;
				if (import_xe_utils.default.isNumber(scrollTop)) scrollBodyElem.scrollTop = scrollTop;
			}
			if (reactData.scrollYLoad) return new Promise((resolve) => {
				setTimeout(() => {
					nextTick(() => {
						resolve();
					});
				}, 50);
			});
			return nextTick();
		};
		/**
		* 刷新滚动条
		*/
		const refreshScroll = () => {
			const { lastScrollLeft, lastScrollTop } = internalData;
			return clearScroll().then(() => {
				if (lastScrollLeft || lastScrollTop) {
					internalData.lastScrollLeft = 0;
					internalData.lastScrollTop = 0;
					return scrollTo(lastScrollLeft, lastScrollTop);
				}
			});
		};
		/**
		* 重新计算列表
		*/
		const recalculate = () => {
			const { scrollYStore } = internalData;
			const { rowHeight } = scrollYStore;
			const el = refElem.value;
			if (el && el.clientWidth && el.clientHeight) {
				const parentEl = getParentElem();
				const headerWrapperEl = refHeaderWrapperElem.value;
				const footerWrapperEl = refFooterWrapperElem.value;
				const headHeight = headerWrapperEl ? headerWrapperEl.clientHeight : 0;
				const footHeight = footerWrapperEl ? footerWrapperEl.clientHeight : 0;
				if (parentEl) {
					const parentPaddingSize = getPaddingTopBottomSize(parentEl);
					reactData.parentHeight = Math.max(headHeight + footHeight + rowHeight, parentEl.clientHeight - parentPaddingSize - headHeight - footHeight);
				}
				updateHeight();
				return computeScrollLoad().then(() => {
					updateHeight();
					updateYSpace();
				});
			}
			return nextTick();
		};
		const loadYData = () => {
			const { scrollYStore } = internalData;
			const { startIndex, endIndex, visibleSize, offsetSize, rowHeight } = scrollYStore;
			const scrollBodyElem = refVirtualWrapper.value;
			if (!scrollBodyElem) return;
			const scrollTop = scrollBodyElem.scrollTop;
			const toVisibleIndex = Math.floor(scrollTop / rowHeight);
			const offsetStartIndex = Math.max(0, toVisibleIndex - 1 - offsetSize);
			const offsetEndIndex = toVisibleIndex + visibleSize + offsetSize;
			if (toVisibleIndex <= startIndex || toVisibleIndex >= endIndex - visibleSize - 1) {
				if (startIndex !== offsetStartIndex || endIndex !== offsetEndIndex) {
					scrollYStore.startIndex = offsetStartIndex;
					scrollYStore.endIndex = offsetEndIndex;
					updateYData();
				}
			}
		};
		const scrollEvent = (evnt) => {
			const scrollBodyElem = evnt.target;
			const scrollTop = scrollBodyElem.scrollTop;
			const scrollLeft = scrollBodyElem.scrollLeft;
			const isX = scrollLeft !== internalData.lastScrollLeft;
			const isY = scrollTop !== internalData.lastScrollTop;
			internalData.lastScrollTop = scrollTop;
			internalData.lastScrollLeft = scrollLeft;
			if (reactData.scrollYLoad) loadYData();
			internalData.lastScrollTime = Date.now();
			dispatchEvent("scroll", {
				scrollLeft,
				scrollTop,
				isX,
				isY
			}, evnt);
		};
		const clearScroll = () => {
			const scrollBodyElem = refVirtualWrapper.value;
			if (scrollBodyElem) {
				scrollBodyElem.scrollTop = 0;
				scrollBodyElem.scrollLeft = 0;
			}
			internalData.lastScrollTop = 0;
			internalData.lastScrollLeft = 0;
			return nextTick();
		};
		const handleNodeMousedownEvent = (evnt, node) => {
			const { drag } = props;
			const { nodeMaps } = internalData;
			const targetEl = evnt.currentTarget;
			const { trigger, isCrossDrag, isPeerDrag, disabledMethod } = computeDragOpts.value;
			const nodeid = getNodeId(node);
			const triggerTreeNode = getEventTargetNode(evnt, targetEl, "vxe-tree--node-item-switcher").flag;
			let isNodeDrag = false;
			if (drag) isNodeDrag = trigger === "node";
			if (!triggerTreeNode) {
				const params = {
					node,
					$tree: $xeTree
				};
				const itemRest = nodeMaps[nodeid];
				if (isNodeDrag && (isCrossDrag || isPeerDrag || itemRest && !itemRest.level) && !(disabledMethod && disabledMethod(params))) handleNodeDragMousedownEvent(evnt, { node });
			}
		};
		const handleNodeClickEvent = (evnt, node) => {
			const { showRadio, showCheckbox, trigger } = props;
			const radioOpts = computeRadioOpts.value;
			const checkboxOpts = computeCheckboxOpts.value;
			const isRowCurrent = computeIsRowCurrent.value;
			let triggerCurrent = false;
			let triggerRadio = false;
			let triggerCheckbox = false;
			let triggerExpand = false;
			if (isRowCurrent) {
				triggerCurrent = true;
				changeCurrentEvent(evnt, node);
			} else if (reactData.currentNode) reactData.currentNode = null;
			if (trigger === "node") {
				triggerExpand = true;
				toggleExpandEvent(evnt, node);
			}
			if (showRadio && radioOpts.trigger === "node") {
				triggerRadio = true;
				changeRadioEvent(evnt, node);
			}
			if (showCheckbox && checkboxOpts.trigger === "node") {
				triggerCheckbox = true;
				changeCheckboxEvent(evnt, node);
			}
			dispatchEvent("node-click", {
				node,
				triggerCurrent,
				triggerRadio,
				triggerCheckbox,
				triggerExpand
			}, evnt);
		};
		const handleNodeDblclickEvent = (evnt, node) => {
			dispatchEvent("node-dblclick", { node }, evnt);
		};
		const handleContextmenuEvent = (evnt, node) => {
			const { menuConfig } = props;
			const isRowCurrent = computeIsRowCurrent.value;
			const menuOpts = computeMenuOpts.value;
			if (menuConfig ? isEnableConf(menuOpts) : menuOpts.enabled) {
				const { options, visibleMethod } = menuOpts;
				if (!visibleMethod || visibleMethod({
					$tree: $xeTree,
					options,
					node
				})) {
					if (isRowCurrent) changeCurrentEvent(evnt, node);
					else if (reactData.currentNode) reactData.currentNode = null;
					if (VxeUI.contextMenu) VxeUI.contextMenu.openByEvent(evnt, {
						options,
						events: { optionClick(eventParams) {
							const { option } = eventParams;
							const gMenuOpts = menus$1.get(option.code);
							const tmMethod = gMenuOpts ? gMenuOpts.treeMenuMethod : null;
							const params = {
								menu: option,
								node,
								$event: evnt,
								$tree: $xeTree,
								/**
								* @@deprecated
								*/
								option
							};
							if (tmMethod) tmMethod(params, evnt);
							dispatchEvent("menu-click", params, eventParams.$event);
						} }
					});
				}
			}
			dispatchEvent("node-menu", { node }, evnt);
		};
		const handleAsyncTreeExpandChilds = (node) => {
			const checkboxOpts = computeCheckboxOpts.value;
			const { loadMethod } = props;
			const { checkStrictly } = checkboxOpts;
			return new Promise((resolve) => {
				if (loadMethod) {
					const { nodeMaps } = internalData;
					const nodeid = getNodeId(node);
					const nodeItem = nodeMaps[nodeid];
					internalData.treeExpandLazyLoadedMaps[nodeid] = true;
					Promise.resolve(loadMethod({
						$tree: $xeTree,
						node
					})).then((childRecords) => {
						const { treeExpandLazyLoadedMaps } = internalData;
						nodeItem.treeLoaded = true;
						if (treeExpandLazyLoadedMaps[nodeid]) treeExpandLazyLoadedMaps[nodeid] = false;
						if (!import_xe_utils.default.isArray(childRecords)) childRecords = [];
						if (childRecords) return $xeTree.loadChildrenNode(node, childRecords).then((childRows) => {
							const { treeExpandedMaps } = internalData;
							if (childRows.length && !treeExpandedMaps[nodeid]) treeExpandedMaps[nodeid] = true;
							reactData.updateExpandedFlag++;
							if (!checkStrictly && $xeTree.isCheckedByCheckboxNodeId(nodeid)) handleCheckedCheckboxNode(childRows, true);
							dispatchEvent("load-success", {
								node,
								data: childRecords
							}, new Event("load-success"));
							return nextTick();
						});
						else dispatchEvent("load-success", {
							node,
							data: childRecords
						}, new Event("load-success"));
					}).catch((e) => {
						const { treeExpandLazyLoadedMaps } = internalData;
						nodeItem.treeLoaded = false;
						if (treeExpandLazyLoadedMaps[nodeid]) treeExpandLazyLoadedMaps[nodeid] = false;
						dispatchEvent("load-error", {
							node,
							data: e
						}, new Event("load-error"));
					}).finally(() => {
						handleTreeToList();
						handleData();
						return recalculate();
					});
				} else resolve();
			});
		};
		/**
		* 展开与收起树节点
		* @param nodeList
		* @param expanded
		* @returns
		*/
		const handleBaseTreeExpand = (nodeList, expanded) => {
			const { lazy, accordion, toggleMethod } = props;
			const { treeExpandLazyLoadedMaps, treeExpandedMaps } = internalData;
			const { nodeMaps } = internalData;
			const childrenField = computeChildrenField.value;
			const hasChildField = computeHasChildField.value;
			const result = [];
			let validNodes = toggleMethod ? nodeList.filter((node) => toggleMethod({
				$tree: $xeTree,
				expanded,
				node
			})) : nodeList;
			if (accordion) {
				validNodes = validNodes.length ? [validNodes[validNodes.length - 1]] : [];
				const nodeItem = nodeMaps[getNodeId(validNodes[0])];
				if (nodeItem) nodeItem.items.forEach((item) => {
					const itemNodeId = getNodeId(item);
					if (treeExpandedMaps[itemNodeId]) delete treeExpandedMaps[itemNodeId];
				});
			}
			const expandNodes = [];
			if (expanded) validNodes.forEach((item) => {
				const itemNodeId = getNodeId(item);
				if (!treeExpandedMaps[itemNodeId]) {
					const nodeItem = nodeMaps[itemNodeId];
					if (lazy && item[hasChildField] && !nodeItem.treeLoaded && !treeExpandLazyLoadedMaps[itemNodeId]) result.push(handleAsyncTreeExpandChilds(item));
					else if (item[childrenField] && item[childrenField].length) {
						treeExpandedMaps[itemNodeId] = true;
						expandNodes.push(item);
					}
				}
			});
			else validNodes.forEach((item) => {
				const itemNodeId = getNodeId(item);
				if (treeExpandedMaps[itemNodeId]) {
					delete treeExpandedMaps[itemNodeId];
					expandNodes.push(item);
				}
			});
			reactData.updateExpandedFlag++;
			handleTreeToList();
			handleData();
			return Promise.all(result).then(() => recalculate());
		};
		const toggleExpandEvent = (evnt, node) => {
			const { lazy } = props;
			const { treeExpandedMaps, treeExpandLazyLoadedMaps } = internalData;
			const nodeid = getNodeId(node);
			const expanded = !treeExpandedMaps[nodeid];
			evnt.stopPropagation();
			if (!lazy || !treeExpandLazyLoadedMaps[nodeid]) handleBaseTreeExpand([node], expanded);
			dispatchEvent("node-expand", {
				node,
				expanded
			}, evnt);
		};
		const updateCheckboxStatus = () => {
			const { transform } = props;
			const { selectCheckboxMaps, indeterminateRowMaps, afterTreeList } = internalData;
			const childrenField = computeChildrenField.value;
			const mapChildrenField = computeMapChildrenField.value;
			const { checkStrictly, checkMethod } = computeCheckboxOpts.value;
			if (!checkStrictly) {
				const childRowMaps = {};
				const childRowList = [];
				import_xe_utils.default.eachTree(afterTreeList, (node) => {
					const nodeid = getNodeId(node);
					const childList = node[childrenField];
					if (childList && childList.length && !childRowMaps[nodeid]) {
						childRowMaps[nodeid] = 1;
						childRowList.unshift([
							node,
							nodeid,
							childList
						]);
					}
				}, { children: transform ? mapChildrenField : childrenField });
				childRowList.forEach((vals) => {
					const node = vals[0];
					const nodeid = vals[1];
					const childList = vals[2];
					let sLen = 0;
					let hLen = 0;
					let vLen = 0;
					const cLen = childList.length;
					childList.forEach(checkMethod ? (item) => {
						const childNodeid = getNodeId(item);
						const isSelect = selectCheckboxMaps[childNodeid];
						if (checkMethod({
							$tree: $xeTree,
							node: item
						})) {
							if (isSelect) sLen++;
							else if (indeterminateRowMaps[childNodeid]) hLen++;
							vLen++;
						} else if (isSelect) sLen++;
						else if (indeterminateRowMaps[childNodeid]) hLen++;
					} : (item) => {
						const childNodeid = getNodeId(item);
						if (selectCheckboxMaps[childNodeid]) sLen++;
						else if (indeterminateRowMaps[childNodeid]) hLen++;
						vLen++;
					});
					let isSelected = false;
					if (cLen > 0) if (vLen > 0) isSelected = (sLen > 0 || hLen > 0) && sLen >= vLen;
					else if (sLen > 0 && sLen >= vLen) isSelected = true;
					else if (selectCheckboxMaps[nodeid]) isSelected = true;
					else isSelected = false;
					else isSelected = selectCheckboxMaps[nodeid];
					const halfSelect = !isSelected && (sLen > 0 || hLen > 0);
					if (isSelected) {
						selectCheckboxMaps[nodeid] = node;
						if (indeterminateRowMaps[nodeid]) delete indeterminateRowMaps[nodeid];
					} else {
						if (selectCheckboxMaps[nodeid]) delete selectCheckboxMaps[nodeid];
						if (halfSelect) indeterminateRowMaps[nodeid] = node;
						else if (indeterminateRowMaps[nodeid]) delete indeterminateRowMaps[nodeid];
					}
				});
				reactData.updateCheckboxFlag++;
			}
		};
		const changeCheckboxEvent = (evnt, node) => {
			evnt.preventDefault();
			evnt.stopPropagation();
			const { transform } = props;
			const { selectCheckboxMaps } = internalData;
			const childrenField = computeChildrenField.value;
			const mapChildrenField = computeMapChildrenField.value;
			const { checkStrictly, checkMethod } = computeCheckboxOpts.value;
			let isDisabled = !!checkMethod;
			if (checkMethod) isDisabled = !checkMethod({
				$tree: $xeTree,
				node
			});
			if (isDisabled) return;
			const nodeid = getNodeId(node);
			let isChecked = false;
			if (selectCheckboxMaps[nodeid]) delete selectCheckboxMaps[nodeid];
			else {
				isChecked = true;
				selectCheckboxMaps[nodeid] = node;
			}
			if (!checkStrictly) import_xe_utils.default.eachTree(import_xe_utils.default.get(node, transform ? mapChildrenField : childrenField), (childNode) => {
				const childNodeid = getNodeId(childNode);
				if (isChecked) {
					if (!selectCheckboxMaps[childNodeid]) selectCheckboxMaps[childNodeid] = true;
				} else if (selectCheckboxMaps[childNodeid]) delete selectCheckboxMaps[childNodeid];
			}, { children: transform ? mapChildrenField : childrenField });
			reactData.updateCheckboxFlag++;
			updateCheckboxStatus();
			const value = import_xe_utils.default.keys(selectCheckboxMaps).map(deNodeValue);
			emitCheckboxMode(value);
			dispatchEvent("checkbox-change", {
				node,
				value,
				checked: isChecked
			}, evnt);
		};
		const changeCurrentEvent = (evnt, node) => {
			evnt.preventDefault();
			const { currentMethod, trigger } = computeNodeOpts.value;
			const childrenField = computeChildrenField.value;
			const childList = import_xe_utils.default.get(node, childrenField);
			const hasChild = childList && childList.length;
			let isDisabled = !!currentMethod;
			if (trigger === "child") {
				if (hasChild) return;
			} else if (trigger === "parent") {
				if (!hasChild) return;
			}
			if (currentMethod) isDisabled = !currentMethod({ node });
			if (isDisabled) return;
			const isChecked = true;
			reactData.currentNode = node;
			dispatchEvent("current-change", {
				node,
				checked: isChecked
			}, evnt);
		};
		const changeRadioEvent = (evnt, node) => {
			evnt.preventDefault();
			evnt.stopPropagation();
			const { checkMethod } = computeRadioOpts.value;
			let isDisabled = !!checkMethod;
			if (checkMethod) isDisabled = !checkMethod({
				$tree: $xeTree,
				node
			});
			if (isDisabled) return;
			const isChecked = true;
			const nodeid = getNodeId(node);
			const value = deNodeValue(nodeid);
			reactData.selectRadioKey = nodeid;
			emitRadioMode(value);
			dispatchEvent("radio-change", {
				node,
				value,
				checked: isChecked
			}, evnt);
		};
		const handleGlobalResizeEvent = () => {
			const el = refElem.value;
			if (!el || !el.clientWidth) return;
			recalculate();
		};
		const treeMethods = {
			dispatchEvent,
			getNodeId,
			getNodeById(nodeid) {
				const { nodeMaps } = internalData;
				if (nodeid) {
					const nodeItem = nodeMaps[nodeid];
					if (nodeItem) return nodeItem.item;
				}
				return null;
			},
			loadData(data) {
				return loadData(data || []);
			},
			reloadData(data) {
				return loadData(data || []);
			},
			clearCurrentNode() {
				reactData.currentNode = null;
				return nextTick();
			},
			getCurrentNodeId() {
				const { currentNode } = reactData;
				if (currentNode) return deNodeValue(getNodeId(currentNode));
				return null;
			},
			getCurrentNode() {
				const { currentNode } = reactData;
				const { nodeMaps } = internalData;
				if (currentNode) {
					const nodeItem = nodeMaps[getNodeId(currentNode)];
					if (nodeItem) return nodeItem.item;
				}
				return null;
			},
			setCurrentNodeId(nodeKey) {
				const { nodeMaps } = internalData;
				const nodeItem = nodeMaps[enNodeValue(nodeKey)];
				reactData.currentNode = nodeItem ? nodeItem.item : null;
				return nextTick();
			},
			setCurrentNode(node) {
				reactData.currentNode = node;
				return nextTick();
			},
			clearRadioNode() {
				reactData.selectRadioKey = null;
				emitRadioMode(null);
				return nextTick();
			},
			getRadioNodeId() {
				return reactData.selectRadioKey || null;
			},
			getRadioNode() {
				const { selectRadioKey } = reactData;
				const { nodeMaps } = internalData;
				if (selectRadioKey) {
					const nodeItem = nodeMaps[selectRadioKey];
					if (nodeItem) return nodeItem.item;
				}
				return null;
			},
			setRadioNodeId(nodeKey) {
				reactData.selectRadioKey = enNodeValue(nodeKey);
				emitRadioMode(nodeKey);
				return nextTick();
			},
			setRadioNode(node) {
				if (node) {
					const nodeid = getNodeId(node);
					reactData.selectRadioKey = nodeid;
					emitRadioMode(deNodeValue(nodeid));
				} else emitRadioMode(null);
				return nextTick();
			},
			setCheckboxNode(nodeList, checked) {
				if (nodeList) {
					if (!import_xe_utils.default.isArray(nodeList)) nodeList = [nodeList];
					handleCheckedCheckboxNode(nodeList, checked);
				}
				emitCheckboxMode($xeTree.getCheckboxNodeIds());
				return nextTick();
			},
			setCheckboxByNodeId(nodeKeys, selected) {
				handleSetCheckboxByNodeId(nodeKeys, selected);
				emitCheckboxMode($xeTree.getCheckboxNodeIds());
				return nextTick();
			},
			getCheckboxNodeIds() {
				const { selectCheckboxMaps } = internalData;
				const nodeKeys = [];
				import_xe_utils.default.each(selectCheckboxMaps, (item, nodeId) => {
					nodeKeys.push(deNodeValue(nodeId));
				});
				return nodeKeys;
			},
			getCheckboxNodes() {
				const { nodeMaps, selectCheckboxMaps } = internalData;
				const list = [];
				import_xe_utils.default.each(selectCheckboxMaps, (item, nodeid) => {
					const nodeItem = nodeMaps[nodeid];
					if (nodeItem) list.push(nodeItem.item);
				});
				return list;
			},
			clearCheckboxNode() {
				internalData.indeterminateRowMaps = {};
				internalData.selectCheckboxMaps = {};
				reactData.updateCheckboxFlag++;
				emitCheckboxMode([]);
				updateCheckboxStatus();
				return nextTick().then(() => {
					return {
						checkNodeKeys: [],
						checkNodes: []
					};
				});
			},
			setAllCheckboxNode(checked) {
				const { transform } = props;
				const selectMaps = {};
				const childrenField = computeChildrenField.value;
				const mapChildrenField = computeMapChildrenField.value;
				const checkKeys = [];
				const checkNodes = [];
				if (checked) import_xe_utils.default.eachTree(internalData.afterTreeList, (node) => {
					const nodeid = getNodeId(node);
					checkKeys.push(nodeid);
					checkNodes.push(node);
					selectMaps[nodeid] = true;
				}, { children: transform ? mapChildrenField : childrenField });
				internalData.indeterminateRowMaps = {};
				internalData.selectCheckboxMaps = selectMaps;
				reactData.updateCheckboxFlag++;
				updateCheckboxStatus();
				return nextTick().then(() => {
					return {
						checkNodeKeys: checkKeys,
						checkNodes
					};
				});
			},
			clearExpandNode() {
				return treeMethods.clearAllExpandNode();
			},
			clearAllExpandNode() {
				const { scrollYLoad } = reactData;
				const { nodeMaps, scrollYStore } = internalData;
				import_xe_utils.default.each(nodeMaps, (nodeItem) => {
					nodeItem.treeLoaded = false;
				});
				internalData.treeExpandedMaps = {};
				reactData.updateExpandedFlag++;
				reactData.topSpaceHeight = 0;
				scrollYStore.startIndex = 0;
				scrollYStore.endIndex = 1;
				handleTreeToList();
				handleData();
				return recalculate().then(() => {
					if (scrollYLoad) loadYData();
					return recalculate();
				});
			},
			setExpandByNodeId(nodeKeys, expanded) {
				const { treeExpandedMaps } = internalData;
				if (nodeKeys) {
					if (!import_xe_utils.default.isArray(nodeKeys)) nodeKeys = [nodeKeys];
					nodeKeys.forEach((nodeKey) => {
						handleSetExpand(enNodeValue(nodeKey), expanded, treeExpandedMaps);
					});
					reactData.updateExpandedFlag++;
				}
				handleTreeToList();
				handleData();
				return recalculate();
			},
			getExpandNodeIds() {
				const { treeExpandedMaps } = internalData;
				const nodeKeys = [];
				import_xe_utils.default.each(treeExpandedMaps, (item, nodeId) => {
					nodeKeys.push(deNodeValue(nodeId));
				});
				return nodeKeys;
			},
			getExpandNodes() {
				const { nodeMaps, treeExpandedMaps } = internalData;
				const list = [];
				import_xe_utils.default.each(treeExpandedMaps, (item, nodeid) => {
					const nodeItem = nodeMaps[nodeid];
					if (nodeItem) list.push(nodeItem.item);
				});
				return list;
			},
			setExpandNode(nodes, expanded) {
				const { treeExpandedMaps } = internalData;
				if (nodes) {
					if (!import_xe_utils.default.isArray(nodes)) nodes = [nodes];
					nodes.forEach((node) => {
						handleSetExpand(getNodeId(node), expanded, treeExpandedMaps);
					});
					reactData.updateExpandedFlag++;
				}
				handleTreeToList();
				handleData();
				return recalculate();
			},
			toggleExpandByNodeId(nodeKeys) {
				const { treeExpandedMaps } = internalData;
				if (nodeKeys) {
					if (!import_xe_utils.default.isArray(nodeKeys)) nodeKeys = [nodeKeys];
					nodeKeys.forEach((nodeKey) => {
						const nodeid = enNodeValue(nodeKey);
						handleSetExpand(nodeid, !treeExpandedMaps[`${nodeid}`], treeExpandedMaps);
					});
					reactData.updateExpandedFlag++;
				}
				handleTreeToList();
				handleData();
				return recalculate();
			},
			toggleExpandNode(nodes) {
				const { treeExpandedMaps } = internalData;
				if (nodes) {
					if (!import_xe_utils.default.isArray(nodes)) nodes = [nodes];
					nodes.forEach((node) => {
						const nodeid = getNodeId(node);
						handleSetExpand(nodeid, !treeExpandedMaps[nodeid], treeExpandedMaps);
					});
					reactData.updateExpandedFlag++;
				}
				handleTreeToList();
				handleData();
				return recalculate();
			},
			setAllExpandNode(expanded) {
				const { transform } = props;
				const { scrollYLoad } = reactData;
				const { scrollYStore, treeExpandedMaps } = internalData;
				const childrenField = computeChildrenField.value;
				const mapChildrenField = computeMapChildrenField.value;
				if (expanded) import_xe_utils.default.eachTree(internalData.afterTreeList, (node) => {
					const childList = import_xe_utils.default.get(node, childrenField);
					if (childList && childList.length) {
						const nodeid = getNodeId(node);
						treeExpandedMaps[nodeid] = true;
					}
				}, { children: transform ? mapChildrenField : childrenField });
				else internalData.treeExpandedMaps = {};
				reactData.updateExpandedFlag++;
				reactData.topSpaceHeight = 0;
				scrollYStore.startIndex = 0;
				scrollYStore.endIndex = 1;
				handleTreeToList();
				handleData();
				return recalculate().then(() => {
					if (scrollYLoad) loadYData();
					return recalculate();
				});
			},
			reloadExpandNode(node) {
				const { lazy } = props;
				if (lazy) {
					treeMethods.clearExpandLoaded(node);
					return handleAsyncTreeExpandChilds(node);
				}
				return recalculate();
			},
			clearExpandLoaded(node) {
				const { lazy } = props;
				const { nodeMaps } = internalData;
				if (lazy) {
					const nodeItem = nodeMaps[getNodeId(node)];
					if (nodeItem) nodeItem.treeLoaded = false;
				}
				return recalculate();
			},
			/**
			* 用于树结构，给行数据加载子节点
			*/
			loadChildrenNode(node, childRecords) {
				const { lazy, transform } = props;
				const { nodeMaps } = internalData;
				if (!lazy) return Promise.resolve([]);
				const childrenField = computeChildrenField.value;
				const mapChildrenField = computeMapChildrenField.value;
				const parentNodeItem = nodeMaps[getNodeId(node)];
				const parentLevel = parentNodeItem ? parentNodeItem.level : 0;
				const parentNodes = parentNodeItem ? parentNodeItem.nodes : [];
				return createNode(childRecords).then((nodeList) => {
					import_xe_utils.default.eachTree(nodeList, (childRow, index, items, path, parent, nodes) => {
						const itemNodeId = getNodeId(childRow);
						nodeMaps[itemNodeId] = {
							item: childRow,
							index: -1,
							$index: -1,
							_index: -1,
							items,
							parent: parent || parentNodeItem.item,
							nodes: parentNodes.concat(nodes),
							level: parentLevel + nodes.length,
							treeIndex: -1,
							lineCount: 0,
							treeLoaded: false
						};
					}, { children: childrenField });
					node[childrenField] = nodeList;
					if (transform) node[mapChildrenField] = nodeList;
					updateAfterDataIndex();
					return nodeList;
				});
			},
			isExpandByNode,
			isCheckedByRadioNodeId,
			isCheckedByRadioNode,
			isCheckedByCheckboxNodeId,
			isIndeterminateByCheckboxNode,
			isCheckedByCheckboxNode,
			getCheckboxIndeterminateNodes() {
				const { nodeMaps, indeterminateRowMaps } = internalData;
				const list = [];
				import_xe_utils.default.each(indeterminateRowMaps, (item, nodeid) => {
					const nodeItem = nodeMaps[nodeid];
					if (nodeItem) list.push(nodeItem.item);
				});
				return list;
			},
			recalculate,
			getFullData() {
				const { treeFullData } = internalData;
				return treeFullData.slice(0);
			},
			scrollTo: handleScrollTo,
			scrollToNode(node) {
				return $xeTree.scrollToNodeId(getNodeId(node));
			},
			scrollToNodeId(nodeid) {
				const { transform } = props;
				const { scrollYStore, afterTreeList } = internalData;
				const childrenField = computeChildrenField.value;
				const mapChildrenField = computeMapChildrenField.value;
				const scrollBodyElem = refVirtualWrapper.value;
				if (nodeid && scrollBodyElem) if (transform) {
					const matchObj = import_xe_utils.default.findTree(afterTreeList, (item) => getNodeId(item) === nodeid, { children: transform ? mapChildrenField : childrenField });
					if (matchObj) return $xeTree.setExpandNode(matchObj.nodes, true).then(() => {
						const itemIndex = import_xe_utils.default.findIndexOf(internalData.afterVisibleList, (item) => getNodeId(item) === nodeid);
						if (itemIndex > -1) {
							const targetTop = Math.max(0, (itemIndex - 1) * scrollYStore.rowHeight);
							return handleScrollTo(scrollBodyElem.scrollLeft, targetTop);
						}
					});
				} else {
					const itemEl = scrollBodyElem.querySelector(`.vxe-tree--node-wrapper[nodeid="${nodeid}"]`);
					if (itemEl) return handleScrollTo(scrollBodyElem.scrollLeft, itemEl.offsetTop);
				}
				return recalculate();
			},
			clearScroll
		};
		const clearNodeDropOrigin = () => {
			const el = refElem.value;
			if (el) {
				const clss = "node--drag-origin";
				import_xe_utils.default.arrayEach(el.querySelectorAll(`.${clss}`), (elem) => {
					elem.draggable = false;
					removeClass(elem, clss);
				});
			}
		};
		const updateNodeDropOrigin = (node) => {
			const el = refElem.value;
			if (el) {
				const clss = "node--drag-origin";
				const nodeid = getNodeId(node);
				import_xe_utils.default.arrayEach(el.querySelectorAll(`.vxe-tree--node-wrapper[nodeid="${nodeid}"]`), (elem) => {
					addClass(elem, clss);
				});
			}
		};
		const updateRowDropTipContent = (itemEl) => {
			const { dragNode } = reactData;
			const { tooltipMethod } = computeDragOpts.value;
			const rTooltipMethod = tooltipMethod;
			let tipContent = "";
			if (rTooltipMethod) tipContent = `${rTooltipMethod({
				$tree: $xeTree,
				node: dragNode
			}) || ""}`;
			else tipContent = getI18n$1("vxe.tree.dragTip", [itemEl.textContent || ""]);
			reactData.dragTipText = tipContent;
		};
		const hideDropTip = () => {
			const rdTipEl = refDragTipElem.value;
			const rdLineEl = refDragNodeLineElem.value;
			if (rdTipEl) rdTipEl.style.display = "";
			if (rdLineEl) rdLineEl.style.display = "";
		};
		const clearCrossTreeDragStatus = () => {
			crossTreeDragNodeInfo.node = null;
		};
		const clearDragStatus = () => {
			const { dragNode } = reactData;
			if (dragNode) {
				hideDropTip();
				clearNodeDropOrigin();
				clearCrossTreeDragStatus();
				reactData.dragNode = null;
			}
		};
		const handleNodeDragMousedownEvent = (evnt, params) => {
			evnt.stopPropagation();
			const { node } = params;
			const { trigger, dragStartMethod } = computeDragOpts.value;
			const dragEl = evnt.currentTarget;
			const itemEl = trigger === "node" ? dragEl : dragEl.parentElement.parentElement;
			clearNodeDropOrigin();
			if (dragStartMethod && !dragStartMethod(params)) {
				itemEl.draggable = false;
				reactData.dragNode = null;
				hideDropTip();
				return;
			}
			reactData.dragNode = node;
			itemEl.draggable = true;
			updateNodeDropOrigin(node);
			updateRowDropTipContent(itemEl);
			dispatchEvent("node-dragstart", params, evnt);
		};
		const handleNodeDragMouseupEvent = () => {
			clearDragStatus();
		};
		const showDropTip = (evnt, itemEl, showLine, dragPos) => {
			const wrapperEl = refElem.value;
			if (!wrapperEl) return;
			const { prevDragToChild } = internalData;
			const wrapperRect = wrapperEl.getBoundingClientRect();
			const wrapperHeight = wrapperEl.clientHeight;
			if (itemEl) {
				const rdLineEl = refDragNodeLineElem.value;
				if (rdLineEl) if (showLine) {
					const trRect = itemEl.getBoundingClientRect();
					let itemHeight = itemEl.clientHeight;
					const offsetTop = Math.max(1, trRect.y - wrapperRect.y);
					if (offsetTop + itemHeight > wrapperHeight) itemHeight = wrapperHeight - offsetTop;
					rdLineEl.style.display = "block";
					rdLineEl.style.top = `${offsetTop}px`;
					rdLineEl.style.height = `${itemHeight}px`;
					rdLineEl.setAttribute("drag-pos", dragPos);
					rdLineEl.setAttribute("drag-to-child", prevDragToChild ? "y" : "n");
				} else rdLineEl.style.display = "";
			}
			const rdTipEl = refDragTipElem.value;
			if (rdTipEl) {
				rdTipEl.style.display = "block";
				rdTipEl.style.top = `${Math.min(wrapperEl.clientHeight - wrapperEl.scrollTop - rdTipEl.clientHeight, evnt.clientY - wrapperRect.y)}px`;
				rdTipEl.style.left = `${Math.min(wrapperEl.clientWidth - wrapperEl.scrollLeft - rdTipEl.clientWidth - 1, evnt.clientX - wrapperRect.x)}px`;
				rdTipEl.setAttribute("drag-status", showLine ? prevDragToChild ? "sub" : "normal" : "disabled");
			}
		};
		const clearNodeDragData = () => {
			const wrapperEl = refElem.value;
			const dtClss = [".vxe-tree--node-wrapper"];
			hideDropTip();
			clearNodeDropOrigin();
			clearRowAnimate(wrapperEl, dtClss);
			internalData.prevDragToChild = false;
			reactData.dragNode = null;
		};
		const handleNodeDragSwapEvent = (evnt, dragNode, prevDragNode, prevDragPos, prevDragToChild) => {
			const { transform } = props;
			const { nodeMaps, treeFullData, afterVisibleList } = internalData;
			const dragConfig = computeDragOpts.value;
			const { animation, isCrossDrag, isPeerDrag, isSelfToChildDrag, dragEndMethod, dragToChildMethod } = dragConfig;
			const dEndMethod = dragEndMethod || (dragConfig ? dragConfig.dragEndMethod : null);
			const keyField = computeKeyField.value;
			const parentField = computeParentField.value;
			const childrenField = computeChildrenField.value;
			const mapChildrenField = computeMapChildrenField.value;
			const dragOffsetIndex = prevDragPos === "bottom" ? 1 : 0;
			const el = refElem.value;
			const errRest = { status: false };
			if (!(el && prevDragNode && dragNode)) return Promise.resolve(errRest);
			if (prevDragNode !== dragNode) {
				const dragParams = {
					oldNode: dragNode,
					newNode: prevDragNode,
					dragNode,
					dragPos: prevDragPos,
					dragToChild: !!prevDragToChild,
					offsetIndex: dragOffsetIndex
				};
				const dragNodeid = getNodeId(dragNode);
				const dragNodeRest = nodeMaps[dragNodeid] || {};
				const _dragNodeIndex = dragNodeRest._index;
				let dragNodeHeight = 0;
				let dragOffsetTop = -1;
				if (animation) {
					const prevItemEl = el.querySelector(`.vxe-tree--node-wrapper[nodeid="${prevDragNode}"]`);
					const oldItemEl = el.querySelector(`.vxe-tree--node-wrapper[nodeid="${dragNodeid}"]`);
					const targetItemEl = prevItemEl || oldItemEl;
					if (targetItemEl) dragNodeHeight = targetItemEl.offsetHeight;
					if (oldItemEl) dragOffsetTop = oldItemEl.offsetTop;
				}
				let oafIndex = -1;
				let nafIndex = -1;
				const oldAllMaps = {};
				let isSelfToChildStatus = false;
				const oldRest = dragNodeRest;
				const newNodeid = getNodeId(prevDragNode);
				const newRest = nodeMaps[newNodeid];
				if (transform) {
					if (oldRest && newRest) {
						const { level: oldLevel } = oldRest;
						const { level: newLevel } = newRest;
						import_xe_utils.default.eachTree([dragNode], (item) => {
							oldAllMaps[getNodeId(item)] = item;
						}, { children: mapChildrenField });
						if (oldLevel && newLevel) if (isPeerDrag && !isCrossDrag) {
							if (oldRest.item[parentField] !== newRest.item[parentField]) {
								clearNodeDragData();
								clearCrossTreeDragStatus();
								return errRest;
							}
						} else {
							if (!isCrossDrag) {
								clearNodeDragData();
								clearCrossTreeDragStatus();
								return errRest;
							}
							if (oldAllMaps[newNodeid]) {
								isSelfToChildStatus = true;
								if (!(isCrossDrag && isSelfToChildDrag)) {
									if (VxeUI.modal) VxeUI.modal.message({
										status: "error",
										content: getI18n$1("vxe.error.treeDragChild")
									});
									clearNodeDragData();
									clearCrossTreeDragStatus();
									return errRest;
								}
							}
						}
						else if (oldLevel) {
							if (!isCrossDrag) {
								clearNodeDragData();
								clearCrossTreeDragStatus();
								return errRest;
							}
						} else if (newLevel) {
							if (!isCrossDrag) {
								clearNodeDragData();
								clearCrossTreeDragStatus();
								return errRest;
							}
							if (oldAllMaps[newNodeid]) {
								isSelfToChildStatus = true;
								if (!(isCrossDrag && isSelfToChildDrag)) {
									if (VxeUI.modal) VxeUI.modal.message({
										status: "error",
										content: getI18n$1("vxe.error.treeDragChild")
									});
									clearNodeDragData();
									clearCrossTreeDragStatus();
									return errRest;
								}
							}
						}
					}
				}
				const isDragToChildFlag = isSelfToChildDrag && dragToChildMethod ? dragToChildMethod(dragParams) : prevDragToChild;
				return Promise.resolve(dEndMethod ? dEndMethod(dragParams) : true).then((status) => {
					if (!status) return errRest;
					if (transform) {
						if (oldRest && newRest) {
							const fullList = import_xe_utils.default.toTreeArray(internalData.afterTreeList, {
								updated: false,
								key: keyField,
								parentKey: parentField,
								children: mapChildrenField
							});
							const otfIndex = $xeTree.findNodeIndexOf(fullList, dragNode);
							fullList.splice(otfIndex, 1);
							const ntfIndex = $xeTree.findNodeIndexOf(fullList, prevDragNode) + dragOffsetIndex;
							fullList.splice(ntfIndex, 0, dragNode);
							if (isSelfToChildStatus && isCrossDrag && isSelfToChildDrag) import_xe_utils.default.each(dragNode[childrenField], (childNode) => {
								childNode[parentField] = dragNode[parentField];
							});
							dragNode[parentField] = isDragToChildFlag ? prevDragNode[keyField] : prevDragNode[parentField];
							internalData.treeFullData = import_xe_utils.default.toArrayTree(fullList, {
								key: keyField,
								parentKey: parentField,
								children: childrenField,
								mapChildren: mapChildrenField
							});
						}
					} else {
						oafIndex = $xeTree.findNodeIndexOf(afterVisibleList, dragNode);
						const otfIndex = $xeTree.findNodeIndexOf(treeFullData, dragNode);
						afterVisibleList.splice(oafIndex, 1);
						treeFullData.splice(otfIndex, 1);
						const pafIndex = $xeTree.findNodeIndexOf(afterVisibleList, prevDragNode);
						const ptfIndex = $xeTree.findNodeIndexOf(treeFullData, prevDragNode);
						nafIndex = pafIndex + dragOffsetIndex;
						const ntfIndex = ptfIndex + dragOffsetIndex;
						afterVisibleList.splice(nafIndex, 0, dragNode);
						treeFullData.splice(ntfIndex, 0, dragNode);
					}
					cacheNodeMap();
					handleData(transform);
					if (!transform) updateAfterDataIndex();
					updateCheckboxStatus();
					if (reactData.scrollYLoad) updateYSpace();
					if (evnt) dispatchEvent("node-dragend", {
						oldNode: dragNode,
						newNode: prevDragNode,
						dragNode,
						dragPos: prevDragPos,
						dragToChild: isDragToChildFlag,
						offsetIndex: dragOffsetIndex,
						_index: {
							newIndex: nafIndex,
							oldIndex: oafIndex
						}
					}, evnt);
					return nextTick().then(() => {
						if (animation) {
							const { treeList } = reactData;
							const { nodeMaps } = internalData;
							const dragNodeRest = nodeMaps[dragNodeid];
							const _newNodeIndex = dragNodeRest._index;
							const firstNode = treeList[0];
							const firstNodeRest = nodeMaps[getNodeId(firstNode)];
							const wrapperEl = el;
							if (firstNodeRest) {
								const _firstNodeIndex = firstNodeRest._index;
								const _lastNodeIndex = _firstNodeIndex + treeList.length;
								let rsIndex = -1;
								let reIndex = -1;
								let offsetRate = 1;
								if (_dragNodeIndex < _firstNodeIndex) {
									rsIndex = 0;
									reIndex = _newNodeIndex - _firstNodeIndex;
								} else if (_dragNodeIndex > _lastNodeIndex) {
									rsIndex = dragNodeRest.$index + 1;
									reIndex = treeList.length;
									offsetRate = -1;
								} else if (_newNodeIndex > _dragNodeIndex) {
									rsIndex = _dragNodeIndex - _firstNodeIndex;
									reIndex = rsIndex + _newNodeIndex - _dragNodeIndex;
								} else {
									rsIndex = _newNodeIndex - _firstNodeIndex;
									reIndex = rsIndex + _dragNodeIndex - _newNodeIndex + 1;
									offsetRate = -1;
								}
								const dragRangeList = treeList.slice(rsIndex, reIndex);
								if (dragRangeList.length) {
									const dtClss = [];
									dragRangeList.forEach((item) => {
										const nodeid = getNodeId(item);
										dtClss.push(`.vxe-tree--node-wrapper[nodeid="${nodeid}"]`);
									});
									moveRowAnimateToTb(wrapperEl.querySelectorAll(dtClss.join(",")), offsetRate * dragNodeHeight);
								}
							}
							const drClss = [`.vxe-tree--node-wrapper[nodeid="${dragNodeid}"]`];
							const newDtTrList = wrapperEl.querySelectorAll(drClss.join(","));
							const newTrEl = newDtTrList[0];
							if (dragOffsetTop > -1 && newTrEl) moveRowAnimateToTb(newDtTrList, dragOffsetTop - newTrEl.offsetTop);
						}
						recalculate();
					}).then(() => {
						return { status: true };
					});
				}).catch(() => {
					return errRest;
				}).then((rest) => {
					clearNodeDragData();
					clearCrossTreeDragStatus();
					return rest;
				});
			}
			clearNodeDragData();
			clearCrossTreeDragStatus();
			return Promise.resolve(errRest);
		};
		const handleNodeDragDragstartEvent = (evnt) => {
			if (evnt.dataTransfer) evnt.dataTransfer.setDragImage(getTpImg(), 0, 0);
		};
		const handleNodeDragDragendEvent = (evnt) => {
			const { lazy } = props;
			const { dragNode } = reactData;
			const { nodeMaps, prevDragNode, prevDragPos, prevDragToChild } = internalData;
			const hasChildField = computeHasChildField.value;
			if (lazy && prevDragToChild) {
				const nodeRest = nodeMaps[getNodeId(prevDragNode)];
				if (prevDragNode[hasChildField]) {
					if (nodeRest && nodeRest.treeLoaded) handleNodeDragSwapEvent(evnt, dragNode, prevDragNode, prevDragPos, prevDragToChild);
				} else handleNodeDragSwapEvent(evnt, dragNode, prevDragNode, prevDragPos, prevDragToChild);
			} else handleNodeDragSwapEvent(evnt, dragNode, prevDragNode, prevDragPos, prevDragToChild);
		};
		const handleNodeDragDragoverEvent = (evnt) => {
			const { lazy, transform } = props;
			const { dragNode } = reactData;
			const { nodeMaps } = internalData;
			const dragConfig = computeDragOpts.value;
			const parentField = computeParentField.value;
			const hasChildField = computeHasChildField.value;
			const { isCrossDrag, isPeerDrag, isToChildDrag } = dragConfig;
			if (!dragNode && !isCrossDrag) evnt.preventDefault();
			const isControlKey = hasControlKey(evnt);
			const itemEl = evnt.currentTarget;
			const nodeid = itemEl.getAttribute("nodeid") || "";
			const nodeItem = nodeMaps[nodeid];
			if (nodeItem) {
				evnt.preventDefault();
				const node = nodeItem.item;
				const dragPos = evnt.clientY - itemEl.getBoundingClientRect().y < itemEl.clientHeight / 2 ? "top" : "bottom";
				internalData.prevDragToChild = !!(transform && isCrossDrag && isToChildDrag && isControlKey);
				internalData.prevDragNode = node;
				internalData.prevDragPos = dragPos;
				if (dragNode && getNodeId(dragNode) === nodeid || isControlKey && lazy && node[hasChildField] && nodeItem && !nodeItem.treeLoaded || !isCrossDrag && transform && (isPeerDrag ? dragNode[parentField] !== node[parentField] : nodeItem.level)) {
					showDropTip(evnt, itemEl, false, dragPos);
					return;
				}
				showDropTip(evnt, itemEl, true, dragPos);
				dispatchEvent("node-dragover", {
					oldRNode: dragNode,
					targetNode: node,
					dragPos
				}, evnt);
			}
		};
		Object.assign($xeTree, treeMethods, {
			handleData,
			cacheNodeMap,
			updateAfterDataIndex,
			updateCheckboxStatus,
			updateYSpace,
			findNodeIndexOf(list, node) {
				return node ? import_xe_utils.default.findIndexOf(list, (item) => $xeTree.eqNode(item, node)) : -1;
			},
			eqNode(node1, node2) {
				if (node1 && node2) {
					if (node1 === node2) return true;
					return getNodeId(node1) === getNodeId(node2);
				}
				return false;
			},
			handleCrossTreeNodeDragCancelEvent() {
				clearNodeDragData();
				clearCrossTreeDragStatus();
			},
			/**
			* 处理跨树拖拽完成
			*/
			handleCrossTreeNodeDragFinishEvent() {},
			/**
			* 处理跨树拖至新的空树
			*/
			handleCrossTreeNodeDragInsertEvent() {},
			/**
			* 处理跨树拖插入
			*/
			handleCrossTreeNodeDragoverEmptyEvent() {},
			hideCrossTreeNodeDropClearStatus() {
				hideDropTip();
			}
		});
		const renderDragIcon = (node, nodeid) => {
			const { drag, transform } = props;
			const { nodeMaps } = internalData;
			const { showIcon, isPeerDrag, isCrossDrag, visibleMethod } = computeDragOpts.value;
			const params = {
				node,
				$tree: $xeTree
			};
			if (drag && showIcon && transform && (!visibleMethod || visibleMethod(params))) {
				const nodeItem = nodeMaps[nodeid];
				if (nodeItem && (isPeerDrag || isCrossDrag || !nodeItem.level)) {
					const { icon, trigger, disabledMethod } = computeDragOpts.value;
					const isDisabled = disabledMethod && disabledMethod(params);
					const ons = {};
					if (trigger !== "node") {
						ons.onMousedown = (evnt) => {
							if (!isDisabled) handleNodeDragMousedownEvent(evnt, params);
						};
						ons.onMouseup = handleNodeDragMouseupEvent;
					}
					return h("div", Object.assign({ class: ["vxe-tree--drag-handle", { "is--disabled": isDisabled }] }, ons), [h("i", { class: icon || getIcon$1().TREE_DRAG })]);
				}
			}
			return renderEmptyElement($xeTree);
		};
		const renderRadio = (node, nodeid, isChecked) => {
			const { showRadio } = props;
			const { showIcon, checkMethod, visibleMethod } = computeRadioOpts.value;
			const isVisible = !visibleMethod || visibleMethod({
				$tree: $xeTree,
				node
			});
			let isDisabled = !!checkMethod;
			if (showRadio && showIcon && isVisible) {
				if (checkMethod) isDisabled = !checkMethod({
					$tree: $xeTree,
					node
				});
				return h("div", {
					class: ["vxe-tree--radio-option", {
						"is--checked": isChecked,
						"is--disabled": isDisabled
					}],
					onClick: (evnt) => {
						if (!isDisabled) changeRadioEvent(evnt, node);
					}
				}, [h("span", { class: ["vxe-radio--icon", isChecked ? getIcon$1().RADIO_CHECKED : getIcon$1().RADIO_UNCHECKED] })]);
			}
			return renderEmptyElement($xeTree);
		};
		const renderCheckbox = (node, nodeid, isChecked) => {
			const { showCheckbox } = props;
			const { showIcon, checkMethod, visibleMethod } = computeCheckboxOpts.value;
			const isIndeterminate = isIndeterminateByCheckboxNodeid(nodeid);
			const isVisible = !visibleMethod || visibleMethod({
				$tree: $xeTree,
				node
			});
			let isDisabled = !!checkMethod;
			if (showCheckbox && showIcon && isVisible) {
				if (checkMethod) isDisabled = !checkMethod({
					$tree: $xeTree,
					node
				});
				return h("div", {
					class: ["vxe-tree--checkbox-option", {
						"is--checked": isChecked,
						"is--indeterminate": isIndeterminate,
						"is--disabled": isDisabled
					}],
					onClick: (evnt) => {
						if (!isDisabled) changeCheckboxEvent(evnt, node);
					}
				}, [h("span", { class: ["vxe-checkbox--icon", isIndeterminate ? getIcon$1().CHECKBOX_INDETERMINATE : isChecked ? getIcon$1().CHECKBOX_CHECKED : getIcon$1().CHECKBOX_UNCHECKED] })]);
			}
			return renderEmptyElement($xeTree);
		};
		const renderNode = (node, nodeid) => {
			const { lazy, drag, transform, showRadio, showCheckbox, showLine, iconOpen, iconClose, iconLoaded, showIcon } = props;
			const { currentNode, selectRadioKey, updateExpandedFlag } = reactData;
			const { afterTreeList, nodeMaps, treeExpandedMaps, treeExpandLazyLoadedMaps } = internalData;
			const childrenField = computeChildrenField.value;
			const titleField = computeTitleField.value;
			const hasChildField = computeHasChildField.value;
			const childList = import_xe_utils.default.get(node, childrenField);
			const hasChild = childList && childList.length;
			const iconSlot = slots.icon;
			const titleSlot = slots.title;
			const extraSlot = slots.extra;
			const isExpand = updateExpandedFlag && treeExpandedMaps[nodeid];
			const nodeItem = nodeMaps[nodeid];
			const nodeValue = import_xe_utils.default.get(node, titleField);
			const nLevel = nodeItem.level;
			let isRadioChecked = false;
			if (showRadio) isRadioChecked = nodeid === String(selectRadioKey);
			let isCheckboxChecked = false;
			if (showCheckbox) isCheckboxChecked = isCheckedByCheckboxNodeId(nodeid);
			let hasLazyChilds = false;
			let isLazyLoading = false;
			let isLazyLoaded = false;
			if (lazy) {
				isLazyLoading = !!treeExpandLazyLoadedMaps[nodeid];
				hasLazyChilds = node[hasChildField];
				isLazyLoaded = !!nodeItem.treeLoaded;
			}
			const prevNode = nodeItem.items[nodeItem.treeIndex - 1];
			const nParams = {
				node,
				isExpand
			};
			const itemOn = {
				onMousedown(evnt) {
					handleNodeMousedownEvent(evnt, node);
				},
				onMouseup: handleNodeDragMouseupEvent,
				onClick(evnt) {
					handleNodeClickEvent(evnt, node);
				},
				onDblclick(evnt) {
					handleNodeDblclickEvent(evnt, node);
				},
				onContextmenu(evnt) {
					handleContextmenuEvent(evnt, node);
				}
			};
			if (drag && transform) {
				itemOn.onDragstart = handleNodeDragDragstartEvent;
				itemOn.onDragend = handleNodeDragDragendEvent;
				itemOn.onDragover = handleNodeDragDragoverEvent;
			}
			return h("div", Object.assign({
				key: nodeid,
				class: ["vxe-tree--node-wrapper", `node--level-${nLevel}`],
				nodeid
			}, itemOn), [h("div", { class: ["vxe-tree--node-item", {
				"is--current": currentNode && nodeid === getNodeId(currentNode),
				"is-radio--checked": isRadioChecked,
				"is-checkbox--checked": isCheckboxChecked
			}] }, [
				showLine ? h("div", { class: "vxe-tree--node-line-wrapper" }, [h("div", {
					class: "vxe-tree--node-line",
					style: { height: `${getNodeId(afterTreeList[0]) === nodeid ? 1 : calcTreeLine($xeTree, node, prevNode)}px` }
				})]) : renderEmptyElement($xeTree),
				h("div", { class: "vxe-tree--node-item-switcher" }, showIcon && (lazy ? isLazyLoaded ? hasChild : hasLazyChilds : hasChild) ? [h("div", {
					class: "vxe-tree--node-item-icon",
					onClick(evnt) {
						toggleExpandEvent(evnt, node);
					}
				}, iconSlot ? getSlotVNs$1(iconSlot(nParams)) : [h("i", { class: isLazyLoading ? iconLoaded || getIcon$1().TREE_NODE_LOADED : isExpand ? iconOpen || getIcon$1().TREE_NODE_OPEN : iconClose || getIcon$1().TREE_NODE_CLOSE })])] : []),
				renderDragIcon(node, nodeid),
				renderRadio(node, nodeid, isRadioChecked),
				renderCheckbox(node, nodeid, isCheckboxChecked),
				h("div", { class: "vxe-tree--node-item-inner" }, [h("div", { class: "vxe-tree--node-item-title" }, titleSlot ? getSlotVNs$1(titleSlot(nParams)) : `${nodeValue}`), extraSlot ? h("div", { class: "vxe-tree--node-item-extra" }, getSlotVNs$1(extraSlot(nParams))) : renderEmptyElement($xeTree)])
			])]);
		};
		const renderList = (treeList) => {
			const { transform } = props;
			const { treeExpandedMaps } = internalData;
			const childrenField = computeChildrenField.value;
			if (!treeList.length) return [h("div", { class: "vxe-tree--empty-placeholder" }, getI18n$1("vxe.tree.searchEmpty"))];
			const nodeVNs = [];
			treeList.forEach(transform ? (node) => {
				const nodeid = getNodeId(node);
				nodeVNs.push(renderNode(node, nodeid));
			} : (node) => {
				const nodeid = getNodeId(node);
				nodeVNs.push(renderNode(node, nodeid));
				const childList = import_xe_utils.default.get(node, childrenField);
				if (childList && childList.length && treeExpandedMaps[nodeid]) nodeVNs.push(...renderList(childList));
			});
			return nodeVNs;
		};
		const renderDragTip = () => {
			const { drag } = props;
			const { dragNode, dragTipText } = reactData;
			const dragOpts = computeDragOpts.value;
			const dNode = dragNode;
			if (drag) return h("div", { class: "vxe-tree--drag-wrapper" }, [h("div", {
				ref: refDragNodeLineElem,
				class: ["vxe-tree--drag-node-line", { "is--guides": dragOpts.showGuidesStatus }]
			}), dNode && dragOpts.showDragTip ? h("div", {
				ref: refDragTipElem,
				class: "vxe-tree--drag-sort-tip"
			}, [h("div", { class: "vxe-tree--drag-sort-tip-wrapper" }, [h("div", { class: "vxe-tree--drag-sort-tip-status" }, [
				h("span", { class: ["vxe-tree--drag-sort-tip-normal-status", getIcon$1().TREE_DRAG_STATUS_NODE] }),
				h("span", { class: ["vxe-tree--drag-sort-tip-sub-status", getIcon$1().TREE_DRAG_STATUS_SUB_NODE] }),
				h("span", { class: ["vxe-tree--drag-sort-tip-disabled-status", getIcon$1().TREE_DRAG_DISABLED] })
			]), h("div", { class: "vxe-tree--drag-sort-tip-content" }, dragTipText)])]) : renderEmptyElement($xeTree)]);
			return renderEmptyElement($xeTree);
		};
		const renderVN = () => {
			const { loading, trigger, showLine } = props;
			const { bodyHeight, topSpaceHeight, treeList } = reactData;
			const vSize = computeSize.value;
			const radioOpts = computeRadioOpts.value;
			const checkboxOpts = computeCheckboxOpts.value;
			const loadingOpts = computeLoadingOpts.value;
			const isRowHover = computeIsRowHover.value;
			const treeStyle = computeTreeStyle.value;
			const loadingSlot = slots.loading;
			const headerSlot = slots.header;
			const footerSlot = slots.footer;
			return h("div", {
				ref: refElem,
				class: ["vxe-tree", {
					[`size--${vSize}`]: vSize,
					"show--line": showLine,
					"checkbox--highlight": checkboxOpts.highlight,
					"radio--highlight": radioOpts.highlight,
					"node--hover": isRowHover,
					"node--trigger": trigger === "node",
					"is--loading": loading
				}]
			}, [
				headerSlot ? h("div", {
					ref: refHeaderWrapperElem,
					class: "vxe-tree--header-wrapper"
				}, headerSlot({ $tree: $xeTree })) : renderEmptyElement($xeTree),
				h("div", {
					ref: refVirtualWrapper,
					class: "vxe-tree--node-list-wrapper",
					style: treeStyle,
					onScroll: scrollEvent
				}, [h("div", {
					class: "vxe-tree--y-space",
					style: { height: bodyHeight ? `${bodyHeight}px` : "" }
				}), h("div", {
					ref: refVirtualBody,
					class: "vxe-tree--node-list-body",
					style: { transform: `translateY(${topSpaceHeight}px)` }
				}, renderList(treeList))]),
				footerSlot ? h("div", {
					ref: refFooterWrapperElem,
					class: "vxe-tree--footer-wrapper"
				}, footerSlot({ $tree: $xeTree })) : renderEmptyElement($xeTree),
				renderDragTip(),
				h(VxeLoading, {
					class: "vxe-tree--loading",
					modelValue: loading,
					icon: loadingOpts.icon,
					text: loadingOpts.text
				}, loadingSlot ? { default: () => loadingSlot({ $tree: $xeTree }) } : {})
			]);
		};
		const dataFlag = ref(0);
		watch(() => props.data ? props.data.length : 0, () => {
			dataFlag.value++;
		});
		watch(() => props.data, () => {
			dataFlag.value++;
		});
		watch(dataFlag, () => {
			loadData(props.data || []);
		});
		watch(() => props.checkNodeKey, (nodeKey) => {
			reactData.selectRadioKey = enNodeValue(nodeKey);
		});
		const checkboxFlag = ref(0);
		watch(() => props.checkNodeKeys ? props.checkNodeKeys.length : 0, () => {
			checkboxFlag.value++;
		});
		watch(() => props.checkNodeKeys, () => {
			checkboxFlag.value++;
		});
		watch(checkboxFlag, () => {
			updateCheckboxChecked(props.checkNodeKeys || []);
		});
		watch(() => props.filterValue, () => {
			triggerSearchEvent(new Event("filter"));
		});
		const hFlag = ref(0);
		watch(() => props.height, () => {
			hFlag.value++;
		});
		watch(() => props.minHeight, () => {
			hFlag.value++;
		});
		watch(() => props.maxHeight, () => {
			hFlag.value++;
		});
		watch(hFlag, () => {
			recalculate();
		});
		onMounted(() => {
			const { transform, drag, menuConfig } = props;
			const dragOpts = computeDragOpts.value;
			if (drag && !transform) errLog("vxe.error.notSupportProp", [
				"drag",
				"transform=false",
				"transform=true"
			]);
			if (dragOpts.isCrossTreeDrag) errLog("vxe.error.notProp", ["drag-config.isCrossTreeDrag"]);
			const VxeUIContextMenu = VxeUI.getComponent("VxeContextMenu");
			if (menuConfig && !VxeUIContextMenu) errLog("vxe.error.reqComp", ["vxe-context-menu"]);
			if (props.autoResize) {
				const el = refElem.value;
				const parentEl = getParentElem();
				const resizeObserver = globalResize.create(() => {
					if (props.autoResize) recalculate();
				});
				if (el) resizeObserver.observe(el);
				if (parentEl) resizeObserver.observe(parentEl);
				internalData.resizeObserver = resizeObserver;
			}
			globalEvents.on($xeTree, "resize", handleGlobalResizeEvent);
		});
		onBeforeUnmount(() => {
			const { resizeObserver } = internalData;
			if (resizeObserver) resizeObserver.disconnect();
			globalEvents.off($xeTree, "resize");
			import_xe_utils.default.assign(reactData, createReactData$1());
			import_xe_utils.default.assign(internalData, createInternalData$1());
		});
		reactData.selectRadioKey = enNodeValue(props.checkNodeKey);
		loadData(props.data || []);
		$xeTree.renderVN = renderVN;
		provide("$xeTree", $xeTree);
		return $xeTree;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/tree/index.js
var VxeTree = Object.assign({}, tree_default, { install(app) {
	app.component(tree_default.name, tree_default);
} });
dynamicApp.use(VxeTree);
VxeUI.component(tree_default);
var Tree = VxeTree;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/tree-select/src/tree-select.js
function getOptUniqueId() {
	return import_xe_utils.default.uniqueId("node_");
}
function createReactData() {
	return {
		initialized: false,
		searchValue: "",
		searchLoading: false,
		panelIndex: 0,
		panelStyle: {},
		panelPlacement: null,
		triggerFocusPanel: false,
		visiblePanel: false,
		isAniVisible: false,
		isActivated: false
	};
}
function createInternalData() {
	return {
		fullOptionList: [],
		fullNodeMaps: {}
	};
}
var tree_select_default = defineVxeComponent({
	name: "VxeTreeSelect",
	props: {
		modelValue: [
			String,
			Number,
			Array
		],
		clearable: Boolean,
		placeholder: {
			type: String,
			default: () => import_xe_utils.default.eqNull(getConfig().treeSelect.placeholder) ? getI18n("vxe.base.pleaseSelect") : getConfig().treeSelect.placeholder
		},
		readonly: {
			type: Boolean,
			default: null
		},
		loading: Boolean,
		disabled: {
			type: Boolean,
			default: null
		},
		filterable: Boolean,
		filterConfig: Object,
		multiple: Boolean,
		className: [String, Function],
		/**
		* 已废弃，请使用 popupConfig.className
		* @deprecated
		*/
		popupClassName: [String, Function],
		prefixIcon: String,
		placement: String,
		lazyOptions: Array,
		options: Array,
		optionProps: Object,
		/**
		* 已废弃，请使用 popupConfig.zIndex
		* @deprecated
		*/
		zIndex: Number,
		size: {
			type: String,
			default: () => getConfig().treeSelect.size || getConfig().size
		},
		remote: Boolean,
		remoteConfig: Function,
		popupConfig: Object,
		treeConfig: Object,
		menuConfig: Object,
		virtualYConfig: Object,
		autoClose: {
			type: Boolean,
			default: () => getConfig().treeSelect.autoClose
		},
		showTotalButoon: {
			type: Boolean,
			default: () => getConfig().treeSelect.showTotalButoon
		},
		showCheckedButoon: {
			type: Boolean,
			default: () => getConfig().treeSelect.showCheckedButoon
		},
		showClearButton: {
			type: Boolean,
			default: () => getConfig().treeSelect.showClearButton
		},
		showExpandButton: {
			type: Boolean,
			default: () => getConfig().treeSelect.showExpandButton
		},
		transfer: {
			type: Boolean,
			default: null
		},
		/**
		* 已废弃，被 remote-config.queryMethod 替换
		* @deprecated
		*/
		remoteMethod: Function
	},
	emits: [
		"update:modelValue",
		"change",
		"all-change",
		"clear",
		"blur",
		"focus",
		"click",
		"node-click"
	],
	setup(props, context) {
		const { emit, slots } = context;
		const $xeModal = inject("$xeModal", null);
		const $xeDrawer = inject("$xeDrawer", null);
		const $xeTable = inject("$xeTable", null);
		const $xeForm = inject("$xeForm", null);
		const formItemInfo = inject("xeFormItemInfo", null);
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const refElem = ref();
		const refInput = ref();
		const refInpSearch = ref();
		const refTreeWrapper = ref();
		const refOptionPanel = ref();
		const refTree = ref();
		const reactData = reactive(createReactData());
		const internalData = createInternalData();
		const refMaps = { refElem };
		const computeFormReadonly = computed(() => {
			const { readonly } = props;
			if (readonly === null) {
				if ($xeForm) return $xeForm.props.readonly;
				return false;
			}
			return readonly;
		});
		const computeIsDisabled = computed(() => {
			const { disabled } = props;
			if (disabled === null) {
				if ($xeForm) return $xeForm.props.disabled;
				return false;
			}
			return disabled;
		});
		const computeBtnTransfer = computed(() => {
			const { transfer } = props;
			const popupOpts = computePopupOpts.value;
			if (import_xe_utils.default.isBoolean(popupOpts.transfer)) return popupOpts.transfer;
			if (transfer === null) {
				const globalTransfer = getConfig().treeSelect.transfer;
				if (import_xe_utils.default.isBoolean(globalTransfer)) return globalTransfer;
				if ($xeTable || $xeModal || $xeDrawer || $xeForm) return true;
			}
			return transfer;
		});
		const computePopupOpts = computed(() => {
			return Object.assign({}, getConfig().treeSelect.popupConfig, props.popupConfig);
		});
		const computeTreeOpts = computed(() => {
			return Object.assign({}, getConfig().treeSelect.treeConfig, props.treeConfig, { data: void 0 });
		});
		const computeMenuOpts = computed(() => {
			return Object.assign({}, getConfig().treeSelect.menuConfig, props.menuConfig);
		});
		const computeTreeNodeOpts = computed(() => {
			const treeOpts = computeTreeOpts.value;
			return Object.assign({ isHover: true }, treeOpts.nodeConfig);
		});
		const computeTreeCheckboxOpts = computed(() => {
			const treeOpts = computeTreeOpts.value;
			return Object.assign({ showIcon: !!treeOpts.showCheckbox }, treeOpts.checkboxConfig, { trigger: "node" });
		});
		const computeTreeRadioOpts = computed(() => {
			const treeOpts = computeTreeOpts.value;
			return Object.assign({ showIcon: !!treeOpts.showRadio }, treeOpts.radioConfig, { trigger: "node" });
		});
		const computePropsOpts = computed(() => {
			return props.optionProps || {};
		});
		const computeNodeKeyField = computed(() => {
			return computeTreeOpts.value.keyField || "id";
		});
		const computeLabelField = computed(() => {
			return computePropsOpts.value.label || "label";
		});
		const computeValueField = computed(() => {
			return computePropsOpts.value.value || "value";
		});
		const computeChildrenField = computed(() => {
			return computePropsOpts.value.children || "children";
		});
		const computeParentField = computed(() => {
			return computePropsOpts.value.parent || "parentField";
		});
		const computeHasChildField = computed(() => {
			return computePropsOpts.value.hasChild || "hasChild";
		});
		const computeVirtualYOpts = computed(() => {
			return Object.assign({}, getConfig().treeSelect.virtualYConfig, props.virtualYConfig);
		});
		const computeRemoteOpts = computed(() => {
			return Object.assign({}, getConfig().treeSelect.remoteConfig, props.remoteConfig);
		});
		const computeFilterOpts = computed(() => {
			const treeOpts = computeTreeOpts.value;
			return Object.assign({}, treeOpts.filterConfig, props.filterConfig);
		});
		const computeSelectLabel = computed(() => {
			const { modelValue, lazyOptions } = props;
			const { fullNodeMaps } = internalData;
			const valueField = computeValueField.value;
			const labelField = computeLabelField.value;
			return (import_xe_utils.default.eqNull(modelValue) ? [] : import_xe_utils.default.isArray(modelValue) ? modelValue : [modelValue]).map((val) => {
				const cacheItem = fullNodeMaps[val];
				if (cacheItem) return cacheItem.item[labelField];
				if (lazyOptions) {
					const lazyItem = lazyOptions.find((item) => item[valueField] === val);
					if (lazyItem) return lazyItem[labelField];
				}
				return val;
			}).join(", ");
		});
		const computePopupWrapperStyle = computed(() => {
			const { height, width } = computePopupOpts.value;
			const stys = {};
			if (width) stys.width = toCssUnit(width);
			if (height) {
				stys.height = toCssUnit(height);
				stys.maxHeight = toCssUnit(height);
			}
			return stys;
		});
		const computeMaps = {};
		const $xeTreeSelect = {
			xID,
			props,
			context,
			reactData,
			internalData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $treeSelect: $xeTreeSelect }, params));
		};
		const emitModel = (value) => {
			emit("update:modelValue", value);
		};
		const callSlot = (slotFunc, params) => {
			if (slotFunc) {
				if (import_xe_utils.default.isString(slotFunc)) slotFunc = slots[slotFunc] || null;
				if (import_xe_utils.default.isFunction(slotFunc)) return getSlotVNs$1(slotFunc(params));
			}
			return [];
		};
		const treeSelectMethods = { dispatchEvent };
		const getNodeid = (option) => {
			const nodeid = option[computeNodeKeyField.value];
			return nodeid ? encodeURIComponent(nodeid) : "";
		};
		const cacheDataMap = () => {
			const { options } = props;
			const treeOpts = computeTreeOpts.value;
			const nodeKeyField = computeNodeKeyField.value;
			const childrenField = computeChildrenField.value;
			const valueField = computeValueField.value;
			const { transform } = treeOpts;
			const nodeMaps = {};
			const keyMaps = {};
			const handleOptNode = (item, index, items, path, parent, nodes) => {
				let nodeid = getNodeid(item);
				if (!nodeid) nodeid = getOptUniqueId();
				if (keyMaps[nodeid]) errLog("vxe.error.repeatKey", [`[tree-select] ${nodeKeyField}`, nodeid]);
				keyMaps[nodeid] = true;
				const value = item[valueField];
				if (nodeMaps[value]) errLog("vxe.error.repeatKey", [`[tree-select] ${valueField}`, value]);
				nodeMaps[value] = {
					item,
					index,
					items,
					parent,
					nodes
				};
			};
			if (options) if (transform) options.forEach((item, index, items) => {
				handleOptNode(item, index, items, [], null, []);
			});
			else import_xe_utils.default.eachTree(options, handleOptNode, { children: childrenField });
			internalData.fullOptionList = options || [];
			internalData.fullNodeMaps = nodeMaps;
		};
		const updateZindex = () => {
			const customZIndex = computePopupOpts.value.zIndex || props.zIndex;
			if (customZIndex) reactData.panelIndex = import_xe_utils.default.toNumber(customZIndex);
			else if (reactData.panelIndex < getLastZIndex()) reactData.panelIndex = nextZIndex();
		};
		const updatePlacement = () => {
			const { placement } = props;
			const { panelIndex } = reactData;
			const targetElem = refElem.value;
			const panelElem = refOptionPanel.value;
			const btnTransfer = computeBtnTransfer.value;
			const popupOpts = computePopupOpts.value;
			const handleStyle = () => {
				const ppObj = updatePanelPlacement(targetElem, panelElem, {
					placement: popupOpts.placement || placement,
					defaultPlacement: popupOpts.defaultPlacement,
					teleportTo: btnTransfer
				});
				reactData.panelStyle = Object.assign(ppObj.style, { zIndex: panelIndex });
				reactData.panelPlacement = ppObj.placement;
			};
			handleStyle();
			return nextTick().then(handleStyle);
		};
		const showOptionPanel = () => {
			const { loading, remote, filterable } = props;
			const { fullOptionList } = internalData;
			const isDisabled = computeIsDisabled.value;
			const remoteOpts = computeRemoteOpts.value;
			if (!loading && !isDisabled) {
				clearTimeout(internalData.hpTimeout);
				if (!reactData.initialized) reactData.initialized = true;
				reactData.isActivated = true;
				reactData.isAniVisible = true;
				if (filterable) {
					if (remote && remoteOpts.enabled && remoteOpts.autoLoad && !fullOptionList.length) handleSearchEvent();
				}
				setTimeout(() => {
					reactData.visiblePanel = true;
					handleFocusSearch();
					updatePlacement();
				}, 10);
				updateZindex();
				updatePlacement();
			}
		};
		const hideOptionPanel = () => {
			reactData.visiblePanel = false;
			internalData.hpTimeout = setTimeout(() => {
				reactData.isAniVisible = false;
			}, 350);
		};
		const changeEvent = (evnt, selectValue, node) => {
			const value = import_xe_utils.default.isArray(selectValue) ? selectValue.map(deNodeValue) : deNodeValue(selectValue);
			emitModel(value);
			if (value !== props.modelValue) {
				dispatchEvent("change", {
					value,
					node,
					option: node
				}, evnt);
				if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, value);
			}
		};
		const clearValueEvent = (evnt, selectValue) => {
			changeEvent(evnt, selectValue, null);
			dispatchEvent("clear", { value: selectValue }, evnt);
		};
		const clearEvent = (params, evnt) => {
			clearValueEvent(evnt, null);
			hideOptionPanel();
		};
		const allCheckedPanelEvent = (params) => {
			const { $event } = params;
			const { multiple, autoClose } = props;
			const $tree = refTree.value;
			if (multiple) {
				if ($tree) $tree.setAllCheckboxNode(true).then(({ checkNodeKeys, checkNodes }) => {
					changeEvent($event, checkNodeKeys, checkNodes[0]);
					dispatchEvent("all-change", { value: checkNodeKeys }, $event);
					if (autoClose) hideOptionPanel();
				});
			}
		};
		const clearCheckedPanelEvent = (params) => {
			const { $event } = params;
			const { multiple, autoClose } = props;
			const $tree = refTree.value;
			if ($tree) {
				const value = multiple ? [] : null;
				$tree.clearCheckboxNode().then(() => {
					if (autoClose) hideOptionPanel();
				});
				changeEvent($event, value, null);
				dispatchEvent("clear", { value }, $event);
			}
		};
		const allExpandPanelEvent = () => {
			const $tree = refTree.value;
			if ($tree) $tree.setAllExpandNode(true);
		};
		const clearExpandPanelEvent = () => {
			const $tree = refTree.value;
			if ($tree) $tree.clearAllExpandNode();
		};
		const handleGlobalMousewheelEvent = (evnt) => {
			const { visiblePanel } = reactData;
			if (!computeIsDisabled.value) {
				if (visiblePanel) {
					const panelElem = refOptionPanel.value;
					if (getEventTargetNode(evnt, panelElem).flag) updatePlacement();
					else hideOptionPanel();
				}
			}
		};
		const handleGlobalMousedownEvent = (evnt) => {
			const { visiblePanel } = reactData;
			if (!computeIsDisabled.value) {
				const el = refElem.value;
				const panelElem = refOptionPanel.value;
				reactData.isActivated = getEventTargetNode(evnt, el).flag || getEventTargetNode(evnt, panelElem).flag;
				if (visiblePanel && !reactData.isActivated) hideOptionPanel();
			}
		};
		const handleGlobalBlurEvent = () => {
			const { visiblePanel, isActivated } = reactData;
			if (visiblePanel) hideOptionPanel();
			if (isActivated) reactData.isActivated = false;
			if (visiblePanel || isActivated) {
				const $input = refInput.value;
				if ($input) $input.blur();
			}
		};
		const handleGlobalResizeEvent = () => {
			const { visiblePanel } = reactData;
			if (visiblePanel) updatePlacement();
		};
		const handleFocusSearch = () => {
			if (props.filterable) nextTick(() => {
				const inpSearch = refInpSearch.value;
				if (inpSearch) inpSearch.focus();
			});
		};
		const focusEvent = (evnt) => {
			if (!computeIsDisabled.value) {
				if (!reactData.visiblePanel) {
					reactData.triggerFocusPanel = true;
					showOptionPanel();
					setTimeout(() => {
						reactData.triggerFocusPanel = false;
					}, 150);
				}
			}
			dispatchEvent("focus", {}, evnt);
		};
		const clickEvent = (evnt) => {
			togglePanelEvent(evnt);
			dispatchEvent("click", {}, evnt);
		};
		const blurEvent = (evnt) => {
			reactData.isActivated = false;
			dispatchEvent("blur", {}, evnt);
		};
		const modelSearchEvent = (value) => {
			reactData.searchValue = value;
		};
		const handleSearchEvent = () => {
			const { modelValue, remote, remoteMethod } = props;
			const { searchValue } = reactData;
			const remoteOpts = computeRemoteOpts.value;
			const queryMethod = remoteOpts.queryMethod || remoteMethod;
			if (remote && queryMethod && remoteOpts.enabled) {
				reactData.searchLoading = true;
				Promise.resolve(queryMethod({
					$treeSelect: $xeTreeSelect,
					searchValue,
					value: modelValue
				})).then(() => nextTick()).catch(() => nextTick()).finally(() => {
					reactData.searchLoading = false;
				});
			}
		};
		const togglePanelEvent = (params) => {
			const { $event } = params;
			$event.preventDefault();
			if (reactData.triggerFocusPanel) reactData.triggerFocusPanel = false;
			else if (reactData.visiblePanel) hideOptionPanel();
			else showOptionPanel();
		};
		const nodeExpandEvent = () => {
			updatePlacement();
		};
		const nodeClickEvent = (params) => {
			const { $event } = params;
			dispatchEvent("node-click", params, $event);
		};
		const radioChangeEvent = (params) => {
			const { value, $event, node } = params;
			changeEvent($event, value, node);
			hideOptionPanel();
		};
		const checkboxChangeEvent = (params) => {
			const { value, $event, node } = params;
			changeEvent($event, value, node);
		};
		const loadSuccessEvent = () => {
			cacheDataMap();
		};
		Object.assign($xeTreeSelect, treeSelectMethods, {});
		const renderVN = () => {
			const { className, modelValue, multiple, options, loading, menuConfig, filterable, showTotalButoon, showCheckedButoon, showClearButton, showExpandButton } = props;
			const { initialized, isActivated, isAniVisible, visiblePanel, searchValue } = reactData;
			const vSize = computeSize.value;
			const isDisabled = computeIsDisabled.value;
			const selectLabel = computeSelectLabel.value;
			const btnTransfer = computeBtnTransfer.value;
			const formReadonly = computeFormReadonly.value;
			const popupWrapperStyle = computePopupWrapperStyle.value;
			const headerSlot = slots.header;
			const footerSlot = slots.footer;
			const prefixSlot = slots.prefix;
			const popupOpts = computePopupOpts.value;
			const ppClassName = popupOpts.className || props.popupClassName;
			if (formReadonly) return h("div", {
				ref: refElem,
				class: ["vxe-tree-select--readonly", className]
			}, [h("span", { class: "vxe-tree-select-label" }, selectLabel)]);
			const treeOpts = computeTreeOpts.value;
			const menuOpts = computeMenuOpts.value;
			const treeNodeOpts = computeTreeNodeOpts.value;
			const treeCheckboxOpts = computeTreeCheckboxOpts.value;
			const treeRadioOpts = computeTreeRadioOpts.value;
			const nodeKeyField = computeNodeKeyField.value;
			const labelField = computeLabelField.value;
			const valueField = computeValueField.value;
			const childrenField = computeChildrenField.value;
			const parentField = computeParentField.value;
			const hasChildField = computeHasChildField.value;
			const virtualYOpts = computeVirtualYOpts.value;
			const filterOpts = computeFilterOpts.value;
			const { slots: treeSlots } = treeOpts;
			const selectVals = import_xe_utils.default.eqNull(modelValue) ? [] : import_xe_utils.default.isArray(modelValue) ? modelValue : [modelValue];
			const treeScopedSlots = {};
			if (treeSlots) {
				const { icon: treeIconSlot, title: treeTitleSlot, extra: treeExtraSlot } = treeSlots;
				if (treeIconSlot) treeScopedSlots.icon = (slotParams) => callSlot(treeIconSlot, slotParams);
				if (treeTitleSlot) treeScopedSlots.title = (slotParams) => callSlot(treeTitleSlot, slotParams);
				if (treeExtraSlot) treeScopedSlots.extra = (slotParams) => callSlot(treeExtraSlot, slotParams);
			}
			return h("div", {
				ref: refElem,
				class: [
					"vxe-tree-select",
					className ? import_xe_utils.default.isFunction(className) ? className({ $treeSelect: $xeTreeSelect }) : className : "",
					{
						[`size--${vSize}`]: vSize,
						"is--filterable": filterable,
						"is--visible": visiblePanel,
						"is--disabled": isDisabled,
						"is--loading": loading,
						"is--active": isActivated
					}
				]
			}, [h(VxeInput, {
				ref: refInput,
				clearable: props.clearable,
				placeholder: loading ? getI18n("vxe.select.loadingText") : props.placeholder,
				editable: false,
				disabled: isDisabled,
				type: "text",
				prefixIcon: props.prefixIcon,
				suffixIcon: loading ? getIcon().TREE_SELECT_LOADED : visiblePanel ? getIcon().TREE_SELECT_OPEN : getIcon().TREE_SELECT_CLOSE,
				modelValue: loading ? "" : selectLabel,
				title: selectLabel,
				onClear: clearEvent,
				onClick: clickEvent,
				onFocus: focusEvent,
				onBlur: blurEvent,
				onSuffixClick: togglePanelEvent
			}, prefixSlot ? { prefix: () => prefixSlot({}) } : {}), h(Teleport, {
				to: "body",
				disabled: btnTransfer ? !initialized : true
			}, [h("div", {
				ref: refOptionPanel,
				class: [
					"vxe-table--ignore-clear vxe-tree-select--panel",
					ppClassName ? import_xe_utils.default.isFunction(ppClassName) ? ppClassName({ $treeSelect: $xeTreeSelect }) : ppClassName : "",
					{
						[`size--${vSize}`]: vSize,
						"is--transfer": btnTransfer,
						"ani--leave": !loading && isAniVisible,
						"ani--enter": !loading && visiblePanel
					}
				],
				placement: reactData.panelPlacement,
				style: reactData.panelStyle
			}, initialized ? [h("div", { class: "vxe-tree-select--panel-wrapper" }, [
				filterable ? h("div", { class: "vxe-tree-select--panel-search" }, [h(VxeInput, {
					ref: refInpSearch,
					class: "vxe-tree-select-search--input",
					modelValue: searchValue,
					clearable: true,
					disabled: false,
					readonly: false,
					placeholder: getI18n("vxe.treeSelect.search"),
					prefixIcon: getIcon().INPUT_SEARCH,
					"onUpdate:modelValue": modelSearchEvent
				})]) : renderEmptyElement($xeTreeSelect),
				showTotalButoon || showCheckedButoon && multiple || showClearButton || showExpandButton || headerSlot ? h("div", { class: "vxe-tree-select--panel-header" }, headerSlot ? headerSlot({}) : [h("div", { class: "vxe-tree-select--header-button" }, [showCheckedButoon && showClearButton ? h("div", { class: "vxe-tree-select--selected-btns" }, [showCheckedButoon && multiple ? h(VxeButton, {
					content: getI18n("vxe.treeSelect.allChecked"),
					mode: "text",
					onClick: allCheckedPanelEvent
				}) : renderEmptyElement($xeTreeSelect), showClearButton ? h(VxeButton, {
					content: getI18n("vxe.treeSelect.clearChecked"),
					mode: "text",
					onClick: clearCheckedPanelEvent
				}) : renderEmptyElement($xeTreeSelect)]) : renderEmptyElement($xeTreeSelect), showExpandButton && showExpandButton ? h("div", { class: "vxe-tree-select--expand-btns" }, [showExpandButton ? h(VxeButton, {
					content: getI18n("vxe.treeSelect.allExpand"),
					mode: "text",
					onClick: allExpandPanelEvent
				}) : renderEmptyElement($xeTreeSelect), showExpandButton ? h(VxeButton, {
					content: getI18n("vxe.treeSelect.clearExpand"),
					mode: "text",
					onClick: clearExpandPanelEvent
				}) : renderEmptyElement($xeTreeSelect)]) : renderEmptyElement($xeTreeSelect)])]) : renderEmptyElement($xeTreeSelect),
				h("div", { class: "vxe-tree-select--panel-body" }, [h("div", {
					ref: refTreeWrapper,
					class: "vxe-tree-select-tree--wrapper",
					style: popupWrapperStyle
				}, [h(VxeTree, {
					ref: refTree,
					class: "vxe-tree-select--tree",
					height: popupOpts.height ? "100%" : treeOpts.height,
					minHeight: treeOpts.minHeight,
					maxHeight: popupOpts.height ? "" : treeOpts.maxHeight,
					autoResize: true,
					data: options,
					indent: treeOpts.indent,
					showRadio: !multiple,
					radioConfig: treeRadioOpts,
					checkNodeKey: multiple ? null : modelValue,
					showCheckbox: !!multiple,
					checkNodeKeys: multiple ? modelValue : null,
					checkboxConfig: treeCheckboxOpts,
					titleField: labelField,
					valueField,
					keyField: nodeKeyField,
					childrenField: treeOpts.childrenField || childrenField,
					parentField: treeOpts.parentField || parentField,
					hasChildField: treeOpts.hasChildField || hasChildField,
					accordion: treeOpts.accordion,
					expandAll: treeOpts.expandAll,
					expandNodeKeys: treeOpts.expandNodeKeys,
					nodeConfig: treeNodeOpts,
					lazy: treeOpts.lazy,
					loadMethod: treeOpts.loadMethod,
					toggleMethod: treeOpts.toggleMethod,
					transform: treeOpts.transform,
					trigger: treeOpts.trigger,
					showIcon: treeOpts.showIcon,
					showLine: treeOpts.showLine,
					iconOpen: treeOpts.iconOpen,
					iconLoaded: treeOpts.iconLoaded,
					iconClose: treeOpts.iconClose,
					filterValue: searchValue,
					filterConfig: filterOpts,
					menuConfig: menuConfig ? menuOpts : void 0,
					virtualYConfig: virtualYOpts,
					onNodeExpand: nodeExpandEvent,
					onNodeClick: nodeClickEvent,
					onRadioChange: radioChangeEvent,
					onCheckboxChange: checkboxChangeEvent,
					onLoadSuccess: loadSuccessEvent
				}, treeScopedSlots)])]),
				footerSlot || showTotalButoon ? h("div", { class: "vxe-tree-select--panel-footer" }, footerSlot ? footerSlot({}) : [h("div", { class: "vxe-tree-select--footer-button" }, [showTotalButoon ? h("div", { class: "vxe-tree-select--total-btns" }, getI18n("vxe.treeSelect.total", [selectVals.length])) : renderEmptyElement($xeTreeSelect)])]) : renderEmptyElement($xeTreeSelect)
			])] : [])])]);
		};
		watch(() => props.options, () => {
			cacheDataMap();
		});
		cacheDataMap();
		onMounted(() => {
			globalEvents.on($xeTreeSelect, "mousewheel", handleGlobalMousewheelEvent);
			globalEvents.on($xeTreeSelect, "mousedown", handleGlobalMousedownEvent);
			globalEvents.on($xeTreeSelect, "blur", handleGlobalBlurEvent);
			globalEvents.on($xeTreeSelect, "resize", handleGlobalResizeEvent);
		});
		onBeforeUnmount(() => {
			globalEvents.off($xeTreeSelect, "mousewheel");
			globalEvents.off($xeTreeSelect, "mousedown");
			globalEvents.off($xeTreeSelect, "blur");
			globalEvents.off($xeTreeSelect, "resize");
			import_xe_utils.default.assign(reactData, createReactData());
			import_xe_utils.default.assign(internalData, createInternalData());
		});
		provide("$xeTreeSelect", $xeTreeSelect);
		$xeTreeSelect.renderVN = renderVN;
		return $xeTreeSelect;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/tree-select/index.js
var VxeTreeSelect = Object.assign({}, tree_select_default, { install(app) {
	app.component(tree_select_default.name, tree_select_default);
} });
dynamicApp.use(VxeTreeSelect);
VxeUI.component(tree_select_default);
var TreeSelect = VxeTreeSelect;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/watermark/src/util.js
var canvasEl = null;
var fontEl = null;
var fontCacheMaps = {};
function getMarkCanvas() {
	if (!canvasEl) {
		canvasEl = document.createElement("canvas");
		canvasEl.style.position = "absolute";
		canvasEl.style.top = "0";
		canvasEl.style.left = "0";
	}
	return canvasEl;
}
function removeMarkElement(elem) {
	if (elem) {
		const parentEl = elem.parentNode;
		if (parentEl) parentEl.removeChild(elem);
	}
}
function calcFontWH(text, fontSize) {
	const fKey = `${fontSize}_${text}`;
	if (!fontCacheMaps[fKey]) {
		if (!fontEl) fontEl = document.createElement("span");
		if (!fontEl.parentNode) document.body.append(fontEl);
		fontEl.textContent = text;
		fontEl.style.fontSize = toCssUnit(fontSize);
		fontCacheMaps[fKey] = {
			width: fontEl.offsetWidth,
			height: fontEl.offsetHeight
		};
	}
	return fontCacheMaps[fKey];
}
function calcContentWH(contList) {
	let contentWidth = 0;
	let contentHeight = 0;
	contList.forEach((item) => {
		contentWidth = Math.max(item.width, contentWidth);
		contentHeight = Math.max(item.height, contentHeight);
	});
	return {
		contentWidth,
		contentHeight
	};
}
function calcCanvasWH(contentWidth, opts) {
	const { gap } = opts;
	const [gapX = 0, gapY = 0] = gap ? import_xe_utils.default.isArray(gap) ? gap : [gap, gap] : [];
	return {
		canvasWidth: contentWidth + import_xe_utils.default.toNumber(gapX),
		canvasHeight: contentWidth + import_xe_utils.default.toNumber(gapY)
	};
}
function getFontConf(item, key, opts) {
	return (item.font ? item.font[key] : "") || (opts.font ? opts.font[key] : "");
}
function createMarkFont(contConf, defaultFontSize, opts) {
	const { offset } = opts;
	const text = import_xe_utils.default.toValueString(contConf.textContent);
	const fontSize = import_xe_utils.default.toNumber(getFontConf(contConf, "fontSize", opts) || defaultFontSize) || 14;
	const [offsetX = 0, offsetY = 0] = offset ? import_xe_utils.default.isArray(offset) ? offset : [offset, offset] : [];
	const { width, height } = calcFontWH(text, fontSize);
	return {
		text,
		fontSize,
		font: contConf.font,
		width: width + import_xe_utils.default.toNumber(offsetX),
		height: height + import_xe_utils.default.toNumber(offsetY)
	};
}
function drayFont(ctx, item, opts) {
	const fontWeight = getFontConf(item, "fontWeight", opts);
	ctx.fillStyle = `${getFontConf(item, "color", opts) || "rgba(0, 0, 0, 0.15)"}`;
	ctx.font = [
		getFontConf(item, "fontStyle", opts) || "normal",
		fontWeight === "bold" || fontWeight === "bolder" ? "bold" : "",
		toCssUnit(item.fontSize),
		getFontConf(item, "fontFamily", opts) || "sans-serif"
	].join(" ");
}
function getContentUrl(content, defaultFontSize, options) {
	const opts = Object.assign({}, options);
	const { rotate } = opts;
	const deg = import_xe_utils.default.toNumber(rotate);
	const contList = (import_xe_utils.default.isArray(content) ? content : [content]).map((item) => {
		if (item) {
			if (item.textContent) return createMarkFont(item, defaultFontSize, opts);
			return createMarkFont({ textContent: `${item}` }, defaultFontSize, opts);
		}
		return createMarkFont({ textContent: "" }, defaultFontSize, opts);
	});
	removeMarkElement(fontEl);
	return new Promise((resolve) => {
		const canvasEl = getMarkCanvas();
		if (!canvasEl.parentNode) document.body.append(canvasEl);
		const ctx = canvasEl.getContext("2d");
		if (ctx && contList.length) {
			const { contentWidth, contentHeight } = calcContentWH(contList);
			const { canvasWidth, canvasHeight } = calcCanvasWH(contentWidth, opts);
			canvasEl.width = canvasWidth;
			canvasEl.height = canvasHeight;
			const x = (canvasWidth - contentWidth) / 2;
			const y = (canvasHeight - contentHeight) / 2;
			const drayX = x + contentWidth / 2;
			const drayY = y + contentHeight / 2;
			ctx.save();
			ctx.translate(drayX, drayY);
			ctx.rotate(deg * Math.PI / 180);
			ctx.translate(-drayX, -drayY);
			let offsetHeight = 0;
			contList.forEach((item) => {
				const align = getFontConf(item, "align", opts);
				drayFont(ctx, item, opts);
				ctx.fillText(item.text, x + (align === "center" ? (contentWidth - item.width) / 2 : 0), y + (contentHeight + contentHeight) / 2 + offsetHeight, contentWidth);
				offsetHeight += item.height;
			});
			ctx.restore();
			resolve(canvasEl.toDataURL());
			removeMarkElement(canvasEl);
		} else {
			resolve("");
			removeMarkElement(canvasEl);
		}
	});
}
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/watermark/src/watermark.js
var watermark_default = defineVxeComponent({
	name: "VxeWatermark",
	props: {
		width: String,
		height: String,
		imageUrl: String,
		rotate: {
			type: [Number, String],
			default: () => getConfig().watermark.rotate
		},
		gap: {
			type: [
				Array,
				Number,
				String
			],
			default: () => import_xe_utils.default.clone(getConfig().watermark.gap, true)
		},
		content: [String, Array],
		font: Object,
		offset: Object,
		zIndex: [String, Number]
	},
	emits: [],
	setup(props, context) {
		const { emit } = context;
		const xID = import_xe_utils.default.uniqueId();
		const refElem = ref();
		const reactData = reactive({ markUrl: "" });
		const refMaps = { refElem };
		const computeFontOpts = computed(() => {
			return import_xe_utils.default.assign({}, import_xe_utils.default.clone(getConfig().watermark.font, true), props.font);
		});
		const computeWrapperStyle = computed(() => {
			const { width, height, zIndex } = props;
			const { markUrl } = reactData;
			const stys = {};
			if (width) stys.width = toCssUnit(width);
			if (height) stys.height = toCssUnit(height);
			if (markUrl) stys.backgroundImage = `url(${markUrl})`;
			if (zIndex) stys.zIndex = zIndex;
			return stys;
		});
		const computeMaps = {};
		const $xeWatermark = {
			xID,
			props,
			context,
			reactData,
			getRefMaps: () => refMaps,
			getComputeMaps: () => computeMaps
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $watermark: $xeWatermark }, params));
		};
		const collapsePaneMethods = { dispatchEvent };
		const updateMarkStyle = () => {
			const { content, gap, rotate, offset } = props;
			const el = refElem.value;
			const fontOpts = computeFontOpts.value;
			if (el) {
				if (content) getContentUrl(content, getComputedStyle(el).fontSize, {
					font: fontOpts,
					rotate,
					gap,
					offset
				}).then((url) => {
					reactData.markUrl = url;
				});
			}
		};
		Object.assign($xeWatermark, collapsePaneMethods, {});
		const renderVN = () => {
			const wrapperStyle = computeWrapperStyle.value;
			return h("div", {
				ref: refElem,
				class: "vxe-watermark",
				style: wrapperStyle
			});
		};
		watch(() => props.imageUrl, () => {
			updateMarkStyle();
		});
		watch(() => props.content, () => {
			updateMarkStyle();
		});
		watch(() => props.gap, () => {
			updateMarkStyle();
		});
		watch(() => props.rotate, () => {
			updateMarkStyle();
		});
		watch(() => props.width, () => {
			updateMarkStyle();
		});
		watch(() => props.height, () => {
			updateMarkStyle();
		});
		watch(() => props.font, () => {
			updateMarkStyle();
		});
		onMounted(() => {
			updateMarkStyle();
			globalEvents.on($xeWatermark, "resize", import_xe_utils.default.throttle(() => {
				updateMarkStyle();
			}, 300, {
				trailing: true,
				leading: true
			}));
		});
		onUnmounted(() => {
			globalEvents.off($xeWatermark, "resize");
		});
		$xeWatermark.renderVN = renderVN;
		return $xeWatermark;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/watermark/index.js
var VxeWatermark = Object.assign({}, watermark_default, { install(app) {
	app.component(watermark_default.name, watermark_default);
} });
dynamicApp.use(VxeWatermark);
VxeUI.component(watermark_default);
var WatermarkController = {
	load(options) {
		checkDynamic();
		dynamicStore.globalWatermark = Object.assign({}, options);
		return Promise.resolve();
	},
	clear() {
		dynamicStore.globalWatermark = null;
		return Promise.resolve();
	}
};
VxeUI.watermark = WatermarkController;
var Watermark = VxeWatermark;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/components.js
var components_exports = /* @__PURE__ */ __exportAll({
	Alert: () => Alert,
	Anchor: () => Anchor,
	AnchorLink: () => AnchorLink,
	Avatar: () => Avatar,
	Backtop: () => Backtop,
	Badge: () => Badge,
	Breadcrumb: () => Breadcrumb,
	BreadcrumbItem: () => BreadcrumbItem,
	Button: () => Button,
	ButtonGroup: () => ButtonGroup,
	Calendar: () => Calendar,
	Card: () => Card,
	Carousel: () => Carousel,
	CarouselItem: () => CarouselItem,
	Cascader: () => Cascader,
	Checkbox: () => Checkbox,
	CheckboxButton: () => CheckboxButton,
	CheckboxGroup: () => CheckboxGroup,
	Col: () => Col,
	Collapse: () => Collapse,
	CollapsePane: () => CollapsePane,
	ColorPicker: () => ColorPicker,
	ContextMenu: () => ContextMenu,
	ContextMenuController: () => ContextMenuController,
	Countdown: () => Countdown,
	DatePanel: () => DatePanel,
	DatePicker: () => DatePicker,
	DateRangePicker: () => DateRangePicker,
	Drawer: () => Drawer,
	DrawerController: () => DrawerController,
	Empty: () => Empty,
	Form: () => Form,
	FormGather: () => FormGather,
	FormGroup: () => FormGroup,
	FormItem: () => FormItem,
	GLOBAL_EVENT_KEYS: () => GLOBAL_EVENT_KEYS,
	Icon: () => Icon,
	IconPicker: () => IconPicker,
	Image: () => Image,
	ImageGroup: () => ImageGroup,
	ImagePreview: () => ImagePreview,
	Input: () => Input,
	LayoutAside: () => LayoutAside,
	LayoutBody: () => LayoutBody,
	LayoutContainer: () => LayoutContainer,
	LayoutFooter: () => LayoutFooter,
	LayoutHeader: () => LayoutHeader,
	Link: () => Link,
	List: () => List,
	Loading: () => Loading,
	LoadingController: () => LoadingController,
	Menu: () => Menu,
	Modal: () => Modal,
	ModalController: () => ModalController,
	NoticeBar: () => NoticeBar,
	NumberInput: () => NumberInput,
	Optgroup: () => Optgroup,
	Option: () => Option,
	Pager: () => Pager,
	PasswordInput: () => PasswordInput,
	Print: () => Print,
	PrintPageBreak: () => PrintPageBreak,
	Pulldown: () => Pulldown,
	Radio: () => Radio,
	RadioButton: () => RadioButton,
	RadioGroup: () => RadioGroup,
	Rate: () => Rate,
	Result: () => Result,
	Row: () => Row,
	Segmented: () => Segmented,
	Select: () => Select,
	Slider: () => Slider,
	Split: () => Split,
	SplitPane: () => SplitPane,
	Splitter: () => Splitter,
	SplitterPanel: () => SplitterPanel,
	Steps: () => Steps,
	Switch: () => Switch,
	TabPane: () => TabPane,
	TableSelect: () => TableSelect,
	Tabs: () => Tabs,
	Tag: () => Tag,
	Text: () => Text,
	TextEllipsis: () => TextEllipsis,
	Textarea: () => Textarea,
	Timeline: () => Timeline,
	TimelineItem: () => TimelineItem,
	Tip: () => Tip,
	Tips: () => Tips,
	Tooltip: () => Tooltip,
	Tour: () => Tour,
	Tree: () => Tree,
	TreeSelect: () => TreeSelect,
	Upload: () => Upload,
	VxeAlert: () => VxeAlert,
	VxeAnchor: () => VxeAnchor,
	VxeAnchorLink: () => VxeAnchorLink,
	VxeAvatar: () => VxeAvatar,
	VxeBacktop: () => VxeBacktop,
	VxeBadge: () => VxeBadge,
	VxeBreadcrumb: () => VxeBreadcrumb,
	VxeBreadcrumbItem: () => VxeBreadcrumbItem,
	VxeButton: () => VxeButton,
	VxeButtonGroup: () => VxeButtonGroup,
	VxeCalendar: () => VxeCalendar,
	VxeCard: () => VxeCard,
	VxeCarousel: () => VxeCarousel,
	VxeCarouselItem: () => VxeCarouselItem,
	VxeCascader: () => VxeCascader,
	VxeCheckbox: () => VxeCheckbox,
	VxeCheckboxButton: () => VxeCheckboxButton,
	VxeCheckboxGroup: () => VxeCheckboxGroup,
	VxeCol: () => VxeCol,
	VxeCollapse: () => VxeCollapse,
	VxeCollapsePane: () => VxeCollapsePane,
	VxeColorPicker: () => VxeColorPicker,
	VxeContextMenu: () => VxeContextMenu,
	VxeCore: () => VxeCore,
	VxeCountdown: () => VxeCountdown,
	VxeDatePanel: () => VxeDatePanel,
	VxeDatePicker: () => VxeDatePicker,
	VxeDateRangePicker: () => VxeDateRangePicker,
	VxeDrawer: () => VxeDrawer,
	VxeEmpty: () => VxeEmpty,
	VxeForm: () => VxeForm,
	VxeFormGather: () => VxeFormGather,
	VxeFormGroup: () => VxeFormGroup,
	VxeFormItem: () => VxeFormItem,
	VxeIcon: () => VxeIcon,
	VxeIconPicker: () => VxeIconPicker,
	VxeImage: () => VxeImage,
	VxeImageGroup: () => VxeImageGroup,
	VxeImagePreview: () => VxeImagePreview,
	VxeInput: () => VxeInput,
	VxeLayoutAside: () => VxeLayoutAside,
	VxeLayoutBody: () => VxeLayoutBody,
	VxeLayoutContainer: () => VxeLayoutContainer,
	VxeLayoutFooter: () => VxeLayoutFooter,
	VxeLayoutHeader: () => VxeLayoutHeader,
	VxeLink: () => VxeLink,
	VxeList: () => VxeList,
	VxeLoading: () => VxeLoading,
	VxeMenu: () => VxeMenu,
	VxeModal: () => VxeModal,
	VxeNoticeBar: () => VxeNoticeBar,
	VxeNumberInput: () => VxeNumberInput,
	VxeOptgroup: () => VxeOptgroup,
	VxeOption: () => VxeOption,
	VxePager: () => VxePager,
	VxePasswordInput: () => VxePasswordInput,
	VxePrint: () => VxePrint,
	VxePrintPageBreak: () => VxePrintPageBreak,
	VxePulldown: () => VxePulldown,
	VxeRadio: () => VxeRadio,
	VxeRadioButton: () => VxeRadioButton,
	VxeRadioGroup: () => VxeRadioGroup,
	VxeRate: () => VxeRate,
	VxeResult: () => VxeResult,
	VxeRow: () => VxeRow,
	VxeSegmented: () => VxeSegmented,
	VxeSelect: () => VxeSelect,
	VxeSlider: () => VxeSlider,
	VxeSplit: () => VxeSplit,
	VxeSplitPane: () => VxeSplitPane,
	VxeSplitter: () => VxeSplitter,
	VxeSplitterPanel: () => VxeSplitterPanel,
	VxeSteps: () => VxeSteps,
	VxeSwitch: () => VxeSwitch,
	VxeTabPane: () => VxeTabPane,
	VxeTableSelect: () => VxeTableSelect,
	VxeTabs: () => VxeTabs,
	VxeTag: () => VxeTag,
	VxeText: () => VxeText,
	VxeTextEllipsis: () => VxeTextEllipsis,
	VxeTextarea: () => VxeTextarea,
	VxeTimeline: () => VxeTimeline,
	VxeTimelineItem: () => VxeTimelineItem,
	VxeTip: () => VxeTip,
	VxeTooltip: () => VxeTooltip,
	VxeTour: () => VxeTour,
	VxeTree: () => VxeTree,
	VxeTreeSelect: () => VxeTreeSelect,
	VxeUI: () => VxeUI,
	VxeUpload: () => VxeUpload,
	VxeWatermark: () => VxeWatermark,
	Watermark: () => Watermark,
	WatermarkController: () => WatermarkController,
	checkVersion: () => checkVersion,
	clipboard: () => clipboard,
	commands: () => commands,
	component: () => component,
	config: () => config,
	coreVersion: () => coreVersion,
	createEvent: () => createEvent,
	drawer: () => drawer,
	formats: () => formats,
	getComponent: () => getComponent,
	getConfig: () => getConfig,
	getI18n: () => getI18n,
	getIcon: () => getIcon,
	getLanguage: () => getLanguage,
	getSlotVNs: () => getSlotVNs,
	getTheme: () => getTheme,
	globalEvents: () => globalEvents,
	globalResize: () => globalResize,
	globalStore: () => globalStore,
	handleCheckInfo: () => handleCheckInfo,
	hasComponent: () => hasComponent,
	hasLanguage: () => hasLanguage,
	hooks: () => hooks,
	install: () => install,
	interceptor: () => interceptor,
	loading: () => loading,
	log: () => log,
	menus: () => menus,
	modal: () => modal,
	permission: () => permission,
	print: () => print,
	readFile: () => readFile,
	renderCustomIcon: () => renderCustomIcon,
	renderEmptyElement: () => renderEmptyElement,
	renderGlobalIcon: () => renderGlobalIcon,
	renderer: () => renderer,
	saveFile: () => saveFile,
	setConfig: () => setConfig,
	setI18n: () => setI18n,
	setIcon: () => setIcon,
	setLanguage: () => setLanguage,
	setTheme: () => setTheme,
	setup: () => setup,
	use: () => use,
	useFns: () => useFns,
	usePermission: () => usePermission,
	useSize: () => useSize,
	validators: () => validators,
	version: () => version,
	watermark: () => watermark
});
var { setI18n: setI18n$1, setLanguage: setLanguage$1, setTheme: setTheme$1, setConfig: setConfig$1 } = VxeUI;
var components = [
	VxeAlert,
	VxeAnchor,
	VxeAnchorLink,
	VxeAvatar,
	VxeBacktop,
	VxeBadge,
	VxeBreadcrumb,
	VxeBreadcrumbItem,
	VxeButton,
	VxeButtonGroup,
	VxeCalendar,
	VxeCard,
	VxeCarousel,
	VxeCarouselItem,
	VxeCascader,
	VxeCheckbox,
	VxeCheckboxButton,
	VxeCheckboxGroup,
	VxeCol,
	VxeCollapse,
	VxeCollapsePane,
	VxeColorPicker,
	VxeContextMenu,
	VxeCountdown,
	VxeDatePanel,
	VxeDatePicker,
	VxeDateRangePicker,
	VxeDrawer,
	VxeEmpty,
	VxeForm,
	VxeFormGather,
	VxeFormGroup,
	VxeFormItem,
	VxeIcon,
	VxeIconPicker,
	VxeImage,
	VxeImageGroup,
	VxeImagePreview,
	VxeInput,
	VxeLayoutAside,
	VxeLayoutBody,
	VxeLayoutContainer,
	VxeLayoutFooter,
	VxeLayoutHeader,
	VxeLink,
	VxeList,
	VxeLoading,
	VxeMenu,
	VxeModal,
	VxeNoticeBar,
	VxeNumberInput,
	VxeOptgroup,
	VxeOption,
	VxePager,
	VxePasswordInput,
	VxePrintPageBreak,
	VxePrint,
	VxePulldown,
	VxeRadio,
	VxeRadioButton,
	VxeRadioGroup,
	VxeRate,
	VxeResult,
	VxeRow,
	VxeSegmented,
	VxeSelect,
	VxeSplitter,
	VxeSplitterPanel,
	VxeSplit,
	VxeSplitPane,
	VxeSlider,
	VxeSteps,
	VxeSwitch,
	VxeTabPane,
	VxeTableSelect,
	VxeTabs,
	VxeTag,
	VxeTextEllipsis,
	VxeText,
	VxeTextarea,
	VxeTimeline,
	VxeTimelineItem,
	VxeTip,
	VxeTooltip,
	VxeTour,
	VxeTree,
	VxeTreeSelect,
	VxeUpload,
	VxeWatermark
];
function install(app, options) {
	setConfig$1(options);
	components.forEach((component) => app.use(component));
}
var defaultLanguage = "zh-CN";
setI18n$1(defaultLanguage, zh_CN_default);
setLanguage$1(defaultLanguage);
setTheme$1("light");
var loading = LoadingController;
var modal = ModalController;
var drawer = DrawerController;
var watermark = WatermarkController;
var print = printHtml;
var saveFile = saveLocalFile;
var readFile = readLocalFile;
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/index.esm.js
var index_esm_default = components_exports;
//#endregion
export { Alert, Anchor, AnchorLink, Avatar, Backtop, Badge, Breadcrumb, BreadcrumbItem, Button, ButtonGroup, Calendar, Card, Carousel, CarouselItem, Cascader, Checkbox, CheckboxButton, CheckboxGroup, Col, Collapse, CollapsePane, ColorPicker, ContextMenu, ContextMenuController, Countdown, DatePanel, DatePicker, DateRangePicker, Drawer, DrawerController, Empty, Form, FormGather, FormGroup, FormItem, GLOBAL_EVENT_KEYS, Icon, IconPicker, Image, ImageGroup, ImagePreview, Input, LayoutAside, LayoutBody, LayoutContainer, LayoutFooter, LayoutHeader, Link, List, Loading, LoadingController, Menu, Modal, ModalController, NoticeBar, NumberInput, Optgroup, Option, Pager, PasswordInput, Print, PrintPageBreak, Pulldown, Radio, RadioButton, RadioGroup, Rate, Result, Row, Segmented, Select, Slider, Split, SplitPane, Splitter, SplitterPanel, Steps, Switch, TabPane, TableSelect, Tabs, Tag, Text, TextEllipsis, Textarea, Timeline, TimelineItem, Tip, Tips, Tooltip, Tour, Tree, TreeSelect, Upload, VxeAlert, VxeAnchor, VxeAnchorLink, VxeAvatar, VxeBacktop, VxeBadge, VxeBreadcrumb, VxeBreadcrumbItem, VxeButton, VxeButtonGroup, VxeCalendar, VxeCard, VxeCarousel, VxeCarouselItem, VxeCascader, VxeCheckbox, VxeCheckboxButton, VxeCheckboxGroup, VxeCol, VxeCollapse, VxeCollapsePane, VxeColorPicker, VxeContextMenu, VxeCore, VxeCountdown, VxeDatePanel, VxeDatePicker, VxeDateRangePicker, VxeDrawer, VxeEmpty, VxeForm, VxeFormGather, VxeFormGroup, VxeFormItem, VxeIcon, VxeIconPicker, VxeImage, VxeImageGroup, VxeImagePreview, VxeInput, VxeLayoutAside, VxeLayoutBody, VxeLayoutContainer, VxeLayoutFooter, VxeLayoutHeader, VxeLink, VxeList, VxeLoading, VxeMenu, VxeModal, VxeNoticeBar, VxeNumberInput, VxeOptgroup, VxeOption, VxePager, VxePasswordInput, VxePrint, VxePrintPageBreak, VxePulldown, VxeRadio, VxeRadioButton, VxeRadioGroup, VxeRate, VxeResult, VxeRow, VxeSegmented, VxeSelect, VxeSlider, VxeSplit, VxeSplitPane, VxeSplitter, VxeSplitterPanel, VxeSteps, VxeSwitch, VxeTabPane, VxeTableSelect, VxeTabs, VxeTag, VxeText, VxeTextEllipsis, VxeTextarea, VxeTimeline, VxeTimelineItem, VxeTip, VxeTooltip, VxeTour, VxeTree, VxeTreeSelect, VxeUI, VxeUpload, VxeWatermark, Watermark, WatermarkController, checkVersion, clipboard, commands, component, config, coreVersion, createEvent, index_esm_default as default, drawer, formats, getComponent, getConfig, getI18n, getIcon, getLanguage, getSlotVNs, getTheme, globalEvents, globalResize, globalStore, handleCheckInfo, hasComponent, hasLanguage, hooks, install, interceptor, loading, log, menus, modal, permission, print, readFile, renderCustomIcon, renderEmptyElement, renderGlobalIcon, renderer, saveFile, setConfig, setI18n, setIcon, setLanguage, setTheme, setup, use, useFns, usePermission, useSize, validators, version, watermark };

//# sourceMappingURL=vxe-pc-ui.js.map