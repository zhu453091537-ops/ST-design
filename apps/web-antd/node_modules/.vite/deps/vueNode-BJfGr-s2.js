import { H as cloneVNode, N as Fragment, R as Text, Tt as isVNode, mt as h } from "./runtime-dom.esm-bundler-BEW9uiVc.js";
import "./vue.js";
import { u as filterEmpty } from "./dist-B3YnmQlL.js";
import "./isFragment-Bdt2ylnq.js";
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/_util/vueNode.js
function replaceElement(element, replacement, props) {
	if (!isVNode(element)) return replacement;
	return cloneVNode(element, typeof props === "function" ? props(element.props || {}) : props);
}
function cloneElement(element, props) {
	return replaceElement(element, element, props);
}
function getVNode(node) {
	if (typeof node === "function") return node?.();
	return node;
}
function checkRenderNode(node) {
	if (!node) return;
	const pureChild = filterEmpty(Array.isArray(node) ? node : [node]);
	if (pureChild.length > 0) if (pureChild.length === 1) return pureChild[0];
	else return h(Fragment, null, pureChild);
}
function getTextByNode(node) {
	if (isVNode(node) && node.type === Text) return node.children;
	else if (typeof node === "object" && node !== null && (typeof node.children === "string" || typeof node.children === "number")) return node.children;
	return node;
}
function getTextNodeArr(nodes) {
	const res = [];
	for (let i = 0; i < nodes.length; i++) {
		const node = nodes[i];
		res.push(getTextByNode(node));
	}
	return res;
}
//#endregion
export { getVNode as a, getTextNodeArr as i, cloneElement as n, replaceElement as o, getTextByNode as r, checkRenderNode as t };

//# sourceMappingURL=vueNode-BJfGr-s2.js.map