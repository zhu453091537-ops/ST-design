import { Et as mergeDefaults, N as Fragment, Ot as mergeProps, Tt as isVNode, W as computed, Xn as shallowRef, _n as watch, rt as defineComponent, tt as createVNode } from "./runtime-dom.esm-bundler-BEW9uiVc.js";
import "./vue.js";
import { _ as toPropsRefs, b as KeyCodeStr, c as clsx, u as filterEmpty, v as omit } from "./dist-B3YnmQlL.js";
import { t as CloseCircleFilled } from "./CloseCircleFilled-BuZIJRtS.js";
import { t as triggerFocus } from "./focus-BmKn4U6i.js";
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/_util/getAllowClear.js
function getAllowClear(allowClear) {
	let mergedAllowClear;
	if (typeof allowClear === "object" && allowClear?.clearIcon) mergedAllowClear = allowClear;
	else if (allowClear) mergedAllowClear = { clearIcon: createVNode(CloseCircleFilled, null, null) };
	return mergedAllowClear;
}
var getAllowClear_default = getAllowClear;
//#endregion
//#region ../../node_modules/.pnpm/@v-c+input@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/input/dist/utils/commonUtils.js
function createPatchedTarget(target, value) {
	const patched = target.cloneNode(true);
	patched.value = value;
	if (typeof target.selectionStart === "number" && typeof target.selectionEnd === "number") {
		patched.selectionStart = target.selectionStart;
		patched.selectionEnd = target.selectionEnd;
	}
	patched.setSelectionRange = (start, end, direction) => {
		target.setSelectionRange(start, end, direction);
	};
	return patched;
}
function cloneEvent(event, target, value) {
	const patchedTarget = createPatchedTarget(target, value);
	return {
		type: event?.type,
		timeStamp: event?.timeStamp,
		bubbles: event?.bubbles,
		cancelable: event?.cancelable,
		composed: event?.composed,
		target: patchedTarget,
		currentTarget: patchedTarget,
		preventDefault: event?.preventDefault ? event.preventDefault.bind(event) : void 0,
		stopPropagation: event?.stopPropagation ? event.stopPropagation.bind(event) : void 0,
		stopImmediatePropagation: event?.stopImmediatePropagation ? event.stopImmediatePropagation.bind(event) : void 0,
		nativeEvent: event
	};
}
function hasAddon(props) {
	return !!(props.addonBefore || props.addonAfter);
}
function hasPrefixSuffix(props) {
	return !!(props.prefix || props.suffix || props.allowClear);
}
function resolveOnChange(target, e, onChange, targetValue) {
	if (!onChange) return;
	if (e?.type === "click") {
		onChange(cloneEvent(e, target, ""));
		return;
	}
	if (target.type !== "file" && targetValue !== void 0) {
		onChange(cloneEvent(e, target, targetValue));
		return;
	}
	onChange(e);
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+input@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/input/dist/BaseInput.js
function _isSlot$1(s) {
	return typeof s === "function" || Object.prototype.toString.call(s) === "[object Object]" && !isVNode(s);
}
var BaseInput = /* @__PURE__ */ defineComponent((props, { slots, expose, attrs }) => {
	const containerRef = shallowRef();
	const onInputClick = (e) => {
		if (containerRef.value?.contains(e.target)) props?.triggerFocus?.();
	};
	const hasAffix = computed(() => hasPrefixSuffix(props));
	const groupRef = shallowRef();
	expose({ nativeElement: computed(() => groupRef.value || containerRef.value) });
	return () => {
		const { components, allowClear, readOnly, disabled, value, prefixCls, handleReset, onClear, suffix, focused, classNames, styles, dataAttrs, prefix, addonAfter, addonBefore, hidden } = props;
		let children = filterEmpty(slots?.default?.() ?? []);
		if (children.length === 1) children = children[0];
		else children = createVNode(Fragment, null, children);
		const inputElement = children;
		const AffixWrapperComponent = components?.affixWrapper || "span";
		const GroupWrapperComponent = components?.groupWrapper || "span";
		const WrapperComponent = components?.wrapper || "span";
		const GroupAddonComponent = components?.groupAddon || "span";
		let element = createVNode(inputElement, {
			value,
			class: !hasAffix.value && classNames?.variant
		});
		if (hasAffix.value) {
			let clearIcon = null;
			if (allowClear) {
				const needClear = !disabled && !readOnly && value;
				const clearIconCls = `${prefixCls}-clear-icon`;
				const iconNode = typeof allowClear === "object" && allowClear?.clearIcon ? allowClear.clearIcon : "✖";
				clearIcon = createVNode("button", {
					"type": "button",
					"tabindex": -1,
					"onClick": (event) => {
						handleReset?.(event);
						onClear?.();
					},
					"onMousedown": (e) => e.preventDefault(),
					"class": clsx(clearIconCls, {
						[`${clearIconCls}-hidden`]: !needClear,
						[`${clearIconCls}-has-suffix`]: !!suffix
					})
				}, [iconNode]);
			}
			const affixWrapperPrefixCls = `${prefixCls}-affix-wrapper`;
			const affixWrapperCls = clsx(affixWrapperPrefixCls, {
				[`${prefixCls}-disabled`]: disabled,
				[`${affixWrapperPrefixCls}-disabled`]: disabled,
				[`${affixWrapperPrefixCls}-focused`]: focused,
				[`${affixWrapperPrefixCls}-readonly`]: readOnly,
				[`${affixWrapperPrefixCls}-input-with-clear-btn`]: suffix && allowClear && value
			}, classNames?.affixWrapper, classNames?.variant);
			const suffixNode = (suffix || allowClear) && createVNode("span", {
				"class": clsx(`${prefixCls}-suffix`, classNames?.suffix),
				"style": styles?.suffix
			}, [clearIcon, suffix]);
			const _element = function() {
				return element;
			}();
			element = createVNode(AffixWrapperComponent, mergeProps({
				"class": affixWrapperCls,
				"style": styles?.affixWrapper,
				"onClick": onInputClick
			}, dataAttrs?.affixWrapper, { "ref": containerRef }), { default: () => [
				prefix && createVNode("span", {
					"class": clsx(`${prefixCls}-prefix`, classNames?.prefix),
					"style": styles?.prefix
				}, [prefix]),
				_element,
				suffixNode
			] });
		}
		if (hasAddon(props)) {
			const wrapperCls = `${prefixCls}-group`;
			const addonCls = `${wrapperCls}-addon`;
			const groupWrapperCls = `${wrapperCls}-wrapper`;
			const mergedWrapperClassName = clsx(`${prefixCls}-wrapper`, wrapperCls, classNames?.wrapper);
			element = createVNode(GroupWrapperComponent, {
				"class": clsx(groupWrapperCls, { [`${groupWrapperCls}-disabled`]: disabled }, classNames?.groupWrapper),
				"ref": groupRef
			}, { default: () => [createVNode(WrapperComponent, { "class": mergedWrapperClassName }, { default: () => [
				addonBefore && createVNode(GroupAddonComponent, { "class": addonCls }, _isSlot$1(addonBefore) ? addonBefore : { default: () => [addonBefore] }),
				element,
				addonAfter && createVNode(GroupAddonComponent, { "class": addonCls }, _isSlot$1(addonAfter) ? addonAfter : { default: () => [addonAfter] })
			] })] });
		}
		return createVNode(element, {
			...attrs,
			hidden
		});
	};
}, {
	props: {
		value: {
			required: false,
			default: void 0
		},
		prefixCls: {
			type: String,
			required: false,
			default: void 0
		},
		disabled: {
			type: Boolean,
			required: false,
			default: void 0
		},
		focused: {
			type: Boolean,
			required: false,
			default: void 0
		},
		triggerFocus: {
			type: Function,
			required: false,
			default: void 0
		},
		readOnly: {
			type: Boolean,
			required: false,
			default: void 0
		},
		handleReset: {
			type: Function,
			required: false,
			default: void 0
		},
		onClear: {
			type: Function,
			required: false,
			default: void 0
		},
		hidden: {
			type: Boolean,
			required: false,
			default: void 0
		},
		dataAttrs: {
			type: Object,
			required: false,
			default: void 0
		},
		components: {
			type: Object,
			required: false,
			default: void 0
		},
		prefix: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		suffix: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		addonBefore: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		addonAfter: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		classes: {
			type: Object,
			required: false,
			default: void 0
		},
		classNames: {
			type: Object,
			required: false,
			default: void 0
		},
		styles: {
			type: Object,
			required: false,
			default: void 0
		},
		allowClear: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		}
	},
	name: "BaseInput",
	inheritAttrs: false
});
//#endregion
//#region ../../node_modules/.pnpm/@v-c+input@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/input/dist/hooks/useCount.js
function useCount(count, showCount) {
	return computed(() => {
		let mergedConfig = {};
		if (showCount?.value) mergedConfig.show = typeof showCount.value === "object" && showCount.value?.formatter ? showCount.value?.formatter : !!showCount.value;
		mergedConfig = {
			...mergedConfig,
			...count?.value
		};
		const { show, ...rest } = mergedConfig;
		return {
			...rest,
			show: !!show,
			showFormatter: typeof show === "function" ? show : void 0,
			strategy: rest.strategy || ((value) => value.length)
		};
	});
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+input@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/input/dist/input.js
function _isSlot(s) {
	return typeof s === "function" || Object.prototype.toString.call(s) === "[object Object]" && !isVNode(s);
}
var defaults = {
	prefixCls: "vc-input",
	type: "text"
};
//#endregion
//#region ../../node_modules/.pnpm/@v-c+input@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/input/dist/index.js
var src_default = /* @__PURE__ */ defineComponent((props, { slots, expose, attrs }) => {
	const focused = shallowRef(false);
	const compositionRef = shallowRef(false);
	const keyLockRef = shallowRef(false);
	const compositionEndValueRef = shallowRef(null);
	const { count, showCount } = toPropsRefs(props, "count", "showCount");
	const onChange = (e) => {
		props?.onChange?.(e);
	};
	const inputRef = shallowRef();
	const holderRef = shallowRef();
	const focus = (option) => {
		if (inputRef.value) triggerFocus(inputRef.value, option);
	};
	const value = shallowRef(props?.value ?? props?.defaultValue);
	watch(() => props.value, (newValue) => {
		value.value = newValue;
	});
	const formatValue = computed(() => value.value === void 0 || value.value === null ? "" : String(value.value));
	const selection = shallowRef(null);
	watch(selection, (newSelection) => {
		if (newSelection && inputRef.value) inputRef.value.setSelectionRange(...newSelection);
	});
	const countConfig = useCount(count, showCount);
	const mergedMax = computed(() => countConfig?.value?.max || props?.maxLength);
	const valueLength = computed(() => countConfig.value?.strategy?.(formatValue.value) ?? 0);
	const isOutOfRange = computed(() => !!mergedMax.value && valueLength.value > mergedMax.value);
	expose({
		focus,
		blur: () => {
			inputRef.value?.blur?.();
		},
		setSelectionRange: (start, end, direction) => {
			inputRef.value?.setSelectionRange(start, end, direction);
		},
		select: () => {
			inputRef.value?.select();
		},
		input: inputRef,
		nativeElement: computed(() => holderRef.value?.nativeElement || inputRef.value)
	});
	watch(() => props.disabled, () => {
		if (keyLockRef.value) keyLockRef.value = false;
		focused.value = focused.value && props.disabled ? false : focused.value;
	}, { immediate: true });
	const triggerChange = (e, currentValue) => {
		if (compositionRef.value && !props.changeOnComposing) return;
		if (compositionEndValueRef.value !== null) {
			if (currentValue === compositionEndValueRef.value) return;
			compositionEndValueRef.value = null;
		}
		let cutValue = currentValue;
		const config = countConfig.value;
		if (!compositionRef.value && config?.exceedFormatter && config.max && config.strategy(currentValue) > config.max) {
			cutValue = config.exceedFormatter(currentValue, { max: config.max });
			if (currentValue !== cutValue) selection.value = [inputRef.value?.selectionStart || 0, inputRef.value?.selectionEnd || 0];
		}
		if (props.value === void 0) value.value = cutValue;
		if (inputRef.value) resolveOnChange(inputRef.value, e, onChange, cutValue);
	};
	const onInternalChange = (e) => {
		triggerChange(e, e.target.value);
	};
	const onInternalCompositionStart = (e) => {
		compositionRef.value = true;
		compositionEndValueRef.value = null;
		props?.onCompositionStart?.(e);
	};
	const onInternalCompositionEnd = (e) => {
		compositionRef.value = false;
		const currentValue = e.target.value;
		if (!props.changeOnComposing && currentValue !== formatValue.value) triggerChange(e, currentValue);
		if (!props.changeOnComposing) compositionEndValueRef.value = currentValue;
		props?.onCompositionEnd?.(e);
	};
	const handleKeyDown = (e) => {
		if (e.key === KeyCodeStr.Enter && !keyLockRef.value && !e.isComposing) {
			keyLockRef.value = true;
			props.onPressEnter?.(e);
		}
		props?.onKeyDown?.(e);
	};
	const handleKeyUp = (e) => {
		if (e.key === "Enter") keyLockRef.value = false;
		props?.onKeyUp?.(e);
	};
	const handleFocus = (e) => {
		focused.value = true;
		props?.onFocus?.(e);
	};
	const handleBlur = (e) => {
		if (keyLockRef.value) keyLockRef.value = false;
		focused.value = false;
		props?.onBlur?.(e);
	};
	const handleReset = (e) => {
		compositionEndValueRef.value = null;
		if (props.value === void 0) value.value = "";
		focus();
		if (inputRef.value) resolveOnChange(inputRef.value, e, onChange);
	};
	const mergedAllowClear = computed(() => {
		if (!props.allowClear) return props.allowClear;
		const clearIcon = slots.clearIcon?.();
		if (clearIcon) return {
			...typeof props.allowClear === "object" ? props.allowClear : {},
			clearIcon
		};
		return props.allowClear;
	});
	return () => {
		const { autoComplete, prefixCls = defaults.prefixCls, disabled, htmlSize, classNames, styles, suffix, type = defaults.type, classes, readOnly, hidden, dataAttrs, components } = props;
		const { class: className, style, ...restAttrs } = attrs;
		const mergedClassName = className ?? props.class;
		const mergedStyle = style ?? props.style;
		const prefixNode = slots.prefix?.() ?? props.prefix;
		const suffixNode = slots.suffix?.() ?? suffix;
		const addonBefore = slots.addonBefore?.() ?? props.addonBefore;
		const addonAfter = slots.addonAfter?.() ?? props.addonAfter;
		const config = countConfig.value;
		const hasMaxLength = Number(mergedMax.value) > 0;
		let mergedSuffix = suffixNode;
		if (suffixNode || config?.show) {
			const dataCount = config?.showFormatter ? config.showFormatter({
				value: formatValue.value,
				count: valueLength.value,
				maxLength: mergedMax.value
			}) : `${valueLength.value}${hasMaxLength ? ` / ${mergedMax.value}` : ""}`;
			mergedSuffix = createVNode(Fragment, null, [config?.show && createVNode("span", {
				"class": clsx(`${prefixCls}-show-count-suffix`, { [`${prefixCls}-show-count-has-suffix`]: !!suffixNode }, classNames?.count),
				"style": styles?.count
			}, [dataCount]), suffixNode]);
		}
		const inputElement = createVNode("input", mergeProps(restAttrs, omit(props, [
			"prefixCls",
			"onPressEnter",
			"addonBefore",
			"addonAfter",
			"prefix",
			"suffix",
			"allowClear",
			"defaultValue",
			"showCount",
			"count",
			"classes",
			"htmlSize",
			"styles",
			"classNames",
			"onClear",
			"dataAttrs",
			"components",
			"hidden",
			"readOnly",
			"value",
			"type",
			"class",
			"style",
			"onFocus",
			"onBlur",
			"onChange",
			"onKeyDown",
			"onKeyUp",
			"onCompositionStart",
			"onCompositionEnd",
			"onInput",
			"changeOnComposing"
		]), {
			"autocomplete": autoComplete,
			"ref": inputRef,
			"value": formatValue.value,
			"onInput": onInternalChange,
			"onFocus": handleFocus,
			"onBlur": handleBlur,
			"onKeydown": handleKeyDown,
			"onKeyup": handleKeyUp,
			"class": clsx(prefixCls, { [`${prefixCls}-disabled`]: disabled }, classNames?.input),
			"style": styles?.input,
			"size": htmlSize,
			"type": type,
			"maxlength": props.maxLength,
			"onCompositionstart": onInternalCompositionStart,
			"onCompositionend": onInternalCompositionEnd,
			"disabled": disabled,
			"readonly": readOnly
		}), null);
		return createVNode(BaseInput, {
			"ref": holderRef,
			"value": formatValue.value,
			"prefixCls": prefixCls,
			"class": clsx(mergedClassName, isOutOfRange.value && `${prefixCls}-out-of-range`),
			"style": mergedStyle,
			"allowClear": mergedAllowClear.value,
			"handleReset": handleReset,
			"suffix": mergedSuffix,
			"prefix": prefixNode,
			"addonBefore": addonBefore,
			"addonAfter": addonAfter,
			"focused": focused.value,
			"triggerFocus": focus,
			"disabled": disabled,
			"readOnly": readOnly,
			"classNames": classNames,
			"styles": styles,
			"dataAttrs": dataAttrs,
			"components": components,
			"hidden": hidden,
			"onClear": props.onClear,
			"classes": classes
		}, _isSlot(inputElement) ? inputElement : { default: () => [inputElement] });
	};
}, {
	props: /* @__PURE__ */ mergeDefaults({
		value: {
			required: false,
			default: void 0
		},
		defaultValue: {
			required: false,
			default: void 0
		},
		disabled: {
			type: Boolean,
			required: false,
			default: void 0
		},
		prefixCls: {
			type: String,
			required: false,
			default: void 0
		},
		type: {
			type: [String, Object],
			required: false,
			default: void 0
		},
		showCount: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		},
		onPressEnter: {
			type: Function,
			required: false,
			default: void 0
		},
		autoComplete: {
			type: String,
			required: false,
			default: void 0
		},
		htmlSize: {
			type: Number,
			required: false,
			default: void 0
		},
		placeholder: {
			type: String,
			required: false,
			default: void 0
		},
		classNames: {
			type: Object,
			required: false,
			default: void 0
		},
		styles: {
			type: Object,
			required: false,
			default: void 0
		},
		count: {
			type: Object,
			required: false,
			default: void 0
		},
		onClear: {
			type: Function,
			required: false,
			default: void 0
		},
		maxLength: {
			type: Number,
			required: false,
			default: void 0
		},
		readOnly: {
			type: Boolean,
			required: false,
			default: void 0
		},
		hidden: {
			type: Boolean,
			required: false,
			default: void 0
		},
		onChange: {
			type: Function,
			required: false,
			default: void 0
		},
		onFocus: {
			type: Function,
			required: false,
			default: void 0
		},
		onBlur: {
			type: Function,
			required: false,
			default: void 0
		},
		onKeyDown: {
			type: Function,
			required: false,
			default: void 0
		},
		onKeyUp: {
			type: Function,
			required: false,
			default: void 0
		},
		onCompositionStart: {
			type: Function,
			required: false,
			default: void 0
		},
		onCompositionEnd: {
			type: Function,
			required: false,
			default: void 0
		},
		changeOnComposing: {
			type: Boolean,
			required: false,
			default: void 0
		},
		components: {
			type: Object,
			required: false,
			default: void 0
		},
		dataAttrs: {
			type: Object,
			required: false,
			default: void 0
		},
		prefix: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		suffix: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		addonBefore: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		addonAfter: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		classes: {
			type: Object,
			required: false,
			default: void 0
		},
		allowClear: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		}
	}, defaults),
	name: "Input",
	inheritAttrs: false
});
//#endregion
export { getAllowClear_default as a, resolveOnChange as i, useCount as n, BaseInput as r, src_default as t };

//# sourceMappingURL=dist-Cs6Ew2fg.js.map