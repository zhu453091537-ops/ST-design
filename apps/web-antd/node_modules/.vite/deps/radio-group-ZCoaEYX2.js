import { o as __toESM } from "./chunk-CqwQKh_b.js";
import { Bt as onUnmounted, Gn as reactive, It as onMounted, St as inject, W as computed, Wt as provide, _n as watch, kt as nextTick, mt as h } from "./runtime-dom.esm-bundler-BEW9uiVc.js";
import "./vue.js";
import { t as require_xe_utils } from "./xe-utils-CectUEgD.js";
import { L as getConfig, j as getIcon, k as createEvent, t as VxeUI, u as useSize } from "./index.esm-9ZlbHbuz.js";
import { c as dynamicApp, u as defineVxeComponent } from "./ui-DJM_GiG-.js";
import { n as getFuncText } from "./utils-C8rmPdj_.js";
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/radio/src/radio.js
var import_xe_utils = /* @__PURE__ */ __toESM(require_xe_utils());
var radio_default = defineVxeComponent({
	name: "VxeRadio",
	props: {
		modelValue: [
			String,
			Number,
			Boolean
		],
		checkedValue: {
			type: [
				String,
				Number,
				Boolean
			],
			default: void 0
		},
		title: [String, Number],
		content: [String, Number],
		disabled: {
			type: Boolean,
			default: null
		},
		name: String,
		strict: {
			type: Boolean,
			default: () => getConfig().radio.strict
		},
		size: {
			type: String,
			default: () => getConfig().radio.size || getConfig().size
		},
		/**
		* 已废弃，被 checkedValue 替换
		*/
		label: {
			type: [
				String,
				Number,
				Boolean
			],
			default: null
		}
	},
	emits: ["update:modelValue", "change"],
	setup(props, context) {
		const { slots, emit } = context;
		const $xeForm = inject("$xeForm", null);
		const formItemInfo = inject("xeFormItemInfo", null);
		const $xeRadioGroup = inject("$xeRadioGroup", null);
		const $xeRadio = {
			xID: import_xe_utils.default.uniqueId(),
			props,
			context,
			reactData: reactive({})
		};
		const { computeSize } = useSize(props);
		const computeIsDisabled = computed(() => {
			const { disabled } = props;
			if (disabled === null) {
				if ($xeRadioGroup) {
					const { computeIsDisabled } = $xeRadioGroup.getComputeMaps();
					return computeIsDisabled.value;
				}
			}
			return disabled;
		});
		const computeName = computed(() => {
			return $xeRadioGroup ? $xeRadioGroup.name : props.name;
		});
		const computeStrict = computed(() => {
			return $xeRadioGroup ? $xeRadioGroup.props.strict : props.strict;
		});
		const computeChecked = computed(() => {
			const { label, checkedValue } = props;
			const radioValue = import_xe_utils.default.isUndefined(checkedValue) ? label : checkedValue;
			return $xeRadioGroup ? $xeRadioGroup.props.modelValue === radioValue : props.modelValue === radioValue;
		});
		const handleValue = (checkedValue, evnt) => {
			const { content } = props;
			if ($xeRadioGroup) $xeRadioGroup.handleChecked({
				label: checkedValue,
				checkedValue,
				checkedLabel: content
			}, evnt);
			else {
				emit("update:modelValue", checkedValue);
				dispatchEvent("change", {
					value: checkedValue,
					label: checkedValue,
					checkedValue,
					checkedLabel: content
				}, evnt);
				if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, checkedValue);
			}
		};
		const changeEvent = (evnt) => {
			if (!computeIsDisabled.value) {
				const { label, checkedValue } = props;
				handleValue(import_xe_utils.default.isUndefined(checkedValue) ? label : checkedValue, evnt);
			}
		};
		const clickEvent = (evnt) => {
			const isDisabled = computeIsDisabled.value;
			const isStrict = computeStrict.value;
			if (!isDisabled && !isStrict) {
				const { label, checkedValue } = props;
				if ((import_xe_utils.default.isUndefined(checkedValue) ? label : checkedValue) === ($xeRadioGroup ? $xeRadioGroup.props.modelValue : props.modelValue)) handleValue(null, evnt);
			}
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $radio: $xeRadio }, params));
		};
		Object.assign($xeRadio, { dispatchEvent }, {});
		const renderVN = () => {
			const { label, checkedValue } = props;
			const radioValue = import_xe_utils.default.isUndefined(checkedValue) ? label : checkedValue;
			const vSize = computeSize.value;
			const isDisabled = computeIsDisabled.value;
			const name = computeName.value;
			const isChecked = computeChecked.value;
			return h("label", {
				key: radioValue,
				class: ["vxe-radio vxe-radio--default", {
					[`size--${vSize}`]: vSize,
					"is--checked": isChecked,
					"is--disabled": isDisabled
				}],
				title: props.title
			}, [
				h("input", {
					class: "vxe-radio--input",
					type: "radio",
					name,
					checked: isChecked,
					disabled: isDisabled,
					onChange: changeEvent,
					onClick: clickEvent
				}),
				h("span", { class: ["vxe-radio--icon", isChecked ? getIcon().RADIO_CHECKED : isDisabled ? getIcon().RADIO_DISABLED_UNCHECKED : getIcon().RADIO_UNCHECKED] }),
				h("span", { class: "vxe-radio--label" }, slots.default ? slots.default({}) : getFuncText(props.content))
			]);
		};
		$xeRadio.renderVN = renderVN;
		return $xeRadio;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/radio/src/button.js
var button_default = defineVxeComponent({
	name: "VxeRadioButton",
	props: {
		modelValue: [
			String,
			Number,
			Boolean
		],
		checkedValue: {
			type: [
				String,
				Number,
				Boolean
			],
			default: void 0
		},
		title: [String, Number],
		content: [String, Number],
		disabled: {
			type: Boolean,
			default: null
		},
		strict: {
			type: Boolean,
			default: () => getConfig().radioButton.strict
		},
		size: {
			type: String,
			default: () => getConfig().radioButton.size || getConfig().size
		},
		/**
		* 已废弃，被 checkedValue 替换
		*/
		label: {
			type: [
				String,
				Number,
				Boolean
			],
			default: null
		}
	},
	emits: ["update:modelValue", "change"],
	setup(props, context) {
		const { slots, emit } = context;
		const $xeForm = inject("$xeForm", null);
		const formItemInfo = inject("xeFormItemInfo", null);
		const $xeRadioGroup = inject("$xeRadioGroup", null);
		const xID = import_xe_utils.default.uniqueId();
		const reactData = reactive({});
		const { computeSize } = useSize(props);
		const $xeRadioButton = {
			xID,
			props,
			context,
			reactData
		};
		const computeIsDisabled = computed(() => {
			const { disabled } = props;
			if (disabled === null) {
				if ($xeRadioGroup) {
					const { computeIsDisabled } = $xeRadioGroup.getComputeMaps();
					return computeIsDisabled.value;
				}
			}
			return disabled;
		});
		const computeName = computed(() => {
			return $xeRadioGroup ? $xeRadioGroup.name : null;
		});
		const computeStrict = computed(() => {
			return $xeRadioGroup ? $xeRadioGroup.props.strict : props.strict;
		});
		const computeChecked = computed(() => {
			const { label, checkedValue } = props;
			const radioValue = import_xe_utils.default.isUndefined(checkedValue) ? label : checkedValue;
			return $xeRadioGroup ? $xeRadioGroup.props.modelValue === radioValue : props.modelValue === radioValue;
		});
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $radioGroup: $xeRadioGroup }, params));
		};
		Object.assign($xeRadioButton, { dispatchEvent }, {});
		const handleValue = (checkedValue, evnt) => {
			const { content } = props;
			if ($xeRadioGroup) $xeRadioGroup.handleChecked({
				label: checkedValue,
				checkedValue,
				checkedLabel: content
			}, evnt);
			else {
				emit("update:modelValue", checkedValue);
				dispatchEvent("change", {
					value: checkedValue,
					label: checkedValue,
					checkedValue,
					checkedLabel: content
				}, evnt);
				if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, checkedValue);
			}
		};
		const changeEvent = (evnt) => {
			if (!computeIsDisabled.value) {
				const { label, checkedValue } = props;
				handleValue(import_xe_utils.default.isUndefined(checkedValue) ? label : checkedValue, evnt);
			}
		};
		const clickEvent = (evnt) => {
			const isDisabled = computeIsDisabled.value;
			const isStrict = computeStrict.value;
			if (!isDisabled && !isStrict) {
				const { label, checkedValue } = props;
				if ((import_xe_utils.default.isUndefined(checkedValue) ? label : checkedValue) === ($xeRadioGroup ? $xeRadioGroup.props.modelValue : props.modelValue)) handleValue(null, evnt);
			}
		};
		const renderVN = () => {
			const { label, checkedValue } = props;
			const radioValue = import_xe_utils.default.isUndefined(checkedValue) ? label : checkedValue;
			const vSize = computeSize.value;
			const isDisabled = computeIsDisabled.value;
			const name = computeName.value;
			const isChecked = computeChecked.value;
			return h("label", {
				key: radioValue,
				class: ["vxe-radio vxe-radio--button", {
					[`size--${vSize}`]: vSize,
					"is--disabled": isDisabled
				}],
				title: props.title
			}, [h("input", {
				class: "vxe-radio--input",
				type: "radio",
				name,
				checked: isChecked,
				disabled: isDisabled,
				onChange: changeEvent,
				onClick: clickEvent
			}), h("span", { class: "vxe-radio--label" }, slots.default ? slots.default({}) : getFuncText(props.content))]);
		};
		$xeRadioButton.renderVN = renderVN;
		return renderVN;
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/radio/src/group.js
function createInternalData() {
	return {};
}
var group_default = defineVxeComponent({
	name: "VxeRadioGroup",
	props: {
		modelValue: [
			String,
			Number,
			Boolean
		],
		disabled: {
			type: Boolean,
			default: null
		},
		type: String,
		options: Array,
		optionProps: Object,
		strict: {
			type: Boolean,
			default: () => getConfig().radioGroup.strict
		},
		size: {
			type: String,
			default: () => getConfig().radioGroup.size || getConfig().size
		},
		defaultConfig: Object
	},
	emits: [
		"update:modelValue",
		"change",
		"default-change"
	],
	setup(props, context) {
		const { slots, emit } = context;
		const $xeForm = inject("$xeForm", null);
		const formItemInfo = inject("xeFormItemInfo", null);
		const xID = import_xe_utils.default.uniqueId();
		const { computeSize } = useSize(props);
		const reactData = reactive({});
		const internalData = createInternalData();
		const computeIsDisabled = computed(() => {
			const { disabled } = props;
			if (disabled === null) {
				if ($xeForm) return $xeForm.props.readonly || $xeForm.props.disabled;
				return false;
			}
			return disabled;
		});
		const computeDefaultOpts = computed(() => {
			return Object.assign({}, props.defaultConfig);
		});
		const computeMaps = { computeIsDisabled };
		const $xeRadioGroup = {
			xID,
			props,
			context,
			reactData,
			name: import_xe_utils.default.uniqueId("xe_group_"),
			getComputeMaps: () => computeMaps
		};
		const computePropsOpts = computed(() => {
			return Object.assign({}, props.optionProps);
		});
		const computeLabelField = computed(() => {
			return computePropsOpts.value.label || "label";
		});
		const computeValueField = computed(() => {
			return computePropsOpts.value.value || "value";
		});
		const computeDisabledField = computed(() => {
			return computePropsOpts.value.disabled || "disabled";
		});
		const emitModel = (value) => {
			emit("update:modelValue", value);
		};
		const emitDefaultValue = (value) => {
			emitModel(value);
			dispatchEvent("default-change", { value }, null);
		};
		const dispatchEvent = (type, params, evnt) => {
			emit(type, createEvent(evnt, { $radioGroup: $xeRadioGroup }, params));
		};
		const radioGroupMethods = { dispatchEvent };
		const radioGroupPrivateMethods = { handleChecked(params, evnt) {
			const { checkedValue, checkedLabel } = params;
			const value = checkedValue;
			emitModel(value);
			dispatchEvent("change", {
				value,
				label: value,
				checkedValue,
				checkedLabel
			}, evnt);
			if ($xeForm && formItemInfo) $xeForm.triggerItemEvent(evnt, formItemInfo.itemConfig.field, value);
		} };
		const loadData = (datas) => {
			const { isLoaded } = internalData;
			const defaultOpts = computeDefaultOpts.value;
			const valueField = computeValueField.value;
			if (!isLoaded) {
				const { selectMode } = defaultOpts;
				if (datas.length > 0 && import_xe_utils.default.eqNull(props.modelValue)) {
					if (selectMode === "first" || selectMode === "last") {
						const selectItem = import_xe_utils.default[selectMode](datas);
						if (selectItem) nextTick(() => {
							if (import_xe_utils.default.eqNull(props.modelValue)) emitDefaultValue(selectItem[valueField]);
						});
					}
					internalData.isLoaded = true;
				}
			}
			return nextTick();
		};
		Object.assign($xeRadioGroup, radioGroupMethods, radioGroupPrivateMethods);
		const renderVN = () => {
			const { options, type } = props;
			const vSize = computeSize.value;
			const defaultSlot = slots.default;
			const valueField = computeValueField.value;
			const labelField = computeLabelField.value;
			const disabledField = computeDisabledField.value;
			const btnComp = type === "button" ? button_default : radio_default;
			return h("div", { class: ["vxe-radio-group", { [`size--${vSize}`]: vSize }] }, defaultSlot ? defaultSlot({}) : options ? options.map((item) => {
				return h(btnComp, {
					key: item[valueField],
					checkedValue: item[valueField],
					content: item[labelField],
					disabled: item[disabledField]
				});
			}) : []);
		};
		watch(() => props.options, (val) => {
			loadData(val || []);
		});
		onMounted(() => {
			nextTick(() => {
				const { options } = props;
				if (options) loadData(options);
			});
		});
		onUnmounted(() => {
			import_xe_utils.default.assign(internalData, createInternalData());
		});
		provide("$xeRadioGroup", $xeRadioGroup);
		$xeRadioGroup.renderVN = renderVN;
		return $xeRadioGroup;
	},
	render() {
		return this.renderVN();
	}
});
//#endregion
//#region ../../node_modules/.pnpm/vxe-pc-ui@4.14.0_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-pc-ui/es/radio-group/index.js
var VxeRadioGroup = Object.assign(group_default, { install: function(app) {
	app.component(group_default.name, group_default);
} });
dynamicApp.use(VxeRadioGroup);
VxeUI.component(group_default);
var RadioGroup = VxeRadioGroup;
//#endregion
export { radio_default as i, VxeRadioGroup as n, button_default as r, RadioGroup as t };

//# sourceMappingURL=radio-group-ZCoaEYX2.js.map