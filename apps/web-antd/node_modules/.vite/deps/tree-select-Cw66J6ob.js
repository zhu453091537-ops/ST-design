import { Et as mergeDefaults, Ot as mergeProps, Qn as toRaw, St as inject, Tt as isVNode, W as computed, Wt as provide, Xn as shallowRef, _n as watch, qn as ref, rt as defineComponent, tt as createVNode, vn as watchEffect } from "./runtime-dom.esm-bundler-BEW9uiVc.js";
import "./vue.js";
import { c as clsx, f as getAttrStyleAndClass, i as useMergedState, o as useId_default, u as filterEmpty, v as omit, w as warning_default, x as KeyCode_default } from "./dist-B3YnmQlL.js";
import { i as CaretDownFilled$1, n as MinusSquareOutlined$1, r as FileOutlined$1, t as PlusSquareOutlined$1 } from "./PlusSquareOutlined-Clrwy7_4.js";
import { C as useToken, I as Keyframes_default, M as unit, Q as useComponentBaseConfig, at as toPropsRefs, b as resetComponent, c as useToArr, et as useConfig, g as genFocusOutline, it as getSlotPropsFnRun, k as merge, l as useToProps, o as useMergeSemantic, p as genStyleHooks, u as omit$1 } from "./useMergeSemantic-NT6cWlpu.js";
import { o as getTransitionName } from "./useEvent-9fIYS1a5.js";
import { t as useSize } from "./useSize-C23Evsd5.js";
import { r as useCompactItemContext } from "./Compact-BlWyMZnR.js";
import { r as useZIndex } from "./motion-D9T9kJ5B.js";
import { t as devUseWarning } from "./warning-DVCi2y10.js";
import { t as useCSSVarCls_default } from "./useCSSVarCls-C_79E43w.js";
import { n as PurePanel_default } from "./dist-CsfrhiVf2.js";
import { n as useDisabledContext } from "./DisabledContext-CNdo8kjf.js";
import { t as AntdIcon } from "./AntdIcon-CGwHAHAW.js";
import { t as LoadingOutlined } from "./LoadingOutlined-CtkCSnaI.js";
import { t as collapse_default } from "./collapse-cMcrwEPw.js";
import { t as DefaultRenderEmpty } from "./defaultRenderEmpty-C1sdinl8.js";
import { n as getStatusClassNames, t as getMergedStatus } from "./statusUtils-DpKVCk3N.js";
import { s as useFormItemInputContext } from "./context-CoFN053h.js";
import { t as useVariant } from "./useVariant-DTfcCpUL.js";
import { c as usePopupRender_default, i as useBaseProps, l as style_default$1, n as BaseSelect, s as useShowArrow, u as mergedBuiltinPlacements_default } from "./dist-DvaLjSKo.js";
import { t as useIcons } from "./useIcons-B31xeUjq.js";
import { t as getStyle } from "./style-zaE9ltDo.js";
import { n as conductCheck, s as convertDataToEntities, t as ExportTree, u as UnstableContextKey } from "./dist-DV3l7Wky.js";
//#region ../../node_modules/.pnpm/@antdv-next+icons@1.0.6_vue@3.5.33_typescript@5.9.3_/node_modules/@antdv-next/icons/dist/icons/CaretDownFilled.js
/**![caret-down](data:image/svg+xml;base64,) */
var CaretDownFilled = /* @__PURE__ */ defineComponent((props) => {
	return () => {
		return createVNode(AntdIcon, mergeProps(props, { "icon": CaretDownFilled$1 }), null);
	};
}, {
	props: {
		twoToneColor: {
			type: [String, Array],
			required: false
		},
		onClick: {
			type: Function,
			required: false
		},
		tabIndex: {
			type: Number,
			required: false
		},
		spin: {
			type: Boolean,
			required: false
		},
		rotate: {
			type: Number,
			required: false
		}
	},
	name: "CaretDownFilled"
});
//#endregion
//#region ../../node_modules/.pnpm/@antdv-next+icons@1.0.6_vue@3.5.33_typescript@5.9.3_/node_modules/@antdv-next/icons/dist/icons/FileOutlined.js
/**![file](data:image/svg+xml;base64,) */
var FileOutlined = /* @__PURE__ */ defineComponent((props) => {
	return () => {
		return createVNode(AntdIcon, mergeProps(props, { "icon": FileOutlined$1 }), null);
	};
}, {
	props: {
		twoToneColor: {
			type: [String, Array],
			required: false
		},
		onClick: {
			type: Function,
			required: false
		},
		tabIndex: {
			type: Number,
			required: false
		},
		spin: {
			type: Boolean,
			required: false
		},
		rotate: {
			type: Number,
			required: false
		}
	},
	name: "FileOutlined"
});
//#endregion
//#region ../../node_modules/.pnpm/@antdv-next+icons@1.0.6_vue@3.5.33_typescript@5.9.3_/node_modules/@antdv-next/icons/dist/icons/MinusSquareOutlined.js
/**![minus-square](data:image/svg+xml;base64,) */
var MinusSquareOutlined = /* @__PURE__ */ defineComponent((props) => {
	return () => {
		return createVNode(AntdIcon, mergeProps(props, { "icon": MinusSquareOutlined$1 }), null);
	};
}, {
	props: {
		twoToneColor: {
			type: [String, Array],
			required: false
		},
		onClick: {
			type: Function,
			required: false
		},
		tabIndex: {
			type: Number,
			required: false
		},
		spin: {
			type: Boolean,
			required: false
		},
		rotate: {
			type: Number,
			required: false
		}
	},
	name: "MinusSquareOutlined"
});
//#endregion
//#region ../../node_modules/.pnpm/@antdv-next+icons@1.0.6_vue@3.5.33_typescript@5.9.3_/node_modules/@antdv-next/icons/dist/icons/PlusSquareOutlined.js
/**![plus-square](data:image/svg+xml;base64,) */
var PlusSquareOutlined = /* @__PURE__ */ defineComponent((props) => {
	return () => {
		return createVNode(AntdIcon, mergeProps(props, { "icon": PlusSquareOutlined$1 }), null);
	};
}, {
	props: {
		twoToneColor: {
			type: [String, Array],
			required: false
		},
		onClick: {
			type: Function,
			required: false
		},
		tabIndex: {
			type: Number,
			required: false
		},
		spin: {
			type: Boolean,
			required: false
		},
		rotate: {
			type: Number,
			required: false
		}
	},
	name: "PlusSquareOutlined"
});
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/tree/style/directory.js
function genDirectoryStyle({ treeCls, treeNodeCls, directoryNodeSelectedBg, directoryNodeSelectedColor, motionDurationMid, borderRadius, controlItemBgHover }) {
	return { [`${treeCls}${treeCls}-directory ${treeNodeCls}`]: {
		[`${treeCls}-node-content-wrapper`]: {
			position: "static",
			[`&:has(${treeCls}-drop-indicator)`]: { position: "relative" },
			[`> *:not(${treeCls}-drop-indicator)`]: { position: "relative" },
			"&:hover": { background: "transparent" },
			"&:before": {
				position: "absolute",
				inset: 0,
				transition: `background-color ${motionDurationMid}`,
				content: "\"\"",
				borderRadius
			},
			"&:hover:before": { background: controlItemBgHover }
		},
		[`${treeCls}-switcher, ${treeCls}-checkbox, ${treeCls}-draggable-icon`]: { zIndex: 1 },
		"&-selected": {
			background: directoryNodeSelectedBg,
			borderRadius,
			[`${treeCls}-switcher, ${treeCls}-draggable-icon`]: { color: directoryNodeSelectedColor },
			[`${treeCls}-node-content-wrapper`]: {
				color: directoryNodeSelectedColor,
				background: "transparent",
				"&, &:hover": { color: directoryNodeSelectedColor },
				"&:before, &:hover:before": { background: directoryNodeSelectedBg }
			}
		}
	} };
}
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/tree/style/index.js
var treeNodeFX = new Keyframes_default("ant-tree-node-fx-do-not-use", {
	"0%": { opacity: 0 },
	"100%": { opacity: 1 }
});
function getSwitchStyle(prefixCls, token) {
	return { [`.${prefixCls}-switcher-icon`]: {
		display: "inline-block",
		fontSize: 10,
		verticalAlign: "baseline",
		svg: { transition: `transform ${token.motionDurationSlow}` }
	} };
}
function getDropIndicatorStyle(prefixCls, token) {
	return { [`.${prefixCls}-drop-indicator`]: {
		position: "absolute",
		zIndex: 1,
		height: 2,
		backgroundColor: token.colorPrimary,
		borderRadius: 1,
		pointerEvents: "none",
		"&:after": {
			position: "absolute",
			top: -3,
			insetInlineStart: -6,
			width: 8,
			height: 8,
			backgroundColor: "transparent",
			border: `${unit(token.lineWidthBold)} solid ${token.colorPrimary}`,
			borderRadius: "50%",
			content: "\"\""
		}
	} };
}
function genBaseStyle$1(prefixCls, token) {
	const { treeCls, treeNodeCls, treeNodePadding, titleHeight, indentSize, switcherSize, motionDurationMid, nodeSelectedBg, nodeHoverBg, colorTextQuaternary, controlItemBgActiveDisabled } = token;
	return { [treeCls]: {
		...resetComponent(token),
		["--rc-virtual-list-scrollbar-bg"]: token.colorSplit,
		background: token.colorBgContainer,
		borderRadius: token.borderRadius,
		transition: `background-color ${token.motionDurationSlow}`,
		"&-rtl": { direction: "rtl" },
		[`&${treeCls}-rtl ${treeCls}-switcher_close ${treeCls}-switcher-icon svg`]: { transform: "rotate(90deg)" },
		[`${treeCls}-list`]: { "&:focus-visible": {
			outline: "none",
			[`${treeNodeCls}-active ${treeCls}-node-content-wrapper`]: { ...genFocusOutline(token) }
		} },
		[`${treeCls}-list-holder-inner`]: { alignItems: "flex-start" },
		[`&${treeCls}-block-node`]: { [`${treeCls}-list-holder-inner`]: {
			alignItems: "stretch",
			[`${treeCls}-node-content-wrapper`]: { flex: "auto" },
			[`${treeNodeCls}.dragging:after`]: {
				position: "absolute",
				inset: 0,
				border: `1px solid ${token.colorPrimary}`,
				opacity: 0,
				animationName: treeNodeFX,
				animationDuration: token.motionDurationSlow,
				animationPlayState: "running",
				animationFillMode: "forwards",
				content: "\"\"",
				pointerEvents: "none",
				borderRadius: token.borderRadius
			}
		} },
		[treeNodeCls]: {
			display: "flex",
			alignItems: "flex-start",
			marginBottom: treeNodePadding,
			lineHeight: unit(titleHeight),
			position: "relative",
			"&:before": {
				content: "\"\"",
				position: "absolute",
				zIndex: 1,
				insetInlineStart: 0,
				width: "100%",
				top: "100%",
				height: treeNodePadding
			},
			[`&-disabled ${treeCls}-node-content-wrapper`]: {
				color: token.colorTextDisabled,
				cursor: "not-allowed",
				"&:hover": { background: "transparent" }
			},
			[`${treeCls}-checkbox-disabled + ${treeCls}-node-selected,&${treeNodeCls}-disabled${treeNodeCls}-selected ${treeCls}-node-content-wrapper`]: { backgroundColor: controlItemBgActiveDisabled },
			[`${treeCls}-checkbox-disabled`]: { pointerEvents: "unset" },
			[`&:not(${treeNodeCls}-disabled)`]: { [`${treeCls}-node-content-wrapper`]: { "&:hover": { color: token.nodeHoverColor } } },
			[`&-active ${treeCls}-node-content-wrapper`]: { background: token.controlItemBgHover },
			[`&:not(${treeNodeCls}-disabled).filter-node ${treeCls}-title`]: {
				color: token.colorPrimary,
				fontWeight: token.fontWeightStrong
			},
			"&-draggable": {
				cursor: "grab",
				[`${treeCls}-draggable-icon`]: {
					flexShrink: 0,
					width: switcherSize,
					textAlign: "center",
					visibility: "visible",
					color: colorTextQuaternary
				},
				[`&${treeNodeCls}-disabled ${treeCls}-draggable-icon`]: { visibility: "hidden" }
			}
		},
		[`${treeCls}-indent`]: {
			alignSelf: "stretch",
			whiteSpace: "nowrap",
			userSelect: "none",
			"&-unit": {
				display: "inline-block",
				width: indentSize
			}
		},
		[`${treeCls}-draggable-icon`]: { visibility: "hidden" },
		[`${treeCls}-switcher, ${treeCls}-checkbox`]: { marginInlineEnd: token.calc(token.calc(switcherSize).sub(token.controlInteractiveSize)).div(2).equal() },
		[`${treeCls}-checkbox`]: {
			flexShrink: 0,
			alignSelf: "flex-start",
			marginBlockStart: token.calc(token.calc(titleHeight).sub(token.controlInteractiveSize)).div(2).equal()
		},
		[`${treeCls}-switcher`]: {
			...getSwitchStyle(prefixCls, token),
			position: "relative",
			flex: "none",
			alignSelf: "stretch",
			width: switcherSize,
			textAlign: "center",
			cursor: "pointer",
			userSelect: "none",
			transition: `all ${token.motionDurationSlow}`,
			"&-noop": { cursor: "unset" },
			"&:before": {
				pointerEvents: "none",
				content: "\"\"",
				width: switcherSize,
				height: titleHeight,
				position: "absolute",
				left: {
					_skip_check_: true,
					value: 0
				},
				top: 0,
				borderRadius: token.borderRadius,
				transition: `all ${token.motionDurationSlow}`
			},
			[`&:not(${treeCls}-switcher-noop):hover:before`]: { backgroundColor: token.colorBgTextHover },
			[`&_close ${treeCls}-switcher-icon svg`]: { transform: "rotate(-90deg)" },
			"&-loading-icon": { color: token.colorPrimary },
			"&-leaf-line": {
				position: "relative",
				zIndex: 1,
				display: "inline-block",
				width: "100%",
				height: "100%",
				"&:before": {
					position: "absolute",
					top: 0,
					insetInlineEnd: token.calc(switcherSize).div(2).equal(),
					bottom: token.calc(treeNodePadding).mul(-1).equal(),
					marginInlineStart: -1,
					borderInlineEnd: `1px solid ${token.colorBorder}`,
					content: "\"\""
				},
				"&:after": {
					position: "absolute",
					width: token.calc(token.calc(switcherSize).div(2).equal()).mul(.8).equal(),
					height: token.calc(titleHeight).div(2).equal(),
					borderBottom: `1px solid ${token.colorBorder}`,
					content: "\"\""
				}
			}
		},
		[`${treeCls}-node-content-wrapper`]: {
			position: "relative",
			minHeight: titleHeight,
			paddingBlock: 0,
			paddingInline: token.paddingXS,
			background: "transparent",
			borderRadius: token.borderRadius,
			cursor: "pointer",
			transition: [
				`all ${motionDurationMid}`,
				"border 0s",
				"line-height 0s",
				"box-shadow 0s"
			].join(", "),
			...getDropIndicatorStyle(prefixCls, token),
			"&:hover": { backgroundColor: nodeHoverBg },
			[`&${treeCls}-node-selected`]: {
				color: token.nodeSelectedColor,
				backgroundColor: nodeSelectedBg
			},
			[`${treeCls}-iconEle`]: {
				display: "inline-block",
				width: switcherSize,
				height: titleHeight,
				textAlign: "center",
				verticalAlign: "top",
				"&:empty": { display: "none" }
			}
		},
		[`${treeCls}-unselectable ${treeCls}-node-content-wrapper:hover`]: { backgroundColor: "transparent" },
		[`${treeNodeCls}.drop-container > [draggable]`]: { boxShadow: `0 0 0 2px ${token.colorPrimary}` },
		"&-show-line": {
			[`${treeCls}-indent-unit`]: {
				position: "relative",
				height: "100%",
				"&:before": {
					position: "absolute",
					top: 0,
					insetInlineEnd: token.calc(switcherSize).div(2).equal(),
					bottom: token.calc(treeNodePadding).mul(-1).equal(),
					borderInlineEnd: `1px solid ${token.colorBorder}`,
					content: "\"\""
				},
				"&-end:before": { display: "none" }
			},
			[`${treeCls}-switcher`]: {
				background: "transparent",
				"&-line-icon": { verticalAlign: "-0.15em" }
			}
		},
		[`${treeNodeCls}-leaf-last ${treeCls}-switcher-leaf-line:before`]: {
			top: "auto !important",
			bottom: "auto !important",
			height: `${unit(token.calc(titleHeight).div(2).equal())} !important`
		}
	} };
}
function genTreeStyle(prefixCls, token, enableDirectory = true) {
	const treeCls = `.${prefixCls}`;
	const treeToken = merge(token, {
		treeCls,
		treeNodeCls: `${treeCls}-treenode`,
		treeNodePadding: token.calc(token.paddingXS).div(2).equal()
	});
	return [genBaseStyle$1(prefixCls, treeToken), enableDirectory && genDirectoryStyle(treeToken)].filter(Boolean);
}
function initComponentToken(token) {
	const { controlHeightSM, controlItemBgHover, controlItemBgActive } = token;
	const titleHeight = controlHeightSM;
	return {
		titleHeight,
		switcherSize: titleHeight,
		indentSize: titleHeight,
		nodeHoverBg: controlItemBgHover,
		nodeHoverColor: token.colorText,
		nodeSelectedBg: controlItemBgActive,
		nodeSelectedColor: token.colorText
	};
}
var prepareComponentToken = (token) => {
	const { colorTextLightSolid, colorPrimary } = token;
	return {
		...initComponentToken(token),
		directoryNodeSelectedColor: colorTextLightSolid,
		directoryNodeSelectedBg: colorPrimary
	};
};
var style_default = genStyleHooks("Tree", (token, { prefixCls }) => [
	{ [token.componentCls]: getStyle(`${prefixCls}-checkbox`, token) },
	genTreeStyle(prefixCls, token),
	collapse_default(token)
], prepareComponentToken);
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/tree/utils/iconUtil.js
var iconUtil_default = /* @__PURE__ */ defineComponent((props, { slots }) => {
	return () => {
		const { prefixCls, switcherIcon, treeNodeProps, showLine } = props;
		const switcherLoadingIcon = getSlotPropsFnRun(slots, props, "switcherLoadingIcon");
		const { isLeaf, expanded, loading } = treeNodeProps;
		if (loading) {
			if (isVNode(switcherLoadingIcon)) return switcherLoadingIcon;
			return createVNode(LoadingOutlined, { "class": `${prefixCls}-switcher-loading-icon` }, null);
		}
		let showLeafIcon;
		if (showLine && typeof showLine === "object") showLeafIcon = showLine.showLeafIcon;
		if (isLeaf) {
			if (!showLine) return null;
			if (typeof showLeafIcon !== "boolean" && !!showLeafIcon) {
				const leafIcon = typeof showLeafIcon === "function" ? showLeafIcon(treeNodeProps) : showLeafIcon;
				const leafCls = `${prefixCls}-switcher-line-custom-icon`;
				if (isVNode(showLeafIcon)) return createVNode(showLeafIcon, { class: leafCls });
				return leafIcon;
			}
			return showLeafIcon ? createVNode(FileOutlined, { "class": `${prefixCls}-switcher-line-icon` }, null) : createVNode("span", { "class": `${prefixCls}-switcher-leaf-line` }, null);
		}
		const switcherCls = `${prefixCls}-switcher-icon`;
		let switcher = typeof switcherIcon === "function" ? switcherIcon(treeNodeProps) : switcherIcon;
		if (Array.isArray(switcher) && switcher.length === 1) switcher = switcher[0];
		if (isVNode(switcher)) return createVNode(switcher, { class: [switcher.props?.classes, showLine ? `${prefixCls}-switcher-line-icon` : switcherCls] });
		if (switcher !== void 0) return switcher;
		if (showLine) return expanded ? createVNode(MinusSquareOutlined, { "class": `${prefixCls}-switcher-line-icon` }, null) : createVNode(PlusSquareOutlined, { "class": `${prefixCls}-switcher-line-icon` }, null);
		return createVNode(CaretDownFilled, { "class": switcherCls }, null);
	};
}, {
	props: {
		prefixCls: {
			type: String,
			required: true
		},
		treeNodeProps: {
			type: Object,
			required: true
		},
		switcherIcon: {
			type: Function,
			required: false,
			skipCheck: true
		},
		switcherLoadingIcon: {
			type: [
				Function,
				String,
				Number,
				null,
				Object,
				Boolean
			],
			required: false,
			default: void 0
		},
		showLine: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		}
	},
	name: "SwitcherIconCom",
	inheritAttrs: false
});
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/tree-select/style/index.js
var genBaseStyle = (token) => {
	const { componentCls, treePrefixCls, colorBgElevated } = token;
	const treeCls = `.${treePrefixCls}`;
	return [{ [`${componentCls}-dropdown`]: [
		{ padding: `${unit(token.paddingXS)} ${unit(token.calc(token.paddingXS).div(2).equal())}` },
		genTreeStyle(treePrefixCls, merge(token, { colorBgContainer: colorBgElevated }), false),
		{ [treeCls]: {
			borderRadius: 0,
			[`${treeCls}-list-holder-inner`]: {
				alignItems: "stretch",
				[`${treeCls}-treenode`]: { [`${treeCls}-node-content-wrapper`]: { flex: "auto" } }
			}
		} },
		getStyle(`${treePrefixCls}-checkbox`, token),
		{ "&-rtl": {
			direction: "rtl",
			[`${treeCls}-switcher${treeCls}-switcher_close`]: { [`${treeCls}-switcher-icon svg`]: { transform: "rotate(90deg)" } }
		} }
	] }];
};
function useTreeSelectStyle(prefixCls, treePrefixCls, rootCls) {
	return genStyleHooks("TreeSelect", (token) => {
		return genBaseStyle(merge(token, { treePrefixCls: treePrefixCls.value }));
	}, initComponentToken, { resetFont: false })(prefixCls, rootCls);
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+tree-select@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/tree-select/dist/TreeNode.js
var TreeNode_default = /* @__PURE__ */ defineComponent(() => {
	return () => null;
});
//#endregion
//#region ../../node_modules/.pnpm/@v-c+tree-select@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/tree-select/dist/utils/valueUtil.js
function toArray(value) {
	return Array.isArray(value) ? value : value !== void 0 ? [value] : [];
}
function fillFieldNames(fieldNames) {
	const { label, value, children } = fieldNames || {};
	return {
		_title: label ? [label] : ["title", "label"],
		value: value || "value",
		key: value || "value",
		children: children || "children"
	};
}
function isCheckDisabled(node) {
	return !node || node.disabled || node.disableCheckbox || node.checkable === false;
}
function getAllKeys(treeData, fieldNames) {
	const keys = [];
	const dig = (list) => {
		list.forEach((item) => {
			const children = item[fieldNames.children];
			if (children) {
				keys.push(item[fieldNames.value]);
				dig(children);
			}
		});
	};
	dig(treeData);
	return keys;
}
var isNil = (val) => val === null || val === void 0;
//#endregion
//#region ../../node_modules/.pnpm/@v-c+tree-select@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/tree-select/dist/utils/strategyUtil.js
var SHOW_ALL = "SHOW_ALL";
var SHOW_PARENT = "SHOW_PARENT";
var SHOW_CHILD = "SHOW_CHILD";
function formatStrategyValues(values, strategy, keyEntities, fieldNames) {
	const valueSet = new Set(values);
	if (strategy === "SHOW_CHILD") return values.filter((key) => {
		const entity = keyEntities[String(key)];
		return !entity || !entity.children || !entity.children.some(({ node }) => valueSet.has(node[fieldNames.value])) || !entity.children.every(({ node }) => isCheckDisabled(node) || valueSet.has(node[fieldNames.value]));
	});
	if (strategy === "SHOW_PARENT") return values.filter((key) => {
		const entity = keyEntities[String(key)];
		const parent = entity ? entity.parent : null;
		return !parent || isCheckDisabled(parent.node) || !valueSet.has(parent.key);
	});
	return values;
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+tree-select@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/tree-select/dist/LegacyContext.js
var LegacyContextKey = Symbol("LegacyTreeSelectContext");
function useLegacyProvider(value) {
	provide(LegacyContextKey, value);
}
function useLegacyContext() {
	return inject(LegacyContextKey, ref(null));
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+tree-select@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/tree-select/dist/TreeSelectContext.js
var TreeSelectContextKey = Symbol("TreeSelectContext");
function useTreeSelectProvider(value) {
	provide(TreeSelectContextKey, value);
}
function useTreeSelectContext() {
	return inject(TreeSelectContextKey, ref(null));
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+tree-select@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/tree-select/dist/OptionList.js
var HIDDEN_STYLE = {
	width: 0,
	height: 0,
	display: "flex",
	overflow: "hidden",
	opacity: 0,
	border: 0,
	padding: 0,
	margin: 0
};
var OptionList_default = /* @__PURE__ */ defineComponent({
	name: "OptionList",
	inheritAttrs: false,
	setup(_, { expose }) {
		const baseProps = useBaseProps();
		const context = useTreeSelectContext();
		const legacyContext = useLegacyContext();
		const treeRef = ref(null);
		const memoTreeData = computed(() => context.value?.treeData || []);
		const mergedCheckedKeys = computed(() => {
			if (!legacyContext.value?.checkable) return null;
			return {
				checked: legacyContext.value.checkedKeys,
				halfChecked: legacyContext.value.halfCheckedKeys
			};
		});
		watch(() => baseProps.value?.open, (open) => {
			if (open && !baseProps.value?.multiple && legacyContext.value?.checkedKeys?.length) treeRef.value?.scrollTo({ key: legacyContext.value.checkedKeys[0] });
		}, { immediate: true });
		const onListMouseDown = (event) => {
			event.preventDefault();
		};
		const onInternalSelect = (_$1, info) => {
			const { node } = info;
			if (legacyContext.value?.checkable && isCheckDisabled(node)) return;
			const checkedKeys = legacyContext.value?.checkedKeys || [];
			context.value?.onSelect(node.key, {
				selected: !checkedKeys.includes(node.key),
				source: "option"
			});
			if (!baseProps.value?.multiple) baseProps.value?.toggleOpen(false);
		};
		const expandedKeys = ref(legacyContext.value?.treeDefaultExpandedKeys || []);
		const searchExpandedKeys = ref(null);
		const mergedExpandedKeys = computed(() => {
			if (legacyContext.value?.treeExpandedKeys) return [...legacyContext.value.treeExpandedKeys];
			if (baseProps.value?.searchValue) return searchExpandedKeys.value || expandedKeys.value || [];
			return expandedKeys.value;
		});
		const onInternalExpand = (keys) => {
			expandedKeys.value = keys;
			searchExpandedKeys.value = keys;
			legacyContext.value?.onTreeExpand?.(keys);
		};
		const filterTreeNode = (treeNode) => {
			const searchValue = String(baseProps.value?.searchValue || "");
			if (!searchValue) return false;
			const lowerSearchValue = searchValue.toLowerCase();
			const treeNodeFilterProp = legacyContext.value?.treeNodeFilterProp || "value";
			return String(treeNode?.[treeNodeFilterProp]).toLowerCase().includes(lowerSearchValue);
		};
		watch(() => baseProps.value?.searchValue, (val) => {
			if (val) searchExpandedKeys.value = getAllKeys(memoTreeData.value, context.value?.fieldNames || {});
		}, { immediate: true });
		const disabledCache = shallowRef(/* @__PURE__ */ new Map());
		watch(() => context.value?.leftMaxCount, (val) => {
			if (val) disabledCache.value = /* @__PURE__ */ new Map();
		}, { immediate: true });
		function getDisabledWithCache(node) {
			const value = node[context.value.fieldNames.value];
			if (!disabledCache.value.has(value)) {
				const entity = context.value?.valueEntities.get(value);
				if (!((entity?.children || []).length === 0)) {
					const checkedKeys = legacyContext.value?.checkedKeys || [];
					const checkableChildren = (entity?.children || []).filter((child) => !child.node.disabled && !child.node.disableCheckbox && !checkedKeys.includes(child.node[context.value.fieldNames.value]));
					disabledCache.value.set(value, checkableChildren.length > (context.value?.leftMaxCount || 0));
				} else disabledCache.value.set(value, false);
			}
			return disabledCache.value.get(value);
		}
		const nodeDisabled = (node) => {
			const checkedKeys = legacyContext.value?.checkedKeys || [];
			const nodeValue = node[context.value.fieldNames.value];
			if (checkedKeys.includes(nodeValue)) return false;
			const leftMaxCount = context.value?.leftMaxCount ?? null;
			if (leftMaxCount === null) return false;
			if (leftMaxCount <= 0) return true;
			if (context.value?.leafCountOnly && leftMaxCount) return getDisabledWithCache(node) || false;
			return false;
		};
		provide(UnstableContextKey, { nodeDisabled });
		const getFirstMatchingNode = (nodes) => {
			for (const node of nodes) {
				if (node.disabled || node.selectable === false) continue;
				if (baseProps.value?.searchValue) {
					if (filterTreeNode(node)) return node;
				} else return node;
				const children = node[context.value.fieldNames.children];
				if (children) {
					const matchInChildren = getFirstMatchingNode(children);
					if (matchInChildren) return matchInChildren;
				}
			}
			return null;
		};
		const activeKey = ref(null);
		const activeEntity = computed(() => legacyContext.value?.keyEntities?.[String(activeKey.value)]);
		watch([() => baseProps.value?.open, () => baseProps.value?.searchValue], ([open]) => {
			if (!open) return;
			const fieldNames = context.value?.fieldNames;
			const getFirstNode = () => {
				const firstNode = getFirstMatchingNode(memoTreeData.value);
				return firstNode ? firstNode[fieldNames?.value] : null;
			};
			let nextActiveKey = null;
			if (!baseProps.value?.multiple && legacyContext.value?.checkedKeys?.length && !baseProps.value?.searchValue) nextActiveKey = legacyContext.value.checkedKeys[0];
			else nextActiveKey = getFirstNode();
			activeKey.value = nextActiveKey;
		}, { immediate: true });
		const onKeyDown = (event) => {
			switch (event.which || event.keyCode) {
				case KeyCode_default.UP:
				case KeyCode_default.DOWN:
				case KeyCode_default.LEFT:
				case KeyCode_default.RIGHT:
					treeRef.value?.onKeyDown(event);
					break;
				case KeyCode_default.ENTER:
					if (activeEntity.value) {
						const isNodeDisabled = nodeDisabled(activeEntity.value.node);
						const { selectable, value, disabled } = activeEntity.value.node;
						if (selectable !== false && !disabled && !isNodeDisabled) onInternalSelect([], {
							node: { key: activeKey.value },
							selected: !(legacyContext.value?.checkedKeys || []).includes(value)
						});
					}
					return;
				case KeyCode_default.ESC: baseProps.value?.toggleOpen(false);
			}
		};
		const onKeyUp = () => {};
		expose({
			scrollTo: (scroll) => {
				treeRef.value?.scrollTo(scroll);
			},
			onKeyDown,
			onKeyUp
		});
		return () => {
			const prefixCls = baseProps.value?.prefixCls;
			const open = baseProps.value?.open;
			const notFoundContent = baseProps.value?.notFoundContent;
			const checkable = legacyContext.value?.checkable;
			const checkedKeys = legacyContext.value?.checkedKeys || [];
			const treeLoadedKeys = legacyContext.value?.treeLoadedKeys;
			const treeDefaultExpandAll = legacyContext.value?.treeDefaultExpandAll;
			const treeIcon = legacyContext.value?.treeIcon;
			const showTreeIcon = legacyContext.value?.showTreeIcon;
			const switcherIcon = legacyContext.value?.switcherIcon;
			const treeLine = legacyContext.value?.treeLine;
			const treeMotion = legacyContext.value?.treeMotion;
			const loadData = legacyContext.value?.loadData;
			const onTreeLoad = legacyContext.value?.onTreeLoad;
			const { fieldNames, virtual, listHeight, listItemHeight, listItemScrollOffset, popupMatchSelectWidth, treeExpandAction, treeTitleRender, onPopupScroll, classNames, styles } = context.value || {};
			if (memoTreeData.value.length === 0) return createVNode("div", {
				"role": "listbox",
				"class": `${prefixCls}-empty`,
				"onMousedown": onListMouseDown
			}, [notFoundContent]);
			const syncLoadData = baseProps.value?.searchValue ? void 0 : loadData;
			return createVNode("div", { "onMousedown": onListMouseDown }, [activeEntity.value && open && createVNode("span", {
				"style": HIDDEN_STYLE,
				"aria-live": "assertive"
			}, [activeEntity.value.node.value]), createVNode(ExportTree, {
				"ref": (el) => {
					treeRef.value = el;
				},
				"focusable": false,
				"prefixCls": `${prefixCls}-tree`,
				"treeData": memoTreeData.value,
				"fieldNames": fieldNames,
				"height": listHeight,
				"itemHeight": listItemHeight,
				"itemScrollOffset": listItemScrollOffset,
				"virtual": virtual !== false && popupMatchSelectWidth !== false,
				"multiple": baseProps.value?.multiple,
				"icon": treeIcon,
				"showIcon": showTreeIcon,
				"switcherIcon": switcherIcon,
				"showLine": treeLine,
				"loadData": syncLoadData,
				"motion": treeMotion,
				"activeKey": activeKey.value,
				"checkable": checkable,
				"checkStrictly": true,
				"checkedKeys": mergedCheckedKeys.value,
				"selectedKeys": !checkable ? checkedKeys : [],
				"defaultExpandAll": treeDefaultExpandAll,
				"titleRender": treeTitleRender,
				"expandedKeys": mergedExpandedKeys.value,
				"loadedKeys": treeLoadedKeys,
				"onActiveChange": (key) => {
					activeKey.value = key;
				},
				"onSelect": onInternalSelect,
				"onCheck": onInternalSelect,
				"onExpand": onInternalExpand,
				"onLoad": onTreeLoad,
				"filterTreeNode": filterTreeNode,
				"expandAction": treeExpandAction,
				"onScroll": onPopupScroll,
				"classNames": classNames?.popup,
				"styles": styles?.popup
			}, null)]);
		};
	}
});
//#endregion
//#region ../../node_modules/.pnpm/@v-c+tree-select@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/tree-select/dist/hooks/useCache.js
function useCache(values) {
	const cacheRef = shallowRef({ valueLabels: /* @__PURE__ */ new Map() });
	return [computed(() => {
		const { valueLabels } = cacheRef.value;
		const valueLabelsCache = /* @__PURE__ */ new Map();
		const merged = values.value.map((item) => {
			const { value, label } = item;
			const mergedLabel = label ?? valueLabels.get(value);
			valueLabelsCache.set(value, mergedLabel);
			return {
				...item,
				label: mergedLabel
			};
		});
		cacheRef.value.valueLabels = valueLabelsCache;
		return merged;
	})];
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+tree-select@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/tree-select/dist/hooks/useCheckedKeys.js
function useCheckedKeys(rawLabeledValues, rawHalfCheckedValues, treeConduction, keyEntities) {
	const merged = computed(() => {
		const extractValues = (values) => values.map(({ value }) => value);
		const checkedKeys = extractValues(rawLabeledValues.value);
		const halfCheckedKeys = extractValues(rawHalfCheckedValues.value);
		const missingValues = checkedKeys.filter((key) => !keyEntities.value[String(key)]);
		let finalCheckedKeys = checkedKeys;
		let finalHalfCheckedKeys = halfCheckedKeys;
		if (treeConduction.value) {
			const conductResult = conductCheck(checkedKeys, true, keyEntities.value);
			finalCheckedKeys = conductResult.checkedKeys;
			finalHalfCheckedKeys = conductResult.halfCheckedKeys;
		}
		return [Array.from(new Set([...missingValues, ...finalCheckedKeys])), finalHalfCheckedKeys];
	});
	return [computed(() => merged.value[0]), computed(() => merged.value[1])];
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+tree-select@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/tree-select/dist/hooks/useDataEntities.js
function useDataEntities(treeData, fieldNames) {
	const valueEntities = shallowRef(/* @__PURE__ */ new Map());
	const keyEntities = shallowRef({});
	watchEffect(() => {
		const mergedFieldNames = fieldNames.value;
		const collection = convertDataToEntities(treeData.value, {
			fieldNames: mergedFieldNames,
			initWrapper: (wrapper) => ({
				...wrapper,
				valueEntities: /* @__PURE__ */ new Map()
			}),
			processEntity: (entity, wrapper) => {
				const val = entity.node[mergedFieldNames.value];
				{
					const key = entity.node.key;
					warning_default(!isNil(val), "TreeNode `value` is invalidate: undefined");
					warning_default(!wrapper.valueEntities.has(val), `Same \`value\` exist in the tree: ${val}`);
					warning_default(!key || String(key) === String(val), `\`key\` or \`value\` with TreeNode must be the same or you can remove one of them. key: ${key}, value: ${val}.`);
				}
				wrapper.valueEntities.set(val, entity);
			}
		});
		keyEntities.value = collection.keyEntities;
		valueEntities.value = collection.valueEntities;
	});
	return {
		valueEntities,
		keyEntities
	};
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+tree-select@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/tree-select/dist/utils/legacyUtil.js
function getNodeChildren(children) {
	let finalChildren = children;
	if (typeof children === "function") finalChildren = children();
	else if (children && typeof children === "object" && "default" in children) finalChildren = typeof children.default === "function" ? children.default() : children.default;
	return Array.isArray(finalChildren) ? finalChildren : [];
}
function convertChildrenToData(nodes = []) {
	return toRaw(nodes).map((node) => {
		if (!isVNode(node) || !node.type) return null;
		const { key, props, children } = node;
		const { value, ...restProps } = props || {};
		const data = {
			key,
			value,
			...restProps
		};
		const childData = convertChildrenToData(getNodeChildren(children));
		if (childData.length) data.children = childData;
		return data;
	}).filter((data) => data !== null);
}
function fillLegacyProps(dataNode) {
	if (!dataNode) return dataNode;
	const cloneNode = { ...dataNode };
	if (!("props" in cloneNode)) Object.defineProperty(cloneNode, "props", { get() {
		warning_default(false, "New `vc-tree-select` not support return node instance as argument anymore. Please consider to remove `props` access.");
		return cloneNode;
	} });
	return cloneNode;
}
function fillAdditionalInfo(extra, triggerValue, checkedValues, treeData, showPosition, fieldNames) {
	let triggerNode = null;
	let nodeList = null;
	function generateMap() {
		function dig(list, level = "0", parentIncluded = false) {
			return (list || []).map((option, index) => {
				const pos = `${level}-${index}`;
				const value = option[fieldNames.value];
				const included = checkedValues.includes(value);
				const children = dig(option[fieldNames.children] || [], pos, included);
				const node = createVNode(TreeNode_default, option, { default: () => children.map((child) => child.node) });
				if (triggerValue === value) triggerNode = node;
				if (included) {
					const checkedNode = {
						pos,
						node,
						children
					};
					if (!parentIncluded) nodeList.push(checkedNode);
					return checkedNode;
				}
				return null;
			}).filter((node) => node !== null);
		}
		if (!nodeList) {
			nodeList = [];
			dig(treeData);
			nodeList.sort((a, b) => {
				const val1 = a.node.props?.value;
				const val2 = b.node.props?.value;
				return checkedValues.indexOf(val1) - checkedValues.indexOf(val2);
			});
		}
	}
	Object.defineProperty(extra, "triggerNode", { get() {
		warning_default(false, "`triggerNode` is deprecated. Please consider decoupling data with node.");
		generateMap();
		return triggerNode;
	} });
	Object.defineProperty(extra, "allCheckedNodes", { get() {
		warning_default(false, "`allCheckedNodes` is deprecated. Please consider decoupling data with node.");
		generateMap();
		if (showPosition) return nodeList;
		return (nodeList || []).map(({ node }) => node);
	} });
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+tree-select@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/tree-select/dist/hooks/useFilterTreeData.js
function useFilterTreeData(treeData, searchValue, options) {
	return computed(() => {
		const { children: fieldChildren } = options.fieldNames.value;
		const mergedSearchValue = searchValue.value;
		if (!mergedSearchValue || options.filterTreeNode.value === false) return treeData.value;
		const filterOptionFunc = typeof options.filterTreeNode.value === "function" ? options.filterTreeNode.value : (_, dataNode) => String(dataNode[options.treeNodeFilterProp.value]).toUpperCase().includes(mergedSearchValue.toUpperCase());
		const filterTreeNodes = (nodes, keepAll = false) => nodes.reduce((filtered, node) => {
			const children = node[fieldChildren];
			const isMatch = keepAll || filterOptionFunc(mergedSearchValue, fillLegacyProps(node));
			const filteredChildren = filterTreeNodes(children || [], isMatch);
			if (isMatch || filteredChildren.length) filtered.push({
				...node,
				isLeaf: void 0,
				[fieldChildren]: filteredChildren
			});
			return filtered;
		}, []);
		return filterTreeNodes(treeData.value);
	});
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+tree-select@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/tree-select/dist/hooks/useRefFunc.js
function useRefFunc(callback) {
	const callbackRef = shallowRef(callback);
	callbackRef.value = callback;
	const cacheFn = ((...args) => {
		return callbackRef.value(...args);
	});
	return cacheFn;
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+tree-select@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/tree-select/dist/hooks/useSearchConfig.js
function useSearchConfig(showSearch, props) {
	return [computed(() => {
		return typeof showSearch.value === "object" ? true : showSearch.value;
	}), computed(() => {
		const { searchValue, inputValue, onSearch, autoClearSearchValue, filterTreeNode, treeNodeFilterProp } = props.value;
		const isObject = typeof showSearch.value === "object";
		return {
			searchValue: searchValue ?? inputValue,
			onSearch,
			autoClearSearchValue,
			filterTreeNode,
			treeNodeFilterProp,
			...isObject ? showSearch.value : {}
		};
	})];
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+tree-select@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/tree-select/dist/hooks/useTreeData.js
function buildTreeStructure(nodes, config) {
	const { id, pId, rootPId } = config;
	const nodeMap = /* @__PURE__ */ new Map();
	const rootNodes = [];
	nodes.forEach((node) => {
		const nodeKey = node[id];
		const clonedNode = {
			...node,
			key: node.key || nodeKey
		};
		nodeMap.set(nodeKey, clonedNode);
	});
	nodeMap.forEach((node) => {
		const parentKey = node[pId];
		const parent = nodeMap.get(parentKey);
		if (parent) {
			parent.children = parent.children || [];
			parent.children.push(node);
		} else if (parentKey === rootPId || rootPId === null) rootNodes.push(node);
	});
	return rootNodes;
}
function useTreeData(treeData, simpleMode) {
	return computed(() => {
		if (simpleMode.value) {
			const config = {
				id: "id",
				pId: "pId",
				rootPId: null,
				...typeof simpleMode.value === "object" ? simpleMode.value : {}
			};
			return buildTreeStructure(treeData.value, config);
		}
		return treeData.value;
	});
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+tree-select@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/tree-select/dist/utils/warningPropsUtil.js
function warningProps(props) {
	const { searchPlaceholder, treeCheckStrictly, treeCheckable, labelInValue, value, multiple, showCheckedStrategy, maxCount } = props;
	warning_default(!searchPlaceholder, "`searchPlaceholder` has been removed.");
	if (treeCheckStrictly && labelInValue === false) warning_default(false, "`treeCheckStrictly` will force set `labelInValue` to `true`.");
	if (labelInValue || treeCheckStrictly) warning_default(toArray(value).every((val) => val && typeof val === "object" && "value" in val), "Invalid prop `value` supplied to `TreeSelect`. You should use { label: string, value: string | number } or [{ label: string, value: string | number }] instead.");
	if (treeCheckStrictly || multiple || treeCheckable) warning_default(!value || Array.isArray(value), "`value` should be an array when `TreeSelect` is checkable or multiple.");
	else warning_default(!Array.isArray(value), "`value` should not be array when `TreeSelect` is single mode.");
	if (maxCount && (showCheckedStrategy === "SHOW_ALL" && !treeCheckStrictly || showCheckedStrategy === "SHOW_PARENT")) warning_default(false, "`maxCount` not work with `showCheckedStrategy=SHOW_ALL` (when `treeCheckStrictly=false`) or `showCheckedStrategy=SHOW_PARENT`.");
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+tree-select@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/tree-select/dist/TreeSelect.js
function isRawValue(value) {
	return !value || typeof value !== "object";
}
var defaults$1 = {
	prefixCls: "vc-tree-select",
	listHeight: 200,
	listItemHeight: 20,
	listItemScrollOffset: 0,
	popupMatchSelectWidth: true
};
var omitKeyList = [
	"id",
	"prefixCls",
	"value",
	"defaultValue",
	"onChange",
	"showSearch",
	"searchValue",
	"inputValue",
	"onSearch",
	"autoClearSearchValue",
	"filterTreeNode",
	"treeNodeFilterProp",
	"onSelect",
	"onDeselect",
	"showCheckedStrategy",
	"treeNodeLabelProp",
	"fieldNames",
	"multiple",
	"treeCheckable",
	"treeCheckStrictly",
	"labelInValue",
	"maxCount",
	"treeData",
	"treeDataSimpleMode",
	"treeDefaultExpandAll",
	"treeExpandedKeys",
	"treeDefaultExpandedKeys",
	"onTreeExpand",
	"treeExpandAction",
	"virtual",
	"listHeight",
	"listItemHeight",
	"listItemScrollOffset",
	"onPopupVisibleChange",
	"popupMatchSelectWidth",
	"treeTitleRender",
	"treeLine",
	"treeIcon",
	"showTreeIcon",
	"switcherIcon",
	"treeMotion",
	"treeLoadedKeys",
	"onTreeLoad",
	"loadData",
	"onPopupScroll",
	"classNames",
	"styles"
];
//#endregion
//#region ../../node_modules/.pnpm/@v-c+tree-select@1.0.3_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/tree-select/dist/index.js
var ExportTreeSelect = /* @__PURE__ */ defineComponent({
	props: /* @__PURE__ */ mergeDefaults({
		prefixCls: {
			type: String,
			required: false,
			default: void 0
		},
		id: {
			type: String,
			required: false,
			default: void 0
		},
		classNames: {
			type: Object,
			required: false,
			default: void 0
		},
		styles: {
			type: Object,
			required: false,
			default: void 0
		},
		value: {
			required: false,
			default: void 0
		},
		defaultValue: {
			required: false,
			default: void 0
		},
		onChange: {
			type: Function,
			required: false,
			default: void 0
		},
		showSearch: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		},
		searchValue: {
			type: String,
			required: false,
			default: void 0
		},
		inputValue: {
			type: String,
			required: false,
			default: void 0
		},
		onSearch: {
			type: Function,
			required: false,
			default: void 0
		},
		autoClearSearchValue: {
			type: Boolean,
			required: false,
			default: void 0
		},
		filterTreeNode: {
			type: [Boolean, Function],
			required: false,
			default: void 0
		},
		treeNodeFilterProp: {
			type: String,
			required: false,
			default: void 0
		},
		onSelect: {
			type: Function,
			required: false,
			default: void 0
		},
		onDeselect: {
			type: Function,
			required: false,
			default: void 0
		},
		showCheckedStrategy: {
			required: false,
			default: void 0
		},
		treeNodeLabelProp: {
			type: String,
			required: false,
			default: void 0
		},
		fieldNames: {
			type: Object,
			required: false,
			default: void 0
		},
		multiple: {
			type: Boolean,
			required: false,
			default: void 0
		},
		treeCheckable: {
			type: [
				Boolean,
				Object,
				Function,
				String,
				Number,
				null,
				Array
			],
			required: false,
			default: void 0
		},
		treeCheckStrictly: {
			type: Boolean,
			required: false,
			default: void 0
		},
		labelInValue: {
			type: Boolean,
			required: false,
			default: void 0
		},
		maxCount: {
			type: Number,
			required: false,
			default: void 0
		},
		treeData: {
			type: Array,
			required: false,
			default: void 0
		},
		treeDataSimpleMode: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		},
		loadData: {
			type: Function,
			required: false,
			default: void 0
		},
		treeLoadedKeys: {
			type: Array,
			required: false,
			default: void 0
		},
		onTreeLoad: {
			type: Function,
			required: false,
			default: void 0
		},
		treeDefaultExpandAll: {
			type: Boolean,
			required: false,
			default: void 0
		},
		treeExpandedKeys: {
			type: Array,
			required: false,
			default: void 0
		},
		treeDefaultExpandedKeys: {
			type: Array,
			required: false,
			default: void 0
		},
		onTreeExpand: {
			type: Function,
			required: false,
			default: void 0
		},
		treeExpandAction: {
			type: [Boolean, String],
			required: false,
			default: void 0
		},
		virtual: {
			type: Boolean,
			required: false,
			default: void 0
		},
		listHeight: {
			type: Number,
			required: false,
			default: void 0
		},
		listItemHeight: {
			type: Number,
			required: false,
			default: void 0
		},
		listItemScrollOffset: {
			type: Number,
			required: false,
			default: void 0
		},
		onPopupVisibleChange: {
			type: Function,
			required: false,
			default: void 0
		},
		treeTitleRender: {
			type: Function,
			required: false,
			default: void 0
		},
		treeLine: {
			type: Boolean,
			required: false,
			default: void 0
		},
		treeIcon: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		showTreeIcon: {
			type: Boolean,
			required: false,
			default: void 0
		},
		switcherIcon: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		treeMotion: {
			required: false,
			default: void 0
		},
		onPopupScroll: {
			type: Function,
			required: false,
			default: void 0
		},
		popupMatchSelectWidth: {
			type: [Boolean, Number],
			required: false,
			default: void 0
		},
		className: {
			type: String,
			required: false,
			default: void 0
		},
		style: {
			type: Object,
			required: false,
			default: void 0
		},
		tagRender: {
			type: Function,
			required: false,
			default: void 0
		},
		direction: {
			type: String,
			required: false,
			default: void 0
		},
		autoFocus: {
			type: Boolean,
			required: false,
			default: void 0
		},
		placeholder: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		title: {
			type: String,
			required: false,
			default: void 0
		},
		tabIndex: {
			type: Number,
			required: false,
			default: void 0
		},
		notFoundContent: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		onClear: {
			type: Function,
			required: false,
			default: void 0
		},
		maxLength: {
			type: Number,
			required: false,
			default: void 0
		},
		showScrollBar: {
			type: [Boolean, String],
			required: false,
			default: void 0
		},
		choiceTransitionName: {
			type: String,
			required: false,
			default: void 0
		},
		disabled: {
			type: Boolean,
			required: false,
			default: void 0
		},
		loading: {
			type: Boolean,
			required: false,
			default: void 0
		},
		open: {
			type: Boolean,
			required: false,
			default: void 0
		},
		defaultOpen: {
			type: Boolean,
			required: false,
			default: void 0
		},
		getInputElement: {
			type: Function,
			required: false,
			default: void 0
		},
		getRawInputElement: {
			type: Function,
			required: false,
			default: void 0
		},
		maxTagTextLength: {
			type: Number,
			required: false,
			default: void 0
		},
		maxTagCount: {
			type: [Number, String],
			required: false,
			default: void 0
		},
		maxTagPlaceholder: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		tokenSeparators: {
			type: Array,
			required: false,
			default: void 0
		},
		allowClear: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		},
		prefix: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		suffixIcon: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		suffix: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		clearIcon: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		removeIcon: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		animation: {
			type: String,
			required: false,
			default: void 0
		},
		transitionName: {
			type: String,
			required: false,
			default: void 0
		},
		popupStyle: {
			type: Object,
			required: false,
			default: void 0
		},
		popupClassName: {
			type: String,
			required: false,
			default: void 0
		},
		popupRender: {
			type: Function,
			required: false,
			default: void 0
		},
		popupAlign: {
			type: Object,
			required: false,
			default: void 0
		},
		placement: {
			type: String,
			required: false,
			default: void 0
		},
		builtinPlacements: {
			type: Object,
			required: false,
			default: void 0
		},
		getPopupContainer: {
			type: Function,
			required: false,
			default: void 0
		},
		showAction: {
			type: Array,
			required: false,
			default: void 0
		},
		onBlur: {
			type: Function,
			required: false,
			default: void 0
		},
		onFocus: {
			type: Function,
			required: false,
			default: void 0
		},
		onKeyUp: {
			type: Function,
			required: false,
			default: void 0
		},
		onKeyDown: {
			type: Function,
			required: false,
			default: void 0
		},
		onMouseDown: {
			type: Function,
			required: false,
			default: void 0
		},
		onInputKeyDown: {
			type: Function,
			required: false,
			default: void 0
		},
		onMouseEnter: {
			type: Function,
			required: false,
			default: void 0
		},
		onMouseLeave: {
			type: Function,
			required: false,
			default: void 0
		},
		onClick: {
			type: Function,
			required: false,
			default: void 0
		},
		components: {
			type: Object,
			required: false,
			default: void 0
		}
	}, defaults$1),
	name: "TreeSelect",
	inheritAttrs: false,
	setup(props, { attrs, expose, slots }) {
		const baseSelectRef = shallowRef(null);
		expose({
			focus: () => baseSelectRef.value?.focus(),
			blur: () => baseSelectRef.value?.blur(),
			scrollTo: (arg) => baseSelectRef.value?.scrollTo?.(arg)
		});
		const mergedId = useId_default(props.id);
		const treeConduction = computed(() => !!props.treeCheckable && !props.treeCheckStrictly);
		const mergedCheckable = computed(() => props.treeCheckable || props.treeCheckStrictly);
		const mergedLabelInValue = computed(() => !!props.treeCheckStrictly || !!props.labelInValue);
		const mergedMultiple = computed(() => !!mergedCheckable.value || !!props.multiple);
		const searchProps = computed(() => ({
			searchValue: props.searchValue,
			inputValue: props.inputValue,
			onSearch: props.onSearch,
			autoClearSearchValue: props.autoClearSearchValue,
			filterTreeNode: props.filterTreeNode,
			treeNodeFilterProp: props.treeNodeFilterProp
		}));
		const [mergedShowSearch, searchConfig] = useSearchConfig(computed(() => props.showSearch), searchProps);
		const mergedTreeNodeFilterProp = computed(() => searchConfig.value.treeNodeFilterProp || "value");
		const mergedAutoClearSearchValue = computed(() => searchConfig.value.autoClearSearchValue !== false);
		const internalValue = shallowRef(props?.value ?? props?.defaultValue);
		watch(() => props.value, () => {
			internalValue.value = props?.value;
		});
		const setInternalValue = (val) => {
			internalValue.value = val;
		};
		const mergedShowCheckedStrategy = computed(() => {
			if (!props.treeCheckable) return SHOW_ALL;
			return props.showCheckedStrategy || "SHOW_CHILD";
		});
		warningProps(props);
		const mergedFieldNames = computed(() => fillFieldNames(props.fieldNames));
		const [internalSearchValue, setSearchValue] = useMergedState(() => "", { value: computed(() => searchConfig.value.searchValue) });
		const mergedSearchValue = computed(() => internalSearchValue.value || "");
		const onInternalSearch = (searchText) => {
			setSearchValue(searchText);
			searchConfig.value.onSearch?.(searchText);
		};
		const slotTreeData = shallowRef([]);
		const slotTreeDataSignature = shallowRef("");
		const mergedSourceTreeData = computed(() => {
			if (props.treeData !== void 0) return props.treeData;
			return slotTreeData.value;
		});
		const getTreeDataSignature = (data) => {
			const dig = (list) => {
				return (list || []).map((node) => {
					const key = String(node?.key);
					const children = node?.children;
					return `${key}{${children?.length ? dig(children) : ""}}`;
				}).join("|");
			};
			return dig(data);
		};
		const mergedTreeData = useTreeData(mergedSourceTreeData, computed(() => props.treeDataSimpleMode));
		const { keyEntities, valueEntities } = useDataEntities(mergedTreeData, mergedFieldNames);
		const splitRawValues = (newRawValues) => {
			const missingRawValues = [];
			const existRawValues = [];
			newRawValues.forEach((val) => {
				if (valueEntities.value.has(val)) existRawValues.push(val);
				else missingRawValues.push(val);
			});
			return {
				missingRawValues,
				existRawValues
			};
		};
		const filteredTreeData = useFilterTreeData(mergedTreeData, mergedSearchValue, {
			fieldNames: mergedFieldNames,
			treeNodeFilterProp: mergedTreeNodeFilterProp,
			filterTreeNode: computed(() => searchConfig.value.filterTreeNode)
		});
		const getLabel = (item) => {
			if (!item) return;
			if (props.treeNodeLabelProp) return item[props.treeNodeLabelProp];
			const titleList = mergedFieldNames.value._title;
			for (let i = 0; i < titleList.length; i += 1) {
				const title = item[titleList[i]];
				if (title !== void 0) return title;
			}
		};
		const toLabeledValues = (draftValues) => {
			return toArray(draftValues).map((val) => {
				if (isRawValue(val)) return { value: val };
				return val;
			});
		};
		const renderTreeTitleRender = (node) => {
			let label;
			const labelInfo = props?.treeTitleRender?.(node);
			if (typeof labelInfo === "string" || typeof labelInfo === "number") label = labelInfo;
			else {
				const labelArr = filterEmpty(Array.isArray(labelInfo) ? labelInfo : [labelInfo]);
				if (labelArr.length) label = labelArr.length === 1 ? labelArr[0] : labelArr;
			}
			return label;
		};
		const convert2LabelValues = (draftValues) => {
			return toLabeledValues(draftValues).map((item) => {
				let { label: rawLabel } = item;
				const { value: rawValue, halfChecked: rawHalfChecked } = item;
				let rawDisabled;
				const entity = valueEntities.value.get(rawValue);
				if (entity) {
					rawLabel = props.treeTitleRender ? renderTreeTitleRender(entity.node) : rawLabel ?? getLabel(entity.node);
					rawDisabled = entity.node.disabled;
				} else if (rawLabel === void 0) rawLabel = toLabeledValues(internalValue.value).find((labeledItem) => labeledItem.value === rawValue)?.label;
				return {
					label: rawLabel,
					value: rawValue,
					halfChecked: rawHalfChecked,
					disabled: rawDisabled
				};
			});
		};
		const rawMixedLabeledValues = computed(() => toLabeledValues(internalValue.value === null ? [] : internalValue.value));
		const rawLabeledValues = computed(() => rawMixedLabeledValues.value.filter((item) => !item.halfChecked));
		const rawHalfLabeledValues = computed(() => rawMixedLabeledValues.value.filter((item) => !!item.halfChecked));
		const rawValues = computed(() => rawLabeledValues.value.map((item) => item.value));
		const [rawCheckedValues, rawHalfCheckedValues] = useCheckedKeys(rawLabeledValues, rawHalfLabeledValues, treeConduction, keyEntities);
		const [cachedDisplayValues] = useCache(computed(() => {
			const rawDisplayValues = convert2LabelValues(formatStrategyValues(rawCheckedValues.value, mergedShowCheckedStrategy.value, keyEntities.value, mergedFieldNames.value).map((key) => keyEntities.value[String(key)]?.node?.[mergedFieldNames.value.value] ?? key).map((val) => {
				const targetItem = rawLabeledValues.value.find((item) => item.value === val);
				let label;
				if (props.labelInValue) label = targetItem?.label;
				else label = renderTreeTitleRender(targetItem);
				return {
					value: val,
					label
				};
			}));
			const firstVal = rawDisplayValues[0];
			if (!mergedMultiple.value && firstVal && isNil(firstVal.value) && isNil(firstVal.label)) return [];
			return rawDisplayValues.map((item) => ({
				...item,
				label: item.label ?? item.value
			}));
		}));
		const mergedMaxCount = computed(() => {
			if (mergedMultiple.value && (mergedShowCheckedStrategy.value === "SHOW_CHILD" || props.treeCheckStrictly || !props.treeCheckable)) return props.maxCount;
			return null;
		});
		const triggerChange = useRefFunc((newRawValues, extra, source) => {
			const formattedKeyList = formatStrategyValues(newRawValues, mergedShowCheckedStrategy.value, keyEntities.value, mergedFieldNames.value);
			if (mergedMaxCount.value && formattedKeyList.length > mergedMaxCount.value) return;
			setInternalValue(convert2LabelValues(newRawValues));
			if (mergedAutoClearSearchValue.value) setSearchValue("");
			if (props.onChange) {
				let eventValues = newRawValues;
				if (treeConduction.value) eventValues = formattedKeyList.map((key) => {
					const entity = valueEntities.value.get(key);
					return entity ? entity.node[mergedFieldNames.value.value] : key;
				});
				const { triggerValue, selected } = extra || {
					triggerValue: void 0,
					selected: void 0
				};
				let returnRawValues = eventValues;
				if (props.treeCheckStrictly) {
					const halfValues = rawHalfLabeledValues.value.filter((item) => !eventValues.includes(item.value));
					returnRawValues = [...returnRawValues, ...halfValues];
				}
				const returnLabeledValues = convert2LabelValues(returnRawValues);
				const additionalInfo = {
					preValue: rawLabeledValues.value,
					triggerValue
				};
				let showPosition = true;
				if (props.treeCheckStrictly || source === "selection" && !selected) showPosition = false;
				fillAdditionalInfo(additionalInfo, triggerValue, newRawValues, mergedTreeData.value, showPosition, mergedFieldNames.value);
				if (mergedCheckable.value) additionalInfo.checked = selected;
				else additionalInfo.selected = selected;
				const returnValues = mergedLabelInValue.value ? returnLabeledValues : returnLabeledValues.map((item) => item.value);
				props.onChange(mergedMultiple.value ? returnValues : returnValues[0], mergedLabelInValue.value ? null : returnLabeledValues.map((item) => item.label), additionalInfo);
			}
		});
		const onOptionSelect = (selectedKey, { selected, source }) => {
			const node = keyEntities.value[String(selectedKey)]?.node;
			const selectedValue = node?.[mergedFieldNames.value.value] ?? selectedKey;
			if (!mergedMultiple.value) triggerChange([selectedValue], {
				selected: true,
				triggerValue: selectedValue
			}, "option");
			else {
				let newRawValues = selected ? [...rawValues.value, selectedValue] : rawCheckedValues.value.filter((v) => v !== selectedValue);
				if (treeConduction.value) {
					const { missingRawValues, existRawValues } = splitRawValues(newRawValues);
					const keyList = existRawValues.map((val) => {
						const entity = valueEntities.value.get(val);
						return entity ? entity.key : val;
					});
					let checkedKeys;
					if (selected) ({checkedKeys} = conductCheck(keyList, true, keyEntities.value));
					else ({checkedKeys} = conductCheck(keyList, {
						checked: false,
						halfCheckedKeys: rawHalfCheckedValues.value
					}, keyEntities.value));
					newRawValues = [...missingRawValues, ...checkedKeys.map((key) => keyEntities.value[String(key)].node[mergedFieldNames.value.value])];
				}
				triggerChange(newRawValues, {
					selected,
					triggerValue: selectedValue
				}, source || "option");
			}
			if (selected || !mergedMultiple.value) props.onSelect?.(selectedValue, fillLegacyProps(node));
			else props.onDeselect?.(selectedValue, fillLegacyProps(node));
		};
		const onInternalPopupVisibleChange = (open) => {
			props.onPopupVisibleChange?.(open);
		};
		const onDisplayValuesChange = useRefFunc((newValues, info) => {
			const newRawValues = newValues.map((item) => item.value);
			if (info.type === "clear") {
				triggerChange(newRawValues, {}, "selection");
				return;
			}
			if (info.values.length) onOptionSelect(info.values[0].value, {
				selected: false,
				source: "selection"
			});
		});
		useTreeSelectProvider(computed(() => {
			return {
				virtual: props.virtual,
				popupMatchSelectWidth: props.popupMatchSelectWidth ?? defaults$1.popupMatchSelectWidth,
				listHeight: props.listHeight ?? defaults$1.listHeight,
				listItemHeight: props.listItemHeight ?? defaults$1.listItemHeight,
				listItemScrollOffset: props.listItemScrollOffset ?? defaults$1.listItemScrollOffset,
				treeData: filteredTreeData.value,
				fieldNames: mergedFieldNames.value,
				onSelect: onOptionSelect,
				treeExpandAction: props.treeExpandAction,
				treeTitleRender: props.treeTitleRender,
				onPopupScroll: props.onPopupScroll,
				leftMaxCount: props.maxCount === void 0 ? null : props.maxCount - cachedDisplayValues.value.length,
				leafCountOnly: mergedShowCheckedStrategy.value === "SHOW_CHILD" && !props.treeCheckStrictly && !!props.treeCheckable,
				valueEntities: valueEntities.value,
				classNames: props.classNames,
				styles: props.styles
			};
		}));
		useLegacyProvider(computed(() => ({
			checkable: mergedCheckable.value,
			loadData: props.loadData,
			treeLoadedKeys: props.treeLoadedKeys,
			onTreeLoad: props.onTreeLoad,
			checkedKeys: rawCheckedValues.value,
			halfCheckedKeys: rawHalfCheckedValues.value,
			treeDefaultExpandAll: props.treeDefaultExpandAll,
			treeExpandedKeys: props.treeExpandedKeys,
			treeDefaultExpandedKeys: props.treeDefaultExpandedKeys || [],
			onTreeExpand: props.onTreeExpand,
			treeIcon: props.treeIcon,
			treeMotion: props.treeMotion,
			showTreeIcon: props.showTreeIcon,
			switcherIcon: props.switcherIcon,
			treeLine: props.treeLine,
			treeNodeFilterProp: mergedTreeNodeFilterProp.value,
			keyEntities: keyEntities.value
		})));
		return () => {
			if (props.treeData === void 0) {
				const parsed = convertChildrenToData(slots.default?.() ?? []);
				const signature = getTreeDataSignature(parsed);
				if (signature !== slotTreeDataSignature.value) {
					slotTreeDataSignature.value = signature;
					slotTreeData.value = parsed;
				}
			}
			return createVNode(BaseSelect, mergeProps({ ...attrs }, omit(props, omitKeyList), {
				"ref": (el) => {
					baseSelectRef.value = el;
				},
				"id": mergedId,
				"prefixCls": props.prefixCls || defaults$1.prefixCls,
				"mode": mergedMultiple.value ? "multiple" : void 0,
				"classNames": props.classNames,
				"styles": props.styles,
				"displayValues": cachedDisplayValues.value,
				"onDisplayValuesChange": onDisplayValuesChange,
				"autoClearSearchValue": mergedAutoClearSearchValue.value,
				"showSearch": mergedShowSearch.value,
				"searchValue": mergedSearchValue.value,
				"onSearch": (v) => {
					onInternalSearch(v);
				},
				"OptionList": OptionList_default,
				"emptyOptions": !mergedTreeData.value.length,
				"onPopupVisibleChange": onInternalPopupVisibleChange,
				"popupMatchSelectWidth": props.popupMatchSelectWidth ?? defaults$1.popupMatchSelectWidth
			}), null);
		};
	}
});
ExportTreeSelect.TreeNode = TreeNode_default;
ExportTreeSelect.SHOW_ALL = SHOW_ALL;
ExportTreeSelect.SHOW_PARENT = SHOW_PARENT;
ExportTreeSelect.SHOW_CHILD = SHOW_CHILD;
var src_default = ExportTreeSelect;
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/tree-select/index.js
var defaults = {
	listHeight: 256,
	choiceTransitionName: "",
	treeIcon: false
};
var omitKeys = [
	"prefixCls",
	"size",
	"disabled",
	"bordered",
	"style",
	"className",
	"rootClassName",
	"treeCheckable",
	"multiple",
	"listHeight",
	"listItemHeight",
	"placement",
	"notFoundContent",
	"switcherIcon",
	"treeLine",
	"getPopupContainer",
	"popupClassName",
	"dropdownClassName",
	"treeIcon",
	"transitionName",
	"choiceTransitionName",
	"status",
	"treeExpandAction",
	"builtinPlacements",
	"dropdownMatchSelectWidth",
	"popupMatchSelectWidth",
	"allowClear",
	"variant",
	"dropdownStyle",
	"dropdownRender",
	"popupRender",
	"tagRender",
	"maxCount",
	"showCheckedStrategy",
	"treeCheckStrictly",
	"styles",
	"classes"
];
var TreeSelect = /* @__PURE__ */ defineComponent((props, { slots, expose, emit, attrs }) => {
	const { prefixCls: treeSelectPrefixCls, getPopupContainer: getContextPopupContainer, direction, styles: contextStyles, classes: contextClassNames, switcherIcon, virtual, getPrefixCls, rootPrefixCls } = useComponentBaseConfig("treeSelect", props, ["switcherIcon"], "tree-select");
	const configCtx = useConfig();
	const { prefixCls: customizePrefixCls, variant: customVariant, size: customizeSize, disabled: customDisabled, status: customStatus, classes, styles } = toPropsRefs(props, "prefixCls", "variant", "size", "disabled", "status", "classes", "styles");
	const bordered = computed(() => props?.bordered ?? true);
	const [, token] = useToken();
	const listItemHeight = computed(() => props?.listItemHeight ?? token?.value?.controlHeightSM + token?.value?.paddingXXS);
	{
		const { treeCheckable, multiple } = props;
		const warning = devUseWarning("TreeSelect");
		Object.entries({
			dropdownMatchSelectWidth: "popupMatchSelectWidth",
			dropdownStyle: "styles.popup.root",
			dropdownClassName: "classNames.popup.root",
			popupClassName: "classNames.popup.root",
			dropdownRender: "popupRender",
			onDropdownVisibleChange: "onOpenChange",
			bordered: "variant"
		}).forEach(([oldProp, newProp]) => {
			warning.deprecated(!(props[oldProp] !== void 0), oldProp, newProp);
		});
		warning(multiple !== false || !treeCheckable, "usage", "`multiple` will always be `true` when `treeCheckable` is true");
		warning(!(props.showArrow !== void 0), "deprecated", "`showArrow` is deprecated which will be removed in next major version. It will be a default behavior, you can hide it by setting `suffixIcon` to null.");
	}
	const prefixCls = computed(() => getPrefixCls("select", customizePrefixCls.value));
	const treePrefixCls = computed(() => getPrefixCls("select-tree", customizePrefixCls.value));
	const { compactSize, compactItemClassnames } = useCompactItemContext(prefixCls, direction);
	const rootCls = useCSSVarCls_default(prefixCls);
	const treeSelectRootCls = useCSSVarCls_default(treeSelectPrefixCls);
	const [hashId, cssVarCls] = style_default$1(prefixCls, rootCls);
	useTreeSelectStyle(treeSelectPrefixCls, treePrefixCls, treeSelectRootCls);
	const [variant, enableVariantCls] = useVariant("treeSelect", customVariant, bordered);
	const mergedSize = useSize((ctx) => customizeSize.value ?? compactSize.value ?? ctx);
	const disabled = useDisabledContext();
	const mergedDisabled = computed(() => customDisabled.value ?? disabled.value);
	const formItemInputContext = useFormItemInputContext();
	const mergedStatus = computed(() => getMergedStatus(formItemInputContext.value.status, customStatus.value));
	const mergedProps = computed(() => ({
		...props,
		size: mergedSize.value,
		disabled: mergedDisabled.value,
		status: mergedStatus.value,
		variant: variant.value
	}));
	const [mergedClassNames, mergedStyles] = useMergeSemantic(useToArr(contextClassNames, classes), useToArr(contextStyles, styles), useToProps(mergedProps), computed(() => {
		return { popup: { _default: "root" } };
	}));
	const mergedOnOpenChange = (open) => {
		emit("openChange", open);
		emit("dropdownVisibleChange", open);
	};
	const mergedMaxCount = computed(() => {
		const { maxCount, showCheckedStrategy, treeCheckStrictly } = props;
		if (maxCount && (showCheckedStrategy === "SHOW_ALL" && !treeCheckStrictly || showCheckedStrategy === "SHOW_PARENT")) return;
		return maxCount;
	});
	const memoizedPlacement = computed(() => {
		const { placement } = props;
		if (placement !== void 0) return placement;
		return direction.value === "rtl" ? "bottomRight" : "bottomLeft";
	});
	const [zIndex] = useZIndex("SelectLike", computed(() => mergedStyles.value?.root?.zIndex));
	const treeSelectRef = shallowRef();
	expose({
		focus: () => treeSelectRef.value?.focus(),
		blur: () => treeSelectRef.value?.blur(),
		scrollTo: (arg) => treeSelectRef.value?.scrollTo?.(arg)
	});
	return () => {
		const { popupClassName, dropdownClassName, rootClass, popupRender, dropdownRender, popupMatchSelectWidth, dropdownMatchSelectWidth, treeCheckable, multiple, allowClear, treeLine, listHeight, builtinPlacements, switcherIcon: customSwitcherIcon, treeIcon, getPopupContainer, choiceTransitionName, transitionName, treeExpandAction, showCheckedStrategy, treeCheckStrictly } = props;
		const restProps = omit$1(props, omitKeys);
		const treeTitleRender = slots?.treeTitleRender ?? props.treeTitleRender;
		const mergedPopupClassName = clsx(popupClassName || dropdownClassName, `${treeSelectPrefixCls.value}-dropdown`, { [`${treeSelectPrefixCls.value}-dropdown-rtl`]: direction.value === "rtl" }, rootClass, mergedClassNames.value.root, mergedClassNames.value.popup?.root, cssVarCls.value, rootCls.value, treeSelectRootCls.value, hashId.value);
		const { style, className, restAttrs } = getAttrStyleAndClass(attrs);
		const mergedPopupRender = usePopupRender_default(popupRender || dropdownRender);
		const customSuffixIcon = getSlotPropsFnRun(slots, props, "suffixIcon", false);
		const showSuffixIcon = useShowArrow(customSuffixIcon, props.showArrow);
		const mergedPopupMatchSelectWidth = popupMatchSelectWidth ?? dropdownMatchSelectWidth ?? configCtx.value?.popupMatchSelectWidth;
		const isMultiple = !!(treeCheckable || multiple);
		const { hasFeedback, feedbackIcon, isFormItemInput } = formItemInputContext.value;
		const { suffixIcon, removeIcon, clearIcon } = useIcons({
			...restProps,
			suffixIcon: customSuffixIcon,
			multiple: isMultiple,
			showSuffixIcon,
			hasFeedback,
			feedbackIcon,
			prefixCls: prefixCls.value,
			componentName: "TreeSelect"
		});
		const mergedAllowClear = allowClear === true ? { clearIcon } : allowClear;
		const notFoundContent = getSlotPropsFnRun(slots, props, "notFoundContent", false);
		let mergedNotFound;
		if (notFoundContent !== void 0) mergedNotFound = notFoundContent;
		else mergedNotFound = configCtx?.value?.renderEmpty?.("Select") || createVNode(DefaultRenderEmpty, { "componentName": "Select" }, null);
		const onAttrs = {
			onFocus(e) {
				emit("focus", e);
			},
			onBlur(e) {
				emit("blur", e);
			},
			onSelect(value, option) {
				emit("select", value, option);
			},
			onChange(value, labelList, extra) {
				emit("change", value, labelList, extra);
				emit("update:value", value);
			},
			onDeselect(value, option) {
				emit("deselect", value, option);
			},
			onTreeExpand(expandedKeys) {
				emit("treeExpand", expandedKeys);
			},
			onTreeLoad(loadedKeys) {
				emit("treeLoad", loadedKeys);
			},
			onPopupScroll(e) {
				emit("popupScroll", e);
			},
			onSearch(value) {
				emit("search", value);
			}
		};
		const selectProps = omit$1(restProps, [
			"suffixIcon",
			"removeIcon",
			"clearIcon",
			"itemIcon",
			"switcherIcon",
			"classes",
			"styles",
			...Object.keys(onAttrs)
		]);
		const mergedClassName = clsx(!customizePrefixCls.value && treeSelectPrefixCls.value, {
			[`${prefixCls.value}-lg`]: mergedSize.value === "large",
			[`${prefixCls.value}-sm`]: mergedSize.value === "small",
			[`${prefixCls.value}-rtl`]: direction.value === "rtl",
			[`${prefixCls.value}-${variant.value}`]: enableVariantCls.value,
			[`${prefixCls.value}-in-form-item`]: isFormItemInput
		}, getStatusClassNames(prefixCls.value, mergedStatus.value, hasFeedback), compactItemClassnames.value, className, rootClass, mergedClassNames?.value?.root, cssVarCls.value, rootCls.value, treeSelectRootCls.value, hashId.value);
		const mergedSwitcherIcon = slots?.switcherIcon ?? customSwitcherIcon ?? switcherIcon.value;
		const renderSwitcherIcon = (nodeProps) => createVNode(iconUtil_default, {
			"prefixCls": treePrefixCls.value,
			"switcherIcon": mergedSwitcherIcon,
			"treeNodeProps": nodeProps,
			"showLine": treeLine
		}, null);
		const popupOverflow = configCtx.value?.popupOverflow;
		const tagRender = slots?.tagRender ?? props?.tagRender;
		return createVNode(src_default, mergeProps(restAttrs, onAttrs, {
			"ref": treeSelectRef,
			"classNames": mergedClassNames.value,
			"styles": mergedStyles.value,
			"virtual": virtual.value,
			"disabled": mergedDisabled.value
		}, selectProps, {
			"popupMatchSelectWidth": mergedPopupMatchSelectWidth,
			"builtinPlacements": mergedBuiltinPlacements_default(builtinPlacements, popupOverflow),
			"prefixCls": prefixCls.value,
			"className": mergedClassName,
			"style": {
				...mergedStyles.value?.root,
				...style
			},
			"listHeight": listHeight,
			"listItemHeight": listItemHeight.value,
			"treeCheckable": treeCheckable ? createVNode("span", { "class": `${prefixCls.value}-tree-checkbox-inner` }, null) : treeCheckable,
			"treeLine": !!treeLine,
			"suffixIcon": suffixIcon,
			"multiple": isMultiple,
			"placement": memoizedPlacement.value,
			"removeIcon": removeIcon,
			"allowClear": mergedAllowClear,
			"switcherIcon": renderSwitcherIcon,
			"showTreeIcon": treeIcon,
			"notFoundContent": mergedNotFound,
			"getPopupContainer": getPopupContainer || getContextPopupContainer,
			"treeMotion": null,
			"popupClassName": mergedPopupClassName,
			"popupStyle": {
				...mergedStyles.value.root,
				...mergedStyles.value.popup?.root,
				zIndex: zIndex.value
			},
			"popupRender": mergedPopupRender,
			"onPopupVisibleChange": mergedOnOpenChange,
			"choiceTransitionName": getTransitionName(rootPrefixCls.value, "", choiceTransitionName),
			"transitionName": getTransitionName(rootPrefixCls.value, "slide-up", transitionName),
			"treeExpandAction": treeExpandAction,
			"tagRender": isMultiple ? tagRender : void 0,
			"maxCount": mergedMaxCount.value,
			"showCheckedStrategy": showCheckedStrategy,
			"treeCheckStrictly": treeCheckStrictly,
			"treeTitleRender": treeTitleRender
		}), null);
	};
}, {
	props: /* @__PURE__ */ mergeDefaults({
		styles: {
			type: [Object, Function],
			required: false
		},
		classes: {
			type: [Object, Function],
			required: false
		},
		suffixIcon: {
			type: [
				Function,
				String,
				Number,
				null,
				Object,
				Boolean
			],
			required: false,
			default: void 0
		},
		size: {
			type: [String, null],
			required: false
		},
		disabled: {
			type: Boolean,
			required: false,
			default: void 0
		},
		placement: {
			type: String,
			required: false
		},
		popupClassName: {
			type: String,
			required: false
		},
		dropdownClassName: {
			type: String,
			required: false
		},
		dropdownRender: {
			type: Function,
			required: false
		},
		popupRender: {
			type: Function,
			required: false
		},
		dropdownStyle: {
			type: Object,
			required: false
		},
		bordered: {
			type: Boolean,
			required: false,
			default: void 0
		},
		treeLine: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		},
		status: {
			type: String,
			required: false
		},
		switcherIcon: {
			type: Function,
			required: false,
			skipCheck: true
		},
		rootClass: {
			type: String,
			required: false
		},
		dropdownMatchSelectWidth: {
			type: [Boolean, Number],
			required: false,
			default: void 0
		},
		popupMatchSelectWidth: {
			type: [Boolean, Number],
			required: false,
			default: void 0
		},
		showArrow: {
			type: Boolean,
			required: false,
			default: void 0
		},
		variant: {
			type: String,
			required: false
		},
		prefixCls: {
			type: String,
			required: false
		},
		id: {
			type: String,
			required: false
		},
		value: { required: false },
		defaultValue: { required: false },
		showSearch: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		},
		searchValue: {
			type: String,
			required: false
		},
		inputValue: {
			type: String,
			required: false
		},
		autoClearSearchValue: {
			type: Boolean,
			required: false,
			default: void 0
		},
		filterTreeNode: {
			type: [Boolean, Function],
			required: false,
			default: void 0
		},
		treeNodeFilterProp: {
			type: String,
			required: false
		},
		showCheckedStrategy: { required: false },
		treeNodeLabelProp: {
			type: String,
			required: false
		},
		fieldNames: {
			type: Object,
			required: false
		},
		multiple: {
			type: Boolean,
			required: false,
			default: void 0
		},
		treeCheckable: {
			type: [
				Boolean,
				Object,
				Function,
				String,
				Number,
				null,
				Array
			],
			required: false,
			default: void 0
		},
		treeCheckStrictly: {
			type: Boolean,
			required: false,
			default: void 0
		},
		labelInValue: {
			type: Boolean,
			required: false,
			default: void 0
		},
		maxCount: {
			type: Number,
			required: false
		},
		treeData: {
			type: Array,
			required: false
		},
		treeDataSimpleMode: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		},
		loadData: {
			type: Function,
			required: false
		},
		treeLoadedKeys: {
			type: Array,
			required: false
		},
		treeDefaultExpandAll: {
			type: Boolean,
			required: false,
			default: void 0
		},
		treeExpandedKeys: {
			type: Array,
			required: false
		},
		treeDefaultExpandedKeys: {
			type: Array,
			required: false
		},
		treeExpandAction: {
			type: [Boolean, String],
			required: false,
			default: void 0
		},
		virtual: {
			type: Boolean,
			required: false,
			default: void 0
		},
		listHeight: {
			type: Number,
			required: false
		},
		listItemHeight: {
			type: Number,
			required: false
		},
		listItemScrollOffset: {
			type: Number,
			required: false
		},
		treeTitleRender: {
			type: Function,
			required: false
		},
		treeIcon: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		className: {
			type: String,
			required: false
		},
		tagRender: {
			type: Function,
			required: false
		},
		direction: {
			type: String,
			required: false
		},
		autoFocus: {
			type: Boolean,
			required: false,
			default: void 0
		},
		placeholder: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		title: {
			type: String,
			required: false
		},
		tabIndex: {
			type: Number,
			required: false
		},
		notFoundContent: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		onClear: {
			type: Function,
			required: false
		},
		maxLength: {
			type: Number,
			required: false
		},
		showScrollBar: {
			type: [Boolean, String],
			required: false,
			default: void 0
		},
		choiceTransitionName: {
			type: String,
			required: false
		},
		loading: {
			type: Boolean,
			required: false,
			default: void 0
		},
		open: {
			type: Boolean,
			required: false,
			default: void 0
		},
		defaultOpen: {
			type: Boolean,
			required: false,
			default: void 0
		},
		getRawInputElement: {
			type: Function,
			required: false
		},
		maxTagTextLength: {
			type: Number,
			required: false
		},
		maxTagCount: {
			type: [Number, String],
			required: false
		},
		maxTagPlaceholder: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		tokenSeparators: {
			type: Array,
			required: false
		},
		allowClear: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		},
		prefix: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		suffix: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		clearIcon: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		removeIcon: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		animation: {
			type: String,
			required: false
		},
		transitionName: {
			type: String,
			required: false
		},
		popupStyle: {
			type: Object,
			required: false
		},
		popupAlign: {
			type: Object,
			required: false
		},
		builtinPlacements: {
			type: Object,
			required: false
		},
		getPopupContainer: {
			type: Function,
			required: false
		},
		showAction: {
			type: Array,
			required: false
		},
		onBlur: {
			type: Function,
			required: false
		},
		onFocus: {
			type: Function,
			required: false
		},
		onKeyUp: {
			type: Function,
			required: false
		},
		onKeyDown: {
			type: Function,
			required: false
		},
		onMouseDown: {
			type: Function,
			required: false
		},
		onInputKeyDown: {
			type: Function,
			required: false
		},
		onMouseEnter: {
			type: Function,
			required: false
		},
		onMouseLeave: {
			type: Function,
			required: false
		},
		onClick: {
			type: Function,
			required: false
		},
		components: {
			type: Object,
			required: false
		}
	}, defaults),
	emits: [
		"focus",
		"blur",
		"openChange",
		"dropdownVisibleChange",
		"select",
		"treeExpand",
		"treeLoad",
		"change",
		"update:value",
		"deselect",
		"popupScroll",
		"search"
	],
	name: "ATreeSelect",
	inheritAttrs: false
});
var TreeSelectNode = TreeNode_default;
TreeSelect.TreeNode = TreeNode_default;
TreeSelect.SHOW_ALL = SHOW_ALL;
TreeSelect.SHOW_PARENT = SHOW_PARENT;
TreeSelect.SHOW_CHILD = SHOW_CHILD;
TreeSelect.install = (app) => {
	app.component(TreeSelect.name, TreeSelect);
	app.component("ATreeSelectOption", TreeSelectNode);
	return app;
};
TreeSelect._InternalPanelDoNotUseOrYouWillBeFired = PurePanel_default(TreeSelect, "popupAlign", (props) => omit$1(props, ["visible"]));
var tree_select_default = TreeSelect;
//#endregion
export { FileOutlined as a, style_default as i, tree_select_default as n, iconUtil_default as r, TreeSelectNode as t };

//# sourceMappingURL=tree-select-Cw66J6ob.js.map