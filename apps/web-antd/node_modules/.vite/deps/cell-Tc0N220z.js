import { o as __toESM } from "./chunk-CqwQKh_b.js";
import { Gn as reactive, _n as watch, mt as h } from "./runtime-dom.esm-bundler-BEW9uiVc.js";
import "./vue.js";
import { t as require_xe_utils } from "./xe-utils-CectUEgD.js";
import { t as VxeUI } from "./index.esm-9ZlbHbuz.js";
import { F as getFuncText, N as eqEmptyValue, P as formatText, z as isEnableConf } from "./ui-BV4jffQB.js";
import { i as getSlotVNs, o as errLog, s as warnLog } from "./vn-BPdvZq5I.js";
//#region ../../node_modules/.pnpm/vxe-table@4.18.13_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-table/es/table/src/columnInfo.js
var import_xe_utils = /* @__PURE__ */ __toESM(require_xe_utils());
var { getI18n: getI18n$1, formats, renderer: renderer$1 } = VxeUI;
var ColumnInfo = class {
	constructor($xeTable, _vm, { renderHeader, renderCell, renderFooter, renderData } = {}) {
		const tableProps = $xeTable.props;
		const $xeGrid = $xeTable.xeGrid;
		const $xeGantt = $xeTable.xeGantt;
		const $xeGGWrapper = $xeGrid || $xeGantt;
		const { field, editRender, filterRender, headerFormatter } = _vm;
		const colId = _vm.colId || import_xe_utils.default.uniqueId("col_");
		const formatter = _vm.formatter;
		const visible = import_xe_utils.default.isBoolean(_vm.visible) ? _vm.visible : true;
		const flCompConf = isEnableConf(filterRender) ? renderer$1.get(filterRender.name) : null;
		const ctFilterOptions = flCompConf ? flCompConf.createTableFilterOptions : null;
		const filters = toFilters(_vm.filters, colId);
		if (headerFormatter) errLog("vxe.error.notProp", ["header-formatter"]);
		const types = [
			"seq",
			"checkbox",
			"radio",
			"expand",
			"html"
		];
		if (_vm.type && types.indexOf(_vm.type) === -1) warnLog("vxe.error.errProp", [`type=${_vm.type}`, types.join(", ")]);
		if (import_xe_utils.default.isBoolean(_vm.cellRender) || _vm.cellRender && !import_xe_utils.default.isObject(_vm.cellRender)) warnLog("vxe.error.errProp", [`column.cell-render=${_vm.cellRender}`, "column.cell-render={}"]);
		if (import_xe_utils.default.isBoolean(_vm.editRender) || _vm.editRender && !import_xe_utils.default.isObject(_vm.editRender)) warnLog("vxe.error.errProp", [`column.edit-render=${_vm.editRender}`, "column.edit-render={}"]);
		if (_vm.type === "expand") {
			const { treeConfig } = tableProps;
			const { computeTreeOpts } = $xeTable.getComputeMaps();
			const treeOpts = computeTreeOpts.value;
			if (treeConfig && (treeOpts.showLine || treeOpts.line)) errLog("vxe.error.errConflicts", ["tree-config.showLine", "column.type=expand"]);
		}
		if (formatter) {
			if (import_xe_utils.default.isString(formatter)) {
				const gFormatOpts = formats.get(formatter) || import_xe_utils.default[formatter];
				if (!gFormatOpts || !import_xe_utils.default.isFunction(gFormatOpts.tableCellFormatMethod || gFormatOpts.cellFormatMethod)) errLog("vxe.error.notFormats", [formatter]);
			} else if (import_xe_utils.default.isArray(formatter)) {
				const gFormatOpts = formats.get(formatter[0]) || import_xe_utils.default[formatter[0]];
				if (!gFormatOpts || !import_xe_utils.default.isFunction(gFormatOpts.tableCellFormatMethod || gFormatOpts.cellFormatMethod)) errLog("vxe.error.notFormats", [formatter[0]]);
			}
		}
		if (_vm.aggFunc) {
			if (!$xeTable.handlePivotTableAggregateData && _vm.aggFunc !== true) errLog("vxe.error.errProp", [`column.agg-func=${_vm.aggFunc}`, "column.agg-func=true"]);
		}
		if (field && editRender) {
			if (editRender.startField && `${editRender.startField}`.indexOf(field) >= 0) errLog("vxe.error.modelConflicts", [`field=${field}`, `edit-render.startField=${editRender.startField}`]);
			if (editRender.endField && `${editRender.endField}`.indexOf(field) >= 0) errLog("vxe.error.modelConflicts", [`field=${field}`, `edit-render.endField=${editRender.endField}`]);
		}
		Object.assign(this, {
			type: _vm.type,
			property: _vm.field,
			field,
			title: _vm.title,
			width: _vm.width,
			minWidth: _vm.minWidth,
			maxWidth: _vm.maxWidth,
			resizable: _vm.resizable,
			fixed: _vm.fixed,
			align: _vm.align,
			headerAlign: _vm.headerAlign,
			footerAlign: _vm.footerAlign,
			showOverflow: _vm.showOverflow,
			showHeaderOverflow: _vm.showHeaderOverflow,
			showFooterOverflow: _vm.showFooterOverflow,
			className: _vm.className,
			headerClassName: _vm.headerClassName,
			footerClassName: _vm.footerClassName,
			formatter,
			headerFormatter: _vm.headerFormatter,
			footerFormatter: _vm.footerFormatter,
			padding: _vm.padding,
			verticalAlign: _vm.verticalAlign,
			sortable: _vm.sortable,
			sortBy: _vm.sortBy,
			sortType: _vm.sortType,
			filters,
			filterMultiple: import_xe_utils.default.isBoolean(_vm.filterMultiple) ? _vm.filterMultiple : true,
			filterMethod: _vm.filterMethod,
			filterResetMethod: _vm.filterResetMethod,
			filterRecoverMethod: _vm.filterRecoverMethod,
			filterRender,
			floatingFilters: _vm.floatingFilters,
			rowGroupNode: _vm.rowGroupNode,
			treeNode: _vm.treeNode,
			dragSort: _vm.dragSort,
			rowResize: _vm.rowResize,
			cellType: _vm.cellType,
			cellRender: _vm.cellRender,
			editRender,
			contentRender: _vm.contentRender,
			headerExportMethod: _vm.headerExportMethod,
			exportMethod: _vm.exportMethod,
			footerExportMethod: _vm.footerExportMethod,
			titleHelp: _vm.titleHelp,
			titlePrefix: _vm.titlePrefix,
			titleSuffix: _vm.titleSuffix,
			aggFunc: _vm.aggFunc,
			copyMethod: _vm.copyMethod,
			cutMethod: _vm.cutMethod,
			pasteMethod: _vm.pasteMethod,
			params: _vm.params,
			id: colId,
			parentId: null,
			visible,
			defaultParentId: null,
			halfVisible: false,
			defaultVisible: visible,
			defaultFixed: _vm.fixed,
			defaultAggGroup: _vm.aggGroup,
			defaultAggFunc: _vm.aggFunc,
			checked: false,
			halfChecked: false,
			disabled: false,
			level: 1,
			rowSpan: 1,
			colSpan: 1,
			order: null,
			sortTime: 0,
			sortNumber: 0,
			renderSortNumber: 0,
			renderAggFn: "",
			renderAggDigits: null,
			renderAggFormat: null,
			renderFixed: "",
			renderVisible: false,
			renderWidth: 0,
			renderHeight: 0,
			renderResizeWidth: 0,
			renderAutoWidth: 0,
			resizeWidth: 0,
			renderLeft: 0,
			renderArgs: [],
			model: {},
			renderHeader: renderHeader || _vm.renderHeader,
			renderCell: renderCell || _vm.renderCell,
			renderFooter: renderFooter || _vm.renderFooter,
			renderData,
			slots: _vm.slots
		});
		if (ctFilterOptions && (!filters || !filters.length)) this.filters = toFilters(ctFilterOptions({
			$table: $xeTable,
			column: this
		}), colId);
		if ($xeGGWrapper) {
			const { computeProxyOpts } = $xeGGWrapper.getComputeMaps();
			const proxyOpts = computeProxyOpts.value;
			if (proxyOpts.beforeColumn) proxyOpts.beforeColumn({
				$table: $xeTable,
				$grid: $xeGrid,
				$gantt: $xeGantt,
				column: this
			});
		}
	}
	getTitle() {
		return getFuncText(this.title || (this.type === "seq" ? getI18n$1("vxe.table.seqTitle") : ""));
	}
	getKey() {
		const { type } = this;
		return this.field || (type ? `type=${type}` : null);
	}
	update(name, value) {
		if (name !== "filters") {
			if (name === "field") this.property = value;
			this[name] = value;
		}
	}
};
//#endregion
//#region ../../node_modules/.pnpm/vxe-table@4.18.13_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-table/es/ui/src/dom.js
var reClsMap = {};
var tpImgEl;
function initTpImg() {
	if (!tpImgEl) {
		tpImgEl = new Image();
		tpImgEl.src = "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNkYAAAAAYAAjCB0C8AAAAASUVORK5CYII=";
	}
	return tpImgEl;
}
function getTpImg() {
	if (!tpImgEl) return initTpImg();
	return tpImgEl;
}
function getPropClass(property, params) {
	return property ? import_xe_utils.default.isFunction(property) ? property(params) : property : "";
}
function getClsRE(cls) {
	if (!reClsMap[cls]) reClsMap[cls] = new RegExp(`(?:^|\\s)${cls}(?!\\S)`, "g");
	return reClsMap[cls];
}
function isPx(val) {
	return val && /^\d+(\.\d+)?(px)?$/.test(val);
}
function isScale(val) {
	return val && /^\d+(\.\d+)?%$/.test(val);
}
function hasClass(elem, cls) {
	return !!(elem && elem.className && elem.className.match && elem.className.match(getClsRE(cls)));
}
function removeClass(elem, cls) {
	if (elem && hasClass(elem, cls)) elem.className = elem.className.replace(getClsRE(cls), "");
}
function addClass(elem, cls) {
	if (elem && !hasClass(elem, cls)) {
		removeClass(elem, cls);
		elem.className = `${elem.className} ${cls}`;
	}
}
function hasControlKey(evnt) {
	return evnt.ctrlKey || evnt.metaKey;
}
function toCssUnit(val, unit = "px") {
	if (import_xe_utils.default.isNumber(val) || /^\d+$/.test(`${val}`)) return `${val}${unit}`;
	return `${val || ""}`;
}
function queryElement(elem, selector) {
	if (elem) return elem.querySelector(selector);
	return null;
}
function getDomNode() {
	const documentElement = document.documentElement;
	const bodyElem = document.body;
	return {
		scrollTop: documentElement.scrollTop || bodyElem.scrollTop,
		scrollLeft: documentElement.scrollLeft || bodyElem.scrollLeft,
		visibleHeight: documentElement.clientHeight || bodyElem.clientHeight,
		visibleWidth: documentElement.clientWidth || bodyElem.clientWidth
	};
}
function getOffsetHeight(elem) {
	return elem ? elem.offsetHeight : 0;
}
function getPaddingTopBottomSize(elem) {
	if (elem) {
		const computedStyle = getComputedStyle(elem);
		return import_xe_utils.default.toNumber(computedStyle.paddingTop) + import_xe_utils.default.toNumber(computedStyle.paddingBottom);
	}
	return 0;
}
function setScrollTop(elem, scrollTop) {
	if (elem) elem.scrollTop = scrollTop;
}
function setScrollLeft(elem, scrollLeft) {
	if (elem) elem.scrollLeft = scrollLeft;
}
function updateCellTitle(overflowElem, column) {
	const content = column.type === "html" ? overflowElem.innerText : overflowElem.textContent;
	if (overflowElem.getAttribute("title") !== content) overflowElem.setAttribute("title", content);
}
function checkTargetElement(target, exEls, endEl) {
	let targetEl = target;
	if (!exEls || !exEls.length) return false;
	const [exEl1, exEl2, exEl3] = exEls;
	while (targetEl) {
		if (exEl1 === targetEl || exEl2 && targetEl === exEl2 || exEl3 && targetEl === exEl3) return true;
		if (endEl && targetEl === endEl) return false;
		targetEl = targetEl.parentElement;
	}
	return false;
}
/**
* 检查触发源是否属于目标节点
*/
function getEventTargetNode(evnt, container, queryCls, queryMethod) {
	let targetElem;
	let target = evnt.target.shadowRoot && evnt.composed ? evnt.composedPath()[0] || evnt.target : evnt.target;
	while (target && target.nodeType && target !== document) {
		if (queryCls && hasClass(target, queryCls) && (!queryMethod || queryMethod(target))) targetElem = target;
		else if (target === container) return {
			flag: queryCls ? !!targetElem : true,
			container,
			targetElem
		};
		target = target.parentNode;
	}
	return { flag: false };
}
function getAbsolutePos(elem) {
	const bounding = elem.getBoundingClientRect();
	const boundingTop = bounding.top;
	const boundingLeft = bounding.left;
	const { scrollTop, scrollLeft, visibleHeight, visibleWidth } = getDomNode();
	return {
		boundingTop,
		top: scrollTop + boundingTop,
		boundingLeft,
		left: scrollLeft + boundingLeft,
		visibleHeight,
		visibleWidth
	};
}
var scrollIntoViewIfNeeded = "scrollIntoViewIfNeeded";
var scrollIntoView = "scrollIntoView";
function scrollToView(elem) {
	if (elem) {
		if (elem[scrollIntoViewIfNeeded]) elem[scrollIntoViewIfNeeded]();
		else if (elem[scrollIntoView]) elem[scrollIntoView]();
	}
}
function triggerEvent(targetElem, type) {
	if (targetElem) targetElem.dispatchEvent(new Event(type));
}
var wtlFrame;
function wheelScrollLeftTo(scrollLeft, cb) {
	if (wtlFrame) cancelAnimationFrame(wtlFrame);
	wtlFrame = requestAnimationFrame(() => {
		cb(scrollLeft);
		wtlFrame = null;
	});
}
var wtaFrame;
function wheelScrollTopTo(diffNum, cb) {
	if (wtaFrame) cancelAnimationFrame(wtaFrame);
	wtaFrame = requestAnimationFrame(() => {
		cb(diffNum);
		wtaFrame = null;
	});
}
//#endregion
//#region ../../node_modules/.pnpm/vxe-table@4.18.13_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-table/es/table/src/util.js
function createInternalData() {
	return {
		tZindex: 0,
		currKeyField: "",
		isCurrDeepKey: false,
		elemStore: {},
		scrollXStore: {
			preloadSize: 0,
			offsetSize: 0,
			visibleSize: 0,
			visibleStartIndex: 0,
			visibleEndIndex: 0,
			startIndex: 0,
			endIndex: 0
		},
		scrollYStore: {
			preloadSize: 0,
			offsetSize: 0,
			visibleSize: 0,
			visibleStartIndex: 0,
			visibleEndIndex: 0,
			startIndex: 0,
			endIndex: 0
		},
		tableWidth: 0,
		tableHeight: 0,
		customHeight: 0,
		customMinHeight: 0,
		customMaxHeight: 0,
		hoverRow: null,
		lastScrollLeft: 0,
		lastScrollTop: 0,
		radioReserveRow: null,
		checkboxReserveRowMap: {},
		rowExpandedReserveRowMap: {},
		treeExpandedReserveRowMap: {},
		treeIndeterminateRowMaps: {},
		tableFullData: [],
		afterFullData: [],
		afterTreeFullData: [],
		afterGroupFullData: [],
		afterFullRowMaps: {},
		tableFullTreeData: [],
		tableFullGroupData: [],
		tableSynchData: [],
		tableSourceData: [],
		collectColumn: [],
		tableFullColumn: [],
		visibleColumn: [],
		fullAllDataRowIdData: {},
		fullDataRowIdData: {},
		visibleDataRowIdData: {},
		keepUpdateFieldMaps: {},
		footerFullDataRowData: {},
		sourceDataRowIdData: {},
		fullColumnIdData: {},
		fullColumnFieldData: {},
		currentRow: null,
		mergeHeaderList: [],
		mergeHeaderMaps: {},
		mergeHeaderCellMaps: {},
		mergeHeaderRowMaps: {},
		mergeHeaderColMaps: {},
		mergeBodyList: [],
		mergeBodyMaps: {},
		mergeBodyCellMaps: {},
		mergeBodyRowMaps: {},
		mergeBodyColMaps: {},
		mergeFooterList: [],
		mergeFooterMaps: {},
		mergeFooterCellMaps: {},
		mergeFooterRowMaps: {},
		mergeFooterColMaps: {},
		rowExpandedMaps: {},
		rowExpandLazyLoadedMaps: {},
		rowGroupExpandedMaps: {},
		treeExpandedMaps: {},
		treeExpandLazyLoadedMaps: {},
		selectCheckboxMaps: {},
		pendingRowMaps: {},
		insertRowMaps: {},
		removeRowMaps: {},
		cvCacheMaps: {},
		tHeaderHeight: 0,
		tBodyHeight: 0,
		tFooterHeight: 0,
		teleportToWrapperElem: null,
		popupToWrapperElem: null,
		customPopupToElem: null,
		lastSTime: 0,
		inited: false,
		tooltipTimeout: null,
		initStatus: false,
		isActivated: false
	};
}
function createReactData() {
	return {
		updateColFlag: 0,
		staticColumns: [],
		tableGroupColumn: [],
		tableColumn: [],
		tableData: [],
		scrollXLoad: false,
		scrollYLoad: false,
		overflowY: true,
		overflowX: false,
		scrollbarWidth: 0,
		scrollbarHeight: 0,
		rowHeight: 0,
		parentHeight: 0,
		isGroup: false,
		isAllOverflow: false,
		isAllSelected: false,
		isIndeterminate: false,
		currentColumn: null,
		selectRadioRow: null,
		footerTableData: [],
		rowGroupColumn: null,
		expandColumn: null,
		checkboxColumn: null,
		radioColumn: null,
		treeNodeColumn: null,
		hasFixedColumn: false,
		upDataFlag: 0,
		reColumnFlag: 0,
		initStore: {
			filter: false,
			import: false,
			export: false,
			custom: false
		},
		customStore: {
			btnEl: null,
			isAll: false,
			isIndeterminate: false,
			activeBtn: false,
			activeWrapper: false,
			visible: false,
			maxHeight: null,
			popupStyle: {},
			oldSortMaps: {},
			oldFixedMaps: {},
			oldVisibleMaps: {}
		},
		customColumnList: [],
		filterStore: {
			isAllSelected: false,
			isIndeterminate: false,
			style: null,
			column: null,
			visible: false,
			maxHeight: null
		},
		columnStore: {
			leftList: [],
			centerList: [],
			rightList: [],
			resizeList: [],
			pxList: [],
			pxMinList: [],
			autoMinList: [],
			scaleList: [],
			scaleMinList: [],
			autoList: [],
			remainList: []
		},
		ctxMenuStore: {
			selected: null,
			visible: false,
			showChild: false,
			selectChild: null,
			list: [],
			style: null
		},
		editStore: {
			indexs: { columns: [] },
			titles: { columns: [] },
			selected: {
				row: null,
				column: null
			},
			copyed: {
				cut: false,
				rows: [],
				columns: []
			},
			actived: {
				row: null,
				column: null
			},
			focused: {
				row: null,
				column: null
			}
		},
		tooltipStore: {
			row: null,
			column: null,
			content: null,
			visible: false,
			type: null,
			currOpts: {}
		},
		validStore: { visible: false },
		validErrorMaps: {},
		importStore: {
			inited: false,
			file: null,
			type: "",
			modeList: [],
			typeList: [],
			filename: "",
			visible: false
		},
		importParams: {
			mode: "",
			types: null,
			message: true
		},
		exportStore: {
			inited: false,
			name: "",
			modeList: [],
			typeList: [],
			columns: [],
			isPrint: false,
			hasFooter: false,
			hasMerge: false,
			hasTree: false,
			hasColgroup: false,
			visible: false
		},
		exportParams: {
			filename: "",
			sheetName: "",
			mode: "",
			type: "",
			isColgroup: false,
			isMerge: false,
			isAllExpand: false,
			useStyle: false,
			original: false,
			message: true,
			isHeader: false,
			isTitle: false,
			isFooter: false
		},
		visiblwRowsFlag: 1,
		isRowGroupStatus: false,
		rowGroupList: [],
		aggHandleFields: [],
		aggHandleAggColumns: [],
		rowGroupExpandedFlag: 1,
		rowExpandedFlag: 1,
		treeExpandedFlag: 1,
		updateCheckboxFlag: 1,
		pendingRowFlag: 1,
		insertRowFlag: 1,
		removeRowFlag: 1,
		mergeHeadFlag: 1,
		mergeBodyFlag: 1,
		mergeFootFlag: 1,
		rowHeightStore: {
			large: 52,
			default: 48,
			medium: 44,
			small: 40,
			mini: 36
		},
		scrollVMLoading: false,
		scrollYHeight: 0,
		scrollYTop: 0,
		isScrollYBig: false,
		scrollXLeft: 0,
		scrollXWidth: 0,
		isScrollXBig: false,
		lazScrollLoading: false,
		rowExpandHeightFlag: 1,
		calcCellHeightFlag: 1,
		resizeHeightFlag: 1,
		resizeWidthFlag: 1,
		isCustomStatus: false,
		isCustomDragStatus: true,
		ctPopupFlag: 1,
		isCrossDragRow: false,
		dragRow: null,
		isCrossDragCol: false,
		dragCol: null,
		dragTipText: "",
		isDragResize: false,
		isRowLoading: false,
		isColLoading: false
	};
}
var getAllConvertColumns = (columns, parentColumn) => {
	const result = [];
	columns.forEach((column) => {
		column.parentId = parentColumn ? parentColumn.id : null;
		if (column.visible) if (column.children && column.children.length && column.children.some((column) => column.visible)) {
			result.push(column);
			result.push(...getAllConvertColumns(column.children, column));
		} else result.push(column);
	});
	return result;
};
var convertHeaderColumnToRows = (originColumns) => {
	let maxLevel = 1;
	const traverse = (column, parent) => {
		if (parent) {
			column.level = parent.level + 1;
			if (maxLevel < column.level) maxLevel = column.level;
		}
		if (column.children && column.children.length && column.children.some((column) => column.visible)) {
			let colSpan = 0;
			column.children.forEach((subColumn) => {
				if (subColumn.visible) {
					traverse(subColumn, column);
					colSpan += subColumn.colSpan;
				}
			});
			column.colSpan = colSpan;
		} else column.colSpan = 1;
	};
	originColumns.forEach((column) => {
		column.level = 1;
		traverse(column);
	});
	const rows = [];
	for (let i = 0; i < maxLevel; i++) rows.push([]);
	getAllConvertColumns(originColumns).forEach((column) => {
		if (column.children && column.children.length && column.children.some((column) => column.visible)) column.rowSpan = 1;
		else column.rowSpan = maxLevel - column.level + 1;
		rows[column.level - 1].push(column);
	});
	return rows;
};
function convertHeaderToGridRows(spanColumns) {
	const rSize = spanColumns.length;
	const cSize = spanColumns[0].reduce((sum, cell) => sum + cell.colSpan, 0);
	const occupiedRows = [];
	const fullRows = [];
	for (let rIndex = 0; rIndex < rSize; rIndex++) {
		const oCols = [];
		const dCols = [];
		for (let cIndex = 0; cIndex < cSize; cIndex++) {
			oCols.push(false);
			dCols.push("");
		}
		occupiedRows.push(oCols);
		fullRows.push(dCols);
	}
	for (let rIndex = 0; rIndex < rSize; rIndex++) {
		let currColIndex = 0;
		for (const column of spanColumns[rIndex]) {
			const { colSpan, rowSpan } = column;
			let startColIndex = -1;
			for (let ccIndex = currColIndex; ccIndex <= cSize - colSpan; ccIndex++) {
				let oFlag = true;
				for (let csIndex = 0; csIndex < colSpan; csIndex++) if (occupiedRows[rIndex][ccIndex + csIndex]) {
					oFlag = false;
					break;
				}
				if (oFlag) {
					startColIndex = ccIndex;
					break;
				}
			}
			if (startColIndex === -1) {
				for (let j = 0; j <= cSize - colSpan; j++) {
					let oFlag = true;
					for (let k = 0; k < colSpan; k++) if (occupiedRows[rIndex][j + k]) {
						oFlag = false;
						break;
					}
					if (oFlag) {
						startColIndex = j;
						break;
					}
				}
				if (startColIndex === -1) break;
			}
			for (let srIndex = rIndex; srIndex < rIndex + rowSpan; srIndex++) for (let scIndex = startColIndex; scIndex < startColIndex + colSpan; scIndex++) {
				occupiedRows[srIndex][scIndex] = true;
				fullRows[srIndex][scIndex] = column;
			}
			currColIndex = startColIndex + colSpan;
		}
	}
	return fullRows;
}
function restoreScrollLocation($xeTable, scrollLeft, scrollTop) {
	const internalData = $xeTable.internalData;
	if (scrollLeft || scrollTop) {
		internalData.intoRunScroll = false;
		internalData.inVirtualScroll = false;
		internalData.inWheelScroll = false;
		internalData.inHeaderScroll = false;
		internalData.inBodyScroll = false;
		internalData.inFooterScroll = false;
		internalData.scrollRenderType = "";
		return $xeTable.scrollTo(scrollLeft, scrollTop);
	}
	return $xeTable.clearScroll();
}
function getRowUniqueId() {
	return import_xe_utils.default.uniqueId("row_");
}
/**
* 生成行的唯一主键
*/
function createRowId(rowOpts, row, keyField) {
	const { createKeyMethod } = rowOpts;
	if (createKeyMethod) return createKeyMethod({
		row,
		keyField
	});
	return getRowUniqueId();
}
function hasDeepKey(rowKey) {
	return rowKey.indexOf(".") > -1;
}
function getRowkey($xeTable) {
	const { currKeyField } = $xeTable.internalData;
	return currKeyField;
}
function getRowid($xeTable, row) {
	const { isCurrDeepKey, currKeyField } = $xeTable.internalData;
	return row ? encodeRowid((isCurrDeepKey ? getDeepRowIdByKey : getFastRowIdByKey)(row, currKeyField)) : "";
}
function createHandleUpdateRowId($xeTable) {
	const { isCurrDeepKey, currKeyField } = $xeTable.internalData;
	const { computeRowOpts } = $xeTable.getComputeMaps();
	const rowOpts = computeRowOpts.value;
	const updateRId = isCurrDeepKey ? updateDeepRowKey : updateFastRowKey;
	return {
		rowKey: currKeyField,
		handleUpdateRowId(row) {
			return row ? updateRId(rowOpts, row, currKeyField) : "";
		}
	};
}
function createHandleGetRowId($xeTable) {
	const { isCurrDeepKey, currKeyField } = $xeTable.internalData;
	const getRId = isCurrDeepKey ? getDeepRowIdByKey : getFastRowIdByKey;
	return {
		rowKey: currKeyField,
		handleGetRowId(row) {
			return row ? encodeRowid(getRId(row, currKeyField)) : "";
		}
	};
}
function encodeRowid(rowVal) {
	return import_xe_utils.default.eqNull(rowVal) ? "" : encodeURIComponent(rowVal);
}
function getDeepRowIdByKey(row, rowKey) {
	return import_xe_utils.default.get(row, rowKey);
}
function updateDeepRowKey(rowOpts, row, rowKey) {
	let rowid = encodeRowid(getDeepRowIdByKey(row, rowKey));
	if (eqEmptyValue(rowid)) {
		rowid = "" + createRowId(rowOpts, row, rowKey);
		import_xe_utils.default.set(row, rowKey, rowid);
	}
	return rowid;
}
function getFastRowIdByKey(row, rowKey) {
	return row[rowKey];
}
function updateFastRowKey(rowOpts, row, rowKey) {
	let rowid = encodeRowid(getFastRowIdByKey(row, rowKey));
	if (eqEmptyValue(rowid)) {
		const newRowid = createRowId(rowOpts, row, rowKey);
		rowid = "" + newRowid;
		row[rowKey] = newRowid;
	}
	return rowid;
}
function handleFieldOrColumn($xeTable, fieldOrColumn) {
	if (fieldOrColumn) return import_xe_utils.default.isString(fieldOrColumn) || import_xe_utils.default.isNumber(fieldOrColumn) ? $xeTable.getColumnByField(`${fieldOrColumn}`) : fieldOrColumn;
	return null;
}
function handleRowidOrRow($xeTable, rowidOrRow) {
	if (rowidOrRow) {
		const rowid = import_xe_utils.default.isString(rowidOrRow) || import_xe_utils.default.isNumber(rowidOrRow) ? rowidOrRow : getRowid($xeTable, rowidOrRow);
		return $xeTable.getRowById(rowid);
	}
	return null;
}
function getCellRestHeight(rowRest, cellOpts, rowOpts, defaultRowHeight) {
	return rowRest.resizeHeight || cellOpts.height || rowOpts.height || rowRest.height || defaultRowHeight;
}
function getPaddingLeftRightSize(elem) {
	if (elem) {
		const computedStyle = getComputedStyle(elem);
		return import_xe_utils.default.toNumber(computedStyle.paddingLeft) + import_xe_utils.default.toNumber(computedStyle.paddingRight);
	}
	return 0;
}
function getElementMarginAndWidth(elem) {
	if (elem) {
		const computedStyle = getComputedStyle(elem);
		const marginLeft = import_xe_utils.default.toNumber(computedStyle.marginLeft);
		const marginRight = import_xe_utils.default.toNumber(computedStyle.marginRight);
		return elem.offsetWidth + marginLeft + marginRight;
	}
	return 0;
}
function toFilters(filters, colid) {
	if (filters) {
		if (import_xe_utils.default.isArray(filters)) return filters.map(({ label, value, data, resetValue, checked }) => {
			return {
				label,
				value,
				data,
				resetValue,
				checked: !!checked,
				_checked: !!checked,
				_colId: colid
			};
		});
		return [];
	}
	return filters;
}
function toTreePathSeq(path) {
	return path.map((num, i) => i % 2 === 0 ? Number(num) + 1 : ".").join("");
}
function getCellValue(row, column) {
	return import_xe_utils.default.get(row, column.field);
}
function setCellValue(row, column, value) {
	return import_xe_utils.default.set(row, column.field, value);
}
function getRefElem(refEl) {
	if (refEl) {
		const rest = refEl.value;
		if (rest) return rest.$el || rest;
	}
	return null;
}
function getCalcHeight(height) {
	if (height === "unset") return 0;
	return height || 0;
}
/**
* 列宽拖动最小宽度
* @param params
* @returns
*/
function getColReMinWidth(params) {
	const { $table, column, cell } = params;
	const tableProps = $table.props;
	const internalData = $table.internalData;
	const { computeResizableOpts } = $table.getComputeMaps();
	const { minWidth: reMinWidth } = computeResizableOpts.value;
	if (reMinWidth) {
		const customMinWidth = import_xe_utils.default.isFunction(reMinWidth) ? reMinWidth(params) : reMinWidth;
		if (customMinWidth !== "auto") return Math.max(1, import_xe_utils.default.toNumber(customMinWidth));
	}
	const { elemStore } = internalData;
	const { showHeaderOverflow: allColumnHeaderOverflow } = tableProps;
	const { showHeaderOverflow, minWidth: colMinWidth } = column;
	const headOverflow = import_xe_utils.default.isUndefined(showHeaderOverflow) || import_xe_utils.default.isNull(showHeaderOverflow) ? allColumnHeaderOverflow : showHeaderOverflow;
	const hasEllipsis = headOverflow === "title" || headOverflow === true || headOverflow === "tooltip" || headOverflow === "ellipsis";
	let mWidth = import_xe_utils.default.floor((import_xe_utils.default.toNumber(getComputedStyle(cell).fontSize) || 14) * 1.8) + (getPaddingLeftRightSize(cell) + getPaddingLeftRightSize(queryElement(cell, ".vxe-cell")));
	if (hasEllipsis) {
		const dragIconWidth = getElementMarginAndWidth(queryElement(cell, ".vxe-cell--drag-handle"));
		const checkboxIconWidth = getElementMarginAndWidth(queryElement(cell, ".vxe-cell--checkbox"));
		const requiredIconWidth = getElementMarginAndWidth(queryElement(cell, ".vxe-cell--required-icon"));
		const editIconWidth = getElementMarginAndWidth(queryElement(cell, ".vxe-cell--edit-icon"));
		const prefixIconWidth = getElementMarginAndWidth(queryElement(cell, ".vxe-cell-title-prefix-icon"));
		const suffixIconWidth = getElementMarginAndWidth(queryElement(cell, ".vxe-cell-title-suffix-icon"));
		const sortIconWidth = getElementMarginAndWidth(queryElement(cell, ".vxe-cell--sort"));
		const filterIconWidth = getElementMarginAndWidth(queryElement(cell, ".vxe-cell--filter"));
		mWidth += dragIconWidth + checkboxIconWidth + requiredIconWidth + editIconWidth + prefixIconWidth + suffixIconWidth + filterIconWidth + sortIconWidth;
	}
	if (colMinWidth) {
		const bodyScrollElem = getRefElem(elemStore["main-body-scroll"]);
		if (bodyScrollElem) {
			if (isScale(colMinWidth)) {
				const meanWidth = (bodyScrollElem.clientWidth - 1) / 100;
				return Math.max(mWidth, Math.floor(import_xe_utils.default.toInteger(colMinWidth) * meanWidth));
			} else if (isPx(colMinWidth)) return Math.max(mWidth, import_xe_utils.default.toInteger(colMinWidth));
		}
	}
	return mWidth;
}
function isColumnInfo(column) {
	return column && (column.constructor === ColumnInfo || column instanceof ColumnInfo);
}
function getColumnList(columns) {
	const result = [];
	columns.forEach((column) => {
		result.push(...column.children && column.children.length ? getColumnList(column.children) : [column]);
	});
	return result;
}
function createColumn($xeTable, options, renderOptions) {
	return isColumnInfo(options) ? options : reactive(new ColumnInfo($xeTable, options, renderOptions));
}
function watchColumn($xeTable, props, column) {
	Object.keys(props).forEach((name) => {
		watch(() => props[name], (value) => {
			column.update(name, value);
			if ($xeTable) {
				if (name === "filters") {
					$xeTable.setFilter(column, value);
					$xeTable.handleUpdateDataQueue();
				} else if ([
					"visible",
					"fixed",
					"width",
					"minWidth",
					"maxWidth"
				].includes(name)) $xeTable.handleRefreshColumnQueue();
			}
		});
	});
}
function assembleColumn($xeTable, elem, column, colgroup) {
	const { reactData } = $xeTable;
	const { staticColumns } = reactData;
	const parentElem = elem.parentNode;
	const parentColumn = colgroup ? colgroup.columnConfig : null;
	const parentCols = parentColumn ? parentColumn.children : staticColumns;
	if (parentElem && parentCols) {
		column.defaultParentId = parentColumn ? parentColumn.id : null;
		parentCols.splice(import_xe_utils.default.arrayIndexOf(parentElem.children, elem), 0, column);
		reactData.staticColumns = staticColumns.slice(0);
	}
}
function destroyColumn($xeTable, column) {
	const { reactData } = $xeTable;
	const { staticColumns } = reactData;
	const matchObj = import_xe_utils.default.findTree(staticColumns, (item) => item.id === column.id, { children: "children" });
	if (matchObj) matchObj.items.splice(matchObj.index, 1);
	reactData.staticColumns = staticColumns.slice(0);
}
function getRootColumn($xeTable, column) {
	const { internalData } = $xeTable;
	const { fullColumnIdData } = internalData;
	if (!column) return null;
	let parentColId = column.parentId;
	while (fullColumnIdData[parentColId]) {
		const column = fullColumnIdData[parentColId].column;
		parentColId = column.parentId;
		if (!parentColId) return column;
	}
	return column;
}
function getFirstChildColumn(column) {
	const { children } = column;
	if (children && children.length) return getFirstChildColumn(import_xe_utils.default.first(children));
	return column;
}
function getLastChildColumn(column) {
	const { children } = column;
	if (children && children.length) return getFirstChildColumn(import_xe_utils.default.last(children));
	return column;
}
function clearTableDefaultStatus($xeTable) {
	const { props, internalData } = $xeTable;
	internalData.initStatus = false;
	const actionList = [
		$xeTable.clearSort(),
		$xeTable.clearCurrentRow(),
		$xeTable.clearCurrentColumn(),
		$xeTable.clearRadioRow(),
		$xeTable.clearRadioReserve(),
		$xeTable.clearCheckboxRow(),
		$xeTable.clearCheckboxReserve(),
		$xeTable.clearRowExpand(),
		$xeTable.clearTreeExpand(),
		$xeTable.clearTreeExpandReserve(),
		$xeTable.clearPendingRow()
	];
	if ($xeTable.clearFilter) actionList.push($xeTable.clearFilter());
	if ($xeTable.clearSelected && (props.keyboardConfig || props.mouseConfig)) actionList.push($xeTable.clearSelected());
	if ($xeTable.clearCellAreas && props.mouseConfig) actionList.push($xeTable.clearCellAreas(), $xeTable.clearCopyCellArea());
	return Promise.all(actionList).then(() => {
		return $xeTable.clearScroll();
	});
}
function clearTableAllStatus($xeTable) {
	if ($xeTable.clearFilter) $xeTable.clearFilter();
	return clearTableDefaultStatus($xeTable);
}
function rowToVisible($xeTable, row) {
	const reactData = $xeTable.reactData;
	const internalData = $xeTable.internalData;
	const { computeLeftFixedWidth, computeRightFixedWidth, computeRowOpts, computeCellOpts, computeDefaultRowHeight } = $xeTable.getComputeMaps();
	const { scrollYLoad, scrollYTop, isAllOverflow } = reactData;
	const { elemStore, afterFullData, fullAllDataRowIdData, isResizeCellHeight } = internalData;
	const rowOpts = computeRowOpts.value;
	const cellOpts = computeCellOpts.value;
	const defaultRowHeight = computeDefaultRowHeight.value;
	const leftFixedWidth = computeLeftFixedWidth.value;
	const rightFixedWidth = computeRightFixedWidth.value;
	const bodyScrollElem = getRefElem(elemStore["main-body-scroll"]);
	const rowid = getRowid($xeTable, row);
	if (bodyScrollElem) {
		const bodyHeight = bodyScrollElem.clientHeight;
		const bodyScrollTop = bodyScrollElem.scrollTop;
		const trElem = bodyScrollElem.querySelector(`[rowid="${rowid}"]`);
		if (trElem) {
			const trOffsetTop = trElem.offsetTop + (scrollYLoad ? scrollYTop : 0);
			const trHeight = trElem.clientHeight;
			if (trOffsetTop < bodyScrollTop || trOffsetTop > bodyScrollTop + bodyHeight) return $xeTable.scrollTo(null, trOffsetTop);
			else if (trOffsetTop + trHeight >= bodyHeight + bodyScrollTop) return $xeTable.scrollTo(null, bodyScrollTop + trHeight);
		} else if (scrollYLoad) {
			if (!(isResizeCellHeight || cellOpts.height || rowOpts.height) && isAllOverflow) return $xeTable.scrollTo(null, ($xeTable.findRowIndexOf(afterFullData, row) - 1) * defaultRowHeight);
			const rowRest = fullAllDataRowIdData[rowid] || {};
			const rHeight = rowRest.resizeHeight || cellOpts.height || rowOpts.height || rowRest.height || defaultRowHeight;
			const scrollTop = rowRest.oTop;
			if (scrollTop < bodyScrollTop) return $xeTable.scrollTo(null, scrollTop - leftFixedWidth - 1);
			return $xeTable.scrollTo(null, scrollTop + rHeight - (bodyHeight - rightFixedWidth - 1));
		}
	}
	return Promise.resolve();
}
function colToVisible($xeTable, isForce, column, row) {
	const reactData = $xeTable.reactData;
	const internalData = $xeTable.internalData;
	const { computeLeftFixedWidth, computeRightFixedWidth } = $xeTable.getComputeMaps();
	const { scrollXLoad, scrollXLeft } = reactData;
	const { elemStore, visibleColumn } = internalData;
	const leftFixedWidth = computeLeftFixedWidth.value;
	const rightFixedWidth = computeRightFixedWidth.value;
	const bodyScrollElem = getRefElem(elemStore["main-body-scroll"]);
	if (column.fixed) return Promise.resolve();
	if (bodyScrollElem) {
		const bodyWidth = bodyScrollElem.clientWidth;
		const bodyScrollLeft = bodyScrollElem.scrollLeft;
		let tdElem = null;
		if (row) {
			const rowid = getRowid($xeTable, row);
			tdElem = bodyScrollElem.querySelector(`[rowid="${rowid}"] .${column.id}`);
		}
		if (!tdElem) tdElem = bodyScrollElem.querySelector(`.${column.id}`);
		if (tdElem) {
			const tdOffsetLeft = tdElem.offsetLeft + (scrollXLoad ? scrollXLeft : 0);
			const cellWidth = tdElem.clientWidth;
			if (isForce || !(tdOffsetLeft <= bodyScrollLeft + leftFixedWidth && tdOffsetLeft + cellWidth > bodyScrollLeft + leftFixedWidth) && !(tdOffsetLeft >= bodyScrollLeft + leftFixedWidth && tdOffsetLeft < bodyScrollLeft + bodyWidth - rightFixedWidth)) {
				if (tdOffsetLeft < bodyScrollLeft + leftFixedWidth) return $xeTable.scrollTo(tdOffsetLeft - leftFixedWidth - 1);
				else if (tdOffsetLeft + cellWidth - bodyScrollLeft > bodyWidth - rightFixedWidth) return $xeTable.scrollTo(tdOffsetLeft + cellWidth - (bodyWidth - rightFixedWidth - 1));
			}
		} else if (scrollXLoad) {
			let tdOffsetLeft = 0;
			const cellWidth = column.renderWidth;
			for (let i = 0; i < visibleColumn.length; i++) {
				const currCol = visibleColumn[i];
				if (currCol === column || currCol.id === column.id) break;
				tdOffsetLeft += currCol.renderWidth;
			}
			if (isForce || !(tdOffsetLeft <= bodyScrollLeft + leftFixedWidth && tdOffsetLeft + cellWidth > bodyScrollLeft + leftFixedWidth) && !(tdOffsetLeft >= bodyScrollLeft + leftFixedWidth && tdOffsetLeft < bodyScrollLeft + bodyWidth - rightFixedWidth)) {
				if (tdOffsetLeft < bodyScrollLeft) return $xeTable.scrollTo(tdOffsetLeft - leftFixedWidth - 1);
				return $xeTable.scrollTo(tdOffsetLeft + cellWidth - (bodyWidth - rightFixedWidth - 1));
			}
		}
	}
	return Promise.resolve();
}
//#endregion
//#region ../../node_modules/.pnpm/vxe-table@4.18.13_vue@3.5.33_typescript@5.9.3_/node_modules/vxe-table/es/table/src/cell.js
var { getI18n, getIcon, renderer, renderEmptyElement } = VxeUI;
function renderTitlePrefixIcon(params) {
	const { $table, column } = params;
	const titlePrefix = column.titlePrefix || column.titleHelp;
	if (titlePrefix) return h("span", {
		class: ["vxe-cell-title-prefix-icon", titlePrefix.iconStatus ? `theme--${titlePrefix.iconStatus}` : ""],
		onMouseenter(evnt) {
			$table.triggerHeaderTitleEvent(evnt, titlePrefix, params);
		},
		onMouseleave(evnt) {
			$table.handleTargetLeaveEvent(evnt);
		}
	}, [h("i", { class: titlePrefix.icon || getIcon().TABLE_TITLE_PREFIX })]);
	return renderEmptyElement($table);
}
function renderTitleSuffixIcon(params) {
	const { $table, column } = params;
	const titleSuffix = column.titleSuffix;
	if (titleSuffix) return h("span", {
		class: ["vxe-cell-title-suffix-icon", titleSuffix.iconStatus ? `theme--${titleSuffix.iconStatus}` : ""],
		onMouseenter(evnt) {
			$table.triggerHeaderTitleEvent(evnt, titleSuffix, params);
		},
		onMouseleave(evnt) {
			$table.handleTargetLeaveEvent(evnt);
		}
	}, [h("i", { class: titleSuffix.icon || getIcon().TABLE_TITLE_SUFFIX })]);
	return renderEmptyElement($table);
}
function renderCellDragIcon(params) {
	const { $table, column } = params;
	const { context } = $table;
	const tableSlots = context.slots;
	const tableProps = $table.props;
	const { slots } = column;
	const { dragConfig } = tableProps;
	const { computeRowDragOpts } = $table.getComputeMaps();
	const { icon, trigger, disabledMethod } = computeRowDragOpts.value;
	const rDisabledMethod = disabledMethod || (dragConfig ? dragConfig.rowDisabledMethod : null);
	const isDisabled = rDisabledMethod && rDisabledMethod(params);
	const rowDragIconSlot = (slots ? slots.rowDragIcon || slots["row-drag-icon"] : null) || tableSlots.rowDragIcon || tableSlots["row-drag-icon"];
	const ons = {};
	if (trigger !== "cell") {
		ons.onMousedown = (evnt) => {
			if (!isDisabled) $table.handleCellDragMousedownEvent(evnt, params);
		};
		ons.onMouseup = $table.handleCellDragMouseupEvent;
	}
	return h("span", Object.assign({
		key: "dg",
		class: ["vxe-cell--drag-handle", { "is--disabled": isDisabled }]
	}, ons), rowDragIconSlot ? $table.callSlot(rowDragIconSlot, params) : [h("i", { class: icon || (dragConfig ? dragConfig.rowIcon : "") || getIcon().TABLE_DRAG_ROW })]);
}
function renderCellBaseVNs(params, content) {
	const { $table, column, level } = params;
	const { dragSort } = column;
	const { treeConfig, dragConfig } = $table.props;
	const { computeRowOpts, computeRowDragOpts, computeTreeOpts } = $table.getComputeMaps();
	const rowOpts = computeRowOpts.value;
	const rowDragOpts = computeRowDragOpts.value;
	const treeOpts = computeTreeOpts.value;
	const { showIcon, isPeerDrag, isCrossDrag, visibleMethod } = rowDragOpts;
	const rVisibleMethod = visibleMethod || (dragConfig ? dragConfig.rowVisibleMethod : null);
	const vns = [];
	if (dragSort && rowOpts.drag && (showIcon || (dragConfig ? dragConfig.showRowIcon : false)) && (!rVisibleMethod || rVisibleMethod(params))) if (treeConfig) {
		if (treeOpts.transform && (isPeerDrag || isCrossDrag || !level)) vns.push(renderCellDragIcon(params));
	} else vns.push(renderCellDragIcon(params));
	return vns.concat(import_xe_utils.default.isArray(content) ? content : [content]);
}
function renderHeaderCellDragIcon(params) {
	const { $table, column } = params;
	const { context } = $table;
	const tableSlots = context.slots;
	const { slots } = column;
	const { computeColumnOpts, computeColumnDragOpts } = $table.getComputeMaps();
	const columnOpts = computeColumnOpts.value;
	const { showIcon, icon, trigger, isPeerDrag, isCrossDrag, visibleMethod, disabledMethod } = computeColumnDragOpts.value;
	if (columnOpts.drag && showIcon && (!visibleMethod || visibleMethod(params))) {
		if (!column.fixed && (isPeerDrag || isCrossDrag || !column.parentId)) {
			const isDisabled = disabledMethod && disabledMethod(params);
			const columnDragIconSlot = (slots ? slots.columnDragIcon || slots["column-drag-icon"] : null) || tableSlots.columnDragIcon || tableSlots["column-drag-icon"];
			const ons = {};
			if (trigger !== "cell") {
				ons.onMousedown = (evnt) => {
					if (!isDisabled) $table.handleHeaderCellDragMousedownEvent(evnt, params);
				};
				ons.onMouseup = $table.handleHeaderCellDragMouseupEvent;
			}
			return h("span", Object.assign({
				key: "dg",
				class: ["vxe-cell--drag-handle", { "is--disabled": isDisabled }]
			}, ons), columnDragIconSlot ? $table.callSlot(columnDragIconSlot, params) : [h("i", { class: icon || getIcon().TABLE_DRAG_COLUMN })]);
		}
	}
	return renderEmptyElement($table);
}
function renderHeaderCellBaseVNs(params, content) {
	return [
		renderTitlePrefixIcon(params),
		renderHeaderCellDragIcon(params),
		...import_xe_utils.default.isArray(content) ? content : [content],
		renderTitleSuffixIcon(params)
	];
}
function getRenderDefaultColumnTitle(column, content) {
	if (column.type === "html" && import_xe_utils.default.isString(content)) return h("span", {
		key: "ch",
		innerHTML: content
	});
	return h("span", { key: "ct" }, getSlotVNs(content));
}
function renderTitleContent(params, content) {
	const { $table, column } = params;
	const tableProps = $table.props;
	const tableReactData = $table.reactData;
	const { computeHeaderTooltipOpts } = $table.getComputeMaps();
	const { showHeaderOverflow: allColumnHeaderOverflow } = tableProps;
	const { isRowGroupStatus } = tableReactData;
	const { showHeaderOverflow, slots } = column;
	const titleSlot = slots ? slots.title : null;
	const showAllTip = computeHeaderTooltipOpts.value.showAll;
	const headOverflow = import_xe_utils.default.eqNull(showHeaderOverflow) ? allColumnHeaderOverflow : showHeaderOverflow;
	const showTitle = headOverflow === "title";
	const showTooltip = headOverflow === true || headOverflow === "tooltip";
	const ons = {};
	if (showTitle || showTooltip || showAllTip) ons.onMouseenter = (evnt) => {
		if (tableReactData.isDragResize) return;
		if (showTitle) updateCellTitle(evnt.currentTarget, column);
		else if (showTooltip || showAllTip) $table.triggerHeaderTooltipEvent(evnt, params);
	};
	if (showTooltip || showAllTip) ons.onMouseleave = (evnt) => {
		if (tableReactData.isDragResize) return;
		if (showTooltip || showAllTip) $table.handleTargetLeaveEvent(evnt);
	};
	const titleVN = getRenderDefaultColumnTitle(column, content);
	return [h("span", Object.assign({ class: "vxe-cell--title" }, ons), isRowGroupStatus && column.aggFunc && $table.getPivotTableAggregateRenderColTitles ? $table.getPivotTableAggregateRenderColTitles(column, titleVN) : titleSlot ? $table.callSlot(titleSlot, params) : [titleVN])];
}
function getFooterContent(params) {
	const { $table, column, row } = params;
	const { editConfig } = $table.props;
	const { slots, editRender, cellRender } = column;
	const footerSlot = slots ? slots.footer : null;
	if (footerSlot) return $table.callSlot(footerSlot, params);
	const editRenderOpts = editConfig && isEnableConf(editConfig) && isEnableConf(editRender) ? editRender : null;
	const cellRenderOpts = isEnableConf(cellRender) ? cellRender : null;
	const renderOpts = editRenderOpts || cellRenderOpts;
	const itemValue = $table.getFooterCellLabel(row, column);
	if (renderOpts) {
		const compConf = renderer.get(renderOpts.name);
		if (compConf) {
			const rtFooter = compConf.renderTableFooter || compConf.renderFooter;
			if (rtFooter) return getSlotVNs(rtFooter(renderOpts, Object.assign(params, {
				cellValue: itemValue,
				itemValue
			})));
		}
	}
	return [h("span", { class: "vxe-cell--label" }, formatText(itemValue, 1))];
}
function getDefaultCellLabel(params) {
	const { $table, row, column } = params;
	return formatText($table.getCellLabel(row, column), 1);
}
function renderCellHandle(params) {
	const { column, row, $table } = params;
	const tableProps = $table.props;
	const { isRowGroupStatus } = $table.reactData;
	const { editConfig } = tableProps;
	const { type, treeNode, rowGroupNode, editRender } = column;
	const { computeEditOpts, computeCheckboxOpts, computeAggregateOpts } = $table.getComputeMaps();
	const { mode } = computeAggregateOpts.value;
	const checkboxOpts = computeCheckboxOpts.value;
	const editOpts = computeEditOpts.value;
	const isDeepCell = treeNode || isRowGroupStatus && (mode === "column" ? column.field === row.groupField : rowGroupNode);
	switch (type) {
		case "seq": return isDeepCell ? Cell.renderDeepIndexCell(params) : Cell.renderSeqCell(params);
		case "radio": return isDeepCell ? Cell.renderDeepRadioCell(params) : Cell.renderRadioCell(params);
		case "checkbox": return checkboxOpts.checkField ? isDeepCell ? Cell.renderDeepSelectionCellByProp(params) : Cell.renderCheckboxCellByProp(params) : isDeepCell ? Cell.renderDeepSelectionCell(params) : Cell.renderCheckboxCell(params);
		case "expand": return Cell.renderExpandCell(params);
		case "html": return isDeepCell ? Cell.renderDeepHTMLCell(params) : Cell.renderHTMLCell(params);
	}
	if (editConfig && isEnableConf(editConfig) && isEnableConf(editRender) ? editRender : null) return editOpts.mode === "cell" ? isDeepCell ? Cell.renderDeepCellEdit(params) : Cell.renderCellEdit(params) : isDeepCell ? Cell.renderDeepRowEdit(params) : Cell.renderRowEdit(params);
	return isDeepCell ? Cell.renderDeepCell(params) : Cell.renderDefaultCell(params);
}
function renderHeaderHandle(params) {
	const { column, $table } = params;
	const { editConfig } = $table.props;
	const { type, filters, sortable, editRender } = column;
	switch (type) {
		case "seq": return Cell.renderSeqHeader(params);
		case "radio": return Cell.renderRadioHeader(params);
		case "checkbox": return Cell.renderCheckboxHeader(params);
		case "html":
			if (filters && sortable) return Cell.renderSortAndFilterHeader(params);
			else if (sortable) return Cell.renderSortHeader(params);
			else if (filters) return Cell.renderFilterHeader(params);
			break;
	}
	if (editConfig && isEnableConf(editConfig) && isEnableConf(editRender) ? editRender : null) return Cell.renderEditHeader(params);
	else if (filters && sortable) return Cell.renderSortAndFilterHeader(params);
	else if (sortable) return Cell.renderSortHeader(params);
	else if (filters) return Cell.renderFilterHeader(params);
	return Cell.renderDefaultHeader(params);
}
function renderFooterHandle(params) {
	return Cell.renderDefaultFooter(params);
}
var Cell = {
	createColumn($xeTable, columnOpts) {
		const { type } = columnOpts;
		const renConfs = {
			renderHeader: renderHeaderHandle,
			renderCell: renderCellHandle,
			renderFooter: renderFooterHandle
		};
		if (type === "expand") renConfs.renderData = Cell.renderExpandData;
		return createColumn($xeTable, columnOpts, renConfs);
	},
	/**
	* 列头标题
	*/
	renderHeaderTitle(params) {
		const { $table, column } = params;
		const { editConfig } = $table.props;
		const { slots, editRender, cellRender } = column;
		const headerSlot = slots ? slots.header : null;
		if (headerSlot) return renderTitleContent(params, $table.callSlot(headerSlot, params));
		const editRenderOpts = editConfig && isEnableConf(editConfig) && isEnableConf(editRender) ? editRender : null;
		const cellRenderOpts = isEnableConf(cellRender) ? cellRender : null;
		const renderOpts = editRenderOpts || cellRenderOpts;
		if (renderOpts) {
			const compConf = renderer.get(renderOpts.name);
			if (compConf) {
				const rtHeader = compConf.renderTableHeader || compConf.renderHeader;
				if (rtHeader) return renderTitleContent(params, getSlotVNs(rtHeader(renderOpts, params)));
			}
		}
		return renderTitleContent(params, formatText(column.getTitle(), 1));
	},
	renderDefaultHeader(params) {
		return renderHeaderCellBaseVNs(params, Cell.renderHeaderTitle(params));
	},
	renderDefaultCell(params) {
		const { $table, row, column } = params;
		const tableProps = $table.props;
		const tableReactData = $table.reactData;
		const tableInternalData = $table.internalData;
		const { isRowGroupStatus } = tableReactData;
		const { editConfig } = tableProps;
		const { field, slots, editRender, cellRender, rowGroupNode, aggFunc, formatter } = column;
		const editRenderOpts = editConfig && isEnableConf(editConfig) && isEnableConf(editRender) ? editRender : null;
		const cellRenderOpts = isEnableConf(cellRender) ? cellRender : null;
		const defaultSlot = slots ? slots.default : null;
		const gcSlot = slots ? slots.groupContent || slots["group-content"] : null;
		const gvSlot = slots ? slots.groupValues || slots["group-values"] : null;
		let cellValue = "";
		if (isRowGroupStatus && field && row.isAggregate) {
			const aggRow = row;
			const { fullColumnFieldData } = tableInternalData;
			const { computeAggregateOpts } = $table.getComputeMaps();
			const { mode, showTotal, totalMethod, countFields, contentMethod, formatValuesMethod, mapChildrenField } = computeAggregateOpts.value;
			const aggData = aggRow.aggData;
			const currAggData = aggData ? aggData[field] : null;
			const groupField = aggRow.groupField;
			const groupContent = aggRow.groupContent;
			const childList = mapChildrenField ? aggRow[mapChildrenField] || [] : [];
			const childCount = aggRow.childCount;
			const colRest = fullColumnFieldData[groupField] || {};
			const ctParams = {
				$table,
				groupField,
				groupColumn: colRest ? colRest.column : null,
				column,
				groupValue: groupContent,
				childList,
				childCount,
				aggValue: null,
				/**
				* 已废弃
				* @deprecated
				*/
				children: childList,
				/**
				* 已废弃
				* @deprecated
				*/
				totalValue: childCount
			};
			if (mode === "column" ? field === aggRow.groupField : rowGroupNode) {
				cellValue = groupContent;
				if (contentMethod) cellValue = `${contentMethod(ctParams)}`;
				if (showTotal) cellValue = getI18n("vxe.table.rowGroupContentTotal", [
					cellValue,
					totalMethod ? totalMethod(ctParams) : childCount,
					childCount
				]);
				if (gcSlot) return renderCellBaseVNs(params, $table.callSlot(gcSlot, Object.assign({
					groupField,
					groupContent,
					childList,
					childCount
				}, params)));
			} else if ($table.getPivotTableAggregateCellAggValue) {
				cellValue = $table.getPivotTableAggregateCellAggValue(params);
				ctParams.aggValue = cellValue;
				if (gvSlot) return renderCellBaseVNs(params, $table.callSlot(gvSlot, Object.assign({
					groupField,
					groupContent,
					childList,
					childCount
				}, params, ctParams)));
			} else if (aggFunc === true || countFields && countFields.includes(field)) {
				cellValue = currAggData ? currAggData.value : childCount;
				ctParams.aggValue = cellValue;
				if (formatValuesMethod) cellValue = formatValuesMethod(ctParams);
				if (gvSlot) return renderCellBaseVNs(params, $table.callSlot(gvSlot, Object.assign({
					groupField,
					groupContent,
					childList,
					childCount
				}, params, ctParams)));
			}
		} else {
			if (defaultSlot) return renderCellBaseVNs(params, $table.callSlot(defaultSlot, params));
			const renderOpts = editRenderOpts || cellRenderOpts;
			if (renderOpts && !formatter) {
				const compConf = renderer.get(renderOpts.name);
				if (compConf) {
					const renderFn = editRenderOpts ? compConf.renderTableCell || compConf.renderCell : compConf.renderTableDefault || compConf.renderDefault;
					if (renderFn) return renderCellBaseVNs(params, getSlotVNs(renderFn(renderOpts, Object.assign({ $type: editRenderOpts ? "edit" : "cell" }, params))));
				}
			}
			cellValue = $table.getCellLabel(row, column);
		}
		const cellPlaceholder = editRenderOpts ? editRenderOpts.placeholder : "";
		return renderCellBaseVNs(params, [h("span", { class: "vxe-cell--label" }, [editRenderOpts && eqEmptyValue(cellValue) ? h("span", { class: "vxe-cell--placeholder" }, formatText(getFuncText(cellPlaceholder), 1)) : h("span", formatText(cellValue, 1))])]);
	},
	renderDeepCell(params) {
		return Cell.renderDeepNodeBtn(params, Cell.renderDefaultCell(params));
	},
	renderDefaultFooter(params) {
		return getFooterContent(params);
	},
	/**
	* 行分组
	*/
	renderRowGroupBtn(params, cellVNodes) {
		const { $table } = params;
		const tableReactData = $table.reactData;
		const tableInternalData = $table.internalData;
		const { row, level } = params;
		const { computeAggregateOpts } = $table.getComputeMaps();
		const { rowGroupExpandedFlag } = tableReactData;
		const { rowGroupExpandedMaps } = tableInternalData;
		const { mode, padding, indent, showIcon, iconOpen, iconClose } = computeAggregateOpts.value;
		const rowid = getRowid($table, row);
		const isExpand = !!rowGroupExpandedFlag && !!rowGroupExpandedMaps[rowid];
		return h("div", {
			class: ["vxe-row-group--tree-node", { "is--expanded": isExpand }],
			style: mode !== "column" && padding && indent ? { paddingLeft: `${level * indent}px` } : void 0
		}, [showIcon && row.isAggregate ? h("span", {
			class: "vxe-row-group--node-btn",
			onClick(evnt) {
				$table.triggerRowGroupExpandEvent(evnt, params);
			}
		}, [h("i", { class: isExpand ? iconOpen || getIcon().TABLE_ROW_GROUP_OPEN : iconClose || getIcon().TABLE_ROW_GROUP_CLOSE })]) : renderEmptyElement($table), h("div", { class: "vxe-row-group-cell" }, cellVNodes)]);
	},
	/**
	* 树
	*/
	renderTreeNodeBtn(params, cellVNodes) {
		const { $table, isHidden } = params;
		const tableReactData = $table.reactData;
		const tableInternalData = $table.internalData;
		const { row, column, level } = params;
		const { slots } = column;
		const iconSlot = slots ? slots.icon : null;
		if (iconSlot) return $table.callSlot(iconSlot, params);
		const { computeTreeOpts } = $table.getComputeMaps();
		const { treeExpandedFlag } = tableReactData;
		const { fullAllDataRowIdData, treeExpandedMaps, treeExpandLazyLoadedMaps } = tableInternalData;
		const treeOpts = computeTreeOpts.value;
		const { padding, indent, lazy, trigger, iconLoaded, showIcon, iconOpen, iconClose } = treeOpts;
		const childrenField = treeOpts.children || treeOpts.childrenField;
		const hasChildField = treeOpts.hasChild || treeOpts.hasChildField;
		const rowChilds = row[childrenField];
		const hasChild = rowChilds && rowChilds.length;
		let hasLazyChilds = false;
		let isActive = false;
		let isLazyLoading = false;
		let isLazyLoaded = false;
		const ons = {};
		if (!isHidden) {
			const rowid = getRowid($table, row);
			isActive = !!treeExpandedFlag && !!treeExpandedMaps[rowid];
			if (lazy) {
				const rest = fullAllDataRowIdData[rowid];
				isLazyLoading = !!treeExpandLazyLoadedMaps[rowid];
				hasLazyChilds = row[hasChildField];
				isLazyLoaded = !!rest.treeLoaded;
			}
		}
		if (!trigger || trigger === "default") ons.onClick = (evnt) => {
			$table.triggerTreeExpandEvent(evnt, params);
		};
		return h("div", {
			class: ["vxe-cell--tree-node", { "is--active": isActive }],
			style: padding && indent ? { paddingLeft: `${level * indent}px` } : void 0
		}, [showIcon && (lazy ? isLazyLoaded ? hasChild : hasChild || hasLazyChilds : hasChild) ? [h("div", Object.assign({ class: "vxe-cell--tree-btn" }, ons), [h("i", { class: isLazyLoading ? iconLoaded || getIcon().TABLE_TREE_LOADED : isActive ? iconOpen || getIcon().TABLE_TREE_OPEN : iconClose || getIcon().TABLE_TREE_CLOSE })])] : null, h("div", { class: "vxe-tree-cell" }, cellVNodes)]);
	},
	/**
	* 层级节点。
	* 行分组、树结构
	*/
	renderDeepNodeBtn(params, cellVNodes) {
		const { $table, row, column } = params;
		const { rowGroupNode } = column;
		const { rowGroupList } = $table.reactData;
		if (rowGroupList.length) {
			const { computeAggregateOpts } = $table.getComputeMaps();
			const { mode } = computeAggregateOpts.value;
			if (mode === "column" ? column.field === row.groupField : rowGroupNode) return [Cell.renderRowGroupBtn(params, cellVNodes)];
		}
		return [Cell.renderTreeNodeBtn(params, cellVNodes)];
	},
	/**
	* 序号
	*/
	renderSeqHeader(params) {
		const { $table, column } = params;
		const { slots } = column;
		const headerSlot = slots ? slots.header : null;
		return renderHeaderCellBaseVNs(params, renderTitleContent(params, headerSlot ? $table.callSlot(headerSlot, params) : formatText(column.getTitle(), 1)));
	},
	renderSeqCell(params) {
		const { $table, column } = params;
		const { treeConfig } = $table.props;
		const { computeSeqOpts } = $table.getComputeMaps();
		const seqOpts = computeSeqOpts.value;
		const { slots } = column;
		const defaultSlot = slots ? slots.default : null;
		if (defaultSlot) return renderCellBaseVNs(params, $table.callSlot(defaultSlot, params));
		const { seq } = params;
		const seqMd = seqOpts.seqMethod;
		return renderCellBaseVNs(params, [h("span", `${formatText(seqMd ? seqMd(params) : treeConfig ? seq : (seqOpts.startIndex || 0) + seq, 1)}`)]);
	},
	renderDeepIndexCell(params) {
		return Cell.renderDeepNodeBtn(params, Cell.renderSeqCell(params));
	},
	/**
	* 单选
	*/
	renderRadioHeader(params) {
		const { $table, column } = params;
		const { slots } = column;
		const headerSlot = slots ? slots.header : null;
		const titleSlot = slots ? slots.title : null;
		return renderHeaderCellBaseVNs(params, renderTitleContent(params, headerSlot ? $table.callSlot(headerSlot, params) : [h("span", { class: "vxe-radio--label" }, titleSlot ? $table.callSlot(titleSlot, params) : formatText(column.getTitle(), 1))]));
	},
	renderRadioCell(params) {
		const { $table, column, isHidden } = params;
		const tableReactData = $table.reactData;
		const { computeRadioOpts } = $table.getComputeMaps();
		const { selectRadioRow } = tableReactData;
		const radioOpts = computeRadioOpts.value;
		const { slots } = column;
		const { labelField, checkMethod, visibleMethod } = radioOpts;
		const { row } = params;
		const defaultSlot = slots ? slots.default : null;
		const radioSlot = slots ? slots.radio : null;
		const isChecked = $table.eqRow(row, selectRadioRow);
		const isVisible = !visibleMethod || visibleMethod({
			$table,
			row
		});
		let isDisabled = !!checkMethod;
		let ons;
		if (!isHidden) {
			ons = { onClick(evnt) {
				if (!isDisabled && isVisible) $table.triggerRadioRowEvent(evnt, params);
			} };
			if (checkMethod) isDisabled = !checkMethod({
				$table,
				row
			});
		}
		const radioParams = Object.assign(Object.assign({}, params), {
			checked: isChecked,
			disabled: isDisabled,
			visible: isVisible
		});
		if (radioSlot) return renderCellBaseVNs(params, $table.callSlot(radioSlot, radioParams));
		const radioVNs = [];
		if (isVisible) radioVNs.push(h("span", { class: ["vxe-radio--icon", isChecked ? getIcon().TABLE_RADIO_CHECKED : isDisabled ? getIcon().TABLE_RADIO_DISABLED_UNCHECKED : getIcon().TABLE_RADIO_UNCHECKED] }));
		if (defaultSlot || labelField) radioVNs.push(h("span", { class: "vxe-radio--label" }, defaultSlot ? $table.callSlot(defaultSlot, radioParams) : import_xe_utils.default.get(row, labelField)));
		return renderCellBaseVNs(params, [h("span", Object.assign({ class: ["vxe-cell--radio", {
			"is--checked": isChecked,
			"is--disabled": isDisabled
		}] }, ons), radioVNs)]);
	},
	renderDeepRadioCell(params) {
		return Cell.renderDeepNodeBtn(params, Cell.renderRadioCell(params));
	},
	/**
	* 多选
	*/
	renderCheckboxHeader(params) {
		const { $table, column, isHidden } = params;
		const tableReactData = $table.reactData;
		const { computeIsAllCheckboxDisabled, computeCheckboxOpts } = $table.getComputeMaps();
		const { isAllSelected: isAllCheckboxSelected, isIndeterminate: isAllCheckboxIndeterminate } = tableReactData;
		const isAllCheckboxDisabled = computeIsAllCheckboxDisabled.value;
		const { slots } = column;
		const headerSlot = slots ? slots.header : null;
		const titleSlot = slots ? slots.title : null;
		const { checkStrictly, showHeader, headerTitle } = computeCheckboxOpts.value;
		const colTitle = column.getTitle();
		const ons = {};
		if (!isHidden) ons.onClick = (evnt) => {
			if (!isAllCheckboxDisabled) $table.triggerCheckAllEvent(evnt, !isAllCheckboxSelected);
		};
		const checkboxParams = Object.assign(Object.assign({}, params), {
			checked: isAllCheckboxSelected,
			disabled: isAllCheckboxDisabled,
			indeterminate: isAllCheckboxIndeterminate
		});
		if (headerSlot) return renderHeaderCellBaseVNs(params, renderTitleContent(checkboxParams, $table.callSlot(headerSlot, checkboxParams)));
		if (checkStrictly ? !showHeader : showHeader === false) return renderHeaderCellBaseVNs(params, renderTitleContent(checkboxParams, [h("span", { class: "vxe-checkbox--label" }, titleSlot ? $table.callSlot(titleSlot, checkboxParams) : colTitle)]));
		return renderHeaderCellBaseVNs(params, renderTitleContent(checkboxParams, [h("span", Object.assign({
			class: ["vxe-cell--checkbox", {
				"is--checked": isAllCheckboxSelected,
				"is--disabled": isAllCheckboxDisabled,
				"is--indeterminate": isAllCheckboxIndeterminate
			}],
			title: import_xe_utils.default.eqNull(headerTitle) ? getI18n("vxe.table.allTitle") : `${headerTitle || ""}`
		}, ons), [h("span", { class: ["vxe-checkbox--icon", isAllCheckboxIndeterminate ? getIcon().TABLE_CHECKBOX_INDETERMINATE : isAllCheckboxSelected ? getIcon().TABLE_CHECKBOX_CHECKED : isAllCheckboxDisabled ? getIcon().TABLE_CHECKBOX_DISABLED_UNCHECKED : getIcon().TABLE_CHECKBOX_UNCHECKED] })].concat(titleSlot || colTitle ? [h("span", { class: "vxe-checkbox--label" }, titleSlot ? $table.callSlot(titleSlot, checkboxParams) : colTitle)] : []))]));
	},
	renderCheckboxCell(params) {
		const { $table, row, column, isHidden } = params;
		const tableProps = $table.props;
		const tableReactData = $table.reactData;
		const tableInternalData = $table.internalData;
		const { treeConfig } = tableProps;
		const { updateCheckboxFlag, isRowGroupStatus } = tableReactData;
		const { selectCheckboxMaps, treeIndeterminateRowMaps } = tableInternalData;
		const { computeCheckboxOpts, computeAggregateOpts } = $table.getComputeMaps();
		const { mapChildrenField } = computeAggregateOpts.value;
		const { labelField, checkMethod, visibleMethod } = computeCheckboxOpts.value;
		const { slots } = column;
		const defaultSlot = slots ? slots.default : null;
		const checkboxSlot = slots ? slots.checkbox : null;
		let indeterminate = false;
		let isChecked = false;
		let isVisible = true;
		let isDisabled = false;
		const ons = {};
		if (!isHidden) {
			const rowid = getRowid($table, row);
			isChecked = !!updateCheckboxFlag && !!selectCheckboxMaps[rowid];
			if (checkMethod && isRowGroupStatus && $table.isAggregateRecord(row)) {
				const childList = row[mapChildrenField || ""];
				if (!childList || !childList.length || childList.every((item) => !checkMethod({
					$table,
					row: item
				}))) isDisabled = true;
			} else {
				isVisible = !visibleMethod || visibleMethod({
					$table,
					row
				});
				isDisabled = checkMethod ? !checkMethod({
					$table,
					row
				}) : !!checkMethod;
			}
			if (treeConfig || isRowGroupStatus) indeterminate = !!treeIndeterminateRowMaps[rowid];
			ons.onClick = (evnt) => {
				if (!isDisabled && isVisible) $table.triggerCheckRowEvent(evnt, params, !isChecked);
			};
		}
		const checkboxParams = Object.assign(Object.assign({}, params), {
			checked: isChecked,
			disabled: isDisabled,
			visible: isVisible,
			indeterminate
		});
		if (checkboxSlot) return renderCellBaseVNs(params, $table.callSlot(checkboxSlot, checkboxParams));
		const checkVNs = [];
		if (isVisible) checkVNs.push(h("span", { class: ["vxe-checkbox--icon", indeterminate ? getIcon().TABLE_CHECKBOX_INDETERMINATE : isChecked ? getIcon().TABLE_CHECKBOX_CHECKED : isDisabled ? getIcon().TABLE_CHECKBOX_DISABLED_UNCHECKED : getIcon().TABLE_CHECKBOX_UNCHECKED] }));
		if (defaultSlot || labelField) checkVNs.push(h("span", { class: "vxe-checkbox--label" }, defaultSlot ? $table.callSlot(defaultSlot, checkboxParams) : import_xe_utils.default.get(row, labelField)));
		return renderCellBaseVNs(params, [h("span", Object.assign({ class: ["vxe-cell--checkbox", {
			"is--checked": isChecked,
			"is--disabled": isDisabled,
			"is--indeterminate": indeterminate,
			"is--hidden": !isVisible
		}] }, ons), checkVNs)]);
	},
	renderDeepSelectionCell(params) {
		return Cell.renderDeepNodeBtn(params, Cell.renderCheckboxCell(params));
	},
	renderCheckboxCellByProp(params) {
		const { $table, row, column, isHidden } = params;
		const tableProps = $table.props;
		const tableReactData = $table.reactData;
		const tableInternalData = $table.internalData;
		const { treeConfig } = tableProps;
		const { updateCheckboxFlag, isRowGroupStatus } = tableReactData;
		const { treeIndeterminateRowMaps } = tableInternalData;
		const { computeCheckboxOpts, computeAggregateOpts } = $table.getComputeMaps();
		const { mapChildrenField } = computeAggregateOpts.value;
		const checkboxOpts = computeCheckboxOpts.value;
		const { labelField, checkField, checkMethod, visibleMethod } = checkboxOpts;
		const indeterminateField = checkboxOpts.indeterminateField || checkboxOpts.halfField;
		const { slots } = column;
		const defaultSlot = slots ? slots.default : null;
		const checkboxSlot = slots ? slots.checkbox : null;
		let indeterminate = false;
		let isChecked = false;
		let isVisible = true;
		let isDisabled = false;
		const ons = {};
		if (!isHidden) {
			const rowid = getRowid($table, row);
			isChecked = !!updateCheckboxFlag && import_xe_utils.default.get(row, checkField);
			if (checkMethod && isRowGroupStatus && $table.isAggregateRecord(row)) {
				const childList = row[mapChildrenField || ""];
				if (!childList || !childList.length || childList.every((item) => !checkMethod({
					$table,
					row: item
				}))) isDisabled = true;
			} else {
				isVisible = !visibleMethod || visibleMethod({
					$table,
					row
				});
				isDisabled = checkMethod ? !checkMethod({
					$table,
					row
				}) : !!checkMethod;
			}
			if (treeConfig || isRowGroupStatus) indeterminate = !!treeIndeterminateRowMaps[rowid];
			ons.onClick = (evnt) => {
				if (!isDisabled && isVisible) $table.triggerCheckRowEvent(evnt, params, !isChecked);
			};
		}
		const checkboxParams = Object.assign(Object.assign({}, params), {
			checked: isChecked,
			disabled: isDisabled,
			visible: isVisible,
			indeterminate
		});
		if (checkboxSlot) return renderCellBaseVNs(params, $table.callSlot(checkboxSlot, checkboxParams));
		const checkVNs = [];
		if (isVisible) {
			checkVNs.push(h("span", { class: ["vxe-checkbox--icon", indeterminate ? getIcon().TABLE_CHECKBOX_INDETERMINATE : isChecked ? getIcon().TABLE_CHECKBOX_CHECKED : isDisabled ? getIcon().TABLE_CHECKBOX_DISABLED_UNCHECKED : getIcon().TABLE_CHECKBOX_UNCHECKED] }));
			if (defaultSlot || labelField) checkVNs.push(h("span", { class: "vxe-checkbox--label" }, defaultSlot ? $table.callSlot(defaultSlot, checkboxParams) : import_xe_utils.default.get(row, labelField)));
		}
		return renderCellBaseVNs(params, [h("span", Object.assign({ class: ["vxe-cell--checkbox", {
			"is--checked": isChecked,
			"is--disabled": isDisabled,
			"is--indeterminate": indeterminateField && !isChecked ? row[indeterminateField] : indeterminate,
			"is--hidden": !isVisible
		}] }, ons), checkVNs)]);
	},
	renderDeepSelectionCellByProp(params) {
		return Cell.renderDeepNodeBtn(params, Cell.renderCheckboxCellByProp(params));
	},
	/**
	* 展开行
	*/
	renderExpandCell(params) {
		const { $table, isHidden, row, column } = params;
		const tableReactData = $table.reactData;
		const tableInternalData = $table.internalData;
		const { isRowGroupStatus } = tableReactData;
		const { rowExpandedMaps, rowExpandLazyLoadedMaps } = tableInternalData;
		const { computeExpandOpts } = $table.getComputeMaps();
		const { lazy, labelField, iconLoaded, showIcon, iconOpen, iconClose, visibleMethod } = computeExpandOpts.value;
		const { slots } = column;
		const defaultSlot = slots ? slots.default : null;
		const iconSlot = slots ? slots.icon : null;
		let isActive = false;
		let isLazyLoading = false;
		if (isRowGroupStatus && row.isAggregate) return renderCellBaseVNs(params, []);
		if (iconSlot) return renderCellBaseVNs(params, $table.callSlot(iconSlot, params));
		if (!isHidden) {
			const rowid = getRowid($table, row);
			isActive = !!rowExpandedMaps[rowid];
			if (lazy) isLazyLoading = !!rowExpandLazyLoadedMaps[rowid];
		}
		return renderCellBaseVNs(params, [showIcon && (!visibleMethod || visibleMethod(params)) ? h("span", {
			class: ["vxe-table--expanded", { "is--active": isActive }],
			onMousedown(evnt) {
				evnt.stopPropagation();
			},
			onClick(evnt) {
				$table.triggerRowExpandEvent(evnt, params);
			}
		}, [h("i", { class: ["vxe-table--expand-btn", isLazyLoading ? iconLoaded || getIcon().TABLE_EXPAND_LOADED : isActive ? iconOpen || getIcon().TABLE_EXPAND_OPEN : iconClose || getIcon().TABLE_EXPAND_CLOSE] })]) : renderEmptyElement($table), defaultSlot || labelField ? h("span", { class: "vxe-table--expand-label" }, defaultSlot ? $table.callSlot(defaultSlot, params) : import_xe_utils.default.get(row, labelField)) : renderEmptyElement($table)]);
	},
	renderExpandData(params) {
		const { $table, column } = params;
		const { slots, contentRender } = column;
		const contentSlot = slots ? slots.content : null;
		if (contentSlot) return $table.callSlot(contentSlot, params);
		if (contentRender) {
			const compConf = renderer.get(contentRender.name);
			if (compConf) {
				const rtExpand = compConf.renderTableExpand || compConf.renderExpand;
				if (rtExpand) return getSlotVNs(rtExpand(contentRender, params));
			}
		}
		return [];
	},
	/**
	* HTML 标签
	*/
	renderHTMLCell(params) {
		const { $table, column } = params;
		const { slots } = column;
		const defaultSlot = slots ? slots.default : null;
		if (defaultSlot) return renderCellBaseVNs(params, $table.callSlot(defaultSlot, params));
		return renderCellBaseVNs(params, [h("span", {
			class: "vxe-cell--html",
			innerHTML: getDefaultCellLabel(params)
		})]);
	},
	renderDeepHTMLCell(params) {
		return Cell.renderDeepNodeBtn(params, Cell.renderHTMLCell(params));
	},
	/**
	* 排序和筛选
	*/
	renderSortAndFilterHeader(params) {
		return renderHeaderCellBaseVNs(params, Cell.renderHeaderTitle(params).concat(Cell.renderSortIcon(params).concat(Cell.renderFilterIcon(params))));
	},
	/**
	* 排序
	*/
	renderSortHeader(params) {
		return renderHeaderCellBaseVNs(params, Cell.renderHeaderTitle(params).concat(Cell.renderSortIcon(params)));
	},
	renderSortIcon(params) {
		const { $table, column } = params;
		const { computeSortOpts } = $table.getComputeMaps();
		const { showIcon, allowBtn, ascTitle, descTitle, iconLayout, iconAsc, iconDesc, iconVisibleMethod } = computeSortOpts.value;
		const { order, slots } = column;
		if (showIcon && (!iconVisibleMethod || iconVisibleMethod(params))) {
			const sortSlot = slots ? slots.sort : null;
			return sortSlot ? getSlotVNs($table.callSlot(sortSlot, params)) : [h("span", { class: ["vxe-cell--sort", `vxe-cell--sort-${iconLayout}-layout`] }, [h("i", {
				class: [
					"vxe-sort--asc-btn",
					iconAsc || getIcon().TABLE_SORT_ASC,
					{ "sort--active": order === "asc" }
				],
				title: import_xe_utils.default.eqNull(ascTitle) ? getI18n("vxe.table.sortAsc") : `${ascTitle || ""}`,
				onClick: allowBtn ? (evnt) => {
					evnt.stopPropagation();
					$table.triggerSortEvent(evnt, column, "asc");
				} : void 0
			}), h("i", {
				class: [
					"vxe-sort--desc-btn",
					iconDesc || getIcon().TABLE_SORT_DESC,
					{ "sort--active": order === "desc" }
				],
				title: import_xe_utils.default.eqNull(descTitle) ? getI18n("vxe.table.sortDesc") : `${descTitle || ""}`,
				onClick: allowBtn ? (evnt) => {
					evnt.stopPropagation();
					$table.triggerSortEvent(evnt, column, "desc");
				} : void 0
			})])];
		}
		return [];
	},
	/**
	* 筛选
	*/
	renderFilterHeader(params) {
		return renderHeaderCellBaseVNs(params, Cell.renderHeaderTitle(params).concat(Cell.renderFilterIcon(params)));
	},
	renderFilterIcon(params) {
		const { $table, column, hasFilter } = params;
		const { filterStore } = $table.reactData;
		const { computeFilterOpts } = $table.getComputeMaps();
		const { showIcon, iconNone, iconMatch, iconVisibleMethod } = computeFilterOpts.value;
		if (showIcon && (!iconVisibleMethod || iconVisibleMethod(params))) return [h("span", {
			class: ["vxe-cell--filter", { "is--active": filterStore.visible && filterStore.column === column }],
			onClick(evnt) {
				if ($table.triggerFilterEvent) $table.triggerFilterEvent(evnt, params.column, params);
			}
		}, [h("i", {
			class: ["vxe-filter--btn", hasFilter ? iconMatch || getIcon().TABLE_FILTER_MATCH : iconNone || getIcon().TABLE_FILTER_NONE],
			title: getI18n("vxe.table.filter")
		})])];
		return [];
	},
	/**
	* 可编辑
	*/
	renderEditHeader(params) {
		const { $table, column } = params;
		const tableProps = $table.props;
		const { computeEditOpts } = $table.getComputeMaps();
		const { editConfig, editRules } = tableProps;
		const editOpts = computeEditOpts.value;
		const { sortable, filters, editRender } = column;
		const isEnableEdit = editConfig && isEnableConf(editConfig);
		const editRenderOpts = isEnableEdit && isEnableConf(editRender) ? editRender : null;
		let isRequired = false;
		if (editRules) {
			const columnRules = import_xe_utils.default.get(editRules, column.field);
			if (columnRules) isRequired = columnRules.some((rule) => rule.required);
		}
		let editIconVNs = [];
		if (isEnableEdit) {
			const { showAsterisk, showIcon, icon } = editOpts;
			editIconVNs = [isRequired && showAsterisk ? h("span", { class: "vxe-cell--required-icon" }, [h("i")]) : renderEmptyElement($table), editRenderOpts && showIcon ? h("span", { class: "vxe-cell--edit-icon" }, import_xe_utils.default.isFunction(icon) ? getSlotVNs(icon({})) : [h("i", { class: icon || getIcon().TABLE_EDIT })]) : renderEmptyElement($table)];
		}
		return renderHeaderCellBaseVNs(params, editIconVNs.concat(Cell.renderHeaderTitle(params)).concat(sortable ? Cell.renderSortIcon(params) : []).concat(filters ? Cell.renderFilterIcon(params) : []));
	},
	renderRowEdit(params) {
		const { $table, column } = params;
		const { editConfig } = $table.props;
		const { editStore } = $table.reactData;
		const { actived } = editStore;
		const { editRender } = column;
		const editRenderOpts = editConfig && isEnableConf(editConfig) && isEnableConf(editRender) ? editRender : null;
		return Cell.runRenderer(params, !!(editRenderOpts && actived && actived.row === params.row));
	},
	renderDeepRowEdit(params) {
		return Cell.renderDeepNodeBtn(params, Cell.renderRowEdit(params));
	},
	renderCellEdit(params) {
		const { $table, column } = params;
		const { editConfig } = $table.props;
		const { editStore } = $table.reactData;
		const { actived } = editStore;
		const { editRender } = column;
		const editRenderOpts = editConfig && isEnableConf(editConfig) && isEnableConf(editRender) ? editRender : null;
		return Cell.runRenderer(params, !!(editRenderOpts && actived && actived.row === params.row && actived.column === params.column));
	},
	renderDeepCellEdit(params) {
		return Cell.renderDeepNodeBtn(params, Cell.renderCellEdit(params));
	},
	runRenderer(params, isEdit) {
		const { $table, row, column } = params;
		const { editConfig } = $table.props;
		const { isRowGroupStatus } = $table.reactData;
		const { slots, field, editRender, formatter } = column;
		const editRenderOpts = editConfig && isEnableConf(editConfig) && isEnableConf(editRender) ? editRender : null;
		const defaultSlot = slots ? slots.default : null;
		const gcSlot = slots ? slots.groupContent || slots["group-content"] : null;
		const editSlot = slots ? slots.edit : null;
		const cellParams = Object.assign({
			$type: "",
			isEdit
		}, params);
		if (isEdit && editRenderOpts) {
			cellParams.$type = "edit";
			if (editSlot) return $table.callSlot(editSlot, cellParams);
			const compConf = renderer.get(editRenderOpts.name);
			const rtEdit = compConf ? compConf.renderTableEdit || compConf.renderEdit : null;
			if (rtEdit) return getSlotVNs(rtEdit(editRenderOpts, cellParams));
			return [];
		}
		if (isRowGroupStatus && field && row.isAggregate) {
			const aggRow = row;
			const { computeAggregateOpts } = $table.getComputeMaps();
			const { mapChildrenField } = computeAggregateOpts.value;
			const groupField = aggRow.groupField;
			const groupContent = aggRow.groupContent;
			const childList = mapChildrenField ? aggRow[mapChildrenField] || [] : [];
			const childCount = aggRow.childCount;
			if (gcSlot) return renderCellBaseVNs(params, $table.callSlot(gcSlot, Object.assign({
				groupField,
				groupContent,
				childList,
				childCount
			}, params)));
		} else if (defaultSlot) return renderCellBaseVNs(params, $table.callSlot(defaultSlot, cellParams));
		if (formatter) return renderCellBaseVNs(params, [h("span", { class: "vxe-cell--label" }, getDefaultCellLabel(cellParams))]);
		return Cell.renderDefaultCell(cellParams);
	}
};
//#endregion
export { triggerEvent as $, setCellValue as A, getPaddingTopBottomSize as B, getRowkey as C, isColumnInfo as D, hasDeepKey as E, checkTargetElement as F, initTpImg as G, getTpImg as H, getAbsolutePos as I, removeClass as J, isPx as K, getDomNode as L, toTreePathSeq as M, watchColumn as N, restoreScrollLocation as O, addClass as P, toCssUnit as Q, getEventTargetNode as R, getRowid as S, handleRowidOrRow as T, hasClass as U, getPropClass as V, hasControlKey as W, setScrollLeft as X, scrollToView as Y, setScrollTop as Z, getColumnList as _, convertHeaderColumnToRows as a, getRootColumn as b, createHandleUpdateRowId as c, createRowId as d, updateCellTitle as et, destroyColumn as f, getColReMinWidth as g, getCellValue as h, colToVisible as i, toFilters as j, rowToVisible as k, createInternalData as l, getCellRestHeight as m, assembleColumn as n, wheelScrollTopTo as nt, convertHeaderToGridRows as o, getCalcHeight as p, isScale as q, clearTableAllStatus as r, createHandleGetRowId as s, Cell as t, wheelScrollLeftTo as tt, createReactData as u, getLastChildColumn as v, handleFieldOrColumn as w, getRowUniqueId as x, getRefElem as y, getOffsetHeight as z };

//# sourceMappingURL=cell-Tc0N220z.js.map