import { It as onMounted, Ot as mergeProps, St as inject, W as computed, Wt as provide, Xn as shallowRef, kt as nextTick, mt as h, rt as defineComponent, tt as createVNode } from "./runtime-dom.esm-bundler-BEW9uiVc.js";
import "./vue.js";
import { n as blue, r as generate, t as getShadowRoot } from "./shadow-A51eKp6U.js";
import { E as updateCSS, s as classNames, w as warning_default } from "./dist-B3YnmQlL.js";
//#region ../../node_modules/.pnpm/@antdv-next+icons@1.0.6_vue@3.5.33_typescript@5.9.3_/node_modules/@antdv-next/icons/dist/components/Context.js
var IconContextKey = Symbol("IconContext");
function useProvideIconContext(context) {
	provide(IconContextKey, context);
}
var IconContextProvider = /* @__PURE__ */ defineComponent((props, { slots }) => {
	useProvideIconContext(computed(() => props));
	return () => {
		return slots?.default?.();
	};
}, { props: {
	prefixCls: {
		type: String,
		required: false
	},
	rootClass: {
		type: String,
		required: false
	},
	csp: {
		type: Object,
		required: false
	},
	layer: {
		type: String,
		required: false
	}
} });
function useIconContext() {
	return inject(IconContextKey, computed(() => ({})));
}
//#endregion
//#region ../../node_modules/.pnpm/@antdv-next+icons@1.0.6_vue@3.5.33_typescript@5.9.3_/node_modules/@antdv-next/icons/dist/utils.js
function isIconDefinition(target) {
	return typeof target === "object" && typeof target.name === "string" && typeof target.theme === "string" && (typeof target.icon === "object" || typeof target.icon === "function");
}
function generate$1(node, key, rootProps) {
	if (!rootProps) return h(node.tag, {
		key,
		...node.attrs
	}, (node.children || []).map((child, index) => generate$1(child, `${key}-${node.tag}-${index}`)));
	return h(node.tag, {
		key,
		...rootProps,
		...node.attrs
	}, (node.children || []).map((child, index) => generate$1(child, `${key}-${node.tag}-${index}`)));
}
function getSecondaryColor(primaryColor) {
	return generate(primaryColor)[0];
}
function normalizeTwoToneColors(twoToneColor) {
	if (!twoToneColor) return [];
	return Array.isArray(twoToneColor) ? twoToneColor : [twoToneColor];
}
var iconStyles = `
.anticon {
  display: inline-flex;
  align-items: center;
  color: inherit;
  font-style: normal;
  line-height: 0;
  text-align: center;
  text-transform: none;
  vertical-align: -0.125em;
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

.anticon > * {
  line-height: 1;
}

.anticon svg {
  display: inline-block;
  vertical-align: inherit;
}

.anticon::before {
  display: none;
}

.anticon .anticon-icon {
  display: block;
}

.anticon[tabindex] {
  cursor: pointer;
}

.anticon-spin {
  -webkit-animation: loadingCircle 1s infinite linear;
  animation: loadingCircle 1s infinite linear;
}

@-webkit-keyframes loadingCircle {
  100% {
    -webkit-transform: rotate(360deg);
    transform: rotate(360deg);
  }
}

@keyframes loadingCircle {
  100% {
    -webkit-transform: rotate(360deg);
    transform: rotate(360deg);
  }
}
`;
function useInsertStyles(eleRef) {
	const iconContext = useIconContext();
	let mergedStyleStr = iconStyles;
	onMounted(async () => {
		await nextTick();
		const { prefixCls, csp, layer } = iconContext.value;
		if (prefixCls) mergedStyleStr = mergedStyleStr.replace(/anticon/g, prefixCls);
		if (layer) mergedStyleStr = `@layer ${layer} {\n${mergedStyleStr}\n}`;
		const ele = eleRef.value;
		const shadowRoot = getShadowRoot(ele);
		updateCSS(mergedStyleStr, "@ant-design-icons", {
			prepend: !layer,
			csp,
			attachTo: shadowRoot
		});
	});
}
//#endregion
//#region ../../node_modules/.pnpm/@antdv-next+icons@1.0.6_vue@3.5.33_typescript@5.9.3_/node_modules/@antdv-next/icons/dist/components/IconBase.js
var twoToneColorPalette = {
	primaryColor: "#333",
	secondaryColor: "#E6E6E6",
	calculated: false
};
function setTwoToneColors({ primaryColor, secondaryColor }) {
	twoToneColorPalette.primaryColor = primaryColor;
	twoToneColorPalette.secondaryColor = secondaryColor || getSecondaryColor(primaryColor);
	twoToneColorPalette.calculated = !!secondaryColor;
}
function getTwoToneColors() {
	return { ...twoToneColorPalette };
}
function unwrapIconDefinition(icon) {
	if (isIconDefinition(icon)) return icon;
	const maybeDefault = icon && typeof icon === "object" && "default" in icon ? icon.default : void 0;
	if (isIconDefinition(maybeDefault)) return maybeDefault;
}
var IconBaseComp = /* @__PURE__ */ defineComponent((props, { attrs }) => {
	const svgRef = shallowRef();
	useInsertStyles(svgRef);
	return () => {
		const { icon, onClick, primaryColor, secondaryColor } = props;
		const resolvedIcon = unwrapIconDefinition(icon);
		let colors = twoToneColorPalette;
		if (primaryColor) colors = {
			primaryColor,
			secondaryColor: secondaryColor || getSecondaryColor(primaryColor)
		};
		warning_default(!!resolvedIcon, `icon should be icon definiton, but got ${icon}`);
		if (!resolvedIcon) return null;
		let target = resolvedIcon;
		if (target && typeof target.icon === "function") target = {
			...target,
			icon: target.icon(colors.primaryColor, colors.secondaryColor)
		};
		return generate$1(target.icon, `svg-${target.name}`, {
			onClick,
			"data-icon": target.name,
			"width": "1em",
			"height": "1em",
			"fill": "currentColor",
			"aria-hidden": "true",
			"ref": svgRef,
			...attrs
		});
	};
}, {
	props: {
		icon: {
			type: Object,
			required: true
		},
		onClick: {
			type: Function,
			required: false
		},
		primaryColor: {
			type: String,
			required: false
		},
		secondaryColor: {
			type: String,
			required: false
		},
		focusable: {
			type: String,
			required: false
		}
	},
	name: "IconBase",
	inheritAttrs: false
});
IconBaseComp.getTwoToneColors = getTwoToneColors;
IconBaseComp.setTwoToneColors = setTwoToneColors;
//#endregion
//#region ../../node_modules/.pnpm/@antdv-next+icons@1.0.6_vue@3.5.33_typescript@5.9.3_/node_modules/@antdv-next/icons/dist/components/twoTonePrimaryColor.js
function setTwoToneColor(twoToneColor) {
	const [primaryColor, secondaryColor] = normalizeTwoToneColors(twoToneColor);
	return IconBaseComp.setTwoToneColors({
		primaryColor,
		secondaryColor
	});
}
function getTwoToneColor() {
	const colors = IconBaseComp.getTwoToneColors();
	if (!colors.calculated) return colors.primaryColor;
	return [colors.primaryColor, colors.secondaryColor];
}
//#endregion
//#region ../../node_modules/.pnpm/@antdv-next+icons@1.0.6_vue@3.5.33_typescript@5.9.3_/node_modules/@antdv-next/icons/dist/components/AntdIcon.js
setTwoToneColor(blue.primary);
var AntdIcon = /* @__PURE__ */ defineComponent((props, { attrs }) => {
	const iconContext = useIconContext();
	return () => {
		const { icon, spin, rotate, onClick, tabIndex, twoToneColor, ...restProps } = props;
		const { prefixCls = "anticon", rootClass } = iconContext.value;
		const classString = classNames(rootClass, prefixCls, {
			[`${prefixCls}-${icon.name}`]: !!icon.name,
			[`${prefixCls}-spin`]: !!spin || icon.name === "loading"
		});
		let iconTabIndex = tabIndex;
		if (iconTabIndex === void 0 && onClick) iconTabIndex = -1;
		const svgStyle = rotate ? {
			msTransform: `rotate(${rotate}deg)`,
			transform: `rotate(${rotate}deg)`
		} : void 0;
		const [primaryColor, secondaryColor] = normalizeTwoToneColors(twoToneColor);
		const restAttrs = { ...attrs };
		delete restAttrs.class;
		return createVNode("span", mergeProps({
			"role": "img",
			"aria-label": icon.name
		}, restProps, restAttrs, {
			"tabindex": iconTabIndex,
			"onClick": onClick,
			"class": [classString, attrs.class]
		}), [createVNode(IconBaseComp, {
			"icon": icon,
			"primaryColor": primaryColor,
			"secondaryColor": secondaryColor,
			"style": svgStyle
		}, null)]);
	};
}, {
	props: {
		icon: {
			type: Object,
			required: true
		},
		twoToneColor: {
			type: [String, Array],
			required: false
		},
		onClick: {
			type: Function,
			required: false
		},
		tabIndex: {
			type: Number,
			required: false
		},
		spin: {
			type: Boolean,
			required: false
		},
		rotate: {
			type: Number,
			required: false
		}
	},
	inheritAttrs: false
});
AntdIcon.getTwoToneColor = getTwoToneColor;
AntdIcon.setTwoToneColor = setTwoToneColor;
//#endregion
export { IconContextProvider as n, AntdIcon as t };

//# sourceMappingURL=AntdIcon-CGwHAHAW.js.map