import { H as cloneVNode, N as Fragment, Ot as mergeProps, Tt as isVNode, W as computed, Xn as shallowRef, _n as watch, rt as defineComponent, tt as createVNode } from "./runtime-dom.esm-bundler-BEW9uiVc.js";
import "./vue.js";
import { c as clsx, y as raf_default } from "./dist-B3YnmQlL.js";
import { Q as useComponentBaseConfig, at as toPropsRefs, c as useToArr, k as merge, l as useToProps, o as useMergeSemantic, p as genStyleHooks, t as getAttrStyleAndClass, u as omit } from "./useMergeSemantic-NT6cWlpu.js";
import { t as useSize } from "./useSize-C23Evsd5.js";
import { r as useCompactItemContext } from "./Compact-BlWyMZnR.js";
import { t as button_default } from "./button-C_ePK3XM.js";
import { t as devUseWarning } from "./warning-DVCi2y10.js";
import { t as useCSSVarCls_default } from "./useCSSVarCls-C_79E43w.js";
import { n as Input_default, t as Password_default } from "./Password-CoPtghrC.js";
import { t as SearchOutlined } from "./SearchOutlined-DexP6mcw.js";
import { t as pickAttrs } from "./pickAttrs-D9GNilF0.js";
import { t as getMergedStatus } from "./statusUtils-DpKVCk3N.js";
import { c as useFormItemInputContextProvider, s as useFormItemInputContext } from "./context-CoFN053h.js";
import { a as style_default, m as initInputToken, o as useSharedStyle, p as initComponentToken } from "./style-g373ECLL.js";
import { n as SpaceCompact } from "./space-BaKUHWw1.js";
import { t as TextArea_default } from "./TextArea-Do1xgm1A.js";
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/input/Group.js
var Group_default = /* @__PURE__ */ defineComponent((props, { slots, attrs }) => {
	devUseWarning("Input.Group").deprecated(false, "Input.Group", "Space.Compact");
	const { getPrefixCls, direction } = useComponentBaseConfig("input", props);
	const prefixCls = computed(() => getPrefixCls("input-group", props.prefixCls));
	const rootCls = useCSSVarCls_default(prefixCls);
	const [hashId, cssVarCls] = useSharedStyle(prefixCls, computed(() => props.rootClass));
	style_default(prefixCls, rootCls);
	const formItemContext = useFormItemInputContext();
	useFormItemInputContextProvider(computed(() => ({
		...formItemContext.value,
		isFormItemInput: false
	})));
	const { style, restAttrs } = getAttrStyleAndClass(attrs);
	return createVNode(SpaceCompact, mergeProps(restAttrs, {
		"class": clsx(prefixCls.value, cssVarCls.value, hashId.value, {
			[`${prefixCls.value}-rtl`]: direction.value === "rtl",
			[`${prefixCls.value}-lg`]: props.size === "large",
			[`${prefixCls.value}-sm`]: props.size === "small",
			[`${prefixCls.value}-compact`]: true
		}, props.rootClass, attrs.class),
		"style": style,
		"size": props.size
	}), { default: () => [slots.default?.()] });
}, {
	props: {
		size: {
			type: [String, null],
			required: false
		},
		rootClass: {
			type: String,
			required: false
		},
		prefixCls: {
			type: String,
			required: false
		}
	},
	name: "AInputGroup",
	inheritAttrs: false
});
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/input/style/otp.js
var genOTPStyle = (token) => {
	const { componentCls, paddingXS } = token;
	return { [componentCls]: {
		display: "inline-flex",
		alignItems: "center",
		flexWrap: "nowrap",
		columnGap: paddingXS,
		[`${componentCls}-input-wrapper`]: {
			position: "relative",
			[`${componentCls}-mask-icon`]: {
				position: "absolute",
				zIndex: "1",
				top: "50%",
				right: "50%",
				transform: "translate(50%, -50%)",
				pointerEvents: "none"
			},
			[`${componentCls}-mask-input`]: {
				color: "transparent",
				caretColor: token.colorText,
				"&::selection": { color: "transparent" }
			},
			[`${componentCls}-mask-input[type=number]::-webkit-inner-spin-button`]: {
				"-webkit-appearance": "none",
				margin: 0
			},
			[`${componentCls}-mask-input[type=number]`]: { "-moz-appearance": "textfield" }
		},
		"&-rtl": { direction: "rtl" },
		[`${componentCls}-input`]: {
			textAlign: "center",
			paddingInline: token.paddingXXS
		},
		[`&${componentCls}-sm ${componentCls}-input`]: { paddingInline: token.calc(token.paddingXXS).div(2).equal() },
		[`&${componentCls}-lg ${componentCls}-input`]: { paddingInline: token.paddingXS }
	} };
};
var otp_default = genStyleHooks(["Input", "OTP"], (token) => {
	return genOTPStyle(merge(token, initInputToken(token)));
}, initComponentToken);
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/input/OTP/OTPInput.js
var OTPInput_default = /* @__PURE__ */ defineComponent((props, { attrs, expose, slots }) => {
	const inputRef = shallowRef();
	expose({
		focus: (...args) => inputRef.value?.focus?.(...args),
		blur: () => inputRef.value?.blur?.(),
		input: inputRef
	});
	const syncSelection = () => {
		raf_default(() => {
			const inputEle = inputRef.value?.input;
			if (document.activeElement === inputEle && inputEle) inputEle.select();
		});
	};
	const handleChange = (e) => {
		props.onChange(props.index, e?.target?.value ?? "");
	};
	const handleKeyDown = (event) => {
		const { key, ctrlKey, metaKey } = event;
		if (key === "ArrowLeft") props.onActiveChange(props.index - 1);
		else if (key === "ArrowRight") props.onActiveChange(props.index + 1);
		else if (key === "z" && (ctrlKey || metaKey)) event.preventDefault();
		else if (key === "Backspace" && !props.value) props.onActiveChange(props.index - 1);
		syncSelection();
	};
	const { className, style, restAttrs } = getAttrStyleAndClass(attrs);
	const restInputProps = omit(props, [
		"prefixCls",
		"index",
		"onChange",
		"onActiveChange",
		"mask"
	]);
	const maskValue = typeof props.mask === "string" ? props.mask : props.value;
	return () => createVNode("span", {
		"class": `${props.prefixCls}-input-wrapper`,
		"role": "presentation"
	}, [props.mask && props.value !== "" && props.value !== void 0 && createVNode("span", {
		"class": `${props.prefixCls}-mask-icon`,
		"aria-hidden": "true"
	}, [maskValue]), createVNode(Input_default, mergeProps(restAttrs, restInputProps, {
		"ref": inputRef,
		"value": props.value,
		"type": props.mask === true ? "password" : props.type ?? "text",
		"class": clsx(className, { [`${props.prefixCls}-mask-input`]: props.mask }),
		"style": style,
		"onChange": handleChange,
		"htmlSize": 1,
		"onKeydown": handleKeyDown,
		"onFocus": () => syncSelection(),
		"onMousedown": () => syncSelection(),
		"onMouseup": () => syncSelection(),
		"aria-label": `OTP Input ${props.index + 1}`
	}, { "onUpdate:value": (value) => props.onChange(props.index, value ?? "") }), slots)]);
}, {
	props: {
		prefixCls: {
			type: String,
			required: true
		},
		index: {
			type: Number,
			required: true
		},
		value: {
			type: String,
			required: false
		},
		onChange: {
			type: Function,
			required: true
		},
		onActiveChange: {
			type: Function,
			required: true
		},
		mask: {
			type: [Boolean, String],
			required: false,
			default: void 0
		},
		size: {
			type: [String, null],
			required: false
		},
		disabled: {
			type: Boolean,
			required: false,
			default: void 0
		},
		status: {
			type: String,
			required: false
		},
		addonBefore: {
			type: [
				Function,
				String,
				Number,
				null,
				Object,
				Boolean
			],
			required: false,
			default: void 0
		},
		addonAfter: {
			type: [
				Function,
				String,
				Number,
				null,
				Object,
				Boolean
			],
			required: false,
			default: void 0
		},
		bordered: {
			type: Boolean,
			required: false,
			default: void 0
		},
		variant: {
			type: String,
			required: false
		},
		classes: {
			type: [Object, Function],
			required: false
		},
		styles: {
			type: [Object, Function],
			required: false
		},
		rootClass: {
			type: String,
			required: false
		},
		defaultValue: { required: false },
		type: { required: false },
		showCount: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		},
		autoComplete: {
			type: String,
			required: false
		},
		htmlSize: {
			type: Number,
			required: false
		},
		placeholder: {
			type: String,
			required: false
		},
		count: { required: false },
		maxlength: {
			type: Number,
			required: false
		},
		readonly: {
			type: Boolean,
			required: false,
			default: void 0
		},
		hidden: {
			type: Boolean,
			required: false,
			default: void 0
		},
		dataAttrs: { required: false },
		components: { required: false },
		prefix: {
			type: [
				Function,
				String,
				Number,
				null,
				Object,
				Boolean
			],
			required: false,
			default: void 0
		},
		suffix: {
			type: [
				Function,
				String,
				Number,
				null,
				Object,
				Boolean
			],
			required: false,
			default: void 0
		},
		allowClear: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		},
		changeOnComposing: {
			type: Boolean,
			required: false,
			default: void 0
		},
		autoFocus: {
			type: Boolean,
			required: false,
			default: void 0
		},
		inputMode: {
			type: String,
			required: false
		}
	},
	name: "AInputOTPInput",
	inheritAttrs: false
});
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/input/OTP/index.js
var strToArr = (str) => (str || "").split("");
var OTP_default = /* @__PURE__ */ defineComponent((props, { attrs, emit, slots }) => {
	devUseWarning("InputOTP")(!(typeof props.mask === "string" && props.mask.length > 1), "usage", "`mask` prop should be a single character.");
	const { prefixCls, direction, class: contextClassName, style: contextStyle, classes: contextClassNames, styles: contextStyles } = useComponentBaseConfig("otp", props);
	const [hashId, cssVarCls] = otp_default(prefixCls);
	const mergedLength = computed(() => props.length ?? 6);
	const mergedSize = useSize((ctx) => props.size ?? ctx);
	const formItemInputContext = useFormItemInputContext();
	const mergedStatus = computed(() => getMergedStatus(formItemInputContext.value.status, props.status));
	useFormItemInputContextProvider(computed(() => ({
		...formItemInputContext.value,
		status: mergedStatus.value,
		hasFeedback: false,
		feedbackIcon: null
	})));
	const { classes, styles } = toPropsRefs(props, "classes", "styles");
	const mergedProps = computed(() => ({
		...props,
		length: mergedLength.value
	}));
	const [mergedClassNames, mergedStyles] = useMergeSemantic(useToArr(contextClassNames, classes), useToArr(contextStyles, styles), useToProps(mergedProps));
	const inputRefs = shallowRef({});
	const internalFormatter = (txt) => props.formatter ? props.formatter(txt) : txt;
	const valueCells = shallowRef(strToArr(internalFormatter(props.value ?? props.defaultValue ?? "")));
	watch(() => props.value, (newValue) => {
		valueCells.value = strToArr(internalFormatter(newValue ?? ""));
	});
	const triggerValueCellsChange = (nextValueCells) => {
		const prevValue = valueCells.value.join("");
		valueCells.value = nextValueCells;
		emit("input", [...nextValueCells]);
		const nextValue = nextValueCells.join("");
		emit("update:value", nextValue);
		if (nextValueCells.length === mergedLength.value && nextValueCells.every((cell) => cell) && nextValue !== prevValue) emit("change", nextValue);
	};
	const patchValue = (index, txt) => {
		let nextCells = [...valueCells.value];
		for (let i = 0; i < index; i += 1) if (!nextCells[i]) nextCells[i] = "";
		if (txt.length <= 1) nextCells[index] = txt;
		else nextCells = nextCells.slice(0, index).concat(strToArr(txt));
		nextCells = nextCells.slice(0, mergedLength.value);
		for (let i = nextCells.length - 1; i >= 0; i -= 1) {
			if (nextCells[i]) break;
			nextCells.pop();
		}
		return strToArr(internalFormatter(nextCells.map((cell) => cell || " ").join(""))).map((cell, i) => {
			if (cell === " " && !nextCells[i]) return nextCells[i];
			return cell;
		});
	};
	const handleInputChange = (index, txt) => {
		const nextCells = patchValue(index, txt);
		const nextIndex = Math.min(index + txt.length, mergedLength.value - 1);
		if (nextIndex !== index && nextCells[index] !== void 0) inputRefs.value[nextIndex]?.focus?.();
		triggerValueCellsChange(nextCells);
	};
	const handleActiveChange = (nextIndex) => {
		inputRefs.value[nextIndex]?.focus?.();
	};
	const renderSeparator = (index) => {
		const separator = slots.separator || props.separator;
		const separatorNode = typeof separator === "function" ? separator({ index }) : separator;
		if (!separatorNode) return null;
		return createVNode("span", {
			"key": `otp-sep-${index}`,
			"class": clsx(`${prefixCls.value}-separator`, mergedClassNames.value.separator),
			"style": mergedStyles.value.separator
		}, [separatorNode]);
	};
	return () => {
		const { className, style, restAttrs } = getAttrStyleAndClass(attrs);
		return createVNode("div", mergeProps(restAttrs, {
			"class": clsx(className, prefixCls.value, cssVarCls.value, props.rootClass, contextClassName.value, hashId.value, {
				[`${prefixCls.value}-sm`]: mergedSize.value === "small",
				[`${prefixCls.value}-lg`]: mergedSize.value === "large",
				[`${prefixCls.value}-rtl`]: direction.value === "rtl"
			}, mergedClassNames.value.root),
			"style": {
				...mergedStyles.value.root,
				...contextStyle.value,
				...style
			},
			"role": "group"
		}), [Array.from({ length: mergedLength.value }).map((_, index) => createVNode(Fragment, null, [createVNode(OTPInput_default, {
			"key": `otp-${index}`,
			"prefixCls": prefixCls.value,
			"index": index,
			"value": valueCells.value[index] || "",
			"onChange": handleInputChange,
			"onActiveChange": handleActiveChange,
			"autoFocus": index === 0 && props.autoFocus,
			"mask": props.mask,
			"type": props.type,
			"inputMode": props.inputMode,
			"size": mergedSize.value,
			"variant": props.variant,
			"status": mergedStatus.value,
			"disabled": props.disabled,
			"class": clsx(`${prefixCls.value}-input`, mergedClassNames.value.input),
			"style": mergedStyles.value.input,
			"ref": (instance) => {
				inputRefs.value[index] = instance;
			}
		}, null), index < mergedLength.value - 1 ? renderSeparator(index) : null]))]);
	};
}, {
	props: {
		length: {
			type: Number,
			required: false
		},
		variant: {
			type: String,
			required: false
		},
		size: {
			type: [String, null],
			required: false
		},
		defaultValue: {
			type: String,
			required: false
		},
		value: {
			type: String,
			required: false
		},
		formatter: {
			type: Function,
			required: false
		},
		separator: {
			type: [
				Function,
				String,
				Number,
				null,
				Object,
				Boolean
			],
			required: false,
			default: void 0
		},
		disabled: {
			type: Boolean,
			required: false,
			default: void 0
		},
		status: {
			type: String,
			required: false
		},
		mask: {
			type: [Boolean, String],
			required: false,
			default: void 0
		},
		type: { required: false },
		inputMode: {
			type: String,
			required: false
		},
		autoFocus: {
			type: Boolean,
			required: false,
			default: void 0
		},
		classes: {
			type: [Object, Function],
			required: false
		},
		styles: {
			type: [Object, Function],
			required: false
		},
		rootClass: {
			type: String,
			required: false
		},
		prefixCls: {
			type: String,
			required: false
		}
	},
	emits: [
		"change",
		"input",
		"update:value"
	],
	name: "AInputOtp",
	inheritAttrs: false
});
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/input/style/search.js
var genSearchStyle = (token) => {
	const { componentCls } = token;
	const btnCls = `${componentCls}-btn`;
	return { [componentCls]: {
		width: "100%",
		[btnCls]: { "&-filled": {
			background: token.colorFillTertiary,
			"&:not(:disabled)": {
				"&:hover": { background: token.colorFillSecondary },
				"&:active": { background: token.colorFill }
			}
		} }
	} };
};
var search_default = genStyleHooks(["Input", "Search"], genSearchStyle);
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/input/Search.js
function _isSlot(s) {
	return typeof s === "function" || Object.prototype.toString.call(s) === "[object Object]" && !isVNode(s);
}
var schema = { button: { _default: "root" } };
var omitInputKeys = [
	"enterButton",
	"loading",
	"classes",
	"styles",
	"rootClass",
	"prefixCls",
	"inputPrefixCls"
];
var Search_default = /* @__PURE__ */ defineComponent((props, { slots, attrs, emit, expose }) => {
	const composedRef = shallowRef(false);
	const inputRef = shallowRef();
	const { prefixCls, direction, class: contextClassName, style: contextStyle, classes: contextClassNames, styles: contextStyles, getPrefixCls } = useComponentBaseConfig("inputSearch", props, void 0, "input-search");
	const inputPrefixCls = computed(() => getPrefixCls("input", props.inputPrefixCls));
	const { classes, styles, size: customizeSize, disabled: customDisabled, variant } = toPropsRefs(props, "classes", "styles", "size", "disabled", "variant");
	const [hashId, cssVarCls] = search_default(prefixCls);
	const { compactSize } = useCompactItemContext(prefixCls, direction);
	const mergedSize = useSize((ctx) => customizeSize.value ?? compactSize.value ?? ctx);
	const mergedProps = computed(() => ({
		...props,
		enterButton: props.enterButton
	}));
	const [mergedClassNames, mergedStyles] = useMergeSemantic(useToArr(contextClassNames, classes), useToArr(contextStyles, styles), useToProps(mergedProps), computed(() => schema));
	const handleSearch = (event, info, value) => {
		emit("search", value ?? inputRef.value?.input?.value ?? "", event, info ?? { source: "input" });
	};
	const handleChange = (e) => {
		if (e?.type === "click" && e?.target?.value !== void 0) handleSearch(e, { source: "clear" }, e.target.value);
		emit("change", e);
	};
	const handleCompositionStart = (e) => {
		composedRef.value = true;
		emit("compositionstart", e);
	};
	const handleCompositionEnd = (e) => {
		composedRef.value = false;
		emit("compositionend", e);
	};
	const handlePressEnter = (e) => {
		if (composedRef.value || props.loading) return;
		emit("pressEnter", e);
		handleSearch(e);
	};
	const onMouseDown = (e) => {
		if (document.activeElement === inputRef.value?.input) e.preventDefault();
	};
	const onSearchClick = (e) => {
		handleSearch(e);
	};
	expose({
		focus: (...args) => inputRef.value?.focus?.(...args),
		blur: () => inputRef.value?.blur?.(),
		input: computed(() => inputRef.value?.input ?? null)
	});
	return () => {
		const { className, style, restAttrs } = getAttrStyleAndClass(attrs);
		const rootAttrs = pickAttrs(restAttrs, { data: true });
		const inputAttrs = { ...restAttrs };
		Object.keys(rootAttrs).forEach((key) => {
			delete inputAttrs[key];
		});
		const restInputProps = omit(props, omitInputKeys);
		const mergedClassName = clsx(prefixCls.value, cssVarCls.value, {
			[`${prefixCls.value}-rtl`]: direction.value === "rtl",
			[`${prefixCls.value}-${mergedSize.value}`]: mergedSize.value,
			[`${prefixCls.value}-with-button`]: !!props.enterButton
		}, className, props.rootClass, contextClassName.value, mergedClassNames.value.root, hashId.value);
		const mergedRootStyle = {
			...mergedStyles.value.root,
			...contextStyle.value,
			...style
		};
		const btnPrefixCls = `${prefixCls.value}-btn`;
		const btnClassName = clsx(btnPrefixCls, { [`${btnPrefixCls}-${variant.value}`]: variant.value });
		const enterButtonValue = props.enterButton ?? false;
		const isBooleanEnterButton = typeof enterButtonValue === "boolean";
		const searchIcon = isBooleanEnterButton ? createVNode(SearchOutlined, null, null) : null;
		const buttonChildren = isBooleanEnterButton ? void 0 : enterButtonValue;
		let buttonNode;
		const enterButtonNode = buttonChildren;
		const isButtonVNode = isVNode(enterButtonNode);
		const isAntdButton = isButtonVNode && Boolean(enterButtonNode.type?.__ANT_BUTTON);
		const isNativeButton = isButtonVNode && enterButtonNode.type === "button";
		if (isAntdButton || isNativeButton) buttonNode = cloneVNode(enterButtonNode, {
			onMousedown: onMouseDown,
			onClick: (e) => {
				enterButtonNode?.props?.onClick?.(e);
				onSearchClick(e);
			},
			class: clsx(enterButtonNode?.props?.class, btnClassName),
			...isAntdButton ? {
				loading: props.loading,
				size: mergedSize.value
			} : {}
		});
		else buttonNode = createVNode(button_default, {
			"classes": mergedClassNames.value.button,
			"styles": mergedStyles.value.button,
			"class": btnClassName,
			"color": props.enterButton ? "primary" : "default",
			"size": mergedSize.value,
			"disabled": customDisabled.value,
			"loading": props.loading,
			"icon": searchIcon,
			"variant": variant.value === "borderless" || variant.value === "filled" || variant.value === "underlined" ? "text" : props.enterButton ? "solid" : void 0,
			"onMousedown": onMouseDown,
			"onClick": onSearchClick
		}, _isSlot(buttonChildren) ? buttonChildren : { default: () => [buttonChildren] });
		const inputClassNames = omit(mergedClassNames.value, ["root", "button"]);
		const inputStyles = omit(mergedStyles.value, ["root", "button"]);
		return createVNode(SpaceCompact, mergeProps({
			"class": mergedClassName,
			"style": mergedRootStyle
		}, { hidden: props.hidden }, rootAttrs, { "size": mergedSize.value }), { default: () => [createVNode(Input_default, mergeProps(inputAttrs, restInputProps, {
			"ref": inputRef,
			"prefixCls": inputPrefixCls.value,
			"size": mergedSize.value,
			"disabled": customDisabled.value,
			"classes": inputClassNames,
			"styles": inputStyles,
			"variant": variant.value,
			"onChange": handleChange,
			"onFocus": (e) => emit("focus", e),
			"onBlur": (e) => emit("blur", e),
			"onKeydown": (e) => emit("keydown", e),
			"onKeyup": (e) => emit("keyup", e),
			"onClear": () => {
				emit("clear");
			},
			"onCompositionstart": handleCompositionStart,
			"onCompositionend": handleCompositionEnd,
			"onPressEnter": handlePressEnter
		}, { "onUpdate:value": (value) => emit("update:value", value) }), slots), buttonNode] });
	};
}, {
	props: {
		inputPrefixCls: {
			type: String,
			required: false
		},
		on: { required: false },
		enterButton: {
			type: [
				Boolean,
				Function,
				String,
				Number,
				null,
				Object
			],
			required: false,
			default: void 0
		},
		loading: {
			type: Boolean,
			required: false,
			default: void 0
		},
		size: {
			type: [String, null],
			required: false
		},
		hidden: {
			type: Boolean,
			required: false,
			default: void 0
		},
		classes: {
			type: [Object, Function],
			required: false
		},
		styles: {
			type: [Object, Function],
			required: false
		},
		disabled: {
			type: Boolean,
			required: false,
			default: void 0
		},
		status: {
			type: String,
			required: false
		},
		addonBefore: {
			type: [
				Function,
				String,
				Number,
				null,
				Object,
				Boolean
			],
			required: false,
			default: void 0
		},
		addonAfter: {
			type: [
				Function,
				String,
				Number,
				null,
				Object,
				Boolean
			],
			required: false,
			default: void 0
		},
		bordered: {
			type: Boolean,
			required: false,
			default: void 0
		},
		variant: {
			type: String,
			required: false
		},
		prefixCls: {
			type: String,
			required: false
		},
		value: { required: false },
		defaultValue: { required: false },
		type: { required: false },
		showCount: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		},
		autoComplete: {
			type: String,
			required: false
		},
		htmlSize: {
			type: Number,
			required: false
		},
		placeholder: {
			type: String,
			required: false
		},
		count: { required: false },
		maxlength: {
			type: Number,
			required: false
		},
		readonly: {
			type: Boolean,
			required: false,
			default: void 0
		},
		dataAttrs: { required: false },
		components: { required: false },
		prefix: {
			type: [
				Function,
				String,
				Number,
				null,
				Object,
				Boolean
			],
			required: false,
			default: void 0
		},
		suffix: {
			type: [
				Function,
				String,
				Number,
				null,
				Object,
				Boolean
			],
			required: false,
			default: void 0
		},
		allowClear: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		},
		changeOnComposing: {
			type: Boolean,
			required: false,
			default: void 0
		},
		autoFocus: {
			type: Boolean,
			required: false,
			default: void 0
		},
		inputMode: {
			type: String,
			required: false
		},
		rootClass: {
			type: String,
			required: false
		}
	},
	emits: [
		"search",
		"pressEnter",
		"clear",
		"change",
		"blur",
		"focus",
		"keydown",
		"keyup",
		"compositionstart",
		"compositionend",
		"update:value"
	],
	name: "AInputSearch",
	inheritAttrs: false
});
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/input/index.js
var CompoundedInput = Input_default;
CompoundedInput.Search = Search_default;
CompoundedInput.TextArea = TextArea_default;
CompoundedInput.Password = Password_default;
CompoundedInput.OTP = OTP_default;
CompoundedInput.Group = Group_default;
CompoundedInput.install = (app) => {
	app.component(Input_default.name, CompoundedInput);
	app.component(Search_default.name, Search_default);
	app.component(TextArea_default.name, TextArea_default);
	app.component(Password_default.name, Password_default);
	app.component(OTP_default.name, OTP_default);
	app.component(Group_default.name, Group_default);
	return app;
};
var input_default = CompoundedInput;
var InputGroup = Group_default;
var InputOTP = OTP_default;
var InputPassword = Password_default;
var InputSearch = Search_default;
//#endregion
export { input_default as a, InputSearch as i, InputOTP as n, InputPassword as r, InputGroup as t };

//# sourceMappingURL=input-MqJYJz01.js.map