import { Bt as onUnmounted, Et as mergeDefaults, H as cloneVNode, It as onMounted, Mt as onBeforeUnmount, N as Fragment, Ot as mergeProps, St as inject, Tt as isVNode, W as computed, Wt as provide, Xn as shallowRef, _n as watch, kt as nextTick, nr as triggerRef, qn as ref, rt as defineComponent, t as Transition, tt as createVNode, vn as watchEffect, w as vShow, wn as withDirectives } from "./runtime-dom.esm-bundler-BEW9uiVc.js";
import "./vue.js";
import { _ as toPropsRefs, c as clsx, f as getAttrStyleAndClass, o as useId_default, s as classNames, u as filterEmpty, v as omit, w as warning_default, x as KeyCode_default, y as raf_default } from "./dist-B3YnmQlL.js";
import { $ as useComponentConfig, I as Keyframes_default, M as unit, Q as useComponentBaseConfig, S as textEllipsis, at as toPropsRefs$1, b as resetComponent, c as useToArr, f as genCssVar, it as getSlotPropsFnRun, k as merge, l as useToProps, o as useMergeSemantic, p as genStyleHooks, t as getAttrStyleAndClass$1, u as omit$1 } from "./useMergeSemantic-NT6cWlpu.js";
import { s as getTransitionProps } from "./useEvent-9fIYS1a5.js";
import { r as src_default$1 } from "./UniqueProvider-Fh_gWjsn.js";
import { n as getDOM } from "./findDOMNode-HhpMFZPk.js";
import { t as useSize } from "./useSize-C23Evsd5.js";
import { t as devUseWarning } from "./warning-DVCi2y10.js";
import { t as useCSSVarCls_default } from "./useCSSVarCls-C_79E43w.js";
import { n as PurePanel_default, t as src_default$2 } from "./dist-CsfrhiVf2.js";
import { n as useDisabledContext } from "./DisabledContext-CNdo8kjf.js";
import { t as toArray } from "./toArray-bRjLGJqR.js";
import { t as DefaultRenderEmpty } from "./defaultRenderEmpty-C1sdinl8.js";
import { n as getStatusClassNames, t as getMergedStatus } from "./statusUtils-DpKVCk3N.js";
import { s as useFormItemInputContext } from "./context-CoFN053h.js";
import { t as useVariant } from "./useVariant-DTfcCpUL.js";
import { c as genBorderlessStyle, d as genOutlinedStyle, f as genUnderlinedStyle, i as genPlaceholderStyle, l as genDisabledStyle, m as initInputToken, p as initComponentToken, t as genBasicInputStyle, u as genFilledStyle } from "./style-g373ECLL.js";
import { a as getAllowClear_default, r as BaseInput } from "./dist-Cs6Ew2fg.js";
import { t as src_default$3 } from "./dist-DAVlwEqg.js";
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/context/PathContext.js
var EmptyList = [];
var PathRegisterContextKey = Symbol("PathRegisterContext");
function useMeasure() {
	return inject(PathRegisterContextKey, null);
}
function useMeasureProvider(context) {
	provide(PathRegisterContextKey, context);
}
var MeasureProvider = /* @__PURE__ */ defineComponent((props, { slots }) => {
	useMeasureProvider(props);
	return () => {
		return slots?.default?.();
	};
}, { props: {
	registerPath: {
		type: Function,
		required: true
	},
	unregisterPath: {
		type: Function,
		required: true
	}
} });
var PathTrackerContextKey = Symbol("PathTrackerContext");
function useFullPath(eventKey) {
	const parentKeyPath = inject(PathTrackerContextKey, ref(EmptyList));
	return computed(() => {
		return eventKey !== void 0 ? [...parentKeyPath.value, eventKey.value] : parentKeyPath.value;
	});
}
var PathTrackerProvider = /* @__PURE__ */ defineComponent((props, { slots }) => {
	provide(PathTrackerContextKey, computed(() => props.value));
	return () => {
		return slots?.default?.();
	};
}, { props: { value: {
	type: Array,
	required: true
} } });
var PathUserContextKey = Symbol("PathUserContext");
function usePathUserContextProvider(context) {
	provide(PathUserContextKey, context);
}
function usePathUserContext() {
	return inject(PathUserContextKey, { isSubPathKey: () => false });
}
var PathUserProvider = /* @__PURE__ */ defineComponent((props, { slots }) => {
	usePathUserContextProvider(props);
	return () => {
		return slots?.default?.();
	};
}, { props: { isSubPathKey: {
	type: Function,
	required: true
} } });
var PathTrackerContext = { Provider: PathTrackerProvider };
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/context/MenuContext.js
var MenuContextKey = Symbol("MenuContextKey");
function mergeProps$1(origin, target) {
	const clone = { ...origin };
	Object.keys(target).forEach((key) => {
		const value = target[key];
		if (value !== void 0) clone[key] = value;
	});
	return clone;
}
function useMenuContext() {
	return inject(MenuContextKey, null);
}
function useMenuContextProvider(context) {
	provide(MenuContextKey, context);
}
var InheritableContextProvider = /* @__PURE__ */ defineComponent((props, { slots }) => {
	const context = useMenuContext();
	useMenuContextProvider(computed(() => {
		return mergeProps$1(context?.value ?? {}, omit(props, ["locked"]));
	}));
	return () => {
		return slots?.default?.();
	};
}, {
	props: {
		locked: {
			type: Boolean,
			required: false,
			default: void 0
		},
		prefixCls: {
			type: String,
			required: false,
			default: void 0
		},
		classes: {
			type: Object,
			required: false,
			default: void 0
		},
		styles: {
			type: Object,
			required: false,
			default: void 0
		},
		rootClass: {
			type: String,
			required: false,
			default: void 0
		},
		openKeys: {
			type: Array,
			required: false,
			default: void 0
		},
		rtl: {
			type: Boolean,
			required: false,
			default: void 0
		},
		mode: {
			type: String,
			required: false,
			default: void 0
		},
		disabled: {
			type: Boolean,
			required: false,
			default: void 0
		},
		overflowDisabled: {
			type: Boolean,
			required: false,
			default: void 0
		},
		activeKey: {
			type: String,
			required: false,
			default: void 0
		},
		onActive: {
			type: Function,
			required: false,
			default: void 0
		},
		onInactive: {
			type: Function,
			required: false,
			default: void 0
		},
		selectedKeys: {
			type: Array,
			required: false,
			default: void 0
		},
		inlineIndent: {
			type: Number,
			required: false,
			default: void 0
		},
		motion: {
			type: Object,
			required: false,
			default: void 0
		},
		defaultMotions: {
			type: Object,
			required: false,
			default: void 0
		},
		subMenuOpenDelay: {
			type: Number,
			required: false,
			default: void 0
		},
		subMenuCloseDelay: {
			type: Number,
			required: false,
			default: void 0
		},
		forceSubMenuRender: {
			type: Boolean,
			required: false,
			default: void 0
		},
		builtinPlacements: {
			type: Object,
			required: false,
			default: void 0
		},
		triggerSubMenuAction: {
			type: String,
			required: false,
			default: void 0
		},
		popupRender: {
			type: Function,
			required: false,
			default: void 0
		},
		itemIcon: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		expandIcon: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		onItemClick: {
			type: Function,
			required: false,
			default: void 0
		},
		onOpenChange: {
			type: Function,
			required: false,
			default: void 0
		},
		getPopupContainer: {
			type: Function,
			required: false,
			default: void 0
		}
	},
	inheritAttrs: false
});
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/Divider.js
var Divider = /* @__PURE__ */ defineComponent((props) => {
	const menuContext = useMenuContext();
	const measure = useMeasure();
	return () => {
		const { prefixCls } = menuContext?.value ?? {};
		if (measure) return null;
		return createVNode("li", {
			"role": "separator",
			"class": clsx(`${prefixCls}-item-divider`, props.class),
			"style": props.style
		}, null);
	};
}, {
	props: {
		style: {
			type: Object,
			required: false,
			default: void 0
		},
		class: {
			type: String,
			required: false,
			default: void 0
		}
	},
	name: "Divider"
});
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/Icon.js
var Icon = /* @__PURE__ */ defineComponent((props, { slots }) => {
	return () => {
		const { icon, props: iconProps } = props;
		const children = slots.default?.();
		let iconNode;
		if (icon === null || icon === false) return null;
		if (typeof icon === "function") {
			const childIcons = icon(iconProps);
			if (!childIcons) {
				iconNode = null;
				return children;
			}
			iconNode = filterEmpty(childIcons ? Array.isArray(childIcons) ? childIcons : [childIcons] : []);
		} else if (typeof icon !== "boolean") iconNode = icon;
		return iconNode || children || null;
	};
}, { props: {
	icon: {
		type: [
			Object,
			Function,
			String,
			Number,
			null,
			Boolean,
			Array
		],
		required: false,
		default: void 0
	},
	props: {
		type: Object,
		required: false,
		default: void 0
	}
} });
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/context/IdContext.js
var IdContextKey = Symbol("IdContext");
function useIdContextProvide(id) {
	provide(IdContextKey, id);
}
function getMenuId(uuid, eventKey) {
	return `${uuid}-${eventKey}`;
}
/**
* Get `data-menu-id`
*/
function useMenuId(eventKey) {
	const id = inject(IdContextKey, ref(""));
	return computed(() => getMenuId(id.value, eventKey.value));
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/context/PrivateContext.js
var PrivateContextKey = Symbol("PrivateContext");
function usePrivateProvider(context) {
	provide(PrivateContextKey, context);
}
function usePrivateContext() {
	return inject(PrivateContextKey, {});
}
var PrivateContextProvider = defineComponent((props, { slots }) => {
	usePrivateProvider(props);
	return () => {
		return slots?.default?.();
	};
}, { props: ["_internalRenderMenuItem", "_internalRenderSubMenuItem"] });
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/hooks/useActive.js
function useActive(eventKey, disabled, onMouseEnter, onMouseLeave) {
	const menuContext = useMenuContext();
	const ret = { active: computed(() => menuContext?.value?.activeKey === eventKey.value) };
	watchEffect(() => {
		if (!disabled.value) {
			ret.onMouseEnter = (domEvent) => {
				onMouseEnter?.({
					key: eventKey.value,
					domEvent
				});
				menuContext?.value?.onActive?.(eventKey.value);
			};
			ret.onMouseLeave = (domEvent) => {
				onMouseLeave?.({
					key: eventKey.value,
					domEvent
				});
				menuContext?.value?.onInactive?.(eventKey.value);
			};
		}
	});
	return ret;
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/hooks/useDirectionStyle.js
function useDirectionStyle(level) {
	const menuContext = useMenuContext();
	return computed(() => {
		const { mode, rtl, inlineIndent } = menuContext?.value ?? {};
		if (mode !== "inline") return null;
		const len = level.value;
		return rtl ? { paddingRight: `${len * inlineIndent}px` } : { paddingLeft: `${len * inlineIndent}px` };
	});
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/utils/warnUtil.js
/**
* `onClick` event return `info.item` which point to react node directly.
* We should warning this since it will not work on FC.
*/
function warnItemProp({ item, ...restInfo }) {
	Object.defineProperty(restInfo, "item", { get: () => {
		warning_default(false, "`info.item` is deprecated since we will move to function component that not provides React Node instance in future.");
		return item;
	} });
	return restInfo;
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/MenuItem.js
var LegacyMenuItem = /* @__PURE__ */ defineComponent((props, { slots, attrs }) => {
	return () => {
		const { title, attribute, ...restProps } = attrs;
		const { elementRef } = props;
		const passedProps = omit(restProps, [
			"eventKey",
			"popupClassName",
			"popupOffset",
			"onTitleClick"
		]);
		warning_default(!attribute, "`attribute` of Menu.Item is deprecated. Please pass attribute directly.");
		return createVNode(src_default$2.Item, mergeProps(attribute, { "title": typeof title === "string" ? title : void 0 }, passedProps, { "ref": elementRef }), { default: () => [slots?.default?.()] });
	};
}, {
	props: { elementRef: {
		required: false,
		default: void 0
	} },
	name: "LegacyMenuItem",
	inheritAttrs: false
});
/**
* Real Menu Item component
*/
var InternalMenuItem = /* @__PURE__ */ defineComponent((props, { slots, attrs }) => {
	const { eventKey } = toPropsRefs(props, "eventKey");
	const domDataId = useMenuId(eventKey);
	const menuContext = useMenuContext();
	const privateContext = usePrivateContext();
	const legacyMenuItemRef = shallowRef();
	const elementRef = shallowRef();
	const mergedDisabled = computed(() => props.disabled ?? menuContext?.value?.disabled);
	const connectedKeys = useFullPath(eventKey);
	if (props.warnKey) warning_default(false, "MenuItem should not leave undefined `key`.");
	const getEventInfo = (e) => {
		return {
			key: eventKey.value,
			keyPath: connectedKeys.value,
			item: legacyMenuItemRef.value,
			domEvent: e
		};
	};
	const ret = useActive(eventKey, mergedDisabled, props?.onMouseEnter, props?.onMouseLeave);
	const active = ret.active;
	const selected = computed(() => !!menuContext?.value?.selectedKeys?.includes?.(eventKey.value));
	const directionStyle = useDirectionStyle(computed(() => connectedKeys.value.length));
	const onInternalClick = (e) => {
		if (mergedDisabled.value) return;
		const info = getEventInfo(e);
		props?.onClick?.(warnItemProp(info));
		menuContext?.value?.onItemClick?.(info);
	};
	const onInternalKeyDown = (e) => {
		props?.onKeyDown?.(e);
		if (e.which === KeyCode_default.ENTER) {
			const info = getEventInfo(e);
			props?.onClick?.(warnItemProp(info));
			menuContext?.value?.onItemClick?.(info);
		}
	};
	/**
	* Used for accessibility. Helper will focus element without key board.
	* We should manually trigger an active
	*/
	const onInternalFocus = (e) => {
		menuContext?.value?.onActive?.(eventKey.value);
		props?.onFocus?.(e);
	};
	return () => {
		const { role, disabled, itemIcon, ...restProps } = props;
		const optionRoleProps = {};
		if (role === "option") optionRoleProps["aria-selected"] = selected.value;
		const { prefixCls, overflowDisabled, itemIcon: contextItemIcon } = menuContext?.value ?? {};
		const mergedItemIcon = itemIcon || contextItemIcon;
		const itemCls = `${prefixCls}-item`;
		const activeProps = {
			onMouseenter: ret.onMouseEnter,
			onMouseleave: ret.onMouseLeave
		};
		let renderNode = createVNode(LegacyMenuItem, mergeProps({
			"ref": legacyMenuItemRef,
			"elementRef": elementRef,
			"role": role === null ? "none" : role || "menuitem",
			"tabIndex": disabled ? null : -1,
			"data-menu-id": overflowDisabled && domDataId.value ? null : domDataId.value
		}, omit({
			...restProps,
			...attrs
		}, ["extra"]), activeProps, optionRoleProps, {
			"component": "li",
			"aria-disabled": disabled,
			"style": [directionStyle.value, props?.style],
			"className": clsx(itemCls, {
				[`${itemCls}-active`]: active.value,
				[`${itemCls}-selected`]: selected.value,
				[`${itemCls}-disabled`]: mergedDisabled.value
			}, props.class),
			"onClick": onInternalClick,
			"onKeydown": onInternalKeyDown,
			"onFocus": onInternalFocus
		}), { default: () => [slots?.default?.(), createVNode(Icon, {
			"props": {
				...props,
				isSelected: selected.value
			},
			"icon": mergedItemIcon
		}, null)] });
		if (privateContext._internalRenderMenuItem) renderNode = privateContext._internalRenderMenuItem(renderNode, props, { selected: selected.value });
		return renderNode;
	};
}, {
	props: {
		eventKey: {
			type: String,
			required: false,
			default: void 0
		},
		warnKey: {
			type: Boolean,
			required: false,
			default: void 0
		},
		attribute: {
			type: Object,
			required: false,
			default: void 0
		},
		onKeyDown: {
			type: Function,
			required: false,
			default: void 0
		},
		onFocus: {
			type: Function,
			required: false,
			default: void 0
		},
		role: {
			type: String,
			required: false,
			default: void 0
		},
		type: {
			type: String,
			required: false,
			default: void 0
		},
		disabled: {
			type: Boolean,
			required: false,
			default: void 0
		},
		itemIcon: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		extra: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		onMouseEnter: {
			type: Function,
			required: false,
			default: void 0
		},
		onMouseLeave: {
			type: Function,
			required: false,
			default: void 0
		},
		onClick: {
			type: Function,
			required: false,
			default: void 0
		},
		style: {
			type: Object,
			required: false,
			default: void 0
		},
		class: {
			type: String,
			required: false,
			default: void 0
		}
	},
	name: "InternalMenuItem",
	inheritAttrs: false
});
var MenuItem = /* @__PURE__ */ defineComponent((props, { slots, attrs }) => {
	const { eventKey } = toPropsRefs(props, "eventKey");
	const measure = useMeasure();
	const connectedKeyPath = useFullPath(eventKey);
	watch([connectedKeyPath], (_n, _o, onCleanup) => {
		if (measure) measure.registerPath(eventKey.value, connectedKeyPath.value);
		onCleanup(() => {
			measure?.unregisterPath(eventKey.value, connectedKeyPath.value);
		});
	}, { immediate: true });
	return () => {
		if (measure) return null;
		return createVNode(InternalMenuItem, {
			...attrs,
			...props
		}, slots);
	};
}, {
	props: {
		eventKey: {
			type: String,
			required: false,
			default: void 0
		},
		warnKey: {
			type: Boolean,
			required: false,
			default: void 0
		},
		attribute: {
			type: Object,
			required: false,
			default: void 0
		},
		onKeyDown: {
			type: Function,
			required: false,
			default: void 0
		},
		onFocus: {
			type: Function,
			required: false,
			default: void 0
		},
		role: {
			type: String,
			required: false,
			default: void 0
		},
		type: {
			type: String,
			required: false,
			default: void 0
		},
		disabled: {
			type: Boolean,
			required: false,
			default: void 0
		},
		itemIcon: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		extra: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		onMouseEnter: {
			type: Function,
			required: false,
			default: void 0
		},
		onMouseLeave: {
			type: Function,
			required: false,
			default: void 0
		},
		onClick: {
			type: Function,
			required: false,
			default: void 0
		},
		style: {
			type: Object,
			required: false,
			default: void 0
		},
		class: {
			type: String,
			required: false,
			default: void 0
		}
	},
	name: "MenuItem",
	inheritAttrs: false
});
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/hooks/useMemoCallback.js
/**
* Memoize a callback function to keep reference stable
*/
function useMemoCallback(callback) {
	const fnRef = ref(callback);
	fnRef.value = callback;
	const memoFn = ((...args) => {
		return fnRef.value(...args);
	});
	return memoFn;
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/utils/commonUtil.js
function parseChildren(children, keyPath) {
	return toArray(children).map((child, index) => {
		if (isVNode(child)) {
			const key = child.key;
			let eventKey = child.props?.eventKey ?? key;
			const emptyKey = eventKey === null || eventKey === void 0;
			if (emptyKey) eventKey = `tmp_key-${[...keyPath, index].join("-")}`;
			const cloneProps = {
				key: eventKey,
				eventKey
			};
			if (emptyKey) cloneProps.warnKey = true;
			return cloneVNode(child, { ...cloneProps });
		}
		return child;
	});
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/SubMenu/SubMenuList.js
var InternalSubMenuList = /* @__PURE__ */ defineComponent((_, { attrs, slots }) => {
	const menuContext = useMenuContext();
	return () => {
		const { prefixCls, mode, rtl } = menuContext?.value ?? {};
		return createVNode("ul", mergeProps({
			"class": clsx(prefixCls, !!rtl && `${prefixCls}-rtl`, `${prefixCls}-sub`, `${prefixCls}-${mode === "inline" ? "inline" : "vertical"}`),
			"role": "menu"
		}, attrs, { "data-menu-list": true }), [slots?.default?.()]);
	};
}, {
	name: "InlineSubMenuList",
	inheritAttrs: false
});
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/utils/motionUtil.js
function getMotion(mode, motion, defaultMotions) {
	if (motion) return motion;
	if (defaultMotions) return defaultMotions?.[mode] || defaultMotions?.other;
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/SubMenu/InlineSubMenuList.js
var InlineSubMenuList = /* @__PURE__ */ defineComponent((props, { slots }) => {
	const fixedMode = "inline";
	const menuContext = useMenuContext();
	const sameModeRef = shallowRef(false);
	watchEffect(() => {
		sameModeRef.value = menuContext?.value?.mode === fixedMode;
	});
	const destroy = shallowRef(!sameModeRef.value);
	watch(() => menuContext?.value?.mode, () => {
		if (sameModeRef.value) destroy.value = false;
	}, { immediate: true });
	const mergedOpen = computed(() => sameModeRef.value ? props?.open : false);
	const mergedMotion = computed(() => {
		const { motion, defaultMotions } = menuContext?.value ?? {};
		const motionData = { ...getMotion(fixedMode, motion, defaultMotions) };
		if (props.keyPath && props.keyPath.length > 1) motionData.appear = false;
		const _onAfterLeave = motionData.onAfterLeave;
		motionData.onAfterLeave = (el) => {
			if (!sameModeRef.value) destroy.value = true;
			return _onAfterLeave?.(el);
		};
		return motionData;
	});
	return () => {
		if (destroy.value) return null;
		return createVNode(InheritableContextProvider, {
			"mode": fixedMode,
			"locked": !sameModeRef.value
		}, { default: () => [createVNode(Transition, getTransitionProps(mergedMotion.value?.name, mergedMotion.value), { default: () => [withDirectives(createVNode(InternalSubMenuList, { "id": props.id }, { default: () => [slots?.default?.()] }), [[vShow, mergedOpen.value]])] })] });
	};
}, {
	props: {
		id: {
			type: String,
			required: false,
			default: void 0
		},
		open: {
			type: Boolean,
			required: true
		},
		keyPath: {
			type: Array,
			required: true
		}
	},
	name: "InlineSubMenuList",
	inheritAttrs: false
});
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/placements.js
var autoAdjustOverflow = {
	adjustX: 1,
	adjustY: 1
};
var placements = {
	topLeft: {
		points: ["bl", "tl"],
		overflow: autoAdjustOverflow
	},
	topRight: {
		points: ["br", "tr"],
		overflow: autoAdjustOverflow
	},
	bottomLeft: {
		points: ["tl", "bl"],
		overflow: autoAdjustOverflow
	},
	bottomRight: {
		points: ["tr", "br"],
		overflow: autoAdjustOverflow
	},
	leftTop: {
		points: ["tr", "tl"],
		overflow: autoAdjustOverflow
	},
	leftBottom: {
		points: ["br", "bl"],
		overflow: autoAdjustOverflow
	},
	rightTop: {
		points: ["tl", "tr"],
		overflow: autoAdjustOverflow
	},
	rightBottom: {
		points: ["bl", "br"],
		overflow: autoAdjustOverflow
	}
};
var placementsRtl = {
	topLeft: {
		points: ["bl", "tl"],
		overflow: autoAdjustOverflow
	},
	topRight: {
		points: ["br", "tr"],
		overflow: autoAdjustOverflow
	},
	bottomLeft: {
		points: ["tl", "bl"],
		overflow: autoAdjustOverflow
	},
	bottomRight: {
		points: ["tr", "br"],
		overflow: autoAdjustOverflow
	},
	rightTop: {
		points: ["tr", "tl"],
		overflow: autoAdjustOverflow
	},
	rightBottom: {
		points: ["br", "bl"],
		overflow: autoAdjustOverflow
	},
	leftTop: {
		points: ["tl", "tr"],
		overflow: autoAdjustOverflow
	},
	leftBottom: {
		points: ["bl", "br"],
		overflow: autoAdjustOverflow
	}
};
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/SubMenu/PopupTrigger.js
var popupPlacementMap = {
	"horizontal": "bottomLeft",
	"vertical": "rightTop",
	"vertical-left": "rightTop",
	"vertical-right": "leftTop"
};
var PopupTrigger = /* @__PURE__ */ defineComponent((props, { slots }) => {
	const menuContext = useMenuContext();
	const innerVisible = shallowRef(props.visible ?? false);
	const placement = computed(() => {
		const rtl = menuContext?.value?.rtl;
		const builtinPlacements = menuContext?.value?.builtinPlacements;
		return rtl ? {
			...placementsRtl,
			...builtinPlacements
		} : {
			...placements,
			...builtinPlacements
		};
	});
	const triggerMode = computed(() => props.mode);
	const popupPlacement = computed(() => {
		return popupPlacementMap[triggerMode.value];
	});
	const defaultMotions = computed(() => menuContext?.value?.defaultMotions);
	const motion = computed(() => menuContext?.value?.motion);
	const targetMotion = computed(() => {
		return { ...getMotion(triggerMode.value, motion.value, defaultMotions.value) };
	});
	const targetMotionRef = shallowRef(targetMotion.value);
	watch(triggerMode, (mode) => {
		if (mode !== "inline")
 /**
		* PopupTrigger is only used for vertical and horizontal types.
		* When collapsed is unfolded, the inline animation will destroy the vertical animation.
		*/
		targetMotionRef.value = targetMotion.value;
	}, { immediate: true });
	watch([motion, defaultMotions], () => {
		if (triggerMode.value !== "inline") targetMotionRef.value = targetMotion.value;
	});
	const mergedMotion = computed(() => {
		return {
			...targetMotionRef.value,
			appear: true
		};
	});
	const visibleRef = shallowRef();
	watch(() => props.visible, (visible, _, onCleanup) => {
		visibleRef.value = raf_default(() => {
			innerVisible.value = visible;
		});
		onCleanup(() => {
			if (visibleRef.value !== void 0) raf_default.cancel(visibleRef.value);
		});
	});
	return () => {
		const { popupClassName, popup, popupStyle, popupOffset, disabled, onVisibleChange, prefixCls } = props;
		const { rtl, rootClass, mode, getPopupContainer, triggerSubMenuAction, subMenuCloseDelay, subMenuOpenDelay, forceSubMenuRender } = menuContext?.value ?? {};
		return createVNode(src_default$1, {
			"prefixCls": prefixCls,
			"popupClassName": clsx(`${prefixCls}-popup`, { [`${prefixCls}-rtl`]: rtl }, popupClassName, rootClass),
			"stretch": mode === "horizontal" ? "minWidth" : void 0,
			"getPopupContainer": getPopupContainer,
			"builtinPlacements": placement.value,
			"popupPlacement": popupPlacement.value,
			"popupVisible": innerVisible.value,
			"popup": popup,
			"popupStyle": popupStyle,
			"popupAlign": popupOffset && { offset: popupOffset },
			"action": disabled ? [] : [triggerSubMenuAction],
			"mouseEnterDelay": subMenuOpenDelay,
			"mouseLeaveDelay": subMenuCloseDelay,
			"onOpenChange": onVisibleChange,
			"forceRender": forceSubMenuRender,
			"popupMotion": mergedMotion.value,
			"fresh": true
		}, { default: () => [slots?.default?.()] });
	};
}, { props: {
	prefixCls: {
		type: String,
		required: true
	},
	mode: {
		type: String,
		required: true
	},
	visible: {
		type: Boolean,
		required: true
	},
	popup: {
		type: [
			Object,
			Function,
			String,
			Number,
			null,
			Boolean,
			Array
		],
		required: true
	},
	popupStyle: {
		type: Object,
		required: false,
		default: void 0
	},
	popupClassName: {
		type: String,
		required: false,
		default: void 0
	},
	popupOffset: {
		type: Array,
		required: false,
		default: void 0
	},
	disabled: {
		type: Boolean,
		required: true
	},
	onVisibleChange: {
		type: Function,
		required: true
	}
} });
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/SubMenu/index.js
function _isSlot$5(s) {
	return typeof s === "function" || Object.prototype.toString.call(s) === "[object Object]" && !isVNode(s);
}
var InternalSubMenu = /* @__PURE__ */ defineComponent((props, { slots, attrs }) => {
	const domDataId = useMenuId(computed(() => props?.eventKey || ""));
	const menuContext = useMenuContext();
	const { _internalRenderSubMenuItem } = usePrivateContext();
	const pathUserContext = usePathUserContext();
	const connectedPath = useFullPath();
	const validConnectedPath = computed(() => connectedPath.value.filter((k) => k !== void 0));
	const prefixCls = computed(() => menuContext?.value?.prefixCls || "vc-menu");
	const mode = computed(() => menuContext?.value?.mode || "vertical");
	const openKeys = computed(() => menuContext?.value?.openKeys || []);
	const contextDisabled = computed(() => menuContext?.value?.disabled);
	const overflowDisabled = computed(() => menuContext?.value?.overflowDisabled);
	const activeKey = computed(() => menuContext?.value?.activeKey);
	const selectedKeys = computed(() => menuContext?.value?.selectedKeys || []);
	const contextExpandIcon = computed(() => menuContext?.value?.expandIcon);
	const contextPopupRender = computed(() => menuContext?.value?.popupRender);
	const onOpenChange = (key, open) => {
		menuContext?.value?.onOpenChange?.(key, open);
	};
	const onActive = (key) => {
		menuContext?.value?.onActive?.(key);
	};
	const subMenuPrefixCls = computed(() => `${prefixCls.value}-submenu`);
	const mergedDisabled = computed(() => !!(contextDisabled.value || props?.disabled));
	if (props?.warnKey) warning_default(false, "SubMenu should not leave undefined `key`.");
	const mergedItemIcon = computed(() => props?.itemIcon ?? menuContext?.value?.itemIcon);
	const mergedExpandIcon = computed(() => props?.expandIcon ?? contextExpandIcon.value);
	const originOpen = computed(() => {
		const key = props?.eventKey;
		return key ? openKeys.value.includes(key) : false;
	});
	const open = computed(() => !overflowDisabled.value && originOpen.value);
	const childrenSelected = computed(() => {
		const key = props?.eventKey;
		return key ? pathUserContext.isSubPathKey(selectedKeys.value, key) : false;
	});
	const { active, ...activeProps } = useActive(computed(() => props?.eventKey || ""), mergedDisabled, (e) => props?.onTitleMouseEnter?.(e), (e) => props?.onTitleMouseLeave?.(e));
	const childrenActive = ref(false);
	const triggerChildrenActive = (newActive) => {
		if (!mergedDisabled.value) childrenActive.value = newActive;
	};
	const onInternalMouseEnter = (domEvent) => {
		triggerChildrenActive(true);
		props?.onMouseEnter?.({
			key: props.eventKey,
			domEvent
		});
	};
	const onInternalMouseLeave = (domEvent) => {
		triggerChildrenActive(false);
		props?.onMouseLeave?.({
			key: props.eventKey,
			domEvent
		});
	};
	const mergedActive = computed(() => {
		if (active.value) return active.value;
		if (mode.value !== "inline") {
			const key = props?.eventKey;
			const currentActiveKey = activeKey.value;
			return childrenActive.value || (key && currentActiveKey ? pathUserContext.isSubPathKey([currentActiveKey], key) : false);
		}
		return false;
	});
	const directionStyle = useDirectionStyle(computed(() => connectedPath.value.length));
	const onInternalTitleClick = (e) => {
		if (mergedDisabled.value) return;
		const key = props?.eventKey;
		props?.onTitleClick?.({
			key,
			domEvent: e
		});
		if (mode.value === "inline" && key) onOpenChange(key, !originOpen.value);
	};
	const onMergedItemClick = useMemoCallback((info) => {
		props?.onClick?.(warnItemProp(info));
		menuContext?.value?.onItemClick?.(info);
	});
	const onPopupVisibleChange = (newVisible) => {
		const key = props?.eventKey;
		if (mode.value !== "inline" && key) onOpenChange(key, newVisible);
	};
	const onInternalFocus = () => {
		const key = props?.eventKey;
		if (key) onActive(key);
	};
	const triggerModeRef = ref(mode.value);
	watch(mode, () => {
		if (mode.value !== "inline" && validConnectedPath.value.length > 1) triggerModeRef.value = "vertical";
		else triggerModeRef.value = mode.value;
	}, { immediate: true });
	return () => {
		const { style, title, class: className, popupClassName, popupOffset, popupStyle, classes, styles, ...restProps } = props;
		const children = slots.default?.();
		const popupId = domDataId.value && `${domDataId.value}-popup`;
		const expandIconProps = {
			isOpen: open.value,
			isSelected: childrenSelected.value,
			isSubMenu: true,
			disabled: mergedDisabled.value
		};
		const expandIconNode = createVNode(Icon, {
			"icon": mode.value !== "horizontal" ? mergedExpandIcon.value : void 0,
			"props": {
				...props,
				...expandIconProps
			}
		}, { default: () => [createVNode("i", { "class": `${subMenuPrefixCls.value}-arrow` }, null)] });
		let titleNode = createVNode("div", mergeProps({
			"role": "menuitem",
			"style": directionStyle.value,
			"class": `${subMenuPrefixCls.value}-title`,
			"tabindex": mergedDisabled.value ? void 0 : -1,
			"title": typeof title === "string" ? title : void 0,
			"data-menu-id": overflowDisabled.value && domDataId.value ? void 0 : domDataId.value,
			"aria-expanded": open.value,
			"aria-haspopup": true,
			"aria-controls": popupId,
			"aria-disabled": mergedDisabled.value,
			"onClick": onInternalTitleClick,
			"onFocus": onInternalFocus
		}, activeProps), [title, expandIconNode]);
		const popupContentTriggerMode = triggerModeRef.value;
		const renderPopupContent = () => {
			const originNode = createVNode(InheritableContextProvider, {
				"classes": classes,
				"styles": styles,
				"mode": popupContentTriggerMode === "horizontal" ? "vertical" : popupContentTriggerMode
			}, { default: () => [createVNode(InternalSubMenuList, { "id": popupId }, _isSlot$5(children) ? children : { default: () => [children] })] });
			const mergedPopupRender = props?.popupRender || contextPopupRender.value;
			if (mergedPopupRender) return mergedPopupRender(originNode, {
				item: props,
				keys: validConnectedPath.value
			});
			return originNode;
		};
		if (!overflowDisabled.value) {
			const triggerMode = triggerModeRef.value;
			const _titleNode = function() {
				return titleNode;
			}();
			titleNode = createVNode(PopupTrigger, {
				"mode": triggerMode,
				"prefixCls": subMenuPrefixCls.value,
				"visible": !props?.internalPopupClose && open.value && mode.value !== "inline",
				"popupClassName": popupClassName,
				"popupOffset": popupOffset,
				"popupStyle": popupStyle,
				"popup": renderPopupContent(),
				"disabled": mergedDisabled.value,
				"onVisibleChange": onPopupVisibleChange
			}, _isSlot$5(titleNode) ? titleNode : { default: () => [_titleNode] });
		}
		let listNode = createVNode(src_default$2.Item, mergeProps({ "role": "none" }, attrs, restProps, {
			"component": "li",
			"style": style,
			"class": classNames(subMenuPrefixCls.value, `${subMenuPrefixCls.value}-${mode.value}`, className, {
				[`${subMenuPrefixCls.value}-open`]: open.value,
				[`${subMenuPrefixCls.value}-active`]: mergedActive.value,
				[`${subMenuPrefixCls.value}-selected`]: childrenSelected.value,
				[`${subMenuPrefixCls.value}-disabled`]: mergedDisabled.value
			}),
			"onMouseenter": onInternalMouseEnter,
			"onMouseleave": onInternalMouseLeave
		}), { default: () => [titleNode, !overflowDisabled.value && createVNode(InlineSubMenuList, {
			"id": popupId,
			"open": open.value,
			"keyPath": validConnectedPath.value
		}, _isSlot$5(children) ? children : { default: () => [children] })] });
		if (_internalRenderSubMenuItem) listNode = _internalRenderSubMenuItem(listNode, props, {
			selected: childrenSelected.value,
			active: mergedActive.value,
			open: open.value,
			disabled: mergedDisabled.value
		});
		return createVNode(InheritableContextProvider, {
			"classes": classes,
			"styles": styles,
			"onItemClick": onMergedItemClick,
			"mode": mode.value === "horizontal" ? "vertical" : mode.value,
			"itemIcon": mergedItemIcon.value,
			"expandIcon": mergedExpandIcon.value
		}, _isSlot$5(listNode) ? listNode : { default: () => [listNode] });
	};
}, {
	props: {
		classes: {
			type: Object,
			required: false,
			default: void 0
		},
		styles: {
			type: Object,
			required: false,
			default: void 0
		},
		title: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		internalPopupClose: {
			type: Boolean,
			required: false,
			default: void 0
		},
		eventKey: {
			type: String,
			required: false,
			default: void 0
		},
		warnKey: {
			type: Boolean,
			required: false,
			default: void 0
		},
		type: {
			type: String,
			required: false,
			default: void 0
		},
		disabled: {
			type: Boolean,
			required: false,
			default: void 0
		},
		rootClass: {
			type: String,
			required: false,
			default: void 0
		},
		itemIcon: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		expandIcon: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		onMouseEnter: {
			type: Function,
			required: false,
			default: void 0
		},
		onMouseLeave: {
			type: Function,
			required: false,
			default: void 0
		},
		popupClassName: {
			type: String,
			required: false,
			default: void 0
		},
		popupOffset: {
			type: Array,
			required: false,
			default: void 0
		},
		popupStyle: {
			type: Object,
			required: false,
			default: void 0
		},
		popupRender: {
			type: Function,
			required: false,
			default: void 0
		},
		onClick: {
			type: Function,
			required: false,
			default: void 0
		},
		onTitleClick: {
			type: Function,
			required: false,
			default: void 0
		},
		onTitleMouseEnter: {
			type: Function,
			required: false,
			default: void 0
		},
		onTitleMouseLeave: {
			type: Function,
			required: false,
			default: void 0
		},
		style: {
			type: Object,
			required: false,
			default: void 0
		},
		class: {
			type: String,
			required: false,
			default: void 0
		}
	},
	name: "InternalSubMenu",
	inheritAttrs: false
});
var SubMenu = /* @__PURE__ */ defineComponent((props, { slots }) => {
	const eventKey = computed(() => props?.eventKey);
	const connectedKeyPath = useFullPath(eventKey);
	const measure = useMeasure();
	const validKeyPath = computed(() => connectedKeyPath.value.filter((k) => k !== void 0));
	watch([connectedKeyPath], (_n, _o, onCleanup) => {
		if (measure) measure.registerPath(eventKey.value, connectedKeyPath.value);
		onCleanup(() => {
			measure?.unregisterPath(eventKey.value, connectedKeyPath.value);
		});
	}, { immediate: true });
	return () => {
		const children = slots.default?.();
		const childList = parseChildren(children, validKeyPath.value);
		let renderNode;
		if (measure) renderNode = childList;
		else renderNode = createVNode(InternalSubMenu, props, _isSlot$5(childList) ? childList : { default: () => [childList] });
		return createVNode(PathTrackerContext.Provider, { "value": connectedKeyPath.value }, _isSlot$5(renderNode) ? renderNode : { default: () => [renderNode] });
	};
}, {
	props: {
		classes: {
			type: Object,
			required: false,
			default: void 0
		},
		styles: {
			type: Object,
			required: false,
			default: void 0
		},
		title: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		internalPopupClose: {
			type: Boolean,
			required: false,
			default: void 0
		},
		eventKey: {
			type: String,
			required: false,
			default: void 0
		},
		warnKey: {
			type: Boolean,
			required: false,
			default: void 0
		},
		type: {
			type: String,
			required: false,
			default: void 0
		},
		disabled: {
			type: Boolean,
			required: false,
			default: void 0
		},
		rootClass: {
			type: String,
			required: false,
			default: void 0
		},
		itemIcon: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		expandIcon: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		onMouseEnter: {
			type: Function,
			required: false,
			default: void 0
		},
		onMouseLeave: {
			type: Function,
			required: false,
			default: void 0
		},
		popupClassName: {
			type: String,
			required: false,
			default: void 0
		},
		popupOffset: {
			type: Array,
			required: false,
			default: void 0
		},
		popupStyle: {
			type: Object,
			required: false,
			default: void 0
		},
		popupRender: {
			type: Function,
			required: false,
			default: void 0
		},
		onClick: {
			type: Function,
			required: false,
			default: void 0
		},
		onTitleClick: {
			type: Function,
			required: false,
			default: void 0
		},
		onTitleMouseEnter: {
			type: Function,
			required: false,
			default: void 0
		},
		onTitleMouseLeave: {
			type: Function,
			required: false,
			default: void 0
		},
		style: {
			type: Object,
			required: false,
			default: void 0
		},
		class: {
			type: String,
			required: false,
			default: void 0
		}
	},
	name: "SubMenu",
	inheritAttrs: false
});
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/MenuItemGroup.js
function _isSlot$4(s) {
	return typeof s === "function" || Object.prototype.toString.call(s) === "[object Object]" && !isVNode(s);
}
var InternalMenuItemGroup = /* @__PURE__ */ defineComponent((props, { slots }) => {
	const context = useMenuContext();
	return () => {
		const { class: className, title, eventKey, ...restProps } = props;
		const { prefixCls, classes: menuClassNames, styles } = context?.value ?? {};
		const groupPrefixCls = `${prefixCls}-item-group`;
		return createVNode("li", mergeProps({ "role": "presentation" }, restProps, {
			"onClick": (e) => e.stopPropagation(),
			"class": clsx(groupPrefixCls, className)
		}), [createVNode("div", {
			"role": "presentation",
			"class": clsx(`${groupPrefixCls}-title`, menuClassNames?.listTitle),
			"style": styles?.listTitle,
			"title": typeof title === "string" ? title : void 0
		}, [title]), createVNode("ul", {
			"role": "group",
			"class": clsx(`${groupPrefixCls}-list`, menuClassNames?.list),
			"style": styles?.list
		}, [slots?.default ? slots.default() : null])]);
	};
}, {
	props: {
		title: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		eventKey: {
			type: String,
			required: false,
			default: void 0
		},
		warnKey: {
			type: Boolean,
			required: false,
			default: void 0
		},
		style: {
			type: Object,
			required: false,
			default: void 0
		},
		class: {
			type: String,
			required: false,
			default: void 0
		}
	},
	name: "InternalMenuItemGroup"
});
var MenuItemGroup = /* @__PURE__ */ defineComponent((props, ctx) => {
	const connectedKeyPath = useFullPath(computed(() => props.eventKey));
	const measure = useMeasure();
	return () => {
		const slots = ctx.slots;
		const childList = parseChildren(filterEmpty(slots.default ? slots.default() : []), connectedKeyPath.value);
		if (measure) return childList;
		return createVNode(InternalMenuItemGroup, omit(props, ["warnKey"]), _isSlot$4(childList) ? childList : { default: () => [childList] });
	};
}, {
	props: {
		title: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		eventKey: {
			type: String,
			required: false,
			default: void 0
		},
		warnKey: {
			type: Boolean,
			required: false,
			default: void 0
		},
		style: {
			type: Object,
			required: false,
			default: void 0
		},
		class: {
			type: String,
			required: false,
			default: void 0
		}
	},
	name: "MenuItemGroup",
	inheritAttrs: false
});
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/hooks/useAccessibility.js
var { LEFT, RIGHT, UP, DOWN, ENTER, ESC, HOME, END } = KeyCode_default;
var ArrowKeys = [
	UP,
	DOWN,
	LEFT,
	RIGHT
];
function getOffset(mode, isRootLevel, isRtl, which) {
	const prev = "prev";
	const next = "next";
	const children = "children";
	const parent = "parent";
	if (mode === "inline" && which === ENTER) return { inlineTrigger: true };
	const inline = {
		[UP]: prev,
		[DOWN]: next
	};
	const horizontal = {
		[LEFT]: isRtl ? next : prev,
		[RIGHT]: isRtl ? prev : next,
		[DOWN]: children,
		[ENTER]: children
	};
	const vertical = {
		[UP]: prev,
		[DOWN]: next,
		[ENTER]: children,
		[ESC]: parent,
		[LEFT]: isRtl ? children : parent,
		[RIGHT]: isRtl ? parent : children
	};
	switch ({
		inline,
		horizontal,
		vertical,
		inlineSub: inline,
		horizontalSub: vertical,
		verticalSub: vertical
	}[`${mode}${isRootLevel ? "" : "Sub"}`]?.[which]) {
		case prev: return {
			offset: -1,
			sibling: true
		};
		case next: return {
			offset: 1,
			sibling: true
		};
		case parent: return {
			offset: -1,
			sibling: false
		};
		case children: return {
			offset: 1,
			sibling: false
		};
		default: return null;
	}
}
function findContainerUL(element) {
	let current = element;
	while (current) {
		if (current.getAttribute("data-menu-list")) return current;
		current = current.parentElement;
	}
	return null;
}
/**
* Get focusable node list from container
*/
function getFocusNodeList(container, includeDisabled) {
	const selector = includeDisabled ? "[tabindex]" : "[tabindex]:not([tabindex=\"-1\"]):not([disabled]):not([aria-disabled=\"true\"])";
	return Array.from(container.querySelectorAll(selector));
}
/**
* Find focused element within element set provided
*/
function getFocusElement(activeElement, elements) {
	let current = activeElement || document.activeElement;
	while (current) {
		if (elements.has(current)) return current;
		current = current.parentElement;
	}
	return null;
}
/**
* Get focusable elements from the element set under provided container
*/
function getFocusableElements(container, elements) {
	if (!container) return [];
	return getFocusNodeList(container, true).filter((ele) => elements.has(ele));
}
function getNextFocusElement(parentQueryContainer, elements, focusMenuElement, offset = 1) {
	if (!parentQueryContainer) return null;
	const sameLevelFocusableMenuElementList = getFocusableElements(parentQueryContainer, elements);
	const count = sameLevelFocusableMenuElementList.length;
	let focusIndex = sameLevelFocusableMenuElementList.findIndex((ele) => focusMenuElement === ele);
	if (offset < 0) if (focusIndex === -1) focusIndex = count - 1;
	else focusIndex -= 1;
	else if (offset > 0) focusIndex += 1;
	focusIndex = (focusIndex + count) % count;
	return sameLevelFocusableMenuElementList[focusIndex];
}
function refreshElements(keys, id) {
	const elements = /* @__PURE__ */ new Set();
	const key2element = /* @__PURE__ */ new Map();
	const element2key = /* @__PURE__ */ new Map();
	keys.forEach((key) => {
		const element = document.querySelector(`[data-menu-id='${getMenuId(id, key)}']`);
		if (element) {
			elements.add(element);
			element2key.set(element, key);
			key2element.set(key, element);
		}
	});
	return {
		elements,
		key2element,
		element2key
	};
}
function useAccessibility(mode, activeKey, isRtl, id, containerRef, getKeys, getKeyPath, triggerActiveKey, triggerAccessibilityOpen, originOnKeyDown) {
	const rafRef = shallowRef();
	const activeRef = shallowRef();
	const cleanRaf = () => {
		if (rafRef.value !== void 0) cancelAnimationFrame(rafRef.value);
	};
	onBeforeUnmount(() => {
		cleanRaf();
	});
	return (e) => {
		activeRef.value = activeKey.value;
		const { which } = e;
		if ([
			...ArrowKeys,
			ENTER,
			ESC,
			HOME,
			END
		].includes(which)) {
			const keys = getKeys();
			let refreshedElements = refreshElements(keys, id);
			const { elements, key2element, element2key } = refreshedElements;
			const focusMenuElement = getFocusElement(key2element.get(activeKey.value) || null, elements);
			const focusMenuKey = element2key.get(focusMenuElement);
			const offsetObj = getOffset(mode.value, getKeyPath(focusMenuKey, true).length === 1, isRtl.value, which);
			if (!offsetObj && which !== HOME && which !== END) return;
			if (ArrowKeys.includes(which) || [HOME, END].includes(which)) e.preventDefault();
			const tryFocus = (menuElement) => {
				if (menuElement) {
					let focusTargetElement = menuElement;
					const link = menuElement.querySelector("a");
					if (link?.getAttribute("href")) focusTargetElement = link;
					const targetKey = element2key.get(menuElement);
					if (targetKey) triggerActiveKey(targetKey);
					/**
					* Do not use immediate here since `tryFocus` may trigger async
					* which makes Vue sync update the `activeKey`
					* that force render before `ref` set the next activeKey
					*/
					cleanRaf();
					rafRef.value = requestAnimationFrame(() => {
						if (activeRef.value === targetKey) focusTargetElement.focus();
					});
				}
			};
			if ([HOME, END].includes(which) || offsetObj?.sibling || !focusMenuElement) {
				let parentQueryContainer;
				if (!focusMenuElement || mode.value === "inline") parentQueryContainer = containerRef.value;
				else parentQueryContainer = findContainerUL(focusMenuElement);
				let targetElement;
				const focusableElements = getFocusableElements(parentQueryContainer, elements);
				if (which === HOME) targetElement = focusableElements[0];
				else if (which === END) targetElement = focusableElements[focusableElements.length - 1];
				else targetElement = getNextFocusElement(parentQueryContainer, elements, focusMenuElement, offsetObj?.offset);
				tryFocus(targetElement);
			} else if (offsetObj.inlineTrigger) triggerAccessibilityOpen(focusMenuKey);
			else if (offsetObj.offset > 0) {
				triggerAccessibilityOpen(focusMenuKey, true);
				cleanRaf();
				rafRef.value = requestAnimationFrame(() => {
					refreshedElements = refreshElements(keys, id);
					const controlId = focusMenuElement.getAttribute("aria-controls");
					tryFocus(getNextFocusElement(controlId ? document.getElementById(controlId) : null, refreshedElements.elements));
				});
			} else if (offsetObj.offset < 0) {
				const keyPath = getKeyPath(focusMenuKey, true);
				const parentKey = keyPath[keyPath.length - 2];
				const parentMenuElement = key2element.get(parentKey);
				triggerAccessibilityOpen(parentKey, false);
				tryFocus(parentMenuElement);
			}
		}
		originOnKeyDown?.(e);
	};
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/utils/timeUtil.js
function nextSlice(callback) {
	/* istanbul ignore next */
	Promise.resolve().then(callback);
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/hooks/useKeyRecords.js
var PATH_SPLIT = "__VC_UTIL_PATH_SPLIT__";
var getPathStr = (keyPath) => keyPath.join(PATH_SPLIT);
var getPathKeys = (keyPathStr) => keyPathStr.split(PATH_SPLIT);
var OVERFLOW_KEY = "vc-menu-more";
function useKeyRecords() {
	const key2pathRef = shallowRef(/* @__PURE__ */ new Map());
	const path2keyRef = shallowRef(/* @__PURE__ */ new Map());
	const overflowKeys = ref([]);
	const updateRef = ref(0);
	const destroyRef = ref(false);
	const schedulePathRegisterUpdate = () => {
		updateRef.value += 1;
		const id = updateRef.value;
		nextSlice(() => {
			if (!destroyRef.value && id === updateRef.value) {
				triggerRef(key2pathRef);
				triggerRef(path2keyRef);
			}
		});
	};
	const registerPath = (key, keyPath) => {
		if (key2pathRef.value.has(key)) console.warn(`Duplicated key '${key}' used in Menu by path [${keyPath.join(" > ")}]`);
		const connectedPath = getPathStr(keyPath);
		path2keyRef.value.set(connectedPath, key);
		key2pathRef.value.set(key, connectedPath);
		schedulePathRegisterUpdate();
	};
	const unregisterPath = (key, keyPath) => {
		const connectedPath = getPathStr(keyPath);
		path2keyRef.value.delete(connectedPath);
		key2pathRef.value.delete(key);
		schedulePathRegisterUpdate();
	};
	const refreshOverflowKeys = (keys) => {
		overflowKeys.value = keys;
	};
	const getKeyPath = (eventKey, includeOverflow) => {
		const keys = getPathKeys(key2pathRef.value.get(eventKey) || "");
		if (includeOverflow && overflowKeys.value.includes(keys[0])) keys.unshift(OVERFLOW_KEY);
		return keys;
	};
	const isSubPathKey = (pathKeys, eventKey) => pathKeys.filter((item) => item !== void 0).some((pathKey) => {
		return getKeyPath(pathKey, true).includes(eventKey);
	});
	const getKeys = () => {
		const keys = [...key2pathRef.value.keys()];
		if (overflowKeys.value.length) keys.push(OVERFLOW_KEY);
		return keys;
	};
	/**
	* Find current key related child path keys
	*/
	const getSubPathKeys = (key) => {
		const connectedPath = `${key2pathRef.value.get(key)}${PATH_SPLIT}`;
		const pathKeys = /* @__PURE__ */ new Set();
		[...path2keyRef.value.keys()].forEach((pathKey) => {
			if (pathKey.startsWith(connectedPath)) pathKeys.add(path2keyRef.value.get(pathKey));
		});
		return pathKeys;
	};
	onBeforeUnmount(() => {
		destroyRef.value = true;
	});
	return {
		registerPath,
		unregisterPath,
		refreshOverflowKeys,
		isSubPathKey,
		getKeyPath,
		getKeys,
		getSubPathKeys
	};
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/utils/nodeUtil.js
function _isSlot$3(s) {
	return typeof s === "function" || Object.prototype.toString.call(s) === "[object Object]" && !isVNode(s);
}
function convertItemsToNodes(list, components, prefixCls, slots) {
	const { item: MergedMenuItem, group: MergedMenuItemGroup, submenu: MergedSubMenu, divider: MergedDivider } = components;
	return (list || []).map((opt, index) => {
		if (opt && typeof opt === "object") {
			const { children, key, type, ...restProps } = opt;
			const mergedKey = key ?? `tmp-${index}`;
			let label;
			let extra;
			let icon;
			const _labelRender = slots?.labelRender ? slots.labelRender(opt) : null;
			const _extraRender = slots?.extraRender ? slots.extraRender(opt) : null;
			const _iconRender = slots?.iconRender ? slots.iconRender(opt) : null;
			const labelArr = filterEmpty(Array.isArray(_labelRender) ? _labelRender : [_labelRender]).filter((item) => item !== void 0 && item !== null);
			const extraArr = filterEmpty(Array.isArray(_extraRender) ? _extraRender : [_extraRender]).filter((item) => item !== void 0 && item !== null);
			const iconArr = filterEmpty(Array.isArray(_iconRender) ? _iconRender : [_iconRender]).filter((item) => item !== void 0 && item !== null);
			if (iconArr.length) icon = iconArr?.[0];
			if (labelArr.length) label = labelArr?.[0];
			if (extraArr.length) extra = extraArr?.[0];
			if (!label) label = opt.label;
			if (!extra) extra = opt.extra;
			if (!icon) icon = opt.icon;
			if (children || type === "group") {
				let _slot2;
				if (type === "group") {
					let _slot;
					return createVNode(MergedMenuItemGroup, mergeProps({ "key": mergedKey }, restProps, {
						"title": label,
						"icon": icon
					}), _isSlot$3(_slot = convertItemsToNodes(children, components, prefixCls, slots)) ? _slot : { default: () => [_slot] });
				}
				return createVNode(MergedSubMenu, mergeProps({ "key": mergedKey }, restProps, {
					"title": label,
					"icon": icon
				}), _isSlot$3(_slot2 = convertItemsToNodes(children, components, prefixCls, slots)) ? _slot2 : { default: () => [_slot2] });
			}
			if (type === "divider") return createVNode(MergedDivider, mergeProps({ "key": mergedKey }, restProps), null);
			return createVNode(MergedMenuItem, mergeProps({ "key": mergedKey }, restProps, {
				"extra": extra,
				"icon": icon
			}), { default: () => [label, (!!extra || extra === 0) && createVNode("span", { "class": `${prefixCls}-item-extra` }, [extra])] });
		}
		return null;
	}).filter((opt) => opt);
}
function parseItems(children, items, keyPath, components, prefixCls, slots) {
	let childNodes = children;
	const mergedComponents = {
		divider: Divider,
		item: MenuItem,
		group: MenuItemGroup,
		submenu: SubMenu,
		...components
	};
	if (items) childNodes = convertItemsToNodes(items, mergedComponents, prefixCls, slots);
	return parseChildren(childNodes, keyPath);
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+util@1.0.19_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/util/dist/isEqual.js
function isEqual(obj1, obj2, shallow = false) {
	const refSet = /* @__PURE__ */ new Set();
	function deepEqual(a, b, level = 1) {
		const circular = refSet.has(a);
		warning_default(!circular, "Warning: There may be circular references");
		if (circular) return false;
		if (a === b) return true;
		if (shallow && level > 1) return false;
		refSet.add(a);
		const newLevel = level + 1;
		if (Array.isArray(a)) {
			if (!Array.isArray(b) || a.length !== b.length) return false;
			for (let i = 0; i < a.length; i++) if (!deepEqual(a[i], b[i], newLevel)) return false;
			return true;
		}
		if (a && b && typeof a === "object" && typeof b === "object") {
			const keys = Object.keys(a);
			if (keys.length !== Object.keys(b).length) return false;
			return keys.every((key) => deepEqual(a[key], b[key], newLevel));
		}
		return false;
	}
	return deepEqual(obj1, obj2);
}
var isEqual_default = isEqual;
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/Menu.js
/**
* Menu modify after refactor:
* ## Add
* - disabled
*
* ## Remove
* - openTransitionName
* - openAnimation
* - onDestroy
* - siderCollapsed: Seems antd do not use this prop (Need test in antd)
* - collapsedWidth: Seems this logic should be handle by antd Layout.Sider
*/
function _isSlot$2(s) {
	return typeof s === "function" || Object.prototype.toString.call(s) === "[object Object]" && !isVNode(s);
}
var EMPTY_LIST = [];
var defaults = {
	prefixCls: "vc-menu",
	mode: "vertical",
	subMenuOpenDelay: .1,
	subMenuCloseDelay: .1,
	selectable: true,
	multiple: false,
	inlineIndent: 24,
	triggerSubMenuAction: "hover",
	overflowedIndicator: "..."
};
//#endregion
//#region ../../node_modules/.pnpm/@v-c+menu@1.0.14_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/menu/dist/index.js
var ExportMenu = /* @__PURE__ */ defineComponent((props, { slots, expose, attrs: _attrs }) => {
	const containerRef = shallowRef();
	const uuid = useId_default(props?.id ? `vc-menu-uuid-${props.id}` : "vc-menu-uuid");
	const isRtl = computed(() => props?.direction === "rtl");
	const childList = shallowRef([]);
	const mergedOverflowIndicator = computed(() => props.overflowedIndicator ?? defaults.overflowedIndicator);
	const overflowIndicatorVersion = ref(0);
	watch(mergedOverflowIndicator, () => {
		overflowIndicatorVersion.value += 1;
	}, { immediate: true });
	useIdContextProvide(computed(() => uuid));
	const innerOpenKeys = ref(props?.openKeys ?? props?.defaultOpenKeys);
	watch(() => props.openKeys, () => {
		innerOpenKeys.value = props?.openKeys;
	});
	const mergedOpenKeys = computed({
		get() {
			if (props.openKeys) return props.openKeys;
			return innerOpenKeys.value ?? EMPTY_LIST;
		},
		set(value) {
			innerOpenKeys.value = value;
		}
	});
	const triggerOpenKeys = (keys, forceFlush = false) => {
		function doUpdate() {
			mergedOpenKeys.value = keys;
			props?.onOpenChange?.(keys);
		}
		if (forceFlush) nextTick(doUpdate);
		else doUpdate();
	};
	const inlineCacheOpenKeys = shallowRef(mergedOpenKeys.value);
	const mountRef = shallowRef(false);
	const modeMerged = computed(() => {
		const { mode, inlineCollapsed } = props;
		if ((mode === "inline" || mode === "vertical") && inlineCollapsed) return ["vertical", inlineCollapsed];
		return [mode, false];
	});
	const mergedMode = computed(() => modeMerged.value[0]);
	const mergedInlineCollapsed = computed(() => modeMerged.value[1]);
	const isInlineMode = computed(() => mergedMode.value === "inline");
	const internalMode = shallowRef(mergedMode.value);
	const internalInlineCollapsed = shallowRef(mergedInlineCollapsed.value);
	watch([mergedMode, mergedInlineCollapsed], () => {
		internalMode.value = mergedMode.value;
		internalInlineCollapsed.value = mergedInlineCollapsed.value;
		if (!mountRef.value) return;
		if (isInlineMode.value) mergedOpenKeys.value = inlineCacheOpenKeys.value;
		else triggerOpenKeys(EMPTY_LIST);
	});
	const lastVisibleIndex = shallowRef(0);
	watch(mergedOpenKeys, () => {
		if (isInlineMode.value) inlineCacheOpenKeys.value = mergedOpenKeys.value;
	});
	onMounted(() => {
		mountRef.value = true;
	});
	onUnmounted(() => {
		mountRef.value = false;
	});
	const { registerPath, unregisterPath, refreshOverflowKeys, isSubPathKey, getKeyPath, getKeys, getSubPathKeys } = useKeyRecords();
	const registerPathContext = computed(() => ({
		registerPath,
		unregisterPath
	}));
	const pathUserContext = computed(() => ({ isSubPathKey }));
	const mergedActiveKey = shallowRef(props?.activeKey);
	watch(() => props.activeKey, () => {
		mergedActiveKey.value = props?.activeKey;
	});
	const onActive = useMemoCallback((key) => {
		mergedActiveKey.value = key;
	});
	const onInactive = useMemoCallback(() => {
		mergedActiveKey.value = void 0;
	});
	const innerSelectKeys = ref(props?.selectedKeys ?? props?.defaultSelectedKeys ?? EMPTY_LIST);
	watch(() => props.selectedKeys, () => {
		innerSelectKeys.value = props.selectedKeys ?? EMPTY_LIST;
	});
	const mergedSelectKeys = computed(() => {
		const keys = innerSelectKeys.value;
		if (Array.isArray(keys)) return keys;
		if (keys === null || keys === void 0) return EMPTY_LIST;
		return [keys];
	});
	const triggerSelection = (info) => {
		if (props.selectable) {
			const { key: targetKey } = info;
			const exist = mergedSelectKeys.value.includes(targetKey);
			let newSelectKeys;
			if (props.multiple) if (exist) newSelectKeys = mergedSelectKeys.value.filter((key) => key !== targetKey);
			else newSelectKeys = [...mergedSelectKeys.value, targetKey];
			else newSelectKeys = [targetKey];
			if (props.selectedKeys === void 0) innerSelectKeys.value = newSelectKeys;
			const selectInfo = {
				...info,
				selectedKeys: newSelectKeys
			};
			if (exist) props.onDeselect?.(selectInfo);
			else props.onSelect?.(selectInfo);
		}
		if (!props.multiple && mergedOpenKeys.value.length && internalMode.value !== "inline") triggerOpenKeys(EMPTY_LIST);
	};
	/**
	* Click for item. SubMenu do not have selection status
	*/
	const onInternalClick = (info) => {
		props.onClick?.(warnItemProp(info));
		triggerSelection(info);
	};
	const onInternalOpenChange = (key, open) => {
		let newOpenKeys = mergedOpenKeys.value.filter((k) => k !== key);
		if (open) newOpenKeys.push(key);
		else if (internalMode.value !== "inline") {
			const subPathKeys = getSubPathKeys(key);
			newOpenKeys = newOpenKeys.filter((k) => !subPathKeys.has(k));
		}
		if (!isEqual_default(mergedOpenKeys.value, newOpenKeys, true)) triggerOpenKeys(newOpenKeys, true);
	};
	const triggerAccessibilityOpen = (key, open) => {
		onInternalOpenChange(key, open ?? !mergedOpenKeys.value.includes(key));
	};
	const setMergedActiveKey = (key) => {
		mergedActiveKey.value = key;
	};
	const onInternalKeyDown = useAccessibility(internalMode, mergedActiveKey, isRtl, uuid, containerRef, getKeys, getKeyPath, setMergedActiveKey, triggerAccessibilityOpen, (...args) => {
		_attrs?.onKeydown?.(...args);
	});
	watch(() => [
		props.activeKey,
		() => props.defaultActiveFirst,
		childList.value
	], () => {
		if (props.activeKey !== void 0) mergedActiveKey.value = props.activeKey;
		else if (props.defaultActiveFirst && childList.value[0]) mergedActiveKey.value = childList.value[0]?.key;
	}, { immediate: true });
	const allVisible = computed(() => lastVisibleIndex.value >= childList.value.length - 1 || internalMode.value !== "horizontal" || props?.disabledOverflow);
	watch([
		lastVisibleIndex,
		allVisible,
		childList
	], () => {
		refreshOverflowKeys(allVisible.value ? EMPTY_LIST : childList.value.slice(lastVisibleIndex.value + 1).map((child) => child.key));
	}, { immediate: true });
	const privateContext = computed(() => ({
		_internalRenderMenuItem: props._internalRenderMenuItem,
		_internalRenderSubMenuItem: props._internalRenderSubMenuItem
	}));
	useMenuContextProvider(computed(() => {
		return {
			prefixCls: props.prefixCls,
			rootClass: props.rootClass,
			classes: props.classes,
			styles: props.styles,
			mode: internalMode.value,
			openKeys: mergedOpenKeys.value,
			rtl: isRtl.value,
			disabled: props.disabled,
			motion: props.motion,
			defaultMotions: props.defaultMotions,
			activeKey: mergedActiveKey.value,
			onActive,
			onInactive,
			selectedKeys: mergedSelectKeys.value,
			inlineIndent: props.inlineIndent || defaults.inlineIndent,
			subMenuOpenDelay: props.subMenuOpenDelay || defaults.subMenuOpenDelay,
			subMenuCloseDelay: props.subMenuCloseDelay || defaults.subMenuCloseDelay,
			forceSubMenuRender: props.forceSubMenuRender,
			builtinPlacements: props.builtinPlacements,
			triggerSubMenuAction: props.triggerSubMenuAction || defaults.triggerSubMenuAction,
			getPopupContainer: props.getPopupContainer,
			itemIcon: props.itemIcon,
			expandIcon: props.expandIcon,
			onItemClick: onInternalClick,
			onOpenChange: onInternalOpenChange,
			popupRender: props.popupRender
		};
	}));
	expose({
		list: containerRef,
		focus: (options) => {
			const keys = getKeys();
			const { elements, key2element, element2key } = refreshElements(keys, uuid);
			const focusableElements = getFocusableElements(containerRef.value, elements);
			let shouldFocusKey;
			if (mergedActiveKey.value && keys.includes(mergedActiveKey.value)) shouldFocusKey = mergedActiveKey.value;
			else shouldFocusKey = focusableElements[0] ? element2key.get(focusableElements[0]) : childList.value.find((node) => !node.props.disabled)?.key;
			const elementToFocus = key2element.get(shouldFocusKey);
			if (shouldFocusKey && elementToFocus) elementToFocus?.focus?.(options);
		},
		findItem: ({ key: itemKey }) => {
			const { key2element } = refreshElements(getKeys(), uuid);
			return key2element.get(itemKey) || null;
		}
	});
	return () => {
		const { prefixCls, rootClass } = props;
		const children = filterEmpty(slots.default?.());
		childList.value = parseItems(children, props?.items, EMPTY_LIST, props?._internalComponents || {}, props?.prefixCls || defaults.prefixCls, {
			labelRender: props?.labelRender,
			extraRender: props?.extraRender,
			iconRender: props?.iconRender
		});
		const measureChildList = parseItems(children, props?.items, EMPTY_LIST, {}, props?.prefixCls || defaults.prefixCls);
		const wrappedChildList = internalMode.value !== "horizontal" || props?.disabledOverflow ? childList.value : childList.value.map((child, index) => createVNode(InheritableContextProvider, {
			"key": child.key,
			"overflowDisabled": index > lastVisibleIndex.value,
			"classes": props.classes,
			"styles": props.styles
		}, _isSlot$2(child) ? child : { default: () => [child] }));
		const container = createVNode(src_default$2, mergeProps({
			"ref": containerRef,
			"prefixCls": `${prefixCls}-overflow`,
			"component": "ul",
			"itemComponent": MenuItem,
			"class": classNames(prefixCls, `${prefixCls}-root`, `${prefixCls}-${internalMode.value}`, _attrs.class || "", {
				[`${prefixCls}-inline-collapsed`]: internalInlineCollapsed.value,
				[`${prefixCls}-rtl`]: isRtl.value
			}, rootClass),
			"style": _attrs.style,
			"data": wrappedChildList
		}, {
			dir: props.direction,
			role: "menu",
			tabindex: _attrs.tabindex ?? 0
		}, {
			"renderRawItem": (node) => {
				return node;
			},
			"renderRawRest": (omitItems) => {
				const len = omitItems.length;
				const originOmitItems = len ? childList.value.slice(-len) : null;
				return createVNode(SubMenu, {
					"eventKey": OVERFLOW_KEY,
					"title": mergedOverflowIndicator.value,
					"disabled": allVisible.value,
					"internalPopupClose": len === 0,
					"popupClassName": props.overflowedIndicatorPopupClassName
				}, _isSlot$2(originOmitItems) ? originOmitItems : { default: () => [originOmitItems] });
			},
			"maxCount": internalMode.value !== "horizontal" || props?.disabledOverflow ? src_default$2.INVALIDATE : src_default$2.RESPONSIVE,
			"ssr": "full",
			"data-menu-list": true,
			"onVisibleChange": (newLastIndex) => {
				lastVisibleIndex.value = newLastIndex;
			}
		}, { onKeydown: onInternalKeyDown }), null);
		return createVNode(PrivateContextProvider, privateContext.value, { default: () => [createVNode(PathUserProvider, pathUserContext.value, _isSlot$2(container) ? container : { default: () => [container] }), createVNode("div", {
			"style": { display: "none" },
			"aria-hidden": true
		}, [createVNode(MeasureProvider, registerPathContext.value, _isSlot$2(measureChildList) ? measureChildList : { default: () => [measureChildList] })])] });
	};
}, {
	props: /* @__PURE__ */ mergeDefaults({
		prefixCls: {
			type: String,
			required: false,
			default: void 0
		},
		rootClass: {
			type: String,
			required: false,
			default: void 0
		},
		classes: {
			type: Object,
			required: false,
			default: void 0
		},
		styles: {
			type: Object,
			required: false,
			default: void 0
		},
		items: {
			type: Array,
			required: false,
			default: void 0
		},
		disabled: {
			type: Boolean,
			required: false,
			default: void 0
		},
		disabledOverflow: {
			type: Boolean,
			required: false,
			default: void 0
		},
		direction: {
			type: String,
			required: false,
			default: void 0
		},
		mode: {
			type: String,
			required: false,
			default: void 0
		},
		inlineCollapsed: {
			type: Boolean,
			required: false,
			default: void 0
		},
		defaultOpenKeys: {
			type: Array,
			required: false,
			default: void 0
		},
		openKeys: {
			type: Array,
			required: false,
			default: void 0
		},
		activeKey: {
			type: String,
			required: false,
			default: void 0
		},
		defaultActiveFirst: {
			type: Boolean,
			required: false,
			default: void 0
		},
		selectable: {
			type: Boolean,
			required: false,
			default: void 0
		},
		multiple: {
			type: Boolean,
			required: false,
			default: void 0
		},
		defaultSelectedKeys: {
			type: Array,
			required: false,
			default: void 0
		},
		selectedKeys: {
			type: Array,
			required: false,
			default: void 0
		},
		onSelect: {
			type: Function,
			required: false,
			default: void 0
		},
		onDeselect: {
			type: Function,
			required: false,
			default: void 0
		},
		inlineIndent: {
			type: Number,
			required: false,
			default: void 0
		},
		motion: {
			type: Object,
			required: false,
			default: void 0
		},
		defaultMotions: {
			type: Object,
			required: false,
			default: void 0
		},
		subMenuOpenDelay: {
			type: Number,
			required: false,
			default: void 0
		},
		subMenuCloseDelay: {
			type: Number,
			required: false,
			default: void 0
		},
		forceSubMenuRender: {
			type: Boolean,
			required: false,
			default: void 0
		},
		triggerSubMenuAction: {
			type: String,
			required: false,
			default: void 0
		},
		builtinPlacements: {
			type: Object,
			required: false,
			default: void 0
		},
		itemIcon: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		expandIcon: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		overflowedIndicator: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		overflowedIndicatorPopupClassName: {
			type: String,
			required: false,
			default: void 0
		},
		getPopupContainer: {
			type: Function,
			required: false,
			default: void 0
		},
		onClick: {
			type: Function,
			required: false,
			default: void 0
		},
		onOpenChange: {
			type: Function,
			required: false,
			default: void 0
		},
		_internalRenderMenuItem: {
			type: Function,
			required: false,
			default: void 0
		},
		_internalRenderSubMenuItem: {
			type: Function,
			required: false,
			default: void 0
		},
		_internalComponents: {
			type: Object,
			required: false,
			default: void 0
		},
		popupRender: {
			type: Function,
			required: false,
			default: void 0
		},
		id: {
			type: String,
			required: false,
			default: void 0
		},
		labelRender: {
			type: Function,
			required: false,
			default: void 0
		},
		extraRender: {
			type: Function,
			required: false,
			default: void 0
		},
		iconRender: {
			type: Function,
			required: false,
			default: void 0
		}
	}, defaults),
	name: "VcMenu",
	inheritAttrs: false
});
ExportMenu.Item = MenuItem;
ExportMenu.SubMenu = SubMenu;
ExportMenu.ItemGroup = MenuItemGroup;
ExportMenu.Divider = Divider;
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/spin/Indicator/Progress.js
var viewSize = 100;
var borderWidth = viewSize / 5;
var radius = viewSize / 2 - borderWidth / 2;
var circumference = radius * 2 * Math.PI;
var position = 50;
var CustomCircle = /* @__PURE__ */ defineComponent((props) => {
	return () => {
		const { dotClassName, hasCircleCls } = props;
		return createVNode("circle", {
			"class": classNames(`${dotClassName}-circle`, { [`${dotClassName}-circle-bg`]: hasCircleCls }),
			"r": radius,
			"cx": position,
			"cy": position,
			"stroke-width": borderWidth
		}, null);
	};
}, { props: {
	dotClassName: {
		type: String,
		required: false
	},
	hasCircleCls: {
		type: Boolean,
		required: false,
		default: void 0
	}
} });
var Progress_default = /* @__PURE__ */ defineComponent((props) => {
	const render = shallowRef(false);
	watch(() => props.percent !== 0, () => {
		if (props.percent !== 0) render.value = true;
	}, { immediate: true });
	return () => {
		const { percent, prefixCls } = props;
		const dotClassName = `${prefixCls}-dot`;
		const holderClassName = `${dotClassName}-holder`;
		const hideClassName = `${holderClassName}-hidden`;
		const safePtg = Math.max(Math.min(percent, 100), 0);
		if (!render.value) return null;
		const circleStyle = {
			strokeDashoffset: `${circumference / 4}`,
			strokeDasharray: `${circumference * safePtg / 100} ${circumference * (100 - safePtg) / 100}`
		};
		return createVNode("span", { "class": classNames(holderClassName, `${dotClassName}-progress`, safePtg <= 0 && hideClassName) }, [createVNode("svg", {
			"viewBox": `0 0 ${viewSize} ${viewSize}`,
			"role": "progressbar",
			"aria-valuemin": 0,
			"aria-valuemax": 100,
			"aria-valuenow": safePtg
		}, [createVNode(CustomCircle, {
			"dotClassName": dotClassName,
			"hasCircleCls": true
		}, null), createVNode(CustomCircle, {
			"dotClassName": dotClassName,
			"style": circleStyle
		}, null)])]);
	};
}, { props: {
	prefixCls: {
		type: String,
		required: true
	},
	percent: {
		type: Number,
		required: true
	}
} });
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/spin/Indicator/Looper.js
var Looper = /* @__PURE__ */ defineComponent((props, { attrs }) => {
	return () => {
		const { prefixCls, percent = 0 } = props;
		const dotClassName = `${prefixCls}-dot`;
		const holderClassName = `${dotClassName}-holder`;
		const hideClassName = `${holderClassName}-hidden`;
		return createVNode(Fragment, null, [createVNode("span", mergeProps(attrs, { "class": classNames(holderClassName, percent > 0 && hideClassName) }), [createVNode("span", { "class": classNames(dotClassName, `${prefixCls}-dot-spin`) }, [[
			1,
			2,
			3,
			4
		].map((i) => createVNode("i", {
			"class": `${prefixCls}-dot-item`,
			"key": i
		}, null))])]), createVNode(Progress_default, {
			"prefixCls": prefixCls,
			"percent": percent
		}, null)]);
	};
}, {
	props: {
		prefixCls: {
			type: String,
			required: true
		},
		percent: {
			type: Number,
			required: false
		}
	},
	inheritAttrs: false
});
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/spin/Indicator/index.js
var Indicator_default = /* @__PURE__ */ defineComponent((props, { slots, attrs }) => {
	return () => {
		const { prefixCls, percent } = props;
		const dotClassName = `${prefixCls}-dot`;
		const indicator = getSlotPropsFnRun(slots, props, "indicator");
		if (indicator && isVNode(indicator)) return createVNode(indicator, {
			class: dotClassName,
			percent
		});
		return createVNode(Looper, mergeProps(attrs, {
			"prefixCls": prefixCls,
			"percent": percent
		}), null);
	};
}, {
	props: /* @__PURE__ */ mergeDefaults({
		prefixCls: {
			type: String,
			required: true
		},
		indicator: {
			type: [
				Function,
				String,
				Number,
				null,
				Object,
				Boolean
			],
			required: false,
			default: void 0
		},
		percent: {
			type: Number,
			required: false
		}
	}, { indicator: void 0 }),
	inheritAttrs: false
});
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/spin/style/index.js
var antSpinMove = new Keyframes_default("antSpinMove", { to: { opacity: 1 } });
var antRotate = new Keyframes_default("antRotate", { to: { transform: "rotate(405deg)" } });
var genSpinStyle = (token) => {
	const { componentCls } = token;
	const sectionCls = `${componentCls}-section`;
	return { [componentCls]: {
		...resetComponent(token),
		position: "relative",
		"&-rtl": { direction: "rtl" },
		[`&${sectionCls}, ${sectionCls}`]: {
			display: "flex",
			alignItems: "center",
			flexDirection: "column",
			gap: token.paddingSM,
			color: token.colorPrimary
		},
		[`&${sectionCls}`]: { display: "inline-flex" },
		[sectionCls]: {
			position: "absolute",
			top: "50%",
			left: {
				_skip_check_: true,
				value: "50%"
			},
			transform: "translate(-50%, -50%)",
			zIndex: 1
		},
		[`${componentCls}-description`]: {
			fontSize: token.fontSize,
			lineHeight: 1
		},
		[`${componentCls}-container`]: {
			position: "relative",
			transition: `opacity ${token.motionDurationSlow}`,
			"&::after": {
				position: "absolute",
				top: 0,
				insetInlineEnd: 0,
				bottom: 0,
				insetInlineStart: 0,
				zIndex: 10,
				width: "100%",
				height: "100%",
				background: token.colorBgContainer,
				opacity: 0,
				transition: `all ${token.motionDurationSlow}`,
				content: "\"\"",
				pointerEvents: "none"
			}
		},
		"&-spinning": {
			[`${componentCls}-description`]: { textShadow: `0 0px 5px ${token.colorBgContainer}` },
			[`${componentCls}-container`]: {
				clear: "both",
				opacity: .5,
				userSelect: "none",
				pointerEvents: "none",
				"&::after": {
					opacity: .4,
					pointerEvents: "auto"
				}
			}
		},
		"&-fullscreen": {
			position: "fixed",
			inset: 0,
			backgroundColor: token.colorBgMask,
			zIndex: token.zIndexPopupBase,
			opacity: 0,
			pointerEvents: "none",
			transition: `all ${token.motionDurationMid}`,
			[`&${componentCls}-spinning`]: {
				opacity: 1,
				pointerEvents: "auto"
			},
			[sectionCls]: {
				color: token.colorWhite,
				[`${componentCls}-description`]: { color: token.colorTextLightSolid }
			}
		}
	} };
};
var genIndicatorStyle = (token) => {
	const { componentCls, antCls, motionDurationSlow } = token;
	const [varName, varRef] = genCssVar(antCls, "spin");
	return { [componentCls]: {
		[varName("dot-holder-size")]: token.dotSize,
		[varName("dot-item-size")]: `calc((${varRef("dot-holder-size")} - ${token.marginXXS} / 2) / 2)`,
		[`${componentCls}-dot`]: {
			"&-holder": {
				width: "1em",
				height: "1em",
				fontSize: varRef("dot-holder-size"),
				display: "inline-block",
				transition: ["transform", "opacity"].map((prop) => `${prop} ${motionDurationSlow} ease`).join(", "),
				transformOrigin: "50% 50%",
				lineHeight: 1,
				"&-hidden": {
					transform: "scale(0.3)",
					opacity: 0
				}
			},
			position: "relative",
			display: "inline-block",
			fontSize: varRef("dot-holder-size"),
			width: "1em",
			height: "1em",
			"&-spin": {
				transform: "rotate(45deg)",
				animationName: antRotate,
				animationDuration: "1.2s",
				animationIterationCount: "infinite",
				animationTimingFunction: "linear"
			},
			"&-item": {
				position: "absolute",
				display: "block",
				width: varRef("dot-item-size"),
				height: varRef("dot-item-size"),
				background: "currentColor",
				borderRadius: "100%",
				transform: "scale(0.75)",
				transformOrigin: "50% 50%",
				opacity: .3,
				animationName: antSpinMove,
				animationDuration: "1s",
				animationIterationCount: "infinite",
				animationTimingFunction: "linear",
				animationDirection: "alternate",
				"&:nth-child(1)": {
					top: 0,
					insetInlineStart: 0,
					animationDelay: "0s"
				},
				"&:nth-child(2)": {
					top: 0,
					insetInlineEnd: 0,
					animationDelay: "0.4s"
				},
				"&:nth-child(3)": {
					insetInlineEnd: 0,
					bottom: 0,
					animationDelay: "0.8s"
				},
				"&:nth-child(4)": {
					bottom: 0,
					insetInlineStart: 0,
					animationDelay: "1.2s"
				}
			},
			"&-progress": {
				position: "absolute",
				left: "50%",
				top: 0,
				transform: "translateX(-50%)"
			},
			"&-circle": {
				strokeLinecap: "round",
				transition: [
					"stroke-dashoffset",
					"stroke-dasharray",
					"stroke",
					"stroke-width",
					"opacity"
				].map((item) => `${item} ${motionDurationSlow} ease`).join(","),
				fillOpacity: 0,
				stroke: "currentcolor"
			},
			"&-circle-bg": { stroke: token.colorFillSecondary }
		}
	} };
};
var genSizeStyle = (token) => {
	const { componentCls } = token;
	const [varName] = genCssVar(token.antCls, "spin");
	return { [componentCls]: {
		"&-sm": { [varName("dot-holder-size")]: token.dotSizeSM },
		"&-lg": { [varName("dot-holder-size")]: token.dotSizeLG }
	} };
};
var prepareComponentToken$1 = (token) => {
	const { controlHeightLG, controlHeight } = token;
	return {
		contentHeight: 400,
		dotSize: controlHeightLG / 2,
		dotSizeSM: controlHeightLG * .35,
		dotSizeLG: controlHeight
	};
};
var style_default$1 = genStyleHooks("Spin", (token) => {
	const spinToken = merge(token, { spinDotDefault: token.colorTextDescription });
	return [
		genSpinStyle(spinToken),
		genIndicatorStyle(spinToken),
		genSizeStyle(spinToken)
	];
}, prepareComponentToken$1);
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/spin/usePercent.js
var AUTO_INTERVAL = 200;
var STEP_BUCKETS = [
	[30, .05],
	[70, .03],
	[96, .01]
];
function usePercent(spinning, percent) {
	const mockPercent = shallowRef(0);
	let mockIntervalRef = null;
	const isAuto = computed(() => percent.value === "auto");
	watch([isAuto, spinning], (_n, _o, onCleanup) => {
		if (isAuto.value && spinning.value) {
			mockPercent.value = 0;
			mockIntervalRef = setInterval(() => {
				const prev = mockPercent.value;
				const restPTG = 100 - prev;
				for (let i = 0; i < STEP_BUCKETS.length; i += 1) {
					const [limit, stepPtg] = STEP_BUCKETS[i];
					if (prev <= limit) {
						mockPercent.value = prev + restPTG * stepPtg;
						break;
					}
				}
			}, AUTO_INTERVAL);
		}
		onCleanup(() => {
			if (mockIntervalRef) {
				clearInterval(mockIntervalRef);
				mockIntervalRef = null;
			}
		});
	}, { immediate: true });
	return computed(() => isAuto.value ? mockPercent.value : percent.value);
}
//#endregion
//#region ../../node_modules/.pnpm/throttle-debounce@5.0.2/node_modules/throttle-debounce/esm/index.js
/**
* Throttle execution of a function. Especially useful for rate limiting
* execution of handlers on events like resize and scroll.
*
* @param {number} delay -                  A zero-or-greater delay in milliseconds. For event callbacks, values around 100 or 250 (or even higher)
*                                            are most useful.
* @param {Function} callback -               A function to be executed after delay milliseconds. The `this` context and all arguments are passed through,
*                                            as-is, to `callback` when the throttled-function is executed.
* @param {object} [options] -              An object to configure options.
* @param {boolean} [options.noTrailing] -   Optional, defaults to false. If noTrailing is true, callback will only execute every `delay` milliseconds
*                                            while the throttled-function is being called. If noTrailing is false or unspecified, callback will be executed
*                                            one final time after the last throttled-function call. (After the throttled-function has not been called for
*                                            `delay` milliseconds, the internal counter is reset).
* @param {boolean} [options.noLeading] -   Optional, defaults to false. If noLeading is false, the first throttled-function call will execute callback
*                                            immediately. If noLeading is true, the first the callback execution will be skipped. It should be noted that
*                                            callback will never executed if both noLeading = true and noTrailing = true.
* @param {boolean} [options.debounceMode] - If `debounceMode` is true (at begin), schedule `clear` to execute after `delay` ms. If `debounceMode` is
*                                            false (at end), schedule `callback` to execute after `delay` ms.
*
* @returns {Function} A new, throttled, function.
*/
function throttle(delay, callback, options) {
	var _ref = options || {}, _ref$noTrailing = _ref.noTrailing, noTrailing = _ref$noTrailing === void 0 ? false : _ref$noTrailing, _ref$noLeading = _ref.noLeading, noLeading = _ref$noLeading === void 0 ? false : _ref$noLeading, _ref$debounceMode = _ref.debounceMode, debounceMode = _ref$debounceMode === void 0 ? void 0 : _ref$debounceMode;
	var timeoutID;
	var cancelled = false;
	var lastExec = 0;
	function clearExistingTimeout() {
		if (timeoutID) clearTimeout(timeoutID);
	}
	function cancel(options) {
		var _ref2$upcomingOnly = (options || {}).upcomingOnly, upcomingOnly = _ref2$upcomingOnly === void 0 ? false : _ref2$upcomingOnly;
		clearExistingTimeout();
		cancelled = !upcomingOnly;
	}
	function wrapper() {
		for (var _len = arguments.length, arguments_ = new Array(_len), _key = 0; _key < _len; _key++) arguments_[_key] = arguments[_key];
		var self = this;
		var elapsed = Date.now() - lastExec;
		if (cancelled) return;
		function exec() {
			lastExec = Date.now();
			callback.apply(self, arguments_);
		}
		function clear() {
			timeoutID = void 0;
		}
		if (!noLeading && debounceMode && !timeoutID) exec();
		clearExistingTimeout();
		if (debounceMode === void 0 && elapsed > delay) if (noLeading) {
			lastExec = Date.now();
			if (!noTrailing) timeoutID = setTimeout(debounceMode ? clear : exec, delay);
		} else exec();
		else if (noTrailing !== true) timeoutID = setTimeout(debounceMode ? clear : exec, debounceMode === void 0 ? delay - elapsed : delay);
	}
	wrapper.cancel = cancel;
	return wrapper;
}
/**
* Debounce execution of a function. Debouncing, unlike throttling,
* guarantees that a function is only executed a single time, either at the
* very beginning of a series of calls, or at the very end.
*
* @param {number} delay -               A zero-or-greater delay in milliseconds. For event callbacks, values around 100 or 250 (or even higher) are most useful.
* @param {Function} callback -          A function to be executed after delay milliseconds. The `this` context and all arguments are passed through, as-is,
*                                        to `callback` when the debounced-function is executed.
* @param {object} [options] -           An object to configure options.
* @param {boolean} [options.atBegin] -  Optional, defaults to false. If atBegin is false or unspecified, callback will only be executed `delay` milliseconds
*                                        after the last debounced-function call. If atBegin is true, callback will be executed only at the first debounced-function call.
*                                        (After the throttled-function has not been called for `delay` milliseconds, the internal counter is reset).
*
* @returns {Function} A new, debounced function.
*/
function debounce(delay, callback, options) {
	var _ref$atBegin = (options || {}).atBegin;
	return throttle(delay, callback, { debounceMode: (_ref$atBegin === void 0 ? false : _ref$atBegin) !== false });
}
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/spin/index.js
var defaultIndicator;
function shouldDelay(spinning, delay) {
	return !!spinning && !!delay && !Number.isNaN(Number(delay));
}
var Spin = /* @__PURE__ */ defineComponent((props, { slots, attrs }) => {
	const componentCtx = useComponentConfig("spin");
	const { direction, prefixCls, class: contextClassName, style: contextStyle, classes: contextClassNames, styles: contextStyles } = useComponentBaseConfig("spin", props, ["indicator"]);
	const [hashId, cssVarCls] = style_default$1(prefixCls);
	const { classes, styles } = toPropsRefs$1(props, "classes", "styles");
	const spinning = shallowRef(shouldDelay(props.spinning, props.delay) ? false : !!props.spinning);
	const mergedPercent = usePercent(spinning, computed(() => props.percent));
	watch([() => props.delay, () => props.spinning], (_, _p, onCleanup) => {
		if (props.spinning) {
			const showSpinning = debounce(props?.delay ?? 0, () => {
				spinning.value = true;
			});
			showSpinning();
			onCleanup(() => {
				showSpinning?.cancel?.();
			});
			return;
		}
		spinning.value = false;
	}, { immediate: true });
	const warning = devUseWarning("Spin");
	const mergedSize = useSize((ctx) => props.size ?? ctx);
	const mergedProps = computed(() => {
		return {
			...props,
			size: mergedSize.value,
			spinning: spinning.value,
			fullscreen: props.fullscreen,
			percent: mergedPercent.value
		};
	});
	const [mergedClassNames, mergedStyles] = useMergeSemantic(useToArr(contextClassNames, classes), useToArr(contextStyles, styles), useToProps(mergedProps));
	return () => {
		const { fullscreen, size, rootClass, wrapperClassName } = props;
		const children = filterEmpty(slots?.default?.() || []);
		const indicator = getSlotPropsFnRun(slots, props, "indicator");
		const contextIndicator = getSlotPropsFnRun({}, componentCtx.value, "indicator");
		const hasChildren = children.length > 0;
		const isNested = hasChildren || fullscreen;
		const description = getSlotPropsFnRun(slots, props, "description");
		const tip = getSlotPropsFnRun(slots, props, "tip");
		const mergedDescription = description ?? tip;
		const { style, className, restAttrs } = getAttrStyleAndClass$1(attrs);
		warning.deprecated(size !== "default", "size=\"default\"", "size=\"medium\"");
		warning.deprecated(!props.tip && !slots.tip, "tip", "description");
		warning.deprecated(!wrapperClassName, "wrapperClassName", "classes.root");
		warning.deprecated(!(mergedClassNames.value?.tip || mergedStyles.value?.tip), "classes.tip and styles.tip", "classes.description and styles.description");
		warning.deprecated(!(mergedClassNames.value?.mask || mergedStyles.value?.mask), "classes.mask and styles.mask", "classes.root and styles.root");
		const mergedIndicator = indicator ?? contextIndicator ?? defaultIndicator;
		const indicatorNode = createVNode(Fragment, null, [createVNode(Indicator_default, {
			"class": classNames(mergedClassNames.value.indicator),
			"style": mergedStyles.value.indicator,
			"prefixCls": prefixCls.value,
			"indicator": mergedIndicator,
			"percent": mergedPercent.value
		}, null), mergedDescription && createVNode("div", {
			"class": classNames(`${prefixCls.value}-description`, mergedClassNames.value.tip, mergedClassNames.value.description),
			"style": {
				...mergedStyles.value.tip,
				...mergedStyles.value.description
			}
		}, [mergedDescription])]);
		return createVNode("div", mergeProps(restAttrs, {
			"class": classNames(prefixCls.value, {
				[`${prefixCls.value}-sm`]: mergedSize.value === "small",
				[`${prefixCls.value}-lg`]: mergedSize.value === "large",
				[`${prefixCls.value}-spinning`]: spinning.value,
				[`${prefixCls.value}-rtl`]: direction.value === "rtl",
				[`${prefixCls.value}-fullscreen`]: fullscreen
			}, rootClass, mergedClassNames.value.root, fullscreen && mergedClassNames.value.mask, isNested ? wrapperClassName : [`${prefixCls.value}-section`, mergedClassNames.value.section], contextClassName.value, className, hashId.value, cssVarCls.value),
			"style": {
				...mergedStyles.value.root,
				...!isNested ? mergedStyles.value.section : {},
				...fullscreen ? mergedStyles.value.mask : {},
				...contextStyle.value,
				...style
			},
			"aria-live": "polite",
			"aria-busy": spinning.value
		}), [spinning.value && (isNested ? createVNode("div", {
			"class": classNames(`${prefixCls.value}-section`, mergedClassNames.value.section),
			"style": mergedStyles.value.section
		}, [indicatorNode]) : indicatorNode), hasChildren && createVNode("div", {
			"class": classNames(`${prefixCls.value}-container`, mergedClassNames.value.container),
			"style": mergedStyles.value.container
		}, [children])]);
	};
}, {
	props: /* @__PURE__ */ mergeDefaults({
		spinning: {
			type: Boolean,
			required: false,
			default: void 0
		},
		size: {
			type: [String, null],
			required: false
		},
		tip: {
			type: [
				Function,
				String,
				Number,
				null,
				Object,
				Boolean
			],
			required: false,
			default: void 0
		},
		description: {
			type: [
				Function,
				String,
				Number,
				null,
				Object,
				Boolean
			],
			required: false,
			default: void 0
		},
		delay: {
			type: Number,
			required: false
		},
		wrapperClassName: {
			type: String,
			required: false
		},
		indicator: {
			type: [
				Function,
				String,
				Number,
				null,
				Object,
				Boolean
			],
			required: false,
			default: void 0
		},
		fullscreen: {
			type: Boolean,
			required: false,
			default: void 0
		},
		percent: {
			type: [Number, String],
			required: false
		},
		classes: {
			type: [Object, Function],
			required: false
		},
		styles: {
			type: [Object, Function],
			required: false
		},
		rootClass: {
			type: String,
			required: false
		},
		prefixCls: {
			type: String,
			required: false
		}
	}, {
		spinning: true,
		delay: 0,
		fullscreen: false
	}),
	name: "ASpin",
	inheritAttrs: false
});
Spin.setDefaultIndicator = (indicator) => {
	defaultIndicator = indicator;
};
Spin.install = (app) => {
	app.component(Spin.name, Spin);
};
var spin_default = Spin;
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/_util/toList.js
function toList(candidate, skipEmpty = false) {
	if (skipEmpty && (candidate === void 0 || candidate === null)) return [];
	return Array.isArray(candidate) ? candidate : [candidate];
}
var toList_default = toList;
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/mentions/style/index.js
var genDropdownStyle = (token) => {
	const { componentCls, fontSize, paddingXXS, colorBgElevated, borderRadiusLG, boxShadowSecondary, itemPaddingVertical, controlPaddingHorizontal, colorText, borderRadius, lineHeight, colorTextDisabled, controlItemBgHover, motionDurationSlow } = token;
	return { [componentCls]: { "&-dropdown": {
		...resetComponent(token),
		position: "absolute",
		top: -9999,
		insetInlineStart: -9999,
		zIndex: token.zIndexPopup,
		boxSizing: "border-box",
		fontSize,
		fontVariant: "initial",
		padding: paddingXXS,
		backgroundColor: colorBgElevated,
		borderRadius: borderRadiusLG,
		outline: "none",
		boxShadow: boxShadowSecondary,
		"&-hidden": { display: "none" },
		[`${componentCls}-dropdown-menu`]: {
			maxHeight: token.dropdownHeight,
			margin: 0,
			paddingInlineStart: 0,
			overflow: "auto",
			listStyle: "none",
			outline: "none",
			"&-item": {
				...textEllipsis,
				position: "relative",
				display: "block",
				minWidth: token.controlItemWidth,
				padding: `${unit(itemPaddingVertical)} ${unit(controlPaddingHorizontal)}`,
				color: colorText,
				borderRadius,
				fontWeight: "normal",
				lineHeight,
				cursor: "pointer",
				transition: `background-color ${motionDurationSlow} ease`,
				"&:hover": { backgroundColor: controlItemBgHover },
				"&-disabled": {
					color: colorTextDisabled,
					cursor: "not-allowed",
					"&:hover": {
						color: colorTextDisabled,
						backgroundColor: controlItemBgHover,
						cursor: "not-allowed"
					}
				},
				"&-selected": {
					color: colorText,
					fontWeight: token.fontWeightStrong,
					backgroundColor: controlItemBgHover
				},
				"&-active": { backgroundColor: controlItemBgHover }
			}
		}
	} } };
};
var genMentionsStyle = (token) => {
	const { componentCls, colorText, antCls, colorTextDisabled, calc } = token;
	const [varName, varRef] = genCssVar(antCls, "cmp-mentions");
	return { [componentCls]: [
		resetComponent(token),
		genBasicInputStyle(token, {
			largeStyle: { padding: 0 },
			smallStyle: { padding: 0 }
		}),
		genOutlinedStyle(token),
		genFilledStyle(token),
		genBorderlessStyle(token),
		genUnderlinedStyle(token),
		{
			[varName("padding-inline")]: token.paddingInline,
			[varName("padding-block")]: token.paddingBlock,
			[varName("control-height")]: token.controlHeight,
			display: "flex",
			padding: 0,
			whiteSpace: "pre-wrap",
			"> textarea": [
				resetComponent(token),
				genPlaceholderStyle(token.colorTextPlaceholder),
				{
					background: "transparent",
					border: "none",
					borderRadius: "inherit",
					outline: "none",
					flex: "auto",
					minWidth: 0,
					resize: "none",
					"&:disabled": { color: colorTextDisabled }
				}
			],
			[`> textarea, ${componentCls}-measure`]: {
				color: colorText,
				boxSizing: "border-box",
				margin: 0,
				minHeight: calc(varRef("control-height")).sub(calc(token.lineWidth).mul(2).equal()).equal(),
				paddingInline: varRef("padding-inline"),
				paddingBlock: varRef("padding-block"),
				overflow: "inherit",
				overflowX: "hidden",
				overflowY: "auto",
				fontWeight: "inherit",
				fontSize: "inherit",
				fontFamily: "inherit",
				fontStyle: "inherit",
				fontVariant: "inherit",
				fontSizeAdjust: "inherit",
				fontStretch: "inherit",
				lineHeight: "inherit",
				direction: "inherit",
				letterSpacing: "inherit",
				whiteSpace: "inherit",
				textAlign: "inherit",
				verticalAlign: "top",
				wordWrap: "break-word",
				wordBreak: "inherit",
				tabSize: "inherit"
			},
			[`${componentCls}-measure`]: {
				position: "absolute",
				inset: 0,
				zIndex: -1,
				color: "transparent",
				pointerEvents: "none",
				"> span": {
					display: "inline-block",
					minHeight: "1em"
				}
			},
			[`${componentCls}-suffix`]: {
				display: "inline-flex",
				alignItems: "center",
				flex: "none",
				color: token.colorTextQuaternary,
				fontSize: token.fontSizeIcon,
				lineHeight: 1,
				position: "absolute",
				top: "50%",
				transform: "translateY(-50%)",
				insetInlineEnd: varRef("padding-inline"),
				columnGap: token.marginXS,
				[`${componentCls}-clear-icon`]: {
					cursor: "pointer",
					border: 0,
					background: "transparent",
					"&:hover": { color: token.colorIcon },
					"&:active": { color: token.colorText },
					"&-hidden": { visibility: "hidden" }
				},
				[`${antCls}-form-item-feedback-icon`]: {
					display: "inline-flex",
					alignItems: "center",
					justifyContent: "center"
				}
			}
		},
		{ "&-has-suffix": { "> textarea": { paddingInlineEnd: calc(token.paddingXXS).mul(1.5).add(token.fontSizeIcon).add(varRef("padding-inline")).equal() } } },
		{ "&-disabled": { "> textarea": { ...genDisabledStyle(token) } } },
		{
			"&-lg": {
				[varName("padding-inline")]: token.paddingInlineLG,
				[varName("padding-block")]: token.paddingBlockLG,
				[varName("control-height")]: token.controlHeightLG
			},
			"&-sm": {
				[varName("padding-inline")]: token.paddingInlineSM,
				[varName("padding-block")]: token.paddingBlockSM,
				[varName("control-height")]: token.controlHeightSM
			}
		}
	] };
};
var prepareComponentToken = (token) => ({
	...initComponentToken(token),
	dropdownHeight: 250,
	controlItemWidth: 100,
	zIndexPopup: token.zIndexPopupBase + 50,
	itemPaddingVertical: (token.controlHeight - token.fontHeight) / 2
});
var style_default = genStyleHooks("Mentions", (token) => {
	const mentionsToken = merge(token, initInputToken(token));
	return [genMentionsStyle(mentionsToken), genDropdownStyle(mentionsToken)];
}, prepareComponentToken);
//#endregion
//#region ../../node_modules/.pnpm/@v-c+mentions@1.0.0_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/mentions/dist/context.js
var UnstableContextKey = Symbol("UnstableContext");
function useUnstableContext() {
	return inject(UnstableContextKey, {});
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+mentions@1.0.0_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/mentions/dist/MentionsContext.js
var MentionsContextKey = Symbol("MentionsContext");
function useMentionsContext() {
	return inject(MentionsContextKey, ref());
}
var MentionsProvider = defineComponent((props, { slots }) => {
	provide(MentionsContextKey, computed(() => props.value));
	return () => {
		return slots?.default?.();
	};
}, {
	name: "MentionsProvider",
	inheritAttrs: false,
	props: ["value"]
});
//#endregion
//#region ../../node_modules/.pnpm/@v-c+mentions@1.0.0_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/mentions/dist/DropdownMenu.js
function _isSlot$1(s) {
	return typeof s === "function" || Object.prototype.toString.call(s) === "[object Object]" && !isVNode(s);
}
var DropdownMenu_default = /* @__PURE__ */ defineComponent((props) => {
	const mentionsContext = useMentionsContext();
	const menuRef = shallowRef();
	const activeIndex = computed(() => mentionsContext.value.activeIndex);
	const activeOption = computed(() => props.options[activeIndex.value] || {});
	const activeOptionKey = computed(() => activeOption.value?.key);
	let removeListListeners;
	const bindListEvents = (list) => {
		if (removeListListeners) {
			removeListListeners();
			removeListListeners = void 0;
		}
		if (!list) return;
		const handleFocus = (event) => {
			mentionsContext.value.onFocus?.(event);
		};
		const handleBlur = (event) => {
			mentionsContext.value.onBlur?.(event);
		};
		const handleScroll = (event) => {
			mentionsContext.value.onScroll?.(event);
		};
		list.addEventListener("focusin", handleFocus);
		list.addEventListener("focusout", handleBlur);
		list.addEventListener("scroll", handleScroll);
		removeListListeners = () => {
			list.removeEventListener("focusin", handleFocus);
			list.removeEventListener("focusout", handleBlur);
			list.removeEventListener("scroll", handleScroll);
		};
	};
	watch(() => menuRef.value?.list, (list, _, onCleanup) => {
		if (list) list = getDOM(list);
		bindListEvents(list || null);
		onCleanup(() => {
			removeListListeners?.();
			removeListListeners = void 0;
		});
	}, {
		immediate: true,
		flush: "post"
	});
	watch([
		activeIndex,
		activeOptionKey,
		() => props.opened
	], () => {
		if (!props.opened || activeIndex.value === -1) return;
		nextTick(() => {
			const key = activeOptionKey.value;
			if (!key) return;
			(menuRef.value?.findItem?.({ key }))?.scrollIntoView({
				block: "nearest",
				inline: "nearest"
			});
		});
	});
	onBeforeUnmount(() => {
		removeListListeners?.();
		removeListListeners = void 0;
	});
	return () => {
		const { notFoundContent, setActiveIndex, selectOption } = mentionsContext.value;
		const { prefixCls, options } = props;
		const activeKey = activeOptionKey.value;
		return createVNode(ExportMenu, {
			"ref": menuRef,
			"prefixCls": `${prefixCls}-menu`,
			"activeKey": activeKey,
			"onSelect": ({ key }) => {
				const option = options.find(({ key: optionKey }) => optionKey === key);
				if (option) selectOption(option);
			}
		}, { default: () => [options.map((option, index) => {
			const { key, disabled, class: className, style, label } = option;
			return createVNode(MenuItem, mergeProps({
				"key": key,
				"disabled": disabled,
				"class": className,
				"style": style
			}, { onMouseenter: () => {
				setActiveIndex(index);
			} }), _isSlot$1(label) ? label : { default: () => [label] });
		}), !options.length && createVNode(MenuItem, {
			"disabled": true,
			"key": "notFound"
		}, _isSlot$1(notFoundContent) ? notFoundContent : { default: () => [notFoundContent] })] });
	};
}, {
	props: {
		prefixCls: {
			type: String,
			required: false,
			default: void 0
		},
		options: {
			type: Array,
			required: true,
			default: void 0
		},
		opened: {
			type: Boolean,
			required: true,
			default: void 0
		}
	},
	name: "DropdownMenu",
	inheritAttrs: false
});
//#endregion
//#region ../../node_modules/.pnpm/@v-c+mentions@1.0.0_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/mentions/dist/KeywordTrigger.js
var BUILT_IN_PLACEMENTS = {
	bottomRight: {
		points: ["tl", "br"],
		offset: [0, 4],
		overflow: {
			adjustX: 1,
			adjustY: 1
		}
	},
	bottomLeft: {
		points: ["tr", "bl"],
		offset: [0, 4],
		overflow: {
			adjustX: 1,
			adjustY: 1
		}
	},
	topRight: {
		points: ["bl", "tr"],
		offset: [0, -4],
		overflow: {
			adjustX: 1,
			adjustY: 1
		}
	},
	topLeft: {
		points: ["br", "tl"],
		offset: [0, -4],
		overflow: {
			adjustX: 1,
			adjustY: 1
		}
	}
};
var KeywordTrigger_default = /* @__PURE__ */ defineComponent((props, { slots }) => {
	const opened = shallowRef(false);
	const dropdownPlacement = computed(() => {
		if (props.direction === "rtl") return props.placement === "top" ? "topLeft" : "bottomLeft";
		return props.placement === "top" ? "topRight" : "bottomRight";
	});
	return () => {
		const { prefixCls, options, visible, transitionName, getPopupContainer, popupClassName, popupStyle } = props;
		const dropdownPrefix = `${prefixCls}-dropdown`;
		return createVNode(src_default$1, {
			"prefixCls": dropdownPrefix,
			"popupVisible": visible,
			"popup": createVNode(DropdownMenu_default, {
				"prefixCls": dropdownPrefix,
				"options": options,
				"opened": opened.value
			}, null),
			"popupPlacement": dropdownPlacement.value,
			"popupMotion": { name: transitionName },
			"builtinPlacements": BUILT_IN_PLACEMENTS,
			"getPopupContainer": getPopupContainer,
			"popupClassName": popupClassName,
			"popupStyle": popupStyle,
			"afterOpenChange": (nextOpen) => {
				opened.value = nextOpen;
			}
		}, { default: () => [slots?.default?.()] });
	};
}, {
	props: {
		loading: {
			type: Boolean,
			required: false,
			default: void 0
		},
		options: {
			type: Array,
			required: true,
			default: void 0
		},
		prefixCls: {
			type: String,
			required: false,
			default: void 0
		},
		placement: {
			type: String,
			required: false,
			default: void 0
		},
		direction: {
			type: String,
			required: false,
			default: void 0
		},
		visible: {
			type: Boolean,
			required: false,
			default: void 0
		},
		transitionName: {
			type: String,
			required: false,
			default: void 0
		},
		getPopupContainer: {
			type: Function,
			required: false,
			default: void 0
		},
		popupClassName: {
			type: String,
			required: false,
			default: void 0
		},
		popupStyle: {
			type: Object,
			required: false,
			default: void 0
		}
	},
	name: "KeywordTrigger",
	inheritAttrs: false
});
//#endregion
//#region ../../node_modules/.pnpm/@v-c+mentions@1.0.0_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/mentions/dist/hooks/useEffectState.js
function useEffectState() {
	const effectId = shallowRef({
		id: 0,
		callback: void 0
	});
	const update = (callback) => {
		effectId.value = {
			id: effectId.value.id + 1,
			callback
		};
	};
	watch(() => effectId?.value?.id, () => {
		effectId.value?.callback?.();
	});
	return update;
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+mentions@1.0.0_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/mentions/dist/util.js
function getBeforeSelectionText(input) {
	const { selectionStart } = input;
	return input.value.slice(0, selectionStart);
}
function getLastMeasureIndex(text, prefix) {
	return prefix.reduce((lastMatch, prefixStr) => {
		const lastIndex = text.lastIndexOf(prefixStr);
		if (lastIndex > lastMatch.location) return {
			location: lastIndex,
			prefix: prefixStr
		};
		return lastMatch;
	}, {
		location: -1,
		prefix: ""
	});
}
function lower(char) {
	return (char || "").toLowerCase();
}
function reduceText(text, targetText, split) {
	const firstChar = text[0];
	if (!firstChar || firstChar === split) return text;
	let restText = text;
	const targetTextLen = targetText.length;
	for (let i = 0; i < targetTextLen; i += 1) if (lower(restText[i]) !== lower(targetText[i])) {
		restText = restText.slice(i);
		break;
	} else if (i === targetTextLen - 1) restText = restText.slice(targetTextLen);
	return restText;
}
function replaceWithMeasure(text, measureConfig) {
	const { measureLocation, prefix, targetText, selectionStart, split } = measureConfig;
	let beforeMeasureText = text.slice(0, measureLocation);
	if (beforeMeasureText[beforeMeasureText.length - split.length] === split) beforeMeasureText = beforeMeasureText.slice(0, beforeMeasureText.length - split.length);
	if (beforeMeasureText) beforeMeasureText = `${beforeMeasureText}${split}`;
	let restText = reduceText(text.slice(selectionStart), targetText.slice(selectionStart - measureLocation - prefix.length), split);
	if (restText.slice(0, split.length) === split) restText = restText.slice(split.length);
	const connectedStartText = `${beforeMeasureText}${prefix}${targetText}${split}`;
	return {
		text: `${connectedStartText}${restText}`,
		selectionLocation: connectedStartText.length
	};
}
function setInputSelection(input, location) {
	input.setSelectionRange(location, location);
	input.blur();
	input.focus();
}
function validateSearch(text, split) {
	if (text === void 0 && split === void 0) return validateSearch;
	return !split || !(text ?? "").includes(split);
}
function filterOption(input, option) {
	if (input === void 0 && option === void 0) return filterOption;
	const lowerCase = (typeof input === "string" ? input : String(input ?? "")).toLowerCase();
	return (option?.value ?? "").toLowerCase().includes(lowerCase);
}
//#endregion
//#region ../../node_modules/.pnpm/@v-c+mentions@1.0.0_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/mentions/dist/Mentions.js
var omitKeys$1 = [
	"prefixCls",
	"className",
	"style",
	"classNames",
	"styles",
	"prefix",
	"split",
	"notFoundContent",
	"value",
	"defaultValue",
	"children",
	"options",
	"allowClear",
	"suffix",
	"hasWrapper",
	"silent",
	"validateSearch",
	"filterOption",
	"onChange",
	"onKeydown",
	"onKeyup",
	"onPressEnter",
	"onSearch",
	"onSelect",
	"onFocus",
	"onBlur",
	"transitionName",
	"placement",
	"direction",
	"getPopupContainer",
	"popupClassName",
	"rows",
	"visible",
	"onPopupScroll"
];
var InternalMentions = /* @__PURE__ */ defineComponent((props, { slots, expose, attrs }) => {
	const mergedPrefix = computed(() => {
		const prefix = props.prefix;
		return Array.isArray(prefix) ? prefix : [prefix];
	});
	const containerRef = shallowRef();
	const textareaRef = shallowRef();
	const measureRef = shallowRef();
	const getTextArea = () => textareaRef.value?.resizableTextArea?.textArea;
	expose({
		focus: () => textareaRef.value?.focus?.(),
		blur: () => textareaRef.value?.blur?.(),
		textarea: computed(() => textareaRef.value?.resizableTextArea?.textArea),
		nativeElement: containerRef
	});
	const measuring = shallowRef(false);
	const measureText = shallowRef("");
	const measurePrefix = shallowRef("");
	const measureLocation = shallowRef(0);
	const activeIndex = shallowRef(0);
	const isFocus = shallowRef(false);
	const setActiveIndex = (index) => {
		activeIndex.value = index;
	};
	const uniqueKey = useId_default(props.id);
	const mergedValue = shallowRef(props.value ?? props?.defaultValue ?? "");
	watch(() => props?.value, () => {
		mergedValue.value = props.value ?? "";
	});
	const { open } = useUnstableContext();
	watch(measuring, () => {
		if (measuring.value && measureRef.value) measureRef.value.scrollTop = getTextArea()?.scrollTop;
	}, { immediate: true });
	const mergedMeasuringInfo = computed(() => {
		if (open?.value) for (let i = 0; i < mergedPrefix.value.length; i += 1) {
			const curPrefix = mergedPrefix.value[i];
			const index = mergedValue.value.lastIndexOf(curPrefix);
			if (index >= 0) return [
				true,
				"",
				curPrefix,
				index
			];
		}
		return [
			measuring.value,
			measureText.value,
			measurePrefix.value,
			measureLocation.value
		];
	});
	const mergedMeasuring = computed(() => mergedMeasuringInfo.value[0]);
	const mergedMeasureText = computed(() => mergedMeasuringInfo.value[1]);
	const mergedMeasurePrefix = computed(() => mergedMeasuringInfo.value[2]);
	const mergedMeasureLocation = computed(() => mergedMeasuringInfo.value[3]);
	const children = computed(() => {
		const _child = slots?.default ? slots?.default?.() : [];
		if (_child) return filterEmpty(_child).filter(Boolean);
		return _child;
	});
	const getOptions = (targetMeasureText) => {
		let list;
		const options = props?.options ?? [];
		const filterOption$1 = props?.filterOption ?? filterOption;
		if (options && options.length > 0) list = options.map((item) => ({
			...item,
			key: `${item?.key ?? item.value}-${uniqueKey}`
		}));
		else list = toArray(children.value).map(({ props: optionProps, key }) => ({
			...optionProps,
			label: optionProps.children?.default?.(),
			key: `${key || optionProps.value}-${uniqueKey}`
		}));
		return list.filter((option) => {
			if (filterOption$1 === false) return true;
			if (typeof filterOption$1 !== "function") return true;
			return filterOption$1(targetMeasureText, option);
		});
	};
	const mergedOptions = computed(() => getOptions(mergedMeasureText.value));
	const onSelectionEffect = useEffectState();
	const startMeasure = (nextMeasureText, nextMeasurePrefix, nextMeasureLocation) => {
		measuring.value = true;
		measureText.value = nextMeasureText;
		measurePrefix.value = nextMeasurePrefix;
		measureLocation.value = nextMeasureLocation;
		activeIndex.value = 0;
	};
	const stopMeasure = (callback) => {
		measuring.value = false;
		measureLocation.value = 0;
		measureText.value = "";
		onSelectionEffect(callback);
	};
	const triggerChange = (nextValue) => {
		mergedValue.value = nextValue;
		props?.onChange?.(nextValue);
	};
	const onInternalChange = (e) => {
		const nextValue = e?.target?.value;
		triggerChange(nextValue);
	};
	const selectOption = (option) => {
		const { value: mentionValue = "" } = option;
		const textArea = getTextArea();
		const { text, selectionLocation } = replaceWithMeasure(mergedValue.value, {
			measureLocation: mergedMeasureLocation.value,
			targetText: mentionValue,
			prefix: mergedMeasurePrefix.value,
			selectionStart: textArea?.selectionStart,
			split: props.split
		});
		triggerChange(text);
		stopMeasure(() => {
			setInputSelection(textArea, selectionLocation);
		});
		props?.onSelect?.(option, mergedMeasurePrefix.value);
	};
	const onInternalKeyDown = (event) => {
		const { which } = event;
		props?.onKeydown?.(event);
		if (!mergedMeasuring.value) return;
		if (which === KeyCode_default.UP || which === KeyCode_default.DOWN) {
			const optionLen = mergedOptions.value.length;
			const offset = which === KeyCode_default.UP ? -1 : 1;
			activeIndex.value = (activeIndex.value + offset + optionLen) % optionLen;
			event.preventDefault();
		} else if (which === KeyCode_default.ESC) stopMeasure();
		else if (which === KeyCode_default.ENTER) {
			event.preventDefault();
			if (props?.silent) return;
			if (!mergedOptions.value.length) {
				stopMeasure();
				return;
			}
			const option = mergedOptions.value[activeIndex.value];
			selectOption(option);
		}
	};
	const onInternalKeyUp = (event) => {
		const { key, which } = event;
		const target = event.target;
		const selectionStartText = getBeforeSelectionText(target);
		const { location: measureIndex, prefix: nextMeasurePrefix } = getLastMeasureIndex(selectionStartText, mergedPrefix.value);
		props?.onKeyup?.(event);
		if ([
			KeyCode_default.ESC,
			KeyCode_default.UP,
			KeyCode_default.DOWN,
			KeyCode_default.ENTER
		].includes(which)) return;
		if (measureIndex !== -1) {
			const nextMeasureText = selectionStartText.slice(measureIndex + nextMeasurePrefix.length);
			const validateMeasure = (typeof props.validateSearch === "function" ? props.validateSearch : validateSearch)(nextMeasureText, props.split);
			const matchOption = !!getOptions(nextMeasureText).length;
			if (validateMeasure) {
				if (key === nextMeasurePrefix || key === "Shift" || which === KeyCode_default.ALT || key === "AltGraph" || mergedMeasuring.value || nextMeasureText !== mergedMeasureText.value && matchOption) startMeasure(nextMeasureText, nextMeasurePrefix, measureIndex);
			} else if (mergedMeasuring.value) stopMeasure();
			const onSearch = props?.onSearch;
			if (onSearch && validateMeasure) onSearch(nextMeasureText, nextMeasurePrefix);
		} else if (mergedMeasuring.value) stopMeasure();
	};
	const onInternalPressEnter = (event) => {
		const onPressEnter = props?.onPressEnter;
		if (!mergedMeasuring.value && onPressEnter) onPressEnter(event);
	};
	const focusRef = shallowRef();
	const onInternalFocus = (event) => {
		window.clearTimeout(focusRef.value);
		const onFocus = props?.onFocus;
		if (!isFocus.value && event && onFocus) onFocus(event);
		isFocus.value = true;
	};
	const onInternalBlur = (event) => {
		focusRef.value = window.setTimeout(() => {
			isFocus.value = false;
			stopMeasure();
			props?.onBlur?.(event);
		}, 0);
	};
	const onDropdownFocus = () => {
		onInternalFocus();
	};
	const onDropdownBlur = () => {
		onInternalBlur();
	};
	const onInternalPopupScroll = (event) => {
		props?.onPopupScroll?.(event);
	};
	return () => {
		const { classNames: mentionClassNames, styles, rows = 1, prefixCls, notFoundContent } = props;
		const restProps = omit(props, omitKeys$1);
		const { className, restAttrs, style } = getAttrStyleAndClass(attrs);
		const resizeStyle = styles?.textarea?.resize ?? style?.resize;
		const mergedTextareaStyle = { ...styles?.textarea };
		if (resizeStyle !== void 0) mergedTextareaStyle.resize = resizeStyle;
		const mergedStyles = {
			...styles,
			textarea: mergedTextareaStyle
		};
		const mentionNode = createVNode(Fragment, null, [createVNode(src_default$3, mergeProps({
			"classNames": { textarea: mentionClassNames?.textarea },
			"styles": mergedStyles,
			"ref": textareaRef,
			"value": mergedValue.value
		}, restAttrs, restProps, { rows }, {
			"onChange": onInternalChange,
			"onKeydown": onInternalKeyDown,
			"onKeyup": onInternalKeyUp,
			"onPressEnter": onInternalPressEnter,
			"onFocus": onInternalFocus,
			"onBlur": onInternalBlur
		}), null), mergedMeasuring.value && createVNode("div", {
			"ref": measureRef,
			"class": `${prefixCls}-measure`
		}, [
			mergedValue.value.slice(0, mergedMeasureLocation.value),
			createVNode(MentionsProvider, { "value": {
				notFoundContent,
				activeIndex: activeIndex.value,
				setActiveIndex,
				selectOption,
				onFocus: onDropdownFocus,
				onBlur: onDropdownBlur,
				onScroll: onInternalPopupScroll
			} }, { default: () => [createVNode(KeywordTrigger_default, {
				"prefixCls": prefixCls,
				"transitionName": props.transitionName,
				"placement": props.placement,
				"direction": props.direction,
				"options": mergedOptions.value,
				"visible": true,
				"getPopupContainer": props.getPopupContainer,
				"popupClassName": clsx(props.popupClassName, mentionClassNames?.popup),
				"popupStyle": styles?.popup
			}, { default: () => [createVNode("span", null, [mergedMeasurePrefix.value])] })] }),
			mergedValue.value.slice(mergedMeasureLocation.value + mergedMeasurePrefix.value.length)
		])]);
		if (!props.hasWrapper) return createVNode("div", {
			"class": clsx(prefixCls, props.className, className),
			"style": style,
			"ref": containerRef
		}, [mentionNode]);
		return mentionNode;
	};
}, {
	props: /* @__PURE__ */ mergeDefaults({
		hasWrapper: {
			type: Boolean,
			required: true,
			default: void 0
		},
		id: {
			type: String,
			required: false,
			default: void 0
		},
		autoFocus: {
			type: Boolean,
			required: false,
			default: void 0
		},
		className: {
			type: String,
			required: false,
			default: void 0
		},
		defaultValue: {
			type: String,
			required: false,
			default: void 0
		},
		notFoundContent: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		split: {
			type: String,
			required: false,
			default: void 0
		},
		transitionName: {
			type: String,
			required: false,
			default: void 0
		},
		placement: {
			type: String,
			required: false,
			default: void 0
		},
		direction: {
			type: String,
			required: false,
			default: void 0
		},
		prefix: {
			type: [String, Array],
			required: false,
			default: void 0
		},
		prefixCls: {
			type: String,
			required: false,
			default: void 0
		},
		value: {
			type: String,
			required: false,
			default: void 0
		},
		silent: {
			type: Boolean,
			required: false,
			default: void 0
		},
		filterOption: {
			type: Boolean,
			required: false,
			skipCheck: true,
			default: void 0
		},
		validateSearch: {
			required: false,
			default: void 0
		},
		onChange: {
			type: Function,
			required: false,
			default: void 0
		},
		onSelect: {
			type: Function,
			required: false,
			default: void 0
		},
		onSearch: {
			type: Function,
			required: false,
			default: void 0
		},
		onFocus: {
			type: Function,
			required: false,
			default: void 0
		},
		onBlur: {
			type: Function,
			required: false,
			default: void 0
		},
		getPopupContainer: {
			type: Function,
			required: false,
			default: void 0
		},
		popupClassName: {
			type: String,
			required: false,
			default: void 0
		},
		options: {
			type: Array,
			required: false,
			default: void 0
		},
		classNames: {
			type: Object,
			required: false,
			default: void 0
		},
		styles: {
			type: Object,
			required: false,
			default: void 0
		},
		onPopupScroll: {
			type: Function,
			required: false,
			default: void 0
		},
		rows: {
			required: false,
			default: void 0
		},
		disabled: {
			type: Boolean,
			required: false,
			default: void 0
		},
		autoSize: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		},
		onPressEnter: {
			type: Function,
			required: false,
			default: void 0
		},
		onResize: {
			type: Function,
			required: false,
			default: void 0
		},
		allowClear: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		},
		suffix: {
			required: false,
			default: void 0
		},
		count: {
			required: false,
			default: void 0
		},
		onClear: {
			type: Function,
			required: false,
			default: void 0
		},
		maxLength: {
			type: Number,
			required: false,
			default: void 0
		},
		hidden: {
			type: Boolean,
			required: false,
			default: void 0
		},
		readOnly: {
			type: Boolean,
			required: false,
			default: void 0
		},
		placeholder: {
			type: String,
			required: false,
			default: void 0
		},
		onKeydown: {
			type: Function,
			required: false,
			default: void 0
		},
		onKeyup: {
			type: Function,
			required: false,
			default: void 0
		}
	}, {
		prefix: "@",
		prefixCls: "vc-mentions",
		split: " ",
		notFoundContent: "Not Found",
		validateSearch,
		filterOption,
		rows: 1
	}),
	name: "VMentions",
	inheritAttrs: false
});
var Mentions_default = /* @__PURE__ */ defineComponent((props, { expose, attrs }) => {
	const hasSuffix = computed(() => !!(props.suffix || props.allowClear));
	const holderRef = shallowRef();
	const mentionRef = shallowRef();
	const mergedValue = shallowRef(props?.value ?? props?.defaultValue ?? "");
	watch(() => props.value, () => {
		mergedValue.value = props.value ?? "";
	});
	const setMergedValue = (value) => {
		mergedValue.value = value;
	};
	const triggerChange = (nextValue) => {
		setMergedValue(nextValue);
		props?.onChange?.(nextValue);
	};
	const handleReset = () => {
		triggerChange("");
	};
	expose({
		focus: () => mentionRef.value?.focus?.(),
		blur: () => mentionRef.value?.blur?.(),
		textarea: computed(() => mentionRef.value?.textarea || null),
		nativeElement: computed(() => holderRef.value?.nativeElement || mentionRef.value?.nativeElement)
	});
	return () => {
		const { suffix, prefixCls = "vc-mentions", allowClear, classNames: mentionsClassNames, styles, className: propsClassName, disabled, onClear, id, value: _value, defaultValue: _defaultValue, onChange: _onChange, ...rest } = props;
		const { className, style } = getAttrStyleAndClass(attrs);
		const internalClassName = clsx(mentionsClassNames?.mentions, propsClassName);
		const internalProps = {
			...attrs,
			...rest,
			id,
			value: mergedValue.value,
			prefixCls,
			className: internalClassName,
			classNames: mentionsClassNames,
			styles,
			disabled,
			hasWrapper: hasSuffix.value,
			onChange: triggerChange
		};
		return createVNode(BaseInput, {
			"ref": holderRef,
			"suffix": suffix,
			"prefixCls": prefixCls,
			"value": mergedValue.value,
			"allowClear": allowClear,
			"handleReset": handleReset,
			"class": clsx(prefixCls, propsClassName, className, { [`${prefixCls}-has-suffix`]: hasSuffix.value }),
			"style": style,
			"classNames": mentionsClassNames,
			"styles": styles,
			"disabled": disabled,
			"onClear": onClear
		}, { default: () => [createVNode(InternalMentions, mergeProps({ "ref": mentionRef }, internalProps), null)] });
	};
}, {
	props: {
		id: {
			type: String,
			required: false,
			default: void 0
		},
		autoFocus: {
			type: Boolean,
			required: false,
			default: void 0
		},
		className: {
			type: String,
			required: false,
			default: void 0
		},
		defaultValue: {
			type: String,
			required: false,
			default: void 0
		},
		notFoundContent: {
			type: [
				Object,
				Function,
				String,
				Number,
				null,
				Boolean,
				Array
			],
			required: false,
			default: void 0
		},
		split: {
			type: String,
			required: false,
			default: void 0
		},
		transitionName: {
			type: String,
			required: false,
			default: void 0
		},
		placement: {
			type: String,
			required: false,
			default: void 0
		},
		direction: {
			type: String,
			required: false,
			default: void 0
		},
		prefix: {
			type: [String, Array],
			required: false,
			default: void 0
		},
		prefixCls: {
			type: String,
			required: false,
			default: void 0
		},
		value: {
			type: String,
			required: false,
			default: void 0
		},
		silent: {
			type: Boolean,
			required: false,
			default: void 0
		},
		filterOption: {
			type: Boolean,
			required: false,
			skipCheck: true,
			default: void 0
		},
		validateSearch: {
			required: false,
			default: void 0
		},
		onChange: {
			type: Function,
			required: false,
			default: void 0
		},
		onSelect: {
			type: Function,
			required: false,
			default: void 0
		},
		onSearch: {
			type: Function,
			required: false,
			default: void 0
		},
		onFocus: {
			type: Function,
			required: false,
			default: void 0
		},
		onBlur: {
			type: Function,
			required: false,
			default: void 0
		},
		getPopupContainer: {
			type: Function,
			required: false,
			default: void 0
		},
		popupClassName: {
			type: String,
			required: false,
			default: void 0
		},
		options: {
			type: Array,
			required: false,
			default: void 0
		},
		classNames: {
			type: Object,
			required: false,
			default: void 0
		},
		styles: {
			type: Object,
			required: false,
			default: void 0
		},
		onPopupScroll: {
			type: Function,
			required: false,
			default: void 0
		},
		rows: {
			required: false,
			default: void 0
		},
		disabled: {
			type: Boolean,
			required: false,
			default: void 0
		},
		autoSize: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		},
		onPressEnter: {
			type: Function,
			required: false,
			default: void 0
		},
		onResize: {
			type: Function,
			required: false,
			default: void 0
		},
		allowClear: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		},
		suffix: {
			required: false,
			default: void 0
		},
		count: {
			required: false,
			default: void 0
		},
		onClear: {
			type: Function,
			required: false,
			default: void 0
		},
		maxLength: {
			type: Number,
			required: false,
			default: void 0
		},
		hidden: {
			type: Boolean,
			required: false,
			default: void 0
		},
		readOnly: {
			type: Boolean,
			required: false,
			default: void 0
		},
		placeholder: {
			type: String,
			required: false,
			default: void 0
		},
		onKeydown: {
			type: Function,
			required: false,
			default: void 0
		},
		onKeyup: {
			type: Function,
			required: false,
			default: void 0
		}
	},
	name: "Mentions",
	inheritAttrs: false
});
//#endregion
//#region ../../node_modules/.pnpm/@v-c+mentions@1.0.0_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/mentions/dist/Option.js
var Option = () => null;
var Option_default = Option;
//#endregion
//#region ../../node_modules/.pnpm/@v-c+mentions@1.0.0_vue@3.5.33_typescript@5.9.3_/node_modules/@v-c/mentions/dist/index.js
var src_default = Mentions_default;
//#endregion
//#region ../../node_modules/.pnpm/antdv-next@1.2.2_vue@3.5.33_typescript@5.9.3_/node_modules/antdv-next/dist/mentions/index.js
function _isSlot(s) {
	return typeof s === "function" || Object.prototype.toString.call(s) === "[object Object]" && !isVNode(s);
}
function loadingFilterOption() {
	return true;
}
var omitKeys = [
	"prefixCls",
	"classes",
	"styles",
	"rootClass",
	"size",
	"status",
	"loading",
	"labelRender",
	"contentRender",
	"variant",
	"allowClear",
	"filterOption",
	"popupClassName",
	"options",
	"notFoundContent"
];
var Mentions = /* @__PURE__ */ defineComponent((props, { slots, emit, expose, attrs }) => {
	devUseWarning("Mentions").deprecated(!slots.default, "Mentions.Option", "options");
	const { prefixCls, direction, class: contextClassName, style: contextStyle, classes: contextClassNames, styles: contextStyles, renderEmpty, getPopupContainer } = useComponentBaseConfig("mentions", props);
	const { classes, styles, rootClass, size: customSize, disabled: customDisabled, status: customStatus, variant: customVariant } = toPropsRefs$1(props, "classes", "styles", "rootClass", "size", "disabled", "status", "variant");
	const rootCls = useCSSVarCls_default(prefixCls);
	const [hashId, cssVarCls] = style_default(prefixCls, rootCls);
	const mergedSize = useSize(customSize);
	const disabledContext = useDisabledContext();
	const mergedDisabled = computed(() => customDisabled.value ?? disabledContext.value);
	const formItemInputContext = useFormItemInputContext();
	const hasFeedback = computed(() => formItemInputContext.value.hasFeedback);
	const feedbackIcon = computed(() => formItemInputContext.value.feedbackIcon);
	const mergedStatus = computed(() => getMergedStatus(formItemInputContext.value.status, customStatus.value));
	const mergedProps = computed(() => {
		return {
			...props,
			disabled: mergedDisabled.value,
			status: mergedStatus.value,
			loading: props.loading,
			options: props.options,
			variant: customVariant.value
		};
	});
	const [mergedClassNames, mergedStyles] = useMergeSemantic(useToArr(contextClassNames, classes), useToArr(contextStyles, styles), useToProps(mergedProps));
	const mergedAllowClear = computed(() => getAllowClear_default(props.allowClear));
	const [mergedVariant, enableVariantCls] = useVariant("mentions", customVariant);
	const focused = shallowRef(false);
	const setFocusState = (value) => {
		focused.value = value;
	};
	const handleFocus = (event) => {
		setFocusState(true);
		emit("focus", event);
	};
	const handleBlur = (event) => {
		setFocusState(false);
		emit("blur", event);
	};
	const handleChange = (value) => {
		emit("update:value", value);
		emit("change", value);
	};
	const handleSelect = (option, prefix) => {
		emit("select", option, prefix);
	};
	const handleSearch = (text, prefix) => {
		emit("search", text, prefix);
	};
	const handlePopupScroll = (event) => {
		emit("popupScroll", event);
	};
	const notFoundContent = computed(() => {
		if (props.notFoundContent !== void 0) return props.notFoundContent;
		return renderEmpty?.value?.("Select") || createVNode(DefaultRenderEmpty, { "componentName": "Select" }, null);
	});
	const mergedFilterOption = computed(() => props.loading ? loadingFilterOption : props.filterOption);
	const resolveRenderNode = (key, ctx) => {
		const node = getSlotPropsFnRun(slots, props, key, false, ctx);
		return node === void 0 ? void 0 : node;
	};
	const mergedOptions = computed(() => {
		if (props.loading) return [{
			value: "ANTD_SEARCHING",
			disabled: true,
			label: createVNode(spin_default, { "size": "small" }, null)
		}];
		if (!props.options) return;
		return props.options.map((option, index) => {
			const mergedLabel = resolveRenderNode("labelRender", {
				option,
				index
			}) ?? option.label ?? option.value;
			return {
				...option,
				label: mergedLabel
			};
		});
	});
	const mentionsRef = shallowRef();
	expose({
		focus: () => mentionsRef.value?.focus?.(),
		blur: () => mentionsRef.value?.blur?.(),
		textarea: computed(() => mentionsRef.value?.textarea || null),
		nativeElement: computed(() => mentionsRef.value?.nativeElement)
	});
	return () => {
		const { className, style, restAttrs } = getAttrStyleAndClass$1(attrs);
		const mergedClassName = clsx({
			[`${prefixCls.value}-sm`]: mergedSize.value === "small",
			[`${prefixCls.value}-lg`]: mergedSize.value === "large"
		}, contextClassName.value, mergedClassNames.value.root, rootClass.value, cssVarCls.value, rootCls.value, hashId.value, className);
		const rootStyle = {
			...mergedStyles.value.root,
			...contextStyle.value,
			...style
		};
		const suffixNodes = filterEmpty(slots.suffix?.() ?? []).filter((node) => node != null);
		if (hasFeedback.value && feedbackIcon.value) suffixNodes.push(feedbackIcon.value);
		const mergedSuffix = suffixNodes.length ? suffixNodes : void 0;
		const mentionOptions = props.loading ? createVNode(Option_default, {
			"value": "ANTD_SEARCHING",
			"disabled": true
		}, { default: () => [createVNode(spin_default, { "size": "small" }, null)] }) : props.options !== void 0 ? null : filterEmpty(slots.default?.() ?? []).filter(Boolean);
		const mergedPopupClassName = clsx(mergedClassNames.value.popup, rootClass.value, hashId.value, cssVarCls.value, rootCls.value);
		const classNames = {
			mentions: clsx({
				[`${prefixCls.value}-disabled`]: mergedDisabled.value,
				[`${prefixCls.value}-focused`]: focused.value,
				[`${prefixCls.value}-rtl`]: direction.value === "rtl"
			}, hashId.value),
			textarea: clsx(mergedClassNames.value.textarea),
			popup: mergedPopupClassName,
			suffix: mergedClassNames.value.suffix,
			variant: clsx({ [`${prefixCls.value}-${mergedVariant.value}`]: enableVariantCls.value }, getStatusClassNames(prefixCls.value, mergedStatus.value)),
			affixWrapper: hashId.value
		};
		const mergedStylesValue = {
			textarea: mergedStyles.value.textarea,
			popup: mergedStyles.value.popup,
			suffix: mergedStyles.value.suffix
		};
		const restProps = omit$1(props, omitKeys);
		return createVNode(src_default, mergeProps({ "ref": mentionsRef }, restAttrs, restProps, {
			"prefixCls": prefixCls.value,
			"className": mergedClassName,
			"style": rootStyle,
			"classNames": classNames,
			"styles": mergedStylesValue,
			"popupClassName": props.popupClassName,
			"notFoundContent": notFoundContent.value,
			"disabled": mergedDisabled.value,
			"allowClear": mergedAllowClear.value,
			"silent": props.loading,
			"filterOption": mergedFilterOption.value,
			"options": mergedOptions.value,
			"suffix": mergedSuffix,
			"direction": direction.value,
			"getPopupContainer": props.getPopupContainer || getPopupContainer,
			"onChange": handleChange,
			"onSelect": handleSelect,
			"onSearch": handleSearch,
			"onPopupScroll": handlePopupScroll,
			"onFocus": handleFocus,
			"onBlur": handleBlur
		}), _isSlot(mentionOptions) ? mentionOptions : { default: () => [mentionOptions] });
	};
}, {
	props: {
		loading: {
			type: Boolean,
			required: false,
			default: void 0
		},
		status: {
			type: String,
			required: false
		},
		options: {
			type: Array,
			required: false
		},
		popupClassName: {
			type: String,
			required: false
		},
		variant: {
			type: String,
			required: false
		},
		classes: {
			type: [Object, Function],
			required: false
		},
		styles: {
			type: [Object, Function],
			required: false
		},
		size: {
			type: [String, null],
			required: false
		},
		labelRender: {
			type: Function,
			required: false
		},
		allowClear: {
			type: [Boolean, Object],
			required: false,
			default: void 0
		},
		disabled: {
			type: Boolean,
			required: false,
			default: void 0
		},
		id: {
			type: String,
			required: false
		},
		autoFocus: {
			type: Boolean,
			required: false,
			default: void 0
		},
		defaultValue: {
			type: String,
			required: false
		},
		notFoundContent: { required: false },
		split: {
			type: String,
			required: false
		},
		transitionName: {
			type: String,
			required: false
		},
		placement: {
			type: String,
			required: false
		},
		direction: {
			type: String,
			required: false
		},
		prefix: {
			type: [String, Array],
			required: false
		},
		prefixCls: {
			type: String,
			required: false
		},
		value: {
			type: String,
			required: false
		},
		silent: {
			type: Boolean,
			required: false,
			default: void 0
		},
		filterOption: {
			type: Boolean,
			required: false,
			skipCheck: true,
			default: void 0
		},
		validateSearch: { required: false },
		getPopupContainer: {
			type: Function,
			required: false
		},
		rows: { required: false },
		rootClass: {
			type: String,
			required: false
		}
	},
	emits: [
		"focus",
		"blur",
		"change",
		"select",
		"popupScroll",
		"search",
		"update:value"
	],
	name: "AMentions",
	inheritAttrs: false
});
Mentions.Option = Option_default;
Mentions.install = (app) => {
	app.component(Mentions.name, Mentions);
	app.component("AMentionsOption", Option_default);
};
Mentions.getMentions = (value = "", config = {}) => {
	const { prefix = "@", split = " " } = config;
	const prefixList = toList_default(prefix);
	return value.split(split).map((str = "") => {
		let hitPrefix = null;
		prefixList.some((prefixStr) => {
			if (str.slice(0, prefixStr.length) === prefixStr) {
				hitPrefix = prefixStr;
				return true;
			}
			return false;
		});
		if (hitPrefix !== null) return {
			prefix: hitPrefix,
			value: str.slice(hitPrefix.length)
		};
		return null;
	}).filter((entity) => !!entity && !!entity.value);
};
Mentions._InternalPanelDoNotUseOrYouWillBeFired = PurePanel_default(Mentions, void 0, void 0, "mentions");
var mentions_default = Mentions;
//#endregion
export { ExportMenu as a, SubMenu as c, useFullPath as d, spin_default as i, MenuItem as l, Option_default as n, isEqual_default as o, toList_default as r, MenuItemGroup as s, mentions_default as t, Divider as u };

//# sourceMappingURL=mentions-BFmPSlC7.js.map